<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>第18讲 多核与多处理器II</title>
      <link href="/2026/06/11/%E7%AC%AC18%E8%AE%B2-%E5%A4%9A%E6%A0%B8%E4%B8%8E%E5%A4%9A%E5%A4%84%E7%90%86%E5%99%A8II/"/>
      <url>/2026/06/11/%E7%AC%AC18%E8%AE%B2-%E5%A4%9A%E6%A0%B8%E4%B8%8E%E5%A4%9A%E5%A4%84%E7%90%86%E5%99%A8II/</url>
      
        <content type="html"><![CDATA[<p>以下根据附件《第18讲 多核与多处理器II.md》整理生成。</p><h1 id="第18讲：多核与多处理器-II-学习指南"><a href="#第18讲：多核与多处理器-II-学习指南" class="headerlink" title="第18讲：多核与多处理器 II 学习指南"></a>第18讲：多核与多处理器 II 学习指南</h1><p>本讲承接上一讲“多核不是免费加速”的主线，进一步讨论两个问题：</p><ol><li><strong>正确性问题</strong>：程序员写下来的代码顺序，为什么不一定等于其他 CPU 核心观察到的顺序？</li><li><strong>性能问题</strong>：即使用了 MCS 锁这类可扩展锁，为什么在多核、NUMA 环境下仍然可能性能很差？</li></ol><hr><h2 id="❗-本节核心结论：多核程序不能只按“代码书写顺序”理解共享内存访问，而必须考虑内存一致性模型。同步原语不仅提供互斥，也提供访存顺序保证；而在高核数和-NUMA-环境下，锁本身可扩展不代表临界区数据访问也可扩展。"><a href="#❗-本节核心结论：多核程序不能只按“代码书写顺序”理解共享内存访问，而必须考虑内存一致性模型。同步原语不仅提供互斥，也提供访存顺序保证；而在高核数和-NUMA-环境下，锁本身可扩展不代表临界区数据访问也可扩展。" class="headerlink" title="❗ 本节核心结论：多核程序不能只按“代码书写顺序”理解共享内存访问，而必须考虑内存一致性模型。同步原语不仅提供互斥，也提供访存顺序保证；而在高核数和 NUMA 环境下，锁本身可扩展不代表临界区数据访问也可扩展。"></a><strong>❗ 本节核心结论：</strong><br>多核程序不能只按“代码书写顺序”理解共享内存访问，而必须考虑内存一致性模型。同步原语不仅提供互斥，也提供访存顺序保证；而在高核数和 NUMA 环境下，锁本身可扩展不代表临界区数据访问也可扩展。</h2><h2 id="1-本章知识地图"><a href="#1-本章知识地图" class="headerlink" title="1. 本章知识地图"></a>1. 本章知识地图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[多核共享内存程序] --&gt; B[代码顺序不等于硬件可见顺序]    B --&gt; C[乱序执行]    C --&gt; D[内存一致性模型]    D --&gt; E[严格一致性]    D --&gt; F[顺序一致性]    D --&gt; G[TSO]    D --&gt; H[弱序一致性]    H --&gt; I[barrier &#x2F; fence]    I --&gt; J[同步原语封装顺序语义]    J --&gt; K[锁保证正确性]    K --&gt; L[锁可扩展性问题]    L --&gt; M[MCS 锁]    M --&gt; N[锁元数据竞争减少]    M --&gt; O[临界区共享数据仍会迁移]    O --&gt; P[NUMA]    P --&gt; Q[本地内存 &#x2F; 远程内存]    Q --&gt; R[NUMA 感知设计]    R --&gt; S[cohort 锁]  </pre></div><p>这张图的核心是：</p><blockquote><p>第17讲主要讲“缓存一致性和锁竞争”，第18讲进一步讲“内存操作顺序”和“NUMA 下的局部性”。</p></blockquote><hr><h2 id="2-为什么-LockOne-Peterson-要重新审视？"><a href="#2-为什么-LockOne-Peterson-要重新审视？" class="headerlink" title="2. 为什么 LockOne &#x2F; Peterson 要重新审视？"></a>2. 为什么 LockOne &#x2F; Peterson 要重新审视？</h2><h3 id="2-1-LockOne-的基本想法"><a href="#2-1-LockOne-的基本想法" class="headerlink" title="2.1 LockOne 的基本想法"></a>2.1 LockOne 的基本想法</h3><p>LockOne 是 Peterson 算法的前身，思想很直观：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">flag[<span class="number">0</span>] = TRUE;</span><br><span class="line"><span class="keyword">while</span> (flag[<span class="number">1</span>] == TRUE)</span><br><span class="line">    ;</span><br><span class="line"><span class="comment">/* critical section */</span></span><br><span class="line">flag[<span class="number">0</span>] = FALSE;</span><br></pre></td></tr></table></figure><p>意思是：</p><ol><li>我先声明自己想进入临界区。</li><li>然后检查对方是否也想进入。</li><li>如果对方不想进入，我就进入。</li></ol><hr><h3 id="2-2-它隐含了什么假设？"><a href="#2-2-它隐含了什么假设？" class="headerlink" title="2.2 它隐含了什么假设？"></a>2.2 它隐含了什么假设？</h3><p>这段代码隐含了一个很强的假设：</p><blockquote><p>我写在前面的语句，会先执行，并且会先被其他核心看到。</p></blockquote><p>也就是程序员默认：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">flag[0] = TRUE</span><br><span class="line">一定早于</span><br><span class="line">读取 flag[1]</span><br></pre></td></tr></table></figure><p>但现代处理器为了性能，可能会让某些访存操作乱序执行。</p><hr><h3 id="2-3-可能怎么出问题？"><a href="#2-3-可能怎么出问题？" class="headerlink" title="2.3 可能怎么出问题？"></a>2.3 可能怎么出问题？</h3><p>程序员写的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">flag[<span class="number">0</span>] = TRUE;</span><br><span class="line"><span class="keyword">while</span> (flag[<span class="number">1</span>] == TRUE)</span><br><span class="line">    ;</span><br></pre></td></tr></table></figure><p>但硬件可能让“读取 <code>flag[1]</code>”先于“写入 <code>flag[0]</code>”对外发生。</p><p>于是两个线程都可能先读到对方的 <code>flag</code> 还是 <code>FALSE</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CPU0 读到 flag[1] == FALSE</span><br><span class="line">CPU1 读到 flag[0] == FALSE</span><br></pre></td></tr></table></figure><p>结果两个线程都认为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">对方不想进临界区，所以我可以进。</span><br></pre></td></tr></table></figure><p>于是同时进入临界区，互斥失败。</p><hr><h2 id="❗-本节核心结论：LockOne-这类算法的问题不是简单“代码写错了”，而是它依赖了较强的访存顺序假设。现代硬件为了性能可能不会默认提供这种顺序。"><a href="#❗-本节核心结论：LockOne-这类算法的问题不是简单“代码写错了”，而是它依赖了较强的访存顺序假设。现代硬件为了性能可能不会默认提供这种顺序。" class="headerlink" title="❗ 本节核心结论：LockOne 这类算法的问题不是简单“代码写错了”，而是它依赖了较强的访存顺序假设。现代硬件为了性能可能不会默认提供这种顺序。"></a><strong>❗ 本节核心结论：</strong><br>LockOne 这类算法的问题不是简单“代码写错了”，而是它依赖了较强的访存顺序假设。现代硬件为了性能可能不会默认提供这种顺序。</h2><h2 id="3-内存一致性模型"><a href="#3-内存一致性模型" class="headerlink" title="3. 内存一致性模型"></a>3. 内存一致性模型</h2><h3 id="3-1-它是什么？"><a href="#3-1-它是什么？" class="headerlink" title="3.1 它是什么？"></a>3.1 它是什么？</h3><p>内存一致性模型 Memory Consistency Model 是硬件和软件之间的约定：</p><blockquote><p>多个 CPU 核心同时读写共享内存时，哪些读写顺序必须被保证，哪些顺序可以被硬件重排。</p></blockquote><p>它关注的不是单个地址最终值是否一致，而是：</p><blockquote><p>多个地址上的读写操作，在不同 CPU 看来顺序是否合法。</p></blockquote><hr><h2 id="4-严格一致性-Strict-Consistency"><a href="#4-严格一致性-Strict-Consistency" class="headerlink" title="4. 严格一致性 Strict Consistency"></a>4. 严格一致性 Strict Consistency</h2><h3 id="4-1-定义"><a href="#4-1-定义" class="headerlink" title="4.1 定义"></a>4.1 定义</h3><p>严格一致性要求：</p><blockquote><p>任意读操作都必须读到全局时间上最近一次写入的值。</p></blockquote><p>它像是系统中有一个绝对全局时钟：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">谁先发生，所有 CPU 都必须按这个顺序看到。</span><br></pre></td></tr></table></figure><hr><h3 id="4-2-为什么它符合直觉？"><a href="#4-2-为什么它符合直觉？" class="headerlink" title="4.2 为什么它符合直觉？"></a>4.2 为什么它符合直觉？</h3><p>因为它最接近程序员的朴素理解：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">data = <span class="number">666</span>;</span><br><span class="line">flag = READY;</span><br></pre></td></tr></table></figure><p>程序员自然认为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">别人看到 flag = READY 时，就应该已经能看到 data = 666。</span><br></pre></td></tr></table></figure><p>严格一致性会保证这种直觉。</p><hr><h3 id="4-3-为什么现实中很难高效实现？"><a href="#4-3-为什么现实中很难高效实现？" class="headerlink" title="4.3 为什么现实中很难高效实现？"></a>4.3 为什么现实中很难高效实现？</h3><p>因为现代处理器有：</p><ul><li>私有缓存</li><li>写缓冲区</li><li>乱序执行</li><li>多级缓存</li><li>跨核心互联</li><li>NUMA 远程访问</li></ul><p>如果每次读写都必须全局同步，CPU 流水线会被严重阻塞，性能代价极高。</p><hr><h2 id="❗-本节核心结论：严格一致性最符合直觉，但实现成本太高，所以现实硬件通常不会提供这么强的默认保证。"><a href="#❗-本节核心结论：严格一致性最符合直觉，但实现成本太高，所以现实硬件通常不会提供这么强的默认保证。" class="headerlink" title="❗ 本节核心结论：严格一致性最符合直觉，但实现成本太高，所以现实硬件通常不会提供这么强的默认保证。"></a><strong>❗ 本节核心结论：</strong><br>严格一致性最符合直觉，但实现成本太高，所以现实硬件通常不会提供这么强的默认保证。</h2><h2 id="5-顺序一致性-Sequential-Consistency"><a href="#5-顺序一致性-Sequential-Consistency" class="headerlink" title="5. 顺序一致性 Sequential Consistency"></a>5. 顺序一致性 Sequential Consistency</h2><h3 id="5-1-定义"><a href="#5-1-定义" class="headerlink" title="5.1 定义"></a>5.1 定义</h3><p>顺序一致性比严格一致性弱一些。</p><p>它不要求操作按照真实时间顺序对所有核心可见，但要求：</p><ol><li>所有操作的结果必须等价于某个全局顺序。</li><li>在这个全局顺序中，每个 CPU 自己的程序顺序不能被打破。</li></ol><p>可以理解为：</p><blockquote><p>不要求全局真实时间一致，但要求能编出一个合法的全局执行顺序。</p></blockquote><hr><h3 id="5-2-经典例子"><a href="#5-2-经典例子" class="headerlink" title="5.2 经典例子"></a>5.2 经典例子</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// CPU0</span></span><br><span class="line">flag[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">A = flag[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// CPU1</span></span><br><span class="line">flag[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">B = flag[<span class="number">0</span>];</span><br></pre></td></tr></table></figure><p>在顺序一致性下，允许出现：</p><table><thead><tr><th>A</th><th>B</th><th>是否可能</th></tr></thead><tbody><tr><td>0</td><td>1</td><td>可能</td></tr><tr><td>1</td><td>0</td><td>可能</td></tr><tr><td>1</td><td>1</td><td>可能</td></tr><tr><td>0</td><td>0</td><td>不可能</td></tr></tbody></table><p>为什么 <code>(A, B) = (0, 0)</code> 不可能？</p><p>因为如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A = 0</span><br></pre></td></tr></table></figure><p>说明 CPU0 读 <code>flag[1]</code> 时，CPU1 的写 <code>flag[1] = 1</code> 还没发生。</p><p>如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">B = 0</span><br></pre></td></tr></table></figure><p>说明 CPU1 读 <code>flag[0]</code> 时，CPU0 的写 <code>flag[0] = 1</code> 还没发生。</p><p>那就会要求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CPU0 的读 &lt; CPU1 的写</span><br><span class="line">CPU1 的读 &lt; CPU0 的写</span><br></pre></td></tr></table></figure><p>同时又必须保持程序顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CPU0 的写 &lt; CPU0 的读</span><br><span class="line">CPU1 的写 &lt; CPU1 的读</span><br></pre></td></tr></table></figure><p>这会形成无法成立的循环顺序。</p><hr><h2 id="❗-本节核心结论：顺序一致性不要求真实时间全局一致，但要求每个线程自己的程序顺序被保留，并且整体结果能对应某个合法全局顺序。"><a href="#❗-本节核心结论：顺序一致性不要求真实时间全局一致，但要求每个线程自己的程序顺序被保留，并且整体结果能对应某个合法全局顺序。" class="headerlink" title="❗ 本节核心结论：顺序一致性不要求真实时间全局一致，但要求每个线程自己的程序顺序被保留，并且整体结果能对应某个合法全局顺序。"></a><strong>❗ 本节核心结论：</strong><br>顺序一致性不要求真实时间全局一致，但要求每个线程自己的程序顺序被保留，并且整体结果能对应某个合法全局顺序。</h2><h2 id="6-TSO：Total-Store-Ordering"><a href="#6-TSO：Total-Store-Ordering" class="headerlink" title="6. TSO：Total Store Ordering"></a>6. TSO：Total Store Ordering</h2><h3 id="6-1-TSO-是什么？"><a href="#6-1-TSO-是什么？" class="headerlink" title="6.1 TSO 是什么？"></a>6.1 TSO 是什么？</h3><p>TSO 是一种比顺序一致性更弱、但比弱序模型更强的内存一致性模型。</p><p>它大致保证：</p><table><thead><tr><th>顺序类型</th><th>TSO 是否保证</th></tr></thead><tbody><tr><td>读 → 读</td><td>保证</td></tr><tr><td>读 → 写</td><td>保证</td></tr><tr><td>写 → 写</td><td>保证</td></tr><tr><td>写 → 读</td><td>不一定保证</td></tr></tbody></table><p>TSO 最关键的放松是：</p><blockquote><p>写之后的读，可能先于这个写对其他核心可见之前执行。</p></blockquote><hr><h3 id="6-2-为什么-TSO-下可能出现-A-B-0-0-？"><a href="#6-2-为什么-TSO-下可能出现-A-B-0-0-？" class="headerlink" title="6.2 为什么 TSO 下可能出现 (A, B) = (0, 0)？"></a>6.2 为什么 TSO 下可能出现 <code>(A, B) = (0, 0)</code>？</h3><p>还是这个例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// CPU0</span></span><br><span class="line">flag[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">A = flag[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// CPU1</span></span><br><span class="line">flag[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">B = flag[<span class="number">0</span>];</span><br></pre></td></tr></table></figure><p>在 TSO 下可能发生：</p><ol><li>CPU0 把 <code>flag[0] = 1</code> 放进本地写缓冲区。</li><li>CPU1 把 <code>flag[1] = 1</code> 放进本地写缓冲区。</li><li>这两个写还没有对对方可见。</li><li>CPU0 先执行读取 <code>flag[1]</code>，读到旧值 0。</li><li>CPU1 先执行读取 <code>flag[0]</code>，读到旧值 0。</li></ol><p>于是得到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A = 0, B = 0</span><br></pre></td></tr></table></figure><hr><h3 id="6-3-TSO-为什么要这样设计？"><a href="#6-3-TSO-为什么要这样设计？" class="headerlink" title="6.3 TSO 为什么要这样设计？"></a>6.3 TSO 为什么要这样设计？</h3><p>因为它允许写操作先进入写缓冲区，不必每次都等全系统可见后再继续执行后面的读。</p><p>这样可以：</p><ul><li>减少流水线阻塞。</li><li>提高处理器吞吐。</li><li>降低每次写共享变量的等待成本。</li></ul><p>但代价是：</p><blockquote><p>程序员不能再简单认为“代码中写在前面的 store，一定先被其他 CPU 看到”。</p></blockquote><hr><h2 id="❗-本节核心结论：TSO-保留了大部分访存顺序，但放松了“写-→-读”顺序。它通过写缓冲提高性能，但也让并发程序可能读到旧值。"><a href="#❗-本节核心结论：TSO-保留了大部分访存顺序，但放松了“写-→-读”顺序。它通过写缓冲提高性能，但也让并发程序可能读到旧值。" class="headerlink" title="❗ 本节核心结论：TSO 保留了大部分访存顺序，但放松了“写 → 读”顺序。它通过写缓冲提高性能，但也让并发程序可能读到旧值。"></a><strong>❗ 本节核心结论：</strong><br>TSO 保留了大部分访存顺序，但放松了“写 → 读”顺序。它通过写缓冲提高性能，但也让并发程序可能读到旧值。</h2><h2 id="7-弱序一致性-Weak-Ordering"><a href="#7-弱序一致性-Weak-Ordering" class="headerlink" title="7. 弱序一致性 Weak Ordering"></a>7. 弱序一致性 Weak Ordering</h2><h3 id="7-1-弱序模型是什么？"><a href="#7-1-弱序模型是什么？" class="headerlink" title="7.1 弱序模型是什么？"></a>7.1 弱序模型是什么？</h3><p>弱序一致性进一步放松访存顺序。</p><p>它通常不保证不同地址之间的普通读写顺序。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">data = <span class="number">666</span>;</span><br><span class="line">flag = READY;</span><br></pre></td></tr></table></figure><p>程序员想表达：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">先写 data，再设置 flag。</span><br></pre></td></tr></table></figure><p>但弱序模型下，其他核心可能先看到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">flag = READY</span><br></pre></td></tr></table></figure><p>然后去读 <code>data</code>，却读到旧值。</p><hr><h3 id="7-2-为什么硬件要采用弱序模型？"><a href="#7-2-为什么硬件要采用弱序模型？" class="headerlink" title="7.2 为什么硬件要采用弱序模型？"></a>7.2 为什么硬件要采用弱序模型？</h3><p>弱序模型可以：</p><ul><li>降低硬件复杂度。</li><li>降低功耗。</li><li>提高流水线和缓存系统自由度。</li><li>给处理器更多重排空间。</li></ul><p>但代价是：</p><blockquote><p>软件必须显式说明哪些地方需要保证顺序。</p></blockquote><p>也就是需要 barrier &#x2F; fence 或正确实现的同步原语。</p><hr><h2 id="8-barrier-fence：访存顺序边界"><a href="#8-barrier-fence：访存顺序边界" class="headerlink" title="8. barrier &#x2F; fence：访存顺序边界"></a>8. barrier &#x2F; fence：访存顺序边界</h2><h3 id="8-1-barrier-是什么？"><a href="#8-1-barrier-是什么？" class="headerlink" title="8.1 barrier 是什么？"></a>8.1 barrier 是什么？</h3><p>barrier，也叫 fence，是内存屏障。</p><p>它的作用是：</p><blockquote><p>屏障前面的访存操作不能越过屏障跑到后面；屏障后面的访存操作不能越过屏障跑到前面。</p></blockquote><p>可以把它理解成硬件访存顺序中的一道闸门。</p><hr><h3 id="8-2-data-flag-例子"><a href="#8-2-data-flag-例子" class="headerlink" title="8.2 data &#x2F; flag 例子"></a>8.2 data &#x2F; flag 例子</h3><p>错误写法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// sender</span></span><br><span class="line">data = <span class="number">666</span>;</span><br><span class="line">flag = READY;</span><br><span class="line"></span><br><span class="line"><span class="comment">// receiver</span></span><br><span class="line"><span class="keyword">while</span> (flag != READY)</span><br><span class="line">    ;</span><br><span class="line">handle(data);</span><br></pre></td></tr></table></figure><p>在弱序模型下，receiver 可能看到 <code>flag = READY</code>，但还没看到新的 <code>data</code>。</p><p>正确思路：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// sender</span></span><br><span class="line">data = <span class="number">666</span>;</span><br><span class="line">barrier();</span><br><span class="line">flag = READY;</span><br><span class="line"></span><br><span class="line"><span class="comment">// receiver</span></span><br><span class="line"><span class="keyword">while</span> (flag != READY)</span><br><span class="line">    ;</span><br><span class="line">barrier();</span><br><span class="line">handle(data);</span><br></pre></td></tr></table></figure><p>发送者侧 barrier 保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">data = 666</span><br><span class="line">先于</span><br><span class="line">flag = READY</span><br></pre></td></tr></table></figure><p>接收者侧 barrier 保证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">看到 flag = READY 后</span><br><span class="line">再读取 data</span><br></pre></td></tr></table></figure><hr><h3 id="8-3-barrier-为什么不能乱加？"><a href="#8-3-barrier-为什么不能乱加？" class="headerlink" title="8.3 barrier 为什么不能乱加？"></a>8.3 barrier 为什么不能乱加？</h3><p>barrier 会限制 CPU 的优化空间，可能导致：</p><ul><li>流水线停顿</li><li>写缓冲区刷新</li><li>乱序执行受限</li><li>缓存系统协调成本增加</li></ul><p>所以 barrier 不是越多越好。</p><p>正确做法通常是：</p><blockquote><p>使用同步原语，让同步原语内部用合适的 barrier 实现顺序保证。</p></blockquote><hr><h2 id="❗-本节核心结论：barrier-的本质是访存顺序边界。它可以保证关键读写顺序，但代价较高，因此应优先通过锁、原子操作等同步原语表达同步关系。"><a href="#❗-本节核心结论：barrier-的本质是访存顺序边界。它可以保证关键读写顺序，但代价较高，因此应优先通过锁、原子操作等同步原语表达同步关系。" class="headerlink" title="❗ 本节核心结论：barrier 的本质是访存顺序边界。它可以保证关键读写顺序，但代价较高，因此应优先通过锁、原子操作等同步原语表达同步关系。"></a><strong>❗ 本节核心结论：</strong><br>barrier 的本质是访存顺序边界。它可以保证关键读写顺序，但代价较高，因此应优先通过锁、原子操作等同步原语表达同步关系。</h2><h2 id="9-同步原语的访存顺序语义"><a href="#9-同步原语的访存顺序语义" class="headerlink" title="9. 同步原语的访存顺序语义"></a>9. 同步原语的访存顺序语义</h2><h3 id="9-1-锁不只是互斥"><a href="#9-1-锁不只是互斥" class="headerlink" title="9.1 锁不只是互斥"></a>9.1 锁不只是互斥</h3><p>很多人初学锁时只理解为：</p><blockquote><p>同一时刻只有一个线程能进入临界区。</p></blockquote><p>但在多核内存模型中，锁还有另一个关键作用：</p><blockquote><p>提供访存顺序保证。</p></blockquote><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mutex);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* critical section */</span></span><br><span class="line"></span><br><span class="line">unlock(&amp;mutex);</span><br></pre></td></tr></table></figure><p>通常意味着：</p><ul><li>临界区内的读写不能跑到 <code>lock</code> 之前。</li><li>临界区内的读写不能跑到 <code>unlock</code> 之后。</li><li>其他线程在获得同一把锁后，应该能看到前一个线程释放锁前的修改。</li></ul><hr><h3 id="9-2-acquire-release-语义"><a href="#9-2-acquire-release-语义" class="headerlink" title="9.2 acquire &#x2F; release 语义"></a>9.2 acquire &#x2F; release 语义</h3><p>锁常对应两类语义：</p><table><thead><tr><th>语义</th><th>含义</th></tr></thead><tbody><tr><td>acquire</td><td>获取锁之后，后续读写不能跑到获取锁之前</td></tr><tr><td>release</td><td>释放锁之前，临界区读写不能跑到释放锁之后</td></tr></tbody></table><p>也就是说：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mutex);    <span class="comment">// acquire</span></span><br><span class="line"><span class="comment">/* 共享数据访问 */</span></span><br><span class="line">unlock(&amp;mutex);  <span class="comment">// release</span></span><br></pre></td></tr></table></figure><p>同步原语内部会用：</p><ul><li>原子指令</li><li>内存屏障</li><li>架构相关的顺序约束</li></ul><p>来实现这些语义。</p><hr><h3 id="9-3-为什么要封装进同步原语？"><a href="#9-3-为什么要封装进同步原语？" class="headerlink" title="9.3 为什么要封装进同步原语？"></a>9.3 为什么要封装进同步原语？</h3><p>因为普通程序员不应该每次都手写：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">barrier();</span><br></pre></td></tr></table></figure><p>更合理的是：</p><ul><li>系统软件开发者正确实现 <code>lock/unlock</code>。</li><li>应用程序员按规范使用同步原语。</li><li>内存模型复杂性被封装起来。</li></ul><hr><h2 id="❗-本节核心结论：同步原语不仅解决“谁能进入临界区”，还解决“临界区内的内存操作如何对其他核心可见”的问题。"><a href="#❗-本节核心结论：同步原语不仅解决“谁能进入临界区”，还解决“临界区内的内存操作如何对其他核心可见”的问题。" class="headerlink" title="❗ 本节核心结论：同步原语不仅解决“谁能进入临界区”，还解决“临界区内的内存操作如何对其他核心可见”的问题。"></a><strong>❗ 本节核心结论：</strong><br>同步原语不仅解决“谁能进入临界区”，还解决“临界区内的内存操作如何对其他核心可见”的问题。</h2><h2 id="10-MCS-锁为什么还不够？"><a href="#10-MCS-锁为什么还不够？" class="headerlink" title="10. MCS 锁为什么还不够？"></a>10. MCS 锁为什么还不够？</h2><h3 id="10-1-MCS-锁优化了什么？"><a href="#10-1-MCS-锁优化了什么？" class="headerlink" title="10.1 MCS 锁优化了什么？"></a>10.1 MCS 锁优化了什么？</h3><p>MCS 锁的核心思想是：</p><blockquote><p>每个等待线程在自己的节点上自旋，而不是所有线程都反复读写同一个锁变量。</p></blockquote><p>它优化的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">锁元数据的缓存行竞争</span><br></pre></td></tr></table></figure><p>也就是减少所有核心对同一个锁变量缓存行的争抢。</p><hr><h3 id="10-2-它没有优化什么？"><a href="#10-2-它没有优化什么？" class="headerlink" title="10.2 它没有优化什么？"></a>10.2 它没有优化什么？</h3><p>MCS 锁不能消除临界区内部共享数据的缓存行迁移。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mcs_lock);</span><br><span class="line"></span><br><span class="line">shared_counter++;</span><br><span class="line">shared_array[i] = ...;</span><br><span class="line">shared_state = ...;</span><br><span class="line"></span><br><span class="line">unlock(&amp;mcs_lock);</span><br></pre></td></tr></table></figure><p>即使 MCS 锁本身很可扩展，临界区里的共享数据仍然可能：</p><ul><li>被不同核心轮流修改。</li><li>所在缓存行在核心之间迁移。</li><li>触发大量缓存一致性通信。</li></ul><hr><h3 id="10-3-类比理解"><a href="#10-3-类比理解" class="headerlink" title="10.3 类比理解"></a>10.3 类比理解</h3><p>MCS 锁像是优化了教室门口排队：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">以前大家挤门口；</span><br><span class="line">现在大家排队，一个一个进。</span><br></pre></td></tr></table></figure><p>但如果进教室之后，所有人都要搬同一张桌子，那么瓶颈就变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">教室里的共享桌子</span><br></pre></td></tr></table></figure><p>对应到系统里：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">锁变量不是瓶颈了；</span><br><span class="line">临界区共享数据可能变成瓶颈。</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：MCS-锁提升的是锁本身的可扩展性，但不能自动解决临界区共享数据的缓存一致性开销。"><a href="#❗-本节核心结论：MCS-锁提升的是锁本身的可扩展性，但不能自动解决临界区共享数据的缓存一致性开销。" class="headerlink" title="❗ 本节核心结论：MCS 锁提升的是锁本身的可扩展性，但不能自动解决临界区共享数据的缓存一致性开销。"></a><strong>❗ 本节核心结论：</strong><br>MCS 锁提升的是锁本身的可扩展性，但不能自动解决临界区共享数据的缓存一致性开销。</h2><h2 id="11-NUMA：非一致内存访问"><a href="#11-NUMA：非一致内存访问" class="headerlink" title="11. NUMA：非一致内存访问"></a>11. NUMA：非一致内存访问</h2><h3 id="11-1-NUMA-是什么？"><a href="#11-1-NUMA-是什么？" class="headerlink" title="11.1 NUMA 是什么？"></a>11.1 NUMA 是什么？</h3><p>NUMA 全称是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Non-Uniform Memory Access</span><br></pre></td></tr></table></figure><p>即非一致内存访问。</p><p>它的核心含义是：</p><blockquote><p>不同 CPU 访问不同位置的内存，代价不一样。</p></blockquote><p>在 NUMA 系统中：</p><ul><li>每个处理器 &#x2F; NUMA 结点附近有本地内存。</li><li>访问本地内存更快。</li><li>访问其他结点的远程内存更慢。</li></ul><hr><h3 id="11-2-为什么需要-NUMA？"><a href="#11-2-为什么需要-NUMA？" class="headerlink" title="11.2 为什么需要 NUMA？"></a>11.2 为什么需要 NUMA？</h3><p>如果所有 CPU 都通过同一个内存控制器访问内存，那么核心数量增加后，内存控制器会成为瓶颈。</p><p>NUMA 的思路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">把内存控制器和内存分散到不同处理器附近</span><br></pre></td></tr></table></figure><p>这样可以：</p><ul><li>提高总内存带宽。</li><li>降低本地访问延迟。</li><li>避免单个内存控制器成为全局瓶颈。</li></ul><hr><h3 id="11-3-NUMA-的代价"><a href="#11-3-NUMA-的代价" class="headerlink" title="11.3 NUMA 的代价"></a>11.3 NUMA 的代价</h3><p>NUMA 的代价是：</p><blockquote><p>内存访问不再均匀。</p></blockquote><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">线程运行在 NUMA node 0</span><br><span class="line">但频繁访问 NUMA node 2 的内存</span><br></pre></td></tr></table></figure><p>这会导致远程访存，代价更高。</p><p>如果多个 NUMA 结点频繁竞争同一份共享数据，还会产生跨结点缓存一致性通信，开销非常大。</p><hr><h2 id="12-NUMA-感知设计与-cohort-锁"><a href="#12-NUMA-感知设计与-cohort-锁" class="headerlink" title="12. NUMA 感知设计与 cohort 锁"></a>12. NUMA 感知设计与 cohort 锁</h2><h3 id="12-1-NUMA-感知设计是什么？"><a href="#12-1-NUMA-感知设计是什么？" class="headerlink" title="12.1 NUMA 感知设计是什么？"></a>12.1 NUMA 感知设计是什么？</h3><p>NUMA 感知设计的目标是：</p><blockquote><p>让线程尽量访问本地数据，减少远程访存和跨结点缓存行迁移。</p></blockquote><p>它关注两个问题：</p><table><thead><tr><th>问题</th><th>目标</th></tr></thead><tbody><tr><td>线程在哪里运行</td><td>尽量固定在合适 NUMA 结点</td></tr><tr><td>数据在哪里分配</td><td>尽量分配到使用它的 NUMA 结点附近</td></tr></tbody></table><hr><h3 id="12-2-cohort-锁的基本思想"><a href="#12-2-cohort-锁的基本思想" class="headerlink" title="12.2 cohort 锁的基本思想"></a>12.2 cohort 锁的基本思想</h3><p>cohort 锁是一种 NUMA 感知锁。</p><p>它的大致思想是：</p><blockquote><p>先在本 NUMA 结点内部排队，再竞争全局锁；释放时优先把锁传给本地等待者。</p></blockquote><p>流程：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[线程申请锁] --&gt; B[先获取本 NUMA 结点本地锁]    B --&gt; C[再竞争全局锁]    C --&gt; D[进入临界区]    D --&gt; E{本地队列还有等待者吗?}    E -- 有 --&gt; F[优先传给本地等待者]    E -- 没有 --&gt; G[释放全局锁给其他 NUMA 结点]  </pre></div><hr><h3 id="12-3-为什么-cohort-锁有效？"><a href="#12-3-为什么-cohort-锁有效？" class="headerlink" title="12.3 为什么 cohort 锁有效？"></a>12.3 为什么 cohort 锁有效？</h3><p>普通全局锁可能这样传递：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">node 0 的线程</span><br><span class="line">→ node 2 的线程</span><br><span class="line">→ node 1 的线程</span><br><span class="line">→ node 3 的线程</span><br></pre></td></tr></table></figure><p>这样每次交接都可能造成：</p><ul><li>锁缓存行跨 NUMA 结点迁移。</li><li>临界区共享数据跨结点迁移。</li><li>远程缓存一致性通信。</li></ul><p>cohort 锁尝试让锁在一段时间内留在本地：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">node 0 内部连续传几次</span><br><span class="line">→ 再交给其他 node</span><br></pre></td></tr></table></figure><p>这样可以提升局部性，减少跨结点开销。</p><hr><h3 id="12-4-cohort-锁的取舍"><a href="#12-4-cohort-锁的取舍" class="headerlink" title="12.4 cohort 锁的取舍"></a>12.4 cohort 锁的取舍</h3><p>cohort 锁不是绝对更优，它是在做取舍：</p><table><thead><tr><th>目标</th><th>影响</th></tr></thead><tbody><tr><td>提高 NUMA 局部性</td><td>减少远程访问</td></tr><tr><td>减少跨结点锁传递</td><td>降低缓存行迁移</td></tr><tr><td>优先本地等待者</td><td>可能牺牲部分全局公平性</td></tr><tr><td>设计更复杂</td><td>实现和调优成本更高</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：NUMA-优化的核心不是“所有线程公平轮流”，而是尽量让访问保持局部性。cohort-锁通过本地优先传递锁，减少跨-NUMA-结点通信。"><a href="#❗-本节核心结论：NUMA-优化的核心不是“所有线程公平轮流”，而是尽量让访问保持局部性。cohort-锁通过本地优先传递锁，减少跨-NUMA-结点通信。" class="headerlink" title="❗ 本节核心结论：NUMA 优化的核心不是“所有线程公平轮流”，而是尽量让访问保持局部性。cohort 锁通过本地优先传递锁，减少跨 NUMA 结点通信。"></a><strong>❗ 本节核心结论：</strong><br>NUMA 优化的核心不是“所有线程公平轮流”，而是尽量让访问保持局部性。cohort 锁通过本地优先传递锁，减少跨 NUMA 结点通信。</h2><h2 id="13-重要对比"><a href="#13-重要对比" class="headerlink" title="13. 重要对比"></a>13. 重要对比</h2><h3 id="13-1-四种内存一致性模型"><a href="#13-1-四种内存一致性模型" class="headerlink" title="13.1 四种内存一致性模型"></a>13.1 四种内存一致性模型</h3><table><thead><tr><th>对比点</th><th>严格一致性</th><th>顺序一致性</th><th>TSO</th><th>弱序一致性</th></tr></thead><tbody><tr><td>保证强度</td><td>最强</td><td>较强</td><td>中等</td><td>较弱</td></tr><tr><td>是否按真实时间全局可见</td><td>是</td><td>不要求</td><td>不要求</td><td>不要求</td></tr><tr><td>是否保持每个核心程序顺序</td><td>基本保持</td><td>保持</td><td>大多数保持</td><td>不一定保持</td></tr><tr><td>典型放松</td><td>几乎不放松</td><td>不按真实时间</td><td>写 → 读 可放松</td><td>不同地址读写都可能放松</td></tr><tr><td>硬件实现成本</td><td>极高</td><td>高</td><td>中等</td><td>较低</td></tr><tr><td>程序员负担</td><td>低</td><td>较低</td><td>中等</td><td>高</td></tr><tr><td>代表场景</td><td>理论模型</td><td>教学 &#x2F; 理论推理</td><td>x86&#x2F;64 常见</td><td>ARM &#x2F; PowerPC 常见</td></tr></tbody></table><hr><h3 id="13-2-缓存一致性-vs-内存一致性"><a href="#13-2-缓存一致性-vs-内存一致性" class="headerlink" title="13.2 缓存一致性 vs 内存一致性"></a>13.2 缓存一致性 vs 内存一致性</h3><table><thead><tr><th>对比点</th><th>缓存一致性 Cache Coherence</th><th>内存一致性 Memory Consistency</th></tr></thead><tbody><tr><td>关注对象</td><td>同一个内存地址</td><td>多个地址上的操作顺序</td></tr><tr><td>核心问题</td><td>一个地址的多个缓存副本怎么保持一致</td><td>不同 CPU 观察到的读写顺序是否合法</td></tr><tr><td>典型例子</td><td><code>flag</code> 的新值什么时候被其他核心看到</td><td><code>data=666</code> 和 <code>flag=READY</code> 谁先对外可见</td></tr><tr><td>解决重点</td><td>缓存行状态、失效、更新、目录项</td><td>顺序模型、barrier、同步原语语义</td></tr><tr><td>易错理解</td><td>以为一个地址一致就够了</td><td>即使单地址一致，不同地址顺序仍可能出问题</td></tr></tbody></table><p>一句话区分：</p><blockquote><p>缓存一致性解决“同一个地址的值对不对”；内存一致性解决“多个地址的读写顺序对不对”。</p></blockquote><hr><h3 id="13-3-MCS-锁-vs-cohort-锁"><a href="#13-3-MCS-锁-vs-cohort-锁" class="headerlink" title="13.3 MCS 锁 vs cohort 锁"></a>13.3 MCS 锁 vs cohort 锁</h3><table><thead><tr><th>对比点</th><th>MCS 锁</th><th>cohort 锁</th></tr></thead><tbody><tr><td>主要目标</td><td>减少锁变量竞争</td><td>减少跨 NUMA 结点传递</td></tr><tr><td>关注层次</td><td>多核共享缓存</td><td>NUMA 多结点</td></tr><tr><td>等待方式</td><td>每个线程在自己的节点上等待</td><td>先本地队列，再全局锁</td></tr><tr><td>优点</td><td>锁元数据可扩展性好</td><td>保持 NUMA 局部性</td></tr><tr><td>局限</td><td>不知道临界区访问了哪些共享数据</td><td>设计复杂，可能牺牲部分公平性</td></tr></tbody></table><hr><h2 id="14-最容易混淆的点"><a href="#14-最容易混淆的点" class="headerlink" title="14. 最容易混淆的点"></a>14. 最容易混淆的点</h2><h3 id="14-1-代码顺序不是硬件可见顺序"><a href="#14-1-代码顺序不是硬件可见顺序" class="headerlink" title="14.1 代码顺序不是硬件可见顺序"></a>14.1 代码顺序不是硬件可见顺序</h3><p>你写：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">data = <span class="number">666</span>;</span><br><span class="line">flag = READY;</span><br></pre></td></tr></table></figure><p>不代表其他 CPU 一定先看到 <code>data</code>，再看到 <code>flag</code>。</p><p>在弱内存模型下，如果没有 barrier 或同步原语，其他核心可能先看到 <code>flag</code>。</p><hr><h3 id="14-2-乱序执行不是-CPU-乱来"><a href="#14-2-乱序执行不是-CPU-乱来" class="headerlink" title="14.2 乱序执行不是 CPU 乱来"></a>14.2 乱序执行不是 CPU 乱来</h3><p>乱序执行不是 CPU 随便改程序。</p><p>它通常要保证：</p><ul><li>单线程结果看起来正确。</li><li>数据依赖不能被随意破坏。</li><li>异常语义不能被随意破坏。</li></ul><p>但多线程共享内存程序中，其他 CPU 看到的顺序可能和本线程代码顺序不同。</p><hr><h3 id="14-3-TSO-不是顺序一致性"><a href="#14-3-TSO-不是顺序一致性" class="headerlink" title="14.3 TSO 不是顺序一致性"></a>14.3 TSO 不是顺序一致性</h3><p>TSO 已经比较强，但它仍然允许：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">写 → 读</span><br></pre></td></tr></table></figure><p>顺序被放松。</p><p>所以不能说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">x86 是 TSO，所以并发代码可以随便写。</span><br></pre></td></tr></table></figure><p>TSO 只是让一部分问题不容易暴露，不代表没有内存顺序问题。</p><hr><h3 id="14-4-barrier-不是越多越好"><a href="#14-4-barrier-不是越多越好" class="headerlink" title="14.4 barrier 不是越多越好"></a>14.4 barrier 不是越多越好</h3><p>barrier 可以保证顺序，但会牺牲性能。</p><p>正确思路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">用同步原语表达同步关系；</span><br><span class="line">让同步原语内部使用合适的 barrier。</span><br></pre></td></tr></table></figure><hr><h3 id="14-5-MCS-锁好，不代表临界区整体性能好"><a href="#14-5-MCS-锁好，不代表临界区整体性能好" class="headerlink" title="14.5 MCS 锁好，不代表临界区整体性能好"></a>14.5 MCS 锁好，不代表临界区整体性能好</h3><p>MCS 锁优化的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">锁变量缓存行竞争</span><br></pre></td></tr></table></figure><p>如果临界区访问大量共享缓存行，瓶颈仍然会存在。</p><hr><h3 id="14-6-NUMA-不是“多个-CPU”这么简单"><a href="#14-6-NUMA-不是“多个-CPU”这么简单" class="headerlink" title="14.6 NUMA 不是“多个 CPU”这么简单"></a>14.6 NUMA 不是“多个 CPU”这么简单</h3><p>NUMA 的关键不是 CPU 多，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">不同 CPU 访问不同内存位置的代价不同。</span><br></pre></td></tr></table></figure><p>同一条 <code>load</code> 指令，访问本地内存和远程内存，代价可能差很多。</p><hr><h2 id="15-本讲一句话总结"><a href="#15-本讲一句话总结" class="headerlink" title="15. 本讲一句话总结"></a>15. 本讲一句话总结</h2><p>多核系统中，不能只用代码书写顺序推理共享内存程序，因为硬件可能乱序执行，不同 CPU 对内存操作的可见顺序也可能不同。内存一致性模型规定了哪些顺序必须被保证；弱内存模型下需要 barrier 或正确实现的同步原语来建立顺序。同时，在多核和 NUMA 环境中，即使 MCS 锁减少了锁变量竞争，临界区共享数据和远程访存仍然可能成为性能瓶颈。</p><hr><h2 id="16-自测题"><a href="#16-自测题" class="headerlink" title="16. 自测题"></a>16. 自测题</h2><ol><li>为什么 LockOne 这类算法会暴露“代码顺序不等于硬件可见顺序”的问题？</li><li>Peterson 算法为什么也需要在现实硬件上考虑内存一致性模型？</li><li>什么是内存一致性模型？</li><li>内存一致性和缓存一致性有什么区别？</li><li>严格一致性为什么符合程序员直觉？</li><li>严格一致性为什么很难高效实现？</li><li>顺序一致性为什么不要求真实时间顺序，却仍然能限制并发结果？</li><li>在顺序一致性下，为什么 <code>(A, B) = (0, 0)</code> 不应该出现？</li><li>TSO 为什么主要放松“写 → 读”顺序？</li><li>TSO 下为什么两个线程都可能读到旧值？</li><li>弱序一致性为什么能降低硬件复杂度？</li><li>弱序一致性为什么增加软件编写难度？</li><li><code>data = 666; flag = READY;</code> 在弱序模型下为什么危险？</li><li>barrier &#x2F; fence 的本质作用是什么？</li><li>为什么说 barrier 是一条“访存顺序边界”？</li><li>为什么普通应用程序员不应该到处手写 barrier？</li><li>锁除了互斥之外，还提供什么访存顺序语义？</li><li>acquire &#x2F; release 分别表示什么？</li><li>MCS 锁相比普通自旋锁主要优化了什么？</li><li>MCS 锁没有优化什么？</li><li>为什么临界区访问更多共享缓存行，会影响 MCS 锁可扩展性？</li><li>NUMA 中本地内存访问和远程内存访问有什么区别？</li><li>NUMA 为什么能提升总内存带宽？</li><li>NUMA 为什么又会带来新的性能问题？</li><li>cohort 锁为什么能改善 NUMA 场景下的锁性能？</li><li>cohort 锁可能牺牲什么？</li></ol><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>本讲可以压缩成一条主线：</p><blockquote><p>多核程序的难点不只是“多个线程会不会同时进入临界区”，还包括“不同核心按什么顺序看到共享内存操作”，以及“核心和内存位置不均匀时，锁和数据如何保持局部性”。</p></blockquote><p>重点记住：</p><ul><li><strong>❗ 内存一致性模型</strong>：规定多个核心观察内存操作顺序的规则。</li><li><strong>❗ 顺序一致性</strong>：要求存在一个保持各线程程序顺序的全局顺序。</li><li><strong>❗ TSO</strong>：大部分顺序保留，但写→读可能被放松。</li><li><strong>❗ 弱序模型</strong>：更多顺序交给软件用 barrier 或同步原语保证。</li><li><strong>❗ barrier &#x2F; fence</strong>：限制访存重排，建立顺序边界。</li><li><strong>❗ 同步原语</strong>：不仅提供互斥，也提供内存顺序语义。</li><li><strong>❗ MCS 锁局限</strong>：优化锁变量竞争，但不消除临界区共享数据迁移。</li><li><strong>❗ NUMA</strong>：本地访问快，远程访问慢。</li><li><strong>❗ cohort 锁</strong>：让锁尽量在本 NUMA 结点内传递，减少跨结点开销。</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第17讲 多核与多处理器I</title>
      <link href="/2026/06/11/%E7%AC%AC17%E8%AE%B2-%E5%A4%9A%E6%A0%B8%E4%B8%8E%E5%A4%9A%E5%A4%84%E7%90%86%E5%99%A8I/"/>
      <url>/2026/06/11/%E7%AC%AC17%E8%AE%B2-%E5%A4%9A%E6%A0%B8%E4%B8%8E%E5%A4%9A%E5%A4%84%E7%90%86%E5%99%A8I/</url>
      
        <content type="html"><![CDATA[<p>以下根据附件《第17讲 多核与多处理器I.md》整理生成。</p><h1 id="第17讲：多核与多处理器-I-学习指南"><a href="#第17讲：多核与多处理器-I-学习指南" class="headerlink" title="第17讲：多核与多处理器 I 学习指南"></a>第17讲：多核与多处理器 I 学习指南</h1><p>本讲的主线是：<strong>多核不是简单的“核心越多越快”</strong>。多核确实带来并行能力，但也引入了缓存一致性、锁竞争、可扩展性断崖、虚假共享和乱序执行等问题。</p><hr><h2 id="❗-本节核心结论：多核系统的核心矛盾是：核心数增加带来并行能力，但共享数据、私有缓存和锁竞争会引入正确性问题与性能问题。多核优化的关键不是“多加核心”，而是减少核心之间的协调成本。"><a href="#❗-本节核心结论：多核系统的核心矛盾是：核心数增加带来并行能力，但共享数据、私有缓存和锁竞争会引入正确性问题与性能问题。多核优化的关键不是“多加核心”，而是减少核心之间的协调成本。" class="headerlink" title="❗ 本节核心结论：多核系统的核心矛盾是：核心数增加带来并行能力，但共享数据、私有缓存和锁竞争会引入正确性问题与性能问题。多核优化的关键不是“多加核心”，而是减少核心之间的协调成本。"></a><strong>❗ 本节核心结论：</strong><br>多核系统的核心矛盾是：核心数增加带来并行能力，但共享数据、私有缓存和锁竞争会引入正确性问题与性能问题。多核优化的关键不是“多加核心”，而是减少核心之间的协调成本。</h2><h2 id="1-本章知识地图"><a href="#1-本章知识地图" class="headerlink" title="1. 本章知识地图"></a>1. 本章知识地图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[单核性能遇到瓶颈] --&gt; B[增加 CPU 核心数]    B --&gt; C[多线程并行执行]    C --&gt; D[每个核心有私有缓存]    D --&gt; E[同一地址可能有多个缓存副本]    E --&gt; F[缓存一致性问题]    F --&gt; G[MSI &#x2F; 目录式一致性]    G --&gt; H[一致性通信开销]    H --&gt; I[锁竞争与可扩展性断崖]    I --&gt; J[Back-off]    I --&gt; K[MCS 锁 &#x2F; QSpinlock]    I --&gt; L[避免虚假共享]    D --&gt; M[乱序执行]    M --&gt; N[内存一致性模型铺垫]  </pre></div><p>这张图要抓住一个核心逻辑：</p><blockquote><p>多核的真正难点不是“多个核心能不能同时跑”，而是“多个核心同时访问共享数据时，如何既正确又高效”。</p></blockquote><hr><h2 id="2-多核为什么不是免费加速？"><a href="#2-多核为什么不是免费加速？" class="headerlink" title="2. 多核为什么不是免费加速？"></a>2. 多核为什么不是免费加速？</h2><h3 id="2-1-多核解决了什么问题？"><a href="#2-1-多核解决了什么问题？" class="headerlink" title="2.1 多核解决了什么问题？"></a>2.1 多核解决了什么问题？</h3><p>单核 CPU 性能提升受到限制：</p><ul><li>频率不能无限提高。</li><li>功耗和发热会急剧上升。</li><li>单核流水线和乱序执行优化也有边界。</li></ul><p>因此现代系统通过增加核心数来提升整体吞吐量。</p><p>直觉上似乎是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1 核 = 1 倍性能</span><br><span class="line">4 核 = 4 倍性能</span><br><span class="line">16 核 = 16 倍性能</span><br></pre></td></tr></table></figure><p>但现实不是这样。</p><hr><h3 id="2-2-多核带来的两类问题"><a href="#2-2-多核带来的两类问题" class="headerlink" title="2.2 多核带来的两类问题"></a>2.2 多核带来的两类问题</h3><table><thead><tr><th>问题类型</th><th>核心问题</th><th>例子</th></tr></thead><tbody><tr><td>正确性问题</td><td>多个核心看到的数据是否一致</td><td>多线程同时 <code>balance++</code>，最后结果错误</td></tr><tr><td>性能问题</td><td>核心数增加后性能是否真的提升</td><td>很多核心抢同一把自旋锁，性能反而下降</td></tr></tbody></table><p>多核程序最容易误解的一点是：</p><blockquote><p>正确性和性能是两件事。</p></blockquote><p>锁可以保证正确性，但锁本身可能成为性能瓶颈。缓存一致性可以保证多个核心对同一地址不“各说各话”，但它也会带来通信开销。</p><hr><h2 id="3-加速比与-Amdahl’s-Law"><a href="#3-加速比与-Amdahl’s-Law" class="headerlink" title="3. 加速比与 Amdahl’s Law"></a>3. 加速比与 Amdahl’s Law</h2><h3 id="3-1-加速比是什么？"><a href="#3-1-加速比是什么？" class="headerlink" title="3.1 加速比是什么？"></a>3.1 加速比是什么？</h3><p>加速比 Speedup 衡量的是：</p><blockquote><p>使用更多资源后，同一个任务完成得快了多少倍。</p></blockquote><p>公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">加速比 = 单核执行时间 / 多核执行时间</span><br></pre></td></tr></table></figure><p>例如：</p><table><thead><tr><th>单核时间</th><th>多核时间</th><th>加速比</th></tr></thead><tbody><tr><td>100 秒</td><td>25 秒</td><td>4</td></tr><tr><td>100 秒</td><td>40 秒</td><td>2.5</td></tr><tr><td>100 秒</td><td>80 秒</td><td>1.25</td></tr></tbody></table><p>如果用了 4 个核心，但加速比只有 2.5，说明并行化并没有充分利用全部核心。</p><hr><h3 id="3-2-Amdahl’s-Law-讲什么？"><a href="#3-2-Amdahl’s-Law-讲什么？" class="headerlink" title="3.2 Amdahl’s Law 讲什么？"></a>3.2 Amdahl’s Law 讲什么？</h3><p>Amdahl 定律说明：</p><blockquote><p>一个程序的串行部分会限制多核加速的理论上限。</p></blockquote><p>S&#x3D;\frac{1}{(1-p)+\frac{p}{s}}</p><p>其中：</p><table><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td><code>S</code></td><td>加速比</td></tr><tr><td><code>p</code></td><td>可以并行的部分占比</td></tr><tr><td><code>1 - p</code></td><td>必须串行的部分占比</td></tr><tr><td><code>s</code></td><td>使用的核心数</td></tr></tbody></table><hr><h3 id="3-3-推导过程"><a href="#3-3-推导过程" class="headerlink" title="3.3 推导过程"></a>3.3 推导过程</h3><p>假设单核执行整个程序需要 1 单位时间。</p><p>程序分成两部分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">串行部分：1 - p</span><br><span class="line">并行部分：p</span><br></pre></td></tr></table></figure><p>单核执行时间：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">T1 = (1 - p) + p = 1</span><br></pre></td></tr></table></figure><p>使用 <code>s</code> 个核心后：</p><ul><li>串行部分不能并行，时间仍然是 <code>1 - p</code>。</li><li>并行部分可以分给 <code>s</code> 个核心，理想时间变为 <code>p / s</code>。</li></ul><p>所以多核执行时间是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Ts = (1 - p) + p / s</span><br></pre></td></tr></table></figure><p>加速比是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">S = T1 / Ts</span><br></pre></td></tr></table></figure><p>因为 <code>T1 = 1</code>，所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">S = 1 / ((1 - p) + p / s)</span><br></pre></td></tr></table></figure><hr><h3 id="3-4-例子"><a href="#3-4-例子" class="headerlink" title="3.4 例子"></a>3.4 例子</h3><p>假设一个程序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">90% 可以并行</span><br><span class="line">10% 必须串行</span><br></pre></td></tr></table></figure><p>也就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">p = 0.9</span><br><span class="line">1 - p = 0.1</span><br></pre></td></tr></table></figure><p>如果用 10 个核心：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">S = 1 / (0.1 + 0.9 / 10)</span><br><span class="line">  = 1 / 0.19</span><br><span class="line">  ≈ 5.26</span><br></pre></td></tr></table></figure><p>注意：用了 10 个核心，不等于快 10 倍。</p><p>如果核心数无限多：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">p / s → 0</span><br><span class="line">S = 1 / (1 - p)</span><br><span class="line">  = 1 / 0.1</span><br><span class="line">  = 10</span><br></pre></td></tr></table></figure><p>也就是说，即使给无限多个核心，最多也只能快 10 倍。</p><hr><h2 id="❗-本节核心结论：加速比不是看核心数量，而是看程序中真正能并行的比例。串行部分越大，多核带来的理论收益越低。"><a href="#❗-本节核心结论：加速比不是看核心数量，而是看程序中真正能并行的比例。串行部分越大，多核带来的理论收益越低。" class="headerlink" title="❗ 本节核心结论：加速比不是看核心数量，而是看程序中真正能并行的比例。串行部分越大，多核带来的理论收益越低。"></a><strong>❗ 本节核心结论：</strong><br>加速比不是看核心数量，而是看程序中真正能并行的比例。串行部分越大，多核带来的理论收益越低。</h2><h2 id="4-多级缓存与缓存一致性"><a href="#4-多级缓存与缓存一致性" class="headerlink" title="4. 多级缓存与缓存一致性"></a>4. 多级缓存与缓存一致性</h2><h3 id="4-1-多核缓存结构"><a href="#4-1-多核缓存结构" class="headerlink" title="4.1 多核缓存结构"></a>4.1 多核缓存结构</h3><p>现代 CPU 通常有多级缓存：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    C0[CPU Core 0] --&gt; L10[L1 Cache]    C1[CPU Core 1] --&gt; L11[L1 Cache]    C2[CPU Core 2] --&gt; L12[L1 Cache]    L10 --&gt; L20[L2 Cache]    L11 --&gt; L21[L2 Cache]    L12 --&gt; L22[L2 Cache]    L20 --&gt; L3[LLC &#x2F; L3 Cache]    L21 --&gt; L3    L22 --&gt; L3    L3 --&gt; MEM[Memory]  </pre></div><p>一般来说：</p><ul><li>L1 最快、最小，通常每个核心私有。</li><li>L2 的组织方式取决于具体 CPU，可能私有，也可能部分共享。</li><li>LLC，通常是 L3，往往被多个核心共享。</li><li>内存最慢，但容量最大。</li></ul><p>关键点是：</p><blockquote><p>缓存一致性主要不是 LLC 的问题，而是多个核心的私有缓存可能持有同一缓存行副本的问题。</p></blockquote><hr><h3 id="4-2-为什么需要缓存一致性？"><a href="#4-2-为什么需要缓存一致性？" class="headerlink" title="4.2 为什么需要缓存一致性？"></a>4.2 为什么需要缓存一致性？</h3><p>假设同一个地址 <code>X</code> 被多个核心缓存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">CPU0 的 L1：X = 1</span><br><span class="line">CPU1 的 L1：X = ?</span><br><span class="line">CPU2 的 L1：X = 2</span><br></pre></td></tr></table></figure><p>如果没有一致性协议，不同核心可能对同一个地址读到不同值。</p><p>缓存一致性要解决的问题是：</p><blockquote><p>不同核心对同一个内存地址的值必须达成共识。</p></blockquote><p>注意，它解决的是正确性问题，不直接保证性能。</p><hr><h2 id="5-MSI-协议"><a href="#5-MSI-协议" class="headerlink" title="5. MSI 协议"></a>5. MSI 协议</h2><p>缓存一致性协议通常以 <strong>缓存行 Cache Line</strong> 为单位，而不是以单个变量为单位。</p><p>MSI 有三种状态：</p><table><thead><tr><th>状态</th><th>英文</th><th>含义</th></tr></thead><tbody><tr><td>M</td><td>Modified</td><td>当前核心独占该缓存行，并且内容可能比内存更新</td></tr><tr><td>S</td><td>Shared</td><td>多个核心可以共享该缓存行，只能安全读</td></tr><tr><td>I</td><td>Invalid</td><td>当前核心里的这份缓存行副本无效</td></tr></tbody></table><hr><h3 id="5-1-MSI-的核心规则"><a href="#5-1-MSI-的核心规则" class="headerlink" title="5.1 MSI 的核心规则"></a>5.1 MSI 的核心规则</h3><p>可以简化为四句话：</p><ul><li><strong>读可以共享。</strong></li><li><strong>写必须独占。</strong></li><li><strong>别人写了，我这里的副本就要失效。</strong></li><li><strong>我写之前，要先让别人手里的副本失效。</strong></li></ul><hr><h3 id="5-2-Invalid-是全局状态吗？"><a href="#5-2-Invalid-是全局状态吗？" class="headerlink" title="5.2 Invalid 是全局状态吗？"></a>5.2 Invalid 是全局状态吗？</h3><p>不是。</p><p><code>Invalid</code> 是针对：</p><blockquote><p>某个核心本地缓存中的某一条缓存行副本。</p></blockquote><p>例如：</p><table><thead><tr><th>核心</th><th>X 所在缓存行状态</th><th>含义</th></tr></thead><tbody><tr><td>CPU0</td><td>Modified</td><td>CPU0 拥有最新副本，可以读写</td></tr><tr><td>CPU1</td><td>Invalid</td><td>CPU1 本地副本失效，不能直接使用</td></tr><tr><td>CPU2</td><td>Invalid</td><td>CPU2 本地副本失效，不能直接使用</td></tr></tbody></table><p>这不是说 <code>X</code> 这个数据“全世界无效”，而是说：</p><blockquote><p>CPU1 &#x2F; CPU2 手里的旧副本不能再用了。</p></blockquote><hr><h3 id="5-3-写共享变量为什么贵？"><a href="#5-3-写共享变量为什么贵？" class="headerlink" title="5.3 写共享变量为什么贵？"></a>5.3 写共享变量为什么贵？</h3><p>假设 CPU0 要写 <code>X</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CPU0 想写 X</span><br><span class="line">    ↓</span><br><span class="line">必须获得 X 所在缓存行的独占修改权限</span><br><span class="line">    ↓</span><br><span class="line">如果 CPU1、CPU2 也缓存了这个缓存行</span><br><span class="line">    ↓</span><br><span class="line">就要通知它们失效</span><br><span class="line">    ↓</span><br><span class="line">CPU0 才能安全写入</span><br></pre></td></tr></table></figure><p>所以代码看起来只是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">x = <span class="number">233</span>;</span><br></pre></td></tr></table></figure><p>但硬件底层可能发生：</p><ul><li>请求独占权限</li><li>查询谁拥有副本</li><li>发送失效消息</li><li>等待确认</li><li>更新缓存行状态</li><li>最后写入</li></ul><p>这些硬件级协调过程就是一致性通信。</p><hr><h2 id="❗-本节核心结论：缓存一致性保证多个核心对同一地址不出现混乱，但它不是免费的。共享缓存行被频繁写入时，一致性通信会成为巨大性能开销。"><a href="#❗-本节核心结论：缓存一致性保证多个核心对同一地址不出现混乱，但它不是免费的。共享缓存行被频繁写入时，一致性通信会成为巨大性能开销。" class="headerlink" title="❗ 本节核心结论：缓存一致性保证多个核心对同一地址不出现混乱，但它不是免费的。共享缓存行被频繁写入时，一致性通信会成为巨大性能开销。"></a><strong>❗ 本节核心结论：</strong><br>缓存一致性保证多个核心对同一地址不出现混乱，但它不是免费的。共享缓存行被频繁写入时，一致性通信会成为巨大性能开销。</h2><h2 id="6-目录式缓存一致性"><a href="#6-目录式缓存一致性" class="headerlink" title="6. 目录式缓存一致性"></a>6. 目录式缓存一致性</h2><h3 id="6-1-目录式一致性是什么？"><a href="#6-1-目录式一致性是什么？" class="headerlink" title="6.1 目录式一致性是什么？"></a>6.1 目录式一致性是什么？</h3><p>目录式缓存一致性维护一个目录项，用来记录某个缓存行的状态。</p><p>目录项通常记录：</p><table><thead><tr><th>信息</th><th>含义</th></tr></thead><tbody><tr><td>脏位 dirty bit</td><td>该缓存行是否被修改，是否可能比内存新</td></tr><tr><td>向量位 bit vector</td><td>哪些 CPU 拥有该缓存行副本</td></tr><tr><td>拥有者 owner</td><td>谁拥有最新或独占副本</td></tr></tbody></table><hr><h3 id="6-2-目录式为什么有用？"><a href="#6-2-目录式为什么有用？" class="headerlink" title="6.2 目录式为什么有用？"></a>6.2 目录式为什么有用？</h3><p>如果没有目录信息，一个核心写某个缓存行时，可能需要广播通知所有核心。</p><p>目录式协议可以做到：</p><blockquote><p>只通知真正拥有该缓存行副本的核心。</p></blockquote><p>例如有 64 个核心，但只有 CPU1 和 CPU7 缓存了 <code>X</code>。</p><p>那么 CPU0 要写 <code>X</code> 时，不必通知所有 64 个核心，只需要通知 CPU1 和 CPU7 失效。</p><hr><h3 id="6-3-目录式一致性的基本流程"><a href="#6-3-目录式一致性的基本流程" class="headerlink" title="6.3 目录式一致性的基本流程"></a>6.3 目录式一致性的基本流程</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant CPU0 as CPU0    participant Dir as 目录    participant CPU1 as CPU1    participant CPU2 as CPU2    CPU0-&gt;&gt;Dir: 请求写 X    Dir-&gt;&gt;Dir: 查询 X 的拥有者&#x2F;共享者    Dir-&gt;&gt;CPU1: 使 X 缓存行失效    Dir-&gt;&gt;CPU2: 使 X 缓存行失效    CPU1--&gt;&gt;Dir: 已失效    CPU2--&gt;&gt;Dir: 已失效    Dir--&gt;&gt;CPU0: 获得独占权限    CPU0-&gt;&gt;CPU0: 写入 X &#x3D; 233  </pre></div><p>目录式不是取消通信，而是让通信更精确。</p><hr><h3 id="6-4-目录式的代价"><a href="#6-4-目录式的代价" class="headerlink" title="6.4 目录式的代价"></a>6.4 目录式的代价</h3><p>目录式仍然需要成本：</p><ul><li>存储目录项。</li><li>查询目录。</li><li>更新目录。</li><li>发送失效消息。</li><li>等待确认。</li><li>必要时转发最新数据。</li></ul><p>如果很多核心反复写同一个缓存行，仍然会出现缓存行所有权在核心之间反复迁移。</p><hr><h2 id="❗-本节核心结论：目录式一致性的优点是减少无目标广播，只通知相关核心；但它仍然需要一致性通信，不能消除共享缓存行竞争。"><a href="#❗-本节核心结论：目录式一致性的优点是减少无目标广播，只通知相关核心；但它仍然需要一致性通信，不能消除共享缓存行竞争。" class="headerlink" title="❗ 本节核心结论：目录式一致性的优点是减少无目标广播，只通知相关核心；但它仍然需要一致性通信，不能消除共享缓存行竞争。"></a><strong>❗ 本节核心结论：</strong><br>目录式一致性的优点是减少无目标广播，只通知相关核心；但它仍然需要一致性通信，不能消除共享缓存行竞争。</h2><h2 id="7-性能可扩展性与可扩展性断崖"><a href="#7-性能可扩展性与可扩展性断崖" class="headerlink" title="7. 性能可扩展性与可扩展性断崖"></a>7. 性能可扩展性与可扩展性断崖</h2><h3 id="7-1-什么是性能可扩展性？"><a href="#7-1-什么是性能可扩展性？" class="headerlink" title="7.1 什么是性能可扩展性？"></a>7.1 什么是性能可扩展性？</h3><p>性能可扩展性指的是：</p><blockquote><p>当硬件资源增加时，系统性能是否能随之有效增长。</p></blockquote><p>在多核中，“规模变大”通常指：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CPU 核心数增加</span><br></pre></td></tr></table></figure><p>希望看到的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">核心数增加 → 吞吐量上升</span><br></pre></td></tr></table></figure><p>如果出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">核心数增加 → 吞吐量不升反降</span><br></pre></td></tr></table></figure><p>就说明性能可扩展性很差。</p><hr><h3 id="7-2-可扩展性不是“能不能加规模”"><a href="#7-2-可扩展性不是“能不能加规模”" class="headerlink" title="7.2 可扩展性不是“能不能加规模”"></a>7.2 可扩展性不是“能不能加规模”</h3><p>更准确地说：</p><blockquote><p>可扩展性 &#x3D; 扩大规模以后，收益是否还能增长，代价是否仍可控。</p></blockquote><p>例如：</p><table><thead><tr><th>场景</th><th>规模变大</th><th>可扩展表现</th><th>不可扩展表现</th></tr></thead><tbody><tr><td>多核处理器</td><td>核心数增加</td><td>吞吐量继续上升</td><td>锁竞争导致性能下降</td></tr><tr><td>P2P 网络</td><td>节点数增加</td><td>节点也贡献上传能力</td><td>中心节点或维护开销爆炸</td></tr><tr><td>Web 服务</td><td>请求数增加</td><td>加机器后能处理更多请求</td><td>数据库成为瓶颈</td></tr><tr><td>软件架构</td><td>功能增加</td><td>模块化扩展</td><td>改一个功能牵动全局</td></tr></tbody></table><p>所以“可扩展”不是简单的“能扩”，而是：</p><ul><li>扩了之后撑得住。</li><li>扩了之后还划算。</li><li>扩了之后不会被某个瓶颈拖垮。</li></ul><hr><h3 id="7-3-什么是可扩展性断崖？"><a href="#7-3-什么是可扩展性断崖？" class="headerlink" title="7.3 什么是可扩展性断崖？"></a>7.3 什么是可扩展性断崖？</h3><p>可扩展性断崖指的是：</p><blockquote><p>核心数增加到某个程度后，程序性能不仅不再提升，反而突然大幅下降。</p></blockquote><p>例如：</p><table><thead><tr><th>核心数</th><th>吞吐量</th></tr></thead><tbody><tr><td>1</td><td>100</td></tr><tr><td>2</td><td>180</td></tr><tr><td>4</td><td>300</td></tr><tr><td>8</td><td>280</td></tr><tr><td>16</td><td>80</td></tr><tr><td>32</td><td>20</td></tr></tbody></table><p>这不是“收益递减”那么简单，而是性能直接塌下去。</p><hr><h2 id="8-自旋锁为什么会导致可扩展性断崖？"><a href="#8-自旋锁为什么会导致可扩展性断崖？" class="headerlink" title="8. 自旋锁为什么会导致可扩展性断崖？"></a>8. 自旋锁为什么会导致可扩展性断崖？</h2><p>普通自旋锁大致如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">lock</span><span class="params">(<span class="type">int</span> *lock)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (atomic_CAS(lock, <span class="number">0</span>, <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line">        ; <span class="comment">// busy-looping</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">unlock</span><span class="params">(<span class="type">int</span> *lock)</span> &#123;</span><br><span class="line">    *lock = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>表面上看，只是在抢一把锁。</p><p>但在多核底层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">所有核心都 CAS 同一个 lock 变量</span><br><span class="line">    ↓</span><br><span class="line">lock 变量位于某个缓存行</span><br><span class="line">    ↓</span><br><span class="line">CAS 是写操作，需要独占该缓存行</span><br><span class="line">    ↓</span><br><span class="line">多个核心反复抢这个缓存行的所有权</span><br><span class="line">    ↓</span><br><span class="line">缓存行在核心之间来回迁移</span><br><span class="line">    ↓</span><br><span class="line">一致性通信暴增</span><br><span class="line">    ↓</span><br><span class="line">性能断崖式下降</span><br></pre></td></tr></table></figure><p>这就是自旋锁危险的地方。</p><p>它的问题不只是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">while 循环浪费 CPU</span><br></pre></td></tr></table></figure><p>更严重的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">所有核心反复争抢同一个锁变量缓存行</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：普通自旋锁在高竞争多核环境下会把锁变量所在缓存行变成热点，导致一致性通信暴增，引发可扩展性断崖。"><a href="#❗-本节核心结论：普通自旋锁在高竞争多核环境下会把锁变量所在缓存行变成热点，导致一致性通信暴增，引发可扩展性断崖。" class="headerlink" title="❗ 本节核心结论：普通自旋锁在高竞争多核环境下会把锁变量所在缓存行变成热点，导致一致性通信暴增，引发可扩展性断崖。"></a><strong>❗ 本节核心结论：</strong><br>普通自旋锁在高竞争多核环境下会把锁变量所在缓存行变成热点，导致一致性通信暴增，引发可扩展性断崖。</h2><h2 id="9-Back-off-回退策略"><a href="#9-Back-off-回退策略" class="headerlink" title="9. Back-off 回退策略"></a>9. Back-off 回退策略</h2><h3 id="9-1-Back-off-是什么？"><a href="#9-1-Back-off-是什么？" class="headerlink" title="9.1 Back-off 是什么？"></a>9.1 Back-off 是什么？</h3><p>Back-off 的思想是：</p><blockquote><p>抢锁失败后，不要立刻继续抢，而是先等一会儿。</p></blockquote><p>伪代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">lock</span><span class="params">(<span class="type">int</span> *lock)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (atomic_CAS(lock, <span class="number">0</span>, <span class="number">1</span>) != <span class="number">0</span>) &#123;</span><br><span class="line">        back_off(DEFAULT_TIME);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="9-2-为什么有用？"><a href="#9-2-为什么有用？" class="headerlink" title="9.2 为什么有用？"></a>9.2 为什么有用？</h3><p>普通自旋锁失败后会立即继续 CAS。</p><p>Back-off 让失败线程暂时退让：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">减少抢锁频率</span><br><span class="line">    ↓</span><br><span class="line">减少同一缓存行被反复争抢</span><br><span class="line">    ↓</span><br><span class="line">降低一致性通信压力</span><br></pre></td></tr></table></figure><hr><h3 id="9-3-固定等待的问题"><a href="#9-3-固定等待的问题" class="headerlink" title="9.3 固定等待的问题"></a>9.3 固定等待的问题</h3><p>如果所有线程等待相同时间：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">大家同时失败</span><br><span class="line">    ↓</span><br><span class="line">大家同时等待</span><br><span class="line">    ↓</span><br><span class="line">大家同时醒来</span><br><span class="line">    ↓</span><br><span class="line">大家同时继续抢</span><br><span class="line">    ↓</span><br><span class="line">再次冲突</span><br></pre></td></tr></table></figure><p>所以更好的方式通常是：</p><ul><li>随机回退</li><li>指数回退</li></ul><hr><h3 id="9-4-Back-off-的局限"><a href="#9-4-Back-off-的局限" class="headerlink" title="9.4 Back-off 的局限"></a>9.4 Back-off 的局限</h3><p>Back-off 只是缓解竞争。</p><p>它没有改变一个事实：</p><blockquote><p>所有线程最终仍然在竞争同一个锁变量缓存行。</p></blockquote><p>所以它不是根本解决方案。</p><hr><h2 id="10-MCS-锁：改变等待方式"><a href="#10-MCS-锁：改变等待方式" class="headerlink" title="10. MCS 锁：改变等待方式"></a>10. MCS 锁：改变等待方式</h2><h3 id="10-1-MCS-锁是什么？"><a href="#10-1-MCS-锁是什么？" class="headerlink" title="10.1 MCS 锁是什么？"></a>10.1 MCS 锁是什么？</h3><p>MCS 锁是一种更可扩展的锁。</p><p>它的核心思想是：</p><blockquote><p>不要让所有等待者都盯着同一个锁变量，而是让每个等待者在自己的节点上等待。</p></blockquote><hr><h3 id="10-2-普通自旋锁-vs-MCS-锁"><a href="#10-2-普通自旋锁-vs-MCS-锁" class="headerlink" title="10.2 普通自旋锁 vs MCS 锁"></a>10.2 普通自旋锁 vs MCS 锁</h3><p>普通自旋锁像这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">所有人都抢同一个门把手</span><br></pre></td></tr></table></figure><p>MCS 锁像这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">所有人排队</span><br><span class="line">前一个人出来后通知后一个人</span><br></pre></td></tr></table></figure><hr><h3 id="10-3-MCS-锁的流程"><a href="#10-3-MCS-锁的流程" class="headerlink" title="10.3 MCS 锁的流程"></a>10.3 MCS 锁的流程</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[线程申请锁] --&gt; B[创建自己的 MCS_node]    B --&gt; C[加入等待队列尾部]    C --&gt; D{前面是否有人?}    D -- 没有 --&gt; E[直接获得锁]    D -- 有 --&gt; F[在自己的节点上自旋等待]    F --&gt; G[前驱释放锁时通知自己]    G --&gt; E    E --&gt; H[进入临界区]    H --&gt; I[释放锁并通知后继]  </pre></div><p>关键好处：</p><ul><li>等待者主要访问自己的节点。</li><li>不再所有核心反复写同一个锁变量。</li><li>缓存一致性冲突显著减少。</li><li>高竞争下可扩展性更好。</li></ul><hr><h3 id="10-4-MCS-锁不是取消互斥"><a href="#10-4-MCS-锁不是取消互斥" class="headerlink" title="10.4 MCS 锁不是取消互斥"></a>10.4 MCS 锁不是取消互斥</h3><p>MCS 锁仍然是锁。</p><p>它没有让多个线程同时进入临界区，只是优化了等待方式：</p><table><thead><tr><th>锁类型</th><th>等待方式</th></tr></thead><tbody><tr><td>普通自旋锁</td><td>所有人反复 CAS 同一个变量</td></tr><tr><td>Back-off 锁</td><td>失败后等一会儿再抢</td></tr><tr><td>MCS 锁</td><td>排队，在自己的节点上等待</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：MCS-锁的核心改进不是取消锁，而是改变等待结构，避免所有线程同时竞争同一个缓存行。"><a href="#❗-本节核心结论：MCS-锁的核心改进不是取消锁，而是改变等待结构，避免所有线程同时竞争同一个缓存行。" class="headerlink" title="❗ 本节核心结论：MCS 锁的核心改进不是取消锁，而是改变等待结构，避免所有线程同时竞争同一个缓存行。"></a><strong>❗ 本节核心结论：</strong><br>MCS 锁的核心改进不是取消锁，而是改变等待结构，避免所有线程同时竞争同一个缓存行。</h2><h2 id="11-QSpinlock"><a href="#11-QSpinlock" class="headerlink" title="11. QSpinlock"></a>11. QSpinlock</h2><p>QSpinlock 可以理解为结合两类路径：</p><table><thead><tr><th>路径</th><th>适用情况</th><th>思想</th></tr></thead><tbody><tr><td>快速路径</td><td>低竞争</td><td>类似普通自旋锁，简单快速</td></tr><tr><td>慢速路径</td><td>高竞争</td><td>类似 MCS 锁，排队等待</td></tr></tbody></table><p>它体现了一个工程设计原则：</p><blockquote><p>低竞争时不要为了复杂性付出过多代价；高竞争时必须避免单一缓存行竞争。</p></blockquote><hr><h2 id="12-虚假共享-False-Sharing"><a href="#12-虚假共享-False-Sharing" class="headerlink" title="12. 虚假共享 False Sharing"></a>12. 虚假共享 False Sharing</h2><h3 id="12-1-什么是虚假共享？"><a href="#12-1-什么是虚假共享？" class="headerlink" title="12.1 什么是虚假共享？"></a>12.1 什么是虚假共享？</h3><p>虚假共享是指：</p><blockquote><p>多个线程访问的是不同变量，但这些变量恰好位于同一个缓存行，于是硬件把它们当成同一个一致性单位处理。</p></blockquote><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> a;  <span class="comment">// thread_0 写</span></span><br><span class="line">    <span class="type">int</span> b;  <span class="comment">// thread_1 写</span></span><br><span class="line">&#125; data;</span><br></pre></td></tr></table></figure><p>逻辑上：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">thread_0 只写 a</span><br><span class="line">thread_1 只写 b</span><br></pre></td></tr></table></figure><p>它们没有共享同一个变量。</p><p>但如果 <code>a</code> 和 <code>b</code> 位于同一个缓存行，那么硬件会以缓存行为单位维护一致性。</p><hr><h3 id="12-2-为什么虚假共享很危险？"><a href="#12-2-为什么虚假共享很危险？" class="headerlink" title="12.2 为什么虚假共享很危险？"></a>12.2 为什么虚假共享很危险？</h3><p>因为缓存一致性的单位是缓存行，而不是变量。</p><p>如果 <code>a</code> 和 <code>b</code> 在同一个缓存行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">thread_0 写 a</span><br><span class="line">    ↓</span><br><span class="line">获得该缓存行独占权限</span><br><span class="line">    ↓</span><br><span class="line">thread_1 的缓存行副本失效</span><br><span class="line"></span><br><span class="line">thread_1 写 b</span><br><span class="line">    ↓</span><br><span class="line">又获得该缓存行独占权限</span><br><span class="line">    ↓</span><br><span class="line">thread_0 的缓存行副本失效</span><br></pre></td></tr></table></figure><p>于是两个线程虽然操作不同变量，却不断互相干扰。</p><hr><h3 id="12-3-如何避免虚假共享？"><a href="#12-3-如何避免虚假共享？" class="headerlink" title="12.3 如何避免虚假共享？"></a>12.3 如何避免虚假共享？</h3><p>可以使用 padding 或 cache-line alignment：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">    <span class="type">char</span> padding[<span class="number">64</span>];</span><br><span class="line">    <span class="type">int</span> b;</span><br><span class="line">&#125; data;</span><br></pre></td></tr></table></figure><p>目标是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">让不同线程高频写入的变量落到不同缓存行</span><br></pre></td></tr></table></figure><p>这不是为了改变程序逻辑，而是为了改变硬件缓存布局。</p><hr><h2 id="❗-本节核心结论：不同变量不代表不会互相影响。只要它们落在同一个缓存行，多核写入时就可能触发无谓的一致性通信，这就是虚假共享。"><a href="#❗-本节核心结论：不同变量不代表不会互相影响。只要它们落在同一个缓存行，多核写入时就可能触发无谓的一致性通信，这就是虚假共享。" class="headerlink" title="❗ 本节核心结论：不同变量不代表不会互相影响。只要它们落在同一个缓存行，多核写入时就可能触发无谓的一致性通信，这就是虚假共享。"></a><strong>❗ 本节核心结论：</strong><br>不同变量不代表不会互相影响。只要它们落在同一个缓存行，多核写入时就可能触发无谓的一致性通信，这就是虚假共享。</h2><h2 id="13-死锁预防回顾"><a href="#13-死锁预防回顾" class="headerlink" title="13. 死锁预防回顾"></a>13. 死锁预防回顾</h2><p>本讲后半部分也回顾了死锁预防。</p><p>死锁的四个必要条件：</p><table><thead><tr><th>条件</th><th>含义</th></tr></thead><tbody><tr><td>互斥访问</td><td>一个资源同一时刻只能被一个线程使用</td></tr><tr><td>持有并等待</td><td>已经持有一些资源，还在等待其他资源</td></tr><tr><td>资源非抢占</td><td>已经被占有的资源不能被强行抢走</td></tr><tr><td>循环等待</td><td>线程之间形成首尾相接的等待链</td></tr></tbody></table><p>死锁预防就是破坏其中至少一个条件。</p><p>最常见的方法是：</p><blockquote><p>给资源编号，所有线程按固定顺序申请资源。</p></blockquote><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A &lt; B &lt; C</span><br></pre></td></tr></table></figure><p>所有线程都只能按：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A → B → C</span><br></pre></td></tr></table></figure><p>申请锁，不能先拿 B 再拿 A。</p><p>这样等待关系只能朝一个方向走，无法形成环。</p><hr><h2 id="14-乱序执行"><a href="#14-乱序执行" class="headerlink" title="14. 乱序执行"></a>14. 乱序执行</h2><h3 id="14-1-乱序执行是什么？"><a href="#14-1-乱序执行是什么？" class="headerlink" title="14.1 乱序执行是什么？"></a>14.1 乱序执行是什么？</h3><p>乱序执行指的是：</p><blockquote><p>CPU 实际完成指令的顺序，可能和程序中看到的指令顺序不同。</p></blockquote><p>例如程序写着：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">x = <span class="number">1</span>;</span><br><span class="line">y = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>单线程视角下，CPU 会尽力保证结果看起来像顺序执行。</p><p>但在多线程场景中，另一个核心可能观察到复杂情况：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">看起来 y 先变化</span><br><span class="line">x 后变化</span><br></pre></td></tr></table></figure><p>这会引出后续的内存一致性模型问题。</p><hr><h3 id="14-2-CPU-为什么要乱序执行？"><a href="#14-2-CPU-为什么要乱序执行？" class="headerlink" title="14.2 CPU 为什么要乱序执行？"></a>14.2 CPU 为什么要乱序执行？</h3><p>因为不同指令耗时不同：</p><ul><li>有的指令要等内存。</li><li>有的指令只做寄存器运算。</li><li>有的指令依赖前一条结果。</li><li>有的指令互不依赖。</li></ul><p>如果 CPU 严格按程序顺序等待，流水线资源会被浪费。</p><p>所以 CPU 会：</p><ul><li>能先执行的先执行。</li><li>不依赖前面结果的指令先执行。</li><li>尽量让流水线保持忙碌。</li></ul><p>乱序执行的目的不是“乱来”，而是提升指令级并行性。</p><hr><h3 id="14-3-乱序执行为什么影响多核？"><a href="#14-3-乱序执行为什么影响多核？" class="headerlink" title="14.3 乱序执行为什么影响多核？"></a>14.3 乱序执行为什么影响多核？</h3><p>单线程中，CPU 通常保证程序表现得像顺序执行。</p><p>但多线程中，问题变成：</p><blockquote><p>一个核心的写入顺序，其他核心是否也按同样顺序看到？</p></blockquote><p>这就从“缓存一致性”进一步走向“内存一致性”。</p><table><thead><tr><th>概念</th><th>关注点</th></tr></thead><tbody><tr><td>缓存一致性 Cache Coherence</td><td>同一个地址的值是否一致</td></tr><tr><td>内存一致性 Memory Consistency</td><td>多个内存操作的可见顺序是否一致</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：乱序执行不是-CPU-随便改程序，而是在不破坏单线程语义的前提下提高效率。但在多线程中，不同核心观察内存操作的顺序会变复杂，因此需要内存一致性模型约束。"><a href="#❗-本节核心结论：乱序执行不是-CPU-随便改程序，而是在不破坏单线程语义的前提下提高效率。但在多线程中，不同核心观察内存操作的顺序会变复杂，因此需要内存一致性模型约束。" class="headerlink" title="❗ 本节核心结论：乱序执行不是 CPU 随便改程序，而是在不破坏单线程语义的前提下提高效率。但在多线程中，不同核心观察内存操作的顺序会变复杂，因此需要内存一致性模型约束。"></a><strong>❗ 本节核心结论：</strong><br>乱序执行不是 CPU 随便改程序，而是在不破坏单线程语义的前提下提高效率。但在多线程中，不同核心观察内存操作的顺序会变复杂，因此需要内存一致性模型约束。</h2><h2 id="15-重要对比"><a href="#15-重要对比" class="headerlink" title="15. 重要对比"></a>15. 重要对比</h2><h3 id="15-1-缓存一致性-vs-内存一致性"><a href="#15-1-缓存一致性-vs-内存一致性" class="headerlink" title="15.1 缓存一致性 vs 内存一致性"></a>15.1 缓存一致性 vs 内存一致性</h3><table><thead><tr><th>对比点</th><th>缓存一致性</th><th>内存一致性</th></tr></thead><tbody><tr><td>英文</td><td>Cache Coherence</td><td>Memory Consistency</td></tr><tr><td>关注对象</td><td>同一个地址的数据值</td><td>多个内存操作的可见顺序</td></tr><tr><td>典型问题</td><td>CPU0 和 CPU1 读同一个地址是否一致</td><td>CPU0 先写 x 再写 y，CPU1 是否也按这个顺序看到</td></tr><tr><td>核心单位</td><td>缓存行</td><td>内存操作顺序</td></tr><tr><td>关键词</td><td>Modified &#x2F; Shared &#x2F; Invalid</td><td>乱序执行、重排、内存屏障</td></tr></tbody></table><hr><h3 id="15-2-自旋锁-vs-Back-off-锁-vs-MCS-锁"><a href="#15-2-自旋锁-vs-Back-off-锁-vs-MCS-锁" class="headerlink" title="15.2 自旋锁 vs Back-off 锁 vs MCS 锁"></a>15.2 自旋锁 vs Back-off 锁 vs MCS 锁</h3><table><thead><tr><th>对比点</th><th>普通自旋锁</th><th>Back-off 锁</th><th>MCS 锁</th></tr></thead><tbody><tr><td>等待方式</td><td>失败后立刻继续 CAS</td><td>失败后等待一段时间</td><td>排队等待</td></tr><tr><td>竞争位置</td><td>同一个锁变量缓存行</td><td>仍是同一个缓存行，但频率降低</td><td>各自在本地节点上等待</td></tr><tr><td>实现复杂度</td><td>低</td><td>中</td><td>高</td></tr><tr><td>低竞争性能</td><td>好</td><td>可能略有额外等待</td><td>不一定最优</td></tr><tr><td>高竞争性能</td><td>差，容易断崖</td><td>有缓解</td><td>更可扩展</td></tr><tr><td>核心思想</td><td>忙等</td><td>降低抢锁频率</td><td>避免单一缓存行高度竞争</td></tr></tbody></table><hr><h3 id="15-3-真实共享-vs-虚假共享"><a href="#15-3-真实共享-vs-虚假共享" class="headerlink" title="15.3 真实共享 vs 虚假共享"></a>15.3 真实共享 vs 虚假共享</h3><table><thead><tr><th>对比点</th><th>真实共享</th><th>虚假共享</th></tr></thead><tbody><tr><td>是否访问同一变量</td><td>是</td><td>否</td></tr><tr><td>是否在同一缓存行</td><td>通常是</td><td>是</td></tr><tr><td>逻辑上是否共享数据</td><td>是</td><td>否</td></tr><tr><td>硬件上是否触发一致性通信</td><td>会</td><td>也会</td></tr><tr><td>例子</td><td>多线程都写 <code>counter</code></td><td>线程 A 写 <code>a</code>，线程 B 写 <code>b</code>，但 <code>a/b</code> 在同一缓存行</td></tr><tr><td>解决方向</td><td>减少共享、分片、换锁设计</td><td>padding、cache-line alignment</td></tr></tbody></table><hr><h2 id="16-最容易混淆的点"><a href="#16-最容易混淆的点" class="headerlink" title="16. 最容易混淆的点"></a>16. 最容易混淆的点</h2><h3 id="16-1-缓存一致性不是没有性能代价"><a href="#16-1-缓存一致性不是没有性能代价" class="headerlink" title="16.1 缓存一致性不是没有性能代价"></a>16.1 缓存一致性不是没有性能代价</h3><p>缓存一致性保证正确性，但会带来：</p><ul><li>状态迁移</li><li>失效通知</li><li>目录查询</li><li>数据转发</li><li>权限确认</li></ul><p>所以不能理解成：</p><blockquote><p>有了缓存一致性，多核共享数据就和单核一样便宜。</p></blockquote><hr><h3 id="16-2-缓存一致性不是只在-LLC-中考虑"><a href="#16-2-缓存一致性不是只在-LLC-中考虑" class="headerlink" title="16.2 缓存一致性不是只在 LLC 中考虑"></a>16.2 缓存一致性不是只在 LLC 中考虑</h3><p>真正麻烦的是：</p><blockquote><p>多个核心的私有缓存中可能有同一个缓存行的副本。</p></blockquote><p>LLC 共享不代表问题消失。只要 L1 &#x2F; L2 中有私有副本，就需要一致性协议协调。</p><hr><h3 id="16-3-不同变量也可能互相拖慢"><a href="#16-3-不同变量也可能互相拖慢" class="headerlink" title="16.3 不同变量也可能互相拖慢"></a>16.3 不同变量也可能互相拖慢</h3><p>如果两个变量位于同一个缓存行，那么硬件会把它们作为一个一致性单位处理。</p><p>变量级别看是独立的，缓存行级别看可能是共享的。</p><hr><h3 id="16-4-自旋锁的问题不只是浪费-CPU"><a href="#16-4-自旋锁的问题不只是浪费-CPU" class="headerlink" title="16.4 自旋锁的问题不只是浪费 CPU"></a>16.4 自旋锁的问题不只是浪费 CPU</h3><p>自旋锁忙等会浪费 CPU，但更严重的是：</p><blockquote><p>多个核心反复争抢锁变量所在缓存行，导致一致性通信暴增。</p></blockquote><hr><h3 id="16-5-Back-off-不是根本解决方案"><a href="#16-5-Back-off-不是根本解决方案" class="headerlink" title="16.5 Back-off 不是根本解决方案"></a>16.5 Back-off 不是根本解决方案</h3><p>Back-off 只是让线程“别抢得太频繁”。</p><p>它减少竞争频率，但没有改变所有线程仍然抢同一个锁变量的事实。</p><hr><h3 id="16-6-MCS-锁不是不用锁"><a href="#16-6-MCS-锁不是不用锁" class="headerlink" title="16.6 MCS 锁不是不用锁"></a>16.6 MCS 锁不是不用锁</h3><p>MCS 锁仍然保证互斥。</p><p>它优化的是等待方式，不是取消临界区互斥。</p><hr><h3 id="16-7-乱序执行不是-CPU-乱来"><a href="#16-7-乱序执行不是-CPU-乱来" class="headerlink" title="16.7 乱序执行不是 CPU 乱来"></a>16.7 乱序执行不是 CPU 乱来</h3><p>乱序执行是在不破坏单线程可观察语义的前提下提高效率。</p><p>问题出现在多线程中：不同核心之间观察内存操作顺序会变复杂。</p><hr><h2 id="17-本讲一句话总结"><a href="#17-本讲一句话总结" class="headerlink" title="17. 本讲一句话总结"></a>17. 本讲一句话总结</h2><p>多核与多处理器的核心矛盾是：核心数增加带来并行能力，但共享数据、私有缓存和锁竞争会引入正确性与性能问题。缓存一致性协议通过 MSI 状态和目录项维护同一地址的数据共识，但高频竞争同一缓存行会导致一致性通信暴增，进而出现可扩展性断崖。因此系统软件需要用 Back-off、MCS 锁、QSpinlock、padding、cache-line alignment 等方法降低核心之间的协调成本。</p><hr><h2 id="18-自测题"><a href="#18-自测题" class="headerlink" title="18. 自测题"></a>18. 自测题</h2><ol><li>为什么不能简单认为“CPU 核心数增加，程序性能就一定线性提升”？</li><li>加速比 Speedup 的定义是什么？</li><li>Amdahl 定律中的 <code>p</code> 表示什么？</li><li>为什么串行部分会限制理论最大加速比？</li><li>多核环境下，为什么每个核心拥有私有 L1 Cache 会带来数据一致性问题？</li><li>缓存一致性要解决的核心问题是什么？</li><li>缓存一致性和“程序跑得快”是同一个问题吗？</li><li>MSI 协议中的 Modified、Shared、Invalid 分别表示什么？</li><li><code>Invalid</code> 是全局状态，还是某个核心本地缓存副本的状态？</li><li>为什么写共享变量通常比读共享变量更容易触发一致性通信？</li><li>目录式缓存一致性中的脏位和向量位分别记录什么？</li><li>为什么目录式一致性可以减少广播成本？</li><li>什么是一致性通信？</li><li>普通自旋锁为什么会导致可扩展性断崖？</li><li>为什么自旋锁的问题不只是“while 循环浪费 CPU”？</li><li>Back-off 策略为什么能缓解自旋锁竞争？</li><li>为什么固定等待时间的 Back-off 可能效果不好？</li><li>MCS 锁相比普通自旋锁的核心改进是什么？</li><li>为什么说 MCS 锁优化的是等待方式，而不是取消互斥？</li><li>QSpinlock 为什么要区分快速路径和慢速路径？</li><li>什么是虚假共享？</li><li>为什么两个不同变量也可能互相拖慢性能？</li><li>padding 和 cache-line alignment 为什么能缓解虚假共享？</li><li>死锁产生的四个必要条件是什么？</li><li>打破循环等待为什么可以预防死锁？</li><li>乱序执行为什么被设计出来？</li><li>乱序执行为什么会为后续内存一致性模型埋下问题？</li></ol><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>本讲可以压缩成一条核心链路：</p><blockquote><p>多核增加并行能力，但私有缓存让同一地址出现多个副本；缓存一致性保证正确性，却带来一致性通信；当多个核心高频争抢同一个缓存行时，性能会出现可扩展性断崖；因此需要从锁设计、数据布局和内存顺序三个层面降低协调成本。</p></blockquote><p>重点记住：</p><ul><li><strong>❗ 多核不是免费加速</strong>：串行部分、锁竞争、缓存通信都会限制加速。</li><li><strong>❗ Amdahl 定律</strong>：串行部分决定理论加速上限。</li><li><strong>❗ 缓存一致性</strong>：解决同一地址的数据共识问题。</li><li><strong>❗ MSI 协议</strong>：读可共享，写要独占，旧副本要失效。</li><li><strong>❗ 目录式一致性</strong>：记录谁拥有缓存行，只通知相关核心。</li><li><strong>❗ 可扩展性断崖</strong>：核心越多，竞争越严重，性能反而下降。</li><li><strong>❗ MCS 锁</strong>：排队等待，避免所有线程争抢同一缓存行。</li><li><strong>❗ 虚假共享</strong>：不同变量落在同一缓存行，也会互相拖慢。</li><li><strong>❗ 乱序执行</strong>：提高单核效率，但让多线程内存可见顺序更复杂。</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第16讲 同步原语III</title>
      <link href="/2026/06/11/%E7%AC%AC16%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADIII/"/>
      <url>/2026/06/11/%E7%AC%AC16%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADIII/</url>
      
        <content type="html"><![CDATA[<p>以下根据附件《第16讲 同步原语III.md》整理生成。</p><h1 id="第16讲：同步原语-III-学习指南"><a href="#第16讲：同步原语-III-学习指南" class="headerlink" title="第16讲：同步原语 III 学习指南"></a>第16讲：同步原语 III 学习指南</h1><p>本讲讨论的是：<strong>同步原语本身可能带来的问题</strong>。</p><p>前面学习的互斥锁、条件变量、信号量、读写锁、RCU 都是为了让多个线程安全共享资源。但只要线程开始“持有资源”“等待资源”，系统就可能出现新的并发问题：</p><ul><li>死锁</li><li>活锁</li><li>优先级反转</li></ul><hr><h2 id="❗-本节核心结论：同步原语解决了共享资源竞争的正确性问题，但如果资源申请顺序、等待方式和调度优先级设计不好，就会产生死锁、活锁、优先级反转等问题。"><a href="#❗-本节核心结论：同步原语解决了共享资源竞争的正确性问题，但如果资源申请顺序、等待方式和调度优先级设计不好，就会产生死锁、活锁、优先级反转等问题。" class="headerlink" title="❗ 本节核心结论：同步原语解决了共享资源竞争的正确性问题，但如果资源申请顺序、等待方式和调度优先级设计不好，就会产生死锁、活锁、优先级反转等问题。"></a><strong>❗ 本节核心结论：</strong><br>同步原语解决了共享资源竞争的正确性问题，但如果资源申请顺序、等待方式和调度优先级设计不好，就会产生死锁、活锁、优先级反转等问题。</h2><h2 id="1-本章知识地图"><a href="#1-本章知识地图" class="headerlink" title="1. 本章知识地图"></a>1. 本章知识地图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[同步原语] --&gt; B[多个线程竞争多个资源]    B --&gt; C[死锁 Deadlock]    C --&gt; D[检测与恢复]    C --&gt; E[死锁预防]    C --&gt; F[死锁避免]        E --&gt; G[破坏四个必要条件]    F --&gt; H[银行家算法]        E --&gt; I[trylock 方案]    I --&gt; J[活锁 Livelock]        A --&gt; K[锁 + 优先级调度]    K --&gt; L[优先级反转]    L --&gt; M[优先级继承协议]  </pre></div><p>这张图的主线是：<strong>同步原语不是问题的终点，而是并发控制的起点</strong>。一旦线程开始持有锁、等待锁，就必须处理死锁、活锁和调度优先级问题。</p><hr><h2 id="2-死锁：线程互相等待，系统无法推进"><a href="#2-死锁：线程互相等待，系统无法推进" class="headerlink" title="2. 死锁：线程互相等待，系统无法推进"></a>2. 死锁：线程互相等待，系统无法推进</h2><h3 id="2-1-死锁是什么？"><a href="#2-1-死锁是什么？" class="headerlink" title="2.1 死锁是什么？"></a>2.1 死锁是什么？</h3><p>死锁是指：</p><blockquote><p>一组线程或进程互相等待对方持有的资源，导致所有相关线程都无法继续执行。</p></blockquote><p>典型例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">proc_A</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    lock(A);</span><br><span class="line">    lock(B);</span><br><span class="line">    <span class="comment">// 临界区</span></span><br><span class="line">    unlock(B);</span><br><span class="line">    unlock(A);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">proc_B</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    lock(B);</span><br><span class="line">    lock(A);</span><br><span class="line">    <span class="comment">// 临界区</span></span><br><span class="line">    unlock(A);</span><br><span class="line">    unlock(B);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可能发生：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">proc_A 已经拿到锁 A，等待锁 B</span><br><span class="line">proc_B 已经拿到锁 B，等待锁 A</span><br></pre></td></tr></table></figure><p>于是：</p><ul><li>A 等 B</li><li>B 等 A</li><li>两个线程都无法继续执行</li></ul><p>这就是死锁。</p><hr><h3 id="2-2-哲学家问题中的死锁"><a href="#2-2-哲学家问题中的死锁" class="headerlink" title="2.2 哲学家问题中的死锁"></a>2.2 哲学家问题中的死锁</h3><p>哲学家问题中，每个哲学家需要同时拿到左右两支筷子才能吃饭。</p><p>如果所有哲学家都执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先拿左手筷子</span><br><span class="line">再等右手筷子</span><br></pre></td></tr></table></figure><p>那么可能出现：</p><ul><li>每个人都拿到了左边筷子</li><li>每个人都在等待右边筷子</li><li>右边筷子又被旁边的人拿着</li><li>所有人都无法吃饭</li></ul><p>这就是一个环形等待。</p><hr><h3 id="2-3-死锁的四个必要条件"><a href="#2-3-死锁的四个必要条件" class="headerlink" title="2.3 死锁的四个必要条件"></a>2.3 死锁的四个必要条件</h3><p>死锁产生必须同时满足四个条件：</p><table><thead><tr><th>条件</th><th>含义</th><th>在锁中的体现</th></tr></thead><tbody><tr><td>互斥访问</td><td>一个资源同一时刻只能被一个线程使用</td><td>一把锁只能被一个线程持有</td></tr><tr><td>持有并等待</td><td>线程已经持有资源，同时等待其他资源</td><td>拿着 A 锁等待 B 锁</td></tr><tr><td>资源非抢占</td><td>已经被占有的资源不能被强行夺走</td><td>不能强制抢走别人手里的锁</td></tr><tr><td>循环等待</td><td>多个线程形成首尾相接的等待链</td><td>A 等 B，B 等 A</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：死锁的本质不是“等得久”，而是形成了循环等待。解决死锁的核心思路，就是破坏四个必要条件中的至少一个。"><a href="#❗-本节核心结论：死锁的本质不是“等得久”，而是形成了循环等待。解决死锁的核心思路，就是破坏四个必要条件中的至少一个。" class="headerlink" title="❗ 本节核心结论：死锁的本质不是“等得久”，而是形成了循环等待。解决死锁的核心思路，就是破坏四个必要条件中的至少一个。"></a><strong>❗ 本节核心结论：</strong><br>死锁的本质不是“等得久”，而是形成了循环等待。解决死锁的核心思路，就是破坏四个必要条件中的至少一个。</h2><h2 id="3-死锁检测与恢复"><a href="#3-死锁检测与恢复" class="headerlink" title="3. 死锁检测与恢复"></a>3. 死锁检测与恢复</h2><h3 id="3-1-基本思想"><a href="#3-1-基本思想" class="headerlink" title="3.1 基本思想"></a>3.1 基本思想</h3><p>死锁检测与恢复的思路是：</p><blockquote><p>不提前禁止死锁，而是在系统运行中检测死锁；一旦发现死锁，再采取恢复措施。</p></blockquote><p>这是一种“出问题后处理”的方案。</p><hr><h3 id="3-2-资源分配图"><a href="#3-2-资源分配图" class="headerlink" title="3.2 资源分配图"></a>3.2 资源分配图</h3><p>资源分配图中有两类节点：</p><table><thead><tr><th>节点</th><th>含义</th></tr></thead><tbody><tr><td><code>P</code></td><td>线程 &#x2F; 进程</td></tr><tr><td><code>O</code></td><td>资源</td></tr></tbody></table><p>有两类边：</p><table><thead><tr><th>边</th><th>含义</th></tr></thead><tbody><tr><td>资源 → 线程</td><td>资源已经分配给该线程</td></tr><tr><td>线程 → 资源</td><td>线程正在请求该资源</td></tr></tbody></table><p>例如：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    P1[P1] --&gt; O2[O2]    O2 --&gt; P3[P3]    P3 --&gt; O3[O3]    O3 --&gt; P4[P4]    P4 --&gt; O1[O1]    O1 --&gt; P1  </pre></div><p>这个环表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">P1 等 O2</span><br><span class="line">O2 被 P3 持有</span><br><span class="line">P3 等 O3</span><br><span class="line">O3 被 P4 持有</span><br><span class="line">P4 等 O1</span><br><span class="line">O1 被 P1 持有</span><br></pre></td></tr></table></figure><p>于是形成循环等待。</p><hr><h3 id="3-3-如何恢复？"><a href="#3-3-如何恢复？" class="headerlink" title="3.3 如何恢复？"></a>3.3 如何恢复？</h3><p>检测到死锁后，常见恢复方式有三种：</p><table><thead><tr><th>恢复方式</th><th>做法</th><th>优点</th><th>代价</th></tr></thead><tbody><tr><td>Kill 所有死锁线程</td><td>直接终止环中的所有线程</td><td>简单直接</td><td>损失最大</td></tr><tr><td>Kill 一个线程后重新检测</td><td>每次杀掉一个，再判断是否解除死锁</td><td>相对保守</td><td>可能需要多轮</td></tr><tr><td>回滚到旧状态</td><td>把线程恢复到之前的安全状态</td><td>尽量保留成果</td><td>需要支持状态保存与恢复</td></tr></tbody></table><p>检测与恢复的问题是：<strong>恢复成本通常很高</strong>。</p><hr><h2 id="❗-本节核心结论：死锁检测与恢复允许死锁发生，再通过资源分配图找环并处理。但它更像事故处理机制，代价通常较大。"><a href="#❗-本节核心结论：死锁检测与恢复允许死锁发生，再通过资源分配图找环并处理。但它更像事故处理机制，代价通常较大。" class="headerlink" title="❗ 本节核心结论：死锁检测与恢复允许死锁发生，再通过资源分配图找环并处理。但它更像事故处理机制，代价通常较大。"></a><strong>❗ 本节核心结论：</strong><br>死锁检测与恢复允许死锁发生，再通过资源分配图找环并处理。但它更像事故处理机制，代价通常较大。</h2><h2 id="4-死锁预防：提前破坏死锁条件"><a href="#4-死锁预防：提前破坏死锁条件" class="headerlink" title="4. 死锁预防：提前破坏死锁条件"></a>4. 死锁预防：提前破坏死锁条件</h2><p>死锁预防的核心思想是：</p><blockquote><p>让死锁四个必要条件不可能同时成立。</p></blockquote><p>因为死锁需要四个条件同时满足，所以只要破坏其中一个，就可以避免死锁。</p><hr><h3 id="4-1-破坏互斥访问"><a href="#4-1-破坏互斥访问" class="headerlink" title="4.1 破坏互斥访问"></a>4.1 破坏互斥访问</h3><p>思路是：</p><blockquote><p>不让多个线程直接竞争资源，而是让代理线程统一执行临界区。</p></blockquote><p>普通锁模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">线程自己进入临界区，直接修改共享资源</span><br></pre></td></tr></table></figure><p>代理执行模式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">线程提交请求</span><br><span class="line">代理线程统一执行临界区代码</span><br><span class="line">再把结果返回给对应线程</span><br></pre></td></tr></table></figure><p>这种方式可以减少直接竞争，但代价是：</p><ul><li>程序结构需要大改</li><li>不是所有临界区都适合代理执行</li><li>工程适用范围有限</li></ul><hr><h3 id="4-2-破坏持有并等待"><a href="#4-2-破坏持有并等待" class="headerlink" title="4.2 破坏持有并等待"></a>4.2 破坏持有并等待</h3><p>思路是：</p><blockquote><p>要么一次性拿到所有资源，要么一个都不拿。</p></blockquote><p>典型做法是 <code>trylock()</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (trylock(A) == SUCC) &#123;</span><br><span class="line">        <span class="keyword">if</span> (trylock(B) == SUCC) &#123;</span><br><span class="line">            <span class="comment">// 临界区</span></span><br><span class="line">            unlock(B);</span><br><span class="line">            unlock(A);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            unlock(A);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的关键是：</p><ul><li>如果拿到了 A，但拿不到 B，就立刻释放 A。</li><li>线程不会一直“持有 A 等待 B”。</li><li>因此破坏了“持有并等待”。</li></ul><p>但这个方法可能引出新问题：<strong>活锁</strong>。</p><hr><h3 id="4-3-破坏资源非抢占"><a href="#4-3-破坏资源非抢占" class="headerlink" title="4.3 破坏资源非抢占"></a>4.3 破坏资源非抢占</h3><p>思路是：</p><blockquote><p>如果线程等不到资源，系统可以从其他线程那里抢回资源。</p></blockquote><p>但这在普通锁里很难，因为：</p><ul><li>被抢占的线程可能已经修改了一半共享状态。</li><li>系统必须能保存、回滚、恢复线程状态。</li><li>很多临界区操作不可安全回滚。</li></ul><p>所以资源抢占更适合某些可以回滚的资源管理场景，不适合简单粗暴地用于所有锁。</p><hr><h3 id="4-4-破坏循环等待"><a href="#4-4-破坏循环等待" class="headerlink" title="4.4 破坏循环等待"></a>4.4 破坏循环等待</h3><p>这是最常见、最实用的方法。</p><p>思路是：</p><blockquote><p>给所有资源编号，所有线程必须按照统一顺序申请资源。</p></blockquote><p>例如规定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A &lt; B &lt; C &lt; D</span><br></pre></td></tr></table></figure><p>所有线程必须按：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A → B → C → D</span><br></pre></td></tr></table></figure><p>这个顺序加锁，不能有人先拿 B 再拿 A。</p><p>这样等待关系只能从小编号资源指向大编号资源，无法绕回去形成环。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[锁 A] --&gt; B[锁 B]    B --&gt; C[锁 C]    C --&gt; D[锁 D]  </pre></div><p>只要所有线程都按递增顺序拿锁，就不会出现 <code>D 等 A</code> 这种回边，因此无法形成循环等待。</p><hr><h2 id="❗-本节核心结论：工程上最常用的死锁预防方法，是统一规定加锁顺序。它通过打破循环等待条件来避免死锁。"><a href="#❗-本节核心结论：工程上最常用的死锁预防方法，是统一规定加锁顺序。它通过打破循环等待条件来避免死锁。" class="headerlink" title="❗ 本节核心结论：工程上最常用的死锁预防方法，是统一规定加锁顺序。它通过打破循环等待条件来避免死锁。"></a><strong>❗ 本节核心结论：</strong><br>工程上最常用的死锁预防方法，是统一规定加锁顺序。它通过打破循环等待条件来避免死锁。</h2><h2 id="5-死锁避免：银行家算法"><a href="#5-死锁避免：银行家算法" class="headerlink" title="5. 死锁避免：银行家算法"></a>5. 死锁避免：银行家算法</h2><h3 id="5-1-死锁避免是什么？"><a href="#5-1-死锁避免是什么？" class="headerlink" title="5.1 死锁避免是什么？"></a>5.1 死锁避免是什么？</h3><p>死锁避免不是固定禁止某种行为，而是在每次资源分配时动态判断：</p><blockquote><p>如果现在把资源分配出去，系统未来是否仍然存在一种安全执行顺序？</p></blockquote><p>如果存在，就允许分配。</p><p>如果不存在，就暂时拒绝分配，让线程等待。</p><hr><h3 id="5-2-银行家算法的直觉"><a href="#5-2-银行家算法的直觉" class="headerlink" title="5.2 银行家算法的直觉"></a>5.2 银行家算法的直觉</h3><p>银行家算法可以类比银行贷款：</p><p>银行不能只看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">现在有没有钱借给这个客户？</span><br></pre></td></tr></table></figure><p>还要看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">借出去之后，银行是否仍然有能力让所有客户最终完成项目并还钱？</span><br></pre></td></tr></table></figure><p>对应到操作系统：</p><ul><li>银行的钱 &#x3D; 系统资源</li><li>客户 &#x3D; 线程 &#x2F; 进程</li><li>借钱 &#x3D; 申请资源</li><li>还钱 &#x3D; 线程执行完成后释放资源</li></ul><hr><h3 id="5-3-银行家算法需要的数据"><a href="#5-3-银行家算法需要的数据" class="headerlink" title="5.3 银行家算法需要的数据"></a>5.3 银行家算法需要的数据</h3><table><thead><tr><th>数据</th><th>含义</th></tr></thead><tbody><tr><td><code>Available</code></td><td>当前系统还剩多少可用资源</td></tr><tr><td><code>Max</code></td><td>每个线程最多可能需要多少资源</td></tr><tr><td><code>Allocation</code></td><td>每个线程已经获得多少资源</td></tr><tr><td><code>Need</code></td><td>每个线程还需要多少资源，<code>Need = Max - Allocation</code></td></tr></tbody></table><p>关键公式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Need = Max - Allocation</span><br></pre></td></tr></table></figure><hr><h3 id="5-4-安全性检查流程"><a href="#5-4-安全性检查流程" class="headerlink" title="5.4 安全性检查流程"></a>5.4 安全性检查流程</h3><p>银行家算法的安全性检查可以这样理解：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[创建 Work &#x3D; Available] --&gt; B[寻找未完成且 Need &lt;&#x3D; Work 的线程]    B --&gt; C{找到了吗?}    C -- 是 --&gt; D[假设该线程执行完成]    D --&gt; E[释放它的 Allocation 给 Work]    E --&gt; B    C -- 否 --&gt; F{所有线程都完成了吗?}    F -- 是 --&gt; G[安全状态]    F -- 否 --&gt; H[非安全状态]  </pre></div><p>具体步骤：</p><ol><li>令 <code>Work = Available</code>。</li><li>找一个还没完成、且 <code>Need &lt;= Work</code> 的线程。</li><li>假装它可以执行完成。</li><li>执行完成后，它释放已经占有的资源。</li><li>更新 <code>Work = Work + Allocation</code>。</li><li>重复寻找下一个可完成线程。</li><li>如果所有线程都能完成，说明存在安全序列。</li><li>如果中途找不到任何可执行线程，说明状态不安全。</li></ol><hr><h3 id="5-5-安全状态、非安全状态、死锁状态"><a href="#5-5-安全状态、非安全状态、死锁状态" class="headerlink" title="5.5 安全状态、非安全状态、死锁状态"></a>5.5 安全状态、非安全状态、死锁状态</h3><p>这三个概念很容易混淆。</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td>安全状态</td><td>存在一个安全序列，保证所有线程最终完成</td></tr><tr><td>非安全状态</td><td>不能保证所有线程最终完成，未来可能死锁</td></tr><tr><td>死锁状态</td><td>已经出现互相等待，线程无法推进</td></tr></tbody></table><p>关键区别：</p><blockquote><p>非安全状态不等于已经死锁。</p></blockquote><p>非安全状态只是说明：</p><ul><li>当前系统无法保证一定能避免死锁。</li><li>如果继续随意分配资源，可能走向死锁。</li></ul><hr><h2 id="❗-本节核心结论：银行家算法的核心不是“当前资源够不够”，而是“分配之后系统是否仍然安全”。安全性的判断标准是是否存在安全序列。"><a href="#❗-本节核心结论：银行家算法的核心不是“当前资源够不够”，而是“分配之后系统是否仍然安全”。安全性的判断标准是是否存在安全序列。" class="headerlink" title="❗ 本节核心结论：银行家算法的核心不是“当前资源够不够”，而是“分配之后系统是否仍然安全”。安全性的判断标准是是否存在安全序列。"></a><strong>❗ 本节核心结论：</strong><br>银行家算法的核心不是“当前资源够不够”，而是“分配之后系统是否仍然安全”。安全性的判断标准是是否存在安全序列。</h2><h2 id="6-活锁：一直在动，但没有进展"><a href="#6-活锁：一直在动，但没有进展" class="headerlink" title="6. 活锁：一直在动，但没有进展"></a>6. 活锁：一直在动，但没有进展</h2><h3 id="6-1-活锁是什么？"><a href="#6-1-活锁是什么？" class="headerlink" title="6.1 活锁是什么？"></a>6.1 活锁是什么？</h3><p>活锁是指：</p><blockquote><p>线程没有阻塞，也一直在运行、释放、重试，但系统整体没有实际推进。</p></blockquote><p>它和死锁不同。</p><p>死锁是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">线程阻塞不动，等待资源</span><br></pre></td></tr></table></figure><p>活锁是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">线程一直动，但不断互相让步 / 重试，始终无法完成</span><br></pre></td></tr></table></figure><hr><h3 id="6-2-trylock-为什么可能导致活锁？"><a href="#6-2-trylock-为什么可能导致活锁？" class="headerlink" title="6.2 trylock 为什么可能导致活锁？"></a>6.2 trylock 为什么可能导致活锁？</h3><p>前面说过，<code>trylock()</code> 可以避免“持有并等待”。</p><p>但如果两个线程总是同步地执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">拿 A</span><br><span class="line">拿 B 失败</span><br><span class="line">释放 A</span><br><span class="line">重试</span><br></pre></td></tr></table></figure><p>另一个线程也同步执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">拿 B</span><br><span class="line">拿 A 失败</span><br><span class="line">释放 B</span><br><span class="line">重试</span><br></pre></td></tr></table></figure><p>那么它们虽然没有阻塞，却一直无法进入临界区。</p><p>这就是活锁。</p><hr><h3 id="6-3-活锁和死锁对比"><a href="#6-3-活锁和死锁对比" class="headerlink" title="6.3 活锁和死锁对比"></a>6.3 活锁和死锁对比</h3><table><thead><tr><th>对比点</th><th>死锁</th><th>活锁</th></tr></thead><tbody><tr><td>线程状态</td><td>阻塞等待</td><td>不断运行 &#x2F; 重试</td></tr><tr><td>是否消耗 CPU</td><td>通常不持续消耗</td><td>可能持续消耗</td></tr><tr><td>是否有进展</td><td>没有</td><td>没有</td></tr><tr><td>典型原因</td><td>循环等待</td><td>双方不断退让或同步重试</td></tr><tr><td>解决方式</td><td>打破死锁条件</td><td>随机退避、打破同步节奏</td></tr></tbody></table><p>解决活锁的常见办法是：</p><ul><li>加入随机退避</li><li>打破所有线程同时重试的节奏</li><li>限制重试次数</li><li>改变资源申请策略</li></ul><hr><h2 id="❗-本节核心结论：活锁不是“卡住不动”，而是“一直在动但没有推进”。trylock-可以避免死锁，但如果所有线程同步重试，就可能产生活锁。"><a href="#❗-本节核心结论：活锁不是“卡住不动”，而是“一直在动但没有推进”。trylock-可以避免死锁，但如果所有线程同步重试，就可能产生活锁。" class="headerlink" title="❗ 本节核心结论：活锁不是“卡住不动”，而是“一直在动但没有推进”。trylock 可以避免死锁，但如果所有线程同步重试，就可能产生活锁。"></a><strong>❗ 本节核心结论：</strong><br>活锁不是“卡住不动”，而是“一直在动但没有推进”。trylock 可以避免死锁，但如果所有线程同步重试，就可能产生活锁。</h2><h2 id="7-优先级反转"><a href="#7-优先级反转" class="headerlink" title="7. 优先级反转"></a>7. 优先级反转</h2><h3 id="7-1-优先级反转是什么？"><a href="#7-1-优先级反转是什么？" class="headerlink" title="7.1 优先级反转是什么？"></a>7.1 优先级反转是什么？</h3><p>优先级反转是指：</p><blockquote><p>高优先级线程因为等待低优先级线程持有的锁，反而被中优先级线程间接阻塞。</p></blockquote><p>典型场景：</p><table><thead><tr><th>线程</th><th>优先级</th><th>状态</th></tr></thead><tbody><tr><td>P1</td><td>低优先级</td><td>持有锁 <code>I</code></td></tr><tr><td>P2</td><td>中优先级</td><td>不需要锁 <code>I</code></td></tr><tr><td>P3</td><td>高优先级</td><td>需要锁 <code>I</code></td></tr></tbody></table><p>执行过程：</p><ol><li>P1 获得锁 <code>I</code>。</li><li>P3 启动，想获得锁 <code>I</code>。</li><li>但锁 <code>I</code> 被 P1 持有，所以 P3 阻塞。</li><li>P2 是中优先级线程，不需要锁 <code>I</code>。</li><li>调度器让 P2 抢占 P1。</li><li>P1 不能运行，就不能释放锁。</li><li>P3 虽然优先级最高，却一直等不到锁。</li></ol><p>结果是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">P2 间接阻塞了 P3</span><br></pre></td></tr></table></figure><p>这就是优先级反转。</p><hr><h3 id="7-2-为什么这不是调度器简单出错？"><a href="#7-2-为什么这不是调度器简单出错？" class="headerlink" title="7.2 为什么这不是调度器简单出错？"></a>7.2 为什么这不是调度器简单出错？</h3><p>调度器并不是故意不执行高优先级线程。</p><p>真正原因是：</p><ul><li>P3 因为等锁而阻塞。</li><li>P1 才是释放锁的关键线程。</li><li>但 P1 优先级低，被 P2 抢占。</li><li>所以 P3 被 P2 间接拖住。</li></ul><p>本质上，这是：</p><blockquote><p>锁依赖关系和优先级调度机制发生冲突。</p></blockquote><hr><h3 id="7-3-优先级继承协议"><a href="#7-3-优先级继承协议" class="headerlink" title="7.3 优先级继承协议"></a>7.3 优先级继承协议</h3><p>解决思路是：</p><blockquote><p>如果低优先级线程持有了高优先级线程等待的锁，那么低优先级线程临时继承高优先级线程的优先级。</p></blockquote><p>在上面的例子中：</p><ol><li>P1 持有锁 <code>I</code>。</li><li>P3 等待锁 <code>I</code>。</li><li>系统发现：高优先级 P3 被低优先级 P1 阻塞。</li><li>P1 临时继承 P3 的高优先级。</li><li>P1 获得调度机会，尽快执行完临界区。</li><li>P1 释放锁 <code>I</code>。</li><li>P3 获得锁并继续执行。</li><li>P1 恢复原来的低优先级。</li></ol><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant P1 as P1 低优先级    participant P2 as P2 中优先级    participant P3 as P3 高优先级    P1-&gt;&gt;P1: 获得锁 I    P3-&gt;&gt;P1: 请求锁 I，发现被 P1 持有    P3-&gt;&gt;P3: 阻塞等待    P1-&gt;&gt;P1: 临时继承 P3 优先级    P1-&gt;&gt;P1: 继续运行并释放锁 I    P3-&gt;&gt;P3: 获得锁 I，继续执行    P1-&gt;&gt;P1: 恢复原优先级  </pre></div><hr><h2 id="❗-本节核心结论：优先级反转的本质是：高优先级线程被低优先级持锁线程阻塞，而低优先级线程又被中优先级线程抢占。优先级继承通过临时提高持锁线程优先级来缓解这个问题。"><a href="#❗-本节核心结论：优先级反转的本质是：高优先级线程被低优先级持锁线程阻塞，而低优先级线程又被中优先级线程抢占。优先级继承通过临时提高持锁线程优先级来缓解这个问题。" class="headerlink" title="❗ 本节核心结论：优先级反转的本质是：高优先级线程被低优先级持锁线程阻塞，而低优先级线程又被中优先级线程抢占。优先级继承通过临时提高持锁线程优先级来缓解这个问题。"></a><strong>❗ 本节核心结论：</strong><br>优先级反转的本质是：高优先级线程被低优先级持锁线程阻塞，而低优先级线程又被中优先级线程抢占。优先级继承通过临时提高持锁线程优先级来缓解这个问题。</h2><h2 id="8-三类问题总对比"><a href="#8-三类问题总对比" class="headerlink" title="8. 三类问题总对比"></a>8. 三类问题总对比</h2><table><thead><tr><th>问题</th><th>核心现象</th><th>线程是否运行</th><th>是否有进展</th><th>典型原因</th><th>典型解决</th></tr></thead><tbody><tr><td>死锁</td><td>互相等待资源</td><td>通常阻塞</td><td>没有</td><td>循环等待</td><td>预防、避免、检测恢复</td></tr><tr><td>活锁</td><td>一直释放重试</td><td>一直运行</td><td>没有</td><td>同步退让 &#x2F; 同步重试</td><td>随机退避</td></tr><tr><td>优先级反转</td><td>高优先级被间接阻塞</td><td>部分线程运行</td><td>高优先级无进展</td><td>锁依赖 + 优先级调度冲突</td><td>优先级继承</td></tr></tbody></table><hr><h2 id="9-死锁处理方式总对比"><a href="#9-死锁处理方式总对比" class="headerlink" title="9. 死锁处理方式总对比"></a>9. 死锁处理方式总对比</h2><table><thead><tr><th>方式</th><th>发生时机</th><th>核心思想</th><th>典型方法</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>检测与恢复</td><td>死锁发生后</td><td>发现环后处理</td><td>资源分配图、kill、回滚</td><td>对正常运行限制少</td><td>恢复代价大</td></tr><tr><td>死锁预防</td><td>程序设计时</td><td>破坏死锁必要条件</td><td>统一加锁顺序、trylock</td><td>原理清楚，工程常用</td><td>可能限制编程方式</td></tr><tr><td>死锁避免</td><td>资源分配时</td><td>保持系统安全状态</td><td>银行家算法</td><td>更精细</td><td>需要提前知道最大需求，有运行时开销</td></tr></tbody></table><hr><h2 id="10-最容易混淆的点"><a href="#10-最容易混淆的点" class="headerlink" title="10. 最容易混淆的点"></a>10. 最容易混淆的点</h2><h3 id="10-1-死锁不是“等很久”"><a href="#10-1-死锁不是“等很久”" class="headerlink" title="10.1 死锁不是“等很久”"></a>10.1 死锁不是“等很久”</h3><p>一个线程等锁很久，不一定是死锁。</p><p>死锁强调的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">多个线程形成互相等待闭环</span><br></pre></td></tr></table></figure><hr><h3 id="10-2-有环和死锁的关系要看资源实例数"><a href="#10-2-有环和死锁的关系要看资源实例数" class="headerlink" title="10.2 有环和死锁的关系要看资源实例数"></a>10.2 有环和死锁的关系要看资源实例数</h3><p>如果每类资源只有一个实例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">资源分配图有环 ⇒ 死锁</span><br></pre></td></tr></table></figure><p>如果每类资源有多个实例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">有环不一定必然死锁，但说明存在风险</span><br></pre></td></tr></table></figure><hr><h3 id="10-3-死锁预防和死锁避免不同"><a href="#10-3-死锁预防和死锁避免不同" class="headerlink" title="10.3 死锁预防和死锁避免不同"></a>10.3 死锁预防和死锁避免不同</h3><table><thead><tr><th>概念</th><th>含义</th></tr></thead><tbody><tr><td>死锁预防</td><td>提前规定规则，让死锁条件不成立</td></tr><tr><td>死锁避免</td><td>每次分配前动态判断，保持安全状态</td></tr></tbody></table><hr><h3 id="10-4-非安全状态不等于死锁状态"><a href="#10-4-非安全状态不等于死锁状态" class="headerlink" title="10.4 非安全状态不等于死锁状态"></a>10.4 非安全状态不等于死锁状态</h3><p>非安全状态只是说明：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">不能保证未来一定不死锁</span><br></pre></td></tr></table></figure><p>死锁状态则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">已经无法继续推进</span><br></pre></td></tr></table></figure><hr><h3 id="10-5-trylock-避免死锁，但可能活锁"><a href="#10-5-trylock-避免死锁，但可能活锁" class="headerlink" title="10.5 trylock 避免死锁，但可能活锁"></a>10.5 trylock 避免死锁，但可能活锁</h3><p><code>trylock</code> 失败后释放已持有资源，可以避免“持有并等待”。</p><p>但如果多个线程总是同步重试，就可能：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">一直运行，一直失败，一直释放，一直重试</span><br></pre></td></tr></table></figure><p>这就是活锁。</p><hr><h3 id="10-6-优先级反转不是高优先级真的变低了"><a href="#10-6-优先级反转不是高优先级真的变低了" class="headerlink" title="10.6 优先级反转不是高优先级真的变低了"></a>10.6 优先级反转不是高优先级真的变低了</h3><p>高优先级线程并没有被调度器主动降级。</p><p>它是因为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">等待低优先级线程手里的锁</span><br></pre></td></tr></table></figure><p>而低优先级线程又被中优先级线程抢占，导致高优先级线程间接受阻。</p><hr><h2 id="11-本讲一句话总结"><a href="#11-本讲一句话总结" class="headerlink" title="11. 本讲一句话总结"></a>11. 本讲一句话总结</h2><p>同步原语虽然能保证共享资源访问的正确性，但也会带来资源等待关系。死锁来自互相等待闭环，处理方式包括检测恢复、预防和避免；银行家算法通过安全序列判断资源分配是否安全；trylock 可能避免死锁但引出活锁；优先级反转则来自锁依赖和优先级调度的冲突，需要用优先级继承缓解。</p><hr><h2 id="12-自测题"><a href="#12-自测题" class="headerlink" title="12. 自测题"></a>12. 自测题</h2><ol><li>为什么说同步原语解决了共享资源竞争问题，但也可能引入新的问题？</li><li>用 <code>proc_A</code> 先拿 A 再拿 B、<code>proc_B</code> 先拿 B 再拿 A 的例子解释死锁如何产生。</li><li>死锁的四个必要条件分别是什么？</li><li>每个死锁必要条件在“锁”的场景中如何体现？</li><li>为什么哲学家问题中“所有人都先拿左手筷子”可能导致死锁？</li><li>资源分配图中的线程节点、资源节点、请求边、分配边分别表示什么？</li><li>为什么资源分配图中出现环可以帮助判断死锁？</li><li>死锁检测与恢复有哪些恢复方式？各自代价是什么？</li><li>死锁预防为什么可以理解为“破坏死锁的必要条件”？</li><li><code>trylock</code> 为什么能破坏“持有并等待”？</li><li><code>trylock</code> 为什么又可能引出活锁？</li><li>为什么允许资源抢占时必须考虑回滚和恢复？</li><li>为什么统一规定所有线程按资源编号递增申请锁，可以打破循环等待？</li><li>什么是安全序列？</li><li>为什么存在安全序列就说明系统处于安全状态？</li><li>银行家算法中的 <code>Available</code>、<code>Max</code>、<code>Allocation</code>、<code>Need</code> 分别表示什么？</li><li>为什么“非安全状态”不等于“已经死锁”？</li><li>活锁和死锁的区别是什么？</li><li>用一个例子说明优先级反转如何发生。</li><li>优先级继承协议为什么能缓解优先级反转？</li></ol><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>本讲可以压缩成一条主线：</p><blockquote><p>多线程同步不仅要保证共享数据正确，还要保证系统能持续推进。</p></blockquote><p>具体来说：</p><ul><li><strong>❗ 死锁</strong>：线程互相等待资源，形成循环等待闭环。</li><li><strong>❗ 死锁检测与恢复</strong>：允许死锁发生，之后通过检测环、kill 或回滚恢复。</li><li><strong>❗ 死锁预防</strong>：提前破坏死锁四个必要条件之一。</li><li><strong>❗ 死锁避免</strong>：运行时判断资源分配后是否仍存在安全序列。</li><li><strong>❗ 银行家算法</strong>：通过模拟线程完成顺序，判断系统是否安全。</li><li><strong>❗ 活锁</strong>：线程一直运行、释放、重试，但没有实际进展。</li><li><strong>❗ 优先级反转</strong>：高优先级线程被低优先级持锁线程间接阻塞。</li><li><strong>❗ 优先级继承</strong>：让持锁的低优先级线程临时继承高优先级，尽快释放锁。</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第15讲 同步原语II</title>
      <link href="/2026/06/11/%E7%AC%AC15%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADII/"/>
      <url>/2026/06/11/%E7%AC%AC15%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADII/</url>
      
        <content type="html"><![CDATA[<p>以下根据附件《第15讲 同步原语II.md》整理。</p><h1 id="第15讲：同步原语-II-学习指南"><a href="#第15讲：同步原语-II-学习指南" class="headerlink" title="第15讲：同步原语 II 学习指南"></a>第15讲：同步原语 II 学习指南</h1><p>同步原语 II 的主线是：在互斥锁之外，操作系统还需要更丰富的同步机制来处理“条件等待”“有限资源”“读写分离”和“极端读多写少”的场景。</p><hr><h2 id="❗-本节核心结论：互斥锁只能解决“同一时刻谁能进入临界区”的问题；条件变量、信号量、读写锁和-RCU-分别进一步解决“什么时候能继续”“资源数量够不够”“读者能否并行”“读路径能否几乎无锁”的问题。"><a href="#❗-本节核心结论：互斥锁只能解决“同一时刻谁能进入临界区”的问题；条件变量、信号量、读写锁和-RCU-分别进一步解决“什么时候能继续”“资源数量够不够”“读者能否并行”“读路径能否几乎无锁”的问题。" class="headerlink" title="❗ 本节核心结论：互斥锁只能解决“同一时刻谁能进入临界区”的问题；条件变量、信号量、读写锁和 RCU 分别进一步解决“什么时候能继续”“资源数量够不够”“读者能否并行”“读路径能否几乎无锁”的问题。"></a><strong>❗ 本节核心结论：</strong><br>互斥锁只能解决“同一时刻谁能进入临界区”的问题；条件变量、信号量、读写锁和 RCU 分别进一步解决“什么时候能继续”“资源数量够不够”“读者能否并行”“读路径能否几乎无锁”的问题。</h2><h2 id="1-本章知识地图"><a href="#1-本章知识地图" class="headerlink" title="1. 本章知识地图"></a>1. 本章知识地图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[互斥锁 &#x2F; 自旋锁] --&gt; B[条件变量]    B --&gt; C[信号量]    C --&gt; D[读写锁]    D --&gt; E[RCU]        A --&gt; A1[只解决临界区互斥]    B --&gt; B1[条件不满足时阻塞等待]    C --&gt; C1[把资源数量和等待唤醒结合]    D --&gt; D1[读读并行，读写互斥]    E --&gt; E1[读者无锁直读，写者复制更新]  </pre></div><p>这张图展示了同步原语的升级路线：从最基础的互斥，逐渐走向条件等待、资源计数、读写区分和读路径极致优化。</p><hr><h2 id="2-条件变量：让线程等到条件成立"><a href="#2-条件变量：让线程等到条件成立" class="headerlink" title="2. 条件变量：让线程等到条件成立"></a>2. 条件变量：让线程等到条件成立</h2><h3 id="2-1-条件变量解决什么问题？"><a href="#2-1-条件变量解决什么问题？" class="headerlink" title="2.1 条件变量解决什么问题？"></a>2.1 条件变量解决什么问题？</h3><p>互斥锁能保证临界区安全，但它不能优雅地处理“条件暂时不满足”的情况。</p><p>例如生产者消费者问题中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (empty_slot == <span class="number">0</span>)</span><br><span class="line">    ; <span class="comment">// 忙等</span></span><br></pre></td></tr></table></figure><p>这段代码的问题是：</p><ul><li>线程明明不能继续执行，却一直占用 CPU。</li><li>它只是反复检查条件，没有真正让出处理器。</li><li>如果等待时间很长，会造成严重浪费。</li></ul><p>条件变量的目的就是：</p><ul><li>条件不满足时，让线程阻塞。</li><li>条件满足后，由其他线程唤醒它。</li><li>避免无意义的忙等。</li></ul><hr><h3 id="2-2-条件变量的基本接口"><a href="#2-2-条件变量的基本接口" class="headerlink" title="2.2 条件变量的基本接口"></a>2.2 条件变量的基本接口</h3><p>条件变量主要有两个操作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cond_wait();</span><br><span class="line">cond_signal();</span><br></pre></td></tr></table></figure><p>它们的含义是：</p><table><thead><tr><th>操作</th><th>含义</th><th>典型角色</th></tr></thead><tbody><tr><td><code>cond_wait()</code></td><td>条件不满足，当前线程进入等待</td><td>等待者</td></tr><tr><td><code>cond_signal()</code></td><td>条件可能满足，唤醒等待线程</td><td>唤醒者</td></tr></tbody></table><p>在生产者消费者问题中：</p><ul><li>生产者发现缓冲区满了，就等待 <code>empty_cond</code>。</li><li>消费者取走一个元素后，产生空位，于是 <code>cond_signal(&amp;empty_cond)</code>。</li><li>消费者发现缓冲区空了，就等待 <code>filled_cond</code>。</li><li>生产者放入一个元素后，产生可消费对象，于是 <code>cond_signal(&amp;filled_cond)</code>。</li></ul><hr><h3 id="2-3-cond-wait-为什么必须配合互斥锁？"><a href="#2-3-cond-wait-为什么必须配合互斥锁？" class="headerlink" title="2.3 cond_wait() 为什么必须配合互斥锁？"></a>2.3 <code>cond_wait()</code> 为什么必须配合互斥锁？</h3><p>条件变量不能单独使用。标准模式是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mutex);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (condition_not_satisfied) &#123;</span><br><span class="line">    cond_wait(&amp;cond, &amp;mutex);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">do_something();</span><br><span class="line"></span><br><span class="line">unlock(&amp;mutex);</span><br></pre></td></tr></table></figure><p><code>cond_wait(&amp;cond, &amp;mutex)</code> 通常要原子地完成两件事：</p><ul><li>把当前线程加入条件变量的等待队列。</li><li>释放互斥锁并阻塞当前线程。</li></ul><p>被唤醒后，它还要：</p><ul><li>重新获得互斥锁。</li><li>再继续检查条件和执行后续代码。</li></ul><p>关键点在于：<strong>释放锁和阻塞必须是原子的</strong>。如果不是原子的，可能出现：</p><ul><li>线程准备睡眠但还没睡眠时，另一个线程已经发送了 signal。</li><li>signal 被“错过”，等待线程之后睡下去再也没人唤醒。</li><li>或者等待线程持锁睡眠，导致其他线程无法修改条件，形成死锁。</li></ul><hr><h3 id="2-4-为什么等待条件要用-while，而不是-if？"><a href="#2-4-为什么等待条件要用-while，而不是-if？" class="headerlink" title="2.4 为什么等待条件要用 while，而不是 if？"></a>2.4 为什么等待条件要用 <code>while</code>，而不是 <code>if</code>？</h3><p>错误写法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (empty_slot == <span class="number">0</span>) &#123;</span><br><span class="line">    cond_wait(&amp;empty_cond, &amp;mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>推荐写法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (empty_slot == <span class="number">0</span>) &#123;</span><br><span class="line">    cond_wait(&amp;empty_cond, &amp;mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>原因是：<strong>线程被唤醒，不等于条件一定成立。</strong></p><p>可能出现：</p><ul><li>多个线程同时被唤醒，但资源只够一个线程使用。</li><li>线程被唤醒后没有立即运行，期间条件又被其他线程改掉。</li><li>某些系统可能存在虚假唤醒。</li><li><code>cond_signal()</code> 只表示“条件可能成立了”，不是保证条件仍然成立。</li></ul><p>所以条件变量的核心范式是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (条件不满足) &#123;</span><br><span class="line">    等待;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：条件变量不是条件本身，而是等待队列。真正的条件仍然要由共享变量表达，并且必须在互斥锁保护下用-while-反复检查。"><a href="#❗-本节核心结论：条件变量不是条件本身，而是等待队列。真正的条件仍然要由共享变量表达，并且必须在互斥锁保护下用-while-反复检查。" class="headerlink" title="❗ 本节核心结论：条件变量不是条件本身，而是等待队列。真正的条件仍然要由共享变量表达，并且必须在互斥锁保护下用 while 反复检查。"></a><strong>❗ 本节核心结论：</strong><br>条件变量不是条件本身，而是等待队列。真正的条件仍然要由共享变量表达，并且必须在互斥锁保护下用 <code>while</code> 反复检查。</h2><h2 id="3-信号量：把资源数量和等待机制合在一起"><a href="#3-信号量：把资源数量和等待机制合在一起" class="headerlink" title="3. 信号量：把资源数量和等待机制合在一起"></a>3. 信号量：把资源数量和等待机制合在一起</h2><h3 id="3-1-信号量是什么？"><a href="#3-1-信号量是什么？" class="headerlink" title="3.1 信号量是什么？"></a>3.1 信号量是什么？</h3><p>信号量可以理解为：</p><blockquote><p>一个带同步能力的资源计数器。</p></blockquote><p>它内部有一个计数值，用来表示当前可用资源数量。</p><p>例如生产者消费者问题中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">sem_t</span> empty_slot;</span><br><span class="line"><span class="type">sem_t</span> filled_slot;</span><br></pre></td></tr></table></figure><p>它们分别表示：</p><table><thead><tr><th>信号量</th><th>含义</th></tr></thead><tbody><tr><td><code>empty_slot</code></td><td>当前还有多少空槽位</td></tr><tr><td><code>filled_slot</code></td><td>当前有多少可消费对象</td></tr></tbody></table><hr><h3 id="3-2-P-V-原语怎么理解？"><a href="#3-2-P-V-原语怎么理解？" class="headerlink" title="3.2 P &#x2F; V 原语怎么理解？"></a>3.2 P &#x2F; V 原语怎么理解？</h3><p>信号量有两个经典操作：</p><table><thead><tr><th>原语</th><th>常见名称</th><th>直观含义</th></tr></thead><tbody><tr><td>P 操作</td><td><code>wait()</code></td><td>申请一个资源</td></tr><tr><td>V 操作</td><td><code>signal()</code></td><td>释放或产生一个资源</td></tr></tbody></table><p>可以记成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">wait(S);    <span class="comment">// 申请资源</span></span><br><span class="line">signal(S);  <span class="comment">// 释放资源</span></span><br></pre></td></tr></table></figure><p>生产者逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wait(&amp;empty_slot);</span><br><span class="line">buffer_add_safe(new_msg);</span><br><span class="line">signal(&amp;filled_slot);</span><br></pre></td></tr></table></figure><p>含义是：</p><ul><li>先申请一个空槽位。</li><li>把消息放入缓冲区。</li><li>通知系统多了一个可消费对象。</li></ul><p>消费者逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wait(&amp;filled_slot);</span><br><span class="line">cur_msg = buffer_remove_safe();</span><br><span class="line">signal(&amp;empty_slot);</span><br></pre></td></tr></table></figure><p>含义是：</p><ul><li>先申请一个已有消息。</li><li>从缓冲区取走消息。</li><li>通知系统多了一个空槽位。</li></ul><hr><h3 id="3-3-信号量和条件变量的区别"><a href="#3-3-信号量和条件变量的区别" class="headerlink" title="3.3 信号量和条件变量的区别"></a>3.3 信号量和条件变量的区别</h3><p>条件变量的问题是：条件变量和真实条件是分离的。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> empty_slot = <span class="number">5</span>;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cond</span> <span class="title">empty_cond</span>;</span></span><br></pre></td></tr></table></figure><p>这里：</p><ul><li><code>empty_slot</code> 才是真正的资源数量。</li><li><code>empty_cond</code> 只是一个等待队列。</li><li>二者的对应关系靠程序员维护。</li></ul><p>信号量则把二者合并：</p><ul><li>计数值表示资源数量。</li><li>当资源不足时自动阻塞。</li><li>当资源增加时自动唤醒等待线程。</li></ul><table><thead><tr><th>对比点</th><th>条件变量</th><th>信号量</th></tr></thead><tbody><tr><td>本质</td><td>等待队列 &#x2F; 通知机制</td><td>资源计数器 + 等待机制</td></tr><tr><td>是否记录资源数量</td><td>不记录</td><td>记录</td></tr><tr><td>是否需要额外条件变量</td><td>需要共享变量表达条件</td><td>信号量值本身表达条件</td></tr><tr><td>典型问题</td><td>缓冲区空&#x2F;满等待</td><td>空槽位、连接池、令牌数</td></tr><tr><td>易错点</td><td>忘记用 <code>while</code> 检查条件</td><td>把信号量当普通整数</td></tr></tbody></table><hr><h3 id="3-4-信号量不是普通整数"><a href="#3-4-信号量不是普通整数" class="headerlink" title="3.4 信号量不是普通整数"></a>3.4 信号量不是普通整数</h3><p>信号量看起来像整数，但不能把它当成普通变量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">S--;</span><br><span class="line">S++;</span><br></pre></td></tr></table></figure><p>这是错误直觉。</p><p>真正的信号量要求：</p><ul><li><code>wait()</code> 内部修改计数值必须是同步安全的。</li><li><code>signal()</code> 内部修改计数值也必须是同步安全的。</li><li>多个线程同时申请或释放资源时不能发生数据竞争。</li><li>必要时线程要进入等待队列，而不是单纯修改整数。</li></ul><p>因此信号量通常可以用：</p><ul><li>互斥锁</li><li>条件变量</li><li>内部计数值</li><li>等待队列</li></ul><p>共同实现。</p><hr><h2 id="❗-本节核心结论：信号量适合表达“有限数量资源”。它不是普通整数，而是把资源计数、互斥保护和等待唤醒组合在一起的同步原语。"><a href="#❗-本节核心结论：信号量适合表达“有限数量资源”。它不是普通整数，而是把资源计数、互斥保护和等待唤醒组合在一起的同步原语。" class="headerlink" title="❗ 本节核心结论：信号量适合表达“有限数量资源”。它不是普通整数，而是把资源计数、互斥保护和等待唤醒组合在一起的同步原语。"></a><strong>❗ 本节核心结论：</strong><br>信号量适合表达“有限数量资源”。它不是普通整数，而是把资源计数、互斥保护和等待唤醒组合在一起的同步原语。</h2><h2 id="4-读写锁：读者之间不必互斥"><a href="#4-读写锁：读者之间不必互斥" class="headerlink" title="4. 读写锁：读者之间不必互斥"></a>4. 读写锁：读者之间不必互斥</h2><h3 id="4-1-为什么需要读写锁？"><a href="#4-1-为什么需要读写锁？" class="headerlink" title="4.1 为什么需要读写锁？"></a>4.1 为什么需要读写锁？</h3><p>互斥锁过于保守。</p><p>如果多个线程只是读取共享数据：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">read_data(data);</span><br></pre></td></tr></table></figure><p>它们之间不会修改数据，也不会互相破坏结果。普通互斥锁却会让它们排队执行，降低并行性。</p><p>读写锁的基本规则是：</p><table><thead><tr><th>组合</th><th>是否允许并行</th><th>原因</th></tr></thead><tbody><tr><td>读者 + 读者</td><td>允许</td><td>读操作不修改数据</td></tr><tr><td>读者 + 写者</td><td>不允许</td><td>写操作可能改变读者看到的数据</td></tr><tr><td>写者 + 写者</td><td>不允许</td><td>多个写者会互相覆盖或破坏状态</td></tr></tbody></table><p>所以读写锁的目标是：</p><ul><li>保证写操作安全。</li><li>尽量放开读操作之间的并行。</li><li>提高读多写少场景下的吞吐量。</li></ul><hr><h3 id="4-2-读写锁的基本使用方式"><a href="#4-2-读写锁的基本使用方式" class="headerlink" title="4.2 读写锁的基本使用方式"></a>4.2 读写锁的基本使用方式</h3><p>读者使用读锁：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">reader</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    lock_reader(&amp;lock);</span><br><span class="line">    read_data(data);</span><br><span class="line">    unlock_reader(&amp;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>写者使用写锁：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">writer</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    lock_writer(&amp;lock);</span><br><span class="line">    update_data(data);</span><br><span class="line">    unlock_writer(&amp;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关键不是“函数名不同”，而是进入规则不同：</p><ul><li>读锁可以被多个读者同时持有。</li><li>写锁必须独占。</li><li>只要有写者进入，读者不能同时进入。</li><li>只要有读者存在，写者通常不能进入。</li></ul><hr><h2 id="5-读写锁的偏向性：吞吐量与公平性的取舍"><a href="#5-读写锁的偏向性：吞吐量与公平性的取舍" class="headerlink" title="5. 读写锁的偏向性：吞吐量与公平性的取舍"></a>5. 读写锁的偏向性：吞吐量与公平性的取舍</h2><h3 id="5-1-问题场景"><a href="#5-1-问题场景" class="headerlink" title="5.1 问题场景"></a>5.1 问题场景</h3><p>读写锁会遇到一个典型问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">t0：已有读者在临界区</span><br><span class="line">t1：写者开始等待</span><br><span class="line">t2：新的读者到来</span><br></pre></td></tr></table></figure><p>此时问题是：</p><blockquote><p>新读者能不能直接进入？</p></blockquote><p>不同答案会形成不同偏向策略。</p><hr><h3 id="5-2-偏向读者"><a href="#5-2-偏向读者" class="headerlink" title="5.2 偏向读者"></a>5.2 偏向读者</h3><p>偏向读者的策略是：</p><ul><li>即使有写者等待，新读者也可以继续进入。</li><li>读者吞吐量更高。</li><li>但写者可能长期等不到机会。</li></ul><p>典型实现思路：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rwlock</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> reader_cnt;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lock</span> <span class="title">reader_lock</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">lock</span> <span class="title">writer_lock</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>核心逻辑：</p><ul><li><code>reader_cnt</code> 记录当前读者数量。</li><li>第一个读者进入时获取 <code>writer_lock</code>，阻止写者进入。</li><li>后续读者只增加 <code>reader_cnt</code>，不需要再抢写者锁。</li><li>最后一个读者离开时释放 <code>writer_lock</code>，写者才有机会进入。</li></ul><p>关键代码含义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (reader_cnt == <span class="number">1</span>) &#123;</span><br><span class="line">    lock(&amp;writer_lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>表示：</p><ul><li>第一个读者负责挡住写者。</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (reader_cnt == <span class="number">0</span>) &#123;</span><br><span class="line">    unlock(&amp;writer_lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>表示：</p><ul><li>最后一个读者负责放行写者。</li></ul><hr><h3 id="5-3-偏向写者"><a href="#5-3-偏向写者" class="headerlink" title="5.3 偏向写者"></a>5.3 偏向写者</h3><p>偏向写者的策略是：</p><ul><li>一旦有写者等待，后续新读者不能继续进入。</li><li>写者更不容易饥饿。</li><li>但读者并行性会被更早限制。</li></ul><p>典型实现中可以引入：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">bool</span> has_writer;</span><br></pre></td></tr></table></figure><p>它表示：</p><ul><li>当前是否已经有写者等待或进入。</li><li>如果 <code>has_writer == TRUE</code>，新读者需要等待。</li><li>这样可以防止新读者源源不断进入，导致写者永远拿不到锁。</li></ul><hr><h3 id="5-4-偏向读者-vs-偏向写者"><a href="#5-4-偏向读者-vs-偏向写者" class="headerlink" title="5.4 偏向读者 vs 偏向写者"></a>5.4 偏向读者 vs 偏向写者</h3><table><thead><tr><th>对比点</th><th>偏向读者</th><th>偏向写者</th></tr></thead><tbody><tr><td>有写者等待时，新读者能否进入</td><td>能</td><td>不能</td></tr><tr><td>优点</td><td>读吞吐量高</td><td>写者更公平</td></tr><tr><td>缺点</td><td>写者可能饥饿</td><td>读者可能更早阻塞</td></tr><tr><td>更关注</td><td>性能 &#x2F; 吞吐量</td><td>公平性 &#x2F; 避免写者饥饿</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：读写锁不等于“读者永远优先”。它必须选择偏向策略：偏向读者提高吞吐量，偏向写者减少写者饥饿。"><a href="#❗-本节核心结论：读写锁不等于“读者永远优先”。它必须选择偏向策略：偏向读者提高吞吐量，偏向写者减少写者饥饿。" class="headerlink" title="❗ 本节核心结论：读写锁不等于“读者永远优先”。它必须选择偏向策略：偏向读者提高吞吐量，偏向写者减少写者饥饿。"></a><strong>❗ 本节核心结论：</strong><br>读写锁不等于“读者永远优先”。它必须选择偏向策略：偏向读者提高吞吐量，偏向写者减少写者饥饿。</h2><h2 id="6-RCU：读多写少场景下的极致优化"><a href="#6-RCU：读多写少场景下的极致优化" class="headerlink" title="6. RCU：读多写少场景下的极致优化"></a>6. RCU：读多写少场景下的极致优化</h2><h3 id="6-1-RCU-是什么？"><a href="#6-1-RCU-是什么？" class="headerlink" title="6.1 RCU 是什么？"></a>6.1 RCU 是什么？</h3><p>RCU 全称是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Read-Copy-Update</span><br></pre></td></tr></table></figure><p>中文可以理解为：</p><blockquote><p>读者直接读，写者复制后更新。</p></blockquote><p>它的基本思想是：</p><ul><li>读者不通过传统锁阻塞。</li><li>写者不直接修改旧对象。</li><li>写者复制旧对象，在副本上修改。</li><li>修改完成后，原子发布新指针。</li><li>等旧读者全部退出后，再释放旧对象。</li></ul><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[旧对象 A] --&gt; B[写者复制 A 得到 A&#39;]    B --&gt; C[在 A&#39; 上修改]    C --&gt; D[发布新指针 gp -&gt; A&#39;]    D --&gt; E[新读者读取 A&#39;]    A --&gt; F[旧读者继续读 A]    F --&gt; G[宽限期结束后释放 A]  </pre></div><p>这张图强调 RCU 的核心不是“阻止读者进入”，而是允许新旧版本在短时间内共存。</p><hr><h3 id="6-2-RCU-为什么适合读多写少？"><a href="#6-2-RCU-为什么适合读多写少？" class="headerlink" title="6.2 RCU 为什么适合读多写少？"></a>6.2 RCU 为什么适合读多写少？</h3><p>读写锁虽然允许多个读者并行，但读者仍然要执行读锁逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">lock_reader(&amp;lock);</span><br><span class="line">read_data(data);</span><br><span class="line">unlock_reader(&amp;lock);</span><br></pre></td></tr></table></figure><p>在读特别多、写特别少的场景中，哪怕读锁很轻，也可能成为热点开销。</p><p>RCU 的目标是：</p><ul><li>让读路径尽可能短。</li><li>让读者几乎不需要传统锁。</li><li>把复杂性转移给写者。</li><li>用复制和延迟回收换取读路径性能。</li></ul><p>适合场景：</p><ul><li>读远多于写。</li><li>读路径非常关键。</li><li>数据通过指针间接访问。</li><li>可以接受旧版本短暂存在。</li><li>可以接受写者复制和延迟释放成本。</li></ul><hr><h3 id="6-3-RCU-更新流程"><a href="#6-3-RCU-更新流程" class="headerlink" title="6.3 RCU 更新流程"></a>6.3 RCU 更新流程</h3><p>假设共享指针 <code>gp</code> 指向旧对象 <code>A</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gp -&gt; A</span><br></pre></td></tr></table></figure><p>写者更新时，不直接修改 <code>A</code>，而是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">p = rcu_dereference(gp);</span><br><span class="line"></span><br><span class="line">q = kmalloc(...);</span><br><span class="line">*q = *p;</span><br><span class="line">q-&gt;v = newv;</span><br><span class="line"></span><br><span class="line">rcu_assign_pointer(gp, q);</span><br><span class="line"></span><br><span class="line">synchronize_rcu();</span><br><span class="line"></span><br><span class="line">kfree(p);</span><br></pre></td></tr></table></figure><p>这个流程可以拆成：</p><ol><li>读取旧指针 <code>p</code>。</li><li>分配新对象 <code>q</code>。</li><li>把旧对象内容复制到新对象。</li><li>在新对象上完成修改。</li><li>用 <code>rcu_assign_pointer()</code> 发布新版本。</li><li>新读者开始看到新对象。</li><li>旧读者仍可能持有旧对象。</li><li>调用 <code>synchronize_rcu()</code> 等待旧读者退出。</li><li>确认安全后释放旧对象。</li></ol><hr><h3 id="6-4-什么是宽限期-Grace-Period？"><a href="#6-4-什么是宽限期-Grace-Period？" class="headerlink" title="6.4 什么是宽限期 Grace Period？"></a>6.4 什么是宽限期 Grace Period？</h3><p>宽限期指的是：</p><blockquote><p>写者发布新版本之后，等待所有可能正在读取旧版本的读者退出 RCU 读临界区的时间。</p></blockquote><p>为什么需要宽限期？</p><p>因为发布新指针后，系统中可能同时存在：</p><table><thead><tr><th>读者类型</th><th>看到的对象</th></tr></thead><tbody><tr><td>旧读者</td><td>旧对象 A</td></tr><tr><td>新读者</td><td>新对象 A’</td></tr></tbody></table><p>如果写者发布新指针后立刻：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kfree(A);</span><br></pre></td></tr></table></figure><p>那么旧读者可能还在访问 <code>A</code>，这会导致 use-after-free。</p><p>所以 RCU 的安全性依赖：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">synchronize_rcu();</span><br></pre></td></tr></table></figure><p>它保证：</p><ul><li>在它返回时，旧读者已经离开 RCU 读临界区。</li><li>旧对象可以安全释放。</li></ul><hr><h3 id="6-5-RCU-读者的约束"><a href="#6-5-RCU-读者的约束" class="headerlink" title="6.5 RCU 读者的约束"></a>6.5 RCU 读者的约束</h3><p>RCU 读者通常写成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">rcu_read_lock();</span><br><span class="line"></span><br><span class="line">p = rcu_dereference(gp);</span><br><span class="line">do_something(p-&gt;data);</span><br><span class="line"></span><br><span class="line">rcu_read_unlock();</span><br></pre></td></tr></table></figure><p>需要注意：</p><ul><li>必须用 <code>rcu_dereference()</code> 读取共享指针。</li><li>它防止编译器或 CPU 重排破坏指针发布顺序。</li><li>RCU 读临界区内通常不能睡眠或调度。</li><li>否则宽限期可能被无限拉长。</li><li>RCU 不等于写者不用锁。</li><li>多个写者之间仍然需要额外互斥，例如 <code>spin_lock()</code>。</li></ul><hr><h3 id="6-6-synchronize-rcu-不能替代写者锁"><a href="#6-6-synchronize-rcu-不能替代写者锁" class="headerlink" title="6.6 synchronize_rcu() 不能替代写者锁"></a>6.6 <code>synchronize_rcu()</code> 不能替代写者锁</h3><p><code>sync_rcu()</code> 或 <code>synchronize_rcu()</code> 只解决一个问题：</p><blockquote><p>什么时候可以安全释放旧对象？</p></blockquote><p>它不解决：</p><ul><li>多个写者同时修改同一结构。</li><li>多个写者同时发布新版本。</li><li>写者之间的更新顺序。</li><li>写者之间的数据竞争。</li></ul><p>所以多个写者仍然可能需要：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">spin_lock(&amp;list_lock);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* update */</span></span><br><span class="line"></span><br><span class="line">spin_unlock(&amp;list_lock);</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：RCU-不是“更快的读写锁”，而是另一种更新策略：读者允许读旧版本或新版本，写者负责复制、发布和延迟回收。"><a href="#❗-本节核心结论：RCU-不是“更快的读写锁”，而是另一种更新策略：读者允许读旧版本或新版本，写者负责复制、发布和延迟回收。" class="headerlink" title="❗ 本节核心结论：RCU 不是“更快的读写锁”，而是另一种更新策略：读者允许读旧版本或新版本，写者负责复制、发布和延迟回收。"></a><strong>❗ 本节核心结论：</strong><br>RCU 不是“更快的读写锁”，而是另一种更新策略：读者允许读旧版本或新版本，写者负责复制、发布和延迟回收。</h2><h2 id="7-五类同步原语总对比"><a href="#7-五类同步原语总对比" class="headerlink" title="7. 五类同步原语总对比"></a>7. 五类同步原语总对比</h2><table><thead><tr><th>同步原语</th><th>主要解决的问题</th><th>核心思想</th><th>典型场景</th><th>易错点</th></tr></thead><tbody><tr><td>互斥锁</td><td>临界区互斥</td><td>同一时刻只允许一个线程进入</td><td>修改共享变量</td><td>只解决互斥，不解决条件等待</td></tr><tr><td>条件变量</td><td>条件不满足时避免忙等</td><td>等待条件，由其他线程唤醒</td><td>缓冲区空&#x2F;满</td><td>条件变量不是条件本身</td></tr><tr><td>信号量</td><td>有限资源数量管理</td><td>资源计数 + 阻塞唤醒</td><td>空槽位、连接池、令牌</td><td>不能当普通整数</td></tr><tr><td>读写锁</td><td>区分读者和写者</td><td>读读并行，读写互斥</td><td>读多写少共享数据</td><td>偏向策略影响饥饿问题</td></tr><tr><td>RCU</td><td>极端优化读多写少</td><td>读者无锁读，写者复制更新</td><td>内核读路径、共享指针结构</td><td>延迟回收不等于写者互斥</td></tr></tbody></table><hr><h2 id="8-最容易混淆的点"><a href="#8-最容易混淆的点" class="headerlink" title="8. 最容易混淆的点"></a>8. 最容易混淆的点</h2><h3 id="8-1-条件变量不是条件本身"><a href="#8-1-条件变量不是条件本身" class="headerlink" title="8.1 条件变量不是条件本身"></a>8.1 条件变量不是条件本身</h3><p><code>empty_cond</code> 不是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">empty_slot != <span class="number">0</span></span><br></pre></td></tr></table></figure><p><code>empty_cond</code> 只是等待队列。真正的条件是共享变量表达的状态。</p><hr><h3 id="8-2-cond-signal-通常不是等待者自己调用"><a href="#8-2-cond-signal-通常不是等待者自己调用" class="headerlink" title="8.2 cond_signal() 通常不是等待者自己调用"></a>8.2 <code>cond_signal()</code> 通常不是等待者自己调用</h3><p>等待者调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cond_wait();</span><br></pre></td></tr></table></figure><p>唤醒者调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cond_signal();</span><br></pre></td></tr></table></figure><p>这和互斥锁不同。互斥锁通常是同一个线程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">lock();</span><br><span class="line">...</span><br><span class="line">unlock();</span><br></pre></td></tr></table></figure><p>而条件变量的等待和唤醒通常发生在不同线程。</p><hr><h3 id="8-3-条件变量必须配合锁"><a href="#8-3-条件变量必须配合锁" class="headerlink" title="8.3 条件变量必须配合锁"></a>8.3 条件变量必须配合锁</h3><p>因为共享条件变量需要保护，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">empty_slot</span><br><span class="line">filled_slot</span><br></pre></td></tr></table></figure><p>如果没有锁，检查条件、修改条件、进入等待之间都可能发生竞态。</p><hr><h3 id="8-4-信号量不是普通整数"><a href="#8-4-信号量不是普通整数" class="headerlink" title="8.4 信号量不是普通整数"></a>8.4 信号量不是普通整数</h3><p>信号量虽然有计数值，但它的 <code>wait()</code> 和 <code>signal()</code> 必须是原子安全的同步操作。</p><hr><h3 id="8-5-读写锁不是固定偏向读者"><a href="#8-5-读写锁不是固定偏向读者" class="headerlink" title="8.5 读写锁不是固定偏向读者"></a>8.5 读写锁不是固定偏向读者</h3><p>读写锁可以偏向读者，也可以偏向写者。具体策略决定了：</p><ul><li>系统更重视读吞吐量。</li><li>还是更重视写者公平性。</li></ul><hr><h3 id="8-6-RCU-不负责写者之间的互斥"><a href="#8-6-RCU-不负责写者之间的互斥" class="headerlink" title="8.6 RCU 不负责写者之间的互斥"></a>8.6 RCU 不负责写者之间的互斥</h3><p>RCU 负责的是：</p><ul><li>读者如何安全看到旧版本或新版本。</li><li>旧对象何时可以安全释放。</li></ul><p>它不负责：</p><ul><li>多个写者如何排队。</li><li>多个写者如何避免覆盖彼此更新。</li></ul><hr><h2 id="9-自测题"><a href="#9-自测题" class="headerlink" title="9. 自测题"></a>9. 自测题</h2><ol><li>为什么 <code>while (empty_slot == 0);</code> 会浪费 CPU？</li><li><code>cond_wait()</code> 为什么必须同时完成“释放锁 + 阻塞”？</li><li>为什么条件变量等待时要用 <code>while</code>，而不是 <code>if</code>？</li><li>条件变量和互斥锁分别解决什么问题？</li><li>为什么说条件变量不是条件本身？</li><li>信号量中的 P 操作和 V 操作分别表示什么？</li><li>为什么信号量适合表达有限资源数量？</li><li>为什么信号量不能被当成普通整数？</li><li>读写锁为什么允许多个读者同时进入？</li><li>读写锁为什么不允许读者和写者同时进入？</li><li>偏向读者的读写锁为什么可能导致写者饥饿？</li><li>偏向写者的读写锁如何避免写者长期等待？</li><li>RCU 为什么不直接修改旧对象？</li><li>RCU 的宽限期到底在等待什么？</li><li>为什么发布新指针后不能立刻释放旧对象？</li><li>为什么 RCU 读临界区中通常不能睡眠？</li><li><code>synchronize_rcu()</code> 为什么不能替代写者之间的互斥锁？</li></ol><hr><h2 id="10-最终总结"><a href="#10-最终总结" class="headerlink" title="10. 最终总结"></a>10. 最终总结</h2><p>同步原语 II 的核心不是再讲一种“新的锁”，而是展示操作系统如何针对不同并发问题设计不同抽象：</p><ul><li><strong>❗ 条件变量</strong>解决“条件不满足时如何睡眠等待”。</li><li><strong>❗ 信号量</strong>解决“有限资源数量如何同步管理”。</li><li><strong>❗ 读写锁</strong>解决“读操作之间是否必须互斥”。</li><li><strong>❗ RCU</strong>解决“读远多于写时，如何让读路径尽可能轻”。</li></ul><p>最终应形成的理解是：</p><blockquote><p>同步原语不是越高级越好，而是要匹配具体并发模式。互斥锁适合保护临界区，条件变量适合等待条件，信号量适合管理资源数量，读写锁适合读多写少，RCU 适合极端读多写少且能接受复制更新与延迟回收的场景。</p></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第14讲 同步原语I</title>
      <link href="/2026/06/11/%E7%AC%AC14%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADI/"/>
      <url>/2026/06/11/%E7%AC%AC14%E8%AE%B2-%E5%90%8C%E6%AD%A5%E5%8E%9F%E8%AF%ADI/</url>
      
        <content type="html"><![CDATA[<h1 id="第14讲-同步原语-I：从竞争条件到互斥锁"><a href="#第14讲-同步原语-I：从竞争条件到互斥锁" class="headerlink" title="第14讲 同步原语 I：从竞争条件到互斥锁"></a>第14讲 同步原语 I：从竞争条件到互斥锁</h1><p>本文根据《第14讲 同步原语I.md》的对话转录整理而成。核心问题是：<strong>当多个线程或进程同时访问共享资源时，如何保证它们不会因为执行顺序不可控而破坏数据一致性。</strong> </p><hr><h2 id="1-本讲要解决什么问题"><a href="#1-本讲要解决什么问题" class="headerlink" title="1. 本讲要解决什么问题"></a>1. 本讲要解决什么问题</h2><p>前面学习 IPC 时，共享内存解决的是“多个进程能不能看到同一块数据”。但共享内存本身并不保证安全。</p><p>真正的问题是：</p><blockquote><p>多个线程或进程同时访问同一份共享数据时，谁先读、谁先写、谁能修改、谁必须等待？</p></blockquote><p>如果没有同步机制，就可能出现：</p><ul><li>两个线程同时修改同一个变量；</li><li>两个生产者写入同一个缓冲区位置；</li><li>一个线程读到另一个线程写了一半的数据；</li><li>程序结果依赖不可预测的调度顺序。</li></ul><p>所以本讲的主线是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">共享数据</span><br><span class="line">  ↓</span><br><span class="line">并发访问</span><br><span class="line">  ↓</span><br><span class="line">竞争条件</span><br><span class="line">  ↓</span><br><span class="line">临界区</span><br><span class="line">  ↓</span><br><span class="line">互斥访问</span><br><span class="line">  ↓</span><br><span class="line">同步原语</span><br><span class="line">  ↓</span><br><span class="line">互斥锁 / 自旋锁 / Ticket Lock</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-同步原语解决的不是“能不能共享数据”，而是“共享数据怎样被安全访问”。"><a href="#❗-本节核心结论：-同步原语解决的不是“能不能共享数据”，而是“共享数据怎样被安全访问”。" class="headerlink" title="❗ 本节核心结论： 同步原语解决的不是“能不能共享数据”，而是“共享数据怎样被安全访问”。"></a>❗ <strong>本节核心结论：</strong> 同步原语解决的不是“能不能共享数据”，而是“共享数据怎样被安全访问”。</h2><h2 id="2-为什么-balance-会出错"><a href="#2-为什么-balance-会出错" class="headerlink" title="2. 为什么 balance++ 会出错"></a>2. 为什么 <code>balance++</code> 会出错</h2><p>很多人会误以为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">balance++;</span><br></pre></td></tr></table></figure><p>是一条不可分割的语句。</p><p>但在机器层面，它通常会被拆成类似这样的步骤：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ldr  r3, [r2]      // 读取 balance</span><br><span class="line">adds r3, r3, #1    // 加 1</span><br><span class="line">str  r3, [r2]      // 写回 balance</span><br></pre></td></tr></table></figure><p>也就是说，<code>balance++</code> 本质上是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">读 → 改 → 写</span><br></pre></td></tr></table></figure><p>这三个动作之间可能被其他线程插入。</p><p>假设初始：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">balance = 10</span><br></pre></td></tr></table></figure><p>两个线程同时执行 <code>balance++</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">线程 A：读 balance = 10</span><br><span class="line">线程 B：读 balance = 10</span><br><span class="line">线程 A：计算 11</span><br><span class="line">线程 B：计算 11</span><br><span class="line">线程 A：写回 11</span><br><span class="line">线程 B：写回 11</span><br></pre></td></tr></table></figure><p>最终结果是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">balance = 11</span><br></pre></td></tr></table></figure><p>但正确结果应该是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">balance = 12</span><br></pre></td></tr></table></figure><p>问题不在于加法本身复杂，而在于“读、改、写”不是原子的。</p><hr><h2 id="3-竞争条件-Race-Condition"><a href="#3-竞争条件-Race-Condition" class="headerlink" title="3. 竞争条件 Race Condition"></a>3. 竞争条件 Race Condition</h2><p>竞争条件指的是：</p><blockquote><p>多个线程同时访问共享数据，并且最终结果依赖于具体执行顺序。</p></blockquote><p>它通常满足三个条件：</p><ol><li>存在多个并发执行流；</li><li>它们访问同一份共享数据；</li><li>至少有一个执行流会修改数据；</li><li>程序结果依赖访问顺序。</li></ol><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">buffer[buffer_write_cnt] = msg;</span><br><span class="line">buffer_write_cnt = (buffer_write_cnt + <span class="number">1</span>) % <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>如果两个生产者同时执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">生产者 A 读到 buffer_write_cnt = 2</span><br><span class="line">生产者 B 也读到 buffer_write_cnt = 2</span><br><span class="line">生产者 A 写 buffer[2]</span><br><span class="line">生产者 B 也写 buffer[2]</span><br></pre></td></tr></table></figure><p>结果就是两个生产者写到了同一个缓冲区位置，导致数据覆盖。</p><p>竞争条件最麻烦的地方是：它不一定每次都出现。程序可能运行十次都正常，第十一次才出错。</p><p>因此：</p><blockquote><p>程序“偶尔正确”，不代表程序没有并发问题。</p></blockquote><hr><h2 id="❗-本节核心结论：-竞争条件的本质是共享数据的最终状态依赖不可控的线程交错顺序。"><a href="#❗-本节核心结论：-竞争条件的本质是共享数据的最终状态依赖不可控的线程交错顺序。" class="headerlink" title="❗ 本节核心结论： 竞争条件的本质是共享数据的最终状态依赖不可控的线程交错顺序。"></a>❗ <strong>本节核心结论：</strong> 竞争条件的本质是共享数据的最终状态依赖不可控的线程交错顺序。</h2><h2 id="4-临界区-Critical-Section"><a href="#4-临界区-Critical-Section" class="headerlink" title="4. 临界区 Critical Section"></a>4. 临界区 Critical Section</h2><p>临界区是指：</p><blockquote><p>访问共享资源，并且多个线程同时进入会导致错误的代码区域。</p></blockquote><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">buffer[buffer_write_cnt] = msg;</span><br><span class="line">buffer_write_cnt = (buffer_write_cnt + <span class="number">1</span>) % <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>这段代码访问了共享的 <code>buffer</code> 和 <code>buffer_write_cnt</code>，如果多个线程同时进入，就可能发生覆盖。因此它应当被视为临界区。</p><p>但要注意：</p><blockquote><p>不能简单断言“程序中互斥的区域就是临界区”。</p></blockquote><p>更准确的说法是：</p><blockquote><p><strong>需要被互斥保护的共享资源访问区域，通常就是临界区。</strong></p></blockquote><p>也就是说，临界区的关键不是“它被加锁了”，而是：</p><ul><li>它访问共享资源；</li><li>并发访问会出错；</li><li>所以它需要互斥保护。</li></ul><p>如果你把一段完全不访问共享资源的代码也加锁，它虽然被互斥执行，但不一定是概念上真正的临界区。</p><hr><h2 id="5-解决临界区问题的三个要求"><a href="#5-解决临界区问题的三个要求" class="headerlink" title="5. 解决临界区问题的三个要求"></a>5. 解决临界区问题的三个要求</h2><p>一个合格的临界区解决方案通常要满足三个要求：</p><table><thead><tr><th>要求</th><th>含义</th><th>反例</th></tr></thead><tbody><tr><td>互斥访问</td><td>同一时刻最多一个线程进入临界区</td><td>两个线程同时修改共享变量</td></tr><tr><td>空闲让进</td><td>临界区没人使用时，应允许等待者进入</td><td>临界区空着，但所有线程都进不去</td></tr><tr><td>有限等待</td><td>线程申请进入后，应在有限时间内获得机会</td><td>某个线程永远排不上</td></tr></tbody></table><p>这三个要求不是一回事。</p><p>例如，普通自旋锁可以保证互斥访问，但不一定保证有限等待。因为多个线程抢锁时，某个线程可能运气很差，一直抢不到。</p><hr><h2 id="6-为什么关闭中断在多核下不够"><a href="#6-为什么关闭中断在多核下不够" class="headerlink" title="6. 为什么关闭中断在多核下不够"></a>6. 为什么关闭中断在多核下不够</h2><p>在单核系统中，关闭中断可以在一定程度上保护临界区。因为线程切换通常依赖时钟中断。关闭中断后，当前线程不会被调度器打断。</p><p>但在多核系统中，这种方法不够。</p><p>原因是：</p><blockquote><p>关闭当前 CPU 核的中断，只能防止当前核被打断，不能阻止其他 CPU 核上的线程同时运行。</p></blockquote><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">CPU0：线程 A 关闭中断，进入临界区</span><br><span class="line">CPU1：线程 B 仍然可以运行，也进入同一个临界区</span><br></pre></td></tr></table></figure><p>这样仍然会破坏互斥访问。</p><p>所以，多核系统需要跨 CPU 生效的同步机制，例如互斥锁、原子操作、自旋锁等。</p><hr><h2 id="❗-本节核心结论：-关闭中断只能控制当前-CPU-的调度行为，不能阻止其他-CPU-核并发访问共享资源。"><a href="#❗-本节核心结论：-关闭中断只能控制当前-CPU-的调度行为，不能阻止其他-CPU-核并发访问共享资源。" class="headerlink" title="❗ 本节核心结论： 关闭中断只能控制当前 CPU 的调度行为，不能阻止其他 CPU 核并发访问共享资源。"></a>❗ <strong>本节核心结论：</strong> 关闭中断只能控制当前 CPU 的调度行为，不能阻止其他 CPU 核并发访问共享资源。</h2><h2 id="7-Peterson-算法：软件层面的临界区门卫"><a href="#7-Peterson-算法：软件层面的临界区门卫" class="headerlink" title="7. Peterson 算法：软件层面的临界区门卫"></a>7. Peterson 算法：软件层面的临界区门卫</h2><p>Peterson 算法是一种经典的软件互斥算法，主要用于两个线程。</p><p>它使用两个变量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">flag[i] = <span class="literal">true</span>;  <span class="comment">// 表示线程 i 想进入临界区</span></span><br><span class="line">turn = j;        <span class="comment">// 表示如果冲突，就让线程 j 先进</span></span><br></pre></td></tr></table></figure><p>线程 0 的逻辑大致是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">flag[<span class="number">0</span>] = <span class="literal">true</span>;</span><br><span class="line">turn = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (flag[<span class="number">1</span>] == <span class="literal">true</span> &amp;&amp; turn == <span class="number">1</span>)</span><br><span class="line">    ;</span><br><span class="line"></span><br><span class="line">进入临界区;</span><br><span class="line"></span><br><span class="line">flag[<span class="number">0</span>] = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure><p>线程 1 类似，只是编号反过来。</p><p>Peterson 算法的思想不是“谁抢得快谁进”，而是“如果双方都想进，就通过 <code>turn</code> 谦让”。</p><p>可以这样理解：</p><table><thead><tr><th>变量</th><th>作用</th></tr></thead><tbody><tr><td><code>flag[i]</code></td><td>表示线程 i 是否想进入临界区</td></tr><tr><td><code>turn</code></td><td>当双方都想进时，决定让谁先进入</td></tr></tbody></table><p>如果只有线程 0 想进入，<code>flag[1] == false</code>，线程 0 可以直接进入。</p><p>如果两个线程都想进入，就看 <code>turn</code>。<code>turn</code> 只能是 0 或 1，因此最终只会允许一个线程进入，另一个等待。</p><p>所以，Peterson 算法可以理解为：</p><blockquote><p>解决“谁可以进入临界区”的软件门卫算法。</p></blockquote><p>它不是处理临界区里面的业务逻辑，而是处理临界区入口处的进入控制问题。</p><hr><h2 id="8-Peterson-算法为什么受现代-CPU-限制"><a href="#8-Peterson-算法为什么受现代-CPU-限制" class="headerlink" title="8. Peterson 算法为什么受现代 CPU 限制"></a>8. Peterson 算法为什么受现代 CPU 限制</h2><p>Peterson 算法依赖一个重要前提：</p><blockquote><p>程序中的读写操作严格按照代码顺序执行，并且其他 CPU 看到的顺序也符合这个顺序。</p></blockquote><p>但现代 CPU 可能进行乱序执行 out-of-order execution。</p><h3 id="8-1-什么是乱序执行"><a href="#8-1-什么是乱序执行" class="headerlink" title="8.1 什么是乱序执行"></a>8.1 什么是乱序执行</h3><p>程序顺序可能是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">flag[<span class="number">0</span>] = <span class="literal">true</span>;</span><br><span class="line">turn = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span> (flag[<span class="number">1</span>] &amp;&amp; turn == <span class="number">1</span>)</span><br><span class="line">    ;</span><br></pre></td></tr></table></figure><p>程序员以为 CPU 一定按下面顺序执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1. 写 flag[0]</span><br><span class="line">2. 写 turn</span><br><span class="line">3. 读取 flag[1] 和 turn</span><br></pre></td></tr></table></figure><p>但现代 CPU 为了提高性能，可能在不影响单线程结果的前提下调整执行顺序。</p><p>例如：</p><ul><li>写操作可能先进入 store buffer，尚未立刻被其他 CPU 看到；</li><li>后面的读操作可能先执行；</li><li>不同 CPU 核看到内存变化的时间可能不同。</li></ul><p>于是，在另一个线程看来，可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">线程 0 还没真正让其他 CPU 看到 flag[0] = true</span><br><span class="line">线程 0 已经开始检查 flag[1]</span><br></pre></td></tr></table></figure><p>这样两个线程都可能误以为对方没有申请进入临界区，最终同时进入临界区。</p><h3 id="8-2-为什么-CPU-要设计乱序执行"><a href="#8-2-为什么-CPU-要设计乱序执行" class="headerlink" title="8.2 为什么 CPU 要设计乱序执行"></a>8.2 为什么 CPU 要设计乱序执行</h3><p>原因很简单：</p><blockquote><p>CPU 很快，内存很慢。</p></blockquote><p>如果 CPU 必须严格等每条指令完成后再执行下一条，会经常空等。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">a = load_from_memory();  <span class="comment">// 很慢</span></span><br><span class="line">b = <span class="number">1</span> + <span class="number">2</span>;               <span class="comment">// 很快，且不依赖 a</span></span><br></pre></td></tr></table></figure><p>如果 <code>b = 1 + 2</code> 不依赖 <code>a</code>，CPU 可以先算 <code>b</code>，不必等待内存加载完成。</p><p>乱序执行的好处包括：</p><ul><li>提高流水线利用率；</li><li>隐藏访存延迟；</li><li>增加指令级并行性；</li><li>提高单核性能。</li></ul><p>但并发算法往往依赖“其他线程看到的内存顺序”。这就使裸 Peterson 算法在现代硬件上不可靠。</p><p>现代系统通常使用：</p><ul><li>硬件原子操作；</li><li>内存屏障 memory barrier；</li><li>C&#x2F;C++ <code>std::atomic</code>；</li><li>操作系统提供的互斥锁。</li></ul><hr><h2 id="❗-本节核心结论：-Peterson-算法依赖严格内存顺序，而现代-CPU-的乱序执行和缓存机制可能破坏这种顺序假设。"><a href="#❗-本节核心结论：-Peterson-算法依赖严格内存顺序，而现代-CPU-的乱序执行和缓存机制可能破坏这种顺序假设。" class="headerlink" title="❗ 本节核心结论： Peterson 算法依赖严格内存顺序，而现代 CPU 的乱序执行和缓存机制可能破坏这种顺序假设。"></a>❗ <strong>本节核心结论：</strong> Peterson 算法依赖严格内存顺序，而现代 CPU 的乱序执行和缓存机制可能破坏这种顺序假设。</h2><h2 id="9-原子操作-Atomic-Operation"><a href="#9-原子操作-Atomic-Operation" class="headerlink" title="9. 原子操作 Atomic Operation"></a>9. 原子操作 Atomic Operation</h2><p>原子操作指的是：</p><blockquote><p>不可被打断、不会被其他线程看到中间状态的操作。</p></blockquote><p>它强调 all-or-nothing：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">要么完整发生</span><br><span class="line">要么完全没发生</span><br><span class="line">不会出现执行一半的可见状态</span><br></pre></td></tr></table></figure><p>原子操作常用于构造锁和无锁数据结构。</p><h3 id="9-1-CAS"><a href="#9-1-CAS" class="headerlink" title="9.1 CAS"></a>9.1 CAS</h3><p>CAS，即 Compare-And-Swap，可以理解为一个原子的“读—比较—条件写”操作。</p><p>伪代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">CAS</span><span class="params">(<span class="type">int</span> *addr, <span class="type">int</span> expected, <span class="type">int</span> new_value)</span> &#123;</span><br><span class="line">    <span class="type">int</span> old = *addr;</span><br><span class="line">    <span class="keyword">if</span> (*addr == expected)</span><br><span class="line">        *addr = new_value;</span><br><span class="line">    <span class="keyword">return</span> old;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>语义是：</p><ol><li>读取 <code>addr</code> 中的旧值；</li><li>判断旧值是否等于 <code>expected</code>；</li><li>如果相等，就写入 <code>new_value</code>；</li><li>返回旧值。</li></ol><p>虽然逻辑上包含多个步骤，但硬件保证它作为一个整体原子执行。</p><p>因此，CAS 不是“只能把两个动作合并成原子操作”。更准确地说：</p><blockquote><p>CAS 是一个原子的读—比较—条件写操作。</p></blockquote><h3 id="9-2-FAA"><a href="#9-2-FAA" class="headerlink" title="9.2 FAA"></a>9.2 FAA</h3><p>FAA，即 Fetch-And-Add，可以理解为：</p><ol><li>读取旧值；</li><li>加上某个数；</li><li>写回新值；</li><li>返回旧值。</li></ol><p>Ticket Lock 常用 FAA 来发号。</p><p>所以，原子操作并不限于“两个动作”。硬件可以提供不同形式的原子读改写指令。</p><p>但要注意：</p><blockquote><p>CAS 不是万能事务机制。</p></blockquote><p>如果你想让任意多行 C 代码整体互斥执行，通常不是靠一个 CAS 包住所有代码，而是使用锁：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mutex);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 多行临界区代码</span></span><br><span class="line"></span><br><span class="line">unlock(&amp;mutex);</span><br></pre></td></tr></table></figure><hr><h2 id="10-互斥锁-Mutex"><a href="#10-互斥锁-Mutex" class="headerlink" title="10. 互斥锁 Mutex"></a>10. 互斥锁 Mutex</h2><p>互斥锁提供两个基本操作：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;mutex);</span><br><span class="line">unlock(&amp;mutex);</span><br></pre></td></tr></table></figure><p>典型使用方式是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">lock(&amp;buffer_lock);</span><br><span class="line"></span><br><span class="line">buffer[buffer_write_cnt] = msg;</span><br><span class="line">buffer_write_cnt = (buffer_write_cnt + <span class="number">1</span>) % <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">unlock(&amp;buffer_lock);</span><br></pre></td></tr></table></figure><h3 id="10-1-为什么同一把锁能保护共享资源"><a href="#10-1-为什么同一把锁能保护共享资源" class="headerlink" title="10.1 为什么同一把锁能保护共享资源"></a>10.1 为什么同一把锁能保护共享资源</h3><p>核心原因是：</p><blockquote><p>锁本身是一个共享状态，并且加锁操作会原子地改变这个状态。</p></blockquote><p>假设锁变量有两个状态：</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td><code>0</code></td><td>没人持有锁</td></tr><tr><td><code>1</code></td><td>锁已被占用</td></tr></tbody></table><p>加锁可以用 CAS 实现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (CAS(lock, <span class="number">0</span>, <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line">    ;</span><br></pre></td></tr></table></figure><p>含义是：</p><ul><li>如果 <code>lock == 0</code>，当前线程把它改成 <code>1</code>，成功进入临界区；</li><li>如果 <code>lock == 1</code>，说明已有线程在临界区，当前线程等待。</li></ul><p>因为 CAS 是原子的，所以不可能两个线程同时成功把 <code>lock</code> 从 <code>0</code> 改成 <code>1</code>。</p><p>因此，只要满足三个前提：</p><ol><li>所有访问同一组共享资源的代码都使用同一把锁；</li><li>没有任何代码绕过锁直接访问共享资源；</li><li>锁本身实现正确；</li></ol><p>那么这些访问就会被串行化。</p><p>也就是说，同一时刻最多只有一个线程能进入由这把锁保护的临界区。</p><h3 id="10-2-锁保护的是一组共享状态"><a href="#10-2-锁保护的是一组共享状态" class="headerlink" title="10.2 锁保护的是一组共享状态"></a>10.2 锁保护的是一组共享状态</h3><p>一把锁保护的不是某一行代码，而是一组共享状态。</p><p>例如，生产者消费者缓冲区中要一起保护：</p><ul><li><code>buffer</code></li><li><code>buffer_write_cnt</code></li><li><code>buffer_read_cnt</code></li><li>其他相关计数变量</li></ul><p>如果写操作用了 <code>buffer_lock</code>，但读操作没用，仍然会出错。<br>如果写操作用 <code>lockA</code>，读操作用 <code>lockB</code>，也不能保证互斥。</p><hr><h2 id="❗-本节核心结论：-互斥锁通过原子地改变锁状态，让所有使用同一把锁访问同一组共享资源的代码串行执行。"><a href="#❗-本节核心结论：-互斥锁通过原子地改变锁状态，让所有使用同一把锁访问同一组共享资源的代码串行执行。" class="headerlink" title="❗ 本节核心结论： 互斥锁通过原子地改变锁状态，让所有使用同一把锁访问同一组共享资源的代码串行执行。"></a>❗ <strong>本节核心结论：</strong> 互斥锁通过原子地改变锁状态，让所有使用同一把锁访问同一组共享资源的代码串行执行。</h2><h2 id="11-自旋锁-Spin-Lock"><a href="#11-自旋锁-Spin-Lock" class="headerlink" title="11. 自旋锁 Spin Lock"></a>11. 自旋锁 Spin Lock</h2><p>自旋锁是一种互斥锁实现方式。</p><p>它的特点是：</p><blockquote><p>拿不到锁时，不睡眠，而是一直循环检查锁是否释放。</p></blockquote><p>典型代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">lock</span><span class="params">(<span class="type">int</span> *lock)</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (atomic_CAS(lock, <span class="number">0</span>, <span class="number">1</span>) != <span class="number">0</span>)</span><br><span class="line">        ;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">unlock</span><span class="params">(<span class="type">int</span> *lock)</span> &#123;</span><br><span class="line">    *lock = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>自旋锁的执行过程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">线程 A：CAS(lock, 0, 1) 成功</span><br><span class="line">线程 A：进入临界区</span><br><span class="line"></span><br><span class="line">线程 B：CAS(lock, 0, 1) 失败</span><br><span class="line">线程 B：持续循环等待</span><br><span class="line"></span><br><span class="line">线程 A：释放锁，lock = 0</span><br><span class="line"></span><br><span class="line">线程 B：CAS(lock, 0, 1) 成功</span><br><span class="line">线程 B：进入临界区</span><br></pre></td></tr></table></figure><p>自旋锁的优点是：</p><ul><li>实现简单；</li><li>响应快；</li><li>临界区很短时效率高；</li><li>避免线程睡眠和唤醒的上下文切换开销。</li></ul><p>但它的缺点也很明显：</p><ul><li>等待期间持续占用 CPU；</li><li>锁持有时间长时浪费严重；</li><li>普通自旋锁不保证公平；</li><li>可能导致某些线程长期抢不到锁。</li></ul><p>所以，自旋锁适合：</p><blockquote><p>临界区非常短、锁竞争不严重、线程睡眠唤醒成本高的场景。</p></blockquote><hr><h2 id="12-Ticket-Lock：给自旋锁加上排队秩序"><a href="#12-Ticket-Lock：给自旋锁加上排队秩序" class="headerlink" title="12. Ticket Lock：给自旋锁加上排队秩序"></a>12. Ticket Lock：给自旋锁加上排队秩序</h2><p>普通自旋锁的问题是：谁能抢到锁比较随机，不保证先来先服务。</p><p>Ticket Lock，即排号自旋锁，用“拿号—叫号”的方式实现公平性。</p><p>它通常包含两个变量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lock</span> &#123;</span></span><br><span class="line">    <span class="keyword">volatile</span> <span class="type">int</span> owner;</span><br><span class="line">    <span class="keyword">volatile</span> <span class="type">int</span> next;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>含义是：</p><table><thead><tr><th>变量</th><th>含义</th></tr></thead><tbody><tr><td><code>next</code></td><td>下一个要发出的号码</td></tr><tr><td><code>owner</code></td><td>当前轮到哪个号码进入临界区</td></tr></tbody></table><p>加锁过程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> my_ticket = FAA(&amp;lock-&gt;next, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (lock-&gt;owner != my_ticket)</span><br><span class="line">    ;</span><br></pre></td></tr></table></figure><p>解锁过程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lock-&gt;owner++;</span><br></pre></td></tr></table></figure><p>执行过程如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">线程 A 拿到 3 号，owner = 3，进入临界区</span><br><span class="line">线程 B 拿到 4 号，等待 owner == 4</span><br><span class="line">线程 C 拿到 5 号，等待 owner == 5</span><br><span class="line"></span><br><span class="line">线程 A 退出，owner++</span><br><span class="line">owner = 4</span><br><span class="line"></span><br><span class="line">线程 B 进入临界区</span><br></pre></td></tr></table></figure><p>Ticket Lock 的本质是：</p><blockquote><p>把进入临界区的问题变成 FIFO &#x2F; FCFS 排队问题。</p></blockquote><p>它解决了普通自旋锁不公平的问题。只要前面的线程会在有限时间内释放锁，后面的线程就能在有限等待后进入。</p><p>但 Ticket Lock 仍然是自旋锁。没轮到自己的线程仍然会循环检查 <code>owner</code>，所以它没有解决忙等浪费 CPU 的问题。</p><hr><h2 id="❗-本节核心结论：-Ticket-Lock-用排号机制保证公平性和有限等待，但仍然保留自旋等待的-CPU-消耗。"><a href="#❗-本节核心结论：-Ticket-Lock-用排号机制保证公平性和有限等待，但仍然保留自旋等待的-CPU-消耗。" class="headerlink" title="❗ 本节核心结论： Ticket Lock 用排号机制保证公平性和有限等待，但仍然保留自旋等待的 CPU 消耗。"></a>❗ <strong>本节核心结论：</strong> Ticket Lock 用排号机制保证公平性和有限等待，但仍然保留自旋等待的 CPU 消耗。</h2><h2 id="13-临界区入口也可以看成调度问题"><a href="#13-临界区入口也可以看成调度问题" class="headerlink" title="13. 临界区入口也可以看成调度问题"></a>13. 临界区入口也可以看成调度问题</h2><p>当多个线程都想进入临界区时，锁需要决定：</p><blockquote><p>下一个获得锁的是谁？</p></blockquote><p>这其实和调度问题有相似之处。</p><p>例如：</p><table><thead><tr><th>锁竞争策略</th><th>类似调度思想</th></tr></thead><tbody><tr><td>普通自旋锁</td><td>非公平竞争，谁抢到谁进</td></tr><tr><td>Ticket Lock</td><td>FCFS &#x2F; FIFO</td></tr><tr><td>优先级锁</td><td>优先级调度</td></tr><tr><td>公平锁</td><td>防止饥饿</td></tr><tr><td>随机唤醒</td><td>随机调度</td></tr></tbody></table><p>因此，可以把“进入临界区”理解成一种资源调度问题。</p><p>但它和 CPU 调度也有区别：</p><ul><li>CPU 调度分配的是 CPU 时间片；</li><li>锁调度分配的是临界区进入权；</li><li>临界区通常应该尽快执行完并释放锁；</li><li>不适合像 RR 那样“临界区执行一半，换另一个线程进来”。</li></ul><p>因为临界区执行一半时，共享状态可能正处于中间状态。如果强行换另一个线程进入，会破坏一致性。</p><p>所以，锁竞争更常见的是 FIFO、公平锁、非公平锁、优先级锁，而不是严格意义上的 RR。</p><hr><h2 id="14-volatile：防编译器优化，不保证线程安全"><a href="#14-volatile：防编译器优化，不保证线程安全" class="headerlink" title="14. volatile：防编译器优化，不保证线程安全"></a>14. <code>volatile</code>：防编译器优化，不保证线程安全</h2><p><code>volatile</code> 是本讲最容易混淆的概念之一。</p><p>在 C&#x2F;C++ 语境中，<code>volatile</code> 的核心作用是：</p><blockquote><p>告诉编译器：这个变量可能被当前控制流之外的东西改变，所以每次访问都要真正读写内存，不要随意优化掉。</p></blockquote><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">int</span> flag;</span><br></pre></td></tr></table></figure><p>编译器看到 <code>volatile</code> 后，不应把：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (flag == <span class="number">0</span>) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>优化成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (flag == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>因为 <code>flag</code> 可能被硬件、中断处理程序或其他执行上下文修改。</p><h3 id="14-1-volatile-能解决什么"><a href="#14-1-volatile-能解决什么" class="headerlink" title="14.1 volatile 能解决什么"></a>14.1 <code>volatile</code> 能解决什么</h3><p>它主要防止编译器做某些优化，例如：</p><ul><li>把变量长期缓存到寄存器；</li><li>认为变量不会变化；</li><li>把多次读取优化成一次读取；</li><li>删除看似无用的读写。</li></ul><p>常见用途包括：</p><ul><li>内存映射 I&#x2F;O；</li><li>硬件寄存器访问；</li><li>某些中断相关变量。</li></ul><h3 id="14-2-volatile-不能解决什么"><a href="#14-2-volatile-不能解决什么" class="headerlink" title="14.2 volatile 不能解决什么"></a>14.2 <code>volatile</code> 不能解决什么</h3><p><code>volatile</code> 不能保证：</p><ul><li>原子性；</li><li>互斥；</li><li>线程安全；</li><li><code>x++</code> 安全；</li><li>足够的跨 CPU 内存同步；</li><li>正确的并发访问顺序。</li></ul><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="type">int</span> balance = <span class="number">0</span>;</span><br><span class="line">balance++;</span><br></pre></td></tr></table></figure><p>即使 <code>balance</code> 是 <code>volatile</code>，<code>balance++</code> 仍然可能被拆成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">读 balance</span><br><span class="line">加 1</span><br><span class="line">写回 balance</span><br></pre></td></tr></table></figure><p>两个线程同时执行时，仍然可能丢失更新。</p><p>所以：</p><blockquote><p><code>volatile</code> 不是锁，也不是原子操作。</p></blockquote><p>可以用一句话区分：</p><table><thead><tr><th>工具</th><th>解决什么问题</th><th>能否保证 <code>x++</code> 线程安全</th></tr></thead><tbody><tr><td><code>volatile</code></td><td>防止编译器省略或缓存访问</td><td>不能</td></tr><tr><td><code>atomic</code></td><td>保证特定变量的原子访问和内存顺序</td><td>可以</td></tr><tr><td><code>lock</code></td><td>保护一整段临界区</td><td>可以</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：-volatile-主要约束编译器优化，不负责互斥、原子性和完整线程安全。"><a href="#❗-本节核心结论：-volatile-主要约束编译器优化，不负责互斥、原子性和完整线程安全。" class="headerlink" title="❗ 本节核心结论： volatile 主要约束编译器优化，不负责互斥、原子性和完整线程安全。"></a>❗ <strong>本节核心结论：</strong> <code>volatile</code> 主要约束编译器优化，不负责互斥、原子性和完整线程安全。</h2><h2 id="15-重要对比表"><a href="#15-重要对比表" class="headerlink" title="15. 重要对比表"></a>15. 重要对比表</h2><h3 id="15-1-竞争条件、临界区、互斥锁"><a href="#15-1-竞争条件、临界区、互斥锁" class="headerlink" title="15.1 竞争条件、临界区、互斥锁"></a>15.1 竞争条件、临界区、互斥锁</h3><table><thead><tr><th>概念</th><th>关注点</th><th>直白理解</th></tr></thead><tbody><tr><td>竞争条件</td><td>错误现象</td><td>多线程抢着改共享数据，结果依赖顺序</td></tr><tr><td>临界区</td><td>危险代码段</td><td>不能让多个线程同时执行的共享资源访问区域</td></tr><tr><td>互斥锁</td><td>解决工具</td><td>进入危险区域前先拿钥匙，出来后还钥匙</td></tr></tbody></table><h3 id="15-2-Peterson-算法、原子操作、互斥锁"><a href="#15-2-Peterson-算法、原子操作、互斥锁" class="headerlink" title="15.2 Peterson 算法、原子操作、互斥锁"></a>15.2 Peterson 算法、原子操作、互斥锁</h3><table><thead><tr><th>对比项</th><th>Peterson 算法</th><th>原子操作</th><th>互斥锁</th></tr></thead><tbody><tr><td>层次</td><td>软件算法</td><td>硬件支持</td><td>同步抽象</td></tr><tr><td>作用</td><td>控制两个线程进入临界区</td><td>提供不可打断操作</td><td>保护临界区</td></tr><tr><td>典型变量</td><td><code>flag[]</code>、<code>turn</code></td><td>CAS、FAA</td><td><code>mutex</code></td></tr><tr><td>优点</td><td>展示互斥思想清楚</td><td>现代系统常用</td><td>易用、通用</td></tr><tr><td>局限</td><td>受线程数和内存顺序限制</td><td>粒度较底层</td><td>使用不当会死锁或性能下降</td></tr></tbody></table><h3 id="15-3-普通自旋锁-vs-Ticket-Lock"><a href="#15-3-普通自旋锁-vs-Ticket-Lock" class="headerlink" title="15.3 普通自旋锁 vs Ticket Lock"></a>15.3 普通自旋锁 vs Ticket Lock</h3><table><thead><tr><th>对比项</th><th>普通自旋锁</th><th>Ticket Lock</th></tr></thead><tbody><tr><td>抢锁方式</td><td>谁 CAS 成功谁进</td><td>先拿号，按号进入</td></tr><tr><td>公平性</td><td>不保证</td><td>保证先到先服务</td></tr><tr><td>有限等待</td><td>不保证</td><td>前序线程释放时可保证</td></tr><tr><td>CPU 消耗</td><td>忙等</td><td>仍然忙等</td></tr><tr><td>适合场景</td><td>临界区短、竞争轻</td><td>临界区短，但希望公平</td></tr></tbody></table><hr><h2 id="16-易混点整理"><a href="#16-易混点整理" class="headerlink" title="16. 易混点整理"></a>16. 易混点整理</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>“互斥区域就是临界区”</td><td>不准确。临界区是需要互斥保护的共享资源访问区域</td></tr><tr><td>Peterson 算法的作用</td><td>控制谁能进入临界区，不处理临界区内部业务</td></tr><tr><td>乱序执行是不是错误</td><td>不是，它是 CPU 提升性能的机制，但会影响并发可见性</td></tr><tr><td>CAS 是否只包含两个动作</td><td>不是。CAS 是原子的读—比较—条件写</td></tr><tr><td>CAS 能否替代所有锁</td><td>不能。复杂多行临界区通常仍需要锁</td></tr><tr><td>所有访问同一资源是否必须用同一把锁</td><td>是，否则无法保证互斥</td></tr><tr><td>Ticket Lock 是否解决忙等</td><td>没有。它解决公平性，不解决自旋等待</td></tr><tr><td><code>volatile</code> 是否保证线程安全</td><td>不保证。它不是锁，也不是 atomic</td></tr></tbody></table><hr><h2 id="17-复习清单"><a href="#17-复习清单" class="headerlink" title="17. 复习清单"></a>17. 复习清单</h2><p>学完本讲后，应能回答：</p><ul><li>为什么 <code>balance++</code> 在多线程环境下可能出错？</li><li>什么是竞争条件？</li><li>竞争条件需要满足哪些关键条件？</li><li>什么是临界区？</li><li>临界区和共享变量是什么关系？</li><li>为什么不能说“所有互斥区域都是临界区”？</li><li>解决临界区问题的三个要求是什么？</li><li>为什么关闭中断在多核系统中不够？</li><li>Peterson 算法中的 <code>flag[]</code> 表示什么？</li><li>Peterson 算法中的 <code>turn</code> 为什么体现“谦让”？</li><li>为什么现代 CPU 的乱序执行可能破坏 Peterson 算法？</li><li>什么是原子操作？</li><li>CAS 的核心语义是什么？</li><li>CAS 和普通多条语句有什么区别？</li><li>为什么同一组共享资源要用同一把锁保护？</li><li>普通自旋锁为什么可能导致饥饿？</li><li>Ticket Lock 中 <code>owner</code> 和 <code>next</code> 分别表示什么？</li><li>Ticket Lock 如何体现 FIFO &#x2F; FCFS？</li><li><code>volatile</code> 能解决什么问题？</li><li>为什么 <code>volatile</code> 不能保证 <code>x++</code> 线程安全？</li></ul><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>同步原语 I 的核心可以压缩为一句话：</p><blockquote><p><strong>多个线程同时访问共享资源时，程序的正确性不能依赖偶然的执行顺序，而必须通过临界区和同步原语强制建立安全访问规则。</strong></p></blockquote><p><code>balance++</code> 出错说明简单语句也可能包含非原子的读—改—写；生产者消费者模型说明共享缓冲区和共享下标都需要保护；竞争条件描述了并发错误的根源；临界区标记了必须互斥访问的代码范围；Peterson 算法展示了软件互斥思想；CAS、FAA 等原子操作为现代锁实现提供硬件基础；互斥锁把底层原子操作封装成易用抽象；自旋锁适合短临界区但可能浪费 CPU；Ticket Lock 用排号机制改善公平性；<code>volatile</code> 则只约束编译器优化，不能替代锁或原子操作。</p><p>真正理解本讲，不是记住某个锁的代码，而是能判断：</p><ul><li>哪些共享状态需要保护；</li><li>哪些代码属于临界区；</li><li>哪些访问路径必须使用同一把锁；</li><li>哪种锁适合当前场景；</li><li>哪些机制只解决可见性，不解决原子性；</li><li>哪些方案保证互斥，但不保证公平或有限等待。</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第13讲 进程间通信</title>
      <link href="/2026/06/11/%E7%AC%AC13%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1/"/>
      <url>/2026/06/11/%E7%AC%AC13%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1/</url>
      
        <content type="html"><![CDATA[<h1 id="第13讲-进程间通信：共享内存、消息队列与同步语义"><a href="#第13讲-进程间通信：共享内存、消息队列与同步语义" class="headerlink" title="第13讲 进程间通信：共享内存、消息队列与同步语义"></a>第13讲 进程间通信：共享内存、消息队列与同步语义</h1><p>本文根据《第13讲 进程间通信.md》的对话转录整理而成。核心问题是：<strong>进程间通信 IPC 不只是“两个进程能传数据”，还要解决数据放在哪里、谁来读取、何时等待、如何避免覆盖和读空，以及通信双方是否需要直接认识彼此。</strong> </p><hr><h2 id="1-为什么需要进程间通信"><a href="#1-为什么需要进程间通信" class="headerlink" title="1. 为什么需要进程间通信"></a>1. 为什么需要进程间通信</h2><p>进程默认拥有独立的地址空间。一个进程不能随意访问另一个进程的内存，这是操作系统提供隔离性和安全性的基础。</p><p>但现实系统中，进程之间经常需要协作。例如：</p><ul><li>浏览器进程和渲染进程交换页面数据；</li><li>shell 通过管道把一个命令的输出交给另一个命令；</li><li>服务器主进程把请求分发给 worker 进程；</li><li>多个进程共享同一批计算结果；</li><li>客户端进程向服务端进程发送请求。</li></ul><p>因此，操作系统需要提供 IPC，Inter-Process Communication，即进程间通信机制。</p><p>IPC 的核心问题可以分为三类：</p><ol><li><p><strong>数据如何传递</strong></p><ul><li>是放在共享内存中，还是通过内核转发？</li></ul></li><li><p><strong>通信双方如何建立关系</strong></p><ul><li>是直接指定对方进程，还是通过中间对象通信？</li></ul></li><li><p><strong>通信操作如何等待</strong></p><ul><li>是同步等待，还是异步返回？</li></ul></li></ol><hr><h2 id="2-IPC-的两条主线：共享内存与消息传递"><a href="#2-IPC-的两条主线：共享内存与消息传递" class="headerlink" title="2. IPC 的两条主线：共享内存与消息传递"></a>2. IPC 的两条主线：共享内存与消息传递</h2><p>IPC 机制可以大致分成两类：</p><table><thead><tr><th>类型</th><th>基本思想</th><th>特点</th></tr></thead><tbody><tr><td>共享内存</td><td>多个进程映射同一块内存区域</td><td>快，但同步复杂</td></tr><tr><td>消息传递</td><td>通过内核或中间对象传递消息</td><td>更安全、结构清晰，但开销可能更高</td></tr></tbody></table><p>共享内存的优势是速度快。数据放在同一块内存中，进程之间不必每次都通过内核复制大量数据。</p><p>但共享内存的困难也很明显：</p><ul><li>发送者不能覆盖接收者还没读的数据；</li><li>接收者不能读取还没有写入的数据；</li><li>多个发送者不能同时写同一个位置；</li><li>多个接收者不能重复读或读错位置；</li><li>如果一直等待，可能浪费 CPU。</li></ul><p>消息传递则把这些问题更多交给内核或消息队列来管理。发送者发送消息，接收者接收消息，通信语义更清晰，但可能需要更多系统调用和数据复制。</p><hr><h2 id="❗-本节核心结论：-共享内存追求速度，但需要程序员处理同步；消息传递牺牲一部分性能，换来更清晰的通信结构和更强的隔离性。"><a href="#❗-本节核心结论：-共享内存追求速度，但需要程序员处理同步；消息传递牺牲一部分性能，换来更清晰的通信结构和更强的隔离性。" class="headerlink" title="❗ 本节核心结论： 共享内存追求速度，但需要程序员处理同步；消息传递牺牲一部分性能，换来更清晰的通信结构和更强的隔离性。"></a>❗ <strong>本节核心结论：</strong> 共享内存追求速度，但需要程序员处理同步；消息传递牺牲一部分性能，换来更清晰的通信结构和更强的隔离性。</h2><h2 id="3-共享内存：快，但必须处理同步"><a href="#3-共享内存：快，但必须处理同步" class="headerlink" title="3. 共享内存：快，但必须处理同步"></a>3. 共享内存：快，但必须处理同步</h2><p>共享内存通信的基本模型是：</p><ol><li>操作系统创建或映射一块共享内存区域；</li><li>两个或多个进程都能访问这块区域；</li><li>一个进程向共享区域写入数据；</li><li>另一个进程从共享区域读取数据。</li></ol><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[进程 A] --&gt; M[共享内存区域]    M --&gt; B[进程 B]    B --&gt; M    M --&gt; A  </pre></div><p>这张图强调：共享内存不是“进程 A 直接进入进程 B 的私有内存”，而是操作系统提供一块双方都能访问的公共区域。</p><p>共享内存最大的问题是：</p><blockquote><p><strong>共享数据本身不会自动同步。</strong></p></blockquote><p>也就是说，操作系统可以让两个进程看到同一块内存，但不会自动保证它们读写顺序正确。</p><p>例如：</p><ul><li>发送者写得太快，可能覆盖接收者还没读的数据；</li><li>接收者读得太快，可能读到尚未写入的数据；</li><li>多个发送者同时写，可能写到同一位置；</li><li>多个接收者同时读，可能重复消费同一数据。</li></ul><p>因此，共享内存通常必须配合锁、信号量、条件变量、原子操作等同步机制使用。</p><hr><h2 id="4-环形缓冲区：共享内存中的数据排队方式"><a href="#4-环形缓冲区：共享内存中的数据排队方式" class="headerlink" title="4. 环形缓冲区：共享内存中的数据排队方式"></a>4. 环形缓冲区：共享内存中的数据排队方式</h2><p>环形缓冲区 circular buffer 是共享内存通信中常见的数据组织方式。</p><p>它的作用不是直接“解决所有同步问题”，而是解决三个更基础的问题：</p><ol><li>新数据应该写到哪里？</li><li>旧数据应该从哪里读？</li><li>缓冲区什么时候空，什么时候满？</li></ol><p>典型结构如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">item buffer[BUFFER_SIZE];</span><br><span class="line"><span class="type">int</span> in = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> out = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>其中：</p><table><thead><tr><th>变量</th><th>含义</th></tr></thead><tbody><tr><td><code>buffer</code></td><td>共享数据区</td></tr><tr><td><code>in</code></td><td>下一个写入位置</td></tr><tr><td><code>out</code></td><td>下一个读取位置</td></tr></tbody></table><p>发送者写入后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">in = (in + <span class="number">1</span>) % BUFFER_SIZE;</span><br></pre></td></tr></table></figure><p>接收者读取后：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">out = (out + <span class="number">1</span>) % BUFFER_SIZE;</span><br></pre></td></tr></table></figure><p><code>% BUFFER_SIZE</code> 是环形缓冲区的关键。它让下标到达数组末尾后重新回到开头。</p><p>例如 <code>BUFFER_SIZE = 10</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0 -&gt; 1 -&gt; 2 -&gt; ... -&gt; 9 -&gt; 0 -&gt; 1 -&gt; ...</span><br></pre></td></tr></table></figure><p>这使得固定大小的数组可以循环复用。</p><hr><h2 id="5-环形缓冲区如何判断空和满"><a href="#5-环形缓冲区如何判断空和满" class="headerlink" title="5. 环形缓冲区如何判断空和满"></a>5. 环形缓冲区如何判断空和满</h2><p>环形缓冲区通过 <code>in</code> 和 <code>out</code> 判断状态。</p><h3 id="5-1-缓冲区为空"><a href="#5-1-缓冲区为空" class="headerlink" title="5.1 缓冲区为空"></a>5.1 缓冲区为空</h3><p>当：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">in == out</span><br></pre></td></tr></table></figure><p>表示没有新数据可读。</p><p>接收者此时不能继续读，否则会读到无效数据。</p><h3 id="5-2-缓冲区为满"><a href="#5-2-缓冲区为满" class="headerlink" title="5.2 缓冲区为满"></a>5.2 缓冲区为满</h3><p>常见设计中，会空出一个位置来区分“空”和“满”。</p><p>当：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(in + <span class="number">1</span>) % BUFFER_SIZE == out</span><br></pre></td></tr></table></figure><p>表示再写就会追上接收者，覆盖尚未读取的数据。</p><p>发送者此时不能继续写。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[发送者 Producer] --&gt; B{缓冲区是否满}    B --&gt;|满| C[等待]    B --&gt;|未满| D[写入 buffer[in]]    D --&gt; E[in 前进]    F[接收者 Consumer] --&gt; G{缓冲区是否空}    G --&gt;|空| H[等待]    G --&gt;|非空| I[读取 buffer[out]]    I --&gt; J[out 前进]  </pre></div><p>这张图说明：环形缓冲区提供了读写位置和空满条件，使发送者和接收者可以围绕这些状态进行协作。</p><hr><h2 id="6-环形缓冲区不能单独完成同步"><a href="#6-环形缓冲区不能单独完成同步" class="headerlink" title="6. 环形缓冲区不能单独完成同步"></a>6. 环形缓冲区不能单独完成同步</h2><p>必须注意：<strong>环形缓冲区不是锁，也不是信号量。</strong></p><p>它只是提供了数据结构和状态判断条件。真正的同步仍然需要额外机制。</p><p>例如，有两个发送者同时执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">buffer[in] = item;</span><br><span class="line">in = (in + <span class="number">1</span>) % BUFFER_SIZE;</span><br></pre></td></tr></table></figure><p>它们可能同时看到同一个 <code>in</code>，然后都写入同一个位置，造成覆盖。</p><p>所以，环形缓冲区只能解决：</p><ul><li>数据放哪里；</li><li>数据从哪里取；</li><li>空和满如何判断；</li><li>存储空间如何循环复用。</li></ul><p>但它不能单独保证：</p><ul><li>多个发送者并发写入一定安全；</li><li>多个接收者并发读取一定安全；</li><li><code>in</code> 和 <code>out</code> 的更新一定原子；</li><li>等待过程不浪费 CPU。</li></ul><p>完整的共享内存通信通常应当是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">共享内存 + 环形缓冲区 + 同步原语</span><br></pre></td></tr></table></figure><table><thead><tr><th>组成部分</th><th>作用</th></tr></thead><tbody><tr><td>共享内存</td><td>让多个进程访问同一块区域</td></tr><tr><td>环形缓冲区</td><td>管理数据位置、复用空间、判断空满</td></tr><tr><td>同步原语</td><td>保证并发访问安全，避免忙等或竞争</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：-环形缓冲区解决的是共享内存中的“数据排队和空间复用”问题，不是完整的并发同步方案。"><a href="#❗-本节核心结论：-环形缓冲区解决的是共享内存中的“数据排队和空间复用”问题，不是完整的并发同步方案。" class="headerlink" title="❗ 本节核心结论： 环形缓冲区解决的是共享内存中的“数据排队和空间复用”问题，不是完整的并发同步方案。"></a>❗ <strong>本节核心结论：</strong> 环形缓冲区解决的是共享内存中的“数据排队和空间复用”问题，不是完整的并发同步方案。</h2><h2 id="7-忙等问题：为什么不能一直-while"><a href="#7-忙等问题：为什么不能一直-while" class="headerlink" title="7. 忙等问题：为什么不能一直 while"></a>7. 忙等问题：为什么不能一直 <code>while</code></h2><p>最基础的环形缓冲区实现可能会使用忙等：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (((in + <span class="number">1</span>) % BUFFER_SIZE) == out)</span><br><span class="line">    ; <span class="comment">// 缓冲区满，发送者等待</span></span><br></pre></td></tr></table></figure><p>或：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (in == out)</span><br><span class="line">    ; <span class="comment">// 缓冲区空，接收者等待</span></span><br></pre></td></tr></table></figure><p>这种写法逻辑简单，但问题是：等待进程会一直占用 CPU 检查条件。</p><p>这叫 busy waiting，忙等。</p><p>忙等的问题包括：</p><ul><li>浪费 CPU 时间；</li><li>降低系统整体吞吐；</li><li>对低优先级任务不友好；</li><li>在多进程系统中可能放大调度压力。</li></ul><p>更合理的做法是使用：</p><ul><li>semaphore；</li><li>mutex；</li><li>condition variable；</li><li>blocking system call；</li><li>event notification。</li></ul><p>这样，当缓冲区满或空时，进程可以进入阻塞状态，让 CPU 去执行其他任务。条件满足后再被唤醒。</p><hr><h2 id="8-消息传递：通过发送和接收完成通信"><a href="#8-消息传递：通过发送和接收完成通信" class="headerlink" title="8. 消息传递：通过发送和接收完成通信"></a>8. 消息传递：通过发送和接收完成通信</h2><p>与共享内存不同，消息传递不要求进程直接共享地址空间。</p><p>它通常抽象为两个基本操作：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">send(message)</span><br><span class="line">receive(message)</span><br></pre></td></tr></table></figure><p>消息传递的优势是：</p><ul><li>通信结构清晰；</li><li>进程隔离性更好；</li><li>不需要双方直接共享内存；</li><li>更容易跨机器扩展；</li><li>内核可以帮助管理队列、权限和阻塞。</li></ul><p>缺点是：</p><ul><li>可能需要系统调用；</li><li>可能发生数据复制；</li><li>性能通常不如共享内存直接；</li><li>大数据传输成本较高。</li></ul><p>消息传递可以进一步分为：</p><ol><li>直接通信；</li><li>间接通信。</li></ol><hr><h2 id="9-直接通信：明确发给某个进程"><a href="#9-直接通信：明确发给某个进程" class="headerlink" title="9. 直接通信：明确发给某个进程"></a>9. 直接通信：明确发给某个进程</h2><p>直接通信 direct communication 的特点是：发送者明确知道接收者是谁。</p><p>形式上可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Send(P, message)</span><br><span class="line">Recv(Q, message)</span><br></pre></td></tr></table></figure><p>含义是：</p><ul><li><code>Send(P, message)</code>：把消息发给进程 P；</li><li><code>Recv(Q, message)</code>：从进程 Q 接收消息。</li></ul><p>直接通信的关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">发送者 -&gt; 接收者</span><br></pre></td></tr></table></figure><p>它的优点是直观，通信对象明确。</p><p>但它也有缺点：</p><ul><li>发送者和接收者耦合较强；</li><li>发送者必须知道接收者身份；</li><li>接收者变化时，发送者可能也要修改；</li><li>不适合多个发送者和多个接收者灵活协作的场景。</li></ul><hr><h2 id="10-间接通信：通过中间对象连接双方"><a href="#10-间接通信：通过中间对象连接双方" class="headerlink" title="10. 间接通信：通过中间对象连接双方"></a>10. 间接通信：通过中间对象连接双方</h2><p>间接通信 indirect communication 的核心是：</p><blockquote><p><strong>发送者不是直接把消息发给某个进程，而是发给一个中间对象；接收者也不是直接从某个进程那里取消息，而是从这个中间对象取。</strong></p></blockquote><p>这个中间对象可以是：</p><ul><li>mailbox；</li><li>pipe；</li><li>message queue；</li><li>channel；</li><li>topic；</li><li>broker。</li></ul><p>形式上可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Send(M, message)</span><br><span class="line">Recv(M, message)</span><br></pre></td></tr></table></figure><p>其中 <code>M</code> 是中间对象。</p><p>通信关系变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">发送者 -&gt; 中间对象 -&gt; 接收者</span><br></pre></td></tr></table></figure><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[发送进程] --&gt; Q[消息队列 &#x2F; 信箱]    Q --&gt; B[接收进程 1]    Q --&gt; C[接收进程 2]  </pre></div><p>这里的“间接”不是说通信一定很慢，也不是说消息绕了很多层，而是说：</p><blockquote><p><strong>通信双方不是靠彼此的进程 ID 建立连接，而是靠共同访问同一个中间对象建立连接。</strong></p></blockquote><hr><h2 id="11-为什么消息队列是间接通信"><a href="#11-为什么消息队列是间接通信" class="headerlink" title="11. 为什么消息队列是间接通信"></a>11. 为什么消息队列是间接通信</h2><p>消息队列 message queue 是典型的间接通信机制。</p><p>它的形式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">发送者 -&gt; 消息队列 -&gt; 接收者</span><br></pre></td></tr></table></figure><p>发送者把消息放入队列，接收者从队列取出消息。发送者不一定关心最后由谁接收，接收者也不一定关心消息由谁发送。</p><p>它们只需要共享同一个队列，并具有相应权限。</p><p>消息队列通常具备以下特征：</p><table><thead><tr><th>特征</th><th>含义</th></tr></thead><tbody><tr><td>队列组织</td><td>消息按队列形式保存</td></tr><tr><td>FIFO</td><td>默认先进入的消息先被读取</td></tr><tr><td>消息边界</td><td>每条消息是独立单位</td></tr><tr><td>类型字段</td><td>消息可以带类型</td></tr><tr><td>权限控制</td><td>只有有权限的进程可以访问</td></tr><tr><td>多发送者&#x2F;接收者</td><td>可支持多个进程共同使用</td></tr></tbody></table><p>消息队列相比普通字节流更结构化。它传递的是“消息”，而不是单纯的字节序列。</p><p>例如，消息可能包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">type: 1</span><br><span class="line">data: &quot;request user info&quot;</span><br></pre></td></tr></table></figure><p>接收者可以按顺序读取，也可以按类型读取。</p><hr><h2 id="❗-本节核心结论：-消息队列之所以是间接通信，是因为发送者和接收者通过共同访问队列建立连接，而不是直接指定彼此通信。"><a href="#❗-本节核心结论：-消息队列之所以是间接通信，是因为发送者和接收者通过共同访问队列建立连接，而不是直接指定彼此通信。" class="headerlink" title="❗ 本节核心结论： 消息队列之所以是间接通信，是因为发送者和接收者通过共同访问队列建立连接，而不是直接指定彼此通信。"></a>❗ <strong>本节核心结论：</strong> 消息队列之所以是间接通信，是因为发送者和接收者通过共同访问队列建立连接，而不是直接指定彼此通信。</h2><h2 id="12-管道与消息队列的区别"><a href="#12-管道与消息队列的区别" class="headerlink" title="12. 管道与消息队列的区别"></a>12. 管道与消息队列的区别</h2><p>管道 pipe 和消息队列都可以作为间接通信方式，但它们的抽象不同。</p><table><thead><tr><th>对比项</th><th>管道 Pipe</th><th>消息队列 Message Queue</th></tr></thead><tbody><tr><td>数据形式</td><td>字节流</td><td>一条条消息</td></tr><tr><td>消息边界</td><td>通常不保留明确消息边界</td><td>保留消息边界</td></tr><tr><td>组织方式</td><td>固定缓冲区更常见</td><td>队列或链表式组织</td></tr><tr><td>读取方式</td><td>按字节顺序读取</td><td>可按顺序或类型读取</td></tr><tr><td>通信关系</td><td>常用于父子进程或相关进程</td><td>可用于多个无关进程</td></tr><tr><td>结构化程度</td><td>较低</td><td>较高</td></tr></tbody></table><p>管道更像水流：写进去的是连续字节，读出来也是连续字节。</p><p>消息队列更像信箱：每封信有边界，甚至可以带类型。</p><hr><h2 id="13-同步与异步：发起操作后要不要等结果"><a href="#13-同步与异步：发起操作后要不要等结果" class="headerlink" title="13. 同步与异步：发起操作后要不要等结果"></a>13. 同步与异步：发起操作后要不要等结果</h2><p>同步和异步讨论的是：</p><blockquote><p><strong>发起通信操作之后，调用者要不要等待结果。</strong></p></blockquote><h3 id="13-1-同步通信"><a href="#13-1-同步通信" class="headerlink" title="13.1 同步通信"></a>13.1 同步通信</h3><p>同步 communication 的特点是：发起操作后等待结果。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Send(message)</span><br></pre></td></tr></table></figure><p>如果消息没有成功发出，发送者就等待。</p><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Recv(message)</span><br></pre></td></tr></table></figure><p>如果还没有消息到来，接收者就等待。</p><p>可以类比为打电话：打过去之后，要等对方接通或回应，才能继续当前事情。</p><p>同步的优点是：</p><ul><li>逻辑简单；</li><li>结果明确；</li><li>程序控制流容易理解；</li><li>不容易漏处理返回结果。</li></ul><p>缺点是：</p><ul><li>可能卡住；</li><li>对方不回应时，自己无法继续；</li><li>大量同步等待会降低并发能力。</li></ul><h3 id="13-2-异步通信"><a href="#13-2-异步通信" class="headerlink" title="13.2 异步通信"></a>13.2 异步通信</h3><p>异步 communication 的特点是：发起操作后不等待结果，调用者可以继续做别的事情。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">AsyncSend(message)</span><br></pre></td></tr></table></figure><p>发送请求提交后直接返回。</p><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TryRecv(message)</span><br></pre></td></tr></table></figure><p>如果当前没有消息，也不等待，直接返回“没有消息”。</p><p>可以类比为发消息：消息发出去后，不必一直盯着对方是否立即回复，可以先去做别的事情。</p><p>异步的优点是：</p><ul><li>不容易被单个通信操作卡住；</li><li>吞吐量更高；</li><li>适合高并发；</li><li>可以同时处理大量请求。</li></ul><p>缺点是：</p><ul><li>编程复杂；</li><li>需要回调、事件、轮询或状态记录；</li><li>错误处理更麻烦；</li><li>可能出现消息已发出但未及时处理的情况。</li></ul><hr><h2 id="❗-本节核心结论：-同步就是“发起后等待结果”，异步就是“发起后不等待结果”。"><a href="#❗-本节核心结论：-同步就是“发起后等待结果”，异步就是“发起后不等待结果”。" class="headerlink" title="❗ 本节核心结论： 同步就是“发起后等待结果”，异步就是“发起后不等待结果”。"></a>❗ <strong>本节核心结论：</strong> 同步就是“发起后等待结果”，异步就是“发起后不等待结果”。</h2><h2 id="14-阻塞与非阻塞：当前执行流会不会停住"><a href="#14-阻塞与非阻塞：当前执行流会不会停住" class="headerlink" title="14. 阻塞与非阻塞：当前执行流会不会停住"></a>14. 阻塞与非阻塞：当前执行流会不会停住</h2><p>阻塞和非阻塞关注的是：</p><blockquote><p><strong>当前执行流会不会因为某个操作停住。</strong></p></blockquote><table><thead><tr><th>概念</th><th>含义</th></tr></thead><tbody><tr><td>阻塞</td><td>当前执行流停住，直到条件满足</td></tr><tr><td>非阻塞</td><td>当前执行流不等待，立即返回</td></tr></tbody></table><p>在 IPC 中：</p><ul><li>阻塞发送：如果消息暂时发不出去，发送者等待；</li><li>非阻塞发送：如果消息发不出去，立即返回失败或状态码；</li><li>阻塞接收：如果没有消息，接收者等待；</li><li>非阻塞接收：如果没有消息，立即返回“无消息”。</li></ul><p>在很多操作系统教材中，可以先近似理解为：</p><table><thead><tr><th>通信语义</th><th>执行表现</th></tr></thead><tbody><tr><td>同步</td><td>通常表现为阻塞</td></tr><tr><td>异步</td><td>通常表现为非阻塞</td></tr></tbody></table><p>但严格来说，两组概念关注角度不同：</p><table><thead><tr><th>概念组</th><th>关注点</th></tr></thead><tbody><tr><td>同步 &#x2F; 异步</td><td>调用者与结果之间的关系</td></tr><tr><td>阻塞 &#x2F; 非阻塞</td><td>当前执行流是否停住</td></tr></tbody></table><p>例如，某些异步操作也可能在提交阶段短暂阻塞；某些同步操作也可能通过轮询方式非阻塞地检查结果。因此，在初学阶段可以先记成：</p><blockquote><p><strong>同步就是等，异步就是不等；阻塞就是停住，非阻塞就是不停住。</strong></p></blockquote><p>但进一步学习时要知道：它们不是完全同义词。</p><hr><h2 id="15-超时机制：同步与异步之间的折中"><a href="#15-超时机制：同步与异步之间的折中" class="headerlink" title="15. 超时机制：同步与异步之间的折中"></a>15. 超时机制：同步与异步之间的折中</h2><p>如果一直同步等待，可能永远卡住。<br>如果完全异步不等，又可能让控制逻辑变复杂。</p><p>因此，系统常提供 timeout，超时机制。</p><p>形式上可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Send(A, message, Time-out)</span><br></pre></td></tr></table></figure><p>含义是：</p><blockquote><p>我愿意等，但最多等 <code>Time-out</code> 这么久。超过时间仍未完成，就返回错误或超时状态。</p></blockquote><p>超时机制相当于一种折中：</p><table><thead><tr><th>模式</th><th>行为</th></tr></thead><tbody><tr><td>无限等待</td><td>阻塞 &#x2F; 同步</td></tr><tr><td>完全不等</td><td>非阻塞 &#x2F; 异步</td></tr><tr><td>等一段时间</td><td>带超时的同步</td></tr></tbody></table><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Recv(message, 5s)</span><br></pre></td></tr></table></figure><p>表示最多等待 5 秒。如果 5 秒内收到消息，就正常返回；如果没有收到，就返回超时。</p><p>这样既避免了无限卡死，也保留了同步等待的简单控制流。</p><hr><h2 id="16-三个核心问题的统一理解"><a href="#16-三个核心问题的统一理解" class="headerlink" title="16. 三个核心问题的统一理解"></a>16. 三个核心问题的统一理解</h2><p>本讲中的三个重点问题可以统一起来看：</p><h3 id="16-1-环形缓冲区回答“数据放哪里”"><a href="#16-1-环形缓冲区回答“数据放哪里”" class="headerlink" title="16.1 环形缓冲区回答“数据放哪里”"></a>16.1 环形缓冲区回答“数据放哪里”</h3><p>共享内存只是提供了一块共同区域，但没有规定数据如何排队。环形缓冲区通过 <code>in</code> 和 <code>out</code> 管理写入位置和读取位置，并通过空满条件防止读空和覆盖。</p><p>它回答的是：</p><ul><li>新数据写在哪里？</li><li>旧数据从哪里读？</li><li>空间如何循环复用？</li><li>什么时候不能写？</li><li>什么时候不能读？</li></ul><h3 id="16-2-消息队列回答“通信双方如何连接”"><a href="#16-2-消息队列回答“通信双方如何连接”" class="headerlink" title="16.2 消息队列回答“通信双方如何连接”"></a>16.2 消息队列回答“通信双方如何连接”</h3><p>消息队列不是让发送者直接找接收者，而是让双方共享一个中间队列。</p><p>它回答的是：</p><ul><li>发送者是否必须知道接收者？</li><li>多个发送者和接收者如何共享通信通道？</li><li>消息如何排队？</li><li>消息是否有类型？</li><li>由谁负责保存未读消息？</li></ul><h3 id="16-3-同步-异步回答“调用者是否等待”"><a href="#16-3-同步-异步回答“调用者是否等待”" class="headerlink" title="16.3 同步&#x2F;异步回答“调用者是否等待”"></a>16.3 同步&#x2F;异步回答“调用者是否等待”</h3><p>同步和异步讨论的是通信操作发起后的控制流问题。</p><p>它回答的是：</p><ul><li>发起发送后是否要等？</li><li>发起接收后是否要等？</li><li>没有消息时是否阻塞？</li><li>消息发不出去时是否返回？</li><li>是否需要超时机制？</li></ul><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[IPC 设计问题] --&gt; B[数据放在哪里]    A --&gt; C[通信双方如何连接]    A --&gt; D[发起操作后是否等待]    B --&gt; B1[共享内存]    B1 --&gt; B2[环形缓冲区]    C --&gt; C1[直接通信]    C --&gt; C2[间接通信]    C2 --&gt; C3[消息队列 &#x2F; 管道 &#x2F; 信箱]    D --&gt; D1[同步 &#x2F; 阻塞]    D --&gt; D2[异步 &#x2F; 非阻塞]    D --&gt; D3[超时机制]  </pre></div><p>这张图展示了 IPC 的三条理解线索：存储位置、连接方式和等待语义。</p><hr><h2 id="17-易混概念整理"><a href="#17-易混概念整理" class="headerlink" title="17. 易混概念整理"></a>17. 易混概念整理</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>环形缓冲区是否等于同步机制</td><td>不等于。它是数据组织方式，需要锁、信号量等配合</td></tr><tr><td>共享内存是否自动安全</td><td>不安全。共享只是能访问，同步还要额外处理</td></tr><tr><td><code>in == out</code> 表示什么</td><td>缓冲区为空</td></tr><tr><td><code>(in + 1) % BUFFER_SIZE == out</code> 表示什么</td><td>缓冲区满，继续写会覆盖未读数据</td></tr><tr><td>消息队列为什么是间接通信</td><td>因为通信双方通过共享队列连接，而不是直接指定彼此</td></tr><tr><td>直接通信和间接通信区别</td><td>直接通信指定进程，间接通信指定中间对象</td></tr><tr><td>管道和消息队列区别</td><td>管道偏字节流，消息队列保留消息边界和类型</td></tr><tr><td>同步和阻塞是否完全一样</td><td>不完全一样，但初学可近似理解为同步常表现为阻塞</td></tr><tr><td>异步和非阻塞是否完全一样</td><td>不完全一样，但初学可近似理解为异步常表现为非阻塞</td></tr><tr><td>超时机制属于什么</td><td>在无限等待和完全不等之间折中</td></tr></tbody></table><hr><h2 id="18-复习清单"><a href="#18-复习清单" class="headerlink" title="18. 复习清单"></a>18. 复习清单</h2><p>学完本讲后，应能回答：</p><ul><li>为什么进程默认不能直接访问彼此内存？</li><li>IPC 主要解决哪几类问题？</li><li>共享内存通信为什么快？</li><li>共享内存为什么同步复杂？</li><li>环形缓冲区为什么要设计成“环形”？</li><li><code>in</code> 和 <code>out</code> 分别表示什么？</li><li>如何判断环形缓冲区为空？</li><li>如何判断环形缓冲区为满？</li><li>环形缓冲区为什么不能单独保证并发安全？</li><li>忙等为什么浪费 CPU？</li><li>消息传递和共享内存有什么区别？</li><li>什么是直接通信？</li><li>什么是间接通信？</li><li>为什么消息队列是间接通信？</li><li>管道和消息队列有什么区别？</li><li>同步和异步的核心区别是什么？</li><li>阻塞和非阻塞的核心区别是什么？</li><li>为什么说同步&#x2F;异步与阻塞&#x2F;非阻塞不是完全同一组概念？</li><li>超时机制解决了什么问题？</li></ul><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>进程间通信的核心不是单纯“传数据”，而是围绕三个问题展开：</p><ol><li><p><strong>数据放在哪里</strong></p><ul><li>共享内存把数据放在共同映射区域；</li><li>环形缓冲区负责在共享区域中组织数据位置和复用空间。</li></ul></li><li><p><strong>通信双方如何连接</strong></p><ul><li>直接通信要求发送者明确知道接收者；</li><li>间接通信通过消息队列、管道、信箱等中间对象建立联系。</li></ul></li><li><p><strong>调用者是否等待结果</strong></p><ul><li>同步通信等待结果，逻辑简单但可能卡住；</li><li>异步通信不等待结果，并发能力强但编程复杂；</li><li>超时机制在二者之间提供折中。</li></ul></li></ol><p>因此，IPC 的学习重点不是背诵机制名称，而是理解每种机制在解决什么问题，又引入了什么新问题。共享内存快，但同步难；消息队列清晰，但可能有额外开销；同步简单，但会等待；异步高效，但控制复杂。真正掌握 IPC，就是能围绕这些取舍判断某个场景下应该选择哪种通信方式。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第12讲 调度II</title>
      <link href="/2026/06/11/%E7%AC%AC12%E8%AE%B2-%E8%B0%83%E5%BA%A6II/"/>
      <url>/2026/06/11/%E7%AC%AC12%E8%AE%B2-%E8%B0%83%E5%BA%A6II/</url>
      
        <content type="html"><![CDATA[<h1 id="第12讲-调度-II：多核调度、负载衡量与能耗感知"><a href="#第12讲-调度-II：多核调度、负载衡量与能耗感知" class="headerlink" title="第12讲 调度 II：多核调度、负载衡量与能耗感知"></a>第12讲 调度 II：多核调度、负载衡量与能耗感知</h1><p>本文根据《第12讲 调度II.md》的对话转录整理而成。核心问题是：<strong>当系统从单核走向多核、异构多核后，调度器不仅要决定“谁运行”，还要决定“在哪个 CPU 上运行”，并在负载均衡、缓存友好、性能与能耗之间做取舍。</strong> </p><hr><h2 id="1-从单核调度到多核调度"><a href="#1-从单核调度到多核调度" class="headerlink" title="1. 从单核调度到多核调度"></a>1. 从单核调度到多核调度</h2><p>在单核调度中，核心问题主要是：</p><ol><li>下一个运行哪个任务？</li><li>这个任务运行多久？</li></ol><p>但在多核系统中，还要增加一个问题：</p><ol start="3"><li>这个任务应该在哪个 CPU 核上运行？</li></ol><p>因此，多核调度可以概括为：</p><blockquote><p><strong>多核调度 &#x3D; 选任务 + 选 CPU + 控制运行时长</strong></p></blockquote><p>这比单核调度复杂得多。因为不同 CPU 核之间可能存在负载差异、缓存差异，甚至性能与功耗差异。</p><p>例如在一个四核系统中，任务 A 可以放在 CPU0，也可以放在 CPU2。表面上只是换一个 CPU，实际上会影响：</p><ul><li>该 CPU 是否已经很忙</li><li>该 CPU 缓存中是否已有任务 A 的数据</li><li>该 CPU 是大核还是小核</li><li>迁移任务是否会破坏局部性</li><li>系统整体能耗是否会上升</li></ul><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[就绪任务] --&gt; B{多核调度器}    B --&gt; C[选择任务]    B --&gt; D[选择 CPU]    B --&gt; E[控制运行时长]    D --&gt; F[CPU0]    D --&gt; G[CPU1]    D --&gt; H[CPU2]    D --&gt; I[CPU3]  </pre></div><p>这张图强调：多核调度不是单纯把单核调度复制多份，而是多了“任务放到哪个 CPU 上”的决策维度。</p><hr><h2 id="2-BSP：并行执行与同步点"><a href="#2-BSP：并行执行与同步点" class="headerlink" title="2. BSP：并行执行与同步点"></a>2. BSP：并行执行与同步点</h2><p>BSP，即 Bulk Synchronous Parallel，是一种常见的并行计算模型。它的关键不是“所有线程在同一个 CPU 上执行”，恰恰相反，BSP 通常就是为多核、多机并行设计的。</p><p>BSP 的基本思想是：</p><ol><li>一轮中没有依赖的任务并行执行。</li><li>到达同步点后，各任务等待其他任务完成。</li><li>同步点进行数据交换或状态对齐。</li><li>然后进入下一轮并行计算。</li></ol><p>可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">逻辑核心 0：执行一部分任务</span><br><span class="line">逻辑核心 1：执行一部分任务</span><br><span class="line">逻辑核心 2：执行一部分任务</span><br><span class="line">        ↓</span><br><span class="line">同步点：等待 / 交换数据 / 对齐状态</span><br><span class="line">        ↓</span><br><span class="line">进入下一轮并行</span><br></pre></td></tr></table></figure><h3 id="2-1-同一进程多线程中的数据交换"><a href="#2-1-同一进程多线程中的数据交换" class="headerlink" title="2.1 同一进程多线程中的数据交换"></a>2.1 同一进程多线程中的数据交换</h3><p>如果多个线程属于同一个进程，并运行在同一台机器的多个 CPU 核上，它们通常共享同一个地址空间。因此，它们可以通过共享内存交换数据。</p><p>例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 线程 1</span></span><br><span class="line">result[<span class="number">0</span>] = partial_result_0;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程 2</span></span><br><span class="line">result[<span class="number">1</span>] = partial_result_1;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 同步点</span></span><br><span class="line"><span class="built_in">barrier_wait</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下一轮中，所有线程都可以读取 result</span></span><br></pre></td></tr></table></figure><p>这里所谓“交换数据”，不一定是 CPU0 真的把数据搬到 CPU1，而是：</p><ul><li>各线程把结果写入共享内存；</li><li>通过 barrier 等同步机制保证大家都写完；</li><li>下一轮中其他线程安全读取这些结果。</li></ul><p>所以，同一进程多线程的核心机制是：</p><blockquote><p><strong>共享内存 + 同步原语</strong></p></blockquote><p>常见同步原语包括：</p><ul><li>barrier</li><li>mutex</li><li>condition variable</li><li>semaphore</li><li>atomic operation</li></ul><h3 id="2-2-分布式系统中的数据交换"><a href="#2-2-分布式系统中的数据交换" class="headerlink" title="2.2 分布式系统中的数据交换"></a>2.2 分布式系统中的数据交换</h3><p>如果任务运行在不同机器上，就不能依赖共享内存。这时需要通过网络交换数据，例如：</p><ul><li>消息传递</li><li>RPC</li><li>分布式框架中的 shuffle</li><li>socket 通信</li><li>分布式文件系统或对象存储</li></ul><p>所以 BSP 中“同步交换数据”要分情况理解：</p><table><thead><tr><th>场景</th><th>数据交换方式</th></tr></thead><tbody><tr><td>同一进程多线程</td><td>共享内存 + 同步原语</td></tr><tr><td>不同进程</td><td>IPC &#x2F; 共享内存 &#x2F; 管道 &#x2F; socket</td></tr><tr><td>不同机器</td><td>网络通信 &#x2F; RPC &#x2F; shuffle</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：-BSP-的重点不是线程是否在同一个-CPU-上，而是“并行阶段-同步点-下一轮并行”的计算结构。"><a href="#❗-本节核心结论：-BSP-的重点不是线程是否在同一个-CPU-上，而是“并行阶段-同步点-下一轮并行”的计算结构。" class="headerlink" title="❗ 本节核心结论： BSP 的重点不是线程是否在同一个 CPU 上，而是“并行阶段 + 同步点 + 下一轮并行”的计算结构。"></a>❗ <strong>本节核心结论：</strong> BSP 的重点不是线程是否在同一个 CPU 上，而是“并行阶段 + 同步点 + 下一轮并行”的计算结构。</h2><h2 id="3-两级调度：全局调度器与本地调度器"><a href="#3-两级调度：全局调度器与本地调度器" class="headerlink" title="3. 两级调度：全局调度器与本地调度器"></a>3. 两级调度：全局调度器与本地调度器</h2><p>“两级调度”的“两级”不是指“选任务”和“运行任务”两个步骤，而是指：</p><ol><li><strong>全局调度器</strong></li><li><strong>每个 CPU 的本地调度器</strong></li></ol><p>可以理解为“总管 + 分店店长”模式：</p><table><thead><tr><th>角色</th><th>对应调度结构</th><th>负责内容</th></tr></thead><tbody><tr><td>总管</td><td>全局调度器</td><td>决定任务大致分配到哪个 CPU</td></tr><tr><td>分店店长</td><td>本地调度器</td><td>每个 CPU 从自己的队列里选任务</td></tr><tr><td>分店排队表</td><td>本地运行队列</td><td>保存该 CPU 上等待运行的任务</td></tr></tbody></table><p>结构如下：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[全局调度器] --&gt; B[CPU0 本地调度器]    A --&gt; C[CPU1 本地调度器]    A --&gt; D[CPU2 本地调度器]    A --&gt; E[CPU3 本地调度器]    B --&gt; BQ[CPU0 运行队列 rq]    C --&gt; CQ[CPU1 运行队列 rq]    D --&gt; DQ[CPU2 运行队列 rq]    E --&gt; EQ[CPU3 运行队列 rq]  </pre></div><p>Linux 中，每个 CPU 都有自己的运行队列，通常称为 <code>rq</code>，也就是 <code>struct rq</code>。</p><p>两级调度的好处是：</p><ul><li>每个 CPU 可以快速从本地队列中选择任务；</li><li>不需要所有 CPU 每次都竞争一个全局队列；</li><li>可以提高可扩展性；</li><li>可以减少任务频繁迁移；</li><li>也可以在必要时进行负载均衡。</li></ul><p>但它也带来一个问题：</p><blockquote><p>每个 CPU 都有自己的队列，如何判断某个 CPU 是否过载？又如何决定是否迁移任务？</p></blockquote><p>这就引出了负载衡量与负载均衡。</p><hr><h2 id="4-CPU-负载不能只看任务数量"><a href="#4-CPU-负载不能只看任务数量" class="headerlink" title="4. CPU 负载不能只看任务数量"></a>4. CPU 负载不能只看任务数量</h2><p>判断 CPU 是否繁忙，不能简单看运行队列中有几个任务。</p><p>原因是不同任务对 CPU 的消耗完全不同。</p><table><thead><tr><th>任务类型</th><th>行为特征</th><th>对 CPU 的负载</th></tr></thead><tbody><tr><td>I&#x2F;O 密集型任务</td><td>短暂使用 CPU，然后等待 I&#x2F;O</td><td>可能较低</td></tr><tr><td>CPU 密集型任务</td><td>长时间持续计算</td><td>通常较高</td></tr><tr><td>交互式任务</td><td>间歇性运行，需要快速响应</td><td>瞬时敏感</td></tr><tr><td>后台批处理任务</td><td>长时间运行但不急</td><td>可被延后</td></tr></tbody></table><p>例如：</p><ul><li>CPU0 上有 5 个 I&#x2F;O 密集型任务，它们大部分时间在等待磁盘或网络。</li><li>CPU1 上只有 1 个 CPU 密集型任务，但它持续满负载计算。</li></ul><p>如果只看任务数量，会误以为 CPU0 更忙。但实际 CPU 使用压力可能是 CPU1 更高。</p><p>所以，多核调度需要更精细的负载指标，而不是简单的“任务个数”。</p><hr><h2 id="❗-本节核心结论：-运行队列长度只能粗略反映负载，不能准确表示-CPU-压力；真正重要的是任务实际消耗了多少-CPU。"><a href="#❗-本节核心结论：-运行队列长度只能粗略反映负载，不能准确表示-CPU-压力；真正重要的是任务实际消耗了多少-CPU。" class="headerlink" title="❗ 本节核心结论： 运行队列长度只能粗略反映负载，不能准确表示 CPU 压力；真正重要的是任务实际消耗了多少 CPU。"></a>❗ <strong>本节核心结论：</strong> 运行队列长度只能粗略反映负载，不能准确表示 CPU 压力；真正重要的是任务实际消耗了多少 CPU。</h2><h2 id="5-任务迁移与缓存局部性"><a href="#5-任务迁移与缓存局部性" class="headerlink" title="5. 任务迁移与缓存局部性"></a>5. 任务迁移与缓存局部性</h2><p>多核调度需要负载均衡。如果 CPU0 很忙而 CPU1 很空，调度器可能把一部分任务从 CPU0 迁移到 CPU1。</p><p>但任务迁移不是免费的。</p><p>常见误解是：任务迁移性能差，是因为要把整个任务状态复制到另一个 CPU。</p><p>这个说法不准确。任务的 PCB、TCB、地址空间等状态本来就在内存中，调度器并不是把整个进程复制到另一个 CPU。</p><p>真正关键的问题是：</p><blockquote><p><strong>任务迁移会破坏缓存局部性。</strong></p></blockquote><p>任务在 CPU0 上运行一段时间后，它访问过的指令和数据可能已经进入 CPU0 相关缓存。此时如果迁移到 CPU1，CPU1 的缓存中可能没有这些数据，于是会出现更多 cache miss，需要从更低层缓存或内存重新加载。</p><p>流程可以理解为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[任务在 CPU0 上运行] --&gt; B[CPU0 缓存中已有任务数据]    B --&gt; C[任务迁移到 CPU1]    C --&gt; D[CPU1 缓存缺少相关数据]    D --&gt; E[cache miss 增多]    E --&gt; F[性能下降]  </pre></div><p>所以，负载均衡和缓存友好之间存在冲突：</p><table><thead><tr><th>目标</th><th>好处</th><th>风险</th></tr></thead><tbody><tr><td>负载均衡</td><td>避免某些 CPU 过忙、某些 CPU 空闲</td><td>可能迁移任务</td></tr><tr><td>缓存友好</td><td>任务长期留在同一 CPU，缓存命中率高</td><td>可能导致负载不均衡</td></tr></tbody></table><p>调度器必须在二者之间取舍。</p><hr><h2 id="❗-本节核心结论：-任务迁移的主要代价不是复制整个任务状态，而是破坏缓存局部性，导致-cache-miss-增多。"><a href="#❗-本节核心结论：-任务迁移的主要代价不是复制整个任务状态，而是破坏缓存局部性，导致-cache-miss-增多。" class="headerlink" title="❗ 本节核心结论： 任务迁移的主要代价不是复制整个任务状态，而是破坏缓存局部性，导致 cache miss 增多。"></a>❗ <strong>本节核心结论：</strong> 任务迁移的主要代价不是复制整个任务状态，而是破坏缓存局部性，导致 cache miss 增多。</h2><h2 id="6-PELT：以调度实体为粒度追踪负载"><a href="#6-PELT：以调度实体为粒度追踪负载" class="headerlink" title="6. PELT：以调度实体为粒度追踪负载"></a>6. PELT：以调度实体为粒度追踪负载</h2><p>Linux 中常用 PELT，即 Per-Entity Load Tracking，来追踪负载。</p><p>这里的 entity 是调度实体。调度实体不能简单等同于“进程”。在调度器视角中，调度实体可以是：</p><ul><li>线程</li><li>任务</li><li>调度组</li><li>其他可被调度器管理的对象</li></ul><p>因此，不能说“一个调度实体就对应一个进程”。更准确的说法是：</p><blockquote><p><strong>调度实体是调度器眼中可以被调度和计量负载的对象。</strong></p></blockquote><p>为什么 PELT 要以调度实体为粒度？</p><p>因为只看 CPU 队列长度无法准确判断负载。不同任务实际运行时间、可运行时间和 CPU 消耗不同。调度器需要知道每个任务对负载的具体贡献。</p><p>例如：</p><table><thead><tr><th>情况</th><th align="right">队列长度</th><th>实际负载</th></tr></thead><tbody><tr><td>5 个 I&#x2F;O 密集型任务</td><td align="right">5</td><td>不一定高</td></tr><tr><td>1 个 CPU 密集型任务</td><td align="right">1</td><td>可能很高</td></tr></tbody></table><p>PELT 的意义就在于：</p><ul><li>记录任务最近一段时间的 CPU 使用情况；</li><li>让越近的行为权重越高；</li><li>让越久远的行为逐渐衰减；</li><li>形成更能反映当前状态的负载估计。</li></ul><hr><h2 id="7-历史负载衰减：针对过去，不是未来"><a href="#7-历史负载衰减：针对过去，不是未来" class="headerlink" title="7. 历史负载衰减：针对过去，不是未来"></a>7. 历史负载衰减：针对过去，不是未来</h2><p>历史负载衰减是 PELT 中非常重要的思想。</p><p>它针对的是<strong>过去的历史负载</strong>，不是预测未来负载会衰减。</p><p>可以抽象写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">当前负载估计 =</span><br><span class="line">当前周期负载</span><br><span class="line">+ γ × 上一周期负载</span><br><span class="line">+ γ² × 上上周期负载</span><br><span class="line">+ γ³ × 更早周期负载</span><br><span class="line">+ ...</span><br></pre></td></tr></table></figure><p>也就是说：</p><table><thead><tr><th>时间位置</th><th>权重</th></tr></thead><tbody><tr><td>当前周期</td><td>最大</td></tr><tr><td>上一周期</td><td>乘 γ</td></tr><tr><td>上上周期</td><td>乘 γ²</td></tr><tr><td>更早周期</td><td>乘更高次 γ</td></tr></tbody></table><p>越久以前的负载，乘的 γ 次数越多，权重越小。</p><p>Linux 中有一个典型关系：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">γ^32 = 0.5</span><br></pre></td></tr></table></figure><p>意思是：一个负载贡献经过 32 个周期后，影响力衰减到原来的一半。</p><p>可以这样理解：</p><ul><li>刚刚很忙：很能说明任务现在可能还忙。</li><li>10ms 前很忙：仍有参考价值，但弱一些。</li><li>很久以前很忙：参考价值很低。</li></ul><p>所以，历史衰减的目的不是预测未来，而是评价过去时，把越久远的历史看得越轻。</p><hr><h2 id="❗-本节核心结论：-衰减系数-γ-作用于过去的历史负载；越久以前的行为，对当前负载判断的影响越小。"><a href="#❗-本节核心结论：-衰减系数-γ-作用于过去的历史负载；越久以前的行为，对当前负载判断的影响越小。" class="headerlink" title="❗ 本节核心结论： 衰减系数 γ 作用于过去的历史负载；越久以前的行为，对当前负载判断的影响越小。"></a>❗ <strong>本节核心结论：</strong> 衰减系数 γ 作用于过去的历史负载；越久以前的行为，对当前负载判断的影响越小。</h2><h2 id="8-CpuScaleFactor：异构-CPU-下的负载标准化"><a href="#8-CpuScaleFactor：异构-CPU-下的负载标准化" class="headerlink" title="8. CpuScaleFactor：异构 CPU 下的负载标准化"></a>8. CpuScaleFactor：异构 CPU 下的负载标准化</h2><p>在传统同构多核系统中，每个 CPU 核性能大致相同。同样运行 1ms，完成的工作量差不多。</p><p>但在异构多核系统中，例如大小核架构，大核和小核性能不同：</p><table><thead><tr><th>CPU 类型</th><th>特点</th></tr></thead><tbody><tr><td>大核</td><td>性能强，功耗高</td></tr><tr><td>小核</td><td>性能弱，功耗低</td></tr></tbody></table><p>这时，如果只看“运行了多久”，就不够准确。</p><p>同样运行 1ms：</p><ul><li>大核可能完成更多计算；</li><li>小核完成的计算较少。</li></ul><p>因此，需要 CpuScaleFactor 这类参数，把不同 CPU 上的负载统一到同一标准下。</p><p>它的作用是：</p><blockquote><p><strong>让不同性能 CPU 核上的负载可以比较。</strong></p></blockquote><p>可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">标准化负载 = 原始负载 × CPU 性能缩放因子</span><br></pre></td></tr></table></figure><p>性能更强的 CPU，ScaleFactor 更高。这样调度器才能知道：</p><ul><li>某个任务放在大核上是否浪费；</li><li>某个任务放在小核上是否会跑不动；</li><li>哪个 CPU 更适合当前任务。</li></ul><hr><h2 id="9-处理器亲和性：缓存友好与负载风险"><a href="#9-处理器亲和性：缓存友好与负载风险" class="headerlink" title="9. 处理器亲和性：缓存友好与负载风险"></a>9. 处理器亲和性：缓存友好与负载风险</h2><p>处理器亲和性 processor affinity 指的是：<strong>限制任务可以在哪些 CPU 上运行</strong>。</p><p>它不一定是“强制任务只在某个 CPU 上运行”，也可以是设置一个允许运行的 CPU 集合。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">任务 A 只能在 CPU0、CPU1 上运行</span><br><span class="line">任务 B 可以在 CPU0 ~ CPU7 上运行</span><br></pre></td></tr></table></figure><p>处理器亲和性的好处包括：</p><ul><li>提高缓存命中率；</li><li>减少任务迁移；</li><li>增强调度可控性；</li><li>便于将某些任务限制在特定核心上；</li><li>在大小核系统中，可以间接影响任务放到高性能核或节能核。</li></ul><p>但它也有风险：</p><ul><li>如果限制过死，可能导致某些 CPU 很忙，其他 CPU 空闲；</li><li>可能破坏负载均衡；</li><li>可能让任务无法迁移到更合适的 CPU；</li><li>在复杂系统中可能降低整体吞吐量。</li></ul><p>所以，处理器亲和性不是越强越好，而是要根据任务需求设置。</p><hr><h2 id="❗-本节核心结论：-处理器亲和性能提高缓存友好性和调度可控性，但过度限制会造成负载不均衡。"><a href="#❗-本节核心结论：-处理器亲和性能提高缓存友好性和调度可控性，但过度限制会造成负载不均衡。" class="headerlink" title="❗ 本节核心结论： 处理器亲和性能提高缓存友好性和调度可控性，但过度限制会造成负载不均衡。"></a>❗ <strong>本节核心结论：</strong> 处理器亲和性能提高缓存友好性和调度可控性，但过度限制会造成负载不均衡。</h2><h2 id="10-大小核调度：任务应放在哪类核心上"><a href="#10-大小核调度：任务应放在哪类核心上" class="headerlink" title="10. 大小核调度：任务应放在哪类核心上"></a>10. 大小核调度：任务应放在哪类核心上</h2><p>在大小核系统中，调度器不仅要考虑 CPU 是否空闲，还要考虑任务适合大核还是小核。</p><p>一个粗略原则是：</p><table><thead><tr><th>任务类型</th><th>更适合的核心</th></tr></thead><tbody><tr><td>轻负载任务</td><td>小核</td></tr><tr><td>后台任务</td><td>小核</td></tr><tr><td>重计算任务</td><td>大核</td></tr><tr><td>延迟敏感任务</td><td>大核</td></tr><tr><td>短时间紧急任务</td><td>可临时放大核</td></tr><tr><td>长时间低优先级任务</td><td>小核或中等核心</td></tr></tbody></table><p>例如：</p><ul><li>后台同步、轻量日志处理：适合小核，节能。</li><li>游戏主线程、UI 渲染、视频编码：更适合大核，保证性能。</li><li>临时突发的交互响应：可以短时间放大核，降低延迟。</li></ul><p>这里的目标不是“所有任务都放大核”，因为大核虽然快，但功耗高、发热高。如果轻任务也全部放大核，系统能耗会显著上升。</p><hr><h2 id="11-能耗感知调度：性能与功耗的权衡"><a href="#11-能耗感知调度：性能与功耗的权衡" class="headerlink" title="11. 能耗感知调度：性能与功耗的权衡"></a>11. 能耗感知调度：性能与功耗的权衡</h2><p>能耗感知调度 Energy-Aware Scheduling，简称 EAS，关注的问题不是单纯负载均衡，而是：</p><blockquote><p><strong>在满足性能需求的前提下，尽量降低能耗。</strong></p></blockquote><p>这和普通负载均衡不一样。</p><table><thead><tr><th>调度目标</th><th>关注重点</th></tr></thead><tbody><tr><td>负载均衡</td><td>CPU 之间负载是否均匀</td></tr><tr><td>能耗感知调度</td><td>当前任务放在哪个 CPU 上更省电，同时性能够用</td></tr></tbody></table><p>能耗感知调度器通常会结合：</p><ul><li>任务负载；</li><li>CPU 性能容量；</li><li>CPU 功耗模型；</li><li>大小核差异；</li><li>当前系统状态；</li><li>任务是否延迟敏感。</li></ul><p>它的典型策略是：</p><ul><li>轻任务放小核，节能；</li><li>重任务放大核，保证性能；</li><li>任务负载变化时动态调整；</li><li>在性能足够的前提下选择功耗更低的核心。</li></ul><p>所以，如果把能耗感知调度理解为“动态分析 CPU 之间的负载均衡情况”，就不够准确。那更像是在描述普通负载均衡。</p><p>更准确的表述是：</p><blockquote><p><strong>能耗感知调度是在性能和功耗之间做权衡。</strong></p></blockquote><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[任务到达] --&gt; B[估计任务负载]    B --&gt; C[查看 CPU 性能容量]    C --&gt; D[结合功耗模型]    D --&gt; E{性能是否足够}    E --&gt;|小核足够| F[选择小核以节能]    E --&gt;|小核不足| G[选择大核以保证性能]  </pre></div><hr><h2 id="❗-本节核心结论：-能耗感知调度不是单纯追求负载均衡，而是在性能够用的前提下尽量省电。"><a href="#❗-本节核心结论：-能耗感知调度不是单纯追求负载均衡，而是在性能够用的前提下尽量省电。" class="headerlink" title="❗ 本节核心结论： 能耗感知调度不是单纯追求负载均衡，而是在性能够用的前提下尽量省电。"></a>❗ <strong>本节核心结论：</strong> 能耗感知调度不是单纯追求负载均衡，而是在性能够用的前提下尽量省电。</h2><h2 id="12-易混概念整理"><a href="#12-易混概念整理" class="headerlink" title="12. 易混概念整理"></a>12. 易混概念整理</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>BSP 是否要求线程在同一 CPU</td><td>不要求，BSP 通常用于多核或分布式并行</td></tr><tr><td>BSP 的同步交换数据</td><td>同机可用共享内存 + barrier，分布式用网络通信</td></tr><tr><td>两级调度的“两级”</td><td>全局调度器 + 每个 CPU 的本地调度器</td></tr><tr><td>Linux 每 CPU 运行队列</td><td><code>rq</code>，即 <code>struct rq</code></td></tr><tr><td>CPU 负载是否等于任务数量</td><td>不是，任务类型和实际 CPU 消耗更重要</td></tr><tr><td>任务迁移为何影响性能</td><td>主要是破坏缓存局部性，不是复制整个任务状态</td></tr><tr><td>调度实体是否等于进程</td><td>不一定，调度实体是调度器计量和调度的对象</td></tr><tr><td>PELT 衰减针对什么</td><td>针对过去历史负载，不是未来</td></tr><tr><td>CpuScaleFactor 的作用</td><td>标准化不同性能 CPU 核上的负载</td></tr><tr><td>处理器亲和性</td><td>限制任务可运行的 CPU 集合</td></tr><tr><td>能耗感知调度</td><td>性能与功耗权衡，不只是负载均衡</td></tr></tbody></table><hr><h2 id="13-复习清单"><a href="#13-复习清单" class="headerlink" title="13. 复习清单"></a>13. 复习清单</h2><p>学完本讲后，应能回答：</p><ul><li>多核调度相比单核调度多了什么问题？</li><li>BSP 中同步点的作用是什么？</li><li>同一进程多线程和分布式任务如何交换数据？</li><li>两级调度的两级分别是什么？</li><li>Linux 中每个 CPU 的运行队列叫什么？</li><li>为什么不能用任务数量简单衡量 CPU 负载？</li><li>任务迁移为什么会破坏缓存局部性？</li><li>负载均衡和缓存友好为什么存在冲突？</li><li>PELT 为什么要以调度实体为粒度追踪负载？</li><li>调度实体为什么不一定等于进程？</li><li>历史负载衰减为什么针对过去？</li><li><code>γ^32 = 0.5</code> 表示什么？</li><li>CpuScaleFactor 为什么在大小核系统中重要？</li><li>处理器亲和性的好处和风险是什么？</li><li>能耗感知调度和普通负载均衡有什么区别？</li><li>大小核系统中，轻任务和重任务通常分别放在哪里？</li></ul><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>调度 II 的核心可以压缩为一句话：</p><blockquote><p><strong>多核调度不只是决定哪个任务运行，还要决定任务在哪个 CPU 上运行，并在负载均衡、缓存局部性、处理器亲和性、异构核心性能和系统能耗之间做取舍。</strong></p></blockquote><p>从单核到多核后，调度问题从“一条队列上谁先运行”扩展为“多个 CPU 上如何分配任务”。两级调度用全局调度器和本地调度器解决可扩展性问题；PELT 用历史衰减追踪任务负载；CpuScaleFactor 让不同性能核心上的负载可比较；处理器亲和性提高缓存友好但可能破坏负载均衡；能耗感知调度则进一步把功耗纳入调度决策。</p><p>真正理解调度 II，需要抓住一个中心矛盾：<strong>任务迁移有助于负载均衡，但会损害缓存局部性；大核能提升性能，但会增加能耗；小核能省电，但可能无法满足重任务和延迟敏感任务的需求。</strong> 操作系统调度器的价值，就在于不断在这些目标之间做动态权衡。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第11讲 调度I</title>
      <link href="/2026/06/11/%E7%AC%AC11%E8%AE%B2-%E8%B0%83%E5%BA%A6I/"/>
      <url>/2026/06/11/%E7%AC%AC11%E8%AE%B2-%E8%B0%83%E5%BA%A6I/</url>
      
        <content type="html"><![CDATA[<h1 id="第11讲-调度-I：从-CPU-排队到实时约束的调度策略"><a href="#第11讲-调度-I：从-CPU-排队到实时约束的调度策略" class="headerlink" title="第11讲 调度 I：从 CPU 排队到实时约束的调度策略"></a>第11讲 调度 I：从 CPU 排队到实时约束的调度策略</h1><p>本文根据《第11讲 调度I.md》的对话转录整理而成，核心目标不是罗列算法名称，而是建立一条主线：<strong>调度本质上是在有限 CPU 资源上，对不同任务的时间需求、响应需求、公平需求和截止时间需求进行权衡</strong>。</p><hr><h2 id="1-调度的基本问题：谁运行、在哪运行、运行多久"><a href="#1-调度的基本问题：谁运行、在哪运行、运行多久" class="headerlink" title="1. 调度的基本问题：谁运行、在哪运行、运行多久"></a>1. 调度的基本问题：谁运行、在哪运行、运行多久</h2><p>在现代操作系统中，任务数量通常远大于 CPU 核心数量。即使是普通桌面系统，也可能同时存在上百个进程或线程，而真正能在某一时刻运行的任务数量受限于处理器核心数。</p><p>因此，调度器至少要回答三个问题：</p><ol><li><strong>下一个执行的任务是谁</strong></li><li><strong>该任务在哪个 CPU 上执行</strong></li><li><strong>该任务执行多长时间</strong></li></ol><p>在单核调度中，重点主要是第一个和第三个问题：同一个 CPU 上，多个就绪任务如何排队、如何切换、每个任务运行多久。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[多个任务进入系统] --&gt; B[就绪队列 Ready Queue]    B --&gt; C{调度器决策}    C --&gt; D[选择下一个任务]    C --&gt; E[决定运行时长]    D --&gt; F[任务运行 Running]    F --&gt;|时间片到| B    F --&gt;|等待 I&#x2F;O&#x2F;锁| G[阻塞 Blocked]    G --&gt;|事件完成| B  </pre></div><p>这张图强调：调度不是孤立算法，而是和进程状态转换、时钟中断、上下文切换共同构成运行机制。</p><hr><h2 id="2-策略与机制：调度算法不是调度的全部"><a href="#2-策略与机制：调度算法不是调度的全部" class="headerlink" title="2. 策略与机制：调度算法不是调度的全部"></a>2. 策略与机制：调度算法不是调度的全部</h2><p>调度中有一个重要区分：<strong>策略 Policy</strong> 和 <strong>机制 Mechanism</strong>。</p><table><thead><tr><th>层面</th><th>回答的问题</th><th>例子</th></tr></thead><tbody><tr><td>策略</td><td>应该选择谁运行</td><td>FCFS、SJF、RR、EDF</td></tr><tr><td>机制</td><td>如何实现选择和切换</td><td>运行队列、时钟中断、上下文切换</td></tr></tbody></table><p>例如，时间片轮转 RR 的策略是“每个任务轮流运行一小段时间”。但这个策略必须依赖机制实现：硬件定时器触发中断，操作系统进入内核态，保存当前任务上下文，再恢复下一个任务上下文。</p><p>所以，学习调度时不能只问“算法是什么”，还要问：</p><ul><li>它依赖什么机制？</li><li>它优化什么指标？</li><li>它牺牲什么指标？</li><li>它适合哪类任务？</li></ul><hr><h2 id="3-调度目标：没有绝对最优，只有场景取舍"><a href="#3-调度目标：没有绝对最优，只有场景取舍" class="headerlink" title="3. 调度目标：没有绝对最优，只有场景取舍"></a>3. 调度目标：没有绝对最优，只有场景取舍</h2><p>调度器并不是简单追求“快”。不同任务对系统有不同要求，因此调度目标天然存在冲突。</p><table><thead><tr><th>指标</th><th>含义</th><th>更关注的场景</th></tr></thead><tbody><tr><td>周转时间</td><td>任务进入系统到任务完成的总时间</td><td>批处理任务</td></tr><tr><td>响应时间</td><td>任务进入系统到第一次产生反馈的时间</td><td>交互式任务</td></tr><tr><td>公平性</td><td>每个任务都能获得 CPU，不长期饥饿</td><td>多用户系统</td></tr><tr><td>实时性</td><td>任务必须在 deadline 前完成</td><td>实时系统</td></tr><tr><td>调度开销</td><td>调度器和上下文切换本身的成本</td><td>所有系统</td></tr></tbody></table><p>这些目标之间经常冲突：</p><ul><li>为了让短任务更快完成，长任务可能长期等待。</li><li>为了提高交互响应，可以缩短时间片，但上下文切换开销会上升。</li><li>为了保证高优先级任务及时执行，低优先级任务可能饥饿。</li><li>为了保证公平，可能牺牲平均周转时间。</li></ul><hr><h2 id="❗-本节核心结论：-调度不是寻找唯一正确算法，而是在响应时间、周转时间、公平性、实时性和开销之间做权衡。"><a href="#❗-本节核心结论：-调度不是寻找唯一正确算法，而是在响应时间、周转时间、公平性、实时性和开销之间做权衡。" class="headerlink" title="❗ 本节核心结论： 调度不是寻找唯一正确算法，而是在响应时间、周转时间、公平性、实时性和开销之间做权衡。"></a>❗ <strong>本节核心结论：</strong> 调度不是寻找唯一正确算法，而是在响应时间、周转时间、公平性、实时性和开销之间做权衡。</h2><h2 id="4-批处理任务、交互式任务与实时任务"><a href="#4-批处理任务、交互式任务与实时任务" class="headerlink" title="4. 批处理任务、交互式任务与实时任务"></a>4. 批处理任务、交互式任务与实时任务</h2><p>调度策略之所以分类，是因为不同任务对系统有不同要求。这里最容易混淆的是批处理任务、交互式任务和实时任务。</p><h3 id="4-1-批处理任务"><a href="#4-1-批处理任务" class="headerlink" title="4.1 批处理任务"></a>4.1 批处理任务</h3><p>批处理任务 batch task 指的是：<strong>不需要用户实时盯着结果，只希望系统把一批工作尽快处理完的任务</strong>。</p><p>典型例子包括：</p><ul><li>编译大型项目</li><li>批量压缩图片</li><li>数据库夜间备份</li><li>批量生成实验数据</li><li>视频转码</li><li>机器学习训练</li></ul><p>批处理任务更关注吞吐量和周转时间，而不是立刻给用户反馈。</p><p>它和 Windows 的 <code>.bat</code> 批处理文件有联系，但不是同一个概念：</p><table><thead><tr><th>概念</th><th>含义</th></tr></thead><tbody><tr><td>批处理任务</td><td>操作系统调度视角下的一类工作负载</td></tr><tr><td><code>.bat</code> 文件</td><td>Windows 中批量执行命令的脚本文件</td></tr></tbody></table><p><code>.bat</code> 文件可能启动批处理任务，但批处理任务不等于 <code>.bat</code> 文件。</p><h3 id="4-2-交互式任务"><a href="#4-2-交互式任务" class="headerlink" title="4.2 交互式任务"></a>4.2 交互式任务</h3><p>交互式任务主要指需要快速响应用户操作的任务，典型是人机交互。</p><p>例如：</p><ul><li>键盘输入</li><li>鼠标移动</li><li>浏览器点击</li><li>终端命令输入</li><li>VS Code 编辑代码</li><li>游戏角色移动</li><li>音乐播放控制</li></ul><p>交互式任务的关键不是计算量大，而是用户希望系统迅速反馈。敲下一个字符后，如果两秒后才显示，系统就会被认为“卡”。</p><p>所以交互式任务最关注的是响应时间。</p><h3 id="4-3-实时任务"><a href="#4-3-实时任务" class="headerlink" title="4.3 实时任务"></a>4.3 实时任务</h3><p>实时任务 real-time task 的“实时”不是“正在运行”的意思，也不是“尽快运行”的意思。</p><p>它的专业含义是：<strong>任务必须在规定的截止时间 deadline 前完成，否则结果价值下降，甚至造成严重后果</strong>。</p><table><thead><tr><th>类型</th><th>例子</th><th>错过 deadline 的后果</th></tr></thead><tbody><tr><td>软实时</td><td>视频播放一帧</td><td>卡顿、掉帧、体验变差</td></tr><tr><td>硬实时</td><td>自动驾驶刹车</td><td>可能造成事故</td></tr><tr><td>硬实时</td><td>方向盘电子助力</td><td>可能产生安全风险</td></tr></tbody></table><p>交互式任务也发生在现实时间中，批处理任务也在真实运行，但它们通常不叫实时任务。实时任务的种差在于：<strong>正确性依赖时间约束</strong>。</p><hr><h2 id="❗-本节核心结论：-批处理关注吞吐和完成时间，交互式任务关注响应时间，实时任务关注-deadline；它们的区别不在于“是否正在运行”，而在于对时间的要求不同。"><a href="#❗-本节核心结论：-批处理关注吞吐和完成时间，交互式任务关注响应时间，实时任务关注-deadline；它们的区别不在于“是否正在运行”，而在于对时间的要求不同。" class="headerlink" title="❗ 本节核心结论： 批处理关注吞吐和完成时间，交互式任务关注响应时间，实时任务关注 deadline；它们的区别不在于“是否正在运行”，而在于对时间的要求不同。"></a>❗ <strong>本节核心结论：</strong> 批处理关注吞吐和完成时间，交互式任务关注响应时间，实时任务关注 deadline；它们的区别不在于“是否正在运行”，而在于对时间的要求不同。</h2><h2 id="5-经典调度算法：FCFS、SJF、STCF-与-RR"><a href="#5-经典调度算法：FCFS、SJF、STCF-与-RR" class="headerlink" title="5. 经典调度算法：FCFS、SJF、STCF 与 RR"></a>5. 经典调度算法：FCFS、SJF、STCF 与 RR</h2><p>经典调度算法主要解决“任务如何排队”的问题。</p><h3 id="5-1-FCFS：先到先得"><a href="#5-1-FCFS：先到先得" class="headerlink" title="5.1 FCFS：先到先得"></a>5.1 FCFS：先到先得</h3><p>FCFS，即 First Come First Served，谁先到谁先执行。</p><p>它的优点是简单、直观、容易实现。缺点是对短任务和 I&#x2F;O 密集型任务不友好。</p><p>假设有三个任务：</p><table><thead><tr><th>任务</th><th align="right">到达时间</th><th align="right">执行时间</th></tr></thead><tbody><tr><td>A</td><td align="right">0</td><td align="right">4</td></tr><tr><td>B</td><td align="right">1</td><td align="right">7</td></tr><tr><td>C</td><td align="right">2</td><td align="right">2</td></tr></tbody></table><p>FCFS 的执行顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A -&gt; B -&gt; C</span><br></pre></td></tr></table></figure><p>C 只需要 2 个时间单位，却要等 B 的 7 个时间单位执行完。这就是典型的 convoy effect：一个长任务堵住后面一串短任务。</p><p>对 I&#x2F;O 密集型任务也类似。I&#x2F;O 密集型任务往往只需要一点 CPU 时间来发起 I&#x2F;O 请求，然后就阻塞等待设备。如果它迟迟拿不到 CPU，它连 I&#x2F;O 请求都发不出去，磁盘或网络设备也可能闲着。</p><h3 id="5-2-SJF：最短任务优先"><a href="#5-2-SJF：最短任务优先" class="headerlink" title="5.2 SJF：最短任务优先"></a>5.2 SJF：最短任务优先</h3><p>SJF，即 Shortest Job First，每次选择总执行时间最短的任务。</p><p>它可以降低平均周转时间，但有两个问题：</p><ul><li>需要提前知道任务运行时间，现实中很难准确知道。</li><li>长任务可能饥饿，因为短任务不断到来时，长任务一直被推迟。</li></ul><h3 id="5-3-STCF：最短完成时间优先"><a href="#5-3-STCF：最短完成时间优先" class="headerlink" title="5.3 STCF：最短完成时间优先"></a>5.3 STCF：最短完成时间优先</h3><p>STCF，即 Shortest Time-to-Completion First，每次选择剩余执行时间最短的任务。</p><p>SJF 和 STCF 的区别是：</p><table><thead><tr><th>算法</th><th>是否抢占</th><th>判断依据</th></tr></thead><tbody><tr><td>SJF</td><td>非抢占</td><td>总执行时间最短</td></tr><tr><td>STCF</td><td>抢占</td><td>剩余完成时间最短</td></tr></tbody></table><p>STCF 允许“插队”：如果一个长任务正在运行，此时来了一个剩余时间更短的新任务，调度器可以打断长任务，先执行新任务。</p><h3 id="5-4-RR：时间片轮转"><a href="#5-4-RR：时间片轮转" class="headerlink" title="5.4 RR：时间片轮转"></a>5.4 RR：时间片轮转</h3><p>RR，即 Round Robin，每个任务运行一个时间片，时间片用完后换下一个任务。</p><p>RR 的核心优势是公平和响应性，适合交互式系统。但时间片长度非常关键：</p><table><thead><tr><th>时间片设置</th><th>后果</th></tr></thead><tbody><tr><td>太长</td><td>退化得接近 FCFS，响应时间变差</td></tr><tr><td>太短</td><td>上下文切换过于频繁，调度开销变高</td></tr></tbody></table><p>时间片过短时，CPU 并不是完全空闲，而是花太多时间在保存和恢复上下文上，真正执行用户任务的有效时间减少。</p><hr><h2 id="❗-本节核心结论：-FCFS-简单但容易拖累短任务；SJF-STCF-优化平均周转时间但可能饿死长任务；RR-提升响应性但依赖合适的时间片。"><a href="#❗-本节核心结论：-FCFS-简单但容易拖累短任务；SJF-STCF-优化平均周转时间但可能饿死长任务；RR-提升响应性但依赖合适的时间片。" class="headerlink" title="❗ 本节核心结论： FCFS 简单但容易拖累短任务；SJF&#x2F;STCF 优化平均周转时间但可能饿死长任务；RR 提升响应性但依赖合适的时间片。"></a>❗ <strong>本节核心结论：</strong> FCFS 简单但容易拖累短任务；SJF&#x2F;STCF 优化平均周转时间但可能饿死长任务；RR 提升响应性但依赖合适的时间片。</h2><h2 id="6-优先级调度：谁更重要，谁先运行"><a href="#6-优先级调度：谁更重要，谁先运行" class="headerlink" title="6. 优先级调度：谁更重要，谁先运行"></a>6. 优先级调度：谁更重要，谁先运行</h2><p>现实中的任务并不完全平等。系统任务、实时任务、交互式任务、后台任务和批处理任务对系统的重要性不同。</p><p>优先级调度的基本思想是：<strong>优先级高的任务先运行</strong>。</p><p>典型优先级可以理解为：</p><table><thead><tr><th>任务类型</th><th>通常优先级</th></tr></thead><tbody><tr><td>实时任务</td><td>最高</td></tr><tr><td>交互式任务</td><td>较高</td></tr><tr><td>普通后台任务</td><td>较低</td></tr><tr><td>批处理任务</td><td>较低</td></tr></tbody></table><p>不过，优先级调度会带来三个典型问题。</p><h3 id="6-1-低资源利用率"><a href="#6-1-低资源利用率" class="headerlink" title="6.1 低资源利用率"></a>6.1 低资源利用率</h3><p>低资源利用率不是说 CPU 一定闲着，而是说系统中的多种资源没有被并行利用起来。</p><p>假设有两个任务：</p><ul><li>A：CPU 密集型任务，一直需要 CPU 计算。</li><li>B：I&#x2F;O 密集型任务，只需要 1ms CPU 发起磁盘读请求，然后等待 10ms 磁盘 I&#x2F;O。</li></ul><p>如果调度器一直偏向 A，那么：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">0ms ~ 20ms：CPU 执行 A</span><br><span class="line">             磁盘闲着</span><br><span class="line">             B 没机会发起 I/O</span><br></pre></td></tr></table></figure><p>这时 CPU 很忙，但磁盘空闲，整体资源利用率不高。</p><p>更好的做法是让 B 先运行 1ms：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">0ms ~ 1ms：B 使用 CPU 发起 I/O</span><br><span class="line">1ms ~ 11ms：磁盘处理 B 的 I/O，同时 CPU 执行 A</span><br></pre></td></tr></table></figure><p>这样 CPU 和磁盘可以并行工作，系统整体吞吐更高。</p><p>因此，I&#x2F;O 密集型任务通常应当获得较高优先级。因为它们只需要一点 CPU，就能把 I&#x2F;O 设备也调动起来。</p><h3 id="6-2-优先级反转"><a href="#6-2-优先级反转" class="headerlink" title="6.2 优先级反转"></a>6.2 优先级反转</h3><p>优先级反转是优先级调度中的经典问题。</p><p>设优先级为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A &gt; B &gt; C</span><br></pre></td></tr></table></figure><p>过程如下：</p><ol><li>低优先级任务 C 持有某个锁。</li><li>高优先级任务 A 也需要这个锁，于是 A 阻塞。</li><li>中优先级任务 B 不需要这个锁，因此可以运行。</li><li>B 抢占 C，导致 C 无法继续运行。</li><li>C 无法释放锁，A 就继续阻塞。</li></ol><p>结果是：A 明明优先级最高，却被 B 间接压制。</p><p>解决方法是优先级继承：A 暂时把自己的高优先级“借给”C，让 C 先于 B 运行，尽快释放锁。锁释放后，A 解除阻塞，C 恢复原优先级。</p><h3 id="6-3-低优先级任务饥饿"><a href="#6-3-低优先级任务饥饿" class="headerlink" title="6.3 低优先级任务饥饿"></a>6.3 低优先级任务饥饿</h3><p>如果系统中不断出现高优先级任务，低优先级任务可能长期得不到 CPU。</p><p>这就是 starvation，饥饿。</p><p>MLFQ 正是为了解决这类问题而出现的一种重要策略。</p><hr><h2 id="7-多级反馈队列-MLFQ：用行为推测任务类型"><a href="#7-多级反馈队列-MLFQ：用行为推测任务类型" class="headerlink" title="7. 多级反馈队列 MLFQ：用行为推测任务类型"></a>7. 多级反馈队列 MLFQ：用行为推测任务类型</h2><p>MLFQ，即 Multi-Level Feedback Queue，多级反馈队列。它的思想是：<strong>系统并不知道任务到底是短任务、长任务、交互式任务还是 CPU 密集型任务，因此通过任务运行行为来动态调整优先级</strong>。</p><p>基本规则可以概括为：</p><ol><li>新任务默认进入最高优先级队列。</li><li>总是优先运行最高优先级的非空队列。</li><li>如果任务用完整个时间片还没结束，说明它可能是长任务，降低优先级。</li><li>如果任务很快主动阻塞，例如等待 I&#x2F;O，说明它可能是交互式任务，保留较高优先级。</li><li>低优先级队列通常使用更长时间片，减少切换开销。</li><li>定期把所有任务提升到最高优先级，防止饥饿。</li></ol><p>可以设定三个队列：</p><table><thead><tr><th>队列</th><th>优先级</th><th align="right">时间片</th></tr></thead><tbody><tr><td>Q0</td><td>最高</td><td align="right">1</td></tr><tr><td>Q1</td><td>中等</td><td align="right">2</td></tr><tr><td>Q2</td><td>最低</td><td align="right">4</td></tr></tbody></table><p>再设定三个任务：</p><table><thead><tr><th>任务</th><th>类型</th><th>行为</th></tr></thead><tbody><tr><td>A</td><td>长计算任务</td><td>需要 12 个时间单位</td></tr><tr><td>B</td><td>交互式&#x2F;I&#x2F;O 型任务</td><td>每次运行 1 个单位后等待 I&#x2F;O</td></tr><tr><td>C</td><td>短任务</td><td>需要 2 个时间单位</td></tr></tbody></table><p>MLFQ 的运行效果大致如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">初始：</span><br><span class="line">Q0: A, B</span><br><span class="line">Q1:</span><br><span class="line">Q2:</span><br><span class="line"></span><br><span class="line">A 运行 1 个单位，用完整个时间片，降到 Q1：</span><br><span class="line">Q0: B</span><br><span class="line">Q1: A</span><br><span class="line">Q2:</span><br><span class="line"></span><br><span class="line">B 运行 1 个单位后主动等待 I/O，保持较高优先级：</span><br><span class="line">Q0:</span><br><span class="line">Q1: A</span><br><span class="line">Q2:</span><br><span class="line">B: blocked</span><br><span class="line"></span><br><span class="line">A 在 Q1 运行 2 个单位，仍未结束，降到 Q2：</span><br><span class="line">Q0:</span><br><span class="line">Q1:</span><br><span class="line">Q2: A</span><br><span class="line"></span><br><span class="line">B 的 I/O 完成，C 到达：</span><br><span class="line">Q0: B, C</span><br><span class="line">Q1:</span><br><span class="line">Q2: A</span><br><span class="line"></span><br><span class="line">B 再次优先运行，C 也较快完成，A 在低优先级队列继续运行。</span><br></pre></td></tr></table></figure><p>MLFQ 的效果是：</p><ul><li>B 这种交互式任务经常很快得到响应。</li><li>C 这种短任务能较快完成。</li><li>A 这种长计算任务会被降级，但仍然能继续运行。</li><li>定期 priority boost 可以防止 A 永远饥饿。</li></ul><hr><h2 id="❗-本节核心结论：-MLFQ-不需要提前知道任务类型，而是根据任务是否用完整个时间片、是否主动阻塞来推测任务类型，并动态调整优先级。"><a href="#❗-本节核心结论：-MLFQ-不需要提前知道任务类型，而是根据任务是否用完整个时间片、是否主动阻塞来推测任务类型，并动态调整优先级。" class="headerlink" title="❗ 本节核心结论： MLFQ 不需要提前知道任务类型，而是根据任务是否用完整个时间片、是否主动阻塞来推测任务类型，并动态调整优先级。"></a>❗ <strong>本节核心结论：</strong> MLFQ 不需要提前知道任务类型，而是根据任务是否用完整个时间片、是否主动阻塞来推测任务类型，并动态调整优先级。</h2><h2 id="8-公平共享调度：公平不是每个任务一样多"><a href="#8-公平共享调度：公平不是每个任务一样多" class="headerlink" title="8. 公平共享调度：公平不是每个任务一样多"></a>8. 公平共享调度：公平不是每个任务一样多</h2><p>优先级调度关心“谁先运行”，公平共享调度关心“长期来看每个人分到多少 CPU”。</p><p>这在多用户或云计算场景中很重要。假设用户 A 创建 4 个任务，用户 B 创建 1 个任务。如果普通 RR 按任务平均分配，那么 A 可能拿到 80% CPU，B 只能拿到 20%。这对用户 B 不公平。</p><p>公平共享调度的核心是：</p><blockquote><p>CPU 应该按用户或任务的权重比例分配，而不是按任务数量机械均分。</p></blockquote><p>例如：</p><table><thead><tr><th>用户</th><th align="right">权重</th></tr></thead><tbody><tr><td>A</td><td align="right">4</td></tr><tr><td>B</td><td align="right">2</td></tr></tbody></table><p>那么 A 应该获得 B 的 2 倍 CPU 时间。这不是不公平，而是按权重定义后的公平。</p><hr><h2 id="9-彩票调度与步幅调度"><a href="#9-彩票调度与步幅调度" class="headerlink" title="9. 彩票调度与步幅调度"></a>9. 彩票调度与步幅调度</h2><p>公平共享调度中，ticket、stride、pass 是三个核心概念。</p><h3 id="9-1-ticket：权重"><a href="#9-1-ticket：权重" class="headerlink" title="9.1 ticket：权重"></a>9.1 ticket：权重</h3><p>ticket 表示任务权重。ticket 越多，任务应该获得的 CPU 时间比例越大。</p><p>公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">任务占用比例 = 任务 ticket / 总 ticket</span><br></pre></td></tr></table></figure><p>例如：</p><table><thead><tr><th>任务</th><th align="right">ticket</th><th align="right">期望 CPU 占比</th></tr></thead><tbody><tr><td>A1</td><td align="right">20</td><td align="right">20%</td></tr><tr><td>A2</td><td align="right">30</td><td align="right">30%</td></tr><tr><td>B1</td><td align="right">50</td><td align="right">50%</td></tr></tbody></table><p>如果 A1 和 A2 都属于用户 A，那么用户 A 总共获得 50%。</p><h3 id="9-2-彩票调度"><a href="#9-2-彩票调度" class="headerlink" title="9.2 彩票调度"></a>9.2 彩票调度</h3><p>彩票调度 Lottery Scheduling 的规则很简单：</p><ol><li>每个任务持有若干 ticket。</li><li>每次调度随机抽一张票。</li><li>抽中的 ticket 属于哪个任务，就运行哪个任务。</li></ol><p>ticket 多的任务被抽中的概率更大。长期来看，任务获得的 CPU 时间比例会接近 ticket 比例。</p><p>彩票调度的优点是简单，并且票少的任务仍有机会被抽中，不容易彻底饥饿。缺点是随机性较强，短期结果可能不精确。</p><h3 id="9-3-stride：步幅"><a href="#9-3-stride：步幅" class="headerlink" title="9.3 stride：步幅"></a>9.3 stride：步幅</h3><p>步幅调度 Stride Scheduling 可以看作彩票调度的确定性版本。</p><p>它使用公式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stride = MaxStride / ticket</span><br></pre></td></tr></table></figure><p>其中，MaxStride 是一个足够大的整数，常见做法是设成所有 ticket 的最小公倍数，目的是避免小数，方便整数计算。</p><p>例如：</p><table><thead><tr><th>任务</th><th align="right">ticket</th><th align="right">stride</th></tr></thead><tbody><tr><td>A1</td><td align="right">30</td><td align="right">10</td></tr><tr><td>A2</td><td align="right">50</td><td align="right">6</td></tr><tr><td>B1</td><td align="right">60</td><td align="right">5</td></tr></tbody></table><p>这里可以取：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">MaxStride = 300</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">A1.stride = 300 / 30 = 10</span><br><span class="line">A2.stride = 300 / 50 = 6</span><br><span class="line">B1.stride = 300 / 60 = 5</span><br></pre></td></tr></table></figure><p>ticket 越大，stride 越小。</p><h3 id="9-4-pass：累计虚拟时间"><a href="#9-4-pass：累计虚拟时间" class="headerlink" title="9.4 pass：累计虚拟时间"></a>9.4 pass：累计虚拟时间</h3><p>pass 表示任务累计运行的虚拟时间。步幅调度规则是：</p><ol><li>每次选择 pass 最小的任务运行。</li><li>任务运行一个时间片后，执行：</li></ol><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pass = pass + stride</span><br></pre></td></tr></table></figure><p>因此：</p><ul><li>ticket 多 → stride 小</li><li>stride 小 → 每次运行后 pass 增长慢</li><li>pass 增长慢 → 更容易再次成为 pass 最小者</li><li>更容易再次被选中 → 获得更多 CPU 时间</li></ul><p>在教学模型中，所有任务的初始 pass 可以设为 0。但真实系统中，新任务加入时通常不会简单设成 0。否则如果系统已经运行很久，老任务 pass 都很大，新任务 pass 为 0，就会不公平地连续被调度。更合理的做法是把新任务 pass 设为当前运行队列的最小 pass 或某个基准虚拟时间。</p><p>Linux CFS 中的 vruntime 就可以类比为这里的 pass。</p><hr><h2 id="❗-本节核心结论：-ticket-决定长期-CPU-占比，stride-决定每次运行后-pass-增加多少，pass-决定下一次谁更该被调度。"><a href="#❗-本节核心结论：-ticket-决定长期-CPU-占比，stride-决定每次运行后-pass-增加多少，pass-决定下一次谁更该被调度。" class="headerlink" title="❗ 本节核心结论： ticket 决定长期 CPU 占比，stride 决定每次运行后 pass 增加多少，pass 决定下一次谁更该被调度。"></a>❗ <strong>本节核心结论：</strong> ticket 决定长期 CPU 占比，stride 决定每次运行后 pass 增加多少，pass 决定下一次谁更该被调度。</h2><h2 id="10-实时调度：重点不是快，而是按时"><a href="#10-实时调度：重点不是快，而是按时" class="headerlink" title="10. 实时调度：重点不是快，而是按时"></a>10. 实时调度：重点不是快，而是按时</h2><p>实时调度的核心不是“尽量快”，而是“必须在 deadline 前完成”。</p><p>对于周期任务，常用 CPU 利用率衡量任务总需求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">U = 执行时间 / 周期</span><br></pre></td></tr></table></figure><p>多个周期任务的总利用率为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">U_total = U1 + U2 + ... + Un</span><br></pre></td></tr></table></figure><p>如果 <code>U_total &gt; 1</code>，说明任务总 CPU 需求已经超过单核 CPU 能力，一定无法全部按时完成。</p><p>但 <code>U_total &lt; 1</code> 只说明从总工作量上看 CPU 理论上够用，不保证某个具体调度策略一定能满足所有 deadline。</p><h3 id="10-1-RM：速率单调调度"><a href="#10-1-RM：速率单调调度" class="headerlink" title="10.1 RM：速率单调调度"></a>10.1 RM：速率单调调度</h3><p>RM，即 Rate-Monotonic，是静态优先级实时调度。</p><p>规则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">周期越短，优先级越高</span><br></pre></td></tr></table></figure><p>它的优先级关系在任务参数确定后基本固定。注意，RM 不是提前规划好完整执行序列，而是提前确定任务之间的相对优先级。实际执行序列仍取决于任务到达、释放、阻塞和抢占情况。</p><p>RM 的问题是：即使 <code>U_total &lt; 1</code>，也可能错过 deadline。</p><p>例如：</p><table><thead><tr><th>任务</th><th align="right">执行时间</th><th align="right">周期</th></tr></thead><tbody><tr><td>A</td><td align="right">5</td><td align="right">13</td></tr><tr><td>B</td><td align="right">3</td><td align="right">5</td></tr></tbody></table><p>总利用率为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">U = 5/13 + 3/5 = 64/65 &lt; 1</span><br></pre></td></tr></table></figure><p>从总量看，CPU 似乎够用。但 RM 规定周期短者优先，所以 B 优先级高于 A。B 会周期性抢占 A，导致 A 可能在自己的 deadline 前还没完成。</p><p>这说明：</p><blockquote><p>总时间够，不代表时间安排一定合适。</p></blockquote><h3 id="10-2-EDF：最早截止时间优先"><a href="#10-2-EDF：最早截止时间优先" class="headerlink" title="10.2 EDF：最早截止时间优先"></a>10.2 EDF：最早截止时间优先</h3><p>EDF，即 Earliest Deadline First，是动态优先级实时调度。</p><p>规则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">谁的 deadline 最近，谁先运行</span><br></pre></td></tr></table></figure><p>EDF 的优先级不是静态固定的，而是随着当前任务集合和各自 deadline 动态变化。</p><table><thead><tr><th>调度算法</th><th>优先级类型</th><th>规则</th></tr></thead><tbody><tr><td>RM</td><td>静态优先级</td><td>周期越短，优先级越高</td></tr><tr><td>EDF</td><td>动态优先级</td><td>deadline 越近，优先级越高</td></tr></tbody></table><p>EDF 更直接围绕 deadline 做决策，因此在实时调度中具有重要理论意义。但如果任务集合本身不可调度，例如 <code>U_total &gt; 1</code>，EDF 也无法创造额外 CPU 时间，甚至可能出现多个任务连续错过 deadline 的多米诺效应。</p><hr><h2 id="❗-本节核心结论：-RM-靠固定周期规则决定优先级，EDF-根据当前-deadline-动态调整优先级；U-1-只是总工作量可能可行，不等于任意调度策略都能成功。"><a href="#❗-本节核心结论：-RM-靠固定周期规则决定优先级，EDF-根据当前-deadline-动态调整优先级；U-1-只是总工作量可能可行，不等于任意调度策略都能成功。" class="headerlink" title="❗ 本节核心结论： RM 靠固定周期规则决定优先级，EDF 根据当前 deadline 动态调整优先级；U &lt; 1 只是总工作量可能可行，不等于任意调度策略都能成功。"></a>❗ <strong>本节核心结论：</strong> RM 靠固定周期规则决定优先级，EDF 根据当前 deadline 动态调整优先级；<code>U &lt; 1</code> 只是总工作量可能可行，不等于任意调度策略都能成功。</h2><h2 id="11-易混概念整理"><a href="#11-易混概念整理" class="headerlink" title="11. 易混概念整理"></a>11. 易混概念整理</h2><table><thead><tr><th>易混点</th><th>正确区分</th></tr></thead><tbody><tr><td>批处理任务 vs <code>.bat</code> 文件</td><td>前者是调度中的工作负载类型，后者是 Windows 脚本文件</td></tr><tr><td>实时任务 vs 正在运行的任务</td><td>实时任务的关键是 deadline，不是字面上的“实时发生”</td></tr><tr><td>交互式任务 vs 普通任务</td><td>交互式任务关注用户或外部请求的快速响应</td></tr><tr><td>SJF vs STCF</td><td>SJF 看总执行时间，STCF 看剩余完成时间；STCF 是抢占式</td></tr><tr><td>优先级 vs 权重</td><td>优先级决定谁先运行，权重决定长期 CPU 占比</td></tr><tr><td>公平共享 vs 平均分配</td><td>公平共享按权重比例分配，不是每个任务一样多</td></tr><tr><td>彩票调度 vs 步幅调度</td><td>彩票调度随机，步幅调度确定性计算</td></tr><tr><td>RM vs EDF</td><td>RM 是静态优先级，EDF 是动态优先级</td></tr></tbody></table><hr><h2 id="12-复习清单"><a href="#12-复习清单" class="headerlink" title="12. 复习清单"></a>12. 复习清单</h2><p>学完本讲后，应能回答：</p><ul><li>调度器主要决定哪三个问题？</li><li>为什么调度没有唯一正确答案？</li><li>周转时间和响应时间有什么区别？</li><li>批处理任务、交互式任务、实时任务分别关注什么？</li><li>FCFS 为什么容易拖累短任务和 I&#x2F;O 密集型任务？</li><li>SJF 和 STCF 的核心区别是什么？</li><li>RR 的时间片太长和太短分别有什么问题？</li><li>什么是优先级反转？优先级继承如何解决？</li><li>为什么 I&#x2F;O 密集型任务优先级太低会降低资源利用率？</li><li>MLFQ 如何根据任务行为动态调整优先级？</li><li>ticket、stride、pass 分别表示什么？</li><li>为什么 stride 越小，任务越容易再次被调度？</li><li>RM 为什么在 <code>U &lt; 1</code> 时仍可能失败？</li><li>EDF 为什么叫动态优先级调度？</li></ul><hr><h2 id="最终总结"><a href="#最终总结" class="headerlink" title="最终总结"></a>最终总结</h2><p>调度 I 的主线可以压缩为一句话：</p><blockquote><p><strong>操作系统调度不是单纯让任务“快点跑”，而是在有限 CPU 上，根据任务的响应需求、完成需求、公平需求和 deadline 约束，决定谁先运行、运行多久、如何避免饥饿和错过时限。</strong></p></blockquote><p>FCFS、SJF、STCF 和 RR 解决的是基本排队问题；优先级调度解决的是“谁更重要”；MLFQ 通过行为反馈动态推测任务类型；公平共享调度解决的是“长期 CPU 时间如何按权重分配”；实时调度则进一步要求任务必须在 deadline 前完成。</p><p>真正掌握调度，不是背下算法名，而是能解释每个策略在优化什么、牺牲什么，以及它为什么适合某一类任务。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第10讲 进程与线程II</title>
      <link href="/2026/06/11/%E7%AC%AC10%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E4%B8%8E%E7%BA%BF%E7%A8%8BII/"/>
      <url>/2026/06/11/%E7%AC%AC10%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E4%B8%8E%E7%BA%BF%E7%A8%8BII/</url>
      
        <content type="html"><![CDATA[<h1 id="进程与线程-II：线程模型、TCB-TLS-与纤程协程的完整理解"><a href="#进程与线程-II：线程模型、TCB-TLS-与纤程协程的完整理解" class="headerlink" title="进程与线程 II：线程模型、TCB&#x2F;TLS 与纤程协程的完整理解"></a>进程与线程 II：线程模型、TCB&#x2F;TLS 与纤程协程的完整理解</h1><p>本文根据《第10讲 进程与线程II.md》的对话内容整理而成，目标是把原始问答重构为一篇可独立阅读、可复习、可维护的正式学习文档。</p><hr><h2 id="引言：本讲的核心问题"><a href="#引言：本讲的核心问题" class="headerlink" title="引言：本讲的核心问题"></a>引言：本讲的核心问题</h2><p>“进程与线程 I”主要解释进程：程序如何变成进程，操作系统如何用 PCB 管理进程，以及 <code>fork</code>、<code>exec</code>、<code>wait</code>、<code>exit</code> 如何构成进程生命周期。</p><p>“进程与线程 II”的主线则进一步推进：</p><blockquote><p>进程太重，所以引入线程；内核线程仍然有开销，所以进一步引入用户态线程、纤程和协程。</p></blockquote><p>本讲要解决的核心问题不是“线程是什么”这一句定义，而是以下几个问题：</p><ol><li>为什么进程内部还需要线程？</li><li>线程和进程到底共享什么、独占什么？</li><li>用户态线程和内核态线程如何区分？</li><li>多对一、一对一、多对多模型中的“对”到底是什么意思？</li><li>TCB 和 TLS 分别解决什么问题？</li><li>pthread 的创建、等待、退出和让出 CPU 如何理解？</li><li>线程上下文切换到底发生了什么？</li><li>纤程和协程为什么比线程更轻量？</li></ol><p>整节课的逻辑可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">进程太重</span><br><span class="line">  ↓</span><br><span class="line">线程：进程内更轻量的执行流</span><br><span class="line">  ↓</span><br><span class="line">内核线程仍有创建与切换开销</span><br><span class="line">  ↓</span><br><span class="line">用户态线程 / 纤程 / 协程：由用户态或语言运行时调度的轻量执行流</span><br></pre></td></tr></table></figure><hr><h2 id="1-为什么需要线程"><a href="#1-为什么需要线程" class="headerlink" title="1. 为什么需要线程"></a>1. 为什么需要线程</h2><p>进程是操作系统管理程序运行的基本抽象，但进程本身比较重。一个进程不仅包含执行状态，还包含代码、数据、堆、栈、页表、文件描述符、地址空间等资源。创建一个进程，意味着操作系统要准备一整套运行环境。</p><p>这带来三个问题。</p><p>第一，创建进程开销大。<br>如果一个任务很短，却每次都创建一个新进程，创建和销毁本身就会变成明显负担。</p><p>第二，进程间通信开销大。<br>不同进程拥有不同地址空间，彼此不能直接访问对方的数据。如果要交互，通常需要 IPC。IPC 安全，但比同一地址空间内的变量访问、函数调用要重得多。</p><p>第三，单个进程内部需要多个执行流。<br>例如浏览器可能同时需要处理页面渲染、网络请求、音频播放、用户输入和脚本执行。如果一个进程内部只有一个执行流，这些任务就只能排队执行。</p><p>因此，操作系统引入线程：</p><blockquote><p>线程是进程内部更轻量的执行流。</p></blockquote><p>线程让一个进程内部可以同时存在多个执行路径。它们共享进程提供的资源，但各自拥有自己的运行状态。</p><hr><h2 id="2-线程和进程的基本区别"><a href="#2-线程和进程的基本区别" class="headerlink" title="2. 线程和进程的基本区别"></a>2. 线程和进程的基本区别</h2><p>可以用一句话区分进程和线程：</p><blockquote><p>进程是资源分配单位，线程是 CPU 调度和执行单位。</p></blockquote><p>进程像一家公司，拥有办公楼、资产、文件和制度；线程像公司里的员工，每个员工有自己的工作状态和执行路线，但共享公司的资源。</p><table><thead><tr><th>对比项</th><th>进程</th><th>线程</th></tr></thead><tbody><tr><td>基本角色</td><td>资源分配单位</td><td>执行与调度单位</td></tr><tr><td>地址空间</td><td>进程之间通常独立</td><td>同一进程内线程共享</td></tr><tr><td>创建开销</td><td>较大</td><td>较小</td></tr><tr><td>通信方式</td><td>IPC，较重</td><td>共享内存，较轻</td></tr><tr><td>隔离性</td><td>强</td><td>弱</td></tr><tr><td>崩溃影响</td><td>通常影响本进程</td><td>一个线程出错可能带崩整个进程</td></tr><tr><td>调度</td><td>早期可作为调度单位</td><td>现代系统主要调度线程</td></tr></tbody></table><p>线程自己的内容主要包括：</p><ul><li>程序计数器 PC；</li><li>寄存器状态；</li><li>用户栈；</li><li>内核栈；</li><li>线程状态；</li><li>线程优先级；</li><li>调度相关信息。</li></ul><p>线程共享的内容主要包括：</p><ul><li>代码段；</li><li>数据段；</li><li>堆；</li><li><code>mmap</code> 区域；</li><li>文件描述符；</li><li>进程级资源；</li><li>同一个虚拟地址空间。</li></ul><p>因此，多线程的本质是：</p><blockquote><p>多个执行流共享同一套进程资源，但每个执行流有自己的运行现场。</p></blockquote><hr><h2 id="3-“共享地址空间”不等于“所有变量都共享”"><a href="#3-“共享地址空间”不等于“所有变量都共享”" class="headerlink" title="3. “共享地址空间”不等于“所有变量都共享”"></a>3. “共享地址空间”不等于“所有变量都共享”</h2><p>一个容易误解的点是：既然同一进程内多个线程共享地址空间，那是不是所有变量都共享？</p><p>答案是否定的。</p><p>更准确地说：</p><blockquote><p>多线程共享同一个虚拟地址空间，但并不意味着所有变量实例都只有一份。</p></blockquote><p>多线程中的变量可以分成三类。</p><h3 id="3-1-普通全局变量：通常共享"><a href="#3-1-普通全局变量：通常共享" class="headerlink" title="3.1 普通全局变量：通常共享"></a>3.1 普通全局变量：通常共享</h3><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> global_count = <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>多个线程访问的是同一个 <code>global_count</code>。如果多个线程同时执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">global_count++;</span><br></pre></td></tr></table></figure><p>就可能发生数据竞争。因为 <code>++</code> 通常不是一个原子操作，而可能被拆成：</p><ol><li>读取变量；</li><li>加一；</li><li>写回变量。</li></ol><p>多个线程交错执行时，可能造成更新丢失。</p><hr><h3 id="3-2-栈变量：每个线程天然独有"><a href="#3-2-栈变量：每个线程天然独有" class="headerlink" title="3.2 栈变量：每个线程天然独有"></a>3.2 栈变量：每个线程天然独有</h3><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *<span class="title function_">func</span><span class="params">(<span class="type">void</span> *arg)</span> &#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">0</span>;</span><br><span class="line">    x++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果多个线程都执行 <code>func</code>，每个线程都有自己的用户栈，所以每个线程都有自己的 <code>x</code>。这些变量名字一样，但位于不同线程的栈上，不是同一个内存位置。</p><hr><h3 id="3-3-TLS-变量：形式像全局变量，但每个线程有一份"><a href="#3-3-TLS-变量：形式像全局变量，但每个线程有一份" class="headerlink" title="3.3 TLS 变量：形式像全局变量，但每个线程有一份"></a>3.3 TLS 变量：形式像全局变量，但每个线程有一份</h3><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">__thread <span class="type">int</span> id;</span><br></pre></td></tr></table></figure><p>这类变量看起来像全局变量，但每个线程都有自己的副本。</p><p>可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">线程 1 的 TLS 区：id = 1</span><br><span class="line">线程 2 的 TLS 区：id = 2</span><br><span class="line">线程 3 的 TLS 区：id = 3</span><br></pre></td></tr></table></figure><p>代码里都写 <code>id</code>，但不同线程访问的是自己 TLS 区中的 <code>id</code>。</p><p>所以更精确的结论是：</p><blockquote><p>同一进程内的多线程共享地址空间；普通全局变量和堆对象通常共享；每个线程的栈变量天然独立；被声明为 TLS 的变量虽然形式上像全局变量，但每个线程有自己的副本。</p></blockquote><hr><h2 id="4-用户态线程和内核态线程"><a href="#4-用户态线程和内核态线程" class="headerlink" title="4. 用户态线程和内核态线程"></a>4. 用户态线程和内核态线程</h2><p>线程可以按照“是否由内核直接管理”分成两类：</p><ol><li>内核态线程；</li><li>用户态线程。</li></ol><p>这里的“内核态线程”并不是说这个线程永远运行在内核态；“用户态线程”也不是说它只能执行普通用户代码。真正的区分点是：</p><blockquote><p>谁知道它，谁管理它，谁调度它。</p></blockquote><hr><h3 id="4-1-内核态线程"><a href="#4-1-内核态线程" class="headerlink" title="4.1 内核态线程"></a>4.1 内核态线程</h3><p>内核态线程是由操作系统内核创建、记录、调度和切换的线程。</p><p>它的特点是：</p><ul><li>内核知道它的存在；</li><li>内核调度器可以直接调度它；</li><li>它通常有对应的内核数据结构；</li><li>它可以被分配到不同 CPU 核心上运行；</li><li>创建和切换通常涉及内核，开销相对更大。</li></ul><p>例如，在主流 Linux 实现中，<code>pthread_create</code> 创建的 pthread 通常对应一个内核调度实体。这个线程执行用户函数时当然是在用户态运行，但它背后的调度对象由内核管理。</p><p>因此，“内核态线程”的重点不是它运行的代码位置，而是它是否是内核可见、内核可调度的对象。</p><hr><h3 id="4-2-用户态线程"><a href="#4-2-用户态线程" class="headerlink" title="4.2 用户态线程"></a>4.2 用户态线程</h3><p>用户态线程是由用户态线程库或语言运行时管理的轻量执行流。内核通常不知道每一个用户态线程的存在。</p><p>它的特点是：</p><ul><li>内核不直接知道它；</li><li>用户态运行时或线程库管理它；</li><li>用户态调度器负责切换它；</li><li>切换通常不需要进入内核；</li><li>创建和切换开销较小；</li><li>是否能利用多核，取决于它映射到底层几个内核线程上。</li></ul><p>可以用下面的结构理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">内核眼中：</span><br><span class="line">    一个内核线程 K</span><br><span class="line"></span><br><span class="line">用户态运行时眼中：</span><br><span class="line">    K 上轮流运行 u1、u2、u3、u4</span><br></pre></td></tr></table></figure><p>内核只看见 <code>K</code>，但用户态运行时知道自己内部还有多个用户态执行流。</p><hr><h3 id="4-3-二者对比"><a href="#4-3-二者对比" class="headerlink" title="4.3 二者对比"></a>4.3 二者对比</h3><table><thead><tr><th>对比项</th><th>用户态线程</th><th>内核态线程</th></tr></thead><tbody><tr><td>内核是否直接可见</td><td>通常不可见</td><td>可见</td></tr><tr><td>谁创建</td><td>用户态线程库 &#x2F; 语言运行时</td><td>操作系统内核</td></tr><tr><td>谁调度</td><td>用户态调度器</td><td>内核调度器</td></tr><tr><td>切换是否进入内核</td><td>通常不需要</td><td>通常需要</td></tr><tr><td>切换开销</td><td>小</td><td>较大</td></tr><tr><td>多核并行能力</td><td>取决于映射到几个内核线程</td><td>可以直接被内核分配到多核</td></tr><tr><td>阻塞影响</td><td>一个底层内核线程阻塞可能影响其上多个用户线程</td><td>一个线程阻塞通常不影响其他内核线程</td></tr><tr><td>典型例子</td><td>ucontext、fiber、协程运行时内部任务</td><td>pthread 背后的 Linux task</td></tr></tbody></table><p>核心结论是：</p><blockquote><p>用户态线程和内核态线程的区别，不在于代码是否运行在用户态，而在于线程由谁管理、谁调度、内核是否看得见。</p></blockquote><hr><h2 id="5-三种线程模型中的“对”是什么意思"><a href="#5-三种线程模型中的“对”是什么意思" class="headerlink" title="5. 三种线程模型中的“对”是什么意思"></a>5. 三种线程模型中的“对”是什么意思</h2><p>线程模型中的“多对一”“一对一”“多对多”，讨论的是：</p><blockquote><p>用户态线程和内核态线程之间的映射关系。</p></blockquote><p>这里的“对”可以理解为：</p><blockquote><p>上层用户态执行流，如何借助下层内核调度实体获得 CPU 执行机会。</p></blockquote><p>用户态线程本身不能直接占用 CPU。真正能被操作系统调度到 CPU 上执行的是内核态线程。因此，用户态线程必须映射或挂靠到某个内核线程上，才能真正运行。</p><p>可以把用户态线程理解为“任务”，把内核态线程理解为“跑道”：</p><table><thead><tr><th>模型</th><th>类比</th></tr></thead><tbody><tr><td>多对一</td><td>很多任务共用一条跑道</td></tr><tr><td>一对一</td><td>一个任务配一条跑道</td></tr><tr><td>多对多</td><td>很多任务共用多条跑道</td></tr></tbody></table><hr><h2 id="6-多对一模型"><a href="#6-多对一模型" class="headerlink" title="6. 多对一模型"></a>6. 多对一模型</h2><p>多对一模型是：</p><blockquote><p>多个用户态线程映射到一个内核态线程。</p></blockquote><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">用户态： u1   u2   u3   u4</span><br><span class="line">          \   |    |   /</span><br><span class="line">内核态：        K1</span><br></pre></td></tr></table></figure><p>在这个模型中，内核只知道一个内核线程 <code>K1</code>，不知道用户态内部还有 <code>u1</code>、<code>u2</code>、<code>u3</code>、<code>u4</code>。</p><p>它的优点是：</p><ul><li>用户态线程创建快；</li><li>用户态切换快；</li><li>不需要频繁进入内核；</li><li>内核管理简单。</li></ul><p>它的缺点也很明显：</p><ul><li>不能真正利用多核；</li><li>一个底层内核线程阻塞，可能影响其上的所有用户态线程；</li><li>可扩展性较差。</li></ul><p>最直白地说：</p><blockquote><p>多对一模型中，用户态觉得“我有很多线程”，但内核觉得“你只有一个线程”。</p></blockquote><hr><h2 id="7-一对一模型"><a href="#7-一对一模型" class="headerlink" title="7. 一对一模型"></a>7. 一对一模型</h2><p>一对一模型是：</p><blockquote><p>一个用户态线程对应一个内核态线程。</p></blockquote><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">用户态： u1   u2   u3</span><br><span class="line">          |    |    |</span><br><span class="line">内核态： K1   K2   K3</span><br></pre></td></tr></table></figure><p>在这个模型中，每个用户态线程都有一个对应的内核线程。内核能看见每个线程，也能分别调度它们。</p><p>它的优点是：</p><ul><li>可以利用多核；</li><li>一个线程阻塞，不一定阻塞其他线程；</li><li>调度由内核统一管理；</li><li>更符合现代操作系统的调度机制。</li></ul><p>它的缺点是：</p><ul><li>内核线程数量可能很多；</li><li>创建、销毁、切换都有内核开销；</li><li>大量短命线程会造成明显负担。</li></ul><p>Linux、Windows、macOS 等主流系统通常采用一对一模型。</p><p>最直白地说：</p><blockquote><p>一对一模型中，用户态觉得“我有几个线程”，内核也基本觉得“你有几个线程”。</p></blockquote><hr><h2 id="8-多对多模型"><a href="#8-多对多模型" class="headerlink" title="8. 多对多模型"></a>8. 多对多模型</h2><p>多对多模型是：</p><blockquote><p>N 个用户态线程映射到 M 个内核态线程，通常 N &gt; M。</p></blockquote><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">用户态： u1   u2   u3   u4   u5   u6</span><br><span class="line">          \   |   /      \   |   /</span><br><span class="line">内核态：       K1            K2</span><br></pre></td></tr></table></figure><p>它试图同时解决多对一和一对一的问题。</p><p>相比多对一，它可以利用多个内核线程，从而更好地利用多核。<br>相比一对一，它不要求每个用户态线程都对应一个内核线程，可以减少内核线程数量。</p><p>它的优点是：</p><ul><li>能利用多核；</li><li>能控制内核线程数量；</li><li>适合大量用户态执行流；</li><li>理论上更加灵活。</li></ul><p>它的缺点是：</p><ul><li>管理复杂；</li><li>用户态调度器和内核调度器需要配合；</li><li>实现难度高。</li></ul><p>最直白地说：</p><blockquote><p>多对多模型中，用户态线程很多，但真正交给内核调度的线程数量被控制住。</p></blockquote><hr><h2 id="9-TCB：线程控制块"><a href="#9-TCB：线程控制块" class="headerlink" title="9. TCB：线程控制块"></a>9. TCB：线程控制块</h2><p>之前学习进程时，有 PCB，即 Process Control Block。对应地，线程也有 TCB，即 Thread Control Block。</p><p>TCB 用于保存线程相关信息，例如：</p><ul><li>线程 ID；</li><li>线程状态；</li><li>线程优先级；</li><li>寄存器上下文；</li><li>栈指针；</li><li>TLS 信息；</li><li>调度相关信息。</li></ul><p>如果说 PCB 是进程的“档案袋”，那么 TCB 就是线程的“档案袋”。</p><p>在线程模型中，TCB 可能分成内核态部分和用户态部分：</p><table><thead><tr><th>部分</th><th>作用</th></tr></thead><tbody><tr><td>内核态 TCB</td><td>由内核维护，支持调度、上下文切换、内核栈等</td></tr><tr><td>用户态 TCB</td><td>由线程库维护，保存 pthread 结构、用户栈、TLS 等</td></tr></tbody></table><p>在 Linux 中，进程和线程在内核层面都可以看作 task，常用同一种核心数据结构表示。它们的差别主要不在于“是不是 task”，而在于共享资源的程度不同。</p><p>可以概括为：</p><blockquote><p>Linux 内核中，进程和线程都可被看作调度实体；线程与进程的差异主要体现在地址空间、文件表等资源是否共享。</p></blockquote><hr><h2 id="10-TLS：线程本地存储"><a href="#10-TLS：线程本地存储" class="headerlink" title="10. TLS：线程本地存储"></a>10. TLS：线程本地存储</h2><p>TLS 是 Thread Local Storage，即线程本地存储。它解决的问题是：</p><blockquote><p>多线程共享地址空间，但有些变量需要每个线程各自拥有一份。</p></blockquote><p>普通全局变量默认共享：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> global_id;</span><br></pre></td></tr></table></figure><p>如果线程 1 修改它，线程 2 也能看到。</p><p>但有些变量虽然写起来像全局变量，却不应该被所有线程共享。例如 <code>errno</code>。如果多个线程同时调用系统库函数出错，每个线程都应该看到自己的错误码，而不是互相覆盖。</p><p>因此需要 TLS：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">__thread <span class="type">int</span> id;</span><br></pre></td></tr></table></figure><p>它表示每个线程都有自己的 <code>id</code>。</p><p>TLS 的访问通常可以通过 TCB 或特定线程寄存器完成，寻址方式可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">线程本地区域基地址 + 变量偏移量</span><br></pre></td></tr></table></figure><p>所以，TLS 的关键不是“变量名不同”，而是：</p><blockquote><p>同一个变量名，在不同线程中对应不同的存储实例。</p></blockquote><hr><h2 id="11-pthread-的基本操作"><a href="#11-pthread-的基本操作" class="headerlink" title="11. pthread 的基本操作"></a>11. pthread 的基本操作</h2><p>pthread 是 POSIX 线程库，常见操作包括：</p><ol><li><code>pthread_create</code></li><li><code>pthread_join</code></li><li><code>pthread_exit</code></li><li><code>pthread_yield</code></li></ol><hr><h3 id="11-1-pthread-create：创建线程"><a href="#11-1-pthread-create：创建线程" class="headerlink" title="11.1 pthread_create：创建线程"></a>11.1 pthread_create：创建线程</h3><p>典型形式如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pthread_create(&amp;thread, <span class="literal">NULL</span>, function, arg);</span><br></pre></td></tr></table></figure><p>它的作用是创建一个新线程，并让新线程从指定函数开始执行。</p><p>在一对一模型下，<code>pthread_create</code> 往往涉及两层工作：</p><table><thead><tr><th>层次</th><th>工作</th></tr></thead><tbody><tr><td>内核态</td><td>创建对应内核线程，准备内核栈，加入调度系统</td></tr><tr><td>应用态</td><td>创建 pthread 结构，准备用户栈，建立 TLS</td></tr></tbody></table><p>因此，pthread 虽然比进程轻，但并不是没有成本。</p><hr><h3 id="11-2-pthread-join：等待线程结束"><a href="#11-2-pthread-join：等待线程结束" class="headerlink" title="11.2 pthread_join：等待线程结束"></a>11.2 pthread_join：等待线程结束</h3><p>典型形式如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pthread_join(thread, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>它的作用是：</p><ul><li>等待指定线程结束；</li><li>获取该线程的返回值；</li><li>回收相关线程资源。</li></ul><p>可以把它理解为：</p><blockquote><p><code>pthread_create</code> 分出一条执行流，<code>pthread_join</code> 等这条执行流结束。</p></blockquote><p>因此，<code>join</code> 有点像 <code>fork</code> 的逆向操作：<code>fork/create</code> 是分出去，<code>join</code> 是等回来。</p><hr><h3 id="11-3-pthread-exit：线程主动退出"><a href="#11-3-pthread-exit：线程主动退出" class="headerlink" title="11.3 pthread_exit：线程主动退出"></a>11.3 pthread_exit：线程主动退出</h3><p>典型形式如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pthread_exit(retval);</span><br></pre></td></tr></table></figure><p>它让当前线程退出，并可以提供返回值。这个返回值可以被其他线程通过 <code>pthread_join</code> 获取。</p><hr><h3 id="11-4-pthread-yield：主动让出-CPU"><a href="#11-4-pthread-yield：主动让出-CPU" class="headerlink" title="11.4 pthread_yield：主动让出 CPU"></a>11.4 pthread_yield：主动让出 CPU</h3><p><code>pthread_yield</code> 的含义是：</p><blockquote><p>当前线程主动暂停，让调度器有机会运行其他线程。</p></blockquote><p>它不是结束线程，而是当前线程说：“我现在可以先不运行，CPU 可以给其他线程。”</p><hr><h2 id="12-join-放在哪里：串行还是并行"><a href="#12-join-放在哪里：串行还是并行" class="headerlink" title="12. join 放在哪里：串行还是并行"></a>12. join 放在哪里：串行还是并行</h2><p>线程题里，一个高频考点是 <code>join</code> 的位置。</p><p>如果写成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">pthread_create(&amp;p1, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(p1, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">pthread_create(&amp;p2, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(p2, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">pthread_create(&amp;p3, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(p3, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>这基本不是并行执行。因为每创建一个线程，主线程马上等待它结束。执行顺序近似为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">p1 完成</span><br><span class="line">p2 完成</span><br><span class="line">p3 完成</span><br></pre></td></tr></table></figure><p>如果每个线程执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">200</span>; i++) &#123;</span><br><span class="line">    balance++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么结果通常稳定为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">p1 后 balance = 200</span><br><span class="line">p2 后 balance = 400</span><br><span class="line">p3 后 balance = 600</span><br><span class="line">Final Balance = 600</span><br></pre></td></tr></table></figure><p>但如果写成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">pthread_create(&amp;p1, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line">pthread_create(&amp;p2, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line">pthread_create(&amp;p3, <span class="literal">NULL</span>, mythread, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">pthread_join(p1, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(p2, <span class="literal">NULL</span>);</span><br><span class="line">pthread_join(p3, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>这时三个线程可能并行执行。由于 <code>balance++</code> 不是原子操作，最终结果不一定可靠。循环次数越大，数据竞争越容易表现出来。</p><p>核心判断方法是：</p><blockquote><p>创建一个就立刻 join：基本串行。<br>先全部 create，再全部 join：可能并行，需要考虑数据竞争。</p></blockquote><hr><h2 id="13-线程上下文切换"><a href="#13-线程上下文切换" class="headerlink" title="13. 线程上下文切换"></a>13. 线程上下文切换</h2><p>线程上下文切换是本讲较硬的部分。它回答的是：</p><blockquote><p>CPU 如何从线程 A 切换到线程 B，并让 B 从上次暂停的位置继续执行？</p></blockquote><p>线程上下文通常包括：</p><ul><li>通用寄存器；</li><li>程序计数器 PC；</li><li>用户栈指针；</li><li>内核栈指针；</li><li>CPU 状态寄存器；</li><li>页表相关寄存器；</li><li>其他体系结构相关状态。</li></ul><p>以一对一线程模型为例，线程是内核调度对象，因此线程切换通常需要进入内核。</p><p>整体流程可以分为三步：</p><ol><li>进入内核态并保存当前线程上下文；</li><li>切换页表和内核栈；</li><li>恢复目标线程上下文并返回用户态。</li></ol><hr><h3 id="13-1-第一步：进入内核态并保存上下文"><a href="#13-1-第一步：进入内核态并保存上下文" class="headerlink" title="13.1 第一步：进入内核态并保存上下文"></a>13.1 第一步：进入内核态并保存上下文</h3><p>线程 A 正在用户态执行时，可能因为以下原因进入内核：</p><ul><li>时钟中断；</li><li>系统调用；</li><li>异常；</li><li>缺页；</li><li>I&#x2F;O 事件。</li></ul><p>进入内核后，硬件通常会自动保存一部分状态，例如：</p><ul><li>用户态 PC；</li><li>CPU 状态；</li><li>用户栈与内核栈切换相关信息。</li></ul><p>然后，内核软件继续保存通用寄存器等状态到线程 A 的内核栈或 TCB 中。</p><hr><h3 id="13-2-第二步：切换页表和内核栈"><a href="#13-2-第二步：切换页表和内核栈" class="headerlink" title="13.2 第二步：切换页表和内核栈"></a>13.2 第二步：切换页表和内核栈</h3><p>调度器选择线程 B 后，内核需要切换到 B 的运行环境。</p><p>这一步包括：</p><ul><li>切换地址空间或页表；</li><li>找到线程 B 的 TCB；</li><li>找到线程 B 的内核栈；</li><li>修改内核栈指针，切换到 B 的内核栈。</li></ul><p>这里有一个非常重要的理解：</p><blockquote><p>切换内核栈可以看作线程切换的分界点。</p></blockquote><p>原因是：切换内核栈之后，接下来恢复上下文时，恢复代码会从目标线程 B 的内核栈中取出 B 之前保存的上下文。也就是说，从此刻开始，内核虽然还在执行同一段切换代码，但它已经站在 B 的执行现场上继续恢复了。</p><hr><h3 id="13-3-第三步：恢复上下文并返回用户态"><a href="#13-3-第三步：恢复上下文并返回用户态" class="headerlink" title="13.3 第三步：恢复上下文并返回用户态"></a>13.3 第三步：恢复上下文并返回用户态</h3><p>内核从线程 B 的保存区域中恢复：</p><ul><li>通用寄存器；</li><li>用户栈指针；</li><li>程序计数器；</li><li>CPU 状态；</li><li>其他必要状态。</li></ul><p>最后执行返回用户态的指令。此时 CPU 回到线程 B 的用户态执行位置，B 像是从上次被暂停的地方继续运行。</p><p>可以用一条链路表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">线程 A 用户栈</span><br><span class="line">  ↓</span><br><span class="line">线程 A 内核栈</span><br><span class="line">  ↓</span><br><span class="line">线程 B 内核栈</span><br><span class="line">  ↓</span><br><span class="line">线程 B 用户栈</span><br></pre></td></tr></table></figure><p>这说明线程上下文切换不仅是“保存和恢复寄存器”，还涉及权限级别、栈、页表、调度实体等多个层面的切换。</p><hr><h2 id="14-一对一线程模型的局限"><a href="#14-一对一线程模型的局限" class="headerlink" title="14. 一对一线程模型的局限"></a>14. 一对一线程模型的局限</h2><p>一对一线程模型是主流系统常用模型，但它并不完美。</p><p>它的主要问题有两个。</p><p>第一，内核调度器不了解应用内部逻辑。<br>例如生产者-消费者模型中，生产者刚生产完数据后，消费者最好马上运行。但内核调度器未必知道这种应用级关系，因此不一定做出最符合业务逻辑的调度选择。</p><p>第二，短命线程开销太大。<br>如果一个 Web 请求只需要亚毫秒级时间，但每个请求都创建一个内核线程，那么创建、销毁和上下文切换的成本可能超过任务本身。</p><p>因此，需要更轻量、更接近用户态逻辑的执行流，这就引出了纤程和协程。</p><hr><h2 id="15-纤程：用户态管理的轻量执行流"><a href="#15-纤程：用户态管理的轻量执行流" class="headerlink" title="15. 纤程：用户态管理的轻量执行流"></a>15. 纤程：用户态管理的轻量执行流</h2><p>纤程可以理解为：</p><blockquote><p>用户态管理的轻量级线程。</p></blockquote><p>它不单独对应一个内核线程。一个内核线程上可以运行多个纤程。</p><p>内核眼中可能只有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">一个内核线程 K</span><br></pre></td></tr></table></figure><p>但用户态运行时眼中有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">纤程 f1、f2、f3、f4</span><br></pre></td></tr></table></figure><p>纤程的优点包括：</p><ul><li>不需要创建新的内核线程；</li><li>创建开销小；</li><li>切换不需要进入内核；</li><li>上下文切换快；</li><li>用户态可以自主调度；</li><li>更适合短任务和高并发场景。</li></ul><p>但它也有限制：</p><ul><li>内核不能直接调度每个纤程；</li><li>如果底层内核线程阻塞，其上的纤程可能受到影响；</li><li>是否能利用多核，取决于运行时如何映射到底层内核线程。</li></ul><p>可以对比如下：</p><table><thead><tr><th>对象</th><th>谁管理</th><th>切换是否进内核</th><th>能否被内核直接调度</th></tr></thead><tbody><tr><td>内核线程</td><td>操作系统</td><td>通常需要</td><td>可以</td></tr><tr><td>纤程</td><td>用户态库 &#x2F; 运行时</td><td>通常不需要</td><td>不可以</td></tr><tr><td>协程</td><td>编程语言运行时</td><td>通常不需要</td><td>不可以直接调度</td></tr></tbody></table><hr><h2 id="16-ucontext：用户态上下文切换"><a href="#16-ucontext：用户态上下文切换" class="headerlink" title="16. ucontext：用户态上下文切换"></a>16. ucontext：用户态上下文切换</h2><p>Linux 中曾提供 <code>ucontext</code> 相关接口，用于保存和切换用户态上下文。常见接口包括：</p><table><thead><tr><th>接口</th><th>作用</th></tr></thead><tbody><tr><td><code>getcontext</code></td><td>保存当前上下文</td></tr><tr><td><code>setcontext</code></td><td>切换到指定上下文</td></tr><tr><td><code>makecontext</code></td><td>创建新的上下文</td></tr></tbody></table><p>每个 <code>ucontext</code> 可以看成一个用户态执行流的上下文。</p><p>经典例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">0</span>;</span><br><span class="line"><span class="type">ucontext_t</span> context, *cp = &amp;context;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">func</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    x++;</span><br><span class="line">    setcontext(cp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    getcontext(cp);</span><br><span class="line">    <span class="keyword">if</span> (!x) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;getcontext has been called\n&quot;</span>);</span><br><span class="line">        func();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;setcontext has been called\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">getcontext has been called</span><br><span class="line">setcontext has been called</span><br></pre></td></tr></table></figure><p>原因如下：</p><ol><li>第一次执行 <code>getcontext(cp)</code>，保存当前上下文。</li><li>此时 <code>x = 0</code>，进入 <code>if</code>，打印第一句。</li><li>调用 <code>func()</code>，执行 <code>x++</code>，使 <code>x = 1</code>。</li><li><code>setcontext(cp)</code> 回到之前保存的上下文。</li><li>程序像是再次从 <code>getcontext(cp)</code> 返回。</li><li>但此时 <code>x = 1</code>，所以进入 <code>else</code>，打印第二句。</li></ol><p>这个例子说明：</p><blockquote><p>用户态上下文切换可以让程序跳回一个先前保存的执行现场，但全局状态的变化仍然保留。</p></blockquote><hr><h2 id="17-协程：语言级轻量执行流"><a href="#17-协程：语言级轻量执行流" class="headerlink" title="17. 协程：语言级轻量执行流"></a>17. 协程：语言级轻量执行流</h2><p>协程和纤程非常接近，但使用语境略有不同：</p><ul><li>纤程偏操作系统或运行时概念；</li><li>协程偏编程语言概念。</li></ul><p>协程通常由语言运行时管理，常见于 Go、Python、Lua、C++20 等语言或标准中。</p><p>协程常见状态包括：</p><ul><li>新生；</li><li>运行；</li><li>暂停；</li><li>终止。</li></ul><p>协程的核心操作是：</p><table><thead><tr><th>操作</th><th>含义</th></tr></thead><tbody><tr><td><code>yield</code></td><td>主动暂停，把控制权交出去</td></tr><tr><td><code>resume</code></td><td>恢复执行，从暂停位置继续</td></tr></tbody></table><p>协程的优势在于：</p><ul><li>创建轻量；</li><li>切换轻量；</li><li>适合大量并发任务；</li><li>可以由语言运行时根据应用逻辑调度；</li><li>不必每个任务都对应一个内核线程。</li></ul><hr><h2 id="18-Go-协程：goroutine"><a href="#18-Go-协程：goroutine" class="headerlink" title="18. Go 协程：goroutine"></a>18. Go 协程：goroutine</h2><p>Go 语言使用 <code>go</code> 关键字启动 goroutine：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">go</span> asyncTask()</span><br></pre></td></tr></table></figure><p>这表示启动一个轻量执行流，由 Go 运行时管理。</p><p>典型网络服务器例子如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">handler</span><span class="params">(c net.Conn)</span></span> &#123;</span><br><span class="line">    c.Write([]<span class="type">byte</span>(<span class="string">&quot;ok&quot;</span>))</span><br><span class="line">    c.Close()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    l, _ := net.Listen(<span class="string">&quot;tcp&quot;</span>, <span class="string">&quot;:8000&quot;</span>)</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        c, _ := l.Accept()</span><br><span class="line">        <span class="keyword">go</span> handler(c)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>每来一个连接，就启动一个 goroutine 处理。</p><p>如果使用传统内核线程，每个请求一个线程，开销可能很大。goroutine 则可以由 Go 运行时把大量轻量任务映射到较少的内核线程上，适合高并发网络服务。</p><p>因此，goroutine 可以看作一种语言运行时管理的轻量执行流。它不是简单等同于内核线程，而是由 Go runtime 调度和管理。</p><hr><h2 id="19-核心概念总表"><a href="#19-核心概念总表" class="headerlink" title="19. 核心概念总表"></a>19. 核心概念总表</h2><table><thead><tr><th>概念</th><th>核心含义</th><th>谁管理</th><th>是否共享地址空间</th><th>开销</th></tr></thead><tbody><tr><td>进程</td><td>资源分配单位</td><td>内核</td><td>进程间通常不共享</td><td>最大</td></tr><tr><td>线程</td><td>进程内执行流</td><td>内核 &#x2F; 线程库</td><td>同进程线程共享</td><td>中等</td></tr><tr><td>用户态线程</td><td>用户态管理的执行流</td><td>用户态库 &#x2F; 运行时</td><td>通常共享</td><td>小</td></tr><tr><td>内核态线程</td><td>内核调度实体</td><td>内核</td><td>取决于所属进程</td><td>中等</td></tr><tr><td>纤程</td><td>用户态轻量线程</td><td>用户态库 &#x2F; 运行时</td><td>通常共享</td><td>小</td></tr><tr><td>协程</td><td>语言级轻量执行流</td><td>语言运行时</td><td>通常共享</td><td>小</td></tr><tr><td>TCB</td><td>线程控制块</td><td>内核 &#x2F; 线程库</td><td>不适用</td><td>用于管理线程</td></tr><tr><td>TLS</td><td>线程本地存储</td><td>运行时 &#x2F; 编译器 &#x2F; 系统支持</td><td>每线程一份变量实例</td><td>用于隔离线程变量</td></tr></tbody></table><hr><h2 id="20-本讲最重要的结论"><a href="#20-本讲最重要的结论" class="headerlink" title="20. 本讲最重要的结论"></a>20. 本讲最重要的结论</h2><p>本讲可以压缩成以下几句话：</p><ol><li>进程是资源分配单位，线程是 CPU 执行和调度单位。</li><li>线程比进程轻，因为同一进程内线程共享代码、数据、堆和地址空间。</li><li>多线程共享地址空间，但不代表所有变量都共享。</li><li>每个线程有自己的栈、寄存器上下文和 TCB。</li><li>TLS 让某些看似全局的变量变成每个线程一份。</li><li>用户态线程和内核态线程的区别在于谁管理、谁调度、内核是否看得见。</li><li>线程模型中的“对”表示用户态线程和内核态线程之间的映射关系。</li><li>多对一切换快但难以利用多核；一对一能利用多核但内核开销大；多对多理论灵活但实现复杂。</li><li>pthread 的 <code>create</code> 分出执行流，<code>join</code> 等待执行流结束。</li><li>线程上下文切换的关键步骤是保存当前上下文、切换内核栈和页表、恢复目标上下文。</li><li>纤程和协程通过用户态调度降低切换成本，适合大量短任务和高并发场景。</li></ol><hr><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>“进程与线程 II”的中心思想是执行流的层层轻量化。</p><p>最初，操作系统用进程管理程序运行。但进程太重，于是引入线程：线程共享进程资源，只保留独立执行所需的最小运行状态。随后，内核线程虽然能利用多核，但创建和切换仍有成本，于是用户态线程、纤程和协程继续把调度权上移到用户态或语言运行时。</p><p>这条主线可以总结为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">进程：拥有资源</span><br><span class="line">线程：共享资源并执行代码</span><br><span class="line">用户态线程 / 纤程 / 协程：更轻量地组织大量执行流</span><br></pre></td></tr></table></figure><p>真正理解本讲的关键，不是背下每个名词，而是抓住三个层次：</p><ol><li><strong>资源层次</strong>：进程提供地址空间、代码、数据、堆和文件等资源。</li><li><strong>执行层次</strong>：线程在进程资源之上独立执行，拥有自己的栈和上下文。</li><li><strong>调度层次</strong>：内核线程由操作系统调度，用户态线程、纤程和协程由用户态运行时调度。</li></ol><p>把这三个层次区分清楚，用户态线程、内核态线程、三种线程模型、TCB、TLS、pthread、上下文切换、纤程和协程就能连成一套完整机制。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第9讲 进程与线程I</title>
      <link href="/2026/06/11/%E7%AC%AC9%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E4%B8%8E%E7%BA%BF%E7%A8%8BI/"/>
      <url>/2026/06/11/%E7%AC%AC9%E8%AE%B2-%E8%BF%9B%E7%A8%8B%E4%B8%8E%E7%BA%BF%E7%A8%8BI/</url>
      
        <content type="html"><![CDATA[<h1 id="进程与线程-I：从进程抽象到-fork-exec-的完整理解"><a href="#进程与线程-I：从进程抽象到-fork-exec-的完整理解" class="headerlink" title="进程与线程 I：从进程抽象到 fork &#x2F; exec 的完整理解"></a>进程与线程 I：从进程抽象到 fork &#x2F; exec 的完整理解</h1><p>本文根据上传的《第9讲 进程与线程I.md》整理而成，目标是把原始对话中的讲解、追问和修正重构为一篇可独立复习的学习文档。 输出方式遵循对话转正式文章的规则要求。</p><hr><h2 id="引言：本讲真正要解决的问题"><a href="#引言：本讲真正要解决的问题" class="headerlink" title="引言：本讲真正要解决的问题"></a>引言：本讲真正要解决的问题</h2><p>“进程与线程 I”这一讲虽然标题同时包含进程和线程，但核心内容首先是<strong>进程</strong>。进程是操作系统管理程序运行的基本抽象。理解进程之后，才能继续理解线程、调度、同步、上下文切换等后续内容。</p><p>本讲的主线可以概括为：</p><blockquote><p>操作系统把“正在运行的程序”抽象成进程，再通过 PCB 记录它，通过状态机管理它，通过上下文切换暂停和恢复它，并通过 <code>fork</code>、<code>exec</code>、<code>wait</code>、<code>exit</code> 等系统调用完成进程的创建、替换、等待和退出。</p></blockquote><p>换言之，本讲不是单纯记几个概念，而是要理解操作系统如何把一个静态程序变成一个可管理、可调度、可暂停、可恢复、可终止的运行实体。</p><hr><h2 id="1-为什么需要进程"><a href="#1-为什么需要进程" class="headerlink" title="1. 为什么需要进程"></a>1. 为什么需要进程</h2><p>早期计算机一次只运行一个任务。如果程序需要等待键盘输入、磁盘读取或网络响应，CPU 就可能空闲。为了提高 CPU 利用率，操作系统引入了多道程序和分时机制。</p><p>一个任务等待 I&#x2F;O 时，操作系统可以切换到另一个任务继续执行。这样，CPU 不必陪着某个程序一起“干等”。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[进程 A 运行] --&gt; B[进程 A 等待 I&#x2F;O]    B --&gt; C[操作系统切换到进程 B]    C --&gt; D[进程 B 运行]    D --&gt; E[进程 A 等待事件完成]    E --&gt; F[进程 A 重新进入就绪队列]  </pre></div><p>这张图说明：进程的存在不是为了给“程序”换一个名字，而是为了让操作系统可以统一管理多个正在运行的任务。</p><hr><h2 id="2-程序与进程的区别"><a href="#2-程序与进程的区别" class="headerlink" title="2. 程序与进程的区别"></a>2. 程序与进程的区别</h2><p>最常见的说法是：</p><blockquote><p>进程是运行中的程序。</p></blockquote><p>这句话正确，但不够完整。更准确地说：</p><table><thead><tr><th>概念</th><th>特征</th><th>例子</th></tr></thead><tbody><tr><td>程序</td><td>静态文件，由代码和数据组成</td><td><code>hello.c</code>、<code>a.out</code>、<code>/bin/ls</code></td></tr><tr><td>进程</td><td>程序运行起来后的动态实体</td><td>执行中的 <code>./a.out</code></td></tr><tr><td>PCB</td><td>操作系统管理进程的数据结构</td><td>记录 pid、状态、上下文、地址空间等</td></tr></tbody></table><p>程序是静态的，进程是动态的。程序放在磁盘上时，只是一组代码和数据；当它被加载、分配地址空间、初始化栈、开始执行后，才成为进程。</p><p>进程通常包含两类内容：</p><table><thead><tr><th>部分</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td>静态部分</td><td>程序运行所需的代码和初始数据</td><td>代码段、数据段、只读数据</td></tr><tr><td>动态部分</td><td>程序运行过程中变化的状态</td><td>PC、寄存器、栈、堆、打开文件、运行状态</td></tr></tbody></table><p>因此，不能简单说“进程就是程序”。程序更像菜谱，进程更像正在做菜的过程。</p><hr><h2 id="3-进程状态：从创建到终止"><a href="#3-进程状态：从创建到终止" class="headerlink" title="3. 进程状态：从创建到终止"></a>3. 进程状态：从创建到终止</h2><p>操作系统通常用若干状态描述进程当前所处的位置。基本状态包括：</p><ol><li>新生态 <code>new</code></li><li>就绪态 <code>ready</code></li><li>运行态 <code>running</code></li><li>阻塞态 <code>blocked</code></li><li>终止态 <code>terminated</code></li></ol><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    New[新生态 new] --&gt; Ready[就绪态 ready]    Ready --&gt; Running[运行态 running]    Running --&gt; Ready    Running --&gt; Blocked[阻塞态 blocked]    Blocked --&gt; Ready    Running --&gt; Terminated[终止态 terminated]  </pre></div><p>各状态的含义如下：</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td>新生态</td><td>进程刚被创建，操作系统正在初始化它</td></tr><tr><td>就绪态</td><td>进程已经准备好运行，但暂时没有获得 CPU</td></tr><tr><td>运行态</td><td>进程正在 CPU 上执行</td></tr><tr><td>阻塞态</td><td>进程在等待某个事件，暂时无法继续执行</td></tr><tr><td>终止态</td><td>进程执行结束，等待资源回收</td></tr></tbody></table><p>最重要的易错点是：</p><blockquote><p>阻塞态不能直接进入运行态。</p></blockquote><p>一个进程如果因为等待输入、磁盘、网络或其他事件进入阻塞态，那么事件完成后，它只能先回到就绪态。之后是否运行，还要看调度器是否选择它。</p><p>所以正确路径是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">blocked -&gt; ready -&gt; running</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">blocked -&gt; running</span><br></pre></td></tr></table></figure><hr><h2 id="4-PCB：操作系统如何记住一个进程"><a href="#4-PCB：操作系统如何记住一个进程" class="headerlink" title="4. PCB：操作系统如何记住一个进程"></a>4. PCB：操作系统如何记住一个进程</h2><p>PCB 是 Process Control Block，即进程控制块。可以把它理解为操作系统为每个进程建立的“档案袋”。</p><p>系统中不是只有一个 PCB，而是通常每个进程都有自己的 PCB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">进程 A -&gt; PCB_A</span><br><span class="line">进程 B -&gt; PCB_B</span><br><span class="line">进程 C -&gt; PCB_C</span><br></pre></td></tr></table></figure><p>PCB 中常见的信息包括：</p><table><thead><tr><th>信息</th><th>作用</th></tr></thead><tbody><tr><td><code>pid</code></td><td>标识这是哪个进程</td></tr><tr><td><code>state</code></td><td>记录进程当前处于新生、就绪、运行、阻塞还是终止</td></tr><tr><td><code>context</code></td><td>保存进程被切换时的 CPU 状态</td></tr><tr><td>地址空间信息</td><td>记录进程的虚拟地址空间、页表等</td></tr><tr><td>内核栈</td><td>进程进入内核态时使用的栈</td></tr><tr><td>打开文件信息</td><td>记录进程打开了哪些文件</td></tr><tr><td>父子关系</td><td>记录父进程、子进程等关系</td></tr><tr><td>调度信息</td><td>记录优先级、运行时间等调度相关数据</td></tr></tbody></table><p>一个考试版答案可以写成：</p><blockquote><p>PCB 至少保存进程标识符、进程状态、上下文、地址空间、内核栈、打开文件等资源信息。</p></blockquote><p>其中最关键的是 <code>context</code>，因为上下文切换依赖它。</p><hr><h2 id="5-上下文切换：谁在保存，谁在恢复"><a href="#5-上下文切换：谁在保存，谁在恢复" class="headerlink" title="5. 上下文切换：谁在保存，谁在恢复"></a>5. 上下文切换：谁在保存，谁在恢复</h2><p>上下文 <code>context</code> 指的是进程运行时 CPU 和相关硬件所处的状态，包括：</p><ul><li>通用寄存器</li><li>程序计数器 PC</li><li>栈指针</li><li>页表基地址寄存器</li><li>CPU 状态寄存器</li><li>其他与执行恢复有关的硬件状态</li></ul><p>所谓上下文切换，就是 CPU 从进程 A 切换到进程 B 的过程。更准确地说，这个过程的主语不是进程 A 或进程 B，而是：</p><blockquote><p>操作系统内核，尤其是调度器和上下文切换代码；部分动作由硬件辅助完成。</p></blockquote><p>完整过程可以写成：</p><ol><li>进程 A 因为时钟中断、系统调用或异常进入内核态。</li><li>内核保存进程 A 的 CPU 状态到 <code>PCB_A</code>。</li><li>调度器选择下一个要运行的进程 B。</li><li>内核切换到进程 B 的地址空间和内核栈。</li><li>内核从 <code>PCB_B</code> 中恢复进程 B 的上下文。</li><li>CPU 返回用户态，进程 B 从上次暂停的位置继续执行。</li></ol><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant A as 进程 A    participant Kernel as 操作系统内核    participant PCB_A as PCB_A    participant PCB_B as PCB_B    participant B as 进程 B    A-&gt;&gt;Kernel: 中断 &#x2F; 系统调用 &#x2F; 异常    Kernel-&gt;&gt;PCB_A: 保存 A 的上下文    Kernel-&gt;&gt;Kernel: 调度器选择 B    Kernel-&gt;&gt;PCB_B: 读取 B 的上下文    Kernel-&gt;&gt;B: 恢复 B 并返回用户态  </pre></div><p>这说明，上下文切换不是进程主动“自己保存自己”，而是进入内核后由操作系统接管。PCB 之所以必须保存上下文，是因为操作系统需要依靠它暂停和恢复进程。</p><hr><h2 id="6-fork：一次调用，两次返回"><a href="#6-fork：一次调用，两次返回" class="headerlink" title="6. fork：一次调用，两次返回"></a>6. fork：一次调用，两次返回</h2><p><code>fork()</code> 是 Unix &#x2F; Linux 中创建进程的经典接口。它的作用是：</p><blockquote><p>为当前进程创建一个几乎一样的新进程。</p></blockquote><p>调用 <code>fork()</code> 的进程叫父进程，新创建出来的进程叫子进程。</p><p>一个最小例子如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">42</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> rc = fork();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;rc = %d, x = %d\n&quot;</span>, rc, x);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>fork()</code> 之前，系统中只有一个进程。执行 <code>fork()</code> 之后，进程变成两个：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">父进程 P</span><br><span class="line">子进程 C</span><br></pre></td></tr></table></figure><p>它们都会从 <code>fork()</code> 返回之后的位置继续执行。最特殊的是，<code>fork()</code> 在两个进程中的返回值不同：</p><table><thead><tr><th>情况</th><th><code>fork()</code> 返回值</th></tr></thead><tbody><tr><td>父进程中</td><td>返回子进程的 pid，是一个正数</td></tr><tr><td>子进程中</td><td>返回 0</td></tr><tr><td>创建失败</td><td>返回负数</td></tr></tbody></table><p>因此，<code>fork()</code> 的核心特征是：</p><blockquote><p>一次调用，两次返回。</p></blockquote><p>也正因为返回值不同，程序才能区分自己当前处于父进程还是子进程中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> rc = fork();</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (rc == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 子进程</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (rc &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 父进程</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// fork 失败</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="7-fork-后父子进程的关系"><a href="#7-fork-后父子进程的关系" class="headerlink" title="7. fork 后父子进程的关系"></a>7. fork 后父子进程的关系</h2><p><code>fork()</code> 后，父进程和子进程有几个关键关系。</p><p>第一，父子进程拥有不同的 pid。它们是两个不同的进程。</p><p>第二，父子进程从 <code>fork()</code> 之后继续执行。子进程不是从 <code>main()</code> 开头重新执行，而是从 <code>fork()</code> 返回的位置继续往下走。</p><p>第三，父子进程看到的 <code>fork()</code> 返回值不同。父进程得到子进程 pid，子进程得到 0。这是判断父子分支的核心依据。</p><p>第四，普通用户态变量通常相互独立。父进程和子进程各自拥有独立的虚拟地址空间。子进程修改自己的普通变量，不会直接修改父进程的变量。</p><p>第五，某些内核对象可能共享。例如，父子进程可能共享同一个打开文件结构，因此文件偏移量也可能共享。</p><p>这解释了为什么父子进程共同读取同一个文件时，不一定会读到完全相同的内容。如果父进程先读走前 10 个字符，文件偏移量已经后移，子进程再读就可能读到后 10 个字符。反过来，如果子进程先读，也是同理。输出顺序不确定，但偏移量共享这一点非常重要。</p><hr><h2 id="8-fork-题的计算方法"><a href="#8-fork-题的计算方法" class="headerlink" title="8. fork 题的计算方法"></a>8. fork 题的计算方法</h2><p>考虑代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    fork();</span><br><span class="line">    fork();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;hello\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>分析进程数量时，可以把每次 <code>fork()</code> 理解为当前所有进程各自复制一份。</p><p>初始时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1 个进程</span><br></pre></td></tr></table></figure><p>第一次 <code>fork()</code> 后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1 -&gt; 2 个进程</span><br></pre></td></tr></table></figure><p>第二次 <code>fork()</code> 后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2 -&gt; 4 个进程</span><br></pre></td></tr></table></figure><p>因此，最终有 4 个进程执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;hello\n&quot;</span>);</span><br></pre></td></tr></table></figure><p>所以 <code>hello</code> 打印 4 次。</p><p>需要注意：打印顺序不固定。因为多个进程的调度顺序由操作系统决定。</p><hr><h2 id="9-exec：不是创建新进程，而是替换当前进程"><a href="#9-exec：不是创建新进程，而是替换当前进程" class="headerlink" title="9. exec：不是创建新进程，而是替换当前进程"></a>9. exec：不是创建新进程，而是替换当前进程</h2><p><code>exec()</code> 和 <code>fork()</code> 经常一起出现，但二者作用完全不同。</p><table><thead><tr><th>操作</th><th>作用</th><th>是否创建新进程</th><th>是否替换当前程序内容</th></tr></thead><tbody><tr><td><code>fork()</code></td><td>创建子进程</td><td>是</td><td>否</td></tr><tr><td><code>exec()</code></td><td>在当前进程中加载另一个程序</td><td>否</td><td>是</td></tr><tr><td><code>wait()</code></td><td>等待子进程结束</td><td>否</td><td>否</td></tr><tr><td><code>exit()</code></td><td>当前进程退出</td><td>否</td><td>否</td></tr></tbody></table><p><code>exec()</code> 的准确含义是：</p><blockquote><p>在当前进程中加载一个新的可执行文件，用新程序的代码、数据和地址空间替换原来的程序映像。</p></blockquote><p>它不会创建新进程。执行 <code>exec()</code> 前后，进程的 pid 通常不变，PCB 仍然代表同一个进程。变化的是这个进程里面运行的程序内容。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before exec\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">execve(<span class="string">&quot;/bin/ls&quot;</span>, argv, envp);</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;after exec\n&quot;</span>);</span><br></pre></td></tr></table></figure><p>如果 <code>execve()</code> 成功，<code>after exec</code> 通常不会被打印。因为当前进程的代码段已经被 <code>/bin/ls</code> 的代码替换，原程序中 <code>execve()</code> 后面的语句已经不再存在于当前进程的地址空间中。</p><p>只有当 <code>execve()</code> 失败时，才会继续执行后面的代码。因此常见写法是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">execve(<span class="string">&quot;/bin/ls&quot;</span>, argv, envp);</span><br><span class="line">perror(<span class="string">&quot;execve failed&quot;</span>);</span><br></pre></td></tr></table></figure><p>这里的意思是：如果执行到了 <code>perror</code>，说明 <code>execve</code> 失败了。</p><hr><h2 id="10-为什么-shell-通常是-fork-后再-exec"><a href="#10-为什么-shell-通常是-fork-后再-exec" class="headerlink" title="10. 为什么 shell 通常是 fork 后再 exec"></a>10. 为什么 shell 通常是 fork 后再 exec</h2><p>在 shell 中输入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span></span><br></pre></td></tr></table></figure><p>用户真正希望的是：</p><ol><li>shell 仍然存在。</li><li>新启动一个程序去执行 <code>ls</code>。</li><li><code>ls</code> 执行完后，shell 继续等待下一条命令。</li></ol><p>如果 shell 直接调用 <code>exec(&quot;ls&quot;)</code>，那么 shell 自己就会被 <code>ls</code> 替换掉。<code>ls</code> 执行完后，shell 也就不存在了。这显然不符合交互式 shell 的需求。</p><p>因此，shell 通常采用如下流程：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[Shell 进程] --&gt; B[fork 创建子进程]    B --&gt; C[父进程仍是 Shell]    B --&gt; D[子进程是 Shell 的复制品]    D --&gt; E[子进程 exec 为 ls]    C --&gt; F[父进程 wait 等待子进程结束]    E --&gt; G[ls 执行结束]    G --&gt; H[Shell 继续读取下一条命令]  </pre></div><p>对应代码结构大致如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> rc = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (rc == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">char</span> *argv[] = &#123;<span class="string">&quot;ls&quot;</span>, <span class="string">&quot;-l&quot;</span>, <span class="literal">NULL</span>&#125;;</span><br><span class="line">        execve(<span class="string">&quot;/bin/ls&quot;</span>, argv, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// exec 成功后不会执行到这里</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;exec failed\n&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        wait(<span class="literal">NULL</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;child finished\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行过程是：</p><ol><li>开始只有 shell 进程。</li><li>shell 调用 <code>fork()</code> 创建子进程。</li><li>父进程仍然是 shell。</li><li>子进程调用 <code>execve()</code>，把自己替换成 <code>/bin/ls</code>。</li><li>父进程调用 <code>wait()</code> 等待子进程结束。</li><li>子进程执行完 <code>ls</code> 后退出。</li><li>父进程 shell 继续接收下一条命令。</li></ol><p>这里需要精确区分：</p><blockquote><p>shell 的父进程 <code>wait</code> 等的是子进程执行完毕；子进程结束后，shell 才继续读取用户的下一条命令。</p></blockquote><hr><h2 id="11-wait-与-exit：进程结束如何被回收"><a href="#11-wait-与-exit：进程结束如何被回收" class="headerlink" title="11. wait 与 exit：进程结束如何被回收"></a>11. wait 与 exit：进程结束如何被回收</h2><p><code>wait()</code> 用于父进程等待子进程结束，并获取子进程的退出状态。它还有一个重要作用：回收子进程结束后遗留的部分资源。</p><p>如果子进程已经结束，但父进程还没有调用 <code>wait()</code> 或相关接口获取其退出状态，那么子进程可能暂时处于僵尸状态。僵尸进程不是还在运行，而是进程已经结束，但仍保留少量信息等待父进程回收。</p><p><code>exit()</code> 用于正常退出当前进程。<code>abort()</code> 更偏向异常终止。对本讲而言，只需要把握：</p><ul><li><code>exit()</code>：当前进程正常退出。</li><li><code>abort()</code>：当前进程异常终止。</li><li><code>wait()</code>：父进程等待并回收子进程退出信息。</li></ul><hr><h2 id="12-本讲核心结构"><a href="#12-本讲核心结构" class="headerlink" title="12. 本讲核心结构"></a>12. 本讲核心结构</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[程序] --&gt; B[加载并运行]    B --&gt; C[进程]    C --&gt; D[PCB]    C --&gt; E[进程状态]    C --&gt; F[上下文切换]    C --&gt; G[系统调用]    E --&gt; E1[new]    E --&gt; E2[ready]    E --&gt; E3[running]    E --&gt; E4[blocked]    E --&gt; E5[terminated]    G --&gt; G1[fork 创建子进程]    G --&gt; G2[exec 替换程序映像]    G --&gt; G3[wait 等待子进程]    G --&gt; G4[exit 退出进程]  </pre></div><p>本讲应掌握的主干知识包括：</p><ol><li>进程不是静态程序，而是程序运行时的抽象。</li><li>每个进程通常有自己的 PCB。</li><li>PCB 保存 pid、状态、上下文、地址空间、内核栈、打开文件等信息。</li><li>上下文切换由操作系统内核主导，核心是保存旧进程上下文并恢复新进程上下文。</li><li>阻塞态不能直接进入运行态，必须先回到就绪态。</li><li><code>fork()</code> 的核心是一次调用、两次返回。</li><li>父进程中 <code>fork()</code> 返回子进程 pid，子进程中返回 0。</li><li><code>exec()</code> 不创建新进程，而是在当前进程中替换程序映像。</li><li>shell 通常先 <code>fork()</code>，再让子进程 <code>exec()</code>，父进程 <code>wait()</code>。</li><li><code>wait()</code> 用于等待并回收子进程退出信息。</li></ol><hr><h2 id="13-易错点整理"><a href="#13-易错点整理" class="headerlink" title="13. 易错点整理"></a>13. 易错点整理</h2><table><thead><tr><th>易错说法</th><th>修正</th></tr></thead><tbody><tr><td>进程就是程序</td><td>进程是程序运行起来后的动态抽象</td></tr><tr><td>系统中只有一个 PCB</td><td>通常每个进程都有自己的 PCB</td></tr><tr><td>进程自己保存上下文</td><td>上下文切换主要由操作系统内核完成，硬件辅助</td></tr><tr><td>blocked 可以直接 running</td><td>blocked 必须先回到 ready，再由调度器选择进入 running</td></tr><tr><td><code>fork()</code> 后子进程从 <code>main()</code> 开始执行</td><td>子进程从 <code>fork()</code> 返回的位置继续执行</td></tr><tr><td><code>fork()</code> 在父子进程中返回值相同</td><td>父进程返回子进程 pid，子进程返回 0</td></tr><tr><td><code>exec()</code> 会创建新进程</td><td><code>exec()</code> 不创建新进程，只替换当前进程的程序映像</td></tr><tr><td>shell 执行命令时直接 <code>exec</code> 就行</td><td>直接 <code>exec</code> 会让 shell 被命令程序替换，所以通常要先 <code>fork</code></td></tr></tbody></table><hr><h2 id="14-自测题答案修正版"><a href="#14-自测题答案修正版" class="headerlink" title="14. 自测题答案修正版"></a>14. 自测题答案修正版</h2><h3 id="题-1：为什么不能说“进程就是程序”？"><a href="#题-1：为什么不能说“进程就是程序”？" class="headerlink" title="题 1：为什么不能说“进程就是程序”？"></a>题 1：为什么不能说“进程就是程序”？</h3><p>程序是静态的代码和数据，进程是程序运行起来之后由操作系统管理的动态实体。进程不仅包含代码和数据，还包含运行状态、地址空间、寄存器上下文、栈、堆、打开文件等信息。</p><h3 id="题-2：哪个状态转换不可能直接发生？"><a href="#题-2：哪个状态转换不可能直接发生？" class="headerlink" title="题 2：哪个状态转换不可能直接发生？"></a>题 2：哪个状态转换不可能直接发生？</h3><p>答案是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">blocked -&gt; running</span><br></pre></td></tr></table></figure><p>原因是阻塞态进程等待的事件完成后，只能先进入就绪态；之后要等调度器选择，才能进入运行态。</p><h3 id="题-3：PCB-至少需要保存哪些信息？为什么上下文要放在-PCB-里？"><a href="#题-3：PCB-至少需要保存哪些信息？为什么上下文要放在-PCB-里？" class="headerlink" title="题 3：PCB 至少需要保存哪些信息？为什么上下文要放在 PCB 里？"></a>题 3：PCB 至少需要保存哪些信息？为什么上下文要放在 PCB 里？</h3><p>PCB 至少需要保存：</p><ul><li>pid</li><li>进程状态</li><li>上下文</li><li>地址空间信息</li><li>内核栈</li><li>打开文件信息</li><li>父子进程关系</li><li>调度相关信息</li></ul><p>上下文要放在 PCB 中，是因为进程被切走时，操作系统需要把它当前的 CPU 状态保存起来；之后恢复该进程时，又要从 PCB 中取出这些状态，让它从上次停止的位置继续执行。</p><h3 id="题-4：fork-后父进程和子进程最关键的区别是什么？"><a href="#题-4：fork-后父进程和子进程最关键的区别是什么？" class="headerlink" title="题 4：fork 后父进程和子进程最关键的区别是什么？"></a>题 4：fork 后父进程和子进程最关键的区别是什么？</h3><p>最关键的是：</p><ul><li>pid 不同；</li><li><code>fork()</code> 返回值不同；</li><li>父进程中返回子进程 pid；</li><li>子进程中返回 0。</li></ul><p>此外，父子进程从 <code>fork()</code> 后继续执行，普通变量通常各自独立，但部分内核对象可能共享，例如打开文件结构。</p><h3 id="题-5：fork-和-exec-的区别是什么？为什么-shell-通常-fork-后再-exec？"><a href="#题-5：fork-和-exec-的区别是什么？为什么-shell-通常-fork-后再-exec？" class="headerlink" title="题 5：fork 和 exec 的区别是什么？为什么 shell 通常 fork 后再 exec？"></a>题 5：fork 和 exec 的区别是什么？为什么 shell 通常 fork 后再 exec？</h3><p><code>fork()</code> 用于创建子进程；<code>exec()</code> 不创建新进程，而是在当前进程中加载另一个程序，替换当前进程的代码、数据和地址空间。</p><p>shell 不希望自己被 <code>ls</code>、<code>gcc</code>、<code>vim</code> 等命令替换掉，所以先 <code>fork()</code> 创建子进程，再让子进程 <code>exec()</code> 目标程序。父进程 shell 可以 <code>wait()</code> 子进程结束，之后继续读取用户输入。</p><h3 id="题-6：下面代码打印几次-hello？"><a href="#题-6：下面代码打印几次-hello？" class="headerlink" title="题 6：下面代码打印几次 hello？"></a>题 6：下面代码打印几次 hello？</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    fork();</span><br><span class="line">    fork();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;hello\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>打印 4 次。</p><p>原因是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">开始：1 个进程</span><br><span class="line">第一次 fork：1 -&gt; 2</span><br><span class="line">第二次 fork：2 -&gt; 4</span><br></pre></td></tr></table></figure><p>最终 4 个进程都会执行 <code>printf(&quot;hello\n&quot;)</code>。</p><hr><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>“进程与线程 I”的关键不是背诵定义，而是建立一条完整链路：</p><blockquote><p>程序被加载后成为进程；进程由 PCB 记录；PCB 保存状态、上下文、地址空间等信息；调度器通过上下文切换在多个进程之间切换 CPU；<code>fork()</code> 创建子进程；<code>exec()</code> 替换当前进程的程序映像；<code>wait()</code> 等待并回收子进程；<code>exit()</code> 终止当前进程。</p></blockquote><p>掌握这一链路后，进程状态、PCB、上下文切换、<code>fork</code>、<code>exec</code> 就不再是孤立概念，而是操作系统管理程序运行的一套连续机制。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第8讲 物理内存管理II</title>
      <link href="/2026/06/11/%E7%AC%AC8%E8%AE%B2-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86II/"/>
      <url>/2026/06/11/%E7%AC%AC8%E8%AE%B2-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86II/</url>
      
        <content type="html"><![CDATA[<h1 id="物理内存管理-II：换页、页替换策略与工作集模型"><a href="#物理内存管理-II：换页、页替换策略与工作集模型" class="headerlink" title="物理内存管理 II：换页、页替换策略与工作集模型"></a>物理内存管理 II：换页、页替换策略与工作集模型</h1><p>《物理内存管理 I》讨论的是：当系统还有可用物理内存时，操作系统如何记录空闲页、分配连续页，并通过伙伴系统与 SLAB&#x2F;SLUB 提高分配效率。</p><p>《物理内存管理 II》进一步讨论的是另一个问题：<strong>当物理内存不够用时，操作系统怎么办。</strong></p><p>这一讲的核心不是“如何从空闲页中分配一个物理页”，而是：</p><ul><li>物理内存不足时，如何继续支撑程序运行；</li><li>页不在物理内存中时，如何通过缺页异常恢复；</li><li>内存满了时，应该换出哪个页；</li><li>如何避免系统陷入频繁换页的颠簸状态；</li><li>工作集模型如何帮助系统判断进程当前真正需要哪些页。</li></ul><p>本讲主线可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">程序拥有虚拟地址空间</span><br><span class="line">↓</span><br><span class="line">真实物理内存有限</span><br><span class="line">↓</span><br><span class="line">OS 不一定让所有虚拟页同时驻留在物理内存</span><br><span class="line">↓</span><br><span class="line">暂时不用的页可以换出到磁盘 Swap</span><br><span class="line">↓</span><br><span class="line">再次访问时触发缺页异常</span><br><span class="line">↓</span><br><span class="line">OS 将页换入物理内存并更新页表</span><br><span class="line">↓</span><br><span class="line">内存满时需要页替换策略</span><br><span class="line">↓</span><br><span class="line">策略不好或内存压力过大，会导致颠簸</span><br><span class="line">↓</span><br><span class="line">工作集模型用于分析进程当前真正需要的页集合</span><br></pre></td></tr></table></figure><hr><h2 id="❗-核心结论：-物理内存管理-II-的核心是“物理内存不够时如何继续运行”：通过换页扩大可用虚拟内存，通过页替换策略选择牺牲页，通过工作集模型避免频繁缺页和颠簸。"><a href="#❗-核心结论：-物理内存管理-II-的核心是“物理内存不够时如何继续运行”：通过换页扩大可用虚拟内存，通过页替换策略选择牺牲页，通过工作集模型避免频繁缺页和颠簸。" class="headerlink" title="❗ 核心结论： 物理内存管理 II 的核心是“物理内存不够时如何继续运行”：通过换页扩大可用虚拟内存，通过页替换策略选择牺牲页，通过工作集模型避免频繁缺页和颠簸。"></a><strong>❗ 核心结论：</strong> 物理内存管理 II 的核心是“物理内存不够时如何继续运行”：通过换页扩大可用虚拟内存，通过页替换策略选择牺牲页，通过工作集模型避免频繁缺页和颠簸。</h2><h2 id="1-物理内存与磁盘：Swap-不是把磁盘变成内存"><a href="#1-物理内存与磁盘：Swap-不是把磁盘变成内存" class="headerlink" title="1. 物理内存与磁盘：Swap 不是把磁盘变成内存"></a>1. 物理内存与磁盘：Swap 不是把磁盘变成内存</h2><p>理解换页前，必须先区分物理内存和磁盘。</p><p>物理内存通常指 RAM，也就是常说的内存条。CPU 执行普通 load&#x2F;store 指令时，最终访问的是物理内存中的数据。</p><p>磁盘包括 SSD、HDD 等外部存储设备。磁盘容量大、断电不丢失，但访问速度远慢于内存，CPU 不能像访问普通内存一样直接用地址访问磁盘内容。</p><table><thead><tr><th>对比项</th><th>物理内存 RAM</th><th>磁盘 &#x2F; SSD &#x2F; HDD</th></tr></thead><tbody><tr><td>系统角色</td><td>主存</td><td>外存 &#x2F; 辅助存储</td></tr><tr><td>访问速度</td><td>快</td><td>慢很多</td></tr><tr><td>CPU 是否能直接用普通内存指令访问</td><td>可以，经 MMU 翻译后访问物理内存</td><td>不可以，需要通过 I&#x2F;O 路径访问</td></tr><tr><td>断电后是否保留</td><td>通常不保留</td><td>保留</td></tr><tr><td>换页中的作用</td><td>存放当前正在使用或可能马上使用的页</td><td>Swap 区临时保存被换出的页内容</td></tr></tbody></table><p>因此，Swap 不是“磁盘变成了物理内存”。更准确地说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Swap 区只是临时存放某些内存页内容的磁盘区域。</span><br><span class="line">真正执行和读写时，数据仍然必须回到物理内存中。</span><br></pre></td></tr></table></figure><p>可以用一个类比：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">物理内存 = 办公桌</span><br><span class="line">Swap 区 = 文件柜</span><br><span class="line"></span><br><span class="line">CPU 只能直接处理办公桌上的资料。</span><br><span class="line">如果资料在文件柜里，必须先拿回办公桌。</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-物理内存是-CPU-能直接访问的-RAM；磁盘是外存。Swap-只是把暂时不用的页内容放到磁盘，不能替代真正的物理内存访问。"><a href="#❗-本节核心结论：-物理内存是-CPU-能直接访问的-RAM；磁盘是外存。Swap-只是把暂时不用的页内容放到磁盘，不能替代真正的物理内存访问。" class="headerlink" title="❗ 本节核心结论： 物理内存是 CPU 能直接访问的 RAM；磁盘是外存。Swap 只是把暂时不用的页内容放到磁盘，不能替代真正的物理内存访问。"></a><strong>❗ 本节核心结论：</strong> 物理内存是 CPU 能直接访问的 RAM；磁盘是外存。Swap 只是把暂时不用的页内容放到磁盘，不能替代真正的物理内存访问。</h2><h2 id="2-为什么需要换页机制"><a href="#2-为什么需要换页机制" class="headerlink" title="2. 为什么需要换页机制"></a>2. 为什么需要换页机制</h2><p>程序看到的是虚拟地址空间，而不是物理内存的真实容量。一个进程可能拥有很大的虚拟地址空间，但它并不一定会同时访问全部页面。</p><p>换页机制要解决两个典型问题。</p><h3 id="2-1-物理内存超售"><a href="#2-1-物理内存超售" class="headerlink" title="2.1 物理内存超售"></a>2.1 物理内存超售</h3><p>假设机器只有 4GB 物理内存，但有两个进程，每个都申请了 3GB 虚拟内存。</p><p>从虚拟地址空间角度看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">进程 A：3GB 虚拟内存</span><br><span class="line">进程 B：3GB 虚拟内存</span><br><span class="line">总虚拟内存需求：6GB</span><br></pre></td></tr></table></figure><p>但物理内存只有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">4GB RAM</span><br></pre></td></tr></table></figure><p>如果两个进程并不是同时访问各自全部 3GB，那么操作系统可以只让当前活跃页驻留在物理内存中，把暂时不用的页放到 Swap 区。</p><p>这使得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟内存总量可以超过物理内存容量。</span><br></pre></td></tr></table></figure><p>但要注意，这不代表 6GB 内容能同时都在 4GB RAM 里。任意时刻真正能被 CPU 直接访问的，仍然只能是已经驻留在物理内存中的页。</p><h3 id="2-2-按需分配"><a href="#2-2-按需分配" class="headerlink" title="2.2 按需分配"></a>2.2 按需分配</h3><p>程序申请很大一段虚拟内存，但可能只使用其中一小部分。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *buf = mmap(<span class="literal">NULL</span>, <span class="number">1</span>GB, PROT_READ | PROT_WRITE,</span><br><span class="line">                 MAP_PRIVATE | MAP_ANONYMOUS, <span class="number">-1</span>, <span class="number">0</span>);</span><br><span class="line">buf[<span class="number">0</span>] = <span class="string">&#x27;A&#x27;</span>;</span><br></pre></td></tr></table></figure><p>程序申请了 1GB，但只访问第一个字节。如果操作系统立即为整个 1GB 分配物理页，会浪费大量内存。</p><p>因此，现代系统常采用按需分配：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">先记录虚拟地址区域合法</span><br><span class="line">↓</span><br><span class="line">不马上分配所有物理页</span><br><span class="line">↓</span><br><span class="line">程序第一次访问某页时触发缺页异常</span><br><span class="line">↓</span><br><span class="line">OS 再为该页分配物理页并建立映射</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-换页和按需分配让虚拟内存使用量可以超过物理内存容量，但真实执行时仍必须把被访问页放回物理内存。"><a href="#❗-本节核心结论：-换页和按需分配让虚拟内存使用量可以超过物理内存容量，但真实执行时仍必须把被访问页放回物理内存。" class="headerlink" title="❗ 本节核心结论： 换页和按需分配让虚拟内存使用量可以超过物理内存容量，但真实执行时仍必须把被访问页放回物理内存。"></a><strong>❗ 本节核心结论：</strong> 换页和按需分配让虚拟内存使用量可以超过物理内存容量，但真实执行时仍必须把被访问页放回物理内存。</h2><h2 id="3-缺页异常：不一定是程序错误"><a href="#3-缺页异常：不一定是程序错误" class="headerlink" title="3. 缺页异常：不一定是程序错误"></a>3. 缺页异常：不一定是程序错误</h2><p>缺页异常 Page Fault 的本质是：CPU&#x2F;MMU 访问某个虚拟地址时，发现当前页表不能直接完成有效翻译，于是陷入操作系统处理。</p><p>缺页异常不一定代表程序出错。它可能有几种原因。</p><table><thead><tr><th>情况</th><th>是否一定是错误</th><th>说明</th></tr></thead><tbody><tr><td>虚拟页合法，但还没分配物理页</td><td>不是错误</td><td>按需分配场景</td></tr><tr><td>虚拟页合法，但内容被换出到 Swap</td><td>不是错误</td><td>换页场景</td></tr><tr><td>虚拟地址不属于任何合法区域</td><td>是错误</td><td>可能导致 Segmentation Fault</td></tr><tr><td>权限不允许，例如写只读页</td><td>视情况而定</td><td>可能是非法访问，也可能是 COW</td></tr></tbody></table><p>可以简化为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Page Fault = 硬件发现无法直接完成地址翻译</span><br><span class="line">Segmentation Fault = OS 判断访问非法后给进程的结果</span><br></pre></td></tr></table></figure><p>不是所有 Page Fault 都会导致 Segmentation Fault。按需分配、换入换出、写时拷贝都可能以 Page Fault 作为正常处理入口。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[访问虚拟地址] --&gt; B{页表是否有有效映射?}    B -- 有 --&gt; C[正常访问物理内存]    B -- 无 --&gt; D[触发 Page Fault]    D --&gt; E{虚拟地址是否合法?}    E -- 否 --&gt; F[Segmentation Fault]    E -- 是 --&gt; G{页面在哪里?}    G -- 从未分配物理页 --&gt; H[分配新物理页]    G -- 已换出到 Swap --&gt; I[从 Swap 换入]    H --&gt; J[更新页表]    I --&gt; J    J --&gt; K[返回用户程序继续执行]  </pre></div><p>这张图强调：缺页异常只是入口，操作系统还要判断这是合法缺页、换入请求，还是非法访问。</p><hr><h2 id="❗-本节核心结论：-缺页异常不等于程序错误。它可能是按需分配、换页或-COW-等机制的正常控制路径。"><a href="#❗-本节核心结论：-缺页异常不等于程序错误。它可能是按需分配、换页或-COW-等机制的正常控制路径。" class="headerlink" title="❗ 本节核心结论： 缺页异常不等于程序错误。它可能是按需分配、换页或 COW 等机制的正常控制路径。"></a><strong>❗ 本节核心结论：</strong> 缺页异常不等于程序错误。它可能是按需分配、换页或 COW 等机制的正常控制路径。</h2><h2 id="4-换页机制：把暂时不用的页放到-Swap"><a href="#4-换页机制：把暂时不用的页放到-Swap" class="headerlink" title="4. 换页机制：把暂时不用的页放到 Swap"></a>4. 换页机制：把暂时不用的页放到 Swap</h2><p>换页 Swapping 的基本思想是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">物理内存不够</span><br><span class="line">↓</span><br><span class="line">选择某个暂时不需要的物理页</span><br><span class="line">↓</span><br><span class="line">把它的内容写入磁盘 Swap 区</span><br><span class="line">↓</span><br><span class="line">释放原来的物理页</span><br><span class="line">↓</span><br><span class="line">把腾出来的物理页用于其他更紧急的虚拟页</span><br></pre></td></tr></table></figure><h3 id="4-1-换出"><a href="#4-1-换出" class="headerlink" title="4.1 换出"></a>4.1 换出</h3><p>假设原来有映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 V → 物理页 P3</span><br></pre></td></tr></table></figure><p>当操作系统决定把这个页换出时，可能执行：</p><ol><li>如果该页被修改过，将页内容写入 Swap 区。</li><li>在内核数据结构中记录：虚拟页 V 的内容现在位于 Swap 的某个位置。</li><li>修改 V 对应的页表项，使其不再指向可访问物理页。</li><li>回收物理页 P3。</li><li>清理或刷新可能存在的 TLB 缓存项。</li></ol><p>换出后可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 V → Swap 区中的某个位置</span><br></pre></td></tr></table></figure><p>此时虚拟页 V 仍然可能是合法虚拟页，只是当前不在物理内存中。</p><h3 id="4-2-换入"><a href="#4-2-换入" class="headerlink" title="4.2 换入"></a>4.2 换入</h3><p>之后程序再次访问虚拟页 V：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">CPU 访问虚拟页 V</span><br><span class="line">↓</span><br><span class="line">页表项无有效物理页</span><br><span class="line">↓</span><br><span class="line">触发 Page Fault</span><br><span class="line">↓</span><br><span class="line">OS 发现 V 的内容在 Swap 区</span><br><span class="line">↓</span><br><span class="line">分配新的物理页，例如 P7</span><br><span class="line">↓</span><br><span class="line">从 Swap 读取内容到 P7</span><br><span class="line">↓</span><br><span class="line">更新页表项：V → P7</span><br><span class="line">↓</span><br><span class="line">返回用户程序继续执行</span><br></pre></td></tr></table></figure><p>于是映射变化为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">换出前：虚拟页 V → 物理页 P3</span><br><span class="line">换出后：虚拟页 V → Swap 区</span><br><span class="line">换入后：虚拟页 V → 物理页 P7</span><br></pre></td></tr></table></figure><p>这里有一个重要点：<strong>换入时不一定回到原来的物理页位置。</strong></p><p>分页机制允许同一个虚拟页在不同时间映射到不同物理页。只要页表项更新正确，程序看到的虚拟地址可以保持不变。</p><hr><h2 id="❗-本节核心结论：-换页不是移动虚拟地址，而是改变虚拟页背后的存放位置；换入后需要更新页表，使虚拟页指向新的物理页。"><a href="#❗-本节核心结论：-换页不是移动虚拟地址，而是改变虚拟页背后的存放位置；换入后需要更新页表，使虚拟页指向新的物理页。" class="headerlink" title="❗ 本节核心结论： 换页不是移动虚拟地址，而是改变虚拟页背后的存放位置；换入后需要更新页表，使虚拟页指向新的物理页。"></a><strong>❗ 本节核心结论：</strong> 换页不是移动虚拟地址，而是改变虚拟页背后的存放位置；换入后需要更新页表，使虚拟页指向新的物理页。</h2><h2 id="5-换入位置改变时，页表如何改变"><a href="#5-换入位置改变时，页表如何改变" class="headerlink" title="5. 换入位置改变时，页表如何改变"></a>5. 换入位置改变时，页表如何改变</h2><p>换入位置可能与换出前不同，这意味着虚拟页到物理页的映射必须更新。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">换出前：</span><br><span class="line">虚拟页 V → 物理页 P3</span><br><span class="line"></span><br><span class="line">换入后：</span><br><span class="line">虚拟页 V → 物理页 P7</span><br></pre></td></tr></table></figure><p>实现这个改变的核心就是修改页表项。</p><p>换出前，页表项可以抽象为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PTE(V):</span><br><span class="line">    valid = 1</span><br><span class="line">    physical_page = P3</span><br></pre></td></tr></table></figure><p>换出后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PTE(V):</span><br><span class="line">    valid = 0</span><br><span class="line">    swap_info = Swap 中的位置</span><br></pre></td></tr></table></figure><p>换入后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PTE(V):</span><br><span class="line">    valid = 1</span><br><span class="line">    physical_page = P7</span><br></pre></td></tr></table></figure><p>同时还要处理 TLB。因为 TLB 可能缓存了旧的地址翻译结果。如果不清理，CPU 可能继续使用旧映射。</p><p>因此完整逻辑是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">修改页表项</span><br><span class="line">↓</span><br><span class="line">使虚拟页指向新的物理页</span><br><span class="line">↓</span><br><span class="line">刷新或失效相关 TLB 项</span><br><span class="line">↓</span><br><span class="line">重新执行刚才触发缺页的指令</span><br></pre></td></tr></table></figure><p>这体现了虚拟内存的一个基本能力：<strong>虚拟地址稳定，物理页可以动态变化。</strong></p><hr><h2 id="❗-本节核心结论：-换入位置改变后，OS-通过更新页表项中的物理页号来改变映射，并需要处理旧-TLB-项。"><a href="#❗-本节核心结论：-换入位置改变后，OS-通过更新页表项中的物理页号来改变映射，并需要处理旧-TLB-项。" class="headerlink" title="❗ 本节核心结论： 换入位置改变后，OS 通过更新页表项中的物理页号来改变映射，并需要处理旧 TLB 项。"></a><strong>❗ 本节核心结论：</strong> 换入位置改变后，OS 通过更新页表项中的物理页号来改变映射，并需要处理旧 TLB 项。</h2><h2 id="6-按需分配的收益与代价"><a href="#6-按需分配的收益与代价" class="headerlink" title="6. 按需分配的收益与代价"></a>6. 按需分配的收益与代价</h2><p>按需分配和换页机制都利用了一个事实：程序申请的虚拟内存，不一定会全部同时使用。</p><h3 id="6-1-收益"><a href="#6-1-收益" class="headerlink" title="6.1 收益"></a>6.1 收益</h3><p>按需分配的主要收益是节省物理内存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">没访问过的虚拟页</span><br><span class="line">↓</span><br><span class="line">不占真实物理页</span><br></pre></td></tr></table></figure><p>这可以让系统同时运行更多进程，或者允许进程拥有比物理内存更大的虚拟地址空间。</p><h3 id="6-2-代价"><a href="#6-2-代价" class="headerlink" title="6.2 代价"></a>6.2 代价</h3><p>代价是首次访问变慢。</p><p>第一次访问某个尚未分配或已换出的页时，需要：</p><ul><li>触发缺页异常；</li><li>陷入内核；</li><li>分配物理页或从磁盘读回；</li><li>修改页表；</li><li>恢复执行。</li></ul><p>如果页面在 Swap 中，还涉及磁盘 I&#x2F;O，延迟会明显增加。</p><h3 id="6-3-缓解方法"><a href="#6-3-缓解方法" class="headerlink" title="6.3 缓解方法"></a>6.3 缓解方法</h3><p>操作系统会利用程序的局部性来缓解这一问题。</p><table><thead><tr><th>局部性</th><th>含义</th><th>对换页的意义</th></tr></thead><tbody><tr><td>时间局部性</td><td>刚访问过的页，短期内可能再次访问</td><td>最近用过的页不应轻易换出</td></tr><tr><td>空间局部性</td><td>访问某页后，附近页可能也会被访问</td><td>可以考虑预取相邻页</td></tr></tbody></table><p>因此，好的换页系统不会随便换出页面，而是尽量预测哪些页近期更可能被使用。</p><hr><h2 id="❗-本节核心结论：-按需分配节省物理内存，但会增加首次访问或换入时的延迟；局部性是优化缺页成本的基础。"><a href="#❗-本节核心结论：-按需分配节省物理内存，但会增加首次访问或换入时的延迟；局部性是优化缺页成本的基础。" class="headerlink" title="❗ 本节核心结论： 按需分配节省物理内存，但会增加首次访问或换入时的延迟；局部性是优化缺页成本的基础。"></a><strong>❗ 本节核心结论：</strong> 按需分配节省物理内存，但会增加首次访问或换入时的延迟；局部性是优化缺页成本的基础。</h2><h2 id="7-页替换策略：内存满了应该换出谁"><a href="#7-页替换策略：内存满了应该换出谁" class="headerlink" title="7. 页替换策略：内存满了应该换出谁"></a>7. 页替换策略：内存满了应该换出谁</h2><p>当物理内存不足时，操作系统必须选择某个页换出。这个选择规则就是页替换策略。</p><p>页替换策略回答的问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">内存满了，应该牺牲哪个页？</span><br></pre></td></tr></table></figure><p>不同策略的差别在于：它们用什么标准判断“哪个页最不重要”。</p><p>本讲主要涉及：</p><ul><li>MIN</li><li>FIFO</li><li>Second Chance</li><li>LRU</li><li>MRU</li></ul><hr><h2 id="❗-本节核心结论：-页替换策略的本质是在物理内存不足时选择牺牲页；策略好坏直接影响缺页次数和系统性能。"><a href="#❗-本节核心结论：-页替换策略的本质是在物理内存不足时选择牺牲页；策略好坏直接影响缺页次数和系统性能。" class="headerlink" title="❗ 本节核心结论： 页替换策略的本质是在物理内存不足时选择牺牲页；策略好坏直接影响缺页次数和系统性能。"></a><strong>❗ 本节核心结论：</strong> 页替换策略的本质是在物理内存不足时选择牺牲页；策略好坏直接影响缺页次数和系统性能。</h2><h2 id="8-MIN：理论最优但现实不可实现"><a href="#8-MIN：理论最优但现实不可实现" class="headerlink" title="8. MIN：理论最优但现实不可实现"></a>8. MIN：理论最优但现实不可实现</h2><p>MIN 策略的规则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">换出未来最长时间不会被访问的页；</span><br><span class="line">如果某页未来再也不会被访问，就优先换出它。</span><br></pre></td></tr></table></figure><p>例如当前内存中有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">内存页：1 2 3</span><br><span class="line">未来访问序列：4 3 5 4 2 3</span><br></pre></td></tr></table></figure><p>现在要访问 4，但内存满了，需要换出一个页。</p><p>观察未来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">页 1：未来不再访问</span><br><span class="line">页 2：未来会访问</span><br><span class="line">页 3：马上会访问</span><br></pre></td></tr></table></figure><p>MIN 会换出页 1，因为页 1 未来不再使用。</p><p>MIN 的优点是理论缺页次数最少。它常用作评价其他策略的理想基准。</p><p>但它的问题也很明显：<strong>操作系统不知道未来。</strong></p><p>现实系统无法提前知道精确的未来访问序列，所以 MIN 难以直接实现。</p><hr><h2 id="❗-本节核心结论：-MIN-是理论最优页替换策略，但依赖未来访问信息，因此现实系统通常无法直接实现。"><a href="#❗-本节核心结论：-MIN-是理论最优页替换策略，但依赖未来访问信息，因此现实系统通常无法直接实现。" class="headerlink" title="❗ 本节核心结论： MIN 是理论最优页替换策略，但依赖未来访问信息，因此现实系统通常无法直接实现。"></a><strong>❗ 本节核心结论：</strong> MIN 是理论最优页替换策略，但依赖未来访问信息，因此现实系统通常无法直接实现。</h2><h2 id="9-FIFO：先进先出"><a href="#9-FIFO：先进先出" class="headerlink" title="9. FIFO：先进先出"></a>9. FIFO：先进先出</h2><p>FIFO 的规则非常简单：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">谁最早进入内存，谁最先被换出。</span><br></pre></td></tr></table></figure><p>它可以用队列实现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">队头：最早进入内存的页</span><br><span class="line">队尾：最新进入内存的页</span><br></pre></td></tr></table></figure><p>例如队列为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">队头 → [3][2][1] ← 队尾</span><br></pre></td></tr></table></figure><p>需要换页时，FIFO 换出队头页 3。</p><p>新页进入后，加入队尾。</p><p>FIFO 的优点是简单，维护成本低。缺点是不考虑页面最近是否被访问。</p><p>例如某个页虽然很早进入内存，但最近一直被频繁使用。FIFO 仍然可能因为它“进来得早”而把它换出。</p><hr><h2 id="❗-本节核心结论：-FIFO-只看进入内存的时间，不看最近使用情况，因此简单但可能换出仍然活跃的页。"><a href="#❗-本节核心结论：-FIFO-只看进入内存的时间，不看最近使用情况，因此简单但可能换出仍然活跃的页。" class="headerlink" title="❗ 本节核心结论： FIFO 只看进入内存的时间，不看最近使用情况，因此简单但可能换出仍然活跃的页。"></a><strong>❗ 本节核心结论：</strong> FIFO 只看进入内存的时间，不看最近使用情况，因此简单但可能换出仍然活跃的页。</h2><h2 id="10-Second-Chance：给-FIFO-一次保命机会"><a href="#10-Second-Chance：给-FIFO-一次保命机会" class="headerlink" title="10. Second Chance：给 FIFO 一次保命机会"></a>10. Second Chance：给 FIFO 一次保命机会</h2><p>Second Chance 是 FIFO 的改进。</p><p>FIFO 的问题是太粗暴：只要一个页进入得早，就可能被换出。Second Chance 加入了访问位，用来判断队头页最近是否被访问过。</p><p>规则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">查看队头页：</span><br><span class="line">    如果访问位 = 1：</span><br><span class="line">        清除访问位</span><br><span class="line">        将该页移到队尾</span><br><span class="line">        给它第二次机会</span><br><span class="line">    如果访问位 = 0：</span><br><span class="line">        换出该页</span><br></pre></td></tr></table></figure><p>可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">FIFO 问：谁最早进来？</span><br><span class="line">Second Chance 多问一句：你最近还用过吗？</span><br></pre></td></tr></table></figure><p>示意流程：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[需要换出页面] --&gt; B[检查 FIFO 队头页]    B --&gt; C{访问位是否为 1?}    C -- 是 --&gt; D[清除访问位]    D --&gt; E[移到队尾]    E --&gt; B    C -- 否 --&gt; F[换出该页]  </pre></div><p>Second Chance 比 FIFO 更合理，因为它避免立即换出最近被访问过的页。</p><hr><h2 id="❗-本节核心结论：-Second-Chance-在-FIFO-基础上加入访问位，让最近访问过的页获得第二次机会，从而降低误换出活跃页的概率。"><a href="#❗-本节核心结论：-Second-Chance-在-FIFO-基础上加入访问位，让最近访问过的页获得第二次机会，从而降低误换出活跃页的概率。" class="headerlink" title="❗ 本节核心结论： Second Chance 在 FIFO 基础上加入访问位，让最近访问过的页获得第二次机会，从而降低误换出活跃页的概率。"></a><strong>❗ 本节核心结论：</strong> Second Chance 在 FIFO 基础上加入访问位，让最近访问过的页获得第二次机会，从而降低误换出活跃页的概率。</h2><h2 id="11-LRU：换出最近最久未使用的页"><a href="#11-LRU：换出最近最久未使用的页" class="headerlink" title="11. LRU：换出最近最久未使用的页"></a>11. LRU：换出最近最久未使用的页</h2><p>LRU 全称 Least Recently Used，规则是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">换出最近最久没有被访问的页。</span><br></pre></td></tr></table></figure><p>它基于局部性原理：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">最近使用过的页，短期内更可能再次使用；</span><br><span class="line">很久没使用的页，短期内相对不重要。</span><br></pre></td></tr></table></figure><p>例如链表记录页面最近使用顺序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">链表头：最久未使用</span><br><span class="line">链表尾：最近使用</span><br></pre></td></tr></table></figure><p>当前链表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[2][3][1]</span><br></pre></td></tr></table></figure><p>如果需要换出，LRU 换出链表头 2。</p><p>如果访问了 3，就把 3 移到链表尾：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">访问前：[2][3][1]</span><br><span class="line">访问后：[2][1][3]</span><br></pre></td></tr></table></figure><p>LRU 比 FIFO 更合理的原因在于：</p><table><thead><tr><th>策略</th><th>判断标准</th></tr></thead><tbody><tr><td>FIFO</td><td>谁最早进入内存</td></tr><tr><td>LRU</td><td>谁最近最久未使用</td></tr></tbody></table><p>FIFO 不关心页是否仍然活跃；LRU 更接近程序的真实访问规律。</p><p>但真实系统中精确维护 LRU 可能有成本，因为每次访问都要更新时间顺序。因此实际系统常用近似 LRU 的方法，例如访问位、时钟算法等。</p><hr><h2 id="❗-本节核心结论：-LRU-根据“最近使用情况”判断牺牲页，更符合局部性原理；但精确实现可能成本较高。"><a href="#❗-本节核心结论：-LRU-根据“最近使用情况”判断牺牲页，更符合局部性原理；但精确实现可能成本较高。" class="headerlink" title="❗ 本节核心结论： LRU 根据“最近使用情况”判断牺牲页，更符合局部性原理；但精确实现可能成本较高。"></a><strong>❗ 本节核心结论：</strong> LRU 根据“最近使用情况”判断牺牲页，更符合局部性原理；但精确实现可能成本较高。</h2><h2 id="12-MRU：换出最近刚使用的页"><a href="#12-MRU：换出最近刚使用的页" class="headerlink" title="12. MRU：换出最近刚使用的页"></a>12. MRU：换出最近刚使用的页</h2><p>MRU 全称 Most Recently Used，规则与 LRU 相反：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">换出最近刚被访问的页。</span><br></pre></td></tr></table></figure><p>这听起来反直觉，但在某些访问模式下可能合理。</p><p>例如程序顺序扫描一个远大于内存的数据集：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1 2 3 4 5 6 7 8 ...</span><br></pre></td></tr></table></figure><p>如果每个页只访问一次，最近访问过的页可能确实不会再用。此时换出最近刚访问过的页不一定差。</p><p>不过对于大多数具有时间局部性的程序，LRU 通常比 MRU 更符合直觉。</p><hr><h2 id="❗-本节核心结论：-MRU-适用于某些“访问后很快不再使用”的模式，但一般程序中-LRU-更常见、更符合局部性假设。"><a href="#❗-本节核心结论：-MRU-适用于某些“访问后很快不再使用”的模式，但一般程序中-LRU-更常见、更符合局部性假设。" class="headerlink" title="❗ 本节核心结论： MRU 适用于某些“访问后很快不再使用”的模式，但一般程序中 LRU 更常见、更符合局部性假设。"></a><strong>❗ 本节核心结论：</strong> MRU 适用于某些“访问后很快不再使用”的模式，但一般程序中 LRU 更常见、更符合局部性假设。</h2><h2 id="13-几种页替换策略的对比"><a href="#13-几种页替换策略的对比" class="headerlink" title="13. 几种页替换策略的对比"></a>13. 几种页替换策略的对比</h2><table><thead><tr><th>策略</th><th>换出标准</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>MIN</td><td>未来最晚再访问或不再访问的页</td><td>理论最优</td><td>需要知道未来，现实不可实现</td></tr><tr><td>FIFO</td><td>最早进入内存的页</td><td>简单</td><td>可能换出活跃页</td></tr><tr><td>Second Chance</td><td>FIFO + 访问位</td><td>比 FIFO 更关注近期访问</td><td>仍是近似策略</td></tr><tr><td>LRU</td><td>最近最久未使用的页</td><td>符合局部性</td><td>精确维护成本高</td></tr><tr><td>MRU</td><td>最近刚使用的页</td><td>特定顺序扫描场景可能有效</td><td>一般场景反直觉</td></tr></tbody></table><p>可以把这些策略压缩成一句话：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">MIN 看未来；</span><br><span class="line">FIFO 看进入时间；</span><br><span class="line">Second Chance 看进入时间 + 最近是否访问；</span><br><span class="line">LRU 看最近一次访问距现在多久；</span><br><span class="line">MRU 看谁刚刚被访问过。</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-页替换策略本质上都是在估计“哪个页未来最不可能被用到”，只是估计依据不同。"><a href="#❗-本节核心结论：-页替换策略本质上都是在估计“哪个页未来最不可能被用到”，只是估计依据不同。" class="headerlink" title="❗ 本节核心结论： 页替换策略本质上都是在估计“哪个页未来最不可能被用到”，只是估计依据不同。"></a><strong>❗ 本节核心结论：</strong> 页替换策略本质上都是在估计“哪个页未来最不可能被用到”，只是估计依据不同。</h2><h2 id="14-好的换页策略能否降低颠簸"><a href="#14-好的换页策略能否降低颠簸" class="headerlink" title="14. 好的换页策略能否降低颠簸"></a>14. 好的换页策略能否降低颠簸</h2><p>可以，但需要加一个限制。</p><p>好的页替换策略可以：</p><ul><li>减少不必要的缺页；</li><li>避免换出马上要用的页；</li><li>降低频繁换入换出的概率；</li><li>减轻颠簸程度。</li></ul><p>但它不能在所有情况下彻底消除颠簸。</p><p>颠簸的根本原因通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">所有活跃进程当前真正需要的物理页数量</span><br><span class="line">&gt;</span><br><span class="line">实际可用物理页数量</span><br></pre></td></tr></table></figure><p>也就是工作集总需求超过了物理内存承载能力。</p><p>如果系统内存严重不足，替换策略再聪明，也只能在有限空间里反复选择牺牲页。此时可能需要：</p><ul><li>减少并发进程；</li><li>挂起部分进程；</li><li>增加物理内存；</li><li>降低程序内存需求；</li><li>适当使用 Swap；</li><li>必要时由 OOM 机制终止进程。</li></ul><p>所以更严谨的说法是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">合适的页替换策略可以减少缺页次数，从而降低颠簸发生的概率和程度；</span><br><span class="line">但如果活跃工作集总量超过物理内存容量，单靠替换策略无法根治颠簸。</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-好的页替换策略能减轻颠簸，但颠簸的根因是活跃页需求超过物理内存容量，不能只靠策略彻底解决。"><a href="#❗-本节核心结论：-好的页替换策略能减轻颠簸，但颠簸的根因是活跃页需求超过物理内存容量，不能只靠策略彻底解决。" class="headerlink" title="❗ 本节核心结论： 好的页替换策略能减轻颠簸，但颠簸的根因是活跃页需求超过物理内存容量，不能只靠策略彻底解决。"></a><strong>❗ 本节核心结论：</strong> 好的页替换策略能减轻颠簸，但颠簸的根因是活跃页需求超过物理内存容量，不能只靠策略彻底解决。</h2><h2 id="15-颠簸-Thrashing：系统把时间浪费在换页上"><a href="#15-颠簸-Thrashing：系统把时间浪费在换页上" class="headerlink" title="15. 颠簸 Thrashing：系统把时间浪费在换页上"></a>15. 颠簸 Thrashing：系统把时间浪费在换页上</h2><p>颠簸指的是系统频繁换入换出页面，以至于真正执行程序的时间很少。</p><p>典型现象是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">页刚换出，很快又要访问</span><br><span class="line">↓</span><br><span class="line">触发缺页异常</span><br><span class="line">↓</span><br><span class="line">页换入</span><br><span class="line">↓</span><br><span class="line">为了腾空间，又换出别的页</span><br><span class="line">↓</span><br><span class="line">别的页很快又被访问</span><br><span class="line">↓</span><br><span class="line">继续缺页</span><br></pre></td></tr></table></figure><p>系统进入这种状态后，大量时间消耗在：</p><ul><li>缺页异常处理；</li><li>等待磁盘 I&#x2F;O；</li><li>换入换出页面；</li><li>调度切换；</li><li>维护页表和相关数据结构。</li></ul><p>而用户程序本身推进很慢。</p><p>可以用图表示：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[物理内存不足] --&gt; B[换出某些页]    B --&gt; C[程序很快再次访问被换出的页]    C --&gt; D[Page Fault]    D --&gt; E[从 Swap 换入]    E --&gt; F[为了腾空间换出其他页]    F --&gt; G[其他页又很快被访问]    G --&gt; D  </pre></div><p>这就是电脑内存不足时“卡死”的本质之一：系统不是完全不工作，而是在大量做换页管理，真正执行用户程序的有效时间很少。</p><p>需要注意的是，颠簸时 CPU 利用率的表现可能复杂：</p><ul><li>有时 CPU 看起来利用率不高，因为进程大量阻塞等待磁盘 I&#x2F;O；</li><li>有时 CPU 也可能忙于内核态处理缺页、调度等工作；</li><li>但无论哪种情况，系统有效吞吐都会下降。</li></ul><hr><h2 id="❗-本节核心结论：-颠簸是频繁缺页和换页导致的系统性能崩溃现象；大量时间用于换页和-I-O，而不是推进程序执行。"><a href="#❗-本节核心结论：-颠簸是频繁缺页和换页导致的系统性能崩溃现象；大量时间用于换页和-I-O，而不是推进程序执行。" class="headerlink" title="❗ 本节核心结论： 颠簸是频繁缺页和换页导致的系统性能崩溃现象；大量时间用于换页和 I&#x2F;O，而不是推进程序执行。"></a><strong>❗ 本节核心结论：</strong> 颠簸是频繁缺页和换页导致的系统性能崩溃现象；大量时间用于换页和 I&#x2F;O，而不是推进程序执行。</h2><h2 id="16-工作集模型：判断进程当前真正需要哪些页"><a href="#16-工作集模型：判断进程当前真正需要哪些页" class="headerlink" title="16. 工作集模型：判断进程当前真正需要哪些页"></a>16. 工作集模型：判断进程当前真正需要哪些页</h2><p>工作集模型用于分析一个进程在当前阶段真正需要哪些页面。</p><p>定义可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">W(t, x) = 在时间区间 [t - x, t] 内被访问过的页集合</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>t</code> 是当前时刻；</li><li><code>x</code> 是观察窗口长度；</li><li><code>[t - x, t]</code> 表示从过去某一时刻到现在的时间区间；</li><li>在这个区间内被访问过的页，构成当前工作集。</li></ul><p>直观理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">最近一段时间用过的页</span><br><span class="line">≈</span><br><span class="line">当前阶段还可能继续使用的页</span><br></pre></td></tr></table></figure><p>这些页最好不要被轻易换出。否则可能马上再次访问，触发缺页，导致性能下降甚至颠簸。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">时间窗口 [t - x, t] 内访问过：</span><br><span class="line">页 1、页 3、页 5、页 8</span><br><span class="line"></span><br><span class="line">则当前工作集：</span><br><span class="line">W(t, x) = &#123;1, 3, 5, 8&#125;</span><br></pre></td></tr></table></figure><p>如果一个页很久没有被访问，它就不属于当前工作集，可以成为换出候选页。</p><p>但要注意：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">不属于工作集 ≠ 必定立刻换出</span><br><span class="line">不属于工作集 = 可以考虑换出</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-工作集模型用最近一段时间访问过的页集合来近似进程当前真正需要的内存。"><a href="#❗-本节核心结论：-工作集模型用最近一段时间访问过的页集合来近似进程当前真正需要的内存。" class="headerlink" title="❗ 本节核心结论： 工作集模型用最近一段时间访问过的页集合来近似进程当前真正需要的内存。"></a><strong>❗ 本节核心结论：</strong> 工作集模型用最近一段时间访问过的页集合来近似进程当前真正需要的内存。</h2><h2 id="17-工作集模型如何维护"><a href="#17-工作集模型如何维护" class="headerlink" title="17. 工作集模型如何维护"></a>17. 工作集模型如何维护</h2><p>工作集模型通常依赖访问位和时间信息。</p><p>一种简化维护方式是：</p><ol><li>每个页有访问位。</li><li>硬件在访问页面时，将访问位置为 1。</li><li>操作系统周期性扫描页面。</li><li>如果访问位为 1，说明最近访问过，记录当前时间戳，并清除访问位。</li><li>如果访问位长期为 0，说明很久没被访问。</li><li>若当前时间与上次访问时间差超过窗口 <code>x</code>，则该页不再属于工作集。</li></ol><p>可以表示为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[周期性扫描页面] --&gt; B{访问位是否为 1?}    B -- 是 --&gt; C[记录当前时间为最近访问时间]    C --&gt; D[清除访问位]    B -- 否 --&gt; E[保持最近访问时间不变]    E --&gt; F{当前时间 - 最近访问时间 &gt; x?}    F -- 是 --&gt; G[不属于工作集，可作为换出候选]    F -- 否 --&gt; H[仍可能属于工作集]  </pre></div><p>工作集模型的目标不是精确预测未来，而是利用过去一段时间的访问情况估计当前活跃页集合。</p><hr><h2 id="❗-本节核心结论：-工作集模型通过访问位和时间窗口估计页面是否仍然活跃，帮助系统选择更合适的换出候选页。"><a href="#❗-本节核心结论：-工作集模型通过访问位和时间窗口估计页面是否仍然活跃，帮助系统选择更合适的换出候选页。" class="headerlink" title="❗ 本节核心结论： 工作集模型通过访问位和时间窗口估计页面是否仍然活跃，帮助系统选择更合适的换出候选页。"></a><strong>❗ 本节核心结论：</strong> 工作集模型通过访问位和时间窗口估计页面是否仍然活跃，帮助系统选择更合适的换出候选页。</h2><h2 id="18-工作集模型与-LRU-的区别"><a href="#18-工作集模型与-LRU-的区别" class="headerlink" title="18. 工作集模型与 LRU 的区别"></a>18. 工作集模型与 LRU 的区别</h2><p>工作集模型和 LRU 都利用“最近访问情况”，但它们不是同一种东西。</p><h3 id="18-1-LRU-是替换策略"><a href="#18-1-LRU-是替换策略" class="headerlink" title="18.1 LRU 是替换策略"></a>18.1 LRU 是替换策略</h3><p>LRU 的问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">当前内存里的这些页中，哪一个最近最久没被访问？</span><br></pre></td></tr></table></figure><p>它关注的是页面之间的相对最近访问顺序。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">页 2 最久未使用</span><br><span class="line">页 5 次久未使用</span><br><span class="line">页 7 最近刚使用</span><br></pre></td></tr></table></figure><p>LRU 会优先换出页 2。</p><h3 id="18-2-工作集模型是内存需求模型"><a href="#18-2-工作集模型是内存需求模型" class="headerlink" title="18.2 工作集模型是内存需求模型"></a>18.2 工作集模型是内存需求模型</h3><p>工作集模型的问题是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">某个进程在最近一段时间内到底用过哪些页？</span><br><span class="line">这个进程当前正常运行大约需要多少页？</span><br></pre></td></tr></table></figure><p>它关注的是一个进程的活跃页集合，而不只是单个页的替换顺序。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">进程 A 当前工作集大小：100 页</span><br><span class="line">进程 B 当前工作集大小：300 页</span><br><span class="line">进程 C 当前工作集大小：200 页</span><br></pre></td></tr></table></figure><p>如果总工作集大小超过物理内存容量：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">100 + 300 + 200 &gt; 可用物理页数量</span><br></pre></td></tr></table></figure><p>系统就有较高风险发生颠簸。</p><h3 id="18-3-对比表"><a href="#18-3-对比表" class="headerlink" title="18.3 对比表"></a>18.3 对比表</h3><table><thead><tr><th>对比项</th><th>LRU</th><th>工作集模型</th></tr></thead><tbody><tr><td>类型</td><td>页替换策略</td><td>内存需求分析模型</td></tr><tr><td>关注对象</td><td>单个页的最近使用顺序</td><td>进程最近一段时间使用过的页集合</td></tr><tr><td>核心问题</td><td>换出哪个页</td><td>当前进程需要多少页</td></tr><tr><td>使用信息</td><td>最近访问顺序</td><td>时间窗口 <code>[t - x, t]</code> 内的访问集合</td></tr><tr><td>主要价值</td><td>选择较合适的牺牲页</td><td>判断活跃页集合，避免颠簸</td></tr><tr><td>局限</td><td>只看替换，不直接判断进程整体需求</td><td>需要选择合适窗口大小并维护访问历史</td></tr></tbody></table><h3 id="18-4-工作集模型相对-LRU-的优势"><a href="#18-4-工作集模型相对-LRU-的优势" class="headerlink" title="18.4 工作集模型相对 LRU 的优势"></a>18.4 工作集模型相对 LRU 的优势</h3><p>工作集模型的优势不是“每次都比 LRU 更会选某个页”，而是它能提供更高层次的信息。</p><h4 id="优势一：判断进程整体内存需求"><a href="#优势一：判断进程整体内存需求" class="headerlink" title="优势一：判断进程整体内存需求"></a>优势一：判断进程整体内存需求</h4><p>LRU 可以告诉系统：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">哪个页最久没用？</span><br></pre></td></tr></table></figure><p>工作集模型可以告诉系统：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">这个进程当前大约需要多少页才能稳定运行？</span><br></pre></td></tr></table></figure><h4 id="优势二：帮助避免颠簸"><a href="#优势二：帮助避免颠簸" class="headerlink" title="优势二：帮助避免颠簸"></a>优势二：帮助避免颠簸</h4><p>如果所有进程的工作集总和大于物理内存，系统可以判断当前并发负载过高。</p><p>这时只靠 LRU 在各个页之间换来换去，可能仍然无法避免频繁缺页。工作集模型可以帮助系统考虑：</p><ul><li>是否减少并发进程；</li><li>是否挂起某些进程；</li><li>是否保护某些进程的工作集；</li><li>是否避免换出最近活跃页。</li></ul><h4 id="优势三：更直接面向“保留活跃集合”"><a href="#优势三：更直接面向“保留活跃集合”" class="headerlink" title="优势三：更直接面向“保留活跃集合”"></a>优势三：更直接面向“保留活跃集合”</h4><p>LRU 的动作是“选出最久未使用的页”。工作集模型的思想是“尽量保留当前工作集中的页”。</p><p>这更接近避免颠簸的目标。</p><hr><h2 id="❗-本节核心结论：-LRU-是具体页替换策略；工作集模型是分析进程当前活跃页集合的模型。工作集模型能为页替换和避免颠簸提供更高层次参考。"><a href="#❗-本节核心结论：-LRU-是具体页替换策略；工作集模型是分析进程当前活跃页集合的模型。工作集模型能为页替换和避免颠簸提供更高层次参考。" class="headerlink" title="❗ 本节核心结论： LRU 是具体页替换策略；工作集模型是分析进程当前活跃页集合的模型。工作集模型能为页替换和避免颠簸提供更高层次参考。"></a><strong>❗ 本节核心结论：</strong> LRU 是具体页替换策略；工作集模型是分析进程当前活跃页集合的模型。工作集模型能为页替换和避免颠簸提供更高层次参考。</h2><h2 id="19-一个完整例子：从缺页到换入再到工作集判断"><a href="#19-一个完整例子：从缺页到换入再到工作集判断" class="headerlink" title="19. 一个完整例子：从缺页到换入再到工作集判断"></a>19. 一个完整例子：从缺页到换入再到工作集判断</h2><p>假设一个程序有 5 个虚拟页：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">V1 V2 V3 V4 V5</span><br></pre></td></tr></table></figure><p>但系统只能给它 3 个物理页。</p><p>当前物理内存中有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">V1 → P1</span><br><span class="line">V2 → P2</span><br><span class="line">V3 → P3</span><br></pre></td></tr></table></figure><p>现在程序访问 V4。</p><h3 id="19-1-触发缺页"><a href="#19-1-触发缺页" class="headerlink" title="19.1 触发缺页"></a>19.1 触发缺页</h3><p>因为 V4 当前不在物理内存中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">访问 V4</span><br><span class="line">↓</span><br><span class="line">页表无有效映射</span><br><span class="line">↓</span><br><span class="line">Page Fault</span><br></pre></td></tr></table></figure><h3 id="19-2-系统选择换出页"><a href="#19-2-系统选择换出页" class="headerlink" title="19.2 系统选择换出页"></a>19.2 系统选择换出页</h3><p>物理内存满了，需要选择牺牲页。</p><p>如果使用 FIFO，可能换出最早进入内存的 V1。</p><p>如果使用 LRU，可能换出最近最久未访问的页。</p><p>如果使用 Second Chance，会先检查队头页最近是否被访问过。</p><h3 id="19-3-换出与换入"><a href="#19-3-换出与换入" class="headerlink" title="19.3 换出与换入"></a>19.3 换出与换入</h3><p>假设系统最终选择换出 V1：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">V1 内容写入 Swap</span><br><span class="line">释放 P1</span><br><span class="line">从磁盘或文件中准备 V4 内容</span><br><span class="line">把 V4 放入 P1 或其他空闲物理页</span><br><span class="line">更新页表：V4 → P1</span><br></pre></td></tr></table></figure><p>如果后续 V1 又马上被访问，系统又会触发缺页并把 V1 换入。</p><p>如果这种情况不断发生，就可能颠簸。</p><h3 id="19-4-工作集分析"><a href="#19-4-工作集分析" class="headerlink" title="19.4 工作集分析"></a>19.4 工作集分析</h3><p>如果最近时间窗口 <code>[t - x, t]</code> 内程序频繁访问的是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">V1 V2 V3 V4</span><br></pre></td></tr></table></figure><p>则工作集大致是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">W(t, x) = &#123;V1, V2, V3, V4&#125;</span><br></pre></td></tr></table></figure><p>工作集大小是 4 页，但系统只给了 3 个物理页。此时无论替换策略怎么选，都可能频繁缺页。</p><p>这说明问题不只是“换出哪个页”，而是“这个进程当前需要的页数已经超过系统给它的物理页数”。</p><hr><h2 id="❗-本节核心结论：-页替换策略解决“换谁”，工作集模型进一步揭示“当前给的物理页够不够支撑进程活跃集合”。"><a href="#❗-本节核心结论：-页替换策略解决“换谁”，工作集模型进一步揭示“当前给的物理页够不够支撑进程活跃集合”。" class="headerlink" title="❗ 本节核心结论： 页替换策略解决“换谁”，工作集模型进一步揭示“当前给的物理页够不够支撑进程活跃集合”。"></a><strong>❗ 本节核心结论：</strong> 页替换策略解决“换谁”，工作集模型进一步揭示“当前给的物理页够不够支撑进程活跃集合”。</h2><h2 id="20-常见误区总结"><a href="#20-常见误区总结" class="headerlink" title="20. 常见误区总结"></a>20. 常见误区总结</h2><h3 id="20-1-Swap-不是物理内存"><a href="#20-1-Swap-不是物理内存" class="headerlink" title="20.1 Swap 不是物理内存"></a>20.1 Swap 不是物理内存</h3><p>Swap 在磁盘上，不能像 RAM 一样被 CPU 直接访问。页内容在 Swap 中时，必须先换入物理内存，程序才能继续访问。</p><h3 id="20-2-被换出的是页内容，不是物理地址"><a href="#20-2-被换出的是页内容，不是物理地址" class="headerlink" title="20.2 被换出的是页内容，不是物理地址"></a>20.2 被换出的是页内容，不是物理地址</h3><p>更严谨的说法是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">虚拟页对应的内容被写到 Swap；</span><br><span class="line">原物理页被释放；</span><br><span class="line">以后换入时可能使用新的物理页。</span><br></pre></td></tr></table></figure><p>不是“物理地址被 swap 出去了”。</p><h3 id="20-3-缺页异常不一定是非法访问"><a href="#20-3-缺页异常不一定是非法访问" class="headerlink" title="20.3 缺页异常不一定是非法访问"></a>20.3 缺页异常不一定是非法访问</h3><p>合法虚拟页尚未分配物理页、或者已被换出到 Swap，都会触发缺页异常。只有操作系统判断访问非法时，才会导致 Segmentation Fault。</p><h3 id="20-4-好策略不能根治内存严重不足"><a href="#20-4-好策略不能根治内存严重不足" class="headerlink" title="20.4 好策略不能根治内存严重不足"></a>20.4 好策略不能根治内存严重不足</h3><p>页替换策略可以减少缺页，但如果所有进程工作集总量超过物理内存容量，仍可能发生颠簸。</p><h3 id="20-5-工作集清出不等于立刻换出"><a href="#20-5-工作集清出不等于立刻换出" class="headerlink" title="20.5 工作集清出不等于立刻换出"></a>20.5 工作集清出不等于立刻换出</h3><p>一个页不属于工作集，表示它可以成为换出候选，不代表它一定马上被换出。</p><h3 id="20-6-LRU-和工作集模型不是同一层概念"><a href="#20-6-LRU-和工作集模型不是同一层概念" class="headerlink" title="20.6 LRU 和工作集模型不是同一层概念"></a>20.6 LRU 和工作集模型不是同一层概念</h3><p>LRU 是页替换策略；工作集模型是内存需求分析模型。二者都利用最近访问情况，但解决问题的层次不同。</p><hr><h2 id="❗-本节核心结论：-本讲最容易混淆的是“Swap-与物理内存”“Page-Fault-与-Segmentation-Fault”“页替换策略与工作集模型”这几组概念。"><a href="#❗-本节核心结论：-本讲最容易混淆的是“Swap-与物理内存”“Page-Fault-与-Segmentation-Fault”“页替换策略与工作集模型”这几组概念。" class="headerlink" title="❗ 本节核心结论： 本讲最容易混淆的是“Swap 与物理内存”“Page Fault 与 Segmentation Fault”“页替换策略与工作集模型”这几组概念。"></a><strong>❗ 本节核心结论：</strong> 本讲最容易混淆的是“Swap 与物理内存”“Page Fault 与 Segmentation Fault”“页替换策略与工作集模型”这几组概念。</h2><h2 id="21-自测题答案修正版"><a href="#21-自测题答案修正版" class="headerlink" title="21. 自测题答案修正版"></a>21. 自测题答案修正版</h2><h3 id="题-1：为什么说换页机制让虚拟内存使用不受物理内存大小严格限制？"><a href="#题-1：为什么说换页机制让虚拟内存使用不受物理内存大小严格限制？" class="headerlink" title="题 1：为什么说换页机制让虚拟内存使用不受物理内存大小严格限制？"></a>题 1：为什么说换页机制让虚拟内存使用不受物理内存大小严格限制？</h3><p>换页机制可以把暂时不用的页内容写入磁盘 Swap 区，从而释放物理页给当前更需要的虚拟页使用。因此，多个进程拥有的虚拟内存总量可以超过物理内存容量。</p><p>但这不表示所有虚拟页能同时驻留在物理内存中。任意时刻，CPU 真正能直接访问的仍然是物理内存中的页。如果活跃页总量超过物理内存容量，就可能频繁换页甚至颠簸。</p><h3 id="题-2：缺页异常一定说明程序访问了非法地址吗？"><a href="#题-2：缺页异常一定说明程序访问了非法地址吗？" class="headerlink" title="题 2：缺页异常一定说明程序访问了非法地址吗？"></a>题 2：缺页异常一定说明程序访问了非法地址吗？</h3><p>不一定。</p><p>缺页异常可能是因为访问了非法地址，也可能是访问了合法虚拟页，但该页尚未分配物理页，或者该页内容已经被换出到 Swap 区。</p><h3 id="题-3：FIFO-和-LRU-的区别是什么？"><a href="#题-3：FIFO-和-LRU-的区别是什么？" class="headerlink" title="题 3：FIFO 和 LRU 的区别是什么？"></a>题 3：FIFO 和 LRU 的区别是什么？</h3><p>FIFO 根据页面进入内存的先后顺序选择牺牲页，最早进入内存的页最先被换出。</p><p>LRU 根据最近访问情况选择牺牲页，最近最久没有被访问的页被换出。</p><h3 id="题-4：Second-Chance-为什么比-FIFO-更合理？"><a href="#题-4：Second-Chance-为什么比-FIFO-更合理？" class="headerlink" title="题 4：Second Chance 为什么比 FIFO 更合理？"></a>题 4：Second Chance 为什么比 FIFO 更合理？</h3><p>Second Chance 在 FIFO 队列基础上增加访问位。如果队头页最近被访问过，就清除访问位并移到队尾，暂时不换出它；如果队头页没有最近访问标记，才换出它。</p><p>因此，Second Chance 比 FIFO 更能避免换出最近仍然活跃的页。</p><h3 id="题-5：为什么-MIN-策略理论最优，但实际不可实现？"><a href="#题-5：为什么-MIN-策略理论最优，但实际不可实现？" class="headerlink" title="题 5：为什么 MIN 策略理论最优，但实际不可实现？"></a>题 5：为什么 MIN 策略理论最优，但实际不可实现？</h3><p>MIN 需要知道未来访问序列，才能选择未来最晚再访问或不再访问的页换出。但现实系统通常无法提前知道程序未来会访问哪些页，所以 MIN 只能作为理论最优基准。</p><h3 id="题-6：颠簸发生时，为什么-CPU-利用率或有效吞吐会变差？"><a href="#题-6：颠簸发生时，为什么-CPU-利用率或有效吞吐会变差？" class="headerlink" title="题 6：颠簸发生时，为什么 CPU 利用率或有效吞吐会变差？"></a>题 6：颠簸发生时，为什么 CPU 利用率或有效吞吐会变差？</h3><p>颠簸时，进程频繁缺页，大量时间消耗在缺页异常处理、等待磁盘 I&#x2F;O、换入换出和调度切换上。程序真正执行的时间变少，因此系统有效吞吐显著下降。</p><p>CPU 可能因为等待 I&#x2F;O 而空闲，也可能忙于内核态换页处理；但无论哪种情况，用户程序推进效率都会很差。</p><h3 id="题-7：工作集模型中的-t-x-t-是什么意思？"><a href="#题-7：工作集模型中的-t-x-t-是什么意思？" class="headerlink" title="题 7：工作集模型中的 [t - x, t] 是什么意思？"></a>题 7：工作集模型中的 <code>[t - x, t]</code> 是什么意思？</h3><p><code>[t - x, t]</code> 表示从过去的 <code>t - x</code> 时刻到当前 <code>t</code> 时刻的时间窗口。</p><p>在这个窗口内被访问过的页组成当前工作集。</p><h3 id="题-8：如果一个页很久没有被访问，工作集模型会如何处理它？"><a href="#题-8：如果一个页很久没有被访问，工作集模型会如何处理它？" class="headerlink" title="题 8：如果一个页很久没有被访问，工作集模型会如何处理它？"></a>题 8：如果一个页很久没有被访问，工作集模型会如何处理它？</h3><p>如果一个页超过时间窗口 <code>x</code> 都没有被访问，它就不再属于当前工作集。</p><p>这意味着它可以成为换出候选页，但不代表系统一定会立刻换出它。</p><hr><h2 id="❗-本节核心结论：-自测题的关键修正是：不要把-Swap-理解成物理内存，不要把-Page-Fault-等同于非法访问，也不要把“不属于工作集”理解成“必定立即换出”。"><a href="#❗-本节核心结论：-自测题的关键修正是：不要把-Swap-理解成物理内存，不要把-Page-Fault-等同于非法访问，也不要把“不属于工作集”理解成“必定立即换出”。" class="headerlink" title="❗ 本节核心结论： 自测题的关键修正是：不要把 Swap 理解成物理内存，不要把 Page Fault 等同于非法访问，也不要把“不属于工作集”理解成“必定立即换出”。"></a><strong>❗ 本节核心结论：</strong> 自测题的关键修正是：不要把 Swap 理解成物理内存，不要把 Page Fault 等同于非法访问，也不要把“不属于工作集”理解成“必定立即换出”。</h2><h2 id="22-复习清单"><a href="#22-复习清单" class="headerlink" title="22. 复习清单"></a>22. 复习清单</h2><p>学完《物理内存管理 II》后，应能回答以下问题：</p><ol><li>物理内存和磁盘分别是什么？</li><li>Swap 区的作用是什么？</li><li>为什么说 Swap 不是把磁盘变成物理内存？</li><li>为什么虚拟内存总量可以超过物理内存容量？</li><li>缺页异常有哪些可能原因？</li><li>Page Fault 和 Segmentation Fault 有什么区别？</li><li>换出一个页时，页表项会发生什么变化？</li><li>换入一个页时，为什么可能使用不同的物理页？</li><li>换入位置改变后，如何更新虚拟页到物理页的映射？</li><li>为什么修改页表后要考虑 TLB？</li><li>按需分配有什么好处和代价？</li><li>页替换策略解决什么问题？</li><li>MIN 为什么理论最优？</li><li>MIN 为什么现实中难以实现？</li><li>FIFO 的规则是什么？</li><li>FIFO 有什么缺点？</li><li>Second Chance 如何改进 FIFO？</li><li>LRU 的规则是什么？</li><li>LRU 为什么符合局部性原理？</li><li>MRU 在什么情况下可能有意义？</li><li>什么是颠簸？</li><li>为什么颠簸会导致系统有效吞吐下降？</li><li>好的页替换策略能否彻底消除颠簸？</li><li>工作集模型中的 <code>[t - x, t]</code> 是什么意思？</li><li>工作集模型和 LRU 有什么区别？</li><li>工作集模型相比 LRU 的优势是什么？</li><li>一个页不属于工作集是否意味着它一定马上被换出？</li></ol><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>《物理内存管理 II》的核心，是理解物理内存不足时操作系统如何维持系统运行。</p><p>这一讲可以压缩为一句话：</p><p><strong>当物理内存不足时，操作系统可以把暂时不用的页内容换出到磁盘 Swap 区；再次访问时通过缺页异常换入，并更新页表映射；内存满时，页替换策略决定换出哪个页；如果活跃页需求超过物理内存容量，就可能发生颠簸；工作集模型用于估计进程当前真正需要的页集合，从而帮助系统减少频繁换页。</strong></p><p>真正掌握这一讲，需要形成四个直觉：</p><ol><li><strong>Swap 不是内存，只是临时存放页内容的外存区域。</strong></li><li><strong>虚拟地址可以保持不变，但背后的物理页可以换出、换入、改变位置。</strong></li><li><strong>页替换策略是在估计“哪个页未来最不重要”。</strong></li><li><strong>工作集模型比单个替换策略更高一层，它关心进程当前需要多少活跃页。</strong></li></ol><p>因此，物理内存管理 II 不是简单地“内存不够就放磁盘”。它是一套围绕缺页异常、页表更新、替换策略、局部性和工作集控制构成的动态管理机制。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第7讲 物理内存管理I</title>
      <link href="/2026/06/11/%E7%AC%AC7%E8%AE%B2-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86I/"/>
      <url>/2026/06/11/%E7%AC%AC7%E8%AE%B2-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86I/</url>
      
        <content type="html"><![CDATA[<h1 id="物理内存管理-I：从空闲页记录到伙伴系统与-SLAB"><a href="#物理内存管理-I：从空闲页记录到伙伴系统与-SLAB" class="headerlink" title="物理内存管理 I：从空闲页记录到伙伴系统与 SLAB"></a>物理内存管理 I：从空闲页记录到伙伴系统与 SLAB</h1><p>虚拟内存管理回答的是“程序看到的虚拟地址如何映射到物理地址”。物理内存管理进一步回答的是：<strong>真实的物理内存页由谁管理、如何记录空闲状态、如何分配连续物理页、如何快速分配内核小对象。</strong></p><p>《物理内存管理 I》的重点不是换页、页替换或工作集模型，而是物理内存分配器本身。它讨论的是操作系统拿到一大片真实物理内存后，如何把它组织成可分配、可回收、可合并、可复用的资源。</p><p>本讲可以用一条主线概括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">物理内存</span><br><span class="line">↓</span><br><span class="line">位图分配器：用 bit 记录每个物理页是否空闲</span><br><span class="line">↓</span><br><span class="line">伙伴系统：高效分配 2^n 个连续物理页</span><br><span class="line">↓</span><br><span class="line">SLAB / SLUB：把页级大块切成小对象</span><br><span class="line">↓</span><br><span class="line">Linux 内核和用户态分配器共同使用</span><br></pre></td></tr></table></figure><hr><h2 id="❗-核心结论：-物理内存管理-I-的核心是“如何分配物理页”。位图、伙伴系统和-SLAB-SLUB-分别解决空闲页记录、连续页分配和小对象分配问题。"><a href="#❗-核心结论：-物理内存管理-I-的核心是“如何分配物理页”。位图、伙伴系统和-SLAB-SLUB-分别解决空闲页记录、连续页分配和小对象分配问题。" class="headerlink" title="❗ 核心结论： 物理内存管理 I 的核心是“如何分配物理页”。位图、伙伴系统和 SLAB&#x2F;SLUB 分别解决空闲页记录、连续页分配和小对象分配问题。"></a><strong>❗ 核心结论：</strong> 物理内存管理 I 的核心是“如何分配物理页”。位图、伙伴系统和 SLAB&#x2F;SLUB 分别解决空闲页记录、连续页分配和小对象分配问题。</h2><h2 id="1-物理内存管理-I-解决什么问题"><a href="#1-物理内存管理-I-解决什么问题" class="headerlink" title="1. 物理内存管理 I 解决什么问题"></a>1. 物理内存管理 I 解决什么问题</h2><p>物理内存可以粗略看成一组连续编号的物理页。假设页大小为 4KB，那么系统可以把物理内存划分为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">物理页 0</span><br><span class="line">物理页 1</span><br><span class="line">物理页 2</span><br><span class="line">物理页 3</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>操作系统需要回答几个问题：</p><ul><li>哪些物理页是空闲的？</li><li>哪些物理页已经被分配？</li><li>如果需要连续 4 个物理页，应该从哪里找？</li><li>分配后如何记录状态？</li><li>释放后如何恢复空闲状态？</li><li>内核只需要几十字节对象时，是否也要分配一整页？</li></ul><p>因此，本讲可以分为三个层次：</p><table><thead><tr><th>层次</th><th>解决的问题</th><th>典型单位</th></tr></thead><tbody><tr><td>位图分配器</td><td>如何记录每个物理页是否空闲</td><td>4KB 页</td></tr><tr><td>伙伴系统</td><td>如何高效分配连续物理页</td><td><code>2^n</code> 个页</td></tr><tr><td>SLAB &#x2F; SLUB</td><td>如何快速分配内核小对象</td><td>几十或几百字节</td></tr></tbody></table><p>物理内存管理 II 主要讨论“物理内存不够时怎么办”，例如页面换出、页替换策略和工作集模型。本讲只讨论“物理内存如何分配”。</p><hr><h2 id="❗-本节核心结论：-物理内存管理-I-关注的是物理页的分配与回收，不重点讨论内存不足时的换页策略。"><a href="#❗-本节核心结论：-物理内存管理-I-关注的是物理页的分配与回收，不重点讨论内存不足时的换页策略。" class="headerlink" title="❗ 本节核心结论： 物理内存管理 I 关注的是物理页的分配与回收，不重点讨论内存不足时的换页策略。"></a><strong>❗ 本节核心结论：</strong> 物理内存管理 I 关注的是物理页的分配与回收，不重点讨论内存不足时的换页策略。</h2><h2 id="2-评价维度：利用率、速度与碎片"><a href="#2-评价维度：利用率、速度与碎片" class="headerlink" title="2. 评价维度：利用率、速度与碎片"></a>2. 评价维度：利用率、速度与碎片</h2><p>物理内存分配器通常要在几个目标之间权衡：</p><ol><li><strong>分配速度</strong>：申请内存时能否快速找到合适区域。</li><li><strong>释放速度</strong>：释放内存后能否快速恢复管理结构。</li><li><strong>内存利用率</strong>：分配出去的空间是否尽量接近真实需求。</li><li><strong>碎片控制</strong>：系统运行一段时间后，是否仍能分配较大的连续空间。</li></ol><p>碎片主要分为两类：外部碎片和内部碎片。</p><table><thead><tr><th>类型</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td>外部碎片</td><td>空闲空间总量足够，但分散在不同位置，无法满足连续分配</td><td>有很多 4KB 空闲页，但无法找到连续 16KB</td></tr><tr><td>内部碎片</td><td>分配出去的块比实际需求大，多出来的部分浪费在块内部</td><td>申请 15KB，系统分配 16KB，多出 1KB</td></tr></tbody></table><p>可以用一个直观类比理解：</p><table><thead><tr><th>类比</th><th>含义</th></tr></thead><tbody><tr><td>外部碎片</td><td>酒店有多个空房间，但不连续；客人要求住连续房间，所以安排不了</td></tr><tr><td>内部碎片</td><td>客人只需要单人间，但酒店只能给双人间，多出来的床位浪费了</td></tr></tbody></table><p>这两个碎片问题贯穿整节课。位图分配器容易遇到连续页查找困难；伙伴系统通过拆分和合并缓解外部碎片，但会引入内部碎片；SLAB&#x2F;SLUB 则是为了减少小对象分配造成的内部浪费。</p><hr><h2 id="❗-本节核心结论：-物理内存分配器的设计核心是权衡速度、利用率、外部碎片和内部碎片。"><a href="#❗-本节核心结论：-物理内存分配器的设计核心是权衡速度、利用率、外部碎片和内部碎片。" class="headerlink" title="❗ 本节核心结论： 物理内存分配器的设计核心是权衡速度、利用率、外部碎片和内部碎片。"></a><strong>❗ 本节核心结论：</strong> 物理内存分配器的设计核心是权衡速度、利用率、外部碎片和内部碎片。</h2><h2 id="3-位图分配器：最朴素的物理页管理方式"><a href="#3-位图分配器：最朴素的物理页管理方式" class="headerlink" title="3. 位图分配器：最朴素的物理页管理方式"></a>3. 位图分配器：最朴素的物理页管理方式</h2><p>位图分配器的思想非常直接：<strong>用一个 bit 表示一个物理页是否空闲。</strong></p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bit bitmap[N];</span><br></pre></td></tr></table></figure><p>如果系统有 <code>N</code> 个 4KB 物理页，那么：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">bitmap[i] = 0 表示第 i 个物理页空闲</span><br><span class="line">bitmap[i] = 1 表示第 i 个物理页已分配</span><br></pre></td></tr></table></figure><h3 id="3-1-分配连续物理页"><a href="#3-1-分配连续物理页" class="headerlink" title="3.1 分配连续物理页"></a>3.1 分配连续物理页</h3><p>如果要分配 <code>n</code> 个连续物理页，位图分配器需要扫描 bitmap，寻找连续 <code>n</code> 个 0：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">扫描 bitmap</span><br><span class="line">↓</span><br><span class="line">找到连续 n 个 0</span><br><span class="line">↓</span><br><span class="line">把这 n 个 bit 设为 1</span><br><span class="line">↓</span><br><span class="line">返回第一个物理页的起始地址</span><br></pre></td></tr></table></figure><p>伪代码可以写成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (bitmap[i] 到 bitmap[i + n - <span class="number">1</span>] 全部为 <span class="number">0</span>) &#123;</span><br><span class="line">        把这些 bit 全部设为 <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span> FREE_MEM_START + i * PAGE_SIZE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-2-释放连续物理页"><a href="#3-2-释放连续物理页" class="headerlink" title="3.2 释放连续物理页"></a>3.2 释放连续物理页</h3><p>释放时，操作系统根据物理地址反推出页号：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">page_idx = (addr - FREE_MEM_START) / PAGE_SIZE</span><br></pre></td></tr></table></figure><p>然后把对应的 bit 改回 0：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bitmap[page_idx] = 0</span><br><span class="line">bitmap[page_idx + 1] = 0</span><br><span class="line">...</span><br></pre></td></tr></table></figure><h3 id="3-3-位图分配器的优缺点"><a href="#3-3-位图分配器的优缺点" class="headerlink" title="3.3 位图分配器的优缺点"></a>3.3 位图分配器的优缺点</h3><p>位图分配器的优点是：</p><ul><li>结构简单；</li><li>容易理解；</li><li>记录状态的空间开销较小；</li><li>每个页是否空闲一目了然。</li></ul><p>缺点也很明显：</p><ul><li>找连续页时可能需要线性扫描；</li><li>系统运行久了以后，空闲页可能分散；</li><li>连续大块分配可能变慢或失败。</li></ul><p>例如 bitmap 状态为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1 0 1 0 0 1 0 1 0 0 0 1</span><br></pre></td></tr></table></figure><p>如果系统要找连续很多个 0，就可能扫描很久。</p><hr><h2 id="❗-本节核心结论：-位图分配器适合说明“如何记录物理页状态”，但查找连续空闲页可能较慢。"><a href="#❗-本节核心结论：-位图分配器适合说明“如何记录物理页状态”，但查找连续空闲页可能较慢。" class="headerlink" title="❗ 本节核心结论： 位图分配器适合说明“如何记录物理页状态”，但查找连续空闲页可能较慢。"></a><strong>❗ 本节核心结论：</strong> 位图分配器适合说明“如何记录物理页状态”，但查找连续空闲页可能较慢。</h2><h2 id="4-伙伴系统：连续物理页分配的核心机制"><a href="#4-伙伴系统：连续物理页分配的核心机制" class="headerlink" title="4. 伙伴系统：连续物理页分配的核心机制"></a>4. 伙伴系统：连续物理页分配的核心机制</h2><p>伙伴系统是更接近真实操作系统的页级分配机制。它的核心思想是：<strong>只按 2 的幂大小分配连续物理页块。</strong></p><p>假设页大小为 4KB，那么伙伴系统管理的块大小通常是：</p><table><thead><tr><th align="right">阶数</th><th align="right">页数</th><th align="right">大小</th></tr></thead><tbody><tr><td align="right">0</td><td align="right">1 页</td><td align="right">4KB</td></tr><tr><td align="right">1</td><td align="right">2 页</td><td align="right">8KB</td></tr><tr><td align="right">2</td><td align="right">4 页</td><td align="right">16KB</td></tr><tr><td align="right">3</td><td align="right">8 页</td><td align="right">32KB</td></tr><tr><td align="right">4</td><td align="right">16 页</td><td align="right">64KB</td></tr></tbody></table><p>因此，伙伴系统不直接分配 7KB、15KB、17KB 这种任意大小，而是向上取整到合适的 <code>2^n</code> 页块。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">申请 7KB  → 分配 8KB</span><br><span class="line">申请 15KB → 分配 16KB</span><br><span class="line">申请 17KB → 分配 32KB</span><br></pre></td></tr></table></figure><p>这会产生一定内部碎片，但换来更简单、更高效的管理结构。</p><hr><h2 id="❗-本节核心结论：-伙伴系统用-2-n-个连续页作为分配单位，用规则化的块大小换取更高效的分配和合并。"><a href="#❗-本节核心结论：-伙伴系统用-2-n-个连续页作为分配单位，用规则化的块大小换取更高效的分配和合并。" class="headerlink" title="❗ 本节核心结论： 伙伴系统用 2^n 个连续页作为分配单位，用规则化的块大小换取更高效的分配和合并。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统用 <code>2^n</code> 个连续页作为分配单位，用规则化的块大小换取更高效的分配和合并。</h2><h2 id="5-分配时拆分，释放时合并：伙伴系统好在哪里"><a href="#5-分配时拆分，释放时合并：伙伴系统好在哪里" class="headerlink" title="5. 分配时拆分，释放时合并：伙伴系统好在哪里"></a>5. 分配时拆分，释放时合并：伙伴系统好在哪里</h2><p>伙伴系统最重要的一句话是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">分配时不断拆分；释放时尽量合并。</span><br></pre></td></tr></table></figure><p>这个设计的好处在于：<strong>既能避免小请求浪费大块，又能在释放后尽量恢复大块连续空间。</strong></p><h3 id="5-1-分配时拆分：避免大块被小请求浪费"><a href="#5-1-分配时拆分：避免大块被小请求浪费" class="headerlink" title="5.1 分配时拆分：避免大块被小请求浪费"></a>5.1 分配时拆分：避免大块被小请求浪费</h3><p>假设当前只有一个 32KB 空闲块：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[              32KB              ]</span><br></pre></td></tr></table></figure><p>现在程序申请 7KB。伙伴系统需要分配 8KB，但当前没有 8KB 块，于是它会拆分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">[              32KB              ]</span><br><span class="line">↓</span><br><span class="line">[      16KB      ][      16KB      ]</span><br><span class="line">↓</span><br><span class="line">[  8KB  ][  8KB  ][      16KB      ]</span><br></pre></td></tr></table></figure><p>然后分配一个 8KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[8KB 已分配][8KB 空闲][16KB 空闲]</span><br></pre></td></tr></table></figure><p>如果不拆分，系统可能直接把 32KB 给这个 7KB 请求，浪费太多。拆分让系统尽量给出“刚好够用”的 <code>2^n</code> 大小块。</p><h3 id="5-2-释放时合并：缓解外部碎片"><a href="#5-2-释放时合并：缓解外部碎片" class="headerlink" title="5.2 释放时合并：缓解外部碎片"></a>5.2 释放时合并：缓解外部碎片</h3><p>如果刚才分配出去的 8KB 被释放，并且它的 8KB 伙伴也空闲：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[8KB 空闲][8KB 空闲][16KB 空闲]</span><br></pre></td></tr></table></figure><p>就可以合并成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[      16KB 空闲      ][16KB 空闲]</span><br></pre></td></tr></table></figure><p>如果这两个 16KB 也是伙伴，还可以继续合并：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[              32KB 空闲              ]</span><br></pre></td></tr></table></figure><p>这就是伙伴系统缓解外部碎片的关键。它不会让释放后的空闲块永远碎成小块，而是尽量合并回大块。</p><h3 id="5-3-为什么伙伴容易找到"><a href="#5-3-为什么伙伴容易找到" class="headerlink" title="5.3 为什么伙伴容易找到"></a>5.3 为什么伙伴容易找到</h3><p>伙伴系统中的“伙伴”不是随便找的。两个互为伙伴的块：</p><ul><li>大小相同；</li><li>物理地址相邻；</li><li>由同一个更大块拆分而来。</li></ul><p>因此，释放某个块时，系统可以根据地址和块大小快速计算它的伙伴地址。抽象公式可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">buddy_addr = block_addr ^ block_size</span><br></pre></td></tr></table></figure><p>这意味着系统不必全局搜索“谁能和我合并”，而是能直接定位候选伙伴。</p><h3 id="5-4-这个方法的本质权衡"><a href="#5-4-这个方法的本质权衡" class="headerlink" title="5.4 这个方法的本质权衡"></a>5.4 这个方法的本质权衡</h3><table><thead><tr><th>操作</th><th>方法</th><th>好处</th></tr></thead><tbody><tr><td>分配</td><td>大块拆成小块</td><td>尽量减少过度分配</td></tr><tr><td>释放</td><td>小块合并成大块</td><td>缓解外部碎片</td></tr><tr><td>查找伙伴</td><td>通过地址计算</td><td>合并效率较高</td></tr><tr><td>块大小</td><td>固定为 2 的幂</td><td>数据结构简单</td></tr></tbody></table><p>但它也有代价：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">申请 17KB</span><br><span class="line">↓</span><br><span class="line">分配 32KB</span><br><span class="line">↓</span><br><span class="line">浪费 15KB</span><br></pre></td></tr></table></figure><p>所以伙伴系统本质上是一个 tradeoff：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">牺牲一定内部碎片</span><br><span class="line">↓</span><br><span class="line">换取更快分配、更快释放、更容易合并、更适合连续物理页管理</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-伙伴系统通过“分配时拆分、释放时合并”在分配速度、连续空间恢复和内部碎片之间取得平衡。"><a href="#❗-本节核心结论：-伙伴系统通过“分配时拆分、释放时合并”在分配速度、连续空间恢复和内部碎片之间取得平衡。" class="headerlink" title="❗ 本节核心结论： 伙伴系统通过“分配时拆分、释放时合并”在分配速度、连续空间恢复和内部碎片之间取得平衡。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统通过“分配时拆分、释放时合并”在分配速度、连续空间恢复和内部碎片之间取得平衡。</h2><h2 id="6-链表数组：伙伴系统如何组织空闲块"><a href="#6-链表数组：伙伴系统如何组织空闲块" class="headerlink" title="6. 链表数组：伙伴系统如何组织空闲块"></a>6. 链表数组：伙伴系统如何组织空闲块</h2><p>伙伴系统通常使用“链表数组”管理空闲块。</p><p>所谓链表数组，就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">一个数组，数组的每个元素是一条链表。</span><br><span class="line">不同链表管理不同大小的空闲块。</span><br></pre></td></tr></table></figure><p>假设页大小为 4KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">free_area[0]：管理 1 页块，也就是 4KB</span><br><span class="line">free_area[1]：管理 2 页块，也就是 8KB</span><br><span class="line">free_area[2]：管理 4 页块，也就是 16KB</span><br><span class="line">free_area[3]：管理 8 页块，也就是 32KB</span><br><span class="line">free_area[4]：管理 16 页块，也就是 64KB</span><br></pre></td></tr></table></figure><p>可以表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">free_area[0] -&gt; 4KB块 -&gt; 4KB块 -&gt; NULL</span><br><span class="line"></span><br><span class="line">free_area[1] -&gt; 8KB块 -&gt; 8KB块 -&gt; NULL</span><br><span class="line"></span><br><span class="line">free_area[2] -&gt; 16KB块 -&gt; NULL</span><br><span class="line"></span><br><span class="line">free_area[3] -&gt; 32KB块 -&gt; 32KB块 -&gt; NULL</span><br></pre></td></tr></table></figure><h3 id="6-1-为什么用“数组-链表”"><a href="#6-1-为什么用“数组-链表”" class="headerlink" title="6.1 为什么用“数组 + 链表”"></a>6.1 为什么用“数组 + 链表”</h3><p>数组的作用是快速定位大小。</p><p>例如申请 8KB，系统直接看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">free_area[1]</span><br></pre></td></tr></table></figure><p>如果 <code>free_area[1]</code> 有空闲块，直接取一个。</p><p>如果没有，就向更高阶查找：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">free_area[2]：16KB</span><br><span class="line">free_area[3]：32KB</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>找到更大块后再拆分。</p><p>链表的作用是保存多个同大小的空闲块。同一种大小可能有很多块，链表适合把它们串起来。</p><h3 id="6-2-申请-7KB-的例子"><a href="#6-2-申请-7KB-的例子" class="headerlink" title="6.2 申请 7KB 的例子"></a>6.2 申请 7KB 的例子</h3><p>7KB 向上取整为 8KB，因此需要找 <code>free_area[1]</code>。</p><p>如果 <code>free_area[1]</code> 有空闲块：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">free_area[1] -&gt; [8KB] -&gt; [8KB]</span><br></pre></td></tr></table></figure><p>直接取一个。</p><p>如果没有 8KB，但有 16KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">free_area[2] -&gt; [16KB]</span><br></pre></td></tr></table></figure><p>就拆成两个 8KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[16KB]</span><br><span class="line">↓</span><br><span class="line">[8KB][8KB]</span><br></pre></td></tr></table></figure><p>其中一个分配出去，另一个挂回 <code>free_area[1]</code>。</p><h3 id="6-3-释放-8KB-的例子"><a href="#6-3-释放-8KB-的例子" class="headerlink" title="6.3 释放 8KB 的例子"></a>6.3 释放 8KB 的例子</h3><p>释放一个 8KB 块时，系统会：</p><ol><li>计算它的 8KB 伙伴地址；</li><li>检查伙伴是否空闲；</li><li>如果伙伴空闲，把两个 8KB 合并成 16KB；</li><li>继续检查 16KB 块的伙伴；</li><li>能合并就继续向上合并；</li><li>不能合并时，把最终块挂到对应 <code>free_area[k]</code> 链表中。</li></ol><p>这个过程让伙伴系统可以在释放时尽量恢复大块连续空间。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[释放 8KB 块] --&gt; B[计算 8KB 伙伴地址]    B --&gt; C{伙伴是否空闲?}    C -- 否 --&gt; D[挂入 free_area[1]]    C -- 是 --&gt; E[合并为 16KB]    E --&gt; F[计算 16KB 伙伴地址]    F --&gt; G{伙伴是否空闲?}    G -- 否 --&gt; H[挂入 free_area[2]]    G -- 是 --&gt; I[继续向上合并]  </pre></div><p>这张图强调：伙伴系统释放时不是简单“放回链表”，而是先尽量合并，直到无法继续合并为止。</p><hr><h2 id="❗-本节核心结论：-伙伴系统的链表数组用数组区分块大小，用链表保存同大小空闲块，从而支持快速查找、拆分和合并。"><a href="#❗-本节核心结论：-伙伴系统的链表数组用数组区分块大小，用链表保存同大小空闲块，从而支持快速查找、拆分和合并。" class="headerlink" title="❗ 本节核心结论： 伙伴系统的链表数组用数组区分块大小，用链表保存同大小空闲块，从而支持快速查找、拆分和合并。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统的链表数组用数组区分块大小，用链表保存同大小空闲块，从而支持快速查找、拆分和合并。</h2><h2 id="7-位图和伙伴系统不是上下级关系"><a href="#7-位图和伙伴系统不是上下级关系" class="headerlink" title="7. 位图和伙伴系统不是上下级关系"></a>7. 位图和伙伴系统不是上下级关系</h2><p>位图和伙伴系统容易被误认为是上下级关系，但更准确的说法是：<strong>它们是同一层的两种页级物理内存分配方案。</strong></p><p>它们都解决类似问题：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">如何记录空闲物理页？</span><br><span class="line">如何分配连续物理页？</span><br><span class="line">如何释放物理页？</span><br></pre></td></tr></table></figure><p>区别在于实现方式不同。</p><table><thead><tr><th>对比项</th><th>位图分配器</th><th>伙伴系统</th></tr></thead><tbody><tr><td>管理单位</td><td>单个物理页</td><td><code>2^n</code> 个连续页组成的块</td></tr><tr><td>空闲状态记录</td><td>每页一个 bit</td><td>每阶一条空闲链表</td></tr><tr><td>分配方式</td><td>扫描 bitmap 找连续 0</td><td>按阶查链表，不够则拆大块</td></tr><tr><td>释放方式</td><td>对应 bit 置 0</td><td>尝试与伙伴合并</td></tr><tr><td>优点</td><td>简单、省空间</td><td>更适合连续页分配和回收</td></tr><tr><td>缺点</td><td>找连续页可能慢</td><td>有内部碎片，结构更复杂</td></tr></tbody></table><p>真正明显的上下级关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">物理内存</span><br><span class="line">↓</span><br><span class="line">伙伴系统</span><br><span class="line">↓</span><br><span class="line">SLAB / SLUB</span><br><span class="line">↓</span><br><span class="line">内核小对象</span><br></pre></td></tr></table></figure><p>也就是说：</p><ul><li>位图和伙伴系统是同层替代方案；</li><li>伙伴系统和 SLAB&#x2F;SLUB 是上下级关系。</li></ul><hr><h2 id="❗-本节核心结论：-位图和伙伴系统都是页级物理内存分配方法，不是上下级；伙伴系统与-SLAB-SLUB-才是上下级关系。"><a href="#❗-本节核心结论：-位图和伙伴系统都是页级物理内存分配方法，不是上下级；伙伴系统与-SLAB-SLUB-才是上下级关系。" class="headerlink" title="❗ 本节核心结论： 位图和伙伴系统都是页级物理内存分配方法，不是上下级；伙伴系统与 SLAB&#x2F;SLUB 才是上下级关系。"></a><strong>❗ 本节核心结论：</strong> 位图和伙伴系统都是页级物理内存分配方法，不是上下级；伙伴系统与 SLAB&#x2F;SLUB 才是上下级关系。</h2><h2 id="8-为什么伙伴系统之上还需要-SLAB-SLUB"><a href="#8-为什么伙伴系统之上还需要-SLAB-SLUB" class="headerlink" title="8. 为什么伙伴系统之上还需要 SLAB &#x2F; SLUB"></a>8. 为什么伙伴系统之上还需要 SLAB &#x2F; SLUB</h2><p>伙伴系统已经能分配物理页，为什么还需要 SLAB 或 SLUB？</p><p>原因是：<strong>伙伴系统的最小单位通常是一个物理页，而内核经常需要分配远小于一页的小对象。</strong></p><p>例如内核中经常分配：</p><ul><li><code>struct task_struct</code></li><li><code>struct file</code></li><li><code>struct inode</code></li><li><code>struct vm_area_struct</code></li><li>各类缓存对象和描述符</li></ul><p>这些对象可能只有几十字节或几百字节。如果每次都从伙伴系统分配一个 4KB 页，会造成严重内部碎片。</p><p>例如申请 128 字节对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">伙伴系统最小分配 4KB = 4096B</span><br><span class="line">实际需要 128B</span><br><span class="line">浪费 4096 - 128 = 3968B</span><br></pre></td></tr></table></figure><p>因此需要在伙伴系统之上再建立一层小对象分配器。</p><p>SLAB&#x2F;SLUB 的基本思想是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">先从伙伴系统拿一批页</span><br><span class="line">↓</span><br><span class="line">把这批页切成固定大小的小对象</span><br><span class="line">↓</span><br><span class="line">内核申请小对象时，从对象池里快速取出</span><br></pre></td></tr></table></figure><p>例如从伙伴系统拿到 4KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[              4KB              ]</span><br></pre></td></tr></table></figure><p>如果这是 128B 对象池，就切成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[128B][128B][128B][128B]...[128B]</span><br></pre></td></tr></table></figure><p>以后内核申请 128B 对象，就不必再直接找伙伴系统，而是从这个池子中取一个对象。</p><hr><h2 id="❗-本节核心结论：-伙伴系统适合页级大块分配；SLAB-SLUB-适合内核小对象分配，能显著减少小对象导致的内部碎片。"><a href="#❗-本节核心结论：-伙伴系统适合页级大块分配；SLAB-SLUB-适合内核小对象分配，能显著减少小对象导致的内部碎片。" class="headerlink" title="❗ 本节核心结论： 伙伴系统适合页级大块分配；SLAB&#x2F;SLUB 适合内核小对象分配，能显著减少小对象导致的内部碎片。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统适合页级大块分配；SLAB&#x2F;SLUB 适合内核小对象分配，能显著减少小对象导致的内部碎片。</h2><h2 id="9-SLAB-SLUB-的核心结构：对象池与-slab"><a href="#9-SLAB-SLUB-的核心结构：对象池与-slab" class="headerlink" title="9. SLAB &#x2F; SLUB 的核心结构：对象池与 slab"></a>9. SLAB &#x2F; SLUB 的核心结构：对象池与 slab</h2><p>SLAB 家族包括 SLAB、SLUB、SLOB 等实现。本讲重点可以抓住 SLUB 的思想：<strong>按对象大小建立缓存池，每个缓存池管理若干 slab。</strong></p><p>一个 slab 可以理解为：从伙伴系统拿到的一块连续内存，再切成固定大小对象。</p><p>例如一个 128B 对象缓存池：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">kmem_cache: 128B 对象池</span><br><span class="line">↓</span><br><span class="line">slab A: [128B][128B][128B]...</span><br><span class="line">slab B: [128B][128B][128B]...</span><br><span class="line">slab C: [128B][128B][128B]...</span><br></pre></td></tr></table></figure><p>如果内核申请 100B，系统可能从 128B 对象池中分配一个对象。这样虽然仍有少量内部碎片，但比直接分配 4KB 页小得多。</p><h3 id="9-1-SLUB-为什么快"><a href="#9-1-SLUB-为什么快" class="headerlink" title="9.1 SLUB 为什么快"></a>9.1 SLUB 为什么快</h3><p>SLUB 快的原因主要有两个：</p><ol><li><strong>复用对象池</strong>：不需要每次都向伙伴系统申请页。</li><li><strong>固定大小对象</strong>：分配和释放可以通过简单链表操作完成。</li></ol><p>分配 128B 对象时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">找到 128B kmem_cache</span><br><span class="line">↓</span><br><span class="line">从当前 slab 的空闲对象链表取出一个对象</span><br><span class="line">↓</span><br><span class="line">返回对象地址</span><br></pre></td></tr></table></figure><p>释放时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">把对象插回所在 slab 的空闲对象链表</span><br></pre></td></tr></table></figure><p>很多操作可以做到接近 O(1)。</p><hr><h2 id="❗-本节核心结论：-SLAB-SLUB-通过对象池复用固定大小小块，避免频繁调用伙伴系统，从而提高内核小对象分配速度。"><a href="#❗-本节核心结论：-SLAB-SLUB-通过对象池复用固定大小小块，避免频繁调用伙伴系统，从而提高内核小对象分配速度。" class="headerlink" title="❗ 本节核心结论： SLAB&#x2F;SLUB 通过对象池复用固定大小小块，避免频繁调用伙伴系统，从而提高内核小对象分配速度。"></a><strong>❗ 本节核心结论：</strong> SLAB&#x2F;SLUB 通过对象池复用固定大小小块，避免频繁调用伙伴系统，从而提高内核小对象分配速度。</h2><h2 id="10-SLAB-SLUB-中的链表结构"><a href="#10-SLAB-SLUB-中的链表结构" class="headerlink" title="10. SLAB&#x2F;SLUB 中的链表结构"></a>10. SLAB&#x2F;SLUB 中的链表结构</h2><p>SLAB&#x2F;SLUB 中也大量使用链表结构，而且至少有两层链表思想：</p><ol><li><strong>slab 之间的链表</strong>；</li><li><strong>slab 内部空闲对象链表</strong>。</li></ol><h3 id="10-1-slab-之间的链表：current、partial、full"><a href="#10-1-slab-之间的链表：current、partial、full" class="headerlink" title="10.1 slab 之间的链表：current、partial、full"></a>10.1 slab 之间的链表：current、partial、full</h3><p>在 SLUB 的抽象模型中，一个对象缓存池可以维护三类 slab：</p><table><thead><tr><th>名称</th><th>含义</th></tr></thead><tbody><tr><td><code>current</code></td><td>当前优先使用的 slab</td></tr><tr><td><code>partial</code></td><td>还有空闲对象、但不是全空或全满的 slab 链表</td></tr><tr><td><code>full</code></td><td>已经没有空闲对象的 slab 链表</td></tr></tbody></table><p>可以表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">current</span><br><span class="line">   ↓</span><br><span class="line">[ slab A：正在使用 ]</span><br><span class="line"></span><br><span class="line">partial</span><br><span class="line">   ↓</span><br><span class="line">[ slab B：还有空闲对象 ] -&gt; [ slab C：还有空闲对象 ] -&gt; NULL</span><br><span class="line"></span><br><span class="line">full</span><br><span class="line">   ↓</span><br><span class="line">[ slab D：已经满了 ] -&gt; [ slab E：已经满了 ] -&gt; NULL</span><br></pre></td></tr></table></figure><p>分配时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">优先从 current slab 分配对象</span><br><span class="line">↓</span><br><span class="line">如果 current 满了，把它移到 full</span><br><span class="line">↓</span><br><span class="line">从 partial 取一个 slab 作为新的 current</span><br><span class="line">↓</span><br><span class="line">如果 partial 也没有，就向伙伴系统申请新的 slab</span><br></pre></td></tr></table></figure><p>释放时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">对象回到所在 slab</span><br><span class="line">↓</span><br><span class="line">如果原来是 full，现在变得不满，则移回 partial</span><br><span class="line">↓</span><br><span class="line">如果某个 slab 全空，可能归还给伙伴系统</span><br></pre></td></tr></table></figure><h3 id="10-2-slab-内部的空闲对象链表"><a href="#10-2-slab-内部的空闲对象链表" class="headerlink" title="10.2 slab 内部的空闲对象链表"></a>10.2 slab 内部的空闲对象链表</h3><p>一个 slab 内部被切成许多固定大小对象。例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">slab A:</span><br><span class="line">[128B][128B][128B][128B][128B][128B]</span><br></pre></td></tr></table></figure><p>其中部分对象已分配，部分对象空闲：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[已用][空闲][已用][空闲][空闲][已用]</span><br></pre></td></tr></table></figure><p>这些空闲对象会被串成空闲链表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">free_list</span><br><span class="line">   ↓</span><br><span class="line">[空闲对象1] -&gt; [空闲对象2] -&gt; [空闲对象3] -&gt; NULL</span><br></pre></td></tr></table></figure><p>分配对象时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">obj = free_list.head</span><br><span class="line">free_list.head = obj-&gt;next</span><br><span class="line">return obj</span><br></pre></td></tr></table></figure><p>释放对象时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">obj-&gt;next = free_list.head</span><br><span class="line">free_list.head = obj</span><br></pre></td></tr></table></figure><p>这样分配和释放都很快。</p><h3 id="10-3-空闲对象的-next-指针存在哪里"><a href="#10-3-空闲对象的-next-指针存在哪里" class="headerlink" title="10.3 空闲对象的 next 指针存在哪里"></a>10.3 空闲对象的 next 指针存在哪里</h3><p>一个重要细节是：空闲对象本身已经不被使用，所以可以把它自身开头的几个字节拿来存 <code>next</code> 指针。</p><p>例如一个 128B 空闲对象可以看成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[ next 指针 | 剩余空间................ ]</span><br></pre></td></tr></table></figure><p>当对象被分配出去时，这 128B 全部交给使用者。当对象被释放回来后，它又可以重新作为链表节点，开头位置保存下一个空闲对象的地址。</p><p>因此，SLUB 不需要为每个空闲小对象额外分配独立链表节点。</p><h3 id="10-4-SLUB-的整体结构"><a href="#10-4-SLUB-的整体结构" class="headerlink" title="10.4 SLUB 的整体结构"></a>10.4 SLUB 的整体结构</h3><p>可以把 SLUB 的对象缓存池表示为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">kmem_cache：128B 对象池</span><br><span class="line">│</span><br><span class="line">├── current</span><br><span class="line">│      ↓</span><br><span class="line">│   slab A</span><br><span class="line">│   ├── 128B 已用</span><br><span class="line">│   ├── 128B 空闲 -&gt; free_list</span><br><span class="line">│   ├── 128B 已用</span><br><span class="line">│   └── 128B 空闲</span><br><span class="line">│</span><br><span class="line">├── partial</span><br><span class="line">│      ↓</span><br><span class="line">│   slab B -&gt; slab C -&gt; slab D</span><br><span class="line">│</span><br><span class="line">└── full</span><br><span class="line">       ↓</span><br><span class="line">    slab E -&gt; slab F</span><br></pre></td></tr></table></figure><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    KC[kmem_cache: 128B 对象池] --&gt; CUR[current slab]    KC --&gt; PAR[partial slab 链表]    KC --&gt; FULL[full slab 链表]    CUR --&gt; O1[已用对象]    CUR --&gt; O2[空闲对象]    O2 --&gt; O3[下一个空闲对象]    O3 --&gt; O4[下一个空闲对象]    PAR --&gt; S1[slab B]    S1 --&gt; S2[slab C]    FULL --&gt; F1[slab D]    F1 --&gt; F2[slab E]  </pre></div><p>这张图强调：SLUB 既要管理 slab 之间的状态，也要管理每个 slab 内部的空闲对象。</p><hr><h2 id="❗-本节核心结论：-SLAB-SLUB-中既有-slab-之间的链表，也有-slab-内部的空闲对象链表；这种结构让小对象分配和释放非常快。"><a href="#❗-本节核心结论：-SLAB-SLUB-中既有-slab-之间的链表，也有-slab-内部的空闲对象链表；这种结构让小对象分配和释放非常快。" class="headerlink" title="❗ 本节核心结论： SLAB&#x2F;SLUB 中既有 slab 之间的链表，也有 slab 内部的空闲对象链表；这种结构让小对象分配和释放非常快。"></a><strong>❗ 本节核心结论：</strong> SLAB&#x2F;SLUB 中既有 slab 之间的链表，也有 slab 内部的空闲对象链表；这种结构让小对象分配和释放非常快。</h2><h2 id="11-伙伴系统与-SLAB-SLUB-的关系"><a href="#11-伙伴系统与-SLAB-SLUB-的关系" class="headerlink" title="11. 伙伴系统与 SLAB&#x2F;SLUB 的关系"></a>11. 伙伴系统与 SLAB&#x2F;SLUB 的关系</h2><p>伙伴系统和 SLAB&#x2F;SLUB 是上下级关系。</p><p>伙伴系统负责页级别的连续物理内存分配：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">4KB、8KB、16KB、32KB、64KB...</span><br></pre></td></tr></table></figure><p>SLAB&#x2F;SLUB 从伙伴系统拿到页级大块，再切成小对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">32B、64B、128B、256B、512B...</span><br></pre></td></tr></table></figure><p>可以表示为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    PM[物理内存] --&gt; BUDDY[伙伴系统：页级连续块]    BUDDY --&gt; SLUB[SLAB&#x2F;SLUB：小对象分配器]    SLUB --&gt; OBJ1[task_struct]    SLUB --&gt; OBJ2[file]    SLUB --&gt; OBJ3[inode]    SLUB --&gt; OBJ4[vm_area_struct]  </pre></div><p>一个直观类比是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">伙伴系统像批发市场，按箱发货；</span><br><span class="line">SLUB 像小卖部，把一箱货拆开，一件一件卖给内核。</span><br></pre></td></tr></table></figure><p>如果内核要一个 128B 对象，不应该直接让伙伴系统给一整页，而应该从 SLUB 的 128B 对象池中取一个。</p><p>如果 SLUB 的对象池没有可用 slab，它才向伙伴系统申请新的页级大块。</p><hr><h2 id="❗-本节核心结论：-伙伴系统给-SLAB-SLUB-提供页级大块；SLAB-SLUB-再把大块切成内核小对象。二者是上下级协作关系。"><a href="#❗-本节核心结论：-伙伴系统给-SLAB-SLUB-提供页级大块；SLAB-SLUB-再把大块切成内核小对象。二者是上下级协作关系。" class="headerlink" title="❗ 本节核心结论： 伙伴系统给 SLAB&#x2F;SLUB 提供页级大块；SLAB&#x2F;SLUB 再把大块切成内核小对象。二者是上下级协作关系。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统给 SLAB&#x2F;SLUB 提供页级大块；SLAB&#x2F;SLUB 再把大块切成内核小对象。二者是上下级协作关系。</h2><h2 id="12-Linux-内存管理架构：用户态与内核态的分工"><a href="#12-Linux-内存管理架构：用户态与内核态的分工" class="headerlink" title="12. Linux 内存管理架构：用户态与内核态的分工"></a>12. Linux 内存管理架构：用户态与内核态的分工</h2><p>Linux 内存管理可以粗略分成用户态分配器和内核态分配器两层。</p><h3 id="12-1-用户态：malloc-free-不是系统调用本身"><a href="#12-1-用户态：malloc-free-不是系统调用本身" class="headerlink" title="12.1 用户态：malloc&#x2F;free 不是系统调用本身"></a>12.1 用户态：malloc&#x2F;free 不是系统调用本身</h3><p>用户程序通常使用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">malloc</span>();</span><br><span class="line"><span class="built_in">free</span>();</span><br></pre></td></tr></table></figure><p>但 <code>malloc</code> 和 <code>free</code> 通常不是系统调用本身，而是 C 库中的用户态分配器接口。</p><p>常见用户态分配器包括：</p><table><thead><tr><th>分配器</th><th>说明</th></tr></thead><tbody><tr><td><code>ptmalloc</code></td><td>glibc 默认分配器</td></tr><tr><td><code>jemalloc</code></td><td>常见高性能内存分配器</td></tr><tr><td><code>tcmalloc</code></td><td>Google 的高性能内存分配器</td></tr></tbody></table><p>用户态分配器会先在自己的堆管理结构中找空间。如果不够，再通过系统调用向内核申请更多虚拟内存，例如：</p><ul><li><code>brk</code></li><li><code>mmap</code></li></ul><h3 id="12-2-内核态：真正管理物理内存资源"><a href="#12-2-内核态：真正管理物理内存资源" class="headerlink" title="12.2 内核态：真正管理物理内存资源"></a>12.2 内核态：真正管理物理内存资源</h3><p>内核负责更底层的内存管理，包括：</p><ul><li>虚拟内存区域管理；</li><li>缺页异常处理；</li><li>页表管理；</li><li>物理页分配器；</li><li>SLAB&#x2F;SLUB 小对象分配器；</li><li>页回收；</li><li>OOM killer；</li><li>内存控制组等。</li></ul><p>可以这样理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">用户态 malloc/free</span><br><span class="line">↓</span><br><span class="line">用户态分配器 ptmalloc/jemalloc/tcmalloc</span><br><span class="line">↓</span><br><span class="line">brk 或 mmap 向内核申请虚拟地址空间</span><br><span class="line">↓</span><br><span class="line">程序第一次访问时触发缺页异常</span><br><span class="line">↓</span><br><span class="line">内核从伙伴系统分配物理页</span><br><span class="line">↓</span><br><span class="line">建立页表映射</span><br><span class="line">↓</span><br><span class="line">程序继续运行</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-用户态-malloc-free-管理的是用户进程视角下的内存块；内核负责虚拟地址区域、缺页异常、页表映射和真实物理页分配。"><a href="#❗-本节核心结论：-用户态-malloc-free-管理的是用户进程视角下的内存块；内核负责虚拟地址区域、缺页异常、页表映射和真实物理页分配。" class="headerlink" title="❗ 本节核心结论： 用户态 malloc/free 管理的是用户进程视角下的内存块；内核负责虚拟地址区域、缺页异常、页表映射和真实物理页分配。"></a><strong>❗ 本节核心结论：</strong> 用户态 <code>malloc/free</code> 管理的是用户进程视角下的内存块；内核负责虚拟地址区域、缺页异常、页表映射和真实物理页分配。</h2><h2 id="13-malloc-100-背后可能发生什么"><a href="#13-malloc-100-背后可能发生什么" class="headerlink" title="13. malloc(100) 背后可能发生什么"></a>13. <code>malloc(100)</code> 背后可能发生什么</h2><p>假设用户程序执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *p = <span class="built_in">malloc</span>(<span class="number">100</span>);</span><br></pre></td></tr></table></figure><p>这个调用背后可能经历：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">用户程序调用 malloc(100)</span><br><span class="line">↓</span><br><span class="line">glibc 的 ptmalloc 检查已有空闲块</span><br><span class="line">↓</span><br><span class="line">如果用户态堆中有合适小块，直接返回</span><br><span class="line">↓</span><br><span class="line">如果不够，通过 brk 或 mmap 向内核申请更多虚拟内存</span><br><span class="line">↓</span><br><span class="line">内核记录新的虚拟内存区域</span><br><span class="line">↓</span><br><span class="line">程序第一次真正访问这块内存</span><br><span class="line">↓</span><br><span class="line">触发缺页异常</span><br><span class="line">↓</span><br><span class="line">内核从物理页分配器拿一个物理页</span><br><span class="line">↓</span><br><span class="line">伙伴系统分配页级物理内存</span><br><span class="line">↓</span><br><span class="line">内核建立虚拟页到物理页的映射</span><br><span class="line">↓</span><br><span class="line">程序继续执行</span><br></pre></td></tr></table></figure><p>这里有一个关键点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">malloc(100) 不一定立刻分配真实物理内存。</span><br></pre></td></tr></table></figure><p>它可能只是让用户态分配器返回一段可用虚拟地址范围。真正的物理页可能要等第一次访问时才由缺页异常触发分配。</p><p>这与虚拟内存管理 II 中的延迟映射是一条连续逻辑：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">先获得虚拟地址区域</span><br><span class="line">↓</span><br><span class="line">第一次访问时再分配物理页</span><br><span class="line">↓</span><br><span class="line">建立页表映射</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-malloc-100-和“立刻拿到-100-字节物理内存”不是一回事；真实物理页可能在首次访问时才由内核分配。"><a href="#❗-本节核心结论：-malloc-100-和“立刻拿到-100-字节物理内存”不是一回事；真实物理页可能在首次访问时才由内核分配。" class="headerlink" title="❗ 本节核心结论： malloc(100) 和“立刻拿到 100 字节物理内存”不是一回事；真实物理页可能在首次访问时才由内核分配。"></a><strong>❗ 本节核心结论：</strong> <code>malloc(100)</code> 和“立刻拿到 100 字节物理内存”不是一回事；真实物理页可能在首次访问时才由内核分配。</h2><h2 id="14-一个完整例子：从物理页到内核小对象"><a href="#14-一个完整例子：从物理页到内核小对象" class="headerlink" title="14. 一个完整例子：从物理页到内核小对象"></a>14. 一个完整例子：从物理页到内核小对象</h2><p>假设内核需要频繁分配一个 128B 的结构体对象。</p><h3 id="14-1-如果只用伙伴系统"><a href="#14-1-如果只用伙伴系统" class="headerlink" title="14.1 如果只用伙伴系统"></a>14.1 如果只用伙伴系统</h3><p>如果每次都从伙伴系统申请：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">申请 128B</span><br><span class="line">↓</span><br><span class="line">伙伴系统最小给 4KB</span><br><span class="line">↓</span><br><span class="line">浪费 3968B</span><br></pre></td></tr></table></figure><p>这对频繁出现的小对象非常低效。</p><h3 id="14-2-使用-SLUB-后"><a href="#14-2-使用-SLUB-后" class="headerlink" title="14.2 使用 SLUB 后"></a>14.2 使用 SLUB 后</h3><p>SLUB 可以先向伙伴系统申请一个 4KB slab：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[              4KB              ]</span><br></pre></td></tr></table></figure><p>然后切成 128B 对象：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[128B][128B][128B][128B]...[128B]</span><br></pre></td></tr></table></figure><p>4KB 可以切出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">4096 / 128 = 32 个对象</span><br></pre></td></tr></table></figure><p>之后每次内核申请 128B：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">从 current slab 的 free_list 拿一个对象</span><br></pre></td></tr></table></figure><p>释放时：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">把对象放回 free_list</span><br></pre></td></tr></table></figure><p>如果当前 slab 满了，SLUB 去 partial 链表找还有空闲对象的 slab。如果 partial 也没有，再向伙伴系统申请新的 slab。</p><p>整个过程可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">物理内存</span><br><span class="line">↓</span><br><span class="line">伙伴系统分配 4KB 页</span><br><span class="line">↓</span><br><span class="line">SLUB 把 4KB 切成 32 个 128B 对象</span><br><span class="line">↓</span><br><span class="line">内核快速分配和释放这些对象</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：-伙伴系统解决“从物理内存拿页”，SLUB-解决“把页切成小对象并高效复用”。"><a href="#❗-本节核心结论：-伙伴系统解决“从物理内存拿页”，SLUB-解决“把页切成小对象并高效复用”。" class="headerlink" title="❗ 本节核心结论： 伙伴系统解决“从物理内存拿页”，SLUB 解决“把页切成小对象并高效复用”。"></a><strong>❗ 本节核心结论：</strong> 伙伴系统解决“从物理内存拿页”，SLUB 解决“把页切成小对象并高效复用”。</h2><h2 id="15-易混点总结"><a href="#15-易混点总结" class="headerlink" title="15. 易混点总结"></a>15. 易混点总结</h2><h3 id="15-1-物理内存管理不是完全独立于虚拟内存管理"><a href="#15-1-物理内存管理不是完全独立于虚拟内存管理" class="headerlink" title="15.1 物理内存管理不是完全独立于虚拟内存管理"></a>15.1 物理内存管理不是完全独立于虚拟内存管理</h3><p>物理内存管理的底层对象是物理页，但它经常与虚拟内存管理协作。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mmap 申请虚拟地址区域</span><br><span class="line">↓</span><br><span class="line">第一次访问触发缺页异常</span><br><span class="line">↓</span><br><span class="line">内核从伙伴系统分配物理页</span><br><span class="line">↓</span><br><span class="line">页表建立虚拟页到物理页的映射</span><br></pre></td></tr></table></figure><p>虚拟内存管理负责地址空间和映射，物理内存管理负责真实物理页资源。</p><h3 id="15-2-分页机制缓解外部碎片，但不代表物理内存完全不需要连续页"><a href="#15-2-分页机制缓解外部碎片，但不代表物理内存完全不需要连续页" class="headerlink" title="15.2 分页机制缓解外部碎片，但不代表物理内存完全不需要连续页"></a>15.2 分页机制缓解外部碎片，但不代表物理内存完全不需要连续页</h3><p>分页让虚拟页可以映射到不连续的物理页，因此普通程序不一定需要连续物理内存。</p><p>但系统中仍有场景需要连续物理页，例如：</p><ul><li>大页；</li><li>某些驱动；</li><li>DMA；</li><li>连续内存分配需求。</li></ul><p>因此，物理内存管理仍然要考虑连续页分配和外部碎片。</p><h3 id="15-3-伙伴系统不是没有碎片"><a href="#15-3-伙伴系统不是没有碎片" class="headerlink" title="15.3 伙伴系统不是没有碎片"></a>15.3 伙伴系统不是没有碎片</h3><p>伙伴系统主要缓解外部碎片，但会产生内部碎片。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">申请 17KB</span><br><span class="line">↓</span><br><span class="line">分配 32KB</span><br><span class="line">↓</span><br><span class="line">浪费 15KB</span><br></pre></td></tr></table></figure><p>所以伙伴系统不是完美分配器，而是在效率和碎片之间做折中。</p><h3 id="15-4-位图和伙伴系统不是上下级"><a href="#15-4-位图和伙伴系统不是上下级" class="headerlink" title="15.4 位图和伙伴系统不是上下级"></a>15.4 位图和伙伴系统不是上下级</h3><p>位图和伙伴系统都是页级物理内存分配方案。它们是同层替代关系。</p><p>真正的上下级关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">伙伴系统</span><br><span class="line">↓</span><br><span class="line">SLAB/SLUB</span><br></pre></td></tr></table></figure><h3 id="15-5-SLAB-SLUB-不是替代伙伴系统"><a href="#15-5-SLAB-SLUB-不是替代伙伴系统" class="headerlink" title="15.5 SLAB&#x2F;SLUB 不是替代伙伴系统"></a>15.5 SLAB&#x2F;SLUB 不是替代伙伴系统</h3><p>SLAB&#x2F;SLUB 依赖伙伴系统。它不能凭空产生物理内存，而是先从伙伴系统拿到页级大块，再切成小对象。</p><hr><h2 id="❗-本节核心结论：-本讲最容易混淆的是“位图与-buddy-的同层关系”“buddy-与-SLUB-的上下级关系”“虚拟内存管理与物理页分配的协作关系”。"><a href="#❗-本节核心结论：-本讲最容易混淆的是“位图与-buddy-的同层关系”“buddy-与-SLUB-的上下级关系”“虚拟内存管理与物理页分配的协作关系”。" class="headerlink" title="❗ 本节核心结论： 本讲最容易混淆的是“位图与 buddy 的同层关系”“buddy 与 SLUB 的上下级关系”“虚拟内存管理与物理页分配的协作关系”。"></a><strong>❗ 本节核心结论：</strong> 本讲最容易混淆的是“位图与 buddy 的同层关系”“buddy 与 SLUB 的上下级关系”“虚拟内存管理与物理页分配的协作关系”。</h2><h2 id="16-复习清单"><a href="#16-复习清单" class="headerlink" title="16. 复习清单"></a>16. 复习清单</h2><p>学完《物理内存管理 I》后，应能回答以下问题：</p><ol><li>物理内存管理 I 和物理内存管理 II 的区别是什么？</li><li>什么是物理页？</li><li>物理内存分配器需要考虑哪些评价指标？</li><li>外部碎片和内部碎片有什么区别？</li><li>位图分配器如何记录物理页状态？</li><li>位图分配器如何分配连续物理页？</li><li>位图分配器的优点和缺点是什么？</li><li>伙伴系统为什么只分配 <code>2^n</code> 页大小的块？</li><li>伙伴系统中“分配时拆分”是什么意思？</li><li>伙伴系统中“释放时合并”是什么意思？</li><li>伙伴系统为什么能缓解外部碎片？</li><li>伙伴系统为什么会产生内部碎片？</li><li>什么是伙伴系统中的链表数组？</li><li><code>free_area[0]</code>、<code>free_area[1]</code>、<code>free_area[2]</code> 分别管理什么？</li><li>位图和伙伴系统是上下级关系吗？</li><li>为什么伙伴系统之上还需要 SLAB&#x2F;SLUB？</li><li>一个 slab 是什么？</li><li>SLUB 中 <code>current</code>、<code>partial</code>、<code>full</code> 分别表示什么？</li><li>slab 内部的空闲对象链表如何工作？</li><li>为什么空闲对象本身可以存放 <code>next</code> 指针？</li><li>伙伴系统和 SLUB 是什么关系？</li><li>用户态 <code>malloc(100)</code> 是否一定立刻分配真实物理内存？</li><li><code>malloc → brk/mmap → 缺页异常 → buddy → 页表映射</code> 这条链路如何理解？</li></ol><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>《物理内存管理 I》的核心是理解现代操作系统如何组织真实物理内存资源。</p><p>这一讲可以压缩为一句话：</p><p><strong>位图用 bit 记录每个物理页是否空闲；伙伴系统用链表数组管理不同大小的连续页块，并通过拆分与合并提高连续页分配能力；SLAB&#x2F;SLUB 建立在伙伴系统之上，把页级大块切成固定大小小对象，从而支持内核高效分配小内存对象。</strong></p><p>真正掌握这一讲，需要形成三个层次直觉：</p><ol><li><strong>位图层面</strong>：物理内存可以被看成一组页，每页有空闲或已分配状态。</li><li><strong>伙伴系统层面</strong>：连续物理页按 <code>2^n</code> 块管理，分配时拆分，释放时合并。</li><li><strong>SLAB&#x2F;SLUB 层面</strong>：内核小对象不能每次都直接拿一整页，而应该从对象池中快速分配。</li></ol><p>因此，物理内存管理并不是单纯“找一块空内存”这么简单。它是一套围绕速度、碎片、连续性和对象复用构造出来的分层分配体系。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第6讲 虚拟内存管理II</title>
      <link href="/2026/06/11/%E7%AC%AC6%E8%AE%B2-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86II/"/>
      <url>/2026/06/11/%E7%AC%AC6%E8%AE%B2-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86II/</url>
      
        <content type="html"><![CDATA[<h1 id="虚拟内存管理-II：从页表维护到缺页异常"><a href="#虚拟内存管理-II：从页表维护到缺页异常" class="headerlink" title="虚拟内存管理 II：从页表维护到缺页异常"></a>虚拟内存管理 II：从页表维护到缺页异常</h1><p>《虚拟内存管理 I》解释了虚拟地址如何通过页表和 MMU 翻译成物理地址。《虚拟内存管理 II》进一步讨论的是：<strong>这些页表由谁创建、谁维护，操作系统如何利用页表、虚拟内存区域和缺页异常来构造进程的地址空间。</strong></p><p>这一讲的主线可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">CPU 使用虚拟地址</span><br><span class="line">↓</span><br><span class="line">MMU 根据页表翻译</span><br><span class="line">↓</span><br><span class="line">页表由操作系统维护</span><br><span class="line">↓</span><br><span class="line">操作系统自己也运行在虚拟地址中</span><br><span class="line">↓</span><br><span class="line">因此需要直接映射来访问物理内存和页表页</span><br><span class="line">↓</span><br><span class="line">进程地址空间由多个虚拟内存区域组成</span><br><span class="line">↓</span><br><span class="line">mmap 可以新增虚拟内存区域</span><br><span class="line">↓</span><br><span class="line">立即映射会马上分配物理页</span><br><span class="line">↓</span><br><span class="line">延迟映射会等第一次访问时通过缺页异常再分配</span><br><span class="line">↓</span><br><span class="line">写时拷贝、内存去重、内存压缩、大页都建立在这套机制之上</span><br></pre></td></tr></table></figure><hr><h2 id="❗-核心结论：-虚拟内存不是一张静态的地址翻译表，而是操作系统通过页表、虚拟内存区域、缺页异常和一系列优化机制共同维护出来的地址空间抽象。"><a href="#❗-核心结论：-虚拟内存不是一张静态的地址翻译表，而是操作系统通过页表、虚拟内存区域、缺页异常和一系列优化机制共同维护出来的地址空间抽象。" class="headerlink" title="❗ 核心结论： 虚拟内存不是一张静态的地址翻译表，而是操作系统通过页表、虚拟内存区域、缺页异常和一系列优化机制共同维护出来的地址空间抽象。"></a><strong>❗ 核心结论：</strong> 虚拟内存不是一张静态的地址翻译表，而是操作系统通过页表、虚拟内存区域、缺页异常和一系列优化机制共同维护出来的地址空间抽象。</h2><h2 id="1-操作系统自己也使用虚拟地址"><a href="#1-操作系统自己也使用虚拟地址" class="headerlink" title="1. 操作系统自己也使用虚拟地址"></a>1. 操作系统自己也使用虚拟地址</h2><p>一个容易被忽略的问题是：既然普通程序使用虚拟地址，那么操作系统自己访问内存时，使用的是虚拟地址还是物理地址？</p><p>答案是：<strong>操作系统代码运行时同样使用虚拟地址。</strong></p><p>CPU 执行内核代码时，指令中的地址仍然是虚拟地址。只是内核使用的是内核地址空间和内核页表，而用户进程使用的是用户地址空间和用户页表。</p><p>在 AArch64 中，可以粗略理解为：</p><table><thead><tr><th>寄存器</th><th>作用</th></tr></thead><tbody><tr><td><code>TTBR0_EL1</code></td><td>通常指向用户地址空间的页表</td></tr><tr><td><code>TTBR1_EL1</code></td><td>通常指向内核地址空间的页表</td></tr></tbody></table><p>于是系统中会有这样的地址空间分布：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">低地址</span><br><span class="line">├── 用户地址空间</span><br><span class="line">├── 无效地址区域</span><br><span class="line">└── 内核地址空间</span><br><span class="line">高地址</span><br></pre></td></tr></table></figure><p>这引出一个关键矛盾：</p><ul><li>操作系统运行时使用虚拟地址；</li><li>但是页表项中保存的是物理地址；</li><li>操作系统维护页表时，必须读写页表页；</li><li>如果页表项给出的是物理地址，操作系统不能直接把它当作指针使用。</li></ul><p>因此，操作系统需要一种简单方法，把物理地址转换成内核可以访问的虚拟地址。这就是直接映射要解决的问题。</p><hr><h2 id="❗-本节核心结论：-操作系统运行时也处在虚拟地址体系中；它不能直接把页表项里的物理地址当作普通指针使用。"><a href="#❗-本节核心结论：-操作系统运行时也处在虚拟地址体系中；它不能直接把页表项里的物理地址当作普通指针使用。" class="headerlink" title="❗ 本节核心结论： 操作系统运行时也处在虚拟地址体系中；它不能直接把页表项里的物理地址当作普通指针使用。"></a><strong>❗ 本节核心结论：</strong> 操作系统运行时也处在虚拟地址体系中；它不能直接把页表项里的物理地址当作普通指针使用。</h2><h2 id="2-直接映射-Direct-Mapping：内核访问物理内存的桥"><a href="#2-直接映射-Direct-Mapping：内核访问物理内存的桥" class="headerlink" title="2. 直接映射 Direct Mapping：内核访问物理内存的桥"></a>2. 直接映射 Direct Mapping：内核访问物理内存的桥</h2><p>直接映射的核心思想是：<strong>内核拿出一段连续的虚拟地址空间，与物理内存建立线性映射关系。</strong></p><p>它可以简化成一个公式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">内核虚拟地址 = 物理地址 + 固定偏移量</span><br></pre></td></tr></table></figure><p>反过来也可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">物理地址 = 内核虚拟地址 - 固定偏移量</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">物理地址 0x0000_3000</span><br><span class="line">↓ 加上固定偏移 OFFSET</span><br><span class="line">内核虚拟地址 0xFFFF_0000_0000_3000</span><br></pre></td></tr></table></figure><p>这样，操作系统只要知道物理地址和固定偏移量，就可以快速得到对应的内核虚拟地址。</p><p>直接映射不是给普通应用程序使用的。它主要服务于内核自身，尤其是：</p><ul><li>访问任意物理页；</li><li>管理物理内存；</li><li>读写页表页；</li><li>在页表项保存的物理地址和内核可访问的虚拟地址之间转换。</li></ul><p>为什么不使用不连续的内核虚拟地址来映射物理内存？不是因为不能，而是因为那会让管理变复杂。</p><p>如果使用线性映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">物理地址 0x0000 → 内核虚拟地址 OFFSET + 0x0000</span><br><span class="line">物理地址 0x1000 → 内核虚拟地址 OFFSET + 0x1000</span><br><span class="line">物理地址 0x2000 → 内核虚拟地址 OFFSET + 0x2000</span><br></pre></td></tr></table></figure><p>内核只需要加减偏移量即可完成转换。</p><p>如果使用不连续映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">物理地址 0x0000 → 内核虚拟地址 A</span><br><span class="line">物理地址 0x1000 → 内核虚拟地址 B</span><br><span class="line">物理地址 0x2000 → 内核虚拟地址 C</span><br></pre></td></tr></table></figure><p>内核每次都要额外查询一张映射表，代码复杂、效率下降，也会让页表管理更绕。</p><hr><h2 id="❗-本节核心结论：-直接映射的价值在于让内核通过简单的加减偏移访问物理内存，而不是每次都额外查找物理页对应的内核虚拟地址。"><a href="#❗-本节核心结论：-直接映射的价值在于让内核通过简单的加减偏移访问物理内存，而不是每次都额外查找物理页对应的内核虚拟地址。" class="headerlink" title="❗ 本节核心结论： 直接映射的价值在于让内核通过简单的加减偏移访问物理内存，而不是每次都额外查找物理页对应的内核虚拟地址。"></a><strong>❗ 本节核心结论：</strong> 直接映射的价值在于让内核通过简单的加减偏移访问物理内存，而不是每次都额外查找物理页对应的内核虚拟地址。</h2><h2 id="3-进程的虚拟内存段分布"><a href="#3-进程的虚拟内存段分布" class="headerlink" title="3. 进程的虚拟内存段分布"></a>3. 进程的虚拟内存段分布</h2><p>一个进程的虚拟地址空间不是一整块简单内存，而是由多个不同用途的区域组成。</p><p>典型进程中常见的区域包括：</p><table><thead><tr><th>区域</th><th>作用</th></tr></thead><tbody><tr><td><code>.text</code></td><td>保存程序机器指令，可读、可执行</td></tr><tr><td><code>.rodata</code></td><td>保存只读数据，例如字符串字面量</td></tr><tr><td><code>.data</code></td><td>保存已经初始化的全局变量和静态变量</td></tr><tr><td><code>.bss</code></td><td>保存未初始化的全局变量和静态变量，初始值通常为 0</td></tr><tr><td><code>heap</code></td><td>堆，供 <code>malloc</code>、<code>new</code> 等动态分配使用</td></tr><tr><td><code>stack</code></td><td>栈，保存函数调用、局部变量、返回地址等</td></tr><tr><td><code>mmap</code> 区域</td><td>由 <code>mmap</code> 创建的匿名映射或文件映射区域</td></tr><tr><td>动态库区域</td><td>例如 <code>libc.so</code> 等共享库</td></tr><tr><td><code>vvar</code> &#x2F; <code>vdso</code></td><td>系统提供的特殊用户态辅助区域</td></tr></tbody></table><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> global_init = <span class="number">10</span>;       <span class="comment">// .data</span></span><br><span class="line"><span class="type">int</span> global_uninit;          <span class="comment">// .bss</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *msg = <span class="string">&quot;hello&quot;</span>;  <span class="comment">// 字符串通常在 .rodata</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">char</span> stack_buf[<span class="number">128</span>];        <span class="comment">// stack</span></span><br><span class="line">    <span class="type">char</span> *heap_buf = <span class="built_in">malloc</span>(<span class="number">4096</span>); <span class="comment">// heap</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>程序从源代码到进程地址空间，大致经历：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">.c / .cpp 源文件</span><br><span class="line">↓</span><br><span class="line">编译</span><br><span class="line">↓</span><br><span class="line">目标文件 .o</span><br><span class="line">↓</span><br><span class="line">链接</span><br><span class="line">↓</span><br><span class="line">ELF 可执行文件</span><br><span class="line">↓</span><br><span class="line">加载</span><br><span class="line">↓</span><br><span class="line">进程虚拟地址空间</span><br></pre></td></tr></table></figure><p>ELF 文件记录了程序的代码段、数据段、只读段等信息。加载器和操作系统根据这些信息，为进程建立相应的虚拟内存区域。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    SRC[源代码] --&gt; OBJ[目标文件]    OBJ --&gt; ELF[ELF 可执行文件]    ELF --&gt; LOAD[加载器加载]    LOAD --&gt; TEXT[.text]    LOAD --&gt; RODATA[.rodata]    LOAD --&gt; DATA[.data]    LOAD --&gt; BSS[.bss]    LOAD --&gt; HEAP[heap]    LOAD --&gt; STACK[stack]    LOAD --&gt; MMAP[mmap 区域]  </pre></div><p>这张图强调：进程地址空间是由多个区域组合而成的，不同区域具有不同来源、权限和用途。</p><hr><h2 id="❗-本节核心结论：-进程地址空间不是单一连续内存，而是由代码、数据、堆、栈、动态库和-mmap-区域等多个虚拟内存区域组成。"><a href="#❗-本节核心结论：-进程地址空间不是单一连续内存，而是由代码、数据、堆、栈、动态库和-mmap-区域等多个虚拟内存区域组成。" class="headerlink" title="❗ 本节核心结论： 进程地址空间不是单一连续内存，而是由代码、数据、堆、栈、动态库和 mmap 区域等多个虚拟内存区域组成。"></a><strong>❗ 本节核心结论：</strong> 进程地址空间不是单一连续内存，而是由代码、数据、堆、栈、动态库和 mmap 区域等多个虚拟内存区域组成。</h2><h2 id="4-页表映射管理：操作系统如何建立-va-→-pa"><a href="#4-页表映射管理：操作系统如何建立-va-→-pa" class="headerlink" title="4. 页表映射管理：操作系统如何建立 va → pa"></a>4. 页表映射管理：操作系统如何建立 <code>va → pa</code></h2><p>虚拟地址能否访问，最终取决于页表中有没有有效映射。页表由操作系统创建和维护，MMU 只是按照页表执行翻译。</p><p>页表管理的核心操作可以抽象成两个函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">add_mapping</span><span class="params">(u64 pgtbl, u64 va, u64 pa)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">delete_mapping</span><span class="params">(u64 pgtbl, u64 va)</span>;</span><br></pre></td></tr></table></figure><p>它们分别表示：</p><ul><li>添加虚拟地址 <code>va</code> 到物理地址 <code>pa</code> 的映射；</li><li>删除虚拟地址 <code>va</code> 对应的映射。</li></ul><p>在四级页表中，添加映射大致需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">根据 va 计算 L0 索引</span><br><span class="line">↓</span><br><span class="line">找到或创建 L1 页表</span><br><span class="line">↓</span><br><span class="line">根据 va 计算 L1 索引</span><br><span class="line">↓</span><br><span class="line">找到或创建 L2 页表</span><br><span class="line">↓</span><br><span class="line">根据 va 计算 L2 索引</span><br><span class="line">↓</span><br><span class="line">找到或创建 L3 页表</span><br><span class="line">↓</span><br><span class="line">根据 va 计算 L3 索引</span><br><span class="line">↓</span><br><span class="line">写入 pa 和权限位</span><br></pre></td></tr></table></figure><p>可以用下图表示：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    VA[虚拟地址 va] --&gt; L0[计算 L0 index]    L0 --&gt; P1{L1 页表存在?}    P1 -- 否 --&gt; A1[分配 L1 页表页]    P1 -- 是 --&gt; L1[计算 L1 index]    A1 --&gt; L1    L1 --&gt; P2{L2 页表存在?}    P2 -- 否 --&gt; A2[分配 L2 页表页]    P2 -- 是 --&gt; L2[计算 L2 index]    A2 --&gt; L2    L2 --&gt; P3{L3 页表存在?}    P3 -- 否 --&gt; A3[分配 L3 页表页]    P3 -- 是 --&gt; L3[计算 L3 index]    A3 --&gt; L3    L3 --&gt; MAP[写入 pa 和权限位]  </pre></div><p>在这个过程中，经常会用到类似 <code>get_next_pgtbl_page</code> 的逻辑：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">u64 <span class="title function_">get_next_pgtbl_page</span><span class="params">(u64 *pgtbl, u32 index)</span> &#123;</span><br><span class="line">    u64 pgtbl_entry;</span><br><span class="line"></span><br><span class="line">    pgtbl_entry = pgtbl[index];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pgtbl_entry == <span class="number">0</span>) &#123;</span><br><span class="line">        pgtbl_entry = alloc_pgtbl_page();</span><br><span class="line">        pgtbl[index] = pgtbl_entry | some_permission;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> paddr_to_vaddr(pgtbl_entry);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个函数与直接映射紧密相关，关键在最后一步：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> paddr_to_vaddr(pgtbl_entry);</span><br></pre></td></tr></table></figure><p>原因是：</p><ul><li>页表项里保存的是下一级页表页的物理地址；</li><li>但操作系统代码运行时不能直接用物理地址当指针；</li><li>所以内核必须把物理地址转换成内核虚拟地址；</li><li>这个转换依赖直接映射。</li></ul><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">页表项里的物理地址</span><br><span class="line">↓</span><br><span class="line">通过直接映射转换</span><br><span class="line">↓</span><br><span class="line">内核可访问的虚拟地址</span><br><span class="line">↓</span><br><span class="line">操作系统继续读写下一级页表</span><br></pre></td></tr></table></figure><p>删除映射时还有一个额外问题：TLB 中可能缓存了旧映射。即使页表项已经删除，如果 TLB 没刷新，CPU 仍可能使用旧的 <code>虚拟页 → 物理页</code> 翻译结果。因此删除或修改映射后，通常需要考虑刷新相关 TLB 项。</p><hr><h2 id="❗-本节核心结论：-页表由操作系统按需创建和修改；页表项保存物理地址，而内核运行时使用虚拟地址，所以页表管理必须借助直接映射完成物理地址到内核虚拟地址的转换。"><a href="#❗-本节核心结论：-页表由操作系统按需创建和修改；页表项保存物理地址，而内核运行时使用虚拟地址，所以页表管理必须借助直接映射完成物理地址到内核虚拟地址的转换。" class="headerlink" title="❗ 本节核心结论： 页表由操作系统按需创建和修改；页表项保存物理地址，而内核运行时使用虚拟地址，所以页表管理必须借助直接映射完成物理地址到内核虚拟地址的转换。"></a><strong>❗ 本节核心结论：</strong> 页表由操作系统按需创建和修改；页表项保存物理地址，而内核运行时使用虚拟地址，所以页表管理必须借助直接映射完成物理地址到内核虚拟地址的转换。</h2><h2 id="5-立即映射-mmap：申请时立刻分配物理页"><a href="#5-立即映射-mmap：申请时立刻分配物理页" class="headerlink" title="5. 立即映射 mmap：申请时立刻分配物理页"></a>5. 立即映射 mmap：申请时立刻分配物理页</h2><p><code>mmap</code> 是用户程序申请虚拟内存区域的一种方式。例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *buf = mmap((<span class="type">void</span> *)<span class="number">0x500000000</span>, <span class="number">0x2000</span>,</span><br><span class="line">                 PROT_READ | PROT_WRITE,</span><br><span class="line">                 MAP_ANONYMOUS | MAP_PRIVATE,</span><br><span class="line">                 <span class="number">-1</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>这表示程序希望申请一段虚拟地址区域：</p><ul><li>起始地址大致为 <code>0x500000000</code>；</li><li>长度为 <code>0x2000</code>，即 8192 字节；</li><li>权限为可读、可写；</li><li>匿名映射，不对应具体文件；</li><li>私有映射。</li></ul><p>如果页面大小是 4KB，那么 <code>0x2000</code> 对应 2 页。</p><p>所谓立即映射，是指 <code>mmap</code> 调用时，操作系统马上完成：</p><ol><li>记录这段虚拟地址区域合法；</li><li>分配对应数量的物理页；</li><li>建立虚拟页到物理页的页表映射。</li></ol><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 0 → 物理页 A</span><br><span class="line">虚拟页 1 → 物理页 B</span><br></pre></td></tr></table></figure><p>这样，<code>mmap</code> 返回后，程序第一次访问这段内存时，页表映射已经存在，不需要再因为“没有物理页”而触发缺页处理。</p><p>立即映射的优点是第一次访问快，逻辑直接。缺点是可能浪费物理内存。</p><p>例如程序申请 1GB 内存，但最后只使用前 4KB。如果采用立即映射，系统可能提前分配大量物理页，其中绝大多数根本没有被使用。</p><hr><h2 id="❗-本节核心结论：-立即映射会在-mmap-时立刻分配物理页并建立页表映射，首次访问快，但可能提前占用大量实际内存。"><a href="#❗-本节核心结论：-立即映射会在-mmap-时立刻分配物理页并建立页表映射，首次访问快，但可能提前占用大量实际内存。" class="headerlink" title="❗ 本节核心结论： 立即映射会在 mmap 时立刻分配物理页并建立页表映射，首次访问快，但可能提前占用大量实际内存。"></a><strong>❗ 本节核心结论：</strong> 立即映射会在 <code>mmap</code> 时立刻分配物理页并建立页表映射，首次访问快，但可能提前占用大量实际内存。</h2><h2 id="6-延迟映射：先记录合法区域，用到再分配"><a href="#6-延迟映射：先记录合法区域，用到再分配" class="headerlink" title="6. 延迟映射：先记录合法区域，用到再分配"></a>6. 延迟映射：先记录合法区域，用到再分配</h2><p>延迟映射的思想是：<strong><code>mmap</code> 时只记录虚拟地址区域合法，不马上分配物理页；等程序第一次真正访问某一页时，再分配物理页并建立映射。</strong></p><p>操作系统需要为进程维护虚拟内存区域信息，例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vmregion</span> &#123;</span></span><br><span class="line">    u64 start;</span><br><span class="line">    u64 end;</span><br><span class="line">    u64 perm;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>一个进程的地址空间可以理解为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vmspace</span> &#123;</span></span><br><span class="line">    u64 pgtbl;</span><br><span class="line">    <span class="built_in">list</span> vmregions;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>也就是说：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">进程地址空间 = 页表 + 虚拟内存区域列表</span><br></pre></td></tr></table></figure><p>延迟映射下，<code>mmap</code> 做的事情主要是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">记录 [start, end) 是合法虚拟地址区域</span><br><span class="line">记录权限 perm</span><br><span class="line">暂时不分配物理页</span><br><span class="line">暂时不建立完整物理映射</span><br></pre></td></tr></table></figure><p>例如程序申请 16KB 匿名内存，页大小为 4KB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 0：合法，但未分配物理页</span><br><span class="line">虚拟页 1：合法，但未分配物理页</span><br><span class="line">虚拟页 2：合法，但未分配物理页</span><br><span class="line">虚拟页 3：合法，但未分配物理页</span><br></pre></td></tr></table></figure><p>如果程序第一次只访问 <code>buf[0]</code>，通常只会为第 0 页分配一个物理页：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 0 → 物理页 A</span><br><span class="line">虚拟页 1 → 未分配</span><br><span class="line">虚拟页 2 → 未分配</span><br><span class="line">虚拟页 3 → 未分配</span><br></pre></td></tr></table></figure><p>之后如果程序访问 <code>buf[5000]</code>，进入第 1 页，系统再为第 1 页分配物理页。</p><p>因此，延迟映射最基本的分配粒度是：<strong>访问哪一页，分配哪一页。</strong></p><p>当然，在某些优化场景中，操作系统可能做预读、预取或透明大页分配。但从基本概念上看，延迟映射不是一次性分配整个申请区域，而是按实际访问逐页补上映射。</p><hr><h2 id="❗-本节核心结论：-延迟映射把“申请虚拟地址区域”和“分配物理内存”分开；通常第一次访问哪一页，就只为哪一页分配物理页。"><a href="#❗-本节核心结论：-延迟映射把“申请虚拟地址区域”和“分配物理内存”分开；通常第一次访问哪一页，就只为哪一页分配物理页。" class="headerlink" title="❗ 本节核心结论： 延迟映射把“申请虚拟地址区域”和“分配物理内存”分开；通常第一次访问哪一页，就只为哪一页分配物理页。"></a><strong>❗ 本节核心结论：</strong> 延迟映射把“申请虚拟地址区域”和“分配物理内存”分开；通常第一次访问哪一页，就只为哪一页分配物理页。</h2><h2 id="7-缺页异常-Page-Fault：不是所有缺页都是错误"><a href="#7-缺页异常-Page-Fault：不是所有缺页都是错误" class="headerlink" title="7. 缺页异常 Page Fault：不是所有缺页都是错误"></a>7. 缺页异常 Page Fault：不是所有缺页都是错误</h2><p>当程序访问某个虚拟地址时，MMU 查页表发现没有有效映射，就会触发缺页异常。</p><p>缺页异常本身不一定意味着程序出错。它只是说明：<strong>当前访问无法直接通过页表完成翻译，需要操作系统介入处理。</strong></p><p>缺页异常大致分为两类。</p><h3 id="7-1-合法缺页"><a href="#7-1-合法缺页" class="headerlink" title="7.1 合法缺页"></a>7.1 合法缺页</h3><p>合法缺页指的是：</p><ul><li>访问的虚拟地址属于某个合法 <code>vmregion</code>；</li><li>访问权限也符合该区域的权限；</li><li>只是物理页尚未分配，或页表映射尚未建立。</li></ul><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *buf = mmap(...);</span><br><span class="line">buf[<span class="number">0</span>] = <span class="string">&#x27;A&#x27;</span>;</span><br></pre></td></tr></table></figure><p>如果采用延迟映射，<code>buf[0]</code> 第一次访问时可能触发缺页异常。操作系统处理流程是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">得到 fault_va</span><br><span class="line">↓</span><br><span class="line">检查 fault_va 是否落在某个合法 vmregion 中</span><br><span class="line">↓</span><br><span class="line">检查访问权限是否允许</span><br><span class="line">↓</span><br><span class="line">分配物理页</span><br><span class="line">↓</span><br><span class="line">调用 add_mapping 建立页表映射</span><br><span class="line">↓</span><br><span class="line">返回用户程序继续执行</span><br></pre></td></tr></table></figure><p>这类缺页异常是正常机制的一部分。</p><h3 id="7-2-非法访问"><a href="#7-2-非法访问" class="headerlink" title="7.2 非法访问"></a>7.2 非法访问</h3><p>非法访问指的是：</p><ul><li>访问地址不属于任何合法虚拟内存区域；</li><li>或者访问方式违反权限，例如向只读区域写入。</li></ul><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *p = <span class="literal">NULL</span>;</span><br><span class="line">*p = <span class="string">&#x27;A&#x27;</span>;</span><br></pre></td></tr></table></figure><p>操作系统发现该地址不合法，就不会分配物理页，而是向进程发送异常信号，最终表现为 <code>Segmentation Fault</code>。</p><p>因此需要区分：</p><table><thead><tr><th>概念</th><th>含义</th></tr></thead><tbody><tr><td>Page Fault</td><td>硬件发现页表无法完成翻译，陷入操作系统</td></tr><tr><td>Segmentation Fault</td><td>操作系统判断访问非法后，终止或通知进程的结果</td></tr></tbody></table><p>不是所有 Page Fault 都会导致 Segmentation Fault。延迟映射、写时拷贝、页面换入等机制都会利用 Page Fault 作为正常控制路径。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    ACCESS[访问虚拟地址] --&gt; MISS{页表有有效映射?}    MISS -- 有 --&gt; OK[正常访问物理内存]    MISS -- 无 --&gt; PF[触发 Page Fault]    PF --&gt; CHECK{地址在合法 vmregion 中?}    CHECK -- 否 --&gt; SEGV[Segmentation Fault]    CHECK -- 是 --&gt; PERM{权限允许?}    PERM -- 否 --&gt; SEGV    PERM -- 是 --&gt; ALLOC[分配物理页]    ALLOC --&gt; MAP[建立页表映射]    MAP --&gt; RET[返回继续执行]  </pre></div><hr><h2 id="❗-本节核心结论：-Page-Fault-是硬件异常入口，不等于程序错误；Segmentation-Fault-是操作系统判断访问非法后的结果。"><a href="#❗-本节核心结论：-Page-Fault-是硬件异常入口，不等于程序错误；Segmentation-Fault-是操作系统判断访问非法后的结果。" class="headerlink" title="❗ 本节核心结论： Page Fault 是硬件异常入口，不等于程序错误；Segmentation Fault 是操作系统判断访问非法后的结果。"></a><strong>❗ 本节核心结论：</strong> Page Fault 是硬件异常入口，不等于程序错误；Segmentation Fault 是操作系统判断访问非法后的结果。</h2><h2 id="8-Linux-风格的缺页处理路径"><a href="#8-Linux-风格的缺页处理路径" class="headerlink" title="8. Linux 风格的缺页处理路径"></a>8. Linux 风格的缺页处理路径</h2><p>在真实 Linux 中，缺页处理不是简单地“分配一页”就结束。系统需要根据页面类型和访问原因进行分派。</p><p>大致路径可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">do_translation_fault</span><br><span class="line">↓</span><br><span class="line">do_page_fault</span><br><span class="line">↓</span><br><span class="line">__do_page_fault</span><br><span class="line">↓</span><br><span class="line">handle_pte_fault</span><br></pre></td></tr></table></figure><p><code>handle_pte_fault</code> 之后可能继续判断：</p><ul><li>页表项是否无效；</li><li>页面是否在 swap 中；</li><li>是否是匿名页；</li><li>是否是文件映射；</li><li>是否涉及写时拷贝；</li><li>是否是权限错误。</li></ul><p>常见处理函数包括：</p><table><thead><tr><th>函数</th><th>大致作用</th></tr></thead><tbody><tr><td><code>do_anonymous_page</code></td><td>处理匿名映射页</td></tr><tr><td><code>do_fault</code></td><td>处理文件映射或共享映射</td></tr><tr><td><code>do_swap_page</code></td><td>处理被换出到磁盘的页面</td></tr><tr><td><code>do_wp_page</code></td><td>处理写保护页，常与写时拷贝相关</td></tr></tbody></table><p>学习这一部分时，不必把源码路径机械背诵到每一层。更重要的是理解：<strong>缺页异常是一个统一入口，操作系统会根据具体原因选择不同处理方式。</strong></p><hr><h2 id="❗-本节核心结论：-Linux-的缺页处理会区分匿名页、文件映射、swap、写保护等多种情况；Page-Fault-是多种内存机制共同使用的处理入口。"><a href="#❗-本节核心结论：-Linux-的缺页处理会区分匿名页、文件映射、swap、写保护等多种情况；Page-Fault-是多种内存机制共同使用的处理入口。" class="headerlink" title="❗ 本节核心结论： Linux 的缺页处理会区分匿名页、文件映射、swap、写保护等多种情况；Page Fault 是多种内存机制共同使用的处理入口。"></a><strong>❗ 本节核心结论：</strong> Linux 的缺页处理会区分匿名页、文件映射、swap、写保护等多种情况；Page Fault 是多种内存机制共同使用的处理入口。</h2><h2 id="9-写时拷贝-Copy-on-Write：不写就共享，一写就复制"><a href="#9-写时拷贝-Copy-on-Write：不写就共享，一写就复制" class="headerlink" title="9. 写时拷贝 Copy-on-Write：不写就共享，一写就复制"></a>9. 写时拷贝 Copy-on-Write：不写就共享，一写就复制</h2><p>写时拷贝简称 COW，是基于页表权限和缺页异常实现的重要优化。</p><p>最典型场景是 <code>fork()</code>。</p><p>假设父进程调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fork();</span><br></pre></td></tr></table></figure><p>如果操作系统立刻复制父进程的全部物理内存，成本会很高。尤其是很多程序在 <code>fork()</code> 后很快调用 <code>exec()</code>，原来的地址空间马上被替换，提前复制就非常浪费。</p><p>因此，操作系统使用写时拷贝：</p><ol><li><code>fork()</code> 后父子进程暂时共享同一批物理页；</li><li>父子进程页表都指向相同物理页；</li><li>这些共享页的页表权限被暂时设置为只读；</li><li>如果没有写入，就一直共享；</li><li>如果某一方尝试写入，就触发缺页异常；</li><li>操作系统分配新物理页，复制原页内容；</li><li>修改写入方页表，让它指向新物理页；</li><li>恢复写入方的写权限。</li></ol><p>过程可以表示为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    FORK[fork 创建子进程] --&gt; SHARE[父子共享物理页]    SHARE --&gt; RO[共享页设为只读]    RO --&gt; WRITE{某进程写入?}    WRITE -- 否 --&gt; KEEP[继续共享]    WRITE -- 是 --&gt; PF[触发缺页异常]    PF --&gt; COPY[分配新页并复制内容]    COPY --&gt; REMAP[修改写入方页表]    REMAP --&gt; W[恢复写权限]  </pre></div><p>写时拷贝的核心是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">不写就共享，一写就复制</span><br></pre></td></tr></table></figure><p>它的优点是：</p><ul><li>减少 <code>fork()</code> 的时间开销；</li><li>节省物理内存；</li><li>避免不必要的数据复制；</li><li>将复制成本推迟到真正写入时才发生。</li></ul><hr><h2 id="❗-本节核心结论：-COW-利用只读权限和缺页异常捕捉写操作，把“立即复制整片内存”优化为“写到哪页才复制哪页”。"><a href="#❗-本节核心结论：-COW-利用只读权限和缺页异常捕捉写操作，把“立即复制整片内存”优化为“写到哪页才复制哪页”。" class="headerlink" title="❗ 本节核心结论： COW 利用只读权限和缺页异常捕捉写操作，把“立即复制整片内存”优化为“写到哪页才复制哪页”。"></a><strong>❗ 本节核心结论：</strong> COW 利用只读权限和缺页异常捕捉写操作，把“立即复制整片内存”优化为“写到哪页才复制哪页”。</h2><h2 id="10-内存去重：发现相同页面后合并共享"><a href="#10-内存去重：发现相同页面后合并共享" class="headerlink" title="10. 内存去重：发现相同页面后合并共享"></a>10. 内存去重：发现相同页面后合并共享</h2><p>内存去重可以理解成写时拷贝的反方向应用。</p><p>写时拷贝是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">原本共享</span><br><span class="line">↓</span><br><span class="line">写入时复制分开</span><br></pre></td></tr></table></figure><p>内存去重是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">原本分开</span><br><span class="line">↓</span><br><span class="line">发现内容相同</span><br><span class="line">↓</span><br><span class="line">合并共享</span><br></pre></td></tr></table></figure><p>例如系统中有两个物理页内容完全一样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">物理页 A：内容 = 0000...</span><br><span class="line">物理页 B：内容 = 0000...</span><br></pre></td></tr></table></figure><p>操作系统可以只保留其中一个页面，让多个虚拟页指向同一个物理页：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">进程 1 虚拟页 → 物理页 A</span><br><span class="line">进程 2 虚拟页 → 物理页 A</span><br></pre></td></tr></table></figure><p>然后释放另一个重复物理页。</p><p>为了保证语义正确，合并后的共享页通常会被设置为只读。如果某个进程之后写入该页面，就触发写时拷贝：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">写入共享页</span><br><span class="line">↓</span><br><span class="line">触发缺页异常</span><br><span class="line">↓</span><br><span class="line">分配新页</span><br><span class="line">↓</span><br><span class="line">复制内容</span><br><span class="line">↓</span><br><span class="line">修改写入方页表</span><br></pre></td></tr></table></figure><p>Linux 中典型机制是 KSM，即 Kernel Same-page Merging。</p><p>不过，内存去重也有安全隐患。因为页面是否被合并可能影响写入延迟，攻击者有可能通过时间差推测其他进程中是否存在某些特定数据。这类问题属于侧信道风险。</p><hr><h2 id="❗-本节核心结论：-内存去重通过合并内容相同的物理页节省内存，并依靠-COW-保证写入时重新分离；但它可能带来侧信道安全风险。"><a href="#❗-本节核心结论：-内存去重通过合并内容相同的物理页节省内存，并依靠-COW-保证写入时重新分离；但它可能带来侧信道安全风险。" class="headerlink" title="❗ 本节核心结论： 内存去重通过合并内容相同的物理页节省内存，并依靠 COW 保证写入时重新分离；但它可能带来侧信道安全风险。"></a><strong>❗ 本节核心结论：</strong> 内存去重通过合并内容相同的物理页节省内存，并依靠 COW 保证写入时重新分离；但它可能带来侧信道安全风险。</h2><h2 id="11-内存压缩：用-CPU-时间换内存空间"><a href="#11-内存压缩：用-CPU-时间换内存空间" class="headerlink" title="11. 内存压缩：用 CPU 时间换内存空间"></a>11. 内存压缩：用 CPU 时间换内存空间</h2><p>当物理内存紧张时，操作系统不一定立刻把页面换出到磁盘。另一种策略是内存压缩。</p><p>传统 swap 的思路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">内存页</span><br><span class="line">↓</span><br><span class="line">写入磁盘</span><br><span class="line">↓</span><br><span class="line">再次访问时从磁盘读回</span><br></pre></td></tr></table></figure><p>缺点是磁盘 I&#x2F;O 较慢，也可能增加 SSD 写入压力。</p><p>内存压缩的思路是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">不常用内存页</span><br><span class="line">↓</span><br><span class="line">压缩</span><br><span class="line">↓</span><br><span class="line">压缩后的数据仍然留在内存</span><br><span class="line">↓</span><br><span class="line">再次访问时解压恢复</span><br></pre></td></tr></table></figure><p>例如一个 4KB 页面压缩后可能只占 1KB。这样系统虽然付出了压缩和解压的 CPU 成本，但减少了真实内存占用，也减少了写磁盘的次数。</p><p>对比来看：</p><table><thead><tr><th>机制</th><th>做法</th><th>优点</th><th>代价</th></tr></thead><tbody><tr><td>Swap</td><td>把页面写入磁盘</td><td>释放内存彻底</td><td>磁盘 I&#x2F;O 慢</td></tr><tr><td>内存压缩</td><td>把页面压缩后留在内存</td><td>比磁盘快，减少 I&#x2F;O</td><td>消耗 CPU 进行压缩&#x2F;解压</td></tr></tbody></table><p>Windows 10 中有内存压缩机制；Linux 中有 <code>zswap</code>，可以把准备换出的页面先压缩放在内存区域中，减少真正写入磁盘的次数。</p><hr><h2 id="❗-本节核心结论：-内存压缩通过压缩不常用页面缓解内存压力，本质上是用-CPU-计算成本换取更少的内存占用和更少的磁盘-I-O。"><a href="#❗-本节核心结论：-内存压缩通过压缩不常用页面缓解内存压力，本质上是用-CPU-计算成本换取更少的内存占用和更少的磁盘-I-O。" class="headerlink" title="❗ 本节核心结论： 内存压缩通过压缩不常用页面缓解内存压力，本质上是用 CPU 计算成本换取更少的内存占用和更少的磁盘 I&#x2F;O。"></a><strong>❗ 本节核心结论：</strong> 内存压缩通过压缩不常用页面缓解内存压力，本质上是用 CPU 计算成本换取更少的内存占用和更少的磁盘 I&#x2F;O。</h2><h2 id="12-大页-Huge-Page：用更大映射粒度降低-TLB-压力"><a href="#12-大页-Huge-Page：用更大映射粒度降低-TLB-压力" class="headerlink" title="12. 大页 Huge Page：用更大映射粒度降低 TLB 压力"></a>12. 大页 Huge Page：用更大映射粒度降低 TLB 压力</h2><p>普通页大小通常是 4KB。大页则可能是：</p><ul><li>2MB；</li><li>1GB。</li></ul><p>大页的主要目标是降低页表和 TLB 压力。</p><p>假设程序访问 1GB 连续内存：</p><p>如果使用 4KB 页：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1GB / 4KB = 262144 个页面</span><br></pre></td></tr></table></figure><p>这意味着需要大量页表项，也会占用大量 TLB 项。</p><p>如果使用 2MB 大页：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1GB / 2MB = 512 个页面</span><br></pre></td></tr></table></figure><p>需要管理的页面数量大幅减少。</p><p>在四级页表中，普通 4KB 页通常走完整路径：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">L0 → L1 → L2 → L3 → 4KB 物理页</span><br></pre></td></tr></table></figure><p>2MB 大页可以提前停止：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">L0 → L1 → L2 → 2MB 物理页</span><br></pre></td></tr></table></figure><p>1GB 大页可以更早停止：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">L0 → L1 → 1GB 物理页</span><br></pre></td></tr></table></figure><p>为什么 2MB 大页和 1GB 大页会自然出现？</p><p>在 4KB 页、每级页表 512 项的设定下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">512 × 4KB = 2MB</span><br></pre></td></tr></table></figure><p>所以如果跳过 L3，让 L2 页表项直接指向物理内存，就得到 2MB 大页。</p><p>继续向上一层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">512 × 2MB = 1GB</span><br></pre></td></tr></table></figure><p>所以如果跳过 L2 和 L3，让 L1 页表项直接指向物理内存，就得到 1GB 大页。</p><p>大页的优缺点如下：</p><table><thead><tr><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>减少页表项数量</td><td>可能浪费物理内存</td></tr><tr><td>减少 TLB 项占用</td><td>需要更大连续物理内存</td></tr><tr><td>提高 TLB 命中率</td><td>管理更复杂</td></tr><tr><td>减少页表遍历级数</td><td>内存碎片问题更明显</td></tr></tbody></table><p>一句话概括：<strong>大页是用空间换性能。</strong></p><hr><h2 id="❗-本节核心结论：-大页通过扩大单个页表项覆盖的地址范围，减少页表项数量和-TLB-压力，但可能带来内存浪费和管理复杂度。"><a href="#❗-本节核心结论：-大页通过扩大单个页表项覆盖的地址范围，减少页表项数量和-TLB-压力，但可能带来内存浪费和管理复杂度。" class="headerlink" title="❗ 本节核心结论： 大页通过扩大单个页表项覆盖的地址范围，减少页表项数量和 TLB 压力，但可能带来内存浪费和管理复杂度。"></a><strong>❗ 本节核心结论：</strong> 大页通过扩大单个页表项覆盖的地址范围，减少页表项数量和 TLB 压力，但可能带来内存浪费和管理复杂度。</h2><h2 id="13-一个完整例子：OS-运行一个程序"><a href="#13-一个完整例子：OS-运行一个程序" class="headerlink" title="13. 一个完整例子：OS 运行一个程序"></a>13. 一个完整例子：OS 运行一个程序</h2><p>假设有一个程序：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> global_init = <span class="number">10</span>;       <span class="comment">// .data</span></span><br><span class="line"><span class="type">int</span> global_uninit;          <span class="comment">// .bss</span></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *msg = <span class="string">&quot;hello&quot;</span>;  <span class="comment">// .rodata</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">char</span> stack_buf[<span class="number">128</span>];              <span class="comment">// stack</span></span><br><span class="line">    <span class="type">char</span> *heap_buf = <span class="built_in">malloc</span>(<span class="number">4096</span>);    <span class="comment">// heap</span></span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> *mmap_buf = mmap(<span class="literal">NULL</span>, <span class="number">8192</span>,</span><br><span class="line">                          PROT_READ | PROT_WRITE,</span><br><span class="line">                          MAP_PRIVATE | MAP_ANONYMOUS,</span><br><span class="line">                          <span class="number">-1</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">strcpy</span>(mmap_buf, <span class="string">&quot;Hello mmap&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">        mmap_buf[<span class="number">0</span>] = <span class="string">&#x27;X&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从操作系统角度看，这个程序经历了以下过程。</p><h3 id="13-1-OS-建立直接映射"><a href="#13-1-OS-建立直接映射" class="headerlink" title="13.1 OS 建立直接映射"></a>13.1 OS 建立直接映射</h3><p>系统启动后，内核建立直接映射区域，使自己能通过：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">内核虚拟地址 = 物理地址 + OFFSET</span><br></pre></td></tr></table></figure><p>访问物理内存和页表页。</p><h3 id="13-2-加载程序并建立虚拟内存区域"><a href="#13-2-加载程序并建立虚拟内存区域" class="headerlink" title="13.2 加载程序并建立虚拟内存区域"></a>13.2 加载程序并建立虚拟内存区域</h3><p>加载器根据 ELF 信息建立：</p><ul><li><code>.text</code>：程序代码；</li><li><code>.rodata</code>：字符串 <code>&quot;hello&quot;</code>；</li><li><code>.data</code>：<code>global_init</code>；</li><li><code>.bss</code>：<code>global_uninit</code>；</li><li><code>heap</code>：供 <code>malloc</code> 使用；</li><li><code>stack</code>：供局部变量和函数调用使用；</li><li><code>mmap</code> 区域：后续由 <code>mmap</code> 创建。</li></ul><h3 id="13-3-OS-维护页表映射"><a href="#13-3-OS-维护页表映射" class="headerlink" title="13.3 OS 维护页表映射"></a>13.3 OS 维护页表映射</h3><p>对于需要立即可访问的区域，操作系统分配物理页，并通过 <code>add_mapping</code> 建立：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页 → 物理页</span><br></pre></td></tr></table></figure><p>如果某一级页表不存在，就分配新的页表页。因为页表项保存的是物理地址，内核需要通过直接映射把它转成可访问的内核虚拟地址。</p><h3 id="13-4-程序调用-mmap"><a href="#13-4-程序调用-mmap" class="headerlink" title="13.4 程序调用 mmap"></a>13.4 程序调用 mmap</h3><p>当程序执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mmap(<span class="literal">NULL</span>, <span class="number">8192</span>, PROT_READ | PROT_WRITE,</span><br><span class="line">     MAP_PRIVATE | MAP_ANONYMOUS, <span class="number">-1</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>操作系统可以有两种策略：</p><ul><li>立即映射：马上分配 2 个 4KB 物理页并填页表；</li><li>延迟映射：只记录这 8192 字节合法，暂时不分配物理页。</li></ul><h3 id="13-5-第一次访问触发缺页异常"><a href="#13-5-第一次访问触发缺页异常" class="headerlink" title="13.5 第一次访问触发缺页异常"></a>13.5 第一次访问触发缺页异常</h3><p>如果采用延迟映射，程序执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">strcpy</span>(mmap_buf, <span class="string">&quot;Hello mmap&quot;</span>);</span><br></pre></td></tr></table></figure><p>第一次访问 <code>mmap_buf[0]</code> 时，页表中还没有有效映射，于是触发 Page Fault。操作系统检查该地址合法且可写，于是分配一个物理页，建立映射，然后返回程序继续执行。</p><p>如果后续访问第二页，例如 <code>mmap_buf[5000]</code>，第二页再触发一次缺页异常，再分配第二个物理页。</p><h3 id="13-6-fork-触发写时拷贝机制"><a href="#13-6-fork-触发写时拷贝机制" class="headerlink" title="13.6 fork 触发写时拷贝机制"></a>13.6 fork 触发写时拷贝机制</h3><p>程序执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">pid_t</span> pid = fork();</span><br></pre></td></tr></table></figure><p>父子进程先共享物理页，页表项被设置为只读。子进程执行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mmap_buf[<span class="number">0</span>] = <span class="string">&#x27;X&#x27;</span>;</span><br></pre></td></tr></table></figure><p>写入只读共享页时触发缺页异常。操作系统识别这是 COW 写异常，于是分配新页、复制内容、修改子进程页表、恢复写权限。</p><h3 id="13-7-系统可能进一步做优化"><a href="#13-7-系统可能进一步做优化" class="headerlink" title="13.7 系统可能进一步做优化"></a>13.7 系统可能进一步做优化</h3><p>在内存管理过程中，操作系统还可能使用：</p><ul><li>内存去重：发现多个页面内容相同后合并共享；</li><li>内存压缩：内存紧张时压缩不常用页面，减少磁盘换页；</li><li>大页：对大块连续内存使用 2MB 或 1GB 页，降低 TLB 压力。</li></ul><p>这个例子把本讲的多个机制串成了一条完整链条。</p><hr><h2 id="❗-本节核心结论：-一个普通程序的运行会同时涉及虚拟内存段分布、页表映射、mmap、缺页异常、COW-以及可能的内存优化机制。"><a href="#❗-本节核心结论：-一个普通程序的运行会同时涉及虚拟内存段分布、页表映射、mmap、缺页异常、COW-以及可能的内存优化机制。" class="headerlink" title="❗ 本节核心结论： 一个普通程序的运行会同时涉及虚拟内存段分布、页表映射、mmap、缺页异常、COW 以及可能的内存优化机制。"></a><strong>❗ 本节核心结论：</strong> 一个普通程序的运行会同时涉及虚拟内存段分布、页表映射、mmap、缺页异常、COW 以及可能的内存优化机制。</h2><h2 id="14-易混点总结"><a href="#14-易混点总结" class="headerlink" title="14. 易混点总结"></a>14. 易混点总结</h2><h3 id="14-1-直接映射不是普通用户程序使用的映射"><a href="#14-1-直接映射不是普通用户程序使用的映射" class="headerlink" title="14.1 直接映射不是普通用户程序使用的映射"></a>14.1 直接映射不是普通用户程序使用的映射</h3><p>直接映射主要供内核使用。它帮助内核把物理地址转换成内核虚拟地址，从而访问物理内存和页表页。</p><h3 id="14-2-get-next-pgtbl-page-和直接映射相连"><a href="#14-2-get-next-pgtbl-page-和直接映射相连" class="headerlink" title="14.2 get_next_pgtbl_page 和直接映射相连"></a>14.2 <code>get_next_pgtbl_page</code> 和直接映射相连</h3><p>因为页表项中保存的是下一级页表的物理地址，而内核要继续访问下一级页表，必须先通过直接映射得到可访问的内核虚拟地址。</p><h3 id="14-3-延迟映射不是一次分配整个区域"><a href="#14-3-延迟映射不是一次分配整个区域" class="headerlink" title="14.3 延迟映射不是一次分配整个区域"></a>14.3 延迟映射不是一次分配整个区域</h3><p>延迟映射通常按页分配。访问哪一页，就为哪一页分配物理页并建立映射。</p><h3 id="14-4-Page-Fault-不等于-Segmentation-Fault"><a href="#14-4-Page-Fault-不等于-Segmentation-Fault" class="headerlink" title="14.4 Page Fault 不等于 Segmentation Fault"></a>14.4 Page Fault 不等于 Segmentation Fault</h3><p>Page Fault 是硬件触发的异常入口；Segmentation Fault 是操作系统判断访问非法后给进程的结果。</p><h3 id="14-5-COW-不是不复制，而是推迟复制"><a href="#14-5-COW-不是不复制，而是推迟复制" class="headerlink" title="14.5 COW 不是不复制，而是推迟复制"></a>14.5 COW 不是不复制，而是推迟复制</h3><p>写时拷贝不是永远不复制，而是在写入发生时才复制对应页面。</p><h3 id="14-6-大页不是一定更好"><a href="#14-6-大页不是一定更好" class="headerlink" title="14.6 大页不是一定更好"></a>14.6 大页不是一定更好</h3><p>大页可以降低 TLB 压力，但如果程序只使用大页中的很小一部分，就可能浪费内存。</p><hr><h2 id="❗-本节核心结论：-本讲最容易混淆的是“物理地址与内核虚拟地址”“Page-Fault-与-Segmentation-Fault”“延迟映射与立即映射”“共享与写时复制”这几组概念。"><a href="#❗-本节核心结论：-本讲最容易混淆的是“物理地址与内核虚拟地址”“Page-Fault-与-Segmentation-Fault”“延迟映射与立即映射”“共享与写时复制”这几组概念。" class="headerlink" title="❗ 本节核心结论： 本讲最容易混淆的是“物理地址与内核虚拟地址”“Page Fault 与 Segmentation Fault”“延迟映射与立即映射”“共享与写时复制”这几组概念。"></a><strong>❗ 本节核心结论：</strong> 本讲最容易混淆的是“物理地址与内核虚拟地址”“Page Fault 与 Segmentation Fault”“延迟映射与立即映射”“共享与写时复制”这几组概念。</h2><h2 id="15-复习清单"><a href="#15-复习清单" class="headerlink" title="15. 复习清单"></a>15. 复习清单</h2><p>学完《虚拟内存管理 II》后，应能回答以下问题：</p><ol><li>为什么操作系统自己运行时也使用虚拟地址？</li><li>页表项中保存的是虚拟地址还是物理地址？</li><li>为什么内核不能直接把物理地址当作指针使用？</li><li>直接映射的公式是什么？</li><li>直接映射为什么通常使用连续线性映射？</li><li>一个进程的虚拟地址空间通常有哪些区域？</li><li><code>.text</code>、<code>.rodata</code>、<code>.data</code>、<code>.bss</code>、heap、stack 分别保存什么？</li><li><code>add_mapping</code> 的作用是什么？</li><li>多级页表中，如果下一级页表不存在，操作系统需要做什么？</li><li><code>get_next_pgtbl_page</code> 为什么要调用 <code>paddr_to_vaddr</code>？</li><li>删除页表映射后为什么可能需要刷新 TLB？</li><li>立即映射和延迟映射有什么区别？</li><li>延迟映射下，<code>mmap</code> 时操作系统做了什么？</li><li>第一次访问延迟映射区域时，为什么会触发缺页异常？</li><li>合法缺页和非法访问有什么区别？</li><li>Page Fault 和 Segmentation Fault 有什么区别？</li><li>写时拷贝如何利用页表权限和缺页异常？</li><li>内存去重和写时拷贝有什么关系？</li><li>内存压缩相对于 swap 的优势是什么？</li><li>2MB 大页和 1GB 大页分别是怎么来的？</li><li>大页为什么能提高 TLB 命中率？</li><li>大页可能带来什么问题？</li></ol><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>《虚拟内存管理 II》的核心，是把虚拟内存从“地址翻译机制”推进到“操作系统如何管理地址空间”。</p><p>这一讲可以压缩为一句话：</p><p><strong>操作系统通过直接映射访问物理内存，通过虚拟内存区域描述进程地址空间，通过页表建立虚拟页到物理页的映射，通过缺页异常按需补上映射，并在此基础上实现写时拷贝、内存去重、内存压缩和大页等优化机制。</strong></p><p>真正理解这一讲，关键不是背诵某个函数名，而是形成一个整体直觉：</p><ul><li>进程看到的是虚拟地址空间；</li><li>页表决定虚拟页是否能翻译到物理页；</li><li>操作系统负责维护这些映射；</li><li>延迟映射让物理页按需分配；</li><li>缺页异常不是单纯的错误，而是操作系统介入内存管理的重要入口；</li><li>COW、大页、压缩、去重，都是围绕“减少开销、节省内存、提高性能”展开的优化。</li></ul><p>因此，虚拟内存不是“假内存”，而是一套由硬件翻译、操作系统策略和异常处理共同实现的精密抽象。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第5讲 虚拟内存管理I</title>
      <link href="/2026/06/11/%E7%AC%AC5%E8%AE%B2-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86I/"/>
      <url>/2026/06/11/%E7%AC%AC5%E8%AE%B2-%E8%99%9A%E6%8B%9F%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86I/</url>
      
        <content type="html"><![CDATA[<h1 id="虚拟内存管理-I：从地址隔离到页表翻译"><a href="#虚拟内存管理-I：从地址隔离到页表翻译" class="headerlink" title="虚拟内存管理 I：从地址隔离到页表翻译"></a>虚拟内存管理 I：从地址隔离到页表翻译</h1><p>虚拟内存管理的第一层问题，并不是“物理内存不够时怎么办”，而是“程序为什么不能直接使用物理地址”。现代操作系统让每个进程运行在自己的虚拟地址空间中，程序看到的是虚拟地址，真正访问内存时再由硬件和操作系统配合，把虚拟地址翻译成物理地址。</p><p>这套机制的核心由四个部分组成：</p><ul><li><strong>操作系统</strong>：建立和维护地址翻译规则。</li><li><strong>页表或段表</strong>：保存虚拟地址空间到物理内存的映射关系。</li><li><strong>MMU</strong>：在 CPU 访存时执行地址翻译。</li><li><strong>TLB</strong>：缓存近期翻译结果，降低查页表的成本。</li></ul><hr><h2 id="❗-核心结论：-虚拟内存不是单纯为了“扩大内存”，而是为了给每个进程提供独立、受保护、可管理的地址空间。"><a href="#❗-核心结论：-虚拟内存不是单纯为了“扩大内存”，而是为了给每个进程提供独立、受保护、可管理的地址空间。" class="headerlink" title="❗ 核心结论： 虚拟内存不是单纯为了“扩大内存”，而是为了给每个进程提供独立、受保护、可管理的地址空间。"></a><strong>❗ 核心结论：</strong> 虚拟内存不是单纯为了“扩大内存”，而是为了给每个进程提供独立、受保护、可管理的地址空间。</h2><h2 id="1-为什么不能直接使用物理地址"><a href="#1-为什么不能直接使用物理地址" class="headerlink" title="1. 为什么不能直接使用物理地址"></a>1. 为什么不能直接使用物理地址</h2><p>物理内存就是机器上真实存在的内存条。程序代码、程序数据、操作系统代码和操作系统数据，最终都必须位于物理内存中，CPU 才能访问。</p><p>在早期单程序环境中，物理内存可以简单划分为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">物理内存</span><br><span class="line">├── 操作系统</span><br><span class="line">└── 一个应用程序</span><br></pre></td></tr></table></figure><p>但在多程序系统中，多个应用会同时驻留在内存中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">物理内存</span><br><span class="line">├── 操作系统</span><br><span class="line">├── 应用 A</span><br><span class="line">├── 应用 B</span><br><span class="line">└── 应用 C</span><br></pre></td></tr></table></figure><p>如果应用程序直接使用物理地址，会带来几个问题：</p><ul><li><strong>缺乏隔离</strong>：一个程序可能访问或破坏另一个程序的数据。</li><li><strong>加载位置不稳定</strong>：程序被加载到哪里取决于当时物理内存的空闲情况。</li><li><strong>暴露真实内存布局</strong>：程序能够感知甚至推断系统和其他程序的位置。</li><li><strong>管理困难</strong>：操作系统难以灵活移动、共享或保护内存区域。</li></ul><p>因此，虚拟内存首先解决的是地址抽象和隔离问题。程序使用虚拟地址，物理地址由系统在背后管理。</p><hr><h2 id="❗-本节核心结论：-程序不应直接使用物理地址，因为物理地址暴露真实内存布局，并削弱进程隔离与系统管理能力。"><a href="#❗-本节核心结论：-程序不应直接使用物理地址，因为物理地址暴露真实内存布局，并削弱进程隔离与系统管理能力。" class="headerlink" title="❗ 本节核心结论： 程序不应直接使用物理地址，因为物理地址暴露真实内存布局，并削弱进程隔离与系统管理能力。"></a><strong>❗ 本节核心结论：</strong> 程序不应直接使用物理地址，因为物理地址暴露真实内存布局，并削弱进程隔离与系统管理能力。</h2><h2 id="2-虚拟地址与地址翻译"><a href="#2-虚拟地址与地址翻译" class="headerlink" title="2. 虚拟地址与地址翻译"></a>2. 虚拟地址与地址翻译</h2><p>虚拟内存抽象下，程序中看到的地址通常不是物理地址。例如程序打印出的指针值，本质上属于当前进程的虚拟地址空间。</p><p>一次普通内存访问可以概括为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    CPU[CPU 发出虚拟地址] --&gt; MMU[MMU 地址翻译]    MMU --&gt; PA[得到物理地址]    PA --&gt; MEM[访问物理内存]  </pre></div><p>这张图强调：程序并不是直接拿物理地址访问内存，而是先经过 MMU 的地址翻译。</p><p>各组件的职责可以概括为：</p><table><thead><tr><th>组件</th><th>主要职责</th></tr></thead><tbody><tr><td>CPU</td><td>执行指令，并发出虚拟地址</td></tr><tr><td>MMU</td><td>根据当前地址翻译规则，把虚拟地址翻译为物理地址</td></tr><tr><td>操作系统</td><td>创建、维护、切换段表或页表</td></tr><tr><td>页表&#x2F;TLB</td><td>页表保存映射关系，TLB 缓存近期翻译结果</td></tr></tbody></table><p>这里最重要的边界是：<strong>操作系统负责建表，MMU 负责查表。</strong> MMU 并不会凭空创造映射关系，它只是使用当前进程对应的翻译规则。</p><hr><h2 id="❗-本节核心结论：-地址翻译是硬件与操作系统合作完成的：操作系统建立规则，MMU-在访存路径上使用规则。"><a href="#❗-本节核心结论：-地址翻译是硬件与操作系统合作完成的：操作系统建立规则，MMU-在访存路径上使用规则。" class="headerlink" title="❗ 本节核心结论： 地址翻译是硬件与操作系统合作完成的：操作系统建立规则，MMU 在访存路径上使用规则。"></a><strong>❗ 本节核心结论：</strong> 地址翻译是硬件与操作系统合作完成的：操作系统建立规则，MMU 在访存路径上使用规则。</h2><h2 id="3-虚拟地址不是全局地址"><a href="#3-虚拟地址不是全局地址" class="headerlink" title="3. 虚拟地址不是全局地址"></a>3. 虚拟地址不是全局地址</h2><p>一个常见误区是：如果程序 A 知道程序 B 的虚拟地址，是否就能拿这个地址去查 B 的段表或页表，从而得到 B 的物理地址？</p><p>答案是：正常情况下不能。</p><p>原因在于虚拟地址不是全局地址。虚拟地址必须绑定到“当前是哪一个进程、当前使用哪一张地址翻译表”这个上下文中才有意义。</p><p>例如，同样的虚拟地址在两个进程中可能对应不同物理地址：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">进程 A：虚拟地址 0x400000 → 物理地址 X</span><br><span class="line">进程 B：虚拟地址 0x400000 → 物理地址 Y</span><br></pre></td></tr></table></figure><p>当进程 A 正在运行时，MMU 使用的是进程 A 的段表或页表。即使 A 知道 B 中存在某个虚拟地址，A 自己访问这个数值时，硬件也会按照 A 的地址空间进行翻译，而不是按照 B 的地址空间进行翻译。</p><p>因此，虚拟内存隔离的关键不是“别人不知道你的虚拟地址”，而是：</p><ul><li>别人不能使用你的地址翻译规则。</li><li>别人不能随便读取你的段表或页表。</li><li>程序运行时，MMU 只使用当前上下文绑定的翻译表。</li></ul><hr><h2 id="❗-本节核心结论：-虚拟地址只有在特定进程地址空间中才有意义；单独知道别人的虚拟地址，并不能访问别人的物理内存。"><a href="#❗-本节核心结论：-虚拟地址只有在特定进程地址空间中才有意义；单独知道别人的虚拟地址，并不能访问别人的物理内存。" class="headerlink" title="❗ 本节核心结论： 虚拟地址只有在特定进程地址空间中才有意义；单独知道别人的虚拟地址，并不能访问别人的物理内存。"></a><strong>❗ 本节核心结论：</strong> 虚拟地址只有在特定进程地址空间中才有意义；单独知道别人的虚拟地址，并不能访问别人的物理内存。</h2><h2 id="4-分段机制：直观但会产生外部碎片"><a href="#4-分段机制：直观但会产生外部碎片" class="headerlink" title="4. 分段机制：直观但会产生外部碎片"></a>4. 分段机制：直观但会产生外部碎片</h2><p>分段机制是一种较早、较直观的虚拟内存组织方式。它把虚拟地址空间分成若干不定长段，例如：</p><ul><li>代码段</li><li>数据段</li><li>堆段</li><li>栈段</li></ul><p>分段机制下，虚拟地址可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟地址 = 段号 + 段内偏移</span><br></pre></td></tr></table></figure><p>段表中保存：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">段号 → 物理起始地址 + 段长度</span><br></pre></td></tr></table></figure><p>若某段物理起始地址为 <code>0x500000</code>，段内偏移为 <code>0x350</code>，则物理地址为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x500000 + 0x350 = 0x500350</span><br></pre></td></tr></table></figure><p>分段的优点是符合程序结构直觉：代码、数据、堆、栈本来就像不同区域。但它的主要问题是<strong>外部碎片</strong>。</p><p>因为段的大小不固定，物理内存可能变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[段1][空洞][段3][空洞][段2]</span><br></pre></td></tr></table></figure><p>这些空洞加起来可能很大，但单个空洞不够大时，新段仍然无法放入。这就是分段机制在内存利用率上的局限。</p><table><thead><tr><th>机制</th><th>管理单位</th><th>地址结构</th><th>优点</th><th>主要问题</th></tr></thead><tbody><tr><td>分段</td><td>不定长段</td><td>段号 + 段内偏移</td><td>符合程序结构</td><td>外部碎片</td></tr><tr><td>分页</td><td>固定大小页</td><td>虚拟页号 + 页内偏移</td><td>管理规整，减少外部碎片</td><td>需要页表和地址翻译</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：-分段的抽象很直观，但不定长段容易造成外部碎片，因此现代系统更依赖分页机制。"><a href="#❗-本节核心结论：-分段的抽象很直观，但不定长段容易造成外部碎片，因此现代系统更依赖分页机制。" class="headerlink" title="❗ 本节核心结论： 分段的抽象很直观，但不定长段容易造成外部碎片，因此现代系统更依赖分页机制。"></a><strong>❗ 本节核心结论：</strong> 分段的抽象很直观，但不定长段容易造成外部碎片，因此现代系统更依赖分页机制。</h2><h2 id="5-分页机制：页号翻译，偏移不变"><a href="#5-分页机制：页号翻译，偏移不变" class="headerlink" title="5. 分页机制：页号翻译，偏移不变"></a>5. 分页机制：页号翻译，偏移不变</h2><p>分页机制把虚拟内存和物理内存都切成固定大小的块：</p><ul><li>虚拟地址空间中的块称为<strong>虚拟页</strong>。</li><li>物理内存中的块称为<strong>物理页</strong>。</li><li>常见页大小是 4KB。</li></ul><p>分页下，虚拟地址被拆成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟地址 = 虚拟页号 + 页内偏移</span><br></pre></td></tr></table></figure><p>页表记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号 → 物理页号</span><br></pre></td></tr></table></figure><p>翻译完成后，物理地址为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">物理地址 = 物理页号 + 同一个页内偏移</span><br></pre></td></tr></table></figure><p>这里最关键的是：<strong>地址翻译只改变页号，不改变页内偏移。</strong></p><p>例如，假设页大小是 4KB，即 <code>0x1000</code>。虚拟地址：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x12345</span><br></pre></td></tr></table></figure><p>可以拆成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号 = 0x12</span><br><span class="line">页内偏移 = 0x345</span><br></pre></td></tr></table></figure><p>如果页表中有映射：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号 0x12 → 物理页号 0xABC</span><br></pre></td></tr></table></figure><p>那么物理地址就是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0xABC000 + 0x345 = 0xABC345</span><br></pre></td></tr></table></figure><p>页表并不是为每一个字节单独记录映射，而是为每个虚拟页记录它对应的物理页。页内第 837 个字节，映射后仍然是物理页内第 837 个字节。</p><hr><h2 id="❗-本节核心结论：-页表保存的是“虚拟页号-→-物理页号”，不是逐字节的“虚拟地址-→-物理地址”；页内偏移保持不变。"><a href="#❗-本节核心结论：-页表保存的是“虚拟页号-→-物理页号”，不是逐字节的“虚拟地址-→-物理地址”；页内偏移保持不变。" class="headerlink" title="❗ 本节核心结论： 页表保存的是“虚拟页号 → 物理页号”，不是逐字节的“虚拟地址 → 物理地址”；页内偏移保持不变。"></a><strong>❗ 本节核心结论：</strong> 页表保存的是“虚拟页号 → 物理页号”，不是逐字节的“虚拟地址 → 物理地址”；页内偏移保持不变。</h2><h2 id="6-单级页表为什么会太大"><a href="#6-单级页表为什么会太大" class="headerlink" title="6. 单级页表为什么会太大"></a>6. 单级页表为什么会太大</h2><p>单级页表可以想象成一个巨大数组：每个虚拟页号对应一个页表项。</p><p>它的空间计算公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">页表大小 = 虚拟页数量 × 每个页表项大小</span><br></pre></td></tr></table></figure><p>其中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页数量 = 虚拟地址空间大小 / 页大小</span><br></pre></td></tr></table></figure><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">页表大小 = 虚拟地址空间大小 / 页大小 × 页表项大小</span><br></pre></td></tr></table></figure><h3 id="6-1-32-位地址空间示例"><a href="#6-1-32-位地址空间示例" class="headerlink" title="6.1 32 位地址空间示例"></a>6.1 32 位地址空间示例</h3><p>设定：</p><ul><li>虚拟地址空间：32 位，即 <code>2^32</code> 字节。</li><li>页大小：4KB，即 <code>2^12</code> 字节。</li><li>页表项大小：4 字节。</li></ul><p>则虚拟页数量为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^32 / 2^12 = 2^20</span><br></pre></td></tr></table></figure><p>页表大小为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^20 × 4B = 2^22B = 4MB</span><br></pre></td></tr></table></figure><p>一个进程一张 4MB 页表，在 32 位系统中还勉强可以接受。</p><h3 id="6-2-64-位地址空间示例"><a href="#6-2-64-位地址空间示例" class="headerlink" title="6.2 64 位地址空间示例"></a>6.2 64 位地址空间示例</h3><p>设定：</p><ul><li>虚拟地址空间：64 位，即 <code>2^64</code> 字节。</li><li>页大小：4KB，即 <code>2^12</code> 字节。</li><li>页表项大小：8 字节。</li></ul><p>则虚拟页数量为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^64 / 2^12 = 2^52</span><br></pre></td></tr></table></figure><p>页表大小为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^52 × 8B = 2^55B</span><br></pre></td></tr></table></figure><p>换算为 GB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^55 / 2^30 = 2^25GB = 33,554,432GB</span><br></pre></td></tr></table></figure><p>这个规模显然不可接受。问题的根源在于：单级页表需要为整个虚拟地址空间中的每个虚拟页准备页表项，即使绝大多数虚拟地址根本没有被使用。</p><hr><h2 id="❗-本节核心结论：-单级页表的空间浪费来自“覆盖整个虚拟地址空间”；64-位地址空间下，这种设计会导致页表规模不可接受。"><a href="#❗-本节核心结论：-单级页表的空间浪费来自“覆盖整个虚拟地址空间”；64-位地址空间下，这种设计会导致页表规模不可接受。" class="headerlink" title="❗ 本节核心结论： 单级页表的空间浪费来自“覆盖整个虚拟地址空间”；64 位地址空间下，这种设计会导致页表规模不可接受。"></a><strong>❗ 本节核心结论：</strong> 单级页表的空间浪费来自“覆盖整个虚拟地址空间”；64 位地址空间下，这种设计会导致页表规模不可接受。</h2><h2 id="7-多级页表：用更多查询步骤换更少空间"><a href="#7-多级页表：用更多查询步骤换更少空间" class="headerlink" title="7. 多级页表：用更多查询步骤换更少空间"></a>7. 多级页表：用更多查询步骤换更少空间</h2><p>多级页表的基本思想是：<strong>不要为根本没有使用的虚拟地址区域分配页表。</strong></p><p>单级页表像一个巨大数组：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号 → 页表项</span><br></pre></td></tr></table></figure><p>多级页表更像一棵按需展开的树：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    L0[L0 页表] --&gt; L1a[L1 页表]    L0 --&gt; Hole1[空洞：不分配下级页表]    L1a --&gt; L2a[L2 页表]    L1a --&gt; Hole2[空洞：不分配下级页表]    L2a --&gt; L3a[L3 页表]    L3a --&gt; P[物理页]  </pre></div><p>这张图说明：如果某个地址区域没有被使用，对应的下级页表可以不存在，从而节省空间。</p><p>多级页表的代价是翻译变慢。如果没有 TLB，地址翻译可能需要逐级访问多张页表，最后才能得到物理页号。</p><p>因此，多级页表体现的是一个典型权衡：</p><table><thead><tr><th>方案</th><th align="right">空间开销</th><th align="right">翻译速度</th><th>核心特点</th></tr></thead><tbody><tr><td>单级页表</td><td align="right">大</td><td align="right">查一次表较直接</td><td>为整个虚拟地址空间建表</td></tr><tr><td>多级页表</td><td align="right">小</td><td align="right">可能多次查表</td><td>只为使用到的区域分配下级页表</td></tr></tbody></table><p>多级页表不是 MMU 自动创建的。它由操作系统在进程创建、程序加载或内存分配时按需创建和维护。MMU 的职责是在运行时按照页表结构逐级查询。</p><hr><h2 id="❗-本节核心结论：-多级页表通过允许地址空间中存在“空洞”来节省空间，但代价是一次地址翻译可能需要多次查表。"><a href="#❗-本节核心结论：-多级页表通过允许地址空间中存在“空洞”来节省空间，但代价是一次地址翻译可能需要多次查表。" class="headerlink" title="❗ 本节核心结论： 多级页表通过允许地址空间中存在“空洞”来节省空间，但代价是一次地址翻译可能需要多次查表。"></a><strong>❗ 本节核心结论：</strong> 多级页表通过允许地址空间中存在“空洞”来节省空间，但代价是一次地址翻译可能需要多次查表。</h2><h2 id="8-AArch64-四级页表：9-9-9-9-12"><a href="#8-AArch64-四级页表：9-9-9-9-12" class="headerlink" title="8. AArch64 四级页表：9 + 9 + 9 + 9 + 12"></a>8. AArch64 四级页表：9 + 9 + 9 + 9 + 12</h2><p>以 AArch64 的常见设定为例：</p><ul><li>页大小：4KB。</li><li>页内偏移位数：<code>log2(4KB) = 12</code> 位。</li><li>有效虚拟地址：48 位。</li></ul><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">48 位虚拟地址 = 36 位虚拟页号 + 12 位页内偏移</span><br></pre></td></tr></table></figure><p>36 位虚拟页号被分成 4 级索引：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">36 = 9 + 9 + 9 + 9</span><br></pre></td></tr></table></figure><p>所以地址结构可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号_0 | 虚拟页号_1 | 虚拟页号_2 | 虚拟页号_3 | 页内偏移</span><br><span class="line">    9 位         9 位         9 位         9 位        12 位</span><br></pre></td></tr></table></figure><p>每一级 9 位，可以索引：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^9 = 512</span><br></pre></td></tr></table></figure><p>也就是说，每一级页表最多有 512 个页表项。</p><p>AArch64 四级页表的翻译过程可以概括为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    VA[虚拟地址] --&gt; Split[拆分为 9+9+9+9+12]    Split --&gt; TTBR[TTBR0_EL1 指向 L0 页表]    TTBR --&gt; L0[用第 1 个 9 位索引 L0]    L0 --&gt; L1[用第 2 个 9 位索引 L1]    L1 --&gt; L2[用第 3 个 9 位索引 L2]    L2 --&gt; L3[用第 4 个 9 位索引 L3]    L3 --&gt; PPN[得到物理页号]    PPN --&gt; PA[物理页号 + 页内偏移 &#x3D; 物理地址]  </pre></div><p>这张图强调：四级页表并不是四种不同地址，而是把虚拟页号分成四段，逐层缩小搜索范围，最后找到物理页号。</p><hr><h2 id="❗-本节核心结论：-在-4KB-页、48-位有效虚拟地址下，AArch64-四级页表的地址拆分规律是-9-9-9-9-12。"><a href="#❗-本节核心结论：-在-4KB-页、48-位有效虚拟地址下，AArch64-四级页表的地址拆分规律是-9-9-9-9-12。" class="headerlink" title="❗ 本节核心结论： 在 4KB 页、48 位有效虚拟地址下，AArch64 四级页表的地址拆分规律是 9 + 9 + 9 + 9 + 12。"></a><strong>❗ 本节核心结论：</strong> 在 4KB 页、48 位有效虚拟地址下，AArch64 四级页表的地址拆分规律是 <code>9 + 9 + 9 + 9 + 12</code>。</h2><h2 id="9-页表项不仅保存物理页号"><a href="#9-页表项不仅保存物理页号" class="headerlink" title="9. 页表项不仅保存物理页号"></a>9. 页表项不仅保存物理页号</h2><p>页表项的核心内容是物理页号，但它通常不只保存物理页号。它还包含权限和状态信息。</p><p>常见字段包括：</p><table><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td>valid 位</td><td>表示页表项是否有效</td></tr><tr><td>AP 位</td><td>控制读写权限</td></tr><tr><td>XN 位</td><td>控制用户态是否禁止执行</td></tr><tr><td>PXN 位</td><td>控制内核态是否禁止执行</td></tr><tr><td>AF 位</td><td>Access Flag，可用于记录页面是否被访问</td></tr><tr><td>AttrIndx</td><td>指示内存类型，如普通内存或设备内存</td></tr></tbody></table><p>因此，页表不只是“地址翻译表”，也是保护机制的一部分。MMU 在查页表时，不仅要得到物理页号，还可能检查当前访问是否合法。</p><p>当页表项无效、权限不足或执行控制不允许时，MMU 不应简单返回物理地址，而应触发异常，让操作系统处理。</p><hr><h2 id="❗-本节核心结论：-页表项既表达地址映射，也表达访问权限、执行控制和内存属性；页表同时承担翻译与保护功能。"><a href="#❗-本节核心结论：-页表项既表达地址映射，也表达访问权限、执行控制和内存属性；页表同时承担翻译与保护功能。" class="headerlink" title="❗ 本节核心结论： 页表项既表达地址映射，也表达访问权限、执行控制和内存属性；页表同时承担翻译与保护功能。"></a><strong>❗ 本节核心结论：</strong> 页表项既表达地址映射，也表达访问权限、执行控制和内存属性；页表同时承担翻译与保护功能。</h2><h2 id="10-页表使能：从物理寻址到虚拟寻址"><a href="#10-页表使能：从物理寻址到虚拟寻址" class="headerlink" title="10. 页表使能：从物理寻址到虚拟寻址"></a>10. 页表使能：从物理寻址到虚拟寻址</h2><p>CPU 刚启动时，系统通常处于较原始的物理寻址阶段。操作系统或底层系统软件初始化页表后，会配置控制寄存器，使地址翻译生效。</p><p>典型例子包括：</p><table><thead><tr><th>架构</th><th>控制方式</th></tr></thead><tbody><tr><td>AArch64</td><td>设置 <code>SCTLR_EL1</code> 的 <code>M</code> 位</td></tr><tr><td>x86_64</td><td>设置 <code>CR0</code> 的 <code>PG</code> 位</td></tr></tbody></table><p>可以简化理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">页表未使能：CPU 发出的地址直接当作物理地址</span><br><span class="line">页表已使能：CPU 发出的地址先经 MMU 翻译，再访问物理内存</span><br></pre></td></tr></table></figure><p>这说明虚拟内存不是只存在于软件概念中，它需要硬件控制位和页表基地址寄存器共同支持。</p><hr><h2 id="❗-本节核心结论：-页表只有在硬件控制位开启后才参与访存路径；开启后，CPU-发出的地址会经过-MMU-翻译。"><a href="#❗-本节核心结论：-页表只有在硬件控制位开启后才参与访存路径；开启后，CPU-发出的地址会经过-MMU-翻译。" class="headerlink" title="❗ 本节核心结论： 页表只有在硬件控制位开启后才参与访存路径；开启后，CPU 发出的地址会经过 MMU 翻译。"></a><strong>❗ 本节核心结论：</strong> 页表只有在硬件控制位开启后才参与访存路径；开启后，CPU 发出的地址会经过 MMU 翻译。</h2><h2 id="11-TLB：让多级页表重新变快"><a href="#11-TLB：让多级页表重新变快" class="headerlink" title="11. TLB：让多级页表重新变快"></a>11. TLB：让多级页表重新变快</h2><p>多级页表节省空间，但也增加了地址翻译成本。若每次访问内存都要查四级页表，那么一次普通访存可能变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">查 L0 页表</span><br><span class="line">查 L1 页表</span><br><span class="line">查 L2 页表</span><br><span class="line">查 L3 页表</span><br><span class="line">真正访问数据</span><br></pre></td></tr></table></figure><p>为降低这个成本，CPU 内部提供 TLB：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TLB = Translation Lookaside Buffer</span><br></pre></td></tr></table></figure><p>它缓存的是近期使用过的地址翻译结果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">虚拟页号 → 物理页号</span><br></pre></td></tr></table></figure><p>地址翻译过程可以概括为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    VA[虚拟地址] --&gt; TLB{TLB 命中?}    TLB -- 是 --&gt; PPN[直接得到物理页号]    TLB -- 否 --&gt; WALK[执行多级页表查询]    WALK --&gt; FILL[把结果填入 TLB]    FILL --&gt; PPN    PPN --&gt; PA[拼接页内偏移]  </pre></div><p>TLB 有效的原因是程序访问内存具有局部性：</p><table><thead><tr><th>局部性</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td>时间局部性</td><td>刚访问过的地址，之后很可能再次访问</td><td>循环中反复访问同一变量</td></tr><tr><td>空间局部性</td><td>访问某地址后，附近地址也很可能被访问</td><td>顺序遍历数组</td></tr></tbody></table><p>例如连续访问数组 <code>a[i]</code> 时，多个元素可能位于同一页内。只要这一页的映射已经在 TLB 中，访问同页其他元素时就可以复用同一条翻译结果。</p><hr><h2 id="❗-本节核心结论：-TLB-是页表查询结果的缓存，它利用程序访问局部性减少多级页表查询次数。"><a href="#❗-本节核心结论：-TLB-是页表查询结果的缓存，它利用程序访问局部性减少多级页表查询次数。" class="headerlink" title="❗ 本节核心结论： TLB 是页表查询结果的缓存，它利用程序访问局部性减少多级页表查询次数。"></a><strong>❗ 本节核心结论：</strong> TLB 是页表查询结果的缓存，它利用程序访问局部性减少多级页表查询次数。</h2><h2 id="12-进程切换、TLB-刷新与-ASID"><a href="#12-进程切换、TLB-刷新与-ASID" class="headerlink" title="12. 进程切换、TLB 刷新与 ASID"></a>12. 进程切换、TLB 刷新与 ASID</h2><p>TLB 缓存的是虚拟页号到物理页号的映射。但不同进程有不同页表，可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">进程 A：虚拟页 0 → 物理页 1</span><br><span class="line">进程 B：虚拟页 0 → 物理页 9</span><br></pre></td></tr></table></figure><p>如果进程切换后仍错误复用旧进程的 TLB 项，就会把 A 的映射用于 B，造成严重错误。</p><p>最直接的解决方式是：每次进程切换都刷新 TLB。但这会丢失缓存，性能较差。</p><p>更好的办法是给 TLB 项加入地址空间标签。例如 AArch64 使用 ASID：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ASID = Address Space ID</span><br></pre></td></tr></table></figure><p>TLB 项可理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ASID + 虚拟页号 → 物理页号</span><br></pre></td></tr></table></figure><p>这样，即使两个进程都有虚拟页 0，也能区分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ASID 1 + 虚拟页 0 → 物理页 1</span><br><span class="line">ASID 5 + 虚拟页 0 → 物理页 9</span><br></pre></td></tr></table></figure><p>ASID 的意义是：把“当前属于哪个地址空间”这个上下文显式加入 TLB 项，从而减少不必要的全量刷新。</p><hr><h2 id="❗-本节核心结论：-ASID-让-TLB-项带上地址空间身份，避免不同进程的相同虚拟页号发生混淆。"><a href="#❗-本节核心结论：-ASID-让-TLB-项带上地址空间身份，避免不同进程的相同虚拟页号发生混淆。" class="headerlink" title="❗ 本节核心结论： ASID 让 TLB 项带上地址空间身份，避免不同进程的相同虚拟页号发生混淆。"></a><strong>❗ 本节核心结论：</strong> ASID 让 TLB 项带上地址空间身份，避免不同进程的相同虚拟页号发生混淆。</h2><h2 id="13-MMU、操作系统、页表和-TLB-的职责边界"><a href="#13-MMU、操作系统、页表和-TLB-的职责边界" class="headerlink" title="13. MMU、操作系统、页表和 TLB 的职责边界"></a>13. MMU、操作系统、页表和 TLB 的职责边界</h2><p>虚拟内存管理容易混淆的地方，是把“谁建立规则”和“谁使用规则”混为一谈。可以用下表区分：</p><table><thead><tr><th>角色</th><th>做什么</th><th>不做什么</th></tr></thead><tbody><tr><td>操作系统</td><td>创建页表、分配页表页、填写 PTE、设置权限、切换页表</td><td>不在每次普通访存时手动翻译地址</td></tr><tr><td>MMU</td><td>根据当前页表和 TLB 执行地址翻译与权限检查</td><td>不自行决定哪些虚拟页应映射到哪些物理页</td></tr><tr><td>TLB</td><td>缓存近期翻译结果</td><td>不替代页表作为权威映射来源</td></tr><tr><td>CPU</td><td>执行指令并发起访存</td><td>不直接理解进程隔离策略</td></tr></tbody></table><p>多级页表的创建流程可以简化为：</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    Need[需要映射某个虚拟地址] --&gt; Check0[检查 L0 项]    Check0 --&gt;|不存在| Alloc1[分配 L1 页表]    Check0 --&gt;|存在| Check1[检查 L1 项]    Alloc1 --&gt; Check1    Check1 --&gt;|不存在| Alloc2[分配 L2 页表]    Check1 --&gt;|存在| Check2[检查 L2 项]    Alloc2 --&gt; Check2    Check2 --&gt;|不存在| Alloc3[分配 L3 页表]    Check2 --&gt;|存在| Fill[填写最终页表项]    Alloc3 --&gt; Fill    Fill --&gt; Done[写入物理页号与权限位]  </pre></div><p>这张图说明：多级页表由操作系统按需创建；程序运行时，MMU 只是沿着已经建立好的结构查找。</p><hr><h2 id="❗-本节核心结论：-操作系统负责构造地址空间，MMU-负责执行翻译，TLB-负责缓存结果；三者分工不能混淆。"><a href="#❗-本节核心结论：-操作系统负责构造地址空间，MMU-负责执行翻译，TLB-负责缓存结果；三者分工不能混淆。" class="headerlink" title="❗ 本节核心结论： 操作系统负责构造地址空间，MMU 负责执行翻译，TLB 负责缓存结果；三者分工不能混淆。"></a><strong>❗ 本节核心结论：</strong> 操作系统负责构造地址空间，MMU 负责执行翻译，TLB 负责缓存结果；三者分工不能混淆。</h2><h2 id="14-常见误区集中澄清"><a href="#14-常见误区集中澄清" class="headerlink" title="14. 常见误区集中澄清"></a>14. 常见误区集中澄清</h2><h3 id="14-1-虚拟内存不是只为了解决“内存不够”"><a href="#14-1-虚拟内存不是只为了解决“内存不够”" class="headerlink" title="14.1 虚拟内存不是只为了解决“内存不够”"></a>14.1 虚拟内存不是只为了解决“内存不够”</h3><p>虚拟内存更基础的目标是隔离、保护和地址空间独立。扩大可用内存只是虚拟内存体系中更后续的一部分主题。</p><h3 id="14-2-页表不是保存“每个虚拟地址到物理地址”"><a href="#14-2-页表不是保存“每个虚拟地址到物理地址”" class="headerlink" title="14.2 页表不是保存“每个虚拟地址到物理地址”"></a>14.2 页表不是保存“每个虚拟地址到物理地址”</h3><p>分页以页为单位管理内存。页表保存的是“虚拟页号 → 物理页号”，页内偏移直接保留。</p><h3 id="14-3-知道别人的虚拟地址没有直接意义"><a href="#14-3-知道别人的虚拟地址没有直接意义" class="headerlink" title="14.3 知道别人的虚拟地址没有直接意义"></a>14.3 知道别人的虚拟地址没有直接意义</h3><p>虚拟地址必须绑定进程地址空间。进程 A 知道进程 B 的虚拟地址，并不代表 A 能让 MMU 使用 B 的页表。</p><h3 id="14-4-多级页表不是-MMU-创建的"><a href="#14-4-多级页表不是-MMU-创建的" class="headerlink" title="14.4 多级页表不是 MMU 创建的"></a>14.4 多级页表不是 MMU 创建的</h3><p>多级页表由操作系统创建和维护。MMU 的职责是读取页表并执行地址翻译。</p><h3 id="14-5-TLB-不是另一张完整页表"><a href="#14-5-TLB-不是另一张完整页表" class="headerlink" title="14.5 TLB 不是另一张完整页表"></a>14.5 TLB 不是另一张完整页表</h3><p>TLB 是缓存，不是权威数据结构。TLB 未命中时仍需要查询页表。</p><hr><h2 id="❗-本节核心结论：-理解虚拟内存的关键，是把“地址空间上下文、页表映射、硬件翻译、缓存加速”四件事分清楚。"><a href="#❗-本节核心结论：-理解虚拟内存的关键，是把“地址空间上下文、页表映射、硬件翻译、缓存加速”四件事分清楚。" class="headerlink" title="❗ 本节核心结论： 理解虚拟内存的关键，是把“地址空间上下文、页表映射、硬件翻译、缓存加速”四件事分清楚。"></a><strong>❗ 本节核心结论：</strong> 理解虚拟内存的关键，是把“地址空间上下文、页表映射、硬件翻译、缓存加速”四件事分清楚。</h2><h2 id="15-复习清单"><a href="#15-复习清单" class="headerlink" title="15. 复习清单"></a>15. 复习清单</h2><p>学完《虚拟内存管理 I》后，应能回答以下问题：</p><ol><li>为什么应用程序不应该直接使用物理地址？</li><li>虚拟地址和物理地址分别是什么？</li><li>为什么虚拟地址必须绑定进程地址空间？</li><li>分段机制的地址结构是什么？</li><li>分段为什么会产生外部碎片？</li><li>分页机制下，虚拟地址由哪两部分组成？</li><li>页表保存的到底是什么映射？</li><li>为什么页内偏移在地址翻译后保持不变？</li><li>单级页表大小如何计算？</li><li>为什么 64 位系统不能简单使用完整单级页表？</li><li>多级页表为什么能节省空间？</li><li>AArch64 四级页表中 <code>9 + 9 + 9 + 9 + 12</code> 分别表示什么？</li><li>页表项除了物理页号，还可能保存哪些控制信息？</li><li>MMU 在地址翻译中负责什么？</li><li>操作系统在页表管理中负责什么？</li><li>TLB 缓存的是什么？</li><li>为什么程序局部性会提高 TLB 的有效性？</li><li>ASID 为什么能减少进程切换时的 TLB 刷新开销？</li></ol><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>《虚拟内存管理 I》的核心链条可以压缩为一句话：</p><p><strong>程序使用虚拟地址；操作系统为每个进程建立独立的地址翻译规则；MMU 根据当前规则把虚拟地址翻译为物理地址；多级页表节省页表空间；TLB 缓存翻译结果以降低查表成本。</strong></p><p>因此，虚拟内存不是单个机制，而是一组机制的组合。分段和分页解决地址组织问题，页表解决映射保存问题，多级页表解决空间开销问题，TLB 解决翻译速度问题，ASID 解决多进程上下文切换中的缓存混淆问题。</p><p>真正掌握这一节，关键不在于死记每个字段，而在于形成一个稳定直觉：<strong>程序运行在虚拟地址世界中，物理内存由操作系统和硬件共同隐藏、翻译和保护。</strong></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>第2讲 操作系统结构</title>
      <link href="/2026/06/11/%E7%AC%AC2%E8%AE%B2-%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%BB%93%E6%9E%84/"/>
      <url>/2026/06/11/%E7%AC%AC2%E8%AE%B2-%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%BB%93%E6%9E%84/</url>
      
        <content type="html"><![CDATA[<h1 id="第2讲：操作系统结构学习指南"><a href="#第2讲：操作系统结构学习指南" class="headerlink" title="第2讲：操作系统结构学习指南"></a>第2讲：操作系统结构学习指南</h1><h2 id="0-本讲主线"><a href="#0-本讲主线" class="headerlink" title="0. 本讲主线"></a>0. 本讲主线</h2><p>操作系统很复杂，它要管理：</p><ul><li>CPU</li><li>内存</li><li>文件系统</li><li>设备驱动</li><li>网络</li><li>进程与线程</li><li>安全隔离</li></ul><p>所以问题不是“OS 有哪些功能”，而是：</p><blockquote><p><strong>这些功能应该如何组织，才能既高效、又可靠、又方便扩展？</strong></p></blockquote><hr><h2 id="❗-本讲核心结论：操作系统结构的本质，是为了控制复杂性。不同结构的区别，主要体现在“哪些功能放在内核态，哪些功能放在用户态，以及模块之间如何通信”。"><a href="#❗-本讲核心结论：操作系统结构的本质，是为了控制复杂性。不同结构的区别，主要体现在“哪些功能放在内核态，哪些功能放在用户态，以及模块之间如何通信”。" class="headerlink" title="❗ 本讲核心结论：操作系统结构的本质，是为了控制复杂性。不同结构的区别，主要体现在“哪些功能放在内核态，哪些功能放在用户态，以及模块之间如何通信”。"></a>❗ <strong>本讲核心结论：</strong><br>操作系统结构的本质，是为了控制复杂性。不同结构的区别，主要体现在“哪些功能放在内核态，哪些功能放在用户态，以及模块之间如何通信”。</h2><h2 id="1-为什么操作系统需要结构？"><a href="#1-为什么操作系统需要结构？" class="headerlink" title="1. 为什么操作系统需要结构？"></a>1. 为什么操作系统需要结构？</h2><p>复杂系统如果只堆功能，很容易失控。</p><p>课件用瓦萨沉船、N1 火箭之类的例子说明：<strong>功能越多不等于系统越强，结构不合理时，功能越多反而越危险。</strong></p><p>操作系统也是一样。</p><p>如果一个 OS 把所有功能随便堆在一起，就会出现：</p><ul><li>一个模块出错，可能拖垮整个系统；</li><li>模块之间耦合严重，难以修改；</li><li>新硬件、新需求出现时，扩展困难；</li><li>安全边界模糊，应用可能互相干扰。</li></ul><p>所以，OS 结构设计的目标是：</p><blockquote><p><strong>让复杂功能可以被管理、维护、隔离和扩展。</strong></p></blockquote><hr><h2 id="❗-本节核心结论：OS-结构不是为了“看起来高级”，而是为了让庞大的系统保持可控。"><a href="#❗-本节核心结论：OS-结构不是为了“看起来高级”，而是为了让庞大的系统保持可控。" class="headerlink" title="❗ 本节核心结论：OS 结构不是为了“看起来高级”，而是为了让庞大的系统保持可控。"></a>❗ <strong>本节核心结论：</strong><br>OS 结构不是为了“看起来高级”，而是为了让庞大的系统保持可控。</h2><h2 id="2-操作系统设计中的目标冲突"><a href="#2-操作系统设计中的目标冲突" class="headerlink" title="2. 操作系统设计中的目标冲突"></a>2. 操作系统设计中的目标冲突</h2><p>OS 同时面对两类目标。</p><table><thead><tr><th>目标类型</th><th>关注点</th></tr></thead><tbody><tr><td>用户目标</td><td>好用、稳定、安全、流畅、功能完整</td></tr><tr><td>系统目标</td><td>易实现、易维护、高效、可靠、可扩展</td></tr></tbody></table><p>问题在于：这些目标经常冲突。</p><p>例如：</p><ul><li>安全隔离越强，性能可能越差；</li><li>抽象越统一，可能越难发挥硬件极限性能；</li><li>功能越多，系统越复杂；</li><li>兼容性越强，系统负担越重。</li></ul><p>所以操作系统结构设计，本质上是工程权衡。</p><hr><h2 id="❗-本节核心结论：操作系统没有“完美结构”，只有针对不同目标做出的取舍。"><a href="#❗-本节核心结论：操作系统没有“完美结构”，只有针对不同目标做出的取舍。" class="headerlink" title="❗ 本节核心结论：操作系统没有“完美结构”，只有针对不同目标做出的取舍。"></a>❗ <strong>本节核心结论：</strong><br>操作系统没有“完美结构”，只有针对不同目标做出的取舍。</h2><h2 id="3-策略与机制分离"><a href="#3-策略与机制分离" class="headerlink" title="3. 策略与机制分离"></a>3. 策略与机制分离</h2><p>这是本讲最重要的思想之一。</p><h3 id="3-1-机制是什么？"><a href="#3-1-机制是什么？" class="headerlink" title="3.1 机制是什么？"></a>3.1 机制是什么？</h3><p>机制回答的是：</p><blockquote><p><strong>怎么做？</strong></p></blockquote><p>比如调度系统中的机制包括：</p><ul><li>调度队列</li><li>线程控制块</li><li>时钟中断</li><li>上下文切换</li><li>调度器入口</li></ul><p>这些是底层工具，通常比较稳定。</p><h3 id="3-2-策略是什么？"><a href="#3-2-策略是什么？" class="headerlink" title="3.2 策略是什么？"></a>3.2 策略是什么？</h3><p>策略回答的是：</p><blockquote><p><strong>按什么规则做？</strong></p></blockquote><p>比如调度策略包括：</p><ul><li>先来先服务 FCFS</li><li>时间片轮转 RR</li><li>优先级调度</li><li>最早截止时间优先 EDF</li></ul><p>这些规则可以根据场景变化。</p><h3 id="3-3-为什么要分离？"><a href="#3-3-为什么要分离？" class="headerlink" title="3.3 为什么要分离？"></a>3.3 为什么要分离？</h3><p>因为：</p><ul><li>机制底层，改动代价大；</li><li>策略上层，应该更灵活；</li><li>好的系统应该允许“机制稳定，策略可换”。</li></ul><p>例如，调度系统不应该把“只能使用时间片轮转”写死，而应该提供通用调度机制，让不同调度策略可以替换。</p><hr><h2 id="❗-本节核心结论：机制是工具，策略是规则。好的-OS-设计应该让底层机制稳定，让上层策略灵活变化。"><a href="#❗-本节核心结论：机制是工具，策略是规则。好的-OS-设计应该让底层机制稳定，让上层策略灵活变化。" class="headerlink" title="❗ 本节核心结论：机制是工具，策略是规则。好的 OS 设计应该让底层机制稳定，让上层策略灵活变化。"></a>❗ <strong>本节核心结论：</strong><br>机制是工具，策略是规则。好的 OS 设计应该让底层机制稳定，让上层策略灵活变化。</h2><h2 id="4-M-A-L-H-原则"><a href="#4-M-A-L-H-原则" class="headerlink" title="4. M.A.L.H. 原则"></a>4. M.A.L.H. 原则</h2><p>课件提出四个结构化原则：</p><table><thead><tr><th>原则</th><th>含义</th><th>例子</th></tr></thead><tbody><tr><td>Modularity 模块化</td><td>把大系统拆成小模块</td><td>进程管理、内存管理、文件系统、设备驱动</td></tr><tr><td>Abstract 抽象</td><td>只暴露接口，不暴露内部细节</td><td><code>open()</code>、<code>read()</code>、<code>write()</code></td></tr><tr><td>Layering 分层</td><td>上层建立在下层能力之上</td><td>应用层 → 系统调用层 → 文件系统层 → 驱动层</td></tr><tr><td>Hierarchy 层级</td><td>整体与部分的组织关系</td><td>OS → 内存管理 → 虚拟内存 → 页表管理</td></tr></tbody></table><h3 id="分层-vs-层级"><a href="#分层-vs-层级" class="headerlink" title="分层 vs 层级"></a>分层 vs 层级</h3><p>这两个容易混。</p><table><thead><tr><th>概念</th><th>重点</th></tr></thead><tbody><tr><td>分层 Layering</td><td>强调上下依赖关系</td></tr><tr><td>层级 Hierarchy</td><td>强调整体和部分的包含关系</td></tr></tbody></table><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">应用程序</span><br><span class="line">  ↓</span><br><span class="line">系统调用</span><br><span class="line">  ↓</span><br><span class="line">文件系统</span><br><span class="line">  ↓</span><br><span class="line">块设备层</span><br><span class="line">  ↓</span><br><span class="line">磁盘驱动</span><br><span class="line">  ↓</span><br><span class="line">硬件</span><br></pre></td></tr></table></figure><p>这是分层。</p><p>而：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">操作系统</span><br><span class="line"> ├── 内存管理</span><br><span class="line"> │    ├── 虚拟内存</span><br><span class="line"> │    ├── 页表管理</span><br><span class="line"> │    └── 页面置换</span><br><span class="line"> ├── 文件系统</span><br><span class="line"> └── 进程管理</span><br></pre></td></tr></table></figure><p>这是层级。</p><hr><h2 id="❗-本节核心结论：模块化、抽象、分层、层级，都是为了降低复杂系统的理解和维护成本。"><a href="#❗-本节核心结论：模块化、抽象、分层、层级，都是为了降低复杂系统的理解和维护成本。" class="headerlink" title="❗ 本节核心结论：模块化、抽象、分层、层级，都是为了降低复杂系统的理解和维护成本。"></a>❗ <strong>本节核心结论：</strong><br>模块化、抽象、分层、层级，都是为了降低复杂系统的理解和维护成本。</h2><h2 id="5-为什么需要内核？"><a href="#5-为什么需要内核？" class="headerlink" title="5. 为什么需要内核？"></a>5. 为什么需要内核？</h2><p>核心问题是：</p><blockquote><p>为什么不能让每个应用程序直接控制硬件？</p></blockquote><p>答案是：<strong>多个应用要共享硬件资源。</strong></p><p>例如：</p><ul><li>多个程序都要用 CPU；</li><li>多个程序都要申请内存；</li><li>多个程序都要读写磁盘；</li><li>多个程序都要使用网卡。</li></ul><p>如果没有内核统一管理，应用之间会互相干扰。</p><p>比如微信和飞书都要用网卡。如果应用能直接控制网卡，就可能出现：</p><ul><li>一个程序霸占网卡；</li><li>一个程序读到另一个程序的数据；</li><li>一个程序错误操作导致整个设备异常。</li></ul><p>所以内核的核心职责是：</p><blockquote><p><strong>管理硬件资源，并隔离不同应用程序。</strong></p></blockquote><hr><h2 id="❗-本节核心结论：内核存在的根本原因，是多个应用共享硬件时需要统一管理和安全隔离。"><a href="#❗-本节核心结论：内核存在的根本原因，是多个应用共享硬件时需要统一管理和安全隔离。" class="headerlink" title="❗ 本节核心结论：内核存在的根本原因，是多个应用共享硬件时需要统一管理和安全隔离。"></a>❗ <strong>本节核心结论：</strong><br>内核存在的根本原因，是多个应用共享硬件时需要统一管理和安全隔离。</h2><h2 id="6-操作系统结构总览图"><a href="#6-操作系统结构总览图" class="headerlink" title="6. 操作系统结构总览图"></a>6. 操作系统结构总览图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[操作系统结构] --&gt; B[简要结构]    A --&gt; C[宏内核]    A --&gt; D[微内核]    A --&gt; E[混合内核]    A --&gt; F[外核]    A --&gt; G[多内核]    C --&gt; C1[大量服务在内核态]    D --&gt; D1[核心机制在内核态&lt;br&#x2F;&gt;服务放用户态]    E --&gt; E1[吸收微内核思想&lt;br&#x2F;&gt;性能敏感部分回到内核态]    F --&gt; F1[内核只管资源保护&lt;br&#x2F;&gt;抽象交给 LibOS]    G --&gt; G1[每个 core 一个小内核&lt;br&#x2F;&gt;通过消息通信]  </pre></div><p>这张图的重点是：不同结构的核心差异在于 <strong>功能放在哪里，以及模块如何通信</strong>。</p><h2 id="7-简要结构"><a href="#7-简要结构" class="headerlink" title="7. 简要结构"></a>7. 简要结构</h2><p>简要结构可以理解为：</p><blockquote><p>OS 和应用程序像一个大程序。</p></blockquote><p>特点：</p><ul><li>没有完善内存管理；</li><li>没有清晰特权级隔离；</li><li>应用和 OS 边界不清；</li><li>函数调用快；</li><li>但一个地方崩了，整个系统可能崩。</li></ul><p>典型例子：<strong>MS-DOS</strong>。</p><p>它的优点是简单、快；缺点是安全性和可靠性差。</p><hr><h2 id="❗-本节核心结论：简要结构性能和实现都简单，但隔离性很差，适合早期简单环境，不适合现代复杂系统。"><a href="#❗-本节核心结论：简要结构性能和实现都简单，但隔离性很差，适合早期简单环境，不适合现代复杂系统。" class="headerlink" title="❗ 本节核心结论：简要结构性能和实现都简单，但隔离性很差，适合早期简单环境，不适合现代复杂系统。"></a>❗ <strong>本节核心结论：</strong><br>简要结构性能和实现都简单，但隔离性很差，适合早期简单环境，不适合现代复杂系统。</h2><h2 id="8-宏内核-Monolithic-Kernel"><a href="#8-宏内核-Monolithic-Kernel" class="headerlink" title="8. 宏内核 Monolithic Kernel"></a>8. 宏内核 Monolithic Kernel</h2><p>宏内核的核心思想是：</p><blockquote><p><strong>大部分 OS 功能都放在内核态。</strong></p></blockquote><p>包括：</p><ul><li>文件系统</li><li>内存管理</li><li>设备驱动</li><li>进程调度</li><li>IPC</li><li>网络协议栈</li></ul><p>典型例子：<strong>Linux</strong>。</p><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><p>宏内核的最大优点是性能好。</p><p>因为很多模块都在内核态，模块之间可以直接函数调用。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">文件系统 → 磁盘驱动</span><br></pre></td></tr></table></figure><p>在宏内核中，这可能只是内核内部的一次函数调用。</p><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>缺点是内核太大，耦合太强。</p><p>如果设备驱动写错了，可能导致整个内核崩溃。</p><p>所以宏内核的问题不是“落后”，而是：</p><blockquote><p>当系统越来越复杂时，宏内核的可靠性和维护压力会变大。</p></blockquote><hr><h2 id="❗-本节核心结论：宏内核用更强的集中管理换取性能，但代价是内核庞大、耦合强、故障影响范围大。"><a href="#❗-本节核心结论：宏内核用更强的集中管理换取性能，但代价是内核庞大、耦合强、故障影响范围大。" class="headerlink" title="❗ 本节核心结论：宏内核用更强的集中管理换取性能，但代价是内核庞大、耦合强、故障影响范围大。"></a>❗ <strong>本节核心结论：</strong><br>宏内核用更强的集中管理换取性能，但代价是内核庞大、耦合强、故障影响范围大。</h2><h2 id="9-微内核-Microkernel"><a href="#9-微内核-Microkernel" class="headerlink" title="9. 微内核 Microkernel"></a>9. 微内核 Microkernel</h2><p>微内核的核心思想是：</p><blockquote><p><strong>内核越小越好。</strong></p></blockquote><p>它只把最核心的机制留在内核态，例如：</p><ul><li>IPC</li><li>基本进程管理</li><li>基本地址空间管理</li><li>基本调度机制</li></ul><p>其他功能放到用户态作为服务运行，例如：</p><ul><li>文件系统服务</li><li>驱动服务</li><li>网络服务</li></ul><h3 id="微内核中的访问过程"><a href="#微内核中的访问过程" class="headerlink" title="微内核中的访问过程"></a>微内核中的访问过程</h3><p>宏内核中可能是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">应用 → 系统调用 → 内核文件系统 → 磁盘驱动</span><br></pre></td></tr></table></figure><p>微内核中可能变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">应用 → IPC → 文件系统服务 → IPC → 驱动服务 → IPC → 微内核/硬件</span><br></pre></td></tr></table></figure><h3 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h3><p>微内核更强调：</p><ul><li>安全性</li><li>可靠性</li><li>可扩展性</li><li>可移植性</li></ul><p>因为服务在用户态，某个服务崩溃时，不一定拖垮整个系统。</p><h3 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h3><p>微内核的主要问题是性能开销。</p><p>因为原来的函数调用变成了 IPC，而 IPC 涉及：</p><ul><li>用户态&#x2F;内核态切换；</li><li>消息传递；</li><li>权限检查；</li><li>调度开销。</li></ul><hr><h2 id="❗-本节核心结论：微内核用-IPC-和用户态服务换取安全与可靠，但牺牲了一部分性能。"><a href="#❗-本节核心结论：微内核用-IPC-和用户态服务换取安全与可靠，但牺牲了一部分性能。" class="headerlink" title="❗ 本节核心结论：微内核用 IPC 和用户态服务换取安全与可靠，但牺牲了一部分性能。"></a>❗ <strong>本节核心结论：</strong><br>微内核用 IPC 和用户态服务换取安全与可靠，但牺牲了一部分性能。</h2><h2 id="10-混合内核-Hybrid-Kernel"><a href="#10-混合内核-Hybrid-Kernel" class="headerlink" title="10. 混合内核 Hybrid Kernel"></a>10. 混合内核 Hybrid Kernel</h2><p>混合内核是现实妥协。</p><p>它吸收微内核思想，但又把性能敏感的模块放回内核态。</p><p>典型例子：</p><ul><li>Windows NT</li><li>macOS &#x2F; iOS</li></ul><p>混合内核的思路是：</p><blockquote><p>能放用户态的尽量放用户态，但性能敏感部分仍然放在内核态。</p></blockquote><p>所以它不是纯粹微内核，也不是传统宏内核，而是折中。</p><hr><h2 id="❗-本节核心结论：混合内核说明现实系统通常不会完全追求架构纯粹性，而会在结构清晰和运行性能之间妥协。"><a href="#❗-本节核心结论：混合内核说明现实系统通常不会完全追求架构纯粹性，而会在结构清晰和运行性能之间妥协。" class="headerlink" title="❗ 本节核心结论：混合内核说明现实系统通常不会完全追求架构纯粹性，而会在结构清晰和运行性能之间妥协。"></a>❗ <strong>本节核心结论：</strong><br>混合内核说明现实系统通常不会完全追求架构纯粹性，而会在结构清晰和运行性能之间妥协。</h2><h2 id="11-外核-Exokernel"><a href="#11-外核-Exokernel" class="headerlink" title="11. 外核 Exokernel"></a>11. 外核 Exokernel</h2><p>外核比微内核更激进。</p><p>它的核心思想是：</p><blockquote><p><strong>内核不应该替应用提供高级抽象。</strong></p></blockquote><p>传统 OS 会提供：</p><ul><li>文件</li><li>进程</li><li>虚拟内存</li><li>socket</li></ul><p>但外核认为：只有应用自己最知道自己需要什么抽象。</p><p>所以外核只负责：</p><ul><li>资源绑定；</li><li>资源保护；</li><li>资源回收；</li><li>应用隔离。</li></ul><p>真正的 OS 抽象交给应用旁边的 <strong>LibOS</strong>。</p><h3 id="LibOS-是什么？"><a href="#LibOS-是什么？" class="headerlink" title="LibOS 是什么？"></a>LibOS 是什么？</h3><p>LibOS 是库操作系统。</p><p>它像一个库一样和应用链接在一起，为应用提供文件、内存、网络等抽象。</p><p>结构大概是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">应用 + LibOS</span><br><span class="line">应用 + LibOS</span><br><span class="line">应用 + LibOS</span><br><span class="line">      ↓</span><br><span class="line">   Exokernel</span><br><span class="line">      ↓</span><br><span class="line">    硬件</span><br></pre></td></tr></table></figure><h3 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h3><ul><li>理论性能高；</li><li>应用可以高度定制；</li><li>适合特殊场景优化。</li></ul><h3 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h3><ul><li>生态困难；</li><li>兼容性差；</li><li>应用自己管理资源，复杂度高；</li><li>通用性不如传统 OS。</li></ul><hr><h2 id="❗-本节核心结论：外核把抽象权交给应用，用定制化换性能，但牺牲通用性和生态。"><a href="#❗-本节核心结论：外核把抽象权交给应用，用定制化换性能，但牺牲通用性和生态。" class="headerlink" title="❗ 本节核心结论：外核把抽象权交给应用，用定制化换性能，但牺牲通用性和生态。"></a>❗ <strong>本节核心结论：</strong><br>外核把抽象权交给应用，用定制化换性能，但牺牲通用性和生态。</h2><h2 id="12-多内核-Multikernel"><a href="#12-多内核-Multikernel" class="headerlink" title="12. 多内核 Multikernel"></a>12. 多内核 Multikernel</h2><p>多内核是为了应对现代硬件复杂化。</p><p>现代机器不只是一个 CPU，而是可能包括：</p><ul><li>多核 CPU</li><li>GPU</li><li>NPU</li><li>智能网卡</li><li>其他加速器</li></ul><p>这些硬件越来越像一个小型分布式系统。</p><p>多内核的核心思想是：</p><blockquote><p><strong>不要默认共享状态，而是默认划分状态。</strong></p></blockquote><p>传统 OS 中，多个 CPU 核共享很多内核数据结构，会带来：</p><ul><li>锁竞争；</li><li>cache 一致性压力；</li><li>共享状态维护成本。</li></ul><p>多内核则让：</p><ul><li>每个 core 上运行一个小内核；</li><li>小内核之间通过消息通信；</li><li>整个 OS 像分布式系统一样协作。</li></ul><p>典型例子：</p><ul><li>Barrelfish</li><li>Popcorn Linux</li></ul><hr><h2 id="❗-本节核心结论：多内核把-OS-看成分布式系统，是为了解决多核和异构硬件带来的共享状态问题。"><a href="#❗-本节核心结论：多内核把-OS-看成分布式系统，是为了解决多核和异构硬件带来的共享状态问题。" class="headerlink" title="❗ 本节核心结论：多内核把 OS 看成分布式系统，是为了解决多核和异构硬件带来的共享状态问题。"></a>❗ <strong>本节核心结论：</strong><br>多内核把 OS 看成分布式系统，是为了解决多核和异构硬件带来的共享状态问题。</h2><h2 id="13-各类结构对比表"><a href="#13-各类结构对比表" class="headerlink" title="13. 各类结构对比表"></a>13. 各类结构对比表</h2><table><thead><tr><th>架构</th><th>核心思想</th><th>优点</th><th>缺点</th><th>典型例子</th></tr></thead><tbody><tr><td>简要结构</td><td>OS 和应用像一个大程序</td><td>快、简单</td><td>不安全，容易整体崩溃</td><td>MS-DOS</td></tr><tr><td>宏内核</td><td>大量 OS 功能在内核态</td><td>性能好，生态强</td><td>内核庞大，可靠性压力大</td><td>Linux</td></tr><tr><td>微内核</td><td>内核最小化，服务放用户态</td><td>安全、可靠、易扩展</td><td>IPC 开销大</td><td>seL4、QNX、MINIX</td></tr><tr><td>混合内核</td><td>宏内核和微内核折中</td><td>兼顾性能与结构</td><td>复杂，不够纯粹</td><td>Windows NT、macOS</td></tr><tr><td>外核</td><td>内核不提供高级抽象，交给 LibOS</td><td>高度定制，理论性能高</td><td>维护和兼容困难</td><td>Exokernel、Unikernel</td></tr><tr><td>多内核</td><td>OS 当成分布式系统</td><td>适合多核异构</td><td>实现复杂，生态弱</td><td>Barrelfish、Popcorn Linux</td></tr></tbody></table><h2 id="14-Android-系统结构怎么理解？"><a href="#14-Android-系统结构怎么理解？" class="headerlink" title="14. Android 系统结构怎么理解？"></a>14. Android 系统结构怎么理解？</h2><p>Android 不能简单说成微内核。</p><p>它底层使用 <strong>Linux 内核</strong>，所以身体上是宏内核。</p><p>但 Android 上层大量系统服务通过 <strong>Binder IPC</strong> 通信，例如：</p><ul><li>Activity Manager</li><li>Window Manager</li><li>Service Manager</li><li>其他系统服务</li></ul><p>所以 Android 有微内核式的服务化思想。</p><p>可以这样理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">Android 应用</span><br><span class="line">    ↓</span><br><span class="line">Android 应用框架</span><br><span class="line">    ↓</span><br><span class="line">系统服务</span><br><span class="line">    ↓</span><br><span class="line">Binder IPC</span><br><span class="line">    ↓</span><br><span class="line">Android Runtime / Android 库</span><br><span class="line">    ↓</span><br><span class="line">硬件抽象层 HAL</span><br><span class="line">    ↓</span><br><span class="line">Linux 内核</span><br><span class="line">    ↓</span><br><span class="line">硬件</span><br></pre></td></tr></table></figure><hr><h2 id="❗-本节核心结论：Android-底层是-Linux-宏内核，但上层系统服务通过-Binder-IPC-组织，体现了微内核式的服务化思想。"><a href="#❗-本节核心结论：Android-底层是-Linux-宏内核，但上层系统服务通过-Binder-IPC-组织，体现了微内核式的服务化思想。" class="headerlink" title="❗ 本节核心结论：Android 底层是 Linux 宏内核，但上层系统服务通过 Binder IPC 组织，体现了微内核式的服务化思想。"></a>❗ <strong>本节核心结论：</strong><br>Android 底层是 Linux 宏内核，但上层系统服务通过 Binder IPC 组织，体现了微内核式的服务化思想。</h2><h2 id="15-最容易混淆的点"><a href="#15-最容易混淆的点" class="headerlink" title="15. 最容易混淆的点"></a>15. 最容易混淆的点</h2><h3 id="1-宏内核不是落后"><a href="#1-宏内核不是落后" class="headerlink" title="1. 宏内核不是落后"></a>1. 宏内核不是落后</h3><p>Linux 是宏内核，但仍然非常成功。</p><p>原因是：</p><ul><li>性能强；</li><li>生态强；</li><li>工程积累深；</li><li>可通过模块化、权限控制、内核模块等方式缓解部分问题。</li></ul><h3 id="2-微内核不是一定不能用"><a href="#2-微内核不是一定不能用" class="headerlink" title="2. 微内核不是一定不能用"></a>2. 微内核不是一定不能用</h3><p>早期微内核 IPC 开销较大，但后来的 L4、seL4 等系统对 IPC 做了大量优化。</p><p>微内核常适合：</p><ul><li>高可靠场景；</li><li>安全关键系统；</li><li>实时系统；</li><li>嵌入式系统。</li></ul><h3 id="3-外核不是没有内核"><a href="#3-外核不是没有内核" class="headerlink" title="3. 外核不是没有内核"></a>3. 外核不是没有内核</h3><p>外核仍然有内核。</p><p>只是它不提供高级抽象，而是负责底层资源保护和绑定。</p><h3 id="4-多内核不是普通多核-OS"><a href="#4-多内核不是普通多核-OS" class="headerlink" title="4. 多内核不是普通多核 OS"></a>4. 多内核不是普通多核 OS</h3><p>Linux 可以跑在多核 CPU 上，但这不等于它是多内核。</p><p>多内核强调：</p><blockquote><p>每个 core 上运行一个 OS node，OS 整体通过消息通信协作。</p></blockquote><h3 id="5-Android-不是微内核"><a href="#5-Android-不是微内核" class="headerlink" title="5. Android 不是微内核"></a>5. Android 不是微内核</h3><p>Android 底层是 Linux，所以不是微内核。</p><p>但它的 Binder IPC 和系统服务化设计，确实有微内核思想。</p><h2 id="16-自测题"><a href="#16-自测题" class="headerlink" title="16. 自测题"></a>16. 自测题</h2><h3 id="题-1：为什么复杂系统必须考虑结构？"><a href="#题-1：为什么复杂系统必须考虑结构？" class="headerlink" title="题 1：为什么复杂系统必须考虑结构？"></a>题 1：为什么复杂系统必须考虑结构？</h3><p>因为复杂系统中目标会冲突，模块会增多，代码规模会扩大。如果没有合理结构，系统会难维护、难扩展、难保证可靠性。OS 同时管理 CPU、内存、磁盘、网络、进程、安全等资源，所以尤其需要结构。</p><h3 id="题-2：什么是策略与机制分离？"><a href="#题-2：什么是策略与机制分离？" class="headerlink" title="题 2：什么是策略与机制分离？"></a>题 2：什么是策略与机制分离？</h3><p>机制是“怎么做”，策略是“按什么规则做”。</p><p>例如调度中：</p><ul><li>机制：调度队列、时钟中断、上下文切换；</li><li>策略：时间片轮转、优先级调度、EDF。</li></ul><p>分离的好处是底层机制稳定，上层策略灵活可换。</p><h3 id="题-3：宏内核为什么性能好？"><a href="#题-3：宏内核为什么性能好？" class="headerlink" title="题 3：宏内核为什么性能好？"></a>题 3：宏内核为什么性能好？</h3><p>因为大量 OS 服务都在内核态，模块之间可以直接函数调用，通信开销低。</p><h3 id="题-4：微内核为什么更安全、更可靠？"><a href="#题-4：微内核为什么更安全、更可靠？" class="headerlink" title="题 4：微内核为什么更安全、更可靠？"></a>题 4：微内核为什么更安全、更可靠？</h3><p>因为它把大量 OS 服务放到用户态，不同服务之间有进程隔离。某个服务崩溃时，不一定导致整个内核崩溃。</p><h3 id="题-5：微内核的主要性能问题是什么？"><a href="#题-5：微内核的主要性能问题是什么？" class="headerlink" title="题 5：微内核的主要性能问题是什么？"></a>题 5：微内核的主要性能问题是什么？</h3><p>主要问题是 IPC 开销。</p><p>原来宏内核里的函数调用，在微内核中变成了用户态服务之间的 IPC，涉及上下文切换、消息传递和权限检查。</p><h3 id="题-6：外核和微内核的区别是什么？"><a href="#题-6：外核和微内核的区别是什么？" class="headerlink" title="题 6：外核和微内核的区别是什么？"></a>题 6：外核和微内核的区别是什么？</h3><p>微内核仍然提供一些统一系统服务，只是把服务放到用户态。</p><p>外核更极端，它尽量不提供高级硬件抽象，只负责资源绑定、保护和回收，把抽象交给应用自己的 LibOS。</p><h3 id="题-7：多内核为什么把-OS-看成分布式系统？"><a href="#题-7：多内核为什么把-OS-看成分布式系统？" class="headerlink" title="题 7：多内核为什么把 OS 看成分布式系统？"></a>题 7：多内核为什么把 OS 看成分布式系统？</h3><p>因为现代硬件中有多个 CPU 核、GPU、NPU、智能网卡等异构计算单元。如果所有核心共享大量状态，会带来锁竞争和 cache 一致性压力。因此多内核选择每个 core 运行一个小内核，通过消息通信协作。</p><h3 id="题-8：Windows-NT、Linux、macOS-分别属于什么结构？"><a href="#题-8：Windows-NT、Linux、macOS-分别属于什么结构？" class="headerlink" title="题 8：Windows NT、Linux、macOS 分别属于什么结构？"></a>题 8：Windows NT、Linux、macOS 分别属于什么结构？</h3><ul><li>Windows NT：混合内核</li><li>Linux：宏内核</li><li>macOS：混合内核，基于 XNU，结合 Mach 和 BSD 思想</li></ul><h2 id="17-最终记忆版"><a href="#17-最终记忆版" class="headerlink" title="17. 最终记忆版"></a>17. 最终记忆版</h2><p>这讲可以压缩成下面几句话：</p><blockquote><p><strong>操作系统结构 &#x3D; 控制复杂性的办法。</strong></p></blockquote><p>复杂系统需要：</p><ul><li>策略与机制分离；</li><li>模块化；</li><li>抽象；</li><li>分层；</li><li>层级。</li></ul><p>内核存在的原因是：</p><blockquote><p>多个应用共享硬件，所以需要统一管理和隔离。</p></blockquote><p>不同结构的核心区别是：</p><table><thead><tr><th>架构</th><th>一句话记忆</th></tr></thead><tbody><tr><td>宏内核</td><td>功能集中在内核态，性能好，但内核庞大</td></tr><tr><td>微内核</td><td>内核最小化，服务放用户态，可靠但 IPC 开销大</td></tr><tr><td>混合内核</td><td>现实折中，兼顾性能和结构</td></tr><tr><td>外核</td><td>内核只管资源，抽象交给 LibOS</td></tr><tr><td>多内核</td><td>面向多核异构，把 OS 当成分布式系统</td></tr><tr><td>Android</td><td>底层 Linux 宏内核，上层服务化，有微内核思想</td></tr></tbody></table><hr><p>❗ <strong>最终核心结论：</strong><br>学这讲不要只背“Linux 是宏内核、Windows 是混合内核”。真正要掌握的是：每种结构为什么出现，它解决了什么问题，又牺牲了什么。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter2-3_P2P视频流DASH与CDN可扩展性</title>
      <link href="/2026/06/05/Chapter2-3-P2P%E8%A7%86%E9%A2%91%E6%B5%81DASH%E4%B8%8ECDN%E5%8F%AF%E6%89%A9%E5%B1%95%E6%80%A7-essay/"/>
      <url>/2026/06/05/Chapter2-3-P2P%E8%A7%86%E9%A2%91%E6%B5%81DASH%E4%B8%8ECDN%E5%8F%AF%E6%89%A9%E5%B1%95%E6%80%A7-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-2-3：P2P、视频流、DASH-与-CDN-的可扩展性"><a href="#Chapter-2-3：P2P、视频流、DASH-与-CDN-的可扩展性" class="headerlink" title="Chapter 2.3：P2P、视频流、DASH 与 CDN 的可扩展性"></a>Chapter 2.3：P2P、视频流、DASH 与 CDN 的可扩展性</h1><p>Chapter 2.3 的主线是应用层系统的可扩展性。当很多用户同时下载文件或观看视频时，中心服务器很容易成为瓶颈。P2P、DASH 和 CDN 分别从上传能力、码率选择和内容位置三个角度解决规模问题。</p><hr><h2 id="本节核心结论：-大规模应用不能只依赖中心服务器；P2P-分散上传能力，DASH-让客户端适应网络变化，CDN-把内容副本放到更靠近用户的位置。"><a href="#本节核心结论：-大规模应用不能只依赖中心服务器；P2P-分散上传能力，DASH-让客户端适应网络变化，CDN-把内容副本放到更靠近用户的位置。" class="headerlink" title="本节核心结论： 大规模应用不能只依赖中心服务器；P2P 分散上传能力，DASH 让客户端适应网络变化，CDN 把内容副本放到更靠近用户的位置。"></a><strong>本节核心结论：</strong> 大规模应用不能只依赖中心服务器；P2P 分散上传能力，DASH 让客户端适应网络变化，CDN 把内容副本放到更靠近用户的位置。</h2><h2 id="1-P2P-的基本思想"><a href="#1-P2P-的基本思想" class="headerlink" title="1. P2P 的基本思想"></a>1. P2P 的基本思想</h2><p>P2P 的基本单位是 peer。在纯 P2P 架构中，几乎没有一直运行的中心服务器，任意端系统之间可以直接通信。每个节点既可以请求服务，也可以提供服务。</p><p>P2P 的自扩展性来自一个关键事实：新节点加入时，一方面增加下载需求，另一方面也带来上传能力。当节点越多时，系统负担增加，但可用服务能力也可能同步增加。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    Seed[初始源节点] --&gt; P1[Peer1]    Seed --&gt; P2[Peer2]    P1 &lt;--&gt; P2    P2 &lt;--&gt; P3[Peer3]    P3 &lt;--&gt; P4[Peer4]    P4 &lt;--&gt; P1  </pre></div><p>这张图强调 P2P 的文件分发不是“所有人只从服务器下载”，而是 peer 之间也会交换数据块。初始源节点把文件注入系统后，下载到块的 peer 也能继续上传给别人。</p><h2 id="2-C-S-与-P2P-文件分发时间"><a href="#2-C-S-与-P2P-文件分发时间" class="headerlink" title="2. C&#x2F;S 与 P2P 文件分发时间"></a>2. C&#x2F;S 与 P2P 文件分发时间</h2><p>设文件大小为 <code>F</code>，peer 数量为 <code>N</code>，服务器上传速率为 <code>u_s</code>，第 <code>i</code> 个 peer 上传速率为 <code>u_i</code>，第 <code>i</code> 个 peer 下载速率为 <code>d_i</code>，所有 peer 中最小下载速率为 <code>d_min</code>。</p><p>在 C&#x2F;S 模式下，服务器至少要上传 <code>N</code> 份文件，总上传量是 <code>NF</code>，所以服务器侧时间下界是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NF / u_s</span><br></pre></td></tr></table></figure><p>最慢客户端下载一份文件至少需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">F / d_min</span><br></pre></td></tr></table></figure><p>因此 C&#x2F;S 分发时间满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">D_cs &gt;= max&#123; NF/u_s, F/d_min &#125;</span><br></pre></td></tr></table></figure><p>这说明 C&#x2F;S 模式下，随着 <code>N</code> 增大，服务器上传压力线性增大。</p><p>在 P2P 模式下，服务器或初始源节点至少要上传一份完整文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">F / u_s</span><br></pre></td></tr></table></figure><p>最慢 peer 仍然至少要下载一份：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">F / d_min</span><br></pre></td></tr></table></figure><p>所有 peer 总共需要下载 <code>NF</code> 数据，而总上传能力是 <code>u_s + Σu_i</code>，因此还有下界：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">NF / (u_s + Σu_i)</span><br></pre></td></tr></table></figure><p>所以 P2P 分发时间满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">D_p2p &gt;= max&#123; F/u_s, F/d_min, NF/(u_s + Σu_i) &#125;</span><br></pre></td></tr></table></figure><p>P2P 的关键优势在第三项：<code>N</code> 增大时，总需求 <code>NF</code> 增加，但总上传能力 <code>u_s + Σu_i</code> 也随 peer 增加而增加。</p><h2 id="3-u-s-是否意味着-P2P-有中心服务器"><a href="#3-u-s-是否意味着-P2P-有中心服务器" class="headerlink" title="3. u_s 是否意味着 P2P 有中心服务器"></a>3. <code>u_s</code> 是否意味着 P2P 有中心服务器</h2><p>公式里的 <code>u_s</code> 不一定表示传统 C&#x2F;S 中长期承担全部下载压力的中心服务器。它表示最初拥有完整文件的源节点或初始种子节点的上传速率。</p><p>哪怕是 P2P，文件一开始也必须由某个节点拥有完整副本。课件为了推导方便，把这个源节点称为 server，并记作 <code>u_s</code>。它的作用是把文件至少注入系统一次，之后 peer 之间可以互相分发。</p><p>如果课件记号中 <code>N</code> 表示下载的 peer 数量，那么总上传能力是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">u_s + Σ(i=1..N) u_i</span><br></pre></td></tr></table></figure><p>如果自己把“总共 <code>n</code> 个节点”定义为包含初始源节点在内，那么剩余 peer 数量是 <code>n - 1</code>，总上传能力可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">u_s + Σ(i=1..n-1) u_i</span><br></pre></td></tr></table></figure><p>关键是先说明 <code>N</code> 是否包含初始源节点。</p><h2 id="4-BitTorrent：分块交换与互惠机制"><a href="#4-BitTorrent：分块交换与互惠机制" class="headerlink" title="4. BitTorrent：分块交换与互惠机制"></a>4. BitTorrent：分块交换与互惠机制</h2><p>BitTorrent 是 P2P 文件分发的典型例子。它不会让每个 peer 一次下载完整文件，而是把文件分成多个 chunk 或 block，各 peer 之间交换不同块。</p><p>新节点加入 torrent 时，大致经历：</p><ol><li>向 tracker 注册。</li><li>获得 peer 列表。</li><li>与部分 peer 建立连接。</li><li>从不同 peer 下载文件块。</li><li>把自己已有的块上传给其他 peer。</li></ol><p>两个核心策略是 rarest first 和 tit-for-tat。</p><p>rarest first 指优先请求网络中最稀缺的块。这样可以避免某些块过少，导致后期大量 peer 都卡在同一个缺失块上。</p><p>tit-for-tat 指优先给上传给自己最快的 peer 回传数据，同时定期随机选择额外 peer 进行试探。这既鼓励互惠，也给新节点进入交换关系的机会。</p><h2 id="5-视频流服务为什么困难"><a href="#5-视频流服务为什么困难" class="headerlink" title="5. 视频流服务为什么困难"></a>5. 视频流服务为什么困难</h2><p>视频流量占据互联网很大比例。视频服务的难点主要有两个：</p><ul><li>规模性：如何同时服务海量用户。</li><li>异构性：不同用户的带宽、设备、网络路径和稳定性差异很大。</li></ul><p>视频本质上是按固定速率播放的图像序列。原始视频数据量巨大，因此必须压缩。压缩主要利用两类冗余：</p><ul><li>空间冗余：同一帧内部有大量相似像素。</li><li>时间冗余：相邻帧之间变化通常不大。</li></ul><p>存储型流媒体还面临 continuous playout constraint，即播放必须连续。网络中存在 jitter、丢包、重传和用户快进快退，因此客户端需要 playout buffer 来吸收网络波动。</p><hr><h2 id="机制理解：-缓冲区不是为了提高画质，而是为了让不稳定的网络传输变成尽量稳定的播放体验。"><a href="#机制理解：-缓冲区不是为了提高画质，而是为了让不稳定的网络传输变成尽量稳定的播放体验。" class="headerlink" title="机制理解： 缓冲区不是为了提高画质，而是为了让不稳定的网络传输变成尽量稳定的播放体验。"></a><strong>机制理解：</strong> 缓冲区不是为了提高画质，而是为了让不稳定的网络传输变成尽量稳定的播放体验。</h2><h2 id="6-DASH：动态自适应-HTTP-流媒体"><a href="#6-DASH：动态自适应-HTTP-流媒体" class="headerlink" title="6. DASH：动态自适应 HTTP 流媒体"></a>6. DASH：动态自适应 HTTP 流媒体</h2><p>DASH 全称是 Dynamic Adaptive Streaming over HTTP。它的核心思想是：把视频分成块，为每个块准备多个码率版本，让客户端根据网络状况动态选择。</p><p>服务器端负责：</p><ol><li>将视频文件切成多个块。</li><li>为每个块编码多个码率版本。</li><li>把块复制到服务器或 CDN 节点。</li><li>提供 manifest file，列出各块和各码率版本的 URL。</li></ol><p>客户端负责：</p><ol><li>获取 manifest file。</li><li>判断什么时候请求下一个块。</li><li>根据带宽和缓冲区选择码率。</li><li>选择从哪个服务器或 CDN 节点请求。</li></ol><p>当网络好时，客户端请求高码率块；当网络变差时，客户端请求低码率块。因此视频清晰度可能在播放过程中自动升降。</p><h2 id="7-CDN：把内容放到更靠近用户的位置"><a href="#7-CDN：把内容放到更靠近用户的位置" class="headerlink" title="7. CDN：把内容放到更靠近用户的位置"></a>7. CDN：把内容放到更靠近用户的位置</h2><p>CDN 是 Content Distribution Network，内容分发网络。它解决的问题是：如何把视频、图片、网页等内容稳定地分发给大量用户。</p><p>单个 mega-server 不可扩展，原因包括路径远、瓶颈链路多、重复长距离传输浪费带宽、单点故障、中心服务器和周边网络容易拥塞。</p><p>CDN 的基本思想是：在全网部署缓存节点，把内容复制到多个位置，用户请求时导向较近或网络状况较好的副本。</p><p>两类部署策略是：</p><table><thead><tr><th>策略</th><th>含义</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>enter deep</td><td>把 CDN 节点深入部署到许多接入网</td><td>离用户近</td><td>节点多，管理复杂</td></tr><tr><td>bring home</td><td>在少数关键位置或 IXP 附近部署集群</td><td>管理简单</td><td>离部分用户较远</td></tr></tbody></table><h2 id="8-DASH-与-CDN-如何配合"><a href="#8-DASH-与-CDN-如何配合" class="headerlink" title="8. DASH 与 CDN 如何配合"></a>8. DASH 与 CDN 如何配合</h2><p>DASH 决定“请求什么质量的视频块”，CDN 决定“从哪里请求这些块”。两者常常一起工作。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant Client as 客户端    participant DNS as DNS&#x2F;CDN 映射    participant CDN as CDN 节点    participant Player as 播放器    Client-&gt;&gt;DNS: 解析视频 URL    DNS--&gt;&gt;Client: 返回合适 CDN 节点    Client-&gt;&gt;CDN: 请求 manifest    CDN--&gt;&gt;Client: 返回块列表和码率    Client-&gt;&gt;CDN: 请求合适码率块    CDN--&gt;&gt;Player: 交付视频块  </pre></div><p>这张图展示了两层选择：DNS&#x2F;CDN 先帮助客户端找到合适节点，DASH 再让客户端根据当前带宽和缓冲区选择合适码率的视频块。</p><p>CDN 常借助 DNS&#x2F;CNAME 重定向用户。用户访问内容网站时，网站可能返回指向 CDN 域名的 CNAME；随后 CDN 的 DNS 系统选择合适节点，客户端再通过 HTTP 请求内容。</p><h2 id="9-易混点"><a href="#9-易混点" class="headerlink" title="9. 易混点"></a>9. 易混点</h2><p>P2P 不是完全没有任何服务器。实际系统中可能有 tracker、索引服务器、认证服务器等辅助组件，但文件块主要在 peer 之间交换。</p><p>DASH 和 CDN 不是同一件事。DASH 解决码率自适应，CDN 解决内容位置和分发路径。</p><p>缓冲区不是为了提高画质，而是为了抵抗 jitter 和短期吞吐波动。</p><p><code>u_s</code> 不一定是传统中心服务器，它可以表示初始源节点或 original seed 的上传能力。</p><h2 id="10-复习清单"><a href="#10-复习清单" class="headerlink" title="10. 复习清单"></a>10. 复习清单</h2><p>学完本节后，应能回答：</p><ul><li>P2P 为什么具有自扩展性？</li><li>C&#x2F;S 文件分发时间为什么会随 <code>N</code> 线性增长？</li><li><code>D_cs</code> 和 <code>D_p2p</code> 的公式中每一项分别来自什么限制？</li><li>BitTorrent 中 tracker、torrent、chunk 分别是什么？</li><li>rarest first 为什么有助于文件分发？</li><li>tit-for-tat 如何抑制只下载不上传？</li><li>视频流服务为什么需要编码、DASH 和缓冲区？</li><li>DASH 的服务器端和客户端分别做什么？</li><li>CDN 为什么比 mega-server 更可扩展？</li><li>CDN 如何通过 DNS&#x2F;CNAME 把用户导向合适节点？</li><li><code>u_s + Σu_i</code> 中的 <code>u_s</code> 应如何理解？</li></ul><hr><h2 id="最终总结：-Chapter-2-3-的核心是可扩展性：P2P-让用户参与上传，DASH-让客户端适应网络变化，CDN-让内容靠近用户。这三类机制共同说明，大规模应用层系统必须把能力分布到网络边缘和多个节点上。"><a href="#最终总结：-Chapter-2-3-的核心是可扩展性：P2P-让用户参与上传，DASH-让客户端适应网络变化，CDN-让内容靠近用户。这三类机制共同说明，大规模应用层系统必须把能力分布到网络边缘和多个节点上。" class="headerlink" title="最终总结： Chapter 2.3 的核心是可扩展性：P2P 让用户参与上传，DASH 让客户端适应网络变化，CDN 让内容靠近用户。这三类机制共同说明，大规模应用层系统必须把能力分布到网络边缘和多个节点上。"></a><strong>最终总结：</strong> Chapter 2.3 的核心是可扩展性：P2P 让用户参与上传，DASH 让客户端适应网络变化，CDN 让内容靠近用户。这三类机制共同说明，大规模应用层系统必须把能力分布到网络边缘和多个节点上。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter2-2_电子邮件SMTP邮件访问协议与DNS</title>
      <link href="/2026/06/05/Chapter2-2-%E7%94%B5%E5%AD%90%E9%82%AE%E4%BB%B6SMTP%E9%82%AE%E4%BB%B6%E8%AE%BF%E9%97%AE%E5%8D%8F%E8%AE%AE%E4%B8%8EDNS-essay/"/>
      <url>/2026/06/05/Chapter2-2-%E7%94%B5%E5%AD%90%E9%82%AE%E4%BB%B6SMTP%E9%82%AE%E4%BB%B6%E8%AE%BF%E9%97%AE%E5%8D%8F%E8%AE%AE%E4%B8%8EDNS-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-2-2：电子邮件、SMTP、邮件访问协议与-DNS"><a href="#Chapter-2-2：电子邮件、SMTP、邮件访问协议与-DNS" class="headerlink" title="Chapter 2.2：电子邮件、SMTP、邮件访问协议与 DNS"></a>Chapter 2.2：电子邮件、SMTP、邮件访问协议与 DNS</h1><p>Chapter 2.2 讲两个典型应用层系统：电子邮件和 DNS。它们分别回答两个问题：一封邮件如何从 Alice 到达 Bob；用户输入域名后，主机如何获得目标服务器的 IP 地址。</p><hr><h2 id="本节核心结论：-邮件系统依靠用户代理、邮件服务器、SMTP-和访问协议协作；DNS-依靠分层命名和分布式数据库完成域名到-IP-地址的映射。"><a href="#本节核心结论：-邮件系统依靠用户代理、邮件服务器、SMTP-和访问协议协作；DNS-依靠分层命名和分布式数据库完成域名到-IP-地址的映射。" class="headerlink" title="本节核心结论： 邮件系统依靠用户代理、邮件服务器、SMTP 和访问协议协作；DNS 依靠分层命名和分布式数据库完成域名到 IP 地址的映射。"></a><strong>本节核心结论：</strong> 邮件系统依靠用户代理、邮件服务器、SMTP 和访问协议协作；DNS 依靠分层命名和分布式数据库完成域名到 IP 地址的映射。</h2><h2 id="1-电子邮件系统不等于-SMTP"><a href="#1-电子邮件系统不等于-SMTP" class="headerlink" title="1. 电子邮件系统不等于 SMTP"></a>1. 电子邮件系统不等于 SMTP</h2><p>电子邮件系统由三个主要部分组成：</p><table><thead><tr><th>组成</th><th>作用</th></tr></thead><tbody><tr><td>User Agent</td><td>撰写、编辑、阅读邮件，如 Outlook、Apple Mail、网页邮箱</td></tr><tr><td>Mail Server</td><td>保存用户 mailbox，维护待发送邮件队列</td></tr><tr><td>SMTP</td><td>在邮件服务器之间发送和转发邮件</td></tr></tbody></table><p>SMTP 不是整个邮件系统，而是邮件发送和服务器间转发协议。用户写邮件、读邮件、管理文件夹，都还需要用户代理和邮件访问协议参与。</p><h2 id="2-Alice-给-Bob-发邮件的完整流程"><a href="#2-Alice-给-Bob-发邮件的完整流程" class="headerlink" title="2. Alice 给 Bob 发邮件的完整流程"></a>2. Alice 给 Bob 发邮件的完整流程</h2><p>Bob 不在线也能收到邮件，因为邮件不是直接送到 Bob 的电脑，而是先送到 Bob 的邮件服务器 mailbox 中。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant AUA as Alice UA    participant AMS as Alice 邮件服务器    participant BMS as Bob 邮件服务器    participant BUA as Bob UA    AUA-&gt;&gt;AMS: 提交邮件    AMS-&gt;&gt;BMS: SMTP 传送邮件    BMS-&gt;&gt;BMS: 放入 Bob mailbox    BUA-&gt;&gt;BMS: POP3&#x2F;IMAP&#x2F;HTTP 读取  </pre></div><p>这张图说明邮件系统的异步性：发送方把邮件交给自己的邮件服务器，服务器之间用 SMTP 投递，收件人稍后再用访问协议读取邮件。</p><h2 id="3-SMTP：发送邮件的协议"><a href="#3-SMTP：发送邮件的协议" class="headerlink" title="3. SMTP：发送邮件的协议"></a>3. SMTP：发送邮件的协议</h2><p>SMTP 使用 TCP，默认端口号是 25。它通常在发送方邮件服务器和接收方邮件服务器之间传输邮件，采用命令&#x2F;响应交互，过程包括 greeting、message transfer 和 close。</p><p>常见 SMTP 命令包括：</p><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>HELO</code></td><td>打招呼，建立 SMTP 会话</td></tr><tr><td><code>MAIL FROM</code></td><td>声明信封发送方</td></tr><tr><td><code>RCPT TO</code></td><td>声明信封接收方</td></tr><tr><td><code>DATA</code></td><td>开始发送邮件内容</td></tr><tr><td><code>QUIT</code></td><td>关闭会话</td></tr></tbody></table><p>SMTP 常被描述为 push：发送方主动把邮件推送到接收方邮件服务器。HTTP 典型资源访问则是 pull：客户端主动向服务器拉取资源。</p><h2 id="4-邮件报文格式与-MIME"><a href="#4-邮件报文格式与-MIME" class="headerlink" title="4. 邮件报文格式与 MIME"></a>4. 邮件报文格式与 MIME</h2><p>邮件内容本身也有格式，通常由 header、blank line 和 body 组成。header 中常见字段包括：</p><ul><li><code>From:</code></li><li><code>To:</code></li><li><code>Subject:</code></li></ul><p>要特别区分：SMTP 命令中的 <code>MAIL FROM</code>、<code>RCPT TO</code> 属于投递层面的信封信息；邮件报文头部中的 <code>From:</code>、<code>To:</code>、<code>Subject:</code> 属于用户看到的邮件内容格式。二者不是同一个层面的字段。</p><p>原始邮件正文主要支持 ASCII。为了支持附件、图片和多媒体内容，需要 MIME。MIME 会在邮件首部添加字段，例如 <code>MIME-Version</code>、<code>Content-Transfer-Encoding</code>、<code>Content-Type</code>，说明邮件体的编码和内容类型。</p><h2 id="5-邮件访问协议：POP3、IMAP、HTTP"><a href="#5-邮件访问协议：POP3、IMAP、HTTP" class="headerlink" title="5. 邮件访问协议：POP3、IMAP、HTTP"></a>5. 邮件访问协议：POP3、IMAP、HTTP</h2><p>SMTP 负责发送和转发邮件，但 Bob 从自己的邮件服务器读取邮件，需要邮件访问协议。</p><table><thead><tr><th>协议</th><th>核心作用</th><th>特点</th></tr></thead><tbody><tr><td>POP3</td><td>下载邮件</td><td>可下载并删除或下载并保留，会话中状态较少</td></tr><tr><td>IMAP</td><td>远程管理邮箱</td><td>服务器保存文件夹、邮件状态和映射</td></tr><tr><td>HTTP&#x2F;HTTPS</td><td>Web 邮箱访问</td><td>Gmail、Hotmail、Yahoo Mail 等网页邮箱常用</td></tr></tbody></table><p>POP3 更像“把邮件取下来”，IMAP 更像“在服务器上管理邮箱”。多设备同步场景通常更适合 IMAP，因为邮件状态和文件夹结构保留在服务器上。</p><h2 id="6-DNS-为什么存在"><a href="#6-DNS-为什么存在" class="headerlink" title="6. DNS 为什么存在"></a>6. DNS 为什么存在</h2><p>人类更容易记住 <code>www.example.com</code>、<code>sse.sysu.edu.cn</code> 这样的主机名；网络层转发则需要 IP 地址。DNS 解决的问题是：如何把主机名映射为 IP 地址。</p><p>DNS 不能设计成一个全球集中式服务器，原因包括：</p><ul><li>单点故障：中心服务器不可用会影响全球解析。</li><li>性能瓶颈：全球查询量巨大，单机难以承受。</li><li>时延问题：所有用户访问同一个中心数据库会很慢。</li><li>维护困难：主机数量巨大，映射不断变化。</li><li>命名冲突：扁平命名难以管理。</li></ul><p>因此 DNS 采用层次化命名和分布式数据库。它服务于 IP 通信，但本身是应用层协议，通常使用 UDP 53。</p><h2 id="7-DNS-层次结构与查询流程"><a href="#7-DNS-层次结构与查询流程" class="headerlink" title="7. DNS 层次结构与查询流程"></a>7. DNS 层次结构与查询流程</h2><p>DNS 的层次结构包括 Root DNS、TLD DNS 和 authoritative DNS。主机通常不会直接从应用程序访问 Root DNS，而是先通过 resolver 查询本地 DNS 服务器。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant Host as 主机 resolver    participant Local as Local DNS    participant Root as Root DNS    participant TLD as TLD DNS    participant Auth as 权威 DNS    Host-&gt;&gt;Local: 查询 www.amazon.com    Local-&gt;&gt;Root: 问 .com 在哪    Root--&gt;&gt;Local: 返回 .com TLD    Local-&gt;&gt;TLD: 问 amazon.com 在哪    TLD--&gt;&gt;Local: 返回权威 DNS    Local-&gt;&gt;Auth: 查询主机记录    Auth--&gt;&gt;Local: 返回 IP    Local--&gt;&gt;Host: 返回 IP  </pre></div><p>这张图展示的是本地 DNS 替主机迭代查询的主干。Root 不直接给出最终 IP，而是指向 TLD；TLD 再指向权威 DNS；权威 DNS 给出最终记录。</p><p>本地 DNS 不严格属于 DNS 层次结构，但它是主机首先联系的 DNS 服务器。它可能直接回答本地区域记录，也可能使用缓存回答。缓存能减少时延和上游负载，但记录可能过时，因此资源记录带有 TTL。</p><p>递归查询和迭代查询的区别是：递归查询中，被问的服务器继续帮请求者查到底；迭代查询中，被问的服务器告诉请求者下一步该问谁。</p><h2 id="8-DNS-资源记录"><a href="#8-DNS-资源记录" class="headerlink" title="8. DNS 资源记录"></a>8. DNS 资源记录</h2><p>DNS 存储的是 Resource Record，格式为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(name, value, type, ttl)</span><br></pre></td></tr></table></figure><p>常见类型包括：</p><table><thead><tr><th>Type</th><th>Name</th><th>Value</th><th>含义</th></tr></thead><tbody><tr><td>A</td><td>主机名</td><td>IP 地址</td><td>主机名到 IP</td></tr><tr><td>NS</td><td>域名</td><td>权威 DNS 服务器名</td><td>某域由哪个服务器负责</td></tr><tr><td>CNAME</td><td>主机别名</td><td>规范主机名</td><td>别名到真实名字</td></tr><tr><td>MX</td><td>邮件域名</td><td>邮件服务器名</td><td>该域邮件应投递到哪里</td></tr></tbody></table><p>MX 记录对邮件系统尤其重要。发送方邮件服务器给 <code>user@example.com</code> 发信时，会查询 <code>example.com</code> 的 MX 记录，找到负责接收该域邮件的邮件服务器。</p><h2 id="9-DNS-安全问题"><a href="#9-DNS-安全问题" class="headerlink" title="9. DNS 安全问题"></a>9. DNS 安全问题</h2><p>DNS spoofing 指攻击者伪造 DNS 响应，让用户获得错误 IP。DNS cache poisoning 指攻击者诱导 DNS 服务器缓存错误记录，使后续用户也被导向错误地址。</p><p>这类攻击的危险在于：用户以为自己访问的是正确域名，但解析结果可能已经被替换。DNSSEC、随机化查询 ID、端口随机化、缓存策略等机制都与降低这类风险有关。</p><h2 id="10-邮件域、MX-记录与端口"><a href="#10-邮件域、MX-记录与端口" class="headerlink" title="10. 邮件域、MX 记录与端口"></a>10. 邮件域、MX 记录与端口</h2><p>邮箱地址中 <code>@</code> 后面的部分是邮件域名。例如 <code>gmail.com</code>、<code>qq.com</code>、<code>139.com</code> 可以粗略判断邮件服务商或邮件域。但严格判断邮件投递路径时，不只看后缀字符串，而要查该域名的 MX 记录。</p><p>有些不同后缀的邮箱可能由同一服务商托管，例如学校或公司域名可能使用 Google Workspace、Microsoft 365 或企业邮箱服务。因此：</p><ul><li>同后缀通常属于同一邮件域。</li><li>不同后缀不一定代表完全不同邮件系统。</li><li>严格投递依据是 DNS MX 记录。</li></ul><p>不同邮件服务商通常遵守同一套标准协议端口：</p><table><thead><tr><th>用途</th><th>协议</th><th>常见端口</th></tr></thead><tbody><tr><td>邮件服务器之间投递</td><td>SMTP</td><td>TCP 25</td></tr><tr><td>用户提交邮件</td><td>SMTP Submission</td><td>TCP 587</td></tr><tr><td>加密 SMTP 提交</td><td>SMTPS</td><td>TCP 465</td></tr><tr><td>下载邮件</td><td>POP3</td><td>TCP 110</td></tr><tr><td>加密 POP3</td><td>POP3S</td><td>TCP 995</td></tr><tr><td>远程管理邮箱</td><td>IMAP</td><td>TCP 143</td></tr><tr><td>加密 IMAP</td><td>IMAPS</td><td>TCP 993</td></tr><tr><td>网页邮箱</td><td>HTTP&#x2F;HTTPS</td><td>TCP 80&#x2F;443</td></tr></tbody></table><p>要区分服务器之间投递和用户客户端提交邮件。邮件服务器之间常用 SMTP 25；用户在 Outlook 等客户端中发邮件，常用 587 或 465。</p><h2 id="11-复习清单"><a href="#11-复习清单" class="headerlink" title="11. 复习清单"></a>11. 复习清单</h2><p>学完本节后，应能回答：</p><ul><li>电子邮件系统的三个主要组成部分是什么？</li><li>为什么 Bob 不在线也能收到 Alice 的邮件？</li><li>SMTP 使用 TCP 还是 UDP？默认端口是多少？</li><li>为什么 SMTP 是 push，而 HTTP 常被说成 pull？</li><li><code>MAIL FROM</code> 和邮件头部 <code>From:</code> 有什么区别？</li><li>POP3 和 IMAP 的核心区别是什么？</li><li>DNS 为什么不能集中式实现？</li><li>主机解析 <code>www.amazon.com</code> 大致经过哪些 DNS 层次？</li><li>递归查询和迭代查询有什么不同？</li><li>A、NS、CNAME、MX 记录分别表示什么？</li><li>判断邮件服务商时，为什么严格上要看 MX 记录？</li></ul><hr><h2 id="最终总结：-Chapter-2-2-展示了应用层协议的两个典型系统：邮件系统通过-SMTP-和访问协议完成异步通信，DNS-通过分布式层次结构完成名字到地址的映射，并为邮件投递、负载均衡和主机别名等功能提供基础。"><a href="#最终总结：-Chapter-2-2-展示了应用层协议的两个典型系统：邮件系统通过-SMTP-和访问协议完成异步通信，DNS-通过分布式层次结构完成名字到地址的映射，并为邮件投递、负载均衡和主机别名等功能提供基础。" class="headerlink" title="最终总结： Chapter 2.2 展示了应用层协议的两个典型系统：邮件系统通过 SMTP 和访问协议完成异步通信，DNS 通过分布式层次结构完成名字到地址的映射，并为邮件投递、负载均衡和主机别名等功能提供基础。"></a><strong>最终总结：</strong> Chapter 2.2 展示了应用层协议的两个典型系统：邮件系统通过 SMTP 和访问协议完成异步通信，DNS 通过分布式层次结构完成名字到地址的映射，并为邮件投递、负载均衡和主机别名等功能提供基础。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter2-1_网络应用与应用层协议框架</title>
      <link href="/2026/06/05/Chapter2-1-%E7%BD%91%E7%BB%9C%E5%BA%94%E7%94%A8%E4%B8%8E%E5%BA%94%E7%94%A8%E5%B1%82%E5%8D%8F%E8%AE%AE%E6%A1%86%E6%9E%B6-essay/"/>
      <url>/2026/06/05/Chapter2-1-%E7%BD%91%E7%BB%9C%E5%BA%94%E7%94%A8%E4%B8%8E%E5%BA%94%E7%94%A8%E5%B1%82%E5%8D%8F%E8%AE%AE%E6%A1%86%E6%9E%B6-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-2-1：网络应用与应用层协议的基本框架"><a href="#Chapter-2-1：网络应用与应用层协议的基本框架" class="headerlink" title="Chapter 2.1：网络应用与应用层协议的基本框架"></a>Chapter 2.1：网络应用与应用层协议的基本框架</h1><p>Chapter 2.1 的作用，是在学习 HTTP、SMTP、DNS 等具体协议之前，先回答一个更基础的问题：网络应用到底如何让不同主机上的程序互相通信。它的核心不在某个单独协议，而在应用层设计的共同框架。</p><hr><h2 id="本节核心结论：-网络应用运行在端系统上，应用进程通过-socket-使用-TCP-UDP-服务，应用层协议规定-message-的格式、语义和交互规则。"><a href="#本节核心结论：-网络应用运行在端系统上，应用进程通过-socket-使用-TCP-UDP-服务，应用层协议规定-message-的格式、语义和交互规则。" class="headerlink" title="本节核心结论： 网络应用运行在端系统上，应用进程通过 socket 使用 TCP&#x2F;UDP 服务，应用层协议规定 message 的格式、语义和交互规则。"></a><strong>本节核心结论：</strong> 网络应用运行在端系统上，应用进程通过 socket 使用 TCP&#x2F;UDP 服务，应用层协议规定 message 的格式、语义和交互规则。</h2><h2 id="1-应用层在网络中的位置"><a href="#1-应用层在网络中的位置" class="headerlink" title="1. 应用层在网络中的位置"></a>1. 应用层在网络中的位置</h2><p>应用层位于 Internet 五层协议栈的最上层。Web、邮件、聊天、游戏、视频会议等网络应用，运行在手机、电脑和服务器等端系统上，而不是运行在网络核心的路由器或交换机中。</p><p>这带来一个重要好处：应用开发者通常不需要修改路由器或 ISP 基础设施，就能部署新应用、更新协议逻辑、快速迭代功能。网络核心主要负责分组转发，不理解具体应用语义。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    App[应用进程] --&gt; Socket[Socket API]    Socket --&gt; Transport[TCP 或 UDP]    Transport --&gt; IP[IP]    IP --&gt; Link[链路层]    Link --&gt; Physical[物理层]  </pre></div><p>这张图说明应用层并不直接控制网络路径。应用进程把 message 交给 socket，socket 后面的 TCP&#x2F;UDP、IP、链路层和物理层负责继续传输。</p><h2 id="2-应用体系结构：C-S、P2P-与混合模式"><a href="#2-应用体系结构：C-S、P2P-与混合模式" class="headerlink" title="2. 应用体系结构：C&#x2F;S、P2P 与混合模式"></a>2. 应用体系结构：C&#x2F;S、P2P 与混合模式</h2><p>客户-服务器结构中，server 通常是 always-on host，具有固定 IP 地址和端口，等待客户端请求。client 主动发起通信，可能间歇连接、使用动态 IP，通常不直接和其他客户端通信。HTTP、IMAP、FTP 都可以从 C&#x2F;S 角度理解。</p><p>P2P 结构中，每个 peer 既可以请求服务，也可以提供服务。它的自扩展性来自一个关键事实：新节点加入时既增加下载需求，也带来上传能力或服务能力。但 P2P 节点可能随时上下线，IP 地址可能变化，管理更复杂。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    S[服务器] --&gt; C1[客户端1]    S --&gt; C2[客户端2]    S --&gt; C3[客户端3]    P1[Peer1] &lt;--&gt; P2[Peer2]    P2 &lt;--&gt; P3[Peer3]    P3 &lt;--&gt; P1  </pre></div><p>这张图对比了两种压力分布方式：C&#x2F;S 的服务能力集中在服务器端，P2P 的服务能力分布在多个 peer 上。现实系统常采用混合结构，例如中心服务器负责登录、搜索和发现节点，真正数据传输由端系统之间完成。</p><h2 id="3-应用层真正通信的是进程"><a href="#3-应用层真正通信的是进程" class="headerlink" title="3. 应用层真正通信的是进程"></a>3. 应用层真正通信的是进程</h2><p>网络应用通信不是抽象的“电脑和电脑聊天”，而是不同主机上的进程交换应用层 message。</p><p>需要区分四个概念：</p><ul><li>host：运行应用的电脑、手机或服务器。</li><li>process：主机上正在运行的程序。</li><li>message：应用层交换的数据。</li><li>protocol：进程交换 message 时遵守的规则。</li></ul><p>同一台主机内的进程可以使用操作系统提供的 IPC 通信；不同主机上的进程则必须通过网络协议和 socket 交换 message。</p><p>在一次具体通信中，client process 是主动发起通信的进程，server process 是等待并响应请求的进程。即使在 P2P 系统中，也可以按某一次交互区分 client process 和 server process，只是这些角色不是固定身份。</p><h2 id="4-Socket-是接口，不是协议"><a href="#4-Socket-是接口，不是协议" class="headerlink" title="4. Socket 是接口，不是协议"></a>4. Socket 是接口，不是协议</h2><p>Socket 是应用进程和传输层之间的接口。应用程序通过 socket 发送或接收数据，但 socket 本身不是 HTTP、TCP 或 UDP 那样的协议。</p><p>更准确的关系是：</p><ul><li>HTTP：应用层协议，规定请求和响应的格式、语义和交互规则。</li><li>TCP：传输层协议，提供可靠、有序、面向连接的字节流服务。</li><li>UDP：传输层协议，提供无连接、不可靠的数据报服务。</li><li>Socket：应用程序调用 TCP&#x2F;UDP 服务的编程接口。</li></ul><p>例如浏览器访问网页时，会按 HTTP 格式构造 GET 请求，然后通过 socket 把这串字节交给 TCP。TCP 不理解 HTTP 的含义，只负责把字节可靠地传给对方主机。服务器进程通过 socket 收到字节后，再按 HTTP 规则解释这些内容。</p><h2 id="5-进程寻址：IP-地址还不够"><a href="#5-进程寻址：IP-地址还不够" class="headerlink" title="5. 进程寻址：IP 地址还不够"></a>5. 进程寻址：IP 地址还不够</h2><p>只知道 IP 地址只能定位主机，不能定位主机上的具体进程。一台主机可能同时运行浏览器、邮件客户端、Web 服务器、数据库服务和聊天软件。</p><p>因此，进程寻址至少需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">IP address + port number</span><br></pre></td></tr></table></figure><p>IP 地址定位主机，端口号定位这台主机上的服务进程。例如 <code>128.119.245.12:80</code> 表示 IP 地址为 <code>128.119.245.12</code> 的主机上监听 80 端口的 Web 服务。</p><h2 id="6-应用层协议规定什么"><a href="#6-应用层协议规定什么" class="headerlink" title="6. 应用层协议规定什么"></a>6. 应用层协议规定什么</h2><p>应用层协议规定的是进程如何交换 message。它通常包括四类内容：</p><ol><li>消息类型，例如 request、response。</li><li>消息语法，即有哪些字段、字段如何组织。</li><li>消息语义，即字段代表什么含义。</li><li>交互规则，即何时发送消息、收到消息后如何响应。</li></ol><p>以 HTTP 为例，客户端可以发送 GET request，服务器返回 response；请求行、首部字段、空行和 body 属于语法；GET 表示请求资源、状态码 200 或 404 表示结果含义；客户端先发请求、服务器再响应则属于交互规则。</p><p>公开协议通常由 RFC 定义，便于不同实现互通，如 HTTP、SMTP、DNS。私有协议则由公司或组织自行定义，如某些即时通信、会议或平台内部协议。</p><h2 id="7-应用对传输层服务的需求"><a href="#7-应用对传输层服务的需求" class="headerlink" title="7. 应用对传输层服务的需求"></a>7. 应用对传输层服务的需求</h2><p>不同应用对传输层服务的需求不同，主要体现在四个方面：</p><table><thead><tr><th>需求</th><th>含义</th><th>典型应用</th></tr></thead><tbody><tr><td>可靠性</td><td>数据不能丢、不能乱</td><td>文件传输、邮件、网页文档</td></tr><tr><td>时延</td><td>要求低延迟或及时播放</td><td>视频会议、语音、游戏</td></tr><tr><td>吞吐量</td><td>需要足够传输速率</td><td>视频流、云游戏</td></tr><tr><td>安全性</td><td>需要加密、完整性和认证</td><td>登录、支付、网银</td></tr></tbody></table><p>这里容易和“应用层协议规定什么”混淆。可靠性、时延、吞吐量、安全性是应用对传输服务的需求；消息类型、语法、语义、交互规则才是应用层协议本身规定的内容。</p><h2 id="8-TCP、UDP-与-TLS"><a href="#8-TCP、UDP-与-TLS" class="headerlink" title="8. TCP、UDP 与 TLS"></a>8. TCP、UDP 与 TLS</h2><p>TCP 提供可靠传输、流量控制、拥塞控制和面向连接服务，适合文件传输、网页、邮件、远程登录等重视可靠性的应用。但 TCP 不保证固定时延，不保证最低吞吐量，也不提供加密。</p><p>UDP 提供无连接、不可靠的数据报服务，不做可靠性、流量控制或拥塞控制。它适合短事务或更重视低时延的场景，例如普通 DNS 查询、实时音视频、游戏和部分流媒体。UDP 本身不可靠，但应用可以在 UDP 之上自行实现可靠性，例如 QUIC 运行在 UDP 上并实现了连接管理、可靠传输和加密。</p><p>TLS 解决安全性问题。TCP 能保证数据可靠送到，但不能保证不被偷看、篡改，也不能证明对方身份。TLS 运行在应用和 TCP 之间，提供加密、完整性保护和端点认证。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">HTTP + TCP       = 明文 HTTP</span><br><span class="line">HTTP + TLS + TCP = HTTPS</span><br></pre></td></tr></table></figure><h2 id="9-易混点"><a href="#9-易混点" class="headerlink" title="9. 易混点"></a>9. 易混点</h2><p>Socket 是接口，不是协议。HTTP 是应用层协议，TCP&#x2F;UDP 是传输层协议。</p><p>IP 地址只能定位主机，端口号才能进一步定位主机上的进程。</p><p>P2P 不是完全不能区分 client 和 server。在一次交互中，主动请求者是 client process，等待并响应者是 server process。</p><p>普通 DNS 查询通常使用 UDP，而不是 TCP。DNS 响应过大、区域传送或某些安全场景下可能使用 TCP，但常规短查询使用 UDP 可以减少连接开销。</p><h2 id="10-复习清单"><a href="#10-复习清单" class="headerlink" title="10. 复习清单"></a>10. 复习清单</h2><p>学完本节后，应能回答：</p><ul><li>为什么网络应用运行在端系统上，而不是网络核心中？</li><li>C&#x2F;S 和 P2P 的核心区别是什么？</li><li>P2P 为什么具有自扩展性？</li><li>socket 和 HTTP、TCP 分别是什么关系？</li><li>为什么只知道 IP 地址不能定位具体应用进程？</li><li>应用层协议一般规定哪四类内容？</li><li>应用对传输层服务有哪些需求？</li><li>文件传输、视频会议、普通 DNS 查询分别更适合 TCP 还是 UDP？</li><li>TCP 已经可靠，为什么还需要 TLS？</li></ul><hr><h2 id="最终总结：-Chapter-2-1-建立了应用层的通用框架：应用运行在端系统，进程通过-socket-使用传输层服务，IP-和端口共同定位进程，应用层协议规定-message-的规则，而-TCP、UDP、TLS-则根据应用需求提供不同能力。"><a href="#最终总结：-Chapter-2-1-建立了应用层的通用框架：应用运行在端系统，进程通过-socket-使用传输层服务，IP-和端口共同定位进程，应用层协议规定-message-的规则，而-TCP、UDP、TLS-则根据应用需求提供不同能力。" class="headerlink" title="最终总结： Chapter 2.1 建立了应用层的通用框架：应用运行在端系统，进程通过 socket 使用传输层服务，IP 和端口共同定位进程，应用层协议规定 message 的规则，而 TCP、UDP、TLS 则根据应用需求提供不同能力。"></a><strong>最终总结：</strong> Chapter 2.1 建立了应用层的通用框架：应用运行在端系统，进程通过 socket 使用传输层服务，IP 和端口共同定位进程，应用层协议规定 message 的规则，而 TCP、UDP、TLS 则根据应用需求提供不同能力。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter1-3_协议分层封装安全与互联网历史</title>
      <link href="/2026/06/05/Chapter1-3-%E5%8D%8F%E8%AE%AE%E5%88%86%E5%B1%82%E5%B0%81%E8%A3%85%E5%AE%89%E5%85%A8%E4%B8%8E%E4%BA%92%E8%81%94%E7%BD%91%E5%8E%86%E5%8F%B2-essay/"/>
      <url>/2026/06/05/Chapter1-3-%E5%8D%8F%E8%AE%AE%E5%88%86%E5%B1%82%E5%B0%81%E8%A3%85%E5%AE%89%E5%85%A8%E4%B8%8E%E4%BA%92%E8%81%94%E7%BD%91%E5%8E%86%E5%8F%B2-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-1-3：协议分层、封装、安全与互联网历史"><a href="#Chapter-1-3：协议分层、封装、安全与互联网历史" class="headerlink" title="Chapter 1.3：协议分层、封装、安全与互联网历史"></a>Chapter 1.3：协议分层、封装、安全与互联网历史</h1><p>Chapter 1.3 的主线，是用分层思想理解互联网。网络系统包含主机、路由器、链路、应用、协议、硬件和软件，如果把所有功能混在一起设计，系统会难以理解、维护和升级。分层的意义，就是把复杂通信拆成若干可管理的问题：应用如何交换消息，进程如何传输数据，主机如何跨网络到达，邻接节点如何交付，bit 如何在媒介上传播。</p><hr><h2 id="本节核心结论：-每一层使用下层服务，通过本层协议实现自己的功能，并向上一层提供服务；数据发送时逐层封装，接收时逐层解封装。"><a href="#本节核心结论：-每一层使用下层服务，通过本层协议实现自己的功能，并向上一层提供服务；数据发送时逐层封装，接收时逐层解封装。" class="headerlink" title="本节核心结论： 每一层使用下层服务，通过本层协议实现自己的功能，并向上一层提供服务；数据发送时逐层封装，接收时逐层解封装。"></a><strong>本节核心结论：</strong> 每一层使用下层服务，通过本层协议实现自己的功能，并向上一层提供服务；数据发送时逐层封装，接收时逐层解封装。</h2><h2 id="1-为什么网络需要分层"><a href="#1-为什么网络需要分层" class="headerlink" title="1. 为什么网络需要分层"></a>1. 为什么网络需要分层</h2><p>网络是复杂系统。它同时包含端系统、路由器、交换机、链路、应用程序、协议、操作系统和物理媒介。若不分层，每次修改底层链路、路由算法或应用协议，都可能牵动整个系统。</p><p>分层把问题拆开：</p><ul><li>应用层解决应用进程之间交换什么消息。</li><li>传输层解决进程到进程的数据传输。</li><li>网络层解决主机到主机的路由。</li><li>链路层解决相邻节点之间的数据传输。</li><li>物理层解决 bit 如何在媒介上传播。</li></ul><p>分层的好处是概念清晰、模块化、易维护、易替换。例如 WiFi 换成以太网，不需要重写 HTTP；底层链路变化时，上层应用仍可继续使用相似的服务接口。</p><h2 id="2-服务是垂直的，协议是水平的"><a href="#2-服务是垂直的，协议是水平的" class="headerlink" title="2. 服务是垂直的，协议是水平的"></a>2. 服务是垂直的，协议是水平的</h2><p>服务和协议是本节最重要的一组区分。</p><p>服务是垂直关系：低层向上一层提供能力。比如 TCP 向应用层提供可靠字节流服务。</p><p>协议是水平关系：同一层的通信实体之间遵守共同规则。比如发送方 TCP 和接收方 TCP 之间如何编号、确认、重传，是 TCP 协议的一部分。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    AppA[应用层 A] -.应用协议.-&gt; AppB[应用层 B]    TransA[传输层 A] -.传输协议.-&gt; TransB[传输层 B]    NetA[网络层 A] -.网络协议.-&gt; NetB[网络层 B]    AppA --&gt;|使用服务| TransA    AppB --&gt;|使用服务| TransB    TransA --&gt;|使用服务| NetA    TransB --&gt;|使用服务| NetB  </pre></div><p>这张图的重点是方向：服务是上下层之间的垂直依赖，协议是对等层之间的水平约定。混淆这两个概念，会导致后续学习 TCP、IP、HTTP 时分不清“规则”和“能力”。</p><h2 id="3-Internet-五层协议栈"><a href="#3-Internet-五层协议栈" class="headerlink" title="3. Internet 五层协议栈"></a>3. Internet 五层协议栈</h2><p>Internet 常用五层模型：应用层、传输层、网络层、链路层、物理层。</p><table><thead><tr><th>层次</th><th>主要问题</th><th>典型协议或技术</th><th>数据单位</th></tr></thead><tbody><tr><td>应用层</td><td>应用之间如何交换消息</td><td>HTTP、SMTP、IMAP、DNS</td><td>Message</td></tr><tr><td>传输层</td><td>进程到进程的数据传输</td><td>TCP、UDP</td><td>Segment</td></tr><tr><td>网络层</td><td>主机到主机的路由</td><td>IP、路由协议</td><td>Datagram &#x2F; Packet</td></tr><tr><td>链路层</td><td>相邻节点之间传输</td><td>Ethernet、802.11 WiFi、PPP</td><td>Frame</td></tr><tr><td>物理层</td><td>bit 在媒介上传播</td><td>电信号、光信号、无线电波</td><td>Bit</td></tr></tbody></table><p>应用层关注消息格式和请求响应，例如浏览器和 Web 服务器之间的 HTTP 交互。传输层关注端口、多路复用、可靠传输、流量控制和拥塞控制等问题。网络层关注 IP 地址和路由路径。链路层关注同一条链路上的帧传输。物理层关注 0 和 1 如何变成可传播的信号。</p><h2 id="4-OSI-七层与-Internet-五层"><a href="#4-OSI-七层与-Internet-五层" class="headerlink" title="4. OSI 七层与 Internet 五层"></a>4. OSI 七层与 Internet 五层</h2><p>OSI 七层模型比 Internet 五层多了表示层和会话层。</p><table><thead><tr><th>模型</th><th>层次</th></tr></thead><tbody><tr><td>OSI 七层</td><td>应用层、表示层、会话层、传输层、网络层、链路层、物理层</td></tr><tr><td>Internet 五层</td><td>应用层、传输层、网络层、链路层、物理层</td></tr></tbody></table><p>表示层负责数据表示相关工作，例如加密、压缩、机器相关表示转换。会话层负责数据交换的同步、检查点和恢复。Internet 协议栈没有单独设置这两层；如果应用需要这些能力，通常由应用层或应用相关协议自己实现。</p><p>考试中常见问题是：Internet 协议栈缺少 OSI 的哪两层？答案是表示层和会话层。</p><h2 id="5-封装：数据如何逐层加头部"><a href="#5-封装：数据如何逐层加头部" class="headerlink" title="5. 封装：数据如何逐层加头部"></a>5. 封装：数据如何逐层加头部</h2><p>封装解释了数据从应用层向下经过协议栈时发生的事情。发送端每经过一层，就会给上层数据加上本层头部；接收端则反向逐层去掉头部。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    M[Message] --&gt; S[Segment]    S --&gt; D[Datagram]    D --&gt; F[Frame]    F --&gt; B[Bit 流]    B --&gt; F2[Frame]    F2 --&gt; D2[Datagram]    D2 --&gt; S2[Segment]    S2 --&gt; M2[Message]  </pre></div><p>这张图展示了封装和解封装的主干：应用层 message 向下变成 segment、datagram、frame 和 bit 流；到达目的端后，再由底层向上恢复为应用层 message。</p><p>更具体地说：</p><ul><li>应用层产生 message。</li><li>传输层加上传输层头部，形成 segment。</li><li>网络层加上网络层头部，形成 datagram 或 packet。</li><li>链路层加上链路层头部，形成 frame。</li><li>物理层把 frame 转换成 bit 信号发送。</li></ul><p>每一层主要读取和处理自己这一层的头部。中间设备通常不需要理解完整应用内容，这也是分层能够扩展的重要原因。</p><h2 id="6-主机、路由器和交换机处理的层次不同"><a href="#6-主机、路由器和交换机处理的层次不同" class="headerlink" title="6. 主机、路由器和交换机处理的层次不同"></a>6. 主机、路由器和交换机处理的层次不同</h2><p>端系统通常具有完整的协议栈：应用层、传输层、网络层、链路层和物理层都需要处理。</p><p>路由器主要处理网络层、链路层和物理层。它关注 IP 数据报如何从一个网络转发到另一个网络。</p><p>交换机主要处理链路层和物理层。它通常关注链路层帧，例如 MAC 地址和局域网内部转发。</p><p>因此，中间网络设备通常不关心 HTTP 报文里的具体网页内容。它们只在自己负责的层次上读取必要信息并完成转发。</p><h2 id="7-安全：互联网最初没有充分威胁模型"><a href="#7-安全：互联网最初没有充分威胁模型" class="headerlink" title="7. 安全：互联网最初没有充分威胁模型"></a>7. 安全：互联网最初没有充分威胁模型</h2><p>Internet 早期设计假设相对开放和互信，并没有把安全威胁作为核心前提。随着互联网规模扩大，攻击者、恶意流量和身份伪造成为现实问题，安全逐渐变成每一层都必须考虑的设计约束。</p><p>典型威胁包括：</p><ul><li>Packet sniffing：在共享媒介中嗅探数据包，可能读取明文密码或敏感信息。</li><li>IP spoofing：伪造源 IP 地址，使接收方误以为数据来自可信主机。</li><li>MITM：攻击者位于通信双方之间，转发、监听或篡改消息。</li><li>DoS &#x2F; DDoS：用大量流量压垮服务器、链路或其他资源，使合法用户无法访问服务。</li></ul><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant A as 通信方 A    participant M as 中间人    participant B as 通信方 B    A-&gt;&gt;M: 发送消息    M-&gt;&gt;B: 转发或篡改    B-&gt;&gt;M: 返回响应    M-&gt;&gt;A: 转发或篡改  </pre></div><p>这张图展示 MITM 的基本结构：A 和 B 都以为自己在直接通信，但消息实际经过中间人。HTTPS&#x2F;TLS 等机制的重要目标之一，就是降低这种监听和篡改风险。</p><p>网络攻击的一般步骤通常包括信息搜集、漏洞挖掘和利用、攻击实施、痕迹消除。学习这些内容的目的不是实施攻击，而是理解为什么协议、认证、加密、日志和监控都是现代网络设计的一部分。</p><h2 id="8-Internet-历史：从分组交换到-TCP-IP"><a href="#8-Internet-历史：从分组交换到-TCP-IP" class="headerlink" title="8. Internet 历史：从分组交换到 TCP&#x2F;IP"></a>8. Internet 历史：从分组交换到 TCP&#x2F;IP</h2><p>历史部分说明了现代互联网架构为什么形成。电路交换适合传统电话通信，却不适合突发性强的计算机通信：连接建立成本高、资源独占、可靠性和扩展性都受限制。分组交换因此成为计算机网络的重要方向。</p><p>重要历史节点可以压缩为一条主线：</p><ul><li>1960 年代：Kleinrock、Baran 等人推动分组交换思想。</li><li>1969 年：ARPAnet 的早期节点开始运行。</li><li>1970 年代：ARPAnet 公开演示，电子邮件、Ethernet、互联网络思想逐渐成熟。</li><li>1980 年代：TCP&#x2F;IP 部署，DNS 定义，TCP 拥塞控制出现。</li></ul><p>Cerf 和 Kahn 的互联原则对今天的 Internet 影响深远：极简主义和自治性、尽力而为服务模型、无状态路由、分散控制。这些原则让不同网络不必内部完全统一，只要能通过共同的互联协议通信，就可以加入全球互联网。</p><hr><h2 id="历史主线：-Internet-的成功不在于要求所有网络内部一致，而在于允许不同网络自治，并通过统一的互联协议实现全球范围的连接。"><a href="#历史主线：-Internet-的成功不在于要求所有网络内部一致，而在于允许不同网络自治，并通过统一的互联协议实现全球范围的连接。" class="headerlink" title="历史主线： Internet 的成功不在于要求所有网络内部一致，而在于允许不同网络自治，并通过统一的互联协议实现全球范围的连接。"></a><strong>历史主线：</strong> Internet 的成功不在于要求所有网络内部一致，而在于允许不同网络自治，并通过统一的互联协议实现全球范围的连接。</h2><h2 id="9-易混点与复习清单"><a href="#9-易混点与复习清单" class="headerlink" title="9. 易混点与复习清单"></a>9. 易混点与复习清单</h2><p>服务和协议不要混淆。服务是下层给上层的能力，协议是同层实体之间的规则。</p><p>Segment、Datagram、Frame 不要都笼统叫“包”。传输层单位是 segment，网络层单位是 datagram 或 packet，链路层单位是 frame。</p><p>Router 和 switch 处理层次不同。Router 主要看网络层 IP 信息，switch 主要看链路层帧信息。</p><p>OSI 七层不是 Internet 实际完整采用的协议栈。Internet 通常讲五层模型，表示层和会话层功能若需要，通常由应用层处理。</p><p>安全不是某一层的附加小节。Internet 每一层都可能有安全问题，也可能需要相应防护机制。</p><p>学完本节后，应能回答：</p><ul><li>为什么复杂网络系统需要分层？</li><li>“服务是垂直的，协议是水平的”是什么意思？</li><li>Internet 五层从上到下分别是什么？</li><li>每一层分别解决什么问题？</li><li>OSI 七层比 Internet 五层多哪两层？</li><li>什么是封装和解封装？</li><li>Message、Segment、Datagram、Frame、Bit 分别属于哪一层？</li><li>路由器和交换机主要处理哪些层次？</li><li>packet sniffing、IP spoofing、MITM、DoS&#x2F;DDoS 的基本思想是什么？</li><li>分组交换、ARPAnet、TCP&#x2F;IP 在互联网历史中分别处于什么位置？</li></ul><hr><h2 id="最终总结：-Chapter-1-3-把-Chapter-1-的网络地图进一步结构化：分层让复杂系统可理解，封装让数据能沿协议栈传递，安全提醒我们互联网早期假设并不充分，历史则解释了分组交换和-TCP-IP-为什么成为现代-Internet-的基础。"><a href="#最终总结：-Chapter-1-3-把-Chapter-1-的网络地图进一步结构化：分层让复杂系统可理解，封装让数据能沿协议栈传递，安全提醒我们互联网早期假设并不充分，历史则解释了分组交换和-TCP-IP-为什么成为现代-Internet-的基础。" class="headerlink" title="最终总结： Chapter 1.3 把 Chapter 1 的网络地图进一步结构化：分层让复杂系统可理解，封装让数据能沿协议栈传递，安全提醒我们互联网早期假设并不充分，历史则解释了分组交换和 TCP&#x2F;IP 为什么成为现代 Internet 的基础。"></a><strong>最终总结：</strong> Chapter 1.3 把 Chapter 1 的网络地图进一步结构化：分层让复杂系统可理解，封装让数据能沿协议栈传递，安全提醒我们互联网早期假设并不充分，历史则解释了分组交换和 TCP&#x2F;IP 为什么成为现代 Internet 的基础。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter1-2_Internet结构延时丢包与吞吐量</title>
      <link href="/2026/06/05/Chapter1-2-Internet%E7%BB%93%E6%9E%84%E5%BB%B6%E6%97%B6%E4%B8%A2%E5%8C%85%E4%B8%8E%E5%90%9E%E5%90%90%E9%87%8F-essay/"/>
      <url>/2026/06/05/Chapter1-2-Internet%E7%BB%93%E6%9E%84%E5%BB%B6%E6%97%B6%E4%B8%A2%E5%8C%85%E4%B8%8E%E5%90%9E%E5%90%90%E9%87%8F-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-1-2：Internet-结构、延时、丢包与吞吐量"><a href="#Chapter-1-2：Internet-结构、延时、丢包与吞吐量" class="headerlink" title="Chapter 1.2：Internet 结构、延时、丢包与吞吐量"></a>Chapter 1.2：Internet 结构、延时、丢包与吞吐量</h1><p>Chapter 1.2 在 Chapter 1.1 的基础上继续追问两个问题：第一，全球大量网络到底如何互联成 Internet；第二，分组在网络中传输时为什么会慢、为什么会丢、吞吐量又由什么决定。前者属于 Internet structure，后者属于 network performance。</p><hr><h2 id="本节核心结论：-Internet-是由多层-ISP、IXP-和内容提供商网络互联形成的复杂结构；分组传输性能主要由延时、丢包和吞吐量刻画。"><a href="#本节核心结论：-Internet-是由多层-ISP、IXP-和内容提供商网络互联形成的复杂结构；分组传输性能主要由延时、丢包和吞吐量刻画。" class="headerlink" title="本节核心结论： Internet 是由多层 ISP、IXP 和内容提供商网络互联形成的复杂结构；分组传输性能主要由延时、丢包和吞吐量刻画。"></a><strong>本节核心结论：</strong> Internet 是由多层 ISP、IXP 和内容提供商网络互联形成的复杂结构；分组传输性能主要由延时、丢包和吞吐量刻画。</h2><h2 id="1-Internet-是网络中的网络"><a href="#1-Internet-是网络中的网络" class="headerlink" title="1. Internet 是网络中的网络"></a>1. Internet 是网络中的网络</h2><p>小写 <code>internet</code> 泛指互联网络，是一种把多个网络连接起来的通用概念；大写 <code>Internet</code> 是专有名词，指全球公共互联网。</p><p>端系统不会直接连接到世界上所有主机。电脑、手机和服务器通常先接入本地 ISP、校园网、企业网或移动网络，再通过 ISP 之间的互联到达其他网络。任意两台主机能够通信，前提是它们所在的接入网络最终能通过某种结构互相到达。</p><p>最朴素的设想是让所有 access ISP 两两直连，但如果有 <code>N</code> 个 ISP，连接数会接近 <code>O(N^2)</code>。当 ISP 数量达到百万级时，这种结构完全不可扩展。因此，Internet 逐渐形成了层次化、对等互联和内容网络并存的结构。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    User[端系统] --&gt; Access[Access ISP]    Access --&gt; Regional[Regional ISP]    Regional --&gt; Tier[Tier-1 ISP]    Access --&gt; IXP[IXP]    Regional --&gt; IXP    Provider[内容网络] --&gt; IXP    Provider --&gt; Cache[近端内容节点]    Cache --&gt; User  </pre></div><p>这张图强调：Internet 不是一个中心化大网络，而是由接入 ISP、区域 ISP、骨干 ISP、IXP 和内容提供商网络共同组成。IXP 让网络之间交换流量，内容节点则把服务部署到更靠近用户的位置。</p><h2 id="2-ISP、IXP-与内容提供商网络"><a href="#2-ISP、IXP-与内容提供商网络" class="headerlink" title="2. ISP、IXP 与内容提供商网络"></a>2. ISP、IXP 与内容提供商网络</h2><p>access ISP 负责把端系统接入互联网，例如家庭宽带、校园网、移动网络。regional ISP 把多个接入网络汇聚起来，再连接到更高层网络。tier-1 或 global ISP 负责大范围骨干互联。</p><p>IXP 是 Internet Exchange Point，可以理解为网络之间的换乘站。不同 ISP 可以在这里交换流量，而不是所有流量都绕经更高层网络。IXP 不是 ISP：ISP 是网络服务提供商，IXP 是多个网络交换流量的基础设施。</p><p>内容提供商网络是现代 Internet 的重要组成部分。Google、Microsoft、Akamai 等大型内容提供商会建设自己的网络、数据中心和缓存节点，把内容放到离用户更近的地方。用户访问视频、搜索或云服务时，数据不一定来自远方总部服务器，可能来自附近的内容节点。</p><hr><h2 id="结构理解：-传统-ISP-结构解决“网络如何互联”，内容提供商网络进一步解决“服务如何靠近用户”。"><a href="#结构理解：-传统-ISP-结构解决“网络如何互联”，内容提供商网络进一步解决“服务如何靠近用户”。" class="headerlink" title="结构理解： 传统 ISP 结构解决“网络如何互联”，内容提供商网络进一步解决“服务如何靠近用户”。"></a><strong>结构理解：</strong> 传统 ISP 结构解决“网络如何互联”，内容提供商网络进一步解决“服务如何靠近用户”。</h2><h2 id="3-网络性能：delay、loss、throughput"><a href="#3-网络性能：delay、loss、throughput" class="headerlink" title="3. 网络性能：delay、loss、throughput"></a>3. 网络性能：delay、loss、throughput</h2><p>分组经过路由器时，可能需要等待、转发、传播，也可能因为缓冲区满而被丢弃。Chapter 1.2 用三类指标刻画性能：</p><ul><li>delay：分组从一个节点或端到端路径通过所需时间。</li><li>loss：分组因缓冲区满等原因被丢弃。</li><li>throughput：源端到目的端实际传输数据的速率。</li></ul><p>这些指标不是彼此孤立的。到达速率超过输出链路能力时，队列增长，排队延时增大；缓冲区被填满后，新到达分组被丢弃；端到端吞吐量则受路径中最慢的链路限制。</p><h2 id="4-四种节点延时"><a href="#4-四种节点延时" class="headerlink" title="4. 四种节点延时"></a>4. 四种节点延时</h2><p>节点总延时可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">d_nodal = d_proc + d_queue + d_trans + d_prop</span><br></pre></td></tr></table></figure><p>四项含义分别是：</p><table><thead><tr><th>延时</th><th>含义</th><th>主要决定因素</th></tr></thead><tbody><tr><td><code>d_proc</code></td><td>处理延时，检查首部和差错、决定下一跳</td><td>路由器处理能力</td></tr><tr><td><code>d_queue</code></td><td>排队延时，在输出链路前等待</td><td>拥塞程度和队列长度</td></tr><tr><td><code>d_trans</code></td><td>传输延时，把分组推上链路</td><td>分组长度 <code>L</code> 和链路速率 <code>R</code></td></tr><tr><td><code>d_prop</code></td><td>传播延时，信号在链路中传播</td><td>链路长度 <code>d</code> 和传播速度 <code>s</code></td></tr></tbody></table><p>传输延时公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">d_trans = L / R</span><br></pre></td></tr></table></figure><p>其中 <code>L</code> 是分组长度，单位 bit；<code>R</code> 是链路传输速率，单位 bps。它表示把整个分组推上链路需要多久。</p><p>传播延时公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">d_prop = d / s</span><br></pre></td></tr></table></figure><p>其中 <code>d</code> 是链路物理长度，<code>s</code> 是信号传播速度。它表示信号已经进入媒介后，从一端跑到另一端所需时间。</p><h2 id="5-传输延时和传播延时不能混淆"><a href="#5-传输延时和传播延时不能混淆" class="headerlink" title="5. 传输延时和传播延时不能混淆"></a>5. 传输延时和传播延时不能混淆</h2><p>传输延时关心“把数据放到链路上要多久”，由 <code>L/R</code> 决定；传播延时关心“信号在链路中跑到对面要多久”，由 <code>d/s</code> 决定。</p><p>可以用车队类比理解：一辆车对应一个 bit，整个车队对应一个 packet。收费站让所有车依次通过的时间，对应传输延时；车通过收费站后在公路上行驶到下一个收费站的时间，对应传播延时。</p><p>因此，一个分组的最后一个 bit 到达下一跳，既要等待整个分组被推出去，也要等待最后一个 bit 在链路上传播过去。</p><h2 id="6-排队、流量强度与丢包"><a href="#6-排队、流量强度与丢包" class="headerlink" title="6. 排队、流量强度与丢包"></a>6. 排队、流量强度与丢包</h2><p>排队发生在路由器输出链路之前。当分组到达速率暂时超过输出链路服务能力时，分组会在缓冲区中等待。</p><p>流量强度通常写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">La / R</span><br></pre></td></tr></table></figure><p>其中 <code>a</code> 是平均分组到达速率，<code>L</code> 是分组长度，<code>R</code> 是链路速率。<code>La</code> 表示平均 bit 到达速率，<code>R</code> 表示输出链路服务速率。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    In[分组到达] --&gt; Buffer[路由器缓冲区]    Buffer --&gt; Link[输出链路]    Link --&gt; Out[下一跳]    Heavy[La&#x2F;R 接近 1] --&gt; Queue[队列变长]    Queue --&gt; Delay[排队延时增大]    Delay --&gt; Full[缓冲区满]    Full --&gt; Loss[新分组丢弃]  </pre></div><p>这张图说明排队和丢包是同一机制的两个阶段。先是到达速率过高导致队列变长，表现为延时增加；当缓冲区满后，才表现为分组丢失。</p><p>当 <code>La/R</code> 接近 0 时，平均排队延时很小；当 <code>La/R</code> 接近 1 时，平均排队延时急剧增大；当 <code>La/R &gt; 1</code> 且持续存在时，平均到达速率超过平均服务速率，队列会不稳定增长。在有限缓冲区的真实路由器中，这最终会导致丢包。</p><h2 id="7-吞吐量与瓶颈链路"><a href="#7-吞吐量与瓶颈链路" class="headerlink" title="7. 吞吐量与瓶颈链路"></a>7. 吞吐量与瓶颈链路</h2><p>Throughput 是源端和目标端之间实际传输数据的速率，可以分为瞬时吞吐量和平均吞吐量。</p><p>若服务器发送链路速率为 <code>R_s</code>，客户端接收链路速率为 <code>R_c</code>，端到端吞吐量最多为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">min(R_s, R_c)</span><br></pre></td></tr></table></figure><p>因为路径上的最慢链路限制了整体速率，这条链路称为瓶颈链路。</p><p>如果 10 个连接公平共享一条 <code>R</code> bps 的瓶颈链路，每个连接大约只能分到 <code>R/10</code>。此时每个连接的端到端吞吐量可以理解为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">min(R_s, R_c, R/10)</span><br></pre></td></tr></table></figure><p>因此，下载慢不一定是服务器慢，也可能是本地接入链路慢，或者中间某条共享链路成为瓶颈。</p><h2 id="8-两个学习澄清"><a href="#8-两个学习澄清" class="headerlink" title="8. 两个学习澄清"></a>8. 两个学习澄清</h2><h3 id="8-1-1-Mbps-链路速率是否稳定"><a href="#8-1-1-Mbps-链路速率是否稳定" class="headerlink" title="8.1 1 Mbps 链路速率是否稳定"></a>8.1 <code>1 Mbps</code> 链路速率是否稳定</h3><p>在课件模型里，<code>R = 1 Mbps</code> 通常被看成链路带宽或传输速率，是给定常数。用它计算 <code>L/R</code> 时，只考虑分组大小和链路速率，不考虑当前是否拥塞。</p><p>真实网络中要区分标称链路速率和实际吞吐量。有线链路的物理配置可能相对稳定，拥塞不会改变其物理发送速率，但会让分组在输出链路前等待，从而增加 <code>d_queue</code>。WiFi、4G&#x2F;5G、共享宽带的实际可用速率则会受到信号、干扰、共享用户、链路层重传和路径拥塞影响。</p><p>更准确地说：链路速率 <code>R</code> 在抽象模型中通常稳定；用户实际感受到的 throughput 会随网络状态波动。</p><h3 id="8-2-La-R-1-是否立刻丢包"><a href="#8-2-La-R-1-是否立刻丢包" class="headerlink" title="8.2 La/R &gt; 1 是否立刻丢包"></a>8.2 <code>La/R &gt; 1</code> 是否立刻丢包</h3><p><code>La/R &gt; 1</code> 表示平均到达速率超过平均服务速率，它首先意味着队列会增长、排队延时会变大，并不一定意味着立刻丢包。</p><p>如果缓冲区还有空间，短时间的 <code>La/R &gt; 1</code> 只会让队列从较短变长；当缓冲区被填满后，新到达的分组才会被丢弃。若 <code>La/R &gt; 1</code> 持续存在，在有限缓冲区下最终会发生 loss。</p><hr><h2 id="严谨表述：-La-R-1-不是“立刻丢包”的条件，而是“队列不稳定增长”的条件；是否立刻丢包取决于缓冲区是否已经满。"><a href="#严谨表述：-La-R-1-不是“立刻丢包”的条件，而是“队列不稳定增长”的条件；是否立刻丢包取决于缓冲区是否已经满。" class="headerlink" title="严谨表述： La/R &gt; 1 不是“立刻丢包”的条件，而是“队列不稳定增长”的条件；是否立刻丢包取决于缓冲区是否已经满。"></a><strong>严谨表述：</strong> <code>La/R &gt; 1</code> 不是“立刻丢包”的条件，而是“队列不稳定增长”的条件；是否立刻丢包取决于缓冲区是否已经满。</h2><h2 id="9-复习清单"><a href="#9-复习清单" class="headerlink" title="9. 复习清单"></a>9. 复习清单</h2><p>学完本节后，应能回答：</p><ul><li>为什么所有 access ISP 不能直接两两互联？</li><li>global ISP、regional ISP、access ISP 分别承担什么角色？</li><li>IXP 的作用是什么？它和 ISP 有什么不同？</li><li>内容提供商为什么要建设自己的网络？</li><li>节点延时由哪四部分组成？</li><li><code>d_trans = L/R</code> 和 <code>d_prop = d/s</code> 分别表示什么？</li><li>为什么排队延时最不稳定？</li><li><code>La/R</code> 接近 1 时为什么排队延时急剧增大？</li><li>分组什么时候会丢失？</li><li>什么是瓶颈链路？为什么端到端吞吐量由瓶颈决定？</li></ul><hr><h2 id="最终总结：-Chapter-1-2-说明了-Internet-的现实结构和性能约束：网络由多层-ISP、IXP-和内容网络互联，分组在转发过程中会经历处理、排队、传输和传播，拥塞会先造成排队再造成丢包，而吞吐量受瓶颈链路限制。"><a href="#最终总结：-Chapter-1-2-说明了-Internet-的现实结构和性能约束：网络由多层-ISP、IXP-和内容网络互联，分组在转发过程中会经历处理、排队、传输和传播，拥塞会先造成排队再造成丢包，而吞吐量受瓶颈链路限制。" class="headerlink" title="最终总结： Chapter 1.2 说明了 Internet 的现实结构和性能约束：网络由多层 ISP、IXP 和内容网络互联，分组在转发过程中会经历处理、排队、传输和传播，拥塞会先造成排队再造成丢包，而吞吐量受瓶颈链路限制。"></a><strong>最终总结：</strong> Chapter 1.2 说明了 Internet 的现实结构和性能约束：网络由多层 ISP、IXP 和内容网络互联，分组在转发过程中会经历处理、排队、传输和传播，拥塞会先造成排队再造成丢包，而吞吐量受瓶颈链路限制。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计网Chapter1-1_互联网整体图像与分组交换</title>
      <link href="/2026/06/05/Chapter1-1-%E4%BA%92%E8%81%94%E7%BD%91%E6%95%B4%E4%BD%93%E5%9B%BE%E5%83%8F%E4%B8%8E%E5%88%86%E7%BB%84%E4%BA%A4%E6%8D%A2-essay/"/>
      <url>/2026/06/05/Chapter1-1-%E4%BA%92%E8%81%94%E7%BD%91%E6%95%B4%E4%BD%93%E5%9B%BE%E5%83%8F%E4%B8%8E%E5%88%86%E7%BB%84%E4%BA%A4%E6%8D%A2-essay/</url>
      
        <content type="html"><![CDATA[<h1 id="Chapter-1-1：从互联网整体图像到分组交换"><a href="#Chapter-1-1：从互联网整体图像到分组交换" class="headerlink" title="Chapter 1.1：从互联网整体图像到分组交换"></a>Chapter 1.1：从互联网整体图像到分组交换</h1><p>Chapter 1.1 的学习目标，是先建立“计算机网络到底由什么组成、如何通信、为什么要这样传输”的总体框架。后续学习应用层、传输层、网络层时，会不断回到这张地图：应用在网络边缘产生数据，数据经接入网进入网络核心，再由路由器和交换机沿链路逐跳转发。</p><hr><h2 id="本节核心结论：-Internet-不是单台机器或单个网络，而是由大量端系统、接入网、网络核心、通信链路和协议共同构成的“网络的网络”。"><a href="#本节核心结论：-Internet-不是单台机器或单个网络，而是由大量端系统、接入网、网络核心、通信链路和协议共同构成的“网络的网络”。" class="headerlink" title="本节核心结论： Internet 不是单台机器或单个网络，而是由大量端系统、接入网、网络核心、通信链路和协议共同构成的“网络的网络”。"></a><strong>本节核心结论：</strong> Internet 不是单台机器或单个网络，而是由大量端系统、接入网、网络核心、通信链路和协议共同构成的“网络的网络”。</h2><h2 id="1-Internet-是“网络的网络”"><a href="#1-Internet-是“网络的网络”" class="headerlink" title="1. Internet 是“网络的网络”"></a>1. Internet 是“网络的网络”</h2><p>Internet 可以理解为许多网络互联形成的巨大系统。用户的电脑、手机或服务器并不是直接连到全世界所有设备，而是先连入家庭网、校园网、企业网、移动网络或 ISP 网络，再通过这些网络之间的互联抵达远端主机。</p><p>从组成角度看，Internet 包含四类核心对象：</p><ul><li>端系统：电脑、手机、服务器等运行应用程序的主机。</li><li>中间节点：路由器、交换机等负责转发数据的设备。</li><li>通信链路：有线或无线链路，用来承载 bit。</li><li>协议：规定通信双方如何交换消息的规则。</li></ul><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    App[应用] --&gt; Host[端系统]    Host --&gt; Access[接入网]    Access --&gt; Core[网络核心]    Core --&gt; Remote[远端网络]    Remote --&gt; Server[服务器]    Protocol[协议规则] -.约束通信.-&gt; App    Protocol -.约束转发.-&gt; Core  </pre></div><p>这张图的重点是：应用并不直接控制整个网络路径，而是在端系统中通过协议栈使用网络服务；数据从边缘进入接入网，再进入网络核心，并在协议规则约束下被转发到远端。</p><h2 id="2-组成视角：节点与链路"><a href="#2-组成视角：节点与链路" class="headerlink" title="2. 组成视角：节点与链路"></a>2. 组成视角：节点与链路</h2><p>网络中的节点可以分为终端节点和中间节点。</p><p>终端节点也叫 host 或 end system，是产生和消费数据的地方。例如浏览器所在的电脑、运行网站的服务器、手机上的邮件客户端，都属于终端节点。</p><p>中间节点主要负责搬运数据，例如 router 和 switch。它们一般不关心用户到底是在访问网页、发送邮件还是打游戏，而是根据相应层次的地址和转发规则，把分组送到下一跳。</p><p>通信链路则是节点之间的边。它既包括主机接入互联网的链路，也包括路由器、交换机之间的主干链路。链路可以是双绞线、同轴电缆、光纤，也可以是 WiFi、蜂窝网络或卫星链路。</p><h2 id="3-服务视角：Internet-给应用提供通信基础设施"><a href="#3-服务视角：Internet-给应用提供通信基础设施" class="headerlink" title="3. 服务视角：Internet 给应用提供通信基础设施"></a>3. 服务视角：Internet 给应用提供通信基础设施</h2><p>从服务角度看，Internet 是一个支持分布式应用通信的基础设施。Web、email、VoIP、网络游戏、电子商务和社交网络，都依赖这套基础设施完成远程通信。</p><p>应用程序通常通过 socket interface 使用网络。socket 可以理解为应用通向网络的门：浏览器要请求网页时，不会直接控制网线、电磁波或路由器，而是把数据交给 socket，再由操作系统和协议栈继续处理。</p><hr><h2 id="关键区分：-socket-是应用使用网络服务的接口；协议是通信实体之间必须遵守的规则。"><a href="#关键区分：-socket-是应用使用网络服务的接口；协议是通信实体之间必须遵守的规则。" class="headerlink" title="关键区分： socket 是应用使用网络服务的接口；协议是通信实体之间必须遵守的规则。"></a><strong>关键区分：</strong> socket 是应用使用网络服务的接口；协议是通信实体之间必须遵守的规则。</h2><h2 id="4-协议：网络通信的共同规则"><a href="#4-协议：网络通信的共同规则" class="headerlink" title="4. 协议：网络通信的共同规则"></a>4. 协议：网络通信的共同规则</h2><p>协议规定两个或多个通信实体之间交换消息的方式。它至少包含三类信息：</p><ul><li>语法：消息长什么样，字段如何排列。</li><li>语义：字段表示什么，收到某类消息意味着什么。</li><li>时序与动作：什么时候发送，收到后如何响应。</li></ul><p>人类对话中，“问候、提问、回答”有默认顺序；网络通信也一样。客户端发起 TCP 连接请求，服务器返回响应，随后客户端发送 HTTP GET，服务器返回网页内容。后续学习 HTTP、TCP、IP，本质上都是在学习不同层次的协议规则。</p><h2 id="5-网络边缘、接入网与物理媒介"><a href="#5-网络边缘、接入网与物理媒介" class="headerlink" title="5. 网络边缘、接入网与物理媒介"></a>5. 网络边缘、接入网与物理媒介</h2><p>网络边缘是用户和应用所在的位置，包括主机、客户端、服务器以及运行在这些主机上的应用程序。</p><p>接入网解决的问题是：如何把端系统连接到边缘路由器。家庭宽带、校园以太网、企业网络、WiFi、4G&#x2F;5G 都属于接入方式。学习接入网时要关注两个指标：</p><ul><li>带宽：链路每秒可传输多少 bit。</li><li>共享方式：链路资源是由多个用户共享，还是为某个连接独享。</li></ul><p>物理媒介可以分为两类。导引性媒介让信号沿实体介质传播，如双绞线、同轴电缆和光纤；非导引性媒介让信号在开放空间中传播，如 WiFi、蜂窝网络和卫星通信。第一轮学习不需要死记所有速率，只要知道它们都是承载 bit 的不同物理方式。</p><h2 id="6-网络核心与交换方式"><a href="#6-网络核心与交换方式" class="headerlink" title="6. 网络核心与交换方式"></a>6. 网络核心与交换方式</h2><p>网络核心是由大量路由器构成的网状系统。它要解决的问题是：数据如何穿过多个中间节点，到达目的主机。</p><p>两类典型交换方式分别是电路交换和分组交换。</p><table><thead><tr><th>交换方式</th><th>基本思想</th><th>资源使用方式</th><th>典型直觉</th></tr></thead><tbody><tr><td>电路交换</td><td>通信前建立专用路径</td><td>通信期间独占资源</td><td>先占好一条路再说话</td></tr><tr><td>分组交换</td><td>数据切成 packet 后逐跳转发</td><td>多个用户按需共享链路</td><td>把数据切成包，边走边转发</td></tr></tbody></table><p>现代 Internet 主要使用分组交换，因为计算机通信通常是突发的：用户不会一直稳定占用链路，而是在短时间内发送请求、接收响应，然后进入空闲状态。分组交换让多个用户共享链路资源，更适合这种通信模式。</p><h2 id="7-存储-转发与传输延迟"><a href="#7-存储-转发与传输延迟" class="headerlink" title="7. 存储-转发与传输延迟"></a>7. 存储-转发与传输延迟</h2><p>分组交换的关键机制是 store-and-forward，即存储-转发。一个路由器必须先完整收到一个分组，才能把它转发到下一条链路。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    H1[源主机] --&gt;|发送分组| R1[路由器1]    R1 --&gt;|完整收到后转发| R2[路由器2]    R2 --&gt;|完整收到后转发| R3[路由器3]    R3 --&gt;|完整收到后交付| H2[目的主机]  </pre></div><p>这张图强调“完整收到后再转发”。分组不是一出现第一个 bit 就整体穿过所有路由器，而是在每一跳都经历接收、存储、排队和转发，因此会产生多跳延迟。</p><p>传输延迟的公式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">d_trans = L / R</span><br></pre></td></tr></table></figure><p>其中 <code>L</code> 是分组长度，单位是 bit；<code>R</code> 是链路传输速率，单位是 bit&#x2F;s；<code>L / R</code> 表示把整个分组推上链路需要的时间。若分组长度为 7.5 Mbits，链路速率为 1.5 Mbps，则一跳的传输延迟是 5 秒。若经过三条同速率链路，并采用存储-转发，则只看传输延迟时约为 15 秒。</p><p>要注意，传输延迟不同于传播延迟。传输延迟关心“把 bit 推上链路要多久”，传播延迟关心“信号在线路中跑过去要多久”。前者由 <code>L / R</code> 决定，后者由距离和传播速度决定。</p><h2 id="8-易混点"><a href="#8-易混点" class="headerlink" title="8. 易混点"></a>8. 易混点</h2><p>Internet 和 internet 不完全相同。小写 internet 泛指互联网络，大写 Internet 指全球公共互联网。</p><p>主机和路由器的职责不同。主机运行应用、产生和接收数据；路由器主要根据转发规则搬运数据。</p><p>协议和服务也不同。协议是通信双方遵守的规则；服务是某一层向上一层提供的能力。例如 TCP 协议通过编号、确认、重传等规则，实现可靠传输服务。</p><p>传输延迟和传播延迟要分开。<code>L / R</code> 算的是分组进入链路所需时间，不是信号从链路一端到另一端所需时间。</p><h2 id="9-复习清单"><a href="#9-复习清单" class="headerlink" title="9. 复习清单"></a>9. 复习清单</h2><p>学完本节后，应能回答：</p><ul><li>为什么 Internet 被称为“网络的网络”？</li><li>终端节点和中间节点分别是什么？</li><li>router 和 switch 在本章层面上共同承担什么角色？</li><li>protocol 规定了哪些内容？</li><li>socket interface 在应用和网络之间起什么作用？</li><li>access network 解决什么问题？</li><li>guided media 和 unguided media 有什么区别？</li><li>packet switching 和 circuit switching 的核心差异是什么？</li><li>store-and-forward 为什么会带来额外延迟？</li><li>为什么长度为 <code>L</code> bit 的分组经过速率为 <code>R</code> bps 的链路时，传输延迟是 <code>L / R</code>？</li></ul><hr><h2 id="最终总结：-Chapter-1-1-要建立的是网络总体地图：端系统位于边缘，接入网负责接入，网络核心负责转发，协议保证通信协同，分组交换让数据以-packet-为单位在核心中逐跳前进。"><a href="#最终总结：-Chapter-1-1-要建立的是网络总体地图：端系统位于边缘，接入网负责接入，网络核心负责转发，协议保证通信协同，分组交换让数据以-packet-为单位在核心中逐跳前进。" class="headerlink" title="最终总结： Chapter 1.1 要建立的是网络总体地图：端系统位于边缘，接入网负责接入，网络核心负责转发，协议保证通信协同，分组交换让数据以 packet 为单位在核心中逐跳前进。"></a><strong>最终总结：</strong> Chapter 1.1 要建立的是网络总体地图：端系统位于边缘，接入网负责接入，网络核心负责转发，协议保证通信协同，分组交换让数据以 packet 为单位在核心中逐跳前进。</h2>]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 计网 </tag>
            
            <tag> 大二课程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 5 讲：BTC 实现（二）：挖矿概率、安全性与攻击分析</title>
      <link href="/2026/06/02/05-BTC-%E5%AE%9E%E7%8E%B02/"/>
      <url>/2026/06/02/05-BTC-%E5%AE%9E%E7%8E%B02/</url>
      
        <content type="html"><![CDATA[<h1 id="第-5-讲：BTC-实现（二）：挖矿概率、安全性与攻击分析"><a href="#第-5-讲：BTC-实现（二）：挖矿概率、安全性与攻击分析" class="headerlink" title="第 5 讲：BTC 实现（二）：挖矿概率、安全性与攻击分析"></a>第 5 讲：BTC 实现（二）：挖矿概率、安全性与攻击分析</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>本节从概率角度解释比特币挖矿：每次尝试 <code>nonce</code> 都可以看作一次伯努利试验（Bernoulli trial）。</li><li>通过伯努利过程、泊松过程和指数分布，理解为什么比特币出块时间具有无记忆性（memoryless property）。</li><li>分析比特币总量为什么约为 2100 万，以及区块奖励减半机制如何产生稀缺性。</li><li>讨论比特币安全性：挖矿不能绝对保证安全，只能提供概率意义上的安全保证。</li><li>重点理解双花攻击（double spending attack）、确认数（confirmation）、零确认（zero confirmation）和自私挖矿（selfish mining）。</li></ol><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td><code>nonce</code></td><td>矿工不断修改的随机数</td><td>用于反复尝试找到满足难度要求的区块哈希</td></tr><tr><td>伯努利试验（Bernoulli trial）</td><td>只有成功 &#x2F; 失败两种结果的随机试验</td><td>每次尝试一个 <code>nonce</code> 可视为一次伯努利试验</td></tr><tr><td>伯努利过程（Bernoulli process）</td><td>一系列相互独立的伯努利试验</td><td>用来描述矿工连续尝试 <code>nonce</code> 的过程</td></tr><tr><td>泊松过程（Poisson process）</td><td>大量低概率独立事件的近似模型</td><td>用来近似整个系统产生新区块的过程</td></tr><tr><td>指数分布（exponential distribution）</td><td>描述下一次事件发生等待时间的分布</td><td>用来描述比特币系统的出块时间</td></tr><tr><td>无记忆性（memoryless property）</td><td>过去等待多久不影响未来还要等多久</td><td>解释为什么“已经挖了很久”不代表“快挖到了”</td></tr><tr><td>Progress free</td><td>挖矿进度不可累积</td><td>保证算力强的矿工只有按算力比例获得优势，而不是超比例优势</td></tr><tr><td>区块奖励（block reward）</td><td>矿工成功出块后获得的新比特币</td><td>是新比特币产生的主要途径</td></tr><tr><td>交易费（transaction fee）</td><td>用户为了让交易被打包支付给矿工的费用</td><td>当区块奖励下降后，继续激励矿工</td></tr><tr><td>双花攻击（double spending attack）</td><td>试图把已经花出去的币再花一遍</td><td>比特币安全分析中的核心攻击场景</td></tr><tr><td>确认数（confirmation）</td><td>某交易后面接上的区块数量</td><td>确认数越多，交易越难被回滚</td></tr><tr><td>零确认（zero confirmation）</td><td>交易已广播但尚未写入区块</td><td>速度快，但安全性弱</td></tr><tr><td>自私挖矿（selfish mining）</td><td>挖到区块后暂不发布，私下继续挖</td><td>可用于制造分叉或减少他人有效竞争</td></tr></tbody></table><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[BTC 挖矿] --&gt; B[概率模型]    A --&gt; C[经济激励]    A --&gt; D[安全性分析]    B --&gt; B1[Bernoulli trial]    B1 --&gt; B2[Bernoulli process]    B2 --&gt; B3[Poisson process 近似]    B3 --&gt; B4[指数分布]    B4 --&gt; B5[Memoryless &#x2F; Progress free]    C --&gt; C1[Block reward]    C1 --&gt; C2[每 21 万区块减半]    C2 --&gt; C3[总量约 2100 万]    C --&gt; C4[Transaction fee]    D --&gt; D1[恶意节点获得记账权]    D1 --&gt; D2[不能伪造签名偷币]    D1 --&gt; D3[可能尝试 double spending]    D3 --&gt; D4[等待 confirmations]    D3 --&gt; D5[Selfish mining]  </pre></div><p>这张图展示了本节的三条主线：第一条是<strong>挖矿的概率模型</strong>，用于解释出块时间和公平性；第二条是<strong>经济激励机制</strong>，解释矿工为什么愿意参与；第三条是<strong>安全性分析</strong>，说明比特币的不可篡改性不是绝对的，而是依靠算力竞争和确认数形成的概率保证。</p><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-挖矿为什么可以看作伯努利试验？"><a href="#4-1-挖矿为什么可以看作伯努利试验？" class="headerlink" title="4.1 挖矿为什么可以看作伯努利试验？"></a>4.1 挖矿为什么可以看作伯努利试验？</h3><p>比特币挖矿的本质是矿工不断尝试不同的 <code>nonce</code>，计算区块头哈希，看看结果是否小于目标阈值。</p><p>每一次尝试只有两种结果：</p><ol><li>成功：哈希值满足难度要求，矿工找到合法区块。</li><li>失败：哈希值不满足要求，矿工继续尝试下一个 <code>nonce</code>。</li></ol><p>这就符合伯努利试验（Bernoulli trial）的定义：<strong>一次随机实验只有两种结果</strong>。</p><p>可以类比抛硬币：</p><ul><li>抛硬币：正面 &#x2F; 反面。</li><li>挖矿：成功 &#x2F; 失败。</li></ul><p>但二者最大的不同是概率。抛普通硬币时，正反面概率接近各一半；而挖矿时，单次尝试成功的概率极低，绝大多数 <code>nonce</code> 都是失败的。</p><p>因此，挖矿不是“靠思考解数学题”，而是不断做大量随机尝试。谁能做更多尝试，谁就有更高概率先找到合法区块。</p><h3 id="4-2-伯努利过程与无记忆性"><a href="#4-2-伯努利过程与无记忆性" class="headerlink" title="4.2 伯努利过程与无记忆性"></a>4.2 伯努利过程与无记忆性</h3><p>如果矿工不断尝试 <code>nonce</code>，每次尝试都是独立的伯努利试验，那么这些试验连在一起，就构成伯努利过程（Bernoulli process）。</p><p>伯努利过程有一个重要性质：<strong>无记忆性（memoryless property）</strong>。</p><p>意思是：</p><blockquote><p>前面失败了多少次，不会影响下一次成功的概率。</p></blockquote><p>比如抛硬币时，前面连续 10 次都是反面，并不意味着第 11 次正面的概率会变大。每次抛硬币仍然按照原本概率独立发生。</p><p>挖矿也一样：</p><ul><li>你已经尝试了很多 <code>nonce</code>，不代表下一个 <code>nonce</code> 更容易成功。</li><li>你已经挖了 9 分钟，不代表第 10 分钟更容易出块。</li><li>你之前做过的失败尝试不会“积累进度”。</li></ul><p>这就是课程中提到的 <code>progress free</code>：<strong>挖矿没有可累积的进度条</strong>。</p><h3 id="4-3-为什么说挖矿是-progress-free？"><a href="#4-3-为什么说挖矿是-progress-free？" class="headerlink" title="4.3 为什么说挖矿是 progress free？"></a>4.3 为什么说挖矿是 progress free？</h3><p><code>progress free</code> 直译是“没有进度优势”。它的意思不是说挖矿没有意义，而是说：</p><blockquote><p>对单个矿工来说，已经尝试过的失败计算不会让后续尝试更容易成功。</p></blockquote><p>这听起来有点“无情”，因为过去做的工作似乎都白费了。但这个性质恰恰保证了挖矿公平性。</p><p>假设有两个矿工：</p><ul><li>矿工 A 的算力是矿工 B 的 10 倍。</li><li>那么 A 每秒能尝试的 <code>nonce</code> 数量大约是 B 的 10 倍。</li><li>理想情况下，A 找到新区块的概率也应该是 B 的 10 倍。</li></ul><p>这就是按算力比例分配机会。</p><p>如果挖矿不是 <code>progress free</code>，而是“失败得越多，下一次越容易成功”，那么算力强的矿工就会获得超比例优势。因为算力强者在同一时间里失败次数更多，于是后续成功概率也会更快变大。这样一来，A 的优势就不只是 10 倍，而可能超过 10 倍。</p><p>所以，<code>progress free</code> 的真正作用是：</p><blockquote><p>防止强矿工因为历史尝试次数更多而获得不成比例的额外优势。</p></blockquote><h3 id="4-4-泊松过程与指数分布：为什么平均出块时间是-10-分钟？"><a href="#4-4-泊松过程与指数分布：为什么平均出块时间是-10-分钟？" class="headerlink" title="4.4 泊松过程与指数分布：为什么平均出块时间是 10 分钟？"></a>4.4 泊松过程与指数分布：为什么平均出块时间是 10 分钟？</h3><p>比特币系统中，每次尝试 <code>nonce</code> 的成功概率极小，但整个网络每秒会进行海量尝试。</p><p>这种场景符合一个常见近似：</p><blockquote><p>大量独立试验 + 单次成功概率极低，可以用泊松过程（Poisson process）近似。</p></blockquote><p>在泊松过程下，等待下一次事件发生的时间服从指数分布（exponential distribution）。</p><p>对于比特币来说，这个“事件”就是：</p><blockquote><p>整个系统中有人成功挖出下一个合法区块。</p></blockquote><p>因此，比特币的出块时间可以理解为服从指数分布。</p><p>注意这里说的是<strong>整个系统的出块时间</strong>，不是某个具体矿工的出块时间。</p><p>比特币协议通过调整挖矿难度，让整个系统的平均出块时间维持在大约 10 分钟。也就是说，从全网角度看，平均每 10 分钟会产生一个新区块。</p><p>但具体到某个矿工，情况取决于他的算力占比。</p><p>例如：</p><ul><li>某矿工拥有全网 1% 的算力。</li><li>那么平均来说，全网每产生 100 个区块，其中约 1 个属于他。</li><li>全网平均 10 分钟一个区块。</li><li>所以这个矿工平均约每 1000 分钟才能挖到一个区块。</li></ul><p>这里要区分两个平均时间：</p><table><thead><tr><th>对象</th><th>平均出块时间</th></tr></thead><tbody><tr><td>整个比特币系统</td><td>约 10 分钟</td></tr><tr><td>单个矿工</td><td>取决于该矿工算力占比</td></tr></tbody></table><h3 id="4-5-指数分布的无记忆性"><a href="#4-5-指数分布的无记忆性" class="headerlink" title="4.5 指数分布的无记忆性"></a>4.5 指数分布的无记忆性</h3><p>指数分布也具有无记忆性。</p><p>这意味着：</p><blockquote><p>即使当前已经过去 10 分钟还没有出块，接下来平均仍然要再等 10 分钟。</p></blockquote><p>这点很容易违反直觉。很多人会觉得：“平均 10 分钟出块，现在已经等了 10 分钟，那应该快出了吧？”</p><p>但概率模型告诉我们，不是这样。</p><p>因为挖矿没有进度积累，系统并不会因为已经很久没出块，就自动提高下一秒出块的概率。每一刻的状态仍然由当前全网算力和当前难度决定。</p><p>所以：</p><ul><li>已经等了 1 分钟，未来平均还要等 10 分钟。</li><li>已经等了 10 分钟，未来平均还要等 10 分钟。</li><li>已经等了 30 分钟，未来平均仍然还要等 10 分钟。</li></ul><p>当然，这里的“平均”不是说每次都一定再等 10 分钟，而是指条件分布的期望仍然不变。</p><h3 id="4-6-比特币总量为什么约为-2100-万？"><a href="#4-6-比特币总量为什么约为-2100-万？" class="headerlink" title="4.6 比特币总量为什么约为 2100 万？"></a>4.6 比特币总量为什么约为 2100 万？</h3><p>比特币中新币的产生主要来自区块奖励（block reward）。</p><p>课程中强调：</p><blockquote><p>区块奖励是系统中产生新比特币的途径。</p></blockquote><p>比特币的区块奖励不是一直不变的，而是每隔约 21 万个区块减半一次。由于平均 10 分钟出一个区块，21 万个区块大约对应 4 年。</p><p>最初：</p><ul><li>前 21 万个区块：每个区块奖励 50 BTC。</li><li>接下来 21 万个区块：每个区块奖励 25 BTC。</li><li>再接下来：12.5 BTC。</li><li>之后继续减半。</li></ul><p>于是总量可以写成一个几何级数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">总量 = 210000 × 50 × (1 + 1/2 + 1/4 + 1/8 + ...)</span><br></pre></td></tr></table></figure><p>括号中的几何级数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1 + 1/2 + 1/4 + 1/8 + ... = 2</span><br></pre></td></tr></table></figure><p>所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">总量 = 210000 × 50 × 2</span><br><span class="line">     = 210000 × 100</span><br><span class="line">     = 21000000</span><br></pre></td></tr></table></figure><p>因此，比特币总量约为 2100 万枚。</p><p>这里要注意一个常见误解：比特币越来越难“获得”，不是因为它在解决某个越来越稀少的数学对象，比如寻找某种特殊质数；而是因为协议规定的区块奖励不断减半，新增比特币越来越少。</p><h3 id="4-7-挖矿本身没有实际计算意义，但有安全意义"><a href="#4-7-挖矿本身没有实际计算意义，但有安全意义" class="headerlink" title="4.7 挖矿本身没有实际计算意义，但有安全意义"></a>4.7 挖矿本身没有实际计算意义，但有安全意义</h3><p>课程中特别区分了两件事：</p><ol><li>挖矿计算本身没有实际业务意义。</li><li>挖矿机制对维护系统安全非常重要。</li></ol><p>也就是说，矿工反复计算哈希，并不是在解决科学问题，也不是在完成某种有实际用途的数学任务。它的直接作用是竞争记账权。</p><p>但是在一个去中心化系统里，没有中心机构决定“谁有资格记账”。比特币需要一种开放的、抗女巫攻击的记账权分配方式。</p><p>Proof of Work 的作用就是：</p><blockquote><p>用算力作为竞争记账权的成本。</p></blockquote><p>只要大部分算力掌握在诚实节点手中，恶意节点想篡改账本就必须投入大量算力与全网竞争。这样，挖矿虽然表面上像是在做“无用功”，但它实际上是在给区块链安全性提供成本屏障。</p><h3 id="4-8-区块奖励下降后，矿工还有动力吗？"><a href="#4-8-区块奖励下降后，矿工还有动力吗？" class="headerlink" title="4.8 区块奖励下降后，矿工还有动力吗？"></a>4.8 区块奖励下降后，矿工还有动力吗？</h3><p>随着区块奖励不断减半，矿工获得的新比特币数量会越来越少。直觉上可能会担心：</p><blockquote><p>如果最后区块奖励趋近于 0，矿工是不是就没有动力挖矿了？</p></blockquote><p>课程中给出的答案是：不一定，因为还有交易费（transaction fee）。</p><p>矿工的收入可以理解为两部分：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">矿工收入 = 区块奖励 + 交易费</span><br></pre></td></tr></table></figure><p>早期，区块奖励是主要激励。随着区块奖励减少，交易费会变得越来越重要。</p><p>所以，比特币长期安全性的一个关键问题是：</p><blockquote><p>当新币发行奖励逐渐降低后，交易费能否继续支撑足够多矿工参与维护系统安全？</p></blockquote><p>本节没有展开这个长期问题，只需要先理解：比特币激励矿工的机制并不只有区块奖励，还有交易费。</p><h3 id="4-9-恶意矿工获得记账权后，能不能偷币？"><a href="#4-9-恶意矿工获得记账权后，能不能偷币？" class="headerlink" title="4.9 恶意矿工获得记账权后，能不能偷币？"></a>4.9 恶意矿工获得记账权后，能不能偷币？</h3><p>假设恶意矿工获得了某个区块的记账权，他能不能直接把别人的钱转给自己？</p><p>答案是：不能。</p><p>原因是比特币交易需要数字签名（digital signature）。</p><p>例如，恶意节点 M 想把 A 的币转给自己，需要构造一笔交易：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A -&gt; M</span><br></pre></td></tr></table></figure><p>但这笔交易必须有 A 的私钥签名才有效。M 虽然获得了记账权，但他不知道 A 的私钥，所以无法伪造 A 的签名。</p><p>如果 M 强行把这笔非法交易写入区块，会发生什么？</p><p>诚实节点会验证区块中的交易。一旦发现交易签名无效，就不会接受这个区块。比特币节点遵循的是扩展最长合法链，而不是扩展任意最长链。</p><p>因此：</p><blockquote><p>不合法的链再长，也不能被诚实节点接受。</p></blockquote><p>这说明，挖矿获得的是记账权，不是随意改账的权力。</p><h3 id="4-10-恶意矿工能不能双花？"><a href="#4-10-恶意矿工能不能双花？" class="headerlink" title="4.10 恶意矿工能不能双花？"></a>4.10 恶意矿工能不能双花？</h3><p>虽然恶意矿工不能伪造别人签名偷币，但他可以尝试另一类攻击：双花攻击（double spending attack）。</p><p>双花的核心目标是：</p><blockquote><p>把同一笔币花出去后，再通过分叉让这笔付款所在的区块作废，从而把钱“拿回来”。</p></blockquote><p>典型场景：</p><ol><li>M 向商家 A 发起一笔付款交易。</li><li>这笔交易被写入区块链。</li><li>商家看到交易上链，以为付款完成，于是发货。</li><li>M 在较早位置制造另一条分叉链，把同一笔钱转回给自己。</li><li>如果 M 的分叉链最终变成最长合法链，那么原来付款给商家的交易就被回滚。</li><li>M 既拿到了商品，又收回了钱。</li></ol><p>这里的关键是：恶意交易不能直接接在已有付款交易后面。因为如果同一笔钱已经在上一笔交易中花出，再在后续区块里花一次，诚实节点会直接识别为 double spending 并拒绝。</p><p>所以攻击者必须从付款交易之前的位置开始分叉，构造另一条合法链。</p><h3 id="4-11-为什么多等几个-confirmation-可以防范双花？"><a href="#4-11-为什么多等几个-confirmation-可以防范双花？" class="headerlink" title="4.11 为什么多等几个 confirmation 可以防范双花？"></a>4.11 为什么多等几个 confirmation 可以防范双花？</h3><p>确认数（confirmation）表示某笔交易被写入区块后，后面又接上了多少个区块。</p><p>可以简单理解为：</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td>0 confirmation</td><td>交易已广播，但还没被写入区块</td></tr><tr><td>1 confirmation</td><td>交易已经被写入一个区块</td></tr><tr><td>2 confirmations</td><td>交易所在区块后面又接了 1 个新区块</td></tr><tr><td>6 confirmations</td><td>交易所在区块后面已有多个区块，回滚难度较高</td></tr></tbody></table><p>比特币中常见经验是等待 6 个 confirmation。因为平均 10 分钟一个区块，6 个确认大约需要 1 小时。</p><p>为什么确认数越多，双花越难？</p><p>因为攻击者要回滚某笔交易，就必须从交易之前的位置开始挖一条分叉链，并让这条分叉链赶上甚至超过诚实链。</p><p>如果交易刚刚上链，攻击者只需要赶上很短的链。</p><p>如果交易后面已经有多个确认，攻击者就必须连续挖出多个区块，才能让自己的分叉链超过诚实链。</p><p>只要大多数算力掌握在诚实节点手中，攻击者连续追上的概率会随着确认数增加而快速下降。</p><p>所以，“不可篡改”不是绝对不能改，而是：</p><blockquote><p>随着确认数增加，被成功篡改的概率快速下降。</p></blockquote><h3 id="4-12-区块链不可篡改是概率意义上的"><a href="#4-12-区块链不可篡改是概率意义上的" class="headerlink" title="4.12 区块链不可篡改是概率意义上的"></a>4.12 区块链不可篡改是概率意义上的</h3><p>很多人会说区块链是不可篡改账本（irreversible ledger）。这句话要谨慎理解。</p><p>严格来说，比特币的不可篡改性不是物理上、逻辑上绝对不可改变。刚刚写入区块链的交易仍然有可能因为分叉而被回滚。</p><p>比特币真正提供的是：</p><blockquote><p>基于算力竞争的概率安全性。</p></blockquote><p>确认数越多，攻击者需要重组的区块越多，成功概率越低。因此，交易不是在某一瞬间从“可篡改”变成“绝对不可篡改”，而是随着后续区块增加，逐渐变得越来越难以篡改。</p><p>可以把它理解成安全性的连续增强：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">0 confirmation：风险最高</span><br><span class="line">1 confirmation：已有初步保证</span><br><span class="line">3 confirmations：更难回滚</span><br><span class="line">6 confirmations：通常认为足够安全</span><br><span class="line">更多 confirmations：安全性继续增强</span><br></pre></td></tr></table></figure><h3 id="4-13-什么是-zero-confirmation？"><a href="#4-13-什么是-zero-confirmation？" class="headerlink" title="4.13 什么是 zero confirmation？"></a>4.13 什么是 zero confirmation？</h3><p>零确认（zero confirmation）指的是：</p><blockquote><p>交易已经广播到网络中，但还没有被写入任何区块。</p></blockquote><p>例如，用户向商家付款后，交易被广播出去，商家节点监听到了这笔交易，并验证其签名合法、输入未花费，但此时还没有新区块包含这笔交易。</p><p>这时商家如果直接接受付款，就属于接受 zero confirmation 交易。</p><p>这显然有风险，因为交易还没进入区块链，更容易被替换或冲突交易影响。</p><p>但课程提到，zero confirmation 在一些实际场景中仍然可能被使用，原因包括：</p><ol><li>节点通常倾向于接受自己最先听到的合法交易。</li><li>很多商业场景本身有处理延迟，比如电商不会立刻发货。</li><li>如果后续发现交易没有进入最长合法链，商家仍可取消发货。</li></ol><p>因此，zero confirmation 不是“没有风险”，而是某些低风险或可撤销场景下，为了提升用户体验而接受的一种权衡。</p><h3 id="4-14-恶意矿工可以故意不打包某些交易吗？"><a href="#4-14-恶意矿工可以故意不打包某些交易吗？" class="headerlink" title="4.14 恶意矿工可以故意不打包某些交易吗？"></a>4.14 恶意矿工可以故意不打包某些交易吗？</h3><p>可以。</p><p>比特币协议并没有强制规定矿工必须把哪些交易写入区块。矿工获得记账权后，可以选择：</p><ul><li>打包某些交易。</li><li>不打包某些交易。</li><li>优先打包交易费高的交易。</li></ul><p>所以，恶意矿工确实可以故意不把某些合法交易写入自己发布的区块。</p><p>但这通常不会造成严重后果，因为：</p><ol><li>其他诚实矿工仍然可以在后续区块中打包这些交易。</li><li>区块容量有限，正常情况下也可能出现合法交易暂时未被打包的情况。</li><li>除非恶意矿工长期控制大量算力，否则很难永久阻止某笔合法交易上链。</li></ol><p>这说明，单个恶意矿工可以延迟交易，但很难在诚实算力占多数时长期审查交易。</p><h3 id="4-15-什么是-selfish-mining？"><a href="#4-15-什么是-selfish-mining？" class="headerlink" title="4.15 什么是 selfish mining？"></a>4.15 什么是 selfish mining？</h3><p>自私挖矿（selfish mining）指的是：</p><blockquote><p>矿工挖到新区块后，不立即广播，而是先藏起来，继续在自己的私有链上挖。</p></blockquote><p>正常挖矿逻辑是：矿工一旦挖到新区块，应该马上发布。原因很简单：如果不发布，别人可能也挖出同一高度的区块，导致自己的区块失去优势。</p><p>但 selfish mining 故意反其道而行之。</p><p>攻击者的想法是：</p><ol><li>自己挖到一个区块后先不公布。</li><li>其他矿工还以为旧区块是链尾，继续在旧区块后面挖。</li><li>攻击者私下继续挖，试图形成一条隐藏的私有链。</li><li>等公开链快追上或某个时机成熟时，攻击者一次性公布私有链。</li><li>如果私有链更长，网络会切换到攻击者的链，其他矿工之前挖出的区块作废。</li></ol><p>selfish mining 的目的可能有两个：</p><ol><li>配合分叉攻击，回滚某些交易。</li><li>在正常挖矿中减少别人有效竞争，让其他矿工做无用功。</li></ol><p>但这种策略有风险。攻击者如果算力不够强，私有链可能被公开链追上甚至超过。那他之前藏起来的区块就可能作废，损失区块奖励。</p><p>因此，selfish mining 不是普通小矿工随便就能稳定成功的策略，它依赖较强算力和合适的网络传播条件。</p><h3 id="4-16-为什么不能提前把未来很多区块都算好？"><a href="#4-16-为什么不能提前把未来很多区块都算好？" class="headerlink" title="4.16 为什么不能提前把未来很多区块都算好？"></a>4.16 为什么不能提前把未来很多区块都算好？</h3><p>课堂中有同学提出一个问题：攻击者能不能提前准备很多未来区块，等需要攻击时一次性发布？</p><p>答案是：不能简单地“跳着算未来区块”。</p><p>因为每个区块头里都要包含前一个区块的哈希值。也就是说，区块之间是通过哈希指针串起来的。</p><p>如果你要挖第 1001 个区块，必须知道第 1000 个区块的哈希。</p><p>如果你要挖第 1002 个区块，必须先知道第 1001 个区块的哈希。</p><p>所以，攻击者不能在不知道前一区块哈希的情况下，直接提前计算后面很多区块。</p><p>他可以做的是：</p><ul><li>从某个已知区块开始，私下构造一条分叉链；</li><li>挖出一个区块后，再基于这个区块继续挖下一个；</li><li>这就是 selfish mining 或分叉攻击中的“私下挖链”。</li></ul><p>但他不能凭空提前计算未来还不存在的区块链位置。</p><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-挖矿概率模型"><a href="#5-1-挖矿概率模型" class="headerlink" title="5.1 挖矿概率模型"></a>5.1 挖矿概率模型</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[尝试一个 nonce] --&gt; B{Hash 是否小于目标值?}    B -- 否 --&gt; C[失败]    C --&gt; A    B -- 是 --&gt; D[找到合法区块]    D --&gt; E[广播区块]    E --&gt; F[获得记账权和奖励]    A --&gt; G[Bernoulli trial]    G --&gt; H[Bernoulli process]    H --&gt; I[Poisson process 近似]    I --&gt; J[出块时间服从指数分布]    J --&gt; K[Memoryless]  </pre></div><p>这张图说明：挖矿不是一步步接近答案，而是不断重复独立随机尝试。每次尝试都是一次伯努利试验，大量尝试形成伯努利过程，从全网角度可以用泊松过程近似，进而得到出块时间服从指数分布。最重要的结论是：出块等待时间具有无记忆性。</p><h3 id="5-2-双花攻击与确认数"><a href="#5-2-双花攻击与确认数" class="headerlink" title="5.2 双花攻击与确认数"></a>5.2 双花攻击与确认数</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[攻击者向商家付款] --&gt; B[付款交易进入区块]    B --&gt; C[商家看到交易上链]    C --&gt; D[商家发货或提供服务]    B --&gt; E[攻击者从付款前分叉]    E --&gt; F[构造另一笔交易: 转给自己]    F --&gt; G[私下挖分叉链]    G --&gt; H{分叉链是否超过诚实链?}    H -- 是 --&gt; I[付款交易被回滚]    I --&gt; J[Double spending 成功]    H -- 否 --&gt; K[攻击失败]    B --&gt; L[等待更多 confirmations]    L --&gt; M[攻击者追赶难度增加]  </pre></div><p>这张图展示了 double spending 的核心逻辑：攻击者不是直接在付款交易后面再次花费同一笔币，而是从付款交易之前的位置制造分叉。确认数越多，诚实链越长，攻击者需要追赶的距离越大，攻击成功概率越低。</p><h3 id="5-3-Selfish-mining-的基本思路"><a href="#5-3-Selfish-mining-的基本思路" class="headerlink" title="5.3 Selfish mining 的基本思路"></a>5.3 Selfish mining 的基本思路</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[矿工挖到新区块] --&gt; B{是否立即广播?}    B -- 正常挖矿 --&gt; C[广播新区块]    C --&gt; D[全网基于新区块继续挖]    B -- Selfish mining --&gt; E[暂时隐藏区块]    E --&gt; F[攻击者私下继续挖]    F --&gt; G[其他矿工仍在旧链上挖]    G --&gt; H{公开链是否快追上?}    H -- 是 --&gt; I[攻击者公布私有链]    H -- 否 --&gt; F    I --&gt; J{私有链更长?}    J -- 是 --&gt; K[公开链部分区块作废]    J -- 否 --&gt; L[攻击者隐藏区块可能作废]  </pre></div><p>这张图说明 selfish mining 的关键不是“算得更快就一定赢”，而是通过隐藏区块改变其他矿工的信息状态，让其他矿工在旧链上浪费算力。但如果攻击者算力不足，隐藏策略反而可能导致自己损失奖励。</p><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>挖矿是不是在解决有实际意义的数学难题？</td><td>不是。挖矿主要是反复尝试 <code>nonce</code>，找到满足难度要求的哈希。它本身没有实际计算意义，但对系统安全有意义。</td></tr><tr><td>比特币越来越难挖，是不是因为“剩下的数学答案越来越少”？</td><td>不是。难度来自协议调节和区块奖励减半机制，不是因为某类数学对象越来越稀缺。</td></tr><tr><td>已经挖了 10 分钟还没出块，是不是马上就要出了？</td><td>不是。出块时间近似服从指数分布，具有无记忆性，未来平均仍然还要等约 10 分钟。</td></tr><tr><td>Progress free 是不是说明矿工之前的工作都没意义？</td><td>对单次成功概率来说，过去失败尝试不能积累进度；但整体上，算力投入越多，单位时间尝试次数越多，成功概率越高。</td></tr><tr><td>区块链不可篡改是不是绝对不能改？</td><td>不是绝对不能改，而是确认数越多，篡改成本越高、成功概率越低。</td></tr><tr><td>最长链是不是只看长度？</td><td>不是。诚实节点扩展的是最长合法链，包含非法交易的链再长也不会被接受。</td></tr><tr><td>恶意矿工获得记账权后能不能偷别人币？</td><td>不能。转走别人的币需要对方私钥签名，恶意矿工无法伪造签名。</td></tr><tr><td>恶意矿工能不能故意不打包某些交易？</td><td>可以，但通常只能延迟交易，除非长期掌握大量算力，否则很难永久阻止合法交易上链。</td></tr><tr><td>Zero confirmation 是不是完全不能用？</td><td>不是。它风险较高，但在低金额、可撤销、存在业务延迟的场景下可能被接受。</td></tr><tr><td>Selfish mining 是不是可以提前把未来区块都算好？</td><td>不能跳着提前计算未来区块，因为每个区块都依赖前一个区块的哈希。攻击者只能从某个已知区块开始私下连续挖。</td></tr></tbody></table><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-为什么指数分布会出现“越等不越近”的现象？"><a href="#7-1-为什么指数分布会出现“越等不越近”的现象？" class="headerlink" title="7.1 为什么指数分布会出现“越等不越近”的现象？"></a>7.1 为什么指数分布会出现“越等不越近”的现象？</h3><p>日常生活中很多任务是有进度条的。比如下载文件，已经下载 90%，说明快完成了。</p><p>但挖矿不是这种任务。挖矿更像不停买彩票：</p><ul><li>每张彩票中奖概率相同。</li><li>你之前买了很多张没中，不代表下一张中奖概率更高。</li><li>当然，你买得越多，总中奖概率越高；但每一张彩票本身不会因为之前没中而变得更容易中。</li></ul><p>所以，“已经挖了很久”只是说明过去没有成功，并不说明未来更接近成功。</p><h3 id="7-2-“概率安全性”怎么理解？"><a href="#7-2-“概率安全性”怎么理解？" class="headerlink" title="7.2 “概率安全性”怎么理解？"></a>7.2 “概率安全性”怎么理解？</h3><p>比特币很多安全结论都不是绝对的，而是概率性的。</p><p>例如：</p><ul><li>恶意节点算力越低，攻击越难。</li><li>确认数越多，攻击越难。</li><li>诚实节点越多，最长合法链越稳定。</li></ul><p>这和传统中心化系统不同。中心化系统通常依赖一个明确的权威服务器判断交易最终状态。比特币则通过全网算力竞争，让某条链逐渐成为更可信的历史。</p><p>因此，比特币的安全性来自：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">密码学签名 + 哈希指针 + Proof of Work + 最长合法链规则 + 经济激励</span><br></pre></td></tr></table></figure><p>本节重点讨论的是后面三个：Proof of Work、最长合法链规则和经济激励。</p><h3 id="7-3-为什么区块奖励和交易费都是安全机制的一部分？"><a href="#7-3-为什么区块奖励和交易费都是安全机制的一部分？" class="headerlink" title="7.3 为什么区块奖励和交易费都是安全机制的一部分？"></a>7.3 为什么区块奖励和交易费都是安全机制的一部分？</h3><p>矿工不是免费维护系统的。矿工投入硬件、电力和时间，是因为能获得收益。</p><p>这些收益来自：</p><ol><li>区块奖励。</li><li>交易费。</li></ol><p>如果奖励不足，矿工减少，系统总算力下降，攻击者发动攻击的成本也会下降。</p><p>所以，区块奖励和交易费不只是经济问题，也是安全问题。比特币通过经济激励吸引矿工参与，再通过矿工算力维护账本安全。</p><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>比特币挖矿可以看作不断尝试 <code>nonce</code> 的伯努利过程，每次尝试只有成功或失败两种结果。</li><li>全网出块过程可以用泊松过程近似，出块等待时间服从指数分布，具有无记忆性。</li><li><code>Progress free</code> 保证了挖矿机会基本按算力比例分配，避免强矿工获得超比例优势。</li><li>比特币总量约 2100 万，来自区块奖励每 21 万个区块减半形成的几何级数。</li><li>挖矿本身没有实际计算意义，但通过 Proof of Work 为去中心化系统提供记账权竞争机制和安全成本。</li><li>比特币的不可篡改性是概率意义上的，确认数越多，交易被回滚的概率越低。</li><li>恶意矿工不能伪造签名偷币，但可以尝试 double spending、交易审查或 selfish mining。</li><li>Selfish mining 的核心是隐藏已挖出的区块，试图让其他矿工浪费算力，但这种策略依赖较强算力，也有失败风险。</li></ol><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-为什么每次尝试-nonce-可以看作一次-Bernoulli-trial？"><a href="#1-为什么每次尝试-nonce-可以看作一次-Bernoulli-trial？" class="headerlink" title="1. 为什么每次尝试 nonce 可以看作一次 Bernoulli trial？"></a>1. 为什么每次尝试 <code>nonce</code> 可以看作一次 Bernoulli trial？</h3><p>参考答案：因为每次尝试只有两种结果：哈希满足目标要求，成功；哈希不满足目标要求，失败。这符合伯努利试验“二元结果”的定义。</p><h3 id="2-什么是挖矿中的-memoryless-property？"><a href="#2-什么是挖矿中的-memoryless-property？" class="headerlink" title="2. 什么是挖矿中的 memoryless property？"></a>2. 什么是挖矿中的 memoryless property？</h3><p>参考答案：过去尝试了多少次、已经挖了多久，不会影响下一次尝试成功的概率。即使已经等了很久没出块，未来平均等待时间仍然不变。</p><h3 id="3-为什么-progress-free-反而有助于挖矿公平？"><a href="#3-为什么-progress-free-反而有助于挖矿公平？" class="headerlink" title="3. 为什么 progress free 反而有助于挖矿公平？"></a>3. 为什么 progress free 反而有助于挖矿公平？</h3><p>参考答案：因为它保证过去失败尝试不能积累成额外优势。算力强的矿工优势只来自单位时间尝试次数更多，而不是因为历史尝试越多导致后续成功概率更高。</p><h3 id="4-比特币总量约-2100-万是怎么来的？"><a href="#4-比特币总量约-2100-万是怎么来的？" class="headerlink" title="4. 比特币总量约 2100 万是怎么来的？"></a>4. 比特币总量约 2100 万是怎么来的？</h3><p>参考答案：区块奖励每 21 万个区块减半。总量为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">210000 × 50 × (1 + 1/2 + 1/4 + ...) = 210000 × 50 × 2 = 21000000</span><br></pre></td></tr></table></figure><h3 id="5-恶意矿工获得记账权后，为什么不能直接偷别人的币？"><a href="#5-恶意矿工获得记账权后，为什么不能直接偷别人的币？" class="headerlink" title="5. 恶意矿工获得记账权后，为什么不能直接偷别人的币？"></a>5. 恶意矿工获得记账权后，为什么不能直接偷别人的币？</h3><p>参考答案：因为交易需要币所有者的私钥签名。恶意矿工不知道别人的私钥，无法伪造合法签名。包含非法签名交易的区块不会被诚实节点接受。</p><h3 id="6-Double-spending-attack-为什么要通过分叉实现？"><a href="#6-Double-spending-attack-为什么要通过分叉实现？" class="headerlink" title="6. Double spending attack 为什么要通过分叉实现？"></a>6. Double spending attack 为什么要通过分叉实现？</h3><p>参考答案：如果攻击者直接在付款交易后面再次花费同一笔币，诚实节点会识别为双花并拒绝。攻击者必须从付款交易之前的位置构造另一条合法分叉链，让原付款交易所在链被回滚。</p><h3 id="7-为什么等待更多-confirmations-可以提高安全性？"><a href="#7-为什么等待更多-confirmations-可以提高安全性？" class="headerlink" title="7. 为什么等待更多 confirmations 可以提高安全性？"></a>7. 为什么等待更多 confirmations 可以提高安全性？</h3><p>参考答案：确认数越多，付款交易后面接上的诚实区块越多。攻击者要回滚该交易，就必须从更早位置开始追赶并超过诚实链，成功概率随确认数增加而快速降低。</p><h3 id="8-Selfish-mining-和正常挖矿的区别是什么？"><a href="#8-Selfish-mining-和正常挖矿的区别是什么？" class="headerlink" title="8. Selfish mining 和正常挖矿的区别是什么？"></a>8. Selfish mining 和正常挖矿的区别是什么？</h3><p>参考答案：正常挖矿是挖到区块后立即广播；selfish mining 是挖到区块后先隐藏，私下继续挖，试图在合适时机公布更长私有链，让其他矿工的区块作废。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>05-BTC-实现1</title>
      <link href="/2026/06/02/05-BTC-%E5%AE%9E%E7%8E%B01/"/>
      <url>/2026/06/02/05-BTC-%E5%AE%9E%E7%8E%B01/</url>
      
        <content type="html"><![CDATA[<h1 id="第-5-讲：BTC-实现（一）"><a href="#第-5-讲：BTC-实现（一）" class="headerlink" title="第 5 讲：BTC 实现（一）"></a>第 5 讲：BTC 实现（一）</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>本节从“比特币如何具体实现去中心化账本”出发，重点讲 <strong>transaction based ledger</strong>。</li><li>比特币不直接记录“账户余额”，而是通过历史交易和 <strong>UTXO（Unspent Transaction Output）</strong> 推算余额。</li><li>交易合法性检查的核心问题是：你要花的币是否真的存在、是否已经被花过。</li><li>矿工的激励不仅来自 <strong>block reward</strong>，还来自 <strong>transaction fee</strong>。</li><li>挖矿并不是对整个区块所有交易直接求 hash，而是对 <strong>block header</strong> 求 hash，其中包含 <strong>Merkle root</strong> 来承诺交易集合。</li></ol><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>Transaction based ledger</td><td>基于交易记录的账本模式</td><td>比特币采用的账本模型，不直接记录账户余额</td></tr><tr><td>Account based ledger</td><td>基于账户余额的账本模式</td><td>与比特币形成对比，以太坊更接近这种模式</td></tr><tr><td>UTXO</td><td>尚未被花费的交易输出集合</td><td>用于推算余额、检测 double spending</td></tr><tr><td>Transaction input</td><td>当前交易要花掉的旧输出</td><td>必须引用之前某个交易的某个 output</td></tr><tr><td>Transaction output</td><td>当前交易产生的新收款结果</td><td>如果未被花费，就会进入 UTXO 集合</td></tr><tr><td>Double spending</td><td>同一笔币被重复花费</td><td>比特币系统必须防止的核心问题</td></tr><tr><td>Transaction fee</td><td>输入总额减去输出总额的差值</td><td>激励矿工把普通交易打包进区块</td></tr><tr><td>Coinbase transaction</td><td>区块中的特殊铸币交易</td><td>产生 block reward，也可携带 extra nonce</td></tr><tr><td>Block reward</td><td>矿工成功出块获得的新币奖励</td><td>早期是矿工挖矿的主要激励</td></tr><tr><td>Block header</td><td>区块头</td><td>挖矿求 hash 的对象，不包含完整交易列表</td></tr><tr><td>Merkle root</td><td>Merkle tree 的根 hash</td><td>在 block header 中承诺整个交易集合</td></tr><tr><td>Nonce</td><td>挖矿时反复调整的随机数</td><td>用于寻找满足 difficulty target 的 block header hash</td></tr><tr><td>Extra nonce</td><td>通常放在 coinbase transaction 中的额外搜索空间</td><td>当 header nonce 空间不够时扩大挖矿搜索范围</td></tr><tr><td>Difficulty target</td><td>挖矿目标阈值</td><td>要求 block header hash 小于等于该目标</td></tr></tbody></table><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[&quot;BTC 实现&quot;] --&gt; B[&quot;Transaction based ledger&quot;]    B --&gt; C[&quot;不直接记录账户余额&quot;]    B --&gt; D[&quot;通过 UTXO 推算余额&quot;]    D --&gt; E[&quot;检测 double spending&quot;]    D --&gt; F[&quot;交易输入引用旧 output&quot;]    D --&gt; G[&quot;交易输出产生新 UTXO&quot;]    A --&gt; H[&quot;矿工激励&quot;]    H --&gt; I[&quot;Block reward&quot;]    H --&gt; J[&quot;Transaction fee&quot;]    A --&gt; K[&quot;区块与挖矿&quot;]    K --&gt; L[&quot;Block header&quot;]    K --&gt; M[&quot;Merkle root&quot;]    K --&gt; N[&quot;Nonce&quot;]    N --&gt; O[&quot;Extra nonce&quot;]  </pre></div><p>这张图可以把本节内容分成三条主线：第一，BTC 的账本模型是基于交易的；第二，UTXO 是检查交易合法性的核心数据结构；第三，矿工通过调整 nonce 和 extra nonce 进行 proof of work，并通过 block reward 与 transaction fee 获得激励。</p><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-比特币采用-transaction-based-ledger"><a href="#4-1-比特币采用-transaction-based-ledger" class="headerlink" title="4.1 比特币采用 transaction based ledger"></a>4.1 比特币采用 transaction based ledger</h3><p>比特币是一个去中心化账本，但它不是像银行账户那样直接记录：</p><blockquote><p>A 账户余额 &#x3D; 多少 BTC<br>B 账户余额 &#x3D; 多少 BTC</p></blockquote><p>比特币采用的是 <strong>transaction based ledger</strong>，也就是账本中记录的是一笔笔交易。</p><p>如果想知道某个地址现在有多少 BTC，不能直接查一个“余额字段”，而是要根据历史交易推算：</p><ol><li>曾经有哪些交易把 BTC 转给这个地址；</li><li>这些交易产生的 output 中，有哪些还没有被花掉；</li><li>把这些未花费 output 的金额加起来，得到当前可用余额。</li></ol><p>所以，比特币中的“余额”不是系统显式保存的结果，而是由 <strong>UTXO 集合</strong> 推导出来的。</p><h3 id="4-2-UTXO：未花费交易输出集合"><a href="#4-2-UTXO：未花费交易输出集合" class="headerlink" title="4.2 UTXO：未花费交易输出集合"></a>4.2 UTXO：未花费交易输出集合</h3><p><strong>UTXO（Unspent Transaction Output）</strong> 指的是区块链上所有“还没有被花出去的交易输出”。</p><p>一个交易可以有多个 output，其中有些可能已经被花掉，有些可能还没被花掉。</p><p>例如：</p><ul><li>某笔交易给 B 转了 5 BTC；</li><li>同一笔交易给 C 转了 3 BTC；</li><li>后来 B 把 5 BTC 花掉了；</li><li>C 还没有花掉 3 BTC。</li></ul><p>那么：</p><ul><li>给 B 的那个 5 BTC output 不再属于 UTXO；</li><li>给 C 的那个 3 BTC output 仍然属于 UTXO。</li></ul><p>UTXO 集合中的每个元素通常需要能够定位到一个具体 output。定位方式是：</p><ol><li>产生该 output 的交易 hash；</li><li>该 output 在该交易中的编号。</li></ol><p>也就是说，一个 UTXO 不是只写“某人有多少钱”，而是写：</p><blockquote><p>来自哪一笔 transaction，以及这笔 transaction 的第几个 output。</p></blockquote><h3 id="4-3-为什么全节点要维护-UTXO"><a href="#4-3-为什么全节点要维护-UTXO" class="headerlink" title="4.3 为什么全节点要维护 UTXO"></a>4.3 为什么全节点要维护 UTXO</h3><p>全节点维护 UTXO 的核心目的，是为了快速判断新交易是否合法，尤其是检测 <strong>double spending</strong>。</p><p>当一个新交易发布时，它会声明自己要花掉之前某些交易的 output。全节点需要检查：</p><ol><li>被引用的 output 是否存在；</li><li>被引用的 output 是否还在 UTXO 集合中；</li><li>如果不在 UTXO 中，说明它要么不存在，要么已经被花过；</li><li>只有在 UTXO 中的 output，才能作为合法 input 被花费。</li></ol><p>因此，UTXO 是比特币系统中验证交易合法性的关键数据结构。</p><p>它解决的问题不是“怎么显示余额”，而是：</p><blockquote><p>当前这笔交易声称要花的钱，是否真的还没被别人花过？</p></blockquote><h3 id="4-4-交易会消耗旧-UTXO，也会产生新-UTXO"><a href="#4-4-交易会消耗旧-UTXO，也会产生新-UTXO" class="headerlink" title="4.4 交易会消耗旧 UTXO，也会产生新 UTXO"></a>4.4 交易会消耗旧 UTXO，也会产生新 UTXO</h3><p>每一笔普通交易都可以理解为一个状态转换过程：</p><ol><li>消耗若干旧的 UTXO；</li><li>产生若干新的 transaction output；</li><li>新 output 如果暂时没有被花掉，就加入 UTXO 集合。</li></ol><p>例如：</p><ul><li>B 原本有一个 5 BTC 的 UTXO；</li><li>B 把这 5 BTC 转给 D；</li><li>那么 B 原来的 5 BTC UTXO 被移除；</li><li>同时交易产生一个给 D 的新 output；</li><li>如果 D 暂时不花，这个 output 就进入 UTXO 集合。</li></ul><p>如果某个人收到 BTC 后永远不花，这个 output 就会长期保留在 UTXO 集合中。可能原因包括：</p><ol><li>持有人主动长期持有；</li><li>私钥丢失，无法再花费；</li><li>地址无人管理。</li></ol><p>这也是为什么 UTXO 集合会随着时间增长。</p><h3 id="4-5-交易输入、输出与交易费"><a href="#4-5-交易输入、输出与交易费" class="headerlink" title="4.5 交易输入、输出与交易费"></a>4.5 交易输入、输出与交易费</h3><p>一笔 BTC 交易可以有多个 input，也可以有多个 output。</p><p>基本关系是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">total inputs &gt;= total outputs</span><br></pre></td></tr></table></figure><p>如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">total inputs = total outputs</span><br></pre></td></tr></table></figure><p>则这笔交易没有 transaction fee。</p><p>如果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">total inputs &gt; total outputs</span><br></pre></td></tr></table></figure><p>二者差值就是 transaction fee：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">transaction fee = total inputs - total outputs</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">total inputs  = 1 BTC</span><br><span class="line">total outputs = 0.99 BTC</span><br><span class="line">transaction fee = 0.01 BTC</span><br></pre></td></tr></table></figure><p>这 0.01 BTC 会作为交易费给成功打包该交易的矿工。</p><p>需要注意：交易费不是显式写在某个 output 里，而是通过 input 总额和 output 总额的差额隐式体现出来。</p><h3 id="4-6-为什么需要-transaction-fee"><a href="#4-6-为什么需要-transaction-fee" class="headerlink" title="4.6 为什么需要 transaction fee"></a>4.6 为什么需要 transaction fee</h3><p>矿工打包交易是有成本的：</p><ol><li>需要验证交易是否合法；</li><li>区块中交易越多，区块越大；</li><li>区块越大，网络传播速度可能越慢；</li><li>传播慢可能导致自己挖出的区块更容易成为 orphan block。</li></ol><p>如果只有 block reward，某些自私矿工可能只打包自己的交易，而不愿意帮别人打包交易。</p><p>所以 BTC 设计了第二种激励机制：<strong>transaction fee</strong>。</p><p>交易费可以理解成用户给矿工的小费：</p><blockquote><p>你把我的交易打包进区块，我给你一点手续费。</p></blockquote><p>课程中强调，在某些早期或课程案例阶段，block reward 仍然是矿工主要收益来源。但从长期看，block reward 会不断减半，transaction fee 的重要性会逐渐提高。</p><h3 id="4-7-Block-reward-与减半机制"><a href="#4-7-Block-reward-与减半机制" class="headerlink" title="4.7 Block reward 与减半机制"></a>4.7 Block reward 与减半机制</h3><p>成功挖出新区块的矿工可以通过 coinbase transaction 获得 block reward。</p><p>BTC 中 block reward 不是固定不变的，而是每隔约 210000 个区块减半一次。</p><p>因为 BTC 的平均出块时间约为 10 分钟，所以：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">210000 blocks × 10 minutes/block</span><br><span class="line">= 2100000 minutes</span><br><span class="line">≈ 4 years</span><br></pre></td></tr></table></figure><p>因此，通常说 BTC 的 block reward 大约每四年减半一次。</p><p>课程中提到这个机制的目的，是说明 BTC 的激励结构会变化：</p><ol><li>早期：block reward 是主要激励；</li><li>随着减半进行：block reward 逐渐变小；</li><li>未来：transaction fee 可能越来越重要。</li></ol><h3 id="4-8-Transaction-based-ledger-与-account-based-ledger-的区别"><a href="#4-8-Transaction-based-ledger-与-account-based-ledger-的区别" class="headerlink" title="4.8 Transaction based ledger 与 account based ledger 的区别"></a>4.8 Transaction based ledger 与 account based ledger 的区别</h3><p>比特币采用 transaction based ledger，而以太坊更接近 account based ledger。</p><p>二者的核心区别如下：</p><table><thead><tr><th>对比项</th><th>Transaction based ledger</th><th>Account based ledger</th></tr></thead><tbody><tr><td>代表系统</td><td>Bitcoin</td><td>Ethereum</td></tr><tr><td>是否显式记录账户余额</td><td>不显式记录</td><td>显式记录</td></tr><tr><td>花钱时是否要说明币的来源</td><td>要说明</td><td>通常不需要逐个说明来源</td></tr><tr><td>核心数据结构</td><td>UTXO</td><td>Account state</td></tr><tr><td>隐私性</td><td>相对更好一些</td><td>账户状态更直接</td></tr><tr><td>交易验证重点</td><td>input 引用的 UTXO 是否存在且未花费</td><td>账户余额是否足够、nonce 是否正确</td></tr></tbody></table><p>比特币中，如果你想转给别人 10 BTC，交易必须说明这 10 BTC 从哪里来。</p><p>原因是系统没有一个地方直接写着：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A 的余额 = 10 BTC</span><br></pre></td></tr></table></figure><p>所以交易必须引用之前某些 transaction output，证明自己确实有可花的钱。</p><h3 id="4-9-普通转账交易中的脚本验证"><a href="#4-9-普通转账交易中的脚本验证" class="headerlink" title="4.9 普通转账交易中的脚本验证"></a>4.9 普通转账交易中的脚本验证</h3><p>课程中还提到 BTC 交易的合法性验证和 script 有关。</p><p>每个 input 和 output 都可以带有 script。验证交易时，不是把同一笔交易内部的 input script 和 output script 随便配对，而是：</p><blockquote><p>当前交易的 input script，要和它引用的上一笔交易中的 output script 配对执行。</p></blockquote><p>也就是说：</p><ol><li>当前交易 input 声称要花掉之前某个 output；</li><li>那个旧 output 中有锁定条件；</li><li>当前 input 要提供解锁信息；</li><li>两段 script 拼接执行成功，说明有权花这笔钱。</li></ol><p>这体现了 BTC 中“花钱”的本质：</p><blockquote><p>花钱不是修改余额，而是证明自己有资格解锁之前某个 output。</p></blockquote><h3 id="4-10-Block-header、Merkle-root-与挖矿"><a href="#4-10-Block-header、Merkle-root-与挖矿" class="headerlink" title="4.10 Block header、Merkle root 与挖矿"></a>4.10 Block header、Merkle root 与挖矿</h3><p>挖矿时，矿工不是对整个 block body 里的所有交易直接求 hash。</p><p>实际求 hash 的对象是 <strong>block header</strong>。</p><p>block header 中包含：</p><ol><li>version；</li><li>previous block hash；</li><li>Merkle root；</li><li>timestamp；</li><li>difficulty target 的编码形式；</li><li>nonce。</li></ol><p>其中，完整交易列表不直接放在 block header 中，而是通过 Merkle tree 计算出一个 <strong>Merkle root</strong>，再把 Merkle root 放进 block header。</p><p>这样做有两个作用：</p><ol><li>block header 很小，便于反复 hash；</li><li>Merkle root 又能承诺整个交易集合，交易一旦被篡改，Merkle root 就会变化，从而导致 block header hash 变化。</li></ol><p>所以，虽然挖矿只 hash block header，但这并不意味着交易内容不受保护。</p><h3 id="4-11-Difficulty-target-与-hash-前导-0"><a href="#4-11-Difficulty-target-与-hash-前导-0" class="headerlink" title="4.11 Difficulty target 与 hash 前导 0"></a>4.11 Difficulty target 与 hash 前导 0</h3><p>BTC 的 proof of work 要求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(block header) &lt;= target</span><br></pre></td></tr></table></figure><p>target 越小，找到满足条件的 hash 越难。</p><p>由于 hash 值通常写成十六进制形式，如果 target 很小，那么满足条件的 hash 往往表现为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">前面有一长串 0</span><br></pre></td></tr></table></figure><p>这就是为什么区块 hash 经常看起来以很多 0 开头。</p><p>这些 0 不是人为添加的，而是 proof of work 难度要求造成的结果。</p><h3 id="4-12-Nonce-空间不够与-extra-nonce"><a href="#4-12-Nonce-空间不够与-extra-nonce" class="headerlink" title="4.12 Nonce 空间不够与 extra nonce"></a>4.12 Nonce 空间不够与 extra nonce</h3><p>block header 中的 nonce 通常是 32-bit unsigned integer。</p><p>因此它最多只有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2^32</span><br></pre></td></tr></table></figure><p>种可能取值。</p><p>在挖矿难度很高时，仅仅遍历 2^32 个 nonce 很可能找不到合法 hash。</p><p>那么矿工还可以调整什么？</p><p>block header 中有些字段不能随意改：</p><table><thead><tr><th>字段</th><th>能否随意改</th><th>原因</th></tr></thead><tbody><tr><td>version</td><td>不能</td><td>协议版本</td></tr><tr><td>previous block hash</td><td>不能</td><td>必须指向当前链末端</td></tr><tr><td>difficulty target</td><td>不能随意改</td><td>按协议周期性调整</td></tr><tr><td>timestamp</td><td>可小范围调整</td><td>时间戳允许一定误差</td></tr><tr><td>nonce</td><td>可以调整</td><td>挖矿搜索空间</td></tr><tr><td>Merkle root</td><td>可以间接调整</td><td>改变交易集合或 coinbase transaction 会影响 Merkle root</td></tr></tbody></table><p>关键在于 <strong>coinbase transaction</strong>。</p><p>coinbase transaction 有一个 coinbase field，可以写入任意内容。矿工可以把其中一部分当作 <strong>extra nonce</strong> 使用。</p><p>当 coinbase field 改变时：</p><ol><li>coinbase transaction 的 hash 改变；</li><li>Merkle tree 中相关节点 hash 改变；</li><li>Merkle root 改变；</li><li>block header 改变；</li><li>block header hash 的搜索空间被扩大。</li></ol><p>真实挖矿可以理解成两层循环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">外层：调整 coinbase transaction 中的 extra nonce</span><br><span class="line">内层：调整 block header 中的 nonce</span><br></pre></td></tr></table></figure><h3 id="4-13-对教材-PPT-中“hash-交易信息”的误解"><a href="#4-13-对教材-PPT-中“hash-交易信息”的误解" class="headerlink" title="4.13 对教材 PPT 中“hash 交易信息”的误解"></a>4.13 对教材 PPT 中“hash 交易信息”的误解</h3><p>课程最后强调了一个容易误解的地方：</p><p>有些示意图可能把交易列表和 block header hash 画得过于接近，容易让人误以为：</p><blockquote><p>挖矿时直接把所有 transactions 放进去一起 hash。</p></blockquote><p>正确理解是：</p><ol><li>挖矿 hash 的对象是 block header；</li><li>block header 不包含完整交易列表；</li><li>block header 只包含 Merkle root；</li><li>Merkle root 是由交易集合构造出来的；</li><li>因此交易内容仍然被间接绑定到 block header 上。</li></ol><p>所以更准确的表达是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">transactions -&gt; Merkle tree -&gt; Merkle root -&gt; block header -&gt; block hash</span><br></pre></td></tr></table></figure><p>而不是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">block hash = H(all transactions)</span><br></pre></td></tr></table></figure><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-UTXO-的消耗与生成"><a href="#5-1-UTXO-的消耗与生成" class="headerlink" title="5.1 UTXO 的消耗与生成"></a>5.1 UTXO 的消耗与生成</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[&quot;旧 UTXO 1&quot;] --&gt; C[&quot;新交易&quot;]    B[&quot;旧 UTXO 2&quot;] --&gt; C    C --&gt; D[&quot;新 output 1&quot;]    C --&gt; E[&quot;新 output 2&quot;]    D --&gt; F[&quot;进入 UTXO 集合&quot;]    E --&gt; G[&quot;进入 UTXO 集合&quot;]    A --&gt; H[&quot;从 UTXO 集合移除&quot;]    B --&gt; I[&quot;从 UTXO 集合移除&quot;]  </pre></div><p>这张图说明：一笔交易会消耗旧 UTXO，同时产生新 output。旧 UTXO 被花掉后要从集合中移除；新 output 如果还没被花，就会进入 UTXO 集合。</p><h3 id="5-2-交易费的形成"><a href="#5-2-交易费的形成" class="headerlink" title="5.2 交易费的形成"></a>5.2 交易费的形成</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[&quot;Transaction inputs 总额&quot;] --&gt; B[&quot;Transaction&quot;]    B --&gt; C[&quot;Transaction outputs 总额&quot;]    B --&gt; D[&quot;Transaction fee&quot;]    E[&quot;输入总额大于输出总额&quot;] --&gt; D    D --&gt; F[&quot;归成功出块矿工所有&quot;]  </pre></div><p>交易费不是一个必须显式写出的 output，而是 input 总额和 output 总额之间的差额。矿工有动力优先打包交易费更高的交易。</p><h3 id="5-3-Block-header-与-Merkle-root"><a href="#5-3-Block-header-与-Merkle-root" class="headerlink" title="5.3 Block header 与 Merkle root"></a>5.3 Block header 与 Merkle root</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[&quot;Transactions&quot;] --&gt; B[&quot;Merkle tree&quot;]    B --&gt; C[&quot;Merkle root&quot;]    C --&gt; D[&quot;Block header&quot;]    E[&quot;Previous block hash&quot;] --&gt; D    F[&quot;Timestamp&quot;] --&gt; D    G[&quot;Difficulty target&quot;] --&gt; D    H[&quot;Nonce&quot;] --&gt; D    D --&gt; I[&quot;Block header hash&quot;]    I --&gt; J[&quot;满足 target 则挖矿成功&quot;]  </pre></div><p>这张图强调：完整交易列表不直接进入 block header。交易集合先形成 Merkle root，Merkle root 再进入 block header。挖矿时反复计算的是 block header hash。</p><h3 id="5-4-Extra-nonce-如何扩大搜索空间"><a href="#5-4-Extra-nonce-如何扩大搜索空间" class="headerlink" title="5.4 Extra nonce 如何扩大搜索空间"></a>5.4 Extra nonce 如何扩大搜索空间</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[&quot;修改 coinbase field&quot;] --&gt; B[&quot;Coinbase transaction hash 改变&quot;]    B --&gt; C[&quot;Merkle root 改变&quot;]    C --&gt; D[&quot;Block header 改变&quot;]    D --&gt; E[&quot;重新遍历 header nonce&quot;]    E --&gt; F[&quot;尝试找到合法 hash&quot;]  </pre></div><p>当 32-bit nonce 不够用时，矿工可以修改 coinbase transaction 中的 extra nonce。这样会改变 Merkle root，从而使 block header 发生变化，提供新的搜索空间。</p><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>比特币有没有账户余额？</td><td>没有像银行或以太坊那样显式维护账户余额。余额是由 UTXO 推算出来的。</td></tr><tr><td>UTXO 是某个用户的余额吗？</td><td>不是。UTXO 是一个个尚未被花费的 transaction output，余额是这些 output 的金额总和。</td></tr><tr><td>一个 transaction 的 output 要么都花掉，要么都没花？</td><td>不对。同一笔交易可以有多个 output，其中一部分可能已花，一部分可能仍在 UTXO 中。</td></tr><tr><td>Transaction fee 是写在某个 output 里的吗？</td><td>通常不是。它是 total inputs 和 total outputs 的差额。</td></tr><tr><td>Coinbase transaction 是普通转账吗？</td><td>不是。它没有普通 input，是系统产生新 BTC 的方式。</td></tr><tr><td>挖矿是对整个区块所有交易直接求 hash 吗？</td><td>不是。挖矿求 hash 的对象是 block header，交易集合通过 Merkle root 间接进入 block header。</td></tr><tr><td>Merkle root 只是为了节省空间吗？</td><td>不只是。它还用于承诺交易集合，使交易内容被篡改时 block header hash 也会受影响。</td></tr><tr><td>Nonce 不够时是不是只能换区块？</td><td>不一定。矿工可以通过修改 coinbase transaction 中的 extra nonce 改变 Merkle root，从而扩大搜索空间。</td></tr><tr><td>Transaction based ledger 和 account based ledger 只是数据格式不同吗？</td><td>不只是格式不同。它们影响余额表示、交易验证、隐私性和系统状态维护方式。</td></tr><tr><td>Block reward 和 transaction fee 是一回事吗？</td><td>不是。Block reward 是新发行 BTC，transaction fee 来自用户交易输入输出差额。</td></tr></tbody></table><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-为什么-UTXO-模型更像“现金”"><a href="#7-1-为什么-UTXO-模型更像“现金”" class="headerlink" title="7.1 为什么 UTXO 模型更像“现金”"></a>7.1 为什么 UTXO 模型更像“现金”</h3><p>UTXO 可以类比成一张张现金钞票。</p><p>假设你有两张“电子钞票”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">3 BTC</span><br><span class="line">7 BTC</span><br></pre></td></tr></table></figure><p>你要支付 5 BTC，不能直接从 7 BTC 中“减去 5 BTC”，而是要把 7 BTC 这个 UTXO 整体花掉，然后产生新的 outputs：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">5 BTC 给对方</span><br><span class="line">2 BTC 找零给自己</span><br></pre></td></tr></table></figure><p>所以 BTC 交易中经常会出现“找零地址”。</p><p>这有助于理解为什么一笔交易可以有多个 input 和多个 output。</p><h3 id="7-2-为什么-UTXO-有利于并行验证"><a href="#7-2-为什么-UTXO-有利于并行验证" class="headerlink" title="7.2 为什么 UTXO 有利于并行验证"></a>7.2 为什么 UTXO 有利于并行验证</h3><p>UTXO 模型中，不同交易如果花费的是不同 UTXO，它们之间的状态依赖相对清晰。</p><p>这使得节点在验证交易时，可以围绕“这个 output 是否还没被花”进行判断。</p><p>相比 account based model，UTXO 模型不需要频繁修改同一个账户余额字段，而是不断消耗旧 output、创建新 output。</p><p>不过这不是说 UTXO 一定在所有方面都更好。它的代价是交易必须显式说明币的来源，理解和管理起来也更复杂。</p><h3 id="7-3-为什么-Merkle-root-足以绑定交易集合"><a href="#7-3-为什么-Merkle-root-足以绑定交易集合" class="headerlink" title="7.3 为什么 Merkle root 足以绑定交易集合"></a>7.3 为什么 Merkle root 足以绑定交易集合</h3><p>Merkle tree 的关键性质是：底层任意一笔 transaction 改变，都会导致它的 transaction hash 改变。</p><p>这个变化会沿着树一路向上传递：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">transaction hash 改变</span><br><span class="line">=&gt; 父节点 hash 改变</span><br><span class="line">=&gt; 更高层 hash 改变</span><br><span class="line">=&gt; Merkle root 改变</span><br><span class="line">=&gt; block header hash 改变</span><br></pre></td></tr></table></figure><p>因此，虽然 block header 中没有完整交易列表，但只要 Merkle root 放在 block header 里，交易集合就被绑定到了 block header 上。</p><h3 id="7-4-Difficulty-adjustment-的补充说明"><a href="#7-4-Difficulty-adjustment-的补充说明" class="headerlink" title="7.4 Difficulty adjustment 的补充说明"></a>7.4 Difficulty adjustment 的补充说明</h3><p>课程文字稿中相关位置可能存在转写误差。BTC 的 difficulty adjustment 通常是每 <strong>2016 blocks</strong> 调整一次，而不是“15 个区块”。</p><p>因为 BTC 目标平均出块时间约为 10 分钟：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">2016 blocks × 10 minutes/block = 20160 minutes ≈ 14 days</span><br></pre></td></tr></table></figure><p>所以难度调整周期大约是两周。</p><p>这个机制的目的，是让全网算力变化时，系统仍然尽量维持平均 10 分钟出一个新区块。</p><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>BTC 采用 transaction based ledger，不直接保存账户余额。</li><li>UTXO 是 BTC 交易验证的核心数据结构，用于判断某个 output 是否仍可被花费。</li><li>普通交易会消耗旧 UTXO，并产生新的 transaction outputs。</li><li>Transaction fee 来自 total inputs 与 total outputs 的差额，是矿工打包交易的重要激励。</li><li>Coinbase transaction 用于产生 block reward，同时可以携带 extra nonce 来扩大挖矿搜索空间。</li><li>挖矿计算的是 block header hash，而不是直接对所有交易求 hash；交易集合通过 Merkle root 进入 block header。</li><li>BTC 的长期激励结构会随着 block reward 减半逐渐更加依赖 transaction fee。</li></ol><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-为什么说比特币没有直接记录账户余额？"><a href="#1-为什么说比特币没有直接记录账户余额？" class="headerlink" title="1. 为什么说比特币没有直接记录账户余额？"></a>1. 为什么说比特币没有直接记录账户余额？</h3><p>参考答案：因为 BTC 使用 transaction based ledger，账本中记录的是一笔笔交易，而不是账户余额字段。某个地址的余额需要通过统计其拥有的未花费 outputs，也就是 UTXO，推算出来。</p><h3 id="2-UTXO-集合中每个元素至少需要哪些信息来定位？"><a href="#2-UTXO-集合中每个元素至少需要哪些信息来定位？" class="headerlink" title="2. UTXO 集合中每个元素至少需要哪些信息来定位？"></a>2. UTXO 集合中每个元素至少需要哪些信息来定位？</h3><p>参考答案：需要产生该 output 的 transaction hash，以及该 output 在该 transaction 中的编号。</p><h3 id="3-为什么-UTXO-可以用来检测-double-spending？"><a href="#3-为什么-UTXO-可以用来检测-double-spending？" class="headerlink" title="3. 为什么 UTXO 可以用来检测 double spending？"></a>3. 为什么 UTXO 可以用来检测 double spending？</h3><p>参考答案：如果某个 output 仍在 UTXO 集合中，说明它尚未被花费，可以作为合法 input。如果不在 UTXO 集合中，则说明它不存在或已经被花过，再次花费就是 double spending。</p><h3 id="4-一笔交易的-transaction-fee-如何计算？"><a href="#4-一笔交易的-transaction-fee-如何计算？" class="headerlink" title="4. 一笔交易的 transaction fee 如何计算？"></a>4. 一笔交易的 transaction fee 如何计算？</h3><p>参考答案：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">transaction fee = total inputs - total outputs</span><br></pre></td></tr></table></figure><p>如果输入总额大于输出总额，差额归成功打包该交易的矿工所有。</p><h3 id="5-为什么-BTC-需要-transaction-fee，而不仅仅依赖-block-reward？"><a href="#5-为什么-BTC-需要-transaction-fee，而不仅仅依赖-block-reward？" class="headerlink" title="5. 为什么 BTC 需要 transaction fee，而不仅仅依赖 block reward？"></a>5. 为什么 BTC 需要 transaction fee，而不仅仅依赖 block reward？</h3><p>参考答案：矿工打包和验证交易有成本。如果没有 transaction fee，矿工可能缺乏动力打包别人的交易。随着 block reward 逐渐减半，transaction fee 对维护矿工激励会越来越重要。</p><h3 id="6-Coinbase-transaction-和普通交易有什么区别？"><a href="#6-Coinbase-transaction-和普通交易有什么区别？" class="headerlink" title="6. Coinbase transaction 和普通交易有什么区别？"></a>6. Coinbase transaction 和普通交易有什么区别？</h3><p>参考答案：Coinbase transaction 没有普通 input，是矿工在新区块中创建的特殊交易，用于获得 block reward。它的 coinbase field 可以写入任意内容，因此也可以用作 extra nonce。</p><h3 id="7-挖矿时-hash-的对象是什么？完整交易列表是否直接参与-hash？"><a href="#7-挖矿时-hash-的对象是什么？完整交易列表是否直接参与-hash？" class="headerlink" title="7. 挖矿时 hash 的对象是什么？完整交易列表是否直接参与 hash？"></a>7. 挖矿时 hash 的对象是什么？完整交易列表是否直接参与 hash？</h3><p>参考答案：挖矿时 hash 的对象是 block header。完整交易列表不直接放入 block header，而是先形成 Merkle tree，再把 Merkle root 放入 block header。</p><h3 id="8-为什么修改-coinbase-field-会影响-block-header-hash？"><a href="#8-为什么修改-coinbase-field-会影响-block-header-hash？" class="headerlink" title="8. 为什么修改 coinbase field 会影响 block header hash？"></a>8. 为什么修改 coinbase field 会影响 block header hash？</h3><p>参考答案：修改 coinbase field 会改变 coinbase transaction 的 hash，进一步改变 Merkle tree 的路径 hash，最终改变 Merkle root。Merkle root 是 block header 的一部分，因此 block header hash 也会改变。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 4 讲：BTC 协议（三）：PoW 共识、分叉与挖矿奖励</title>
      <link href="/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE3/"/>
      <url>/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE3/</url>
      
        <content type="html"><![CDATA[<p>来源依据：课程文字稿《04-BTC-协议3》；笔记结构与写作规范参考《00笔记生成指南》</p><h1 id="第-4-讲：BTC-协议（三）——PoW-共识、分叉与挖矿奖励"><a href="#第-4-讲：BTC-协议（三）——PoW-共识、分叉与挖矿奖励" class="headerlink" title="第 4 讲：BTC 协议（三）——PoW 共识、分叉与挖矿奖励"></a>第 4 讲：BTC 协议（三）——PoW 共识、分叉与挖矿奖励</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>比特币不是按“账户数量”投票，而是按<strong>计算力</strong>投票。</li><li>节点通过不断尝试 <code>nonce</code> 来求解 <code>computational puzzle</code>，从而争夺记账权。</li><li>一个区块即使交易和区块头本身都合法，也不一定会被接受；它还必须扩展在<strong>最长合法链</strong>上。</li><li>分叉可能来自攻击，也可能来自两个矿工几乎同时挖出区块。</li><li>出块奖励和 <code>coinbase transaction</code> 是比特币发行新币的唯一方式，也是矿工参与挖矿的重要激励。</li><li>比特币共识要达成的不是“谁说了算”，而是所有诚实节点对<strong>去中心化账本内容</strong>达成一致。</li></ol><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>记账权</td><td>向比特币账本写入下一个区块的权利</td><td>只有成功挖矿的节点才能发布新区块</td></tr><tr><td><code>nonce</code></td><td>区块头中的随机数字段</td><td>矿工不断尝试不同 <code>nonce</code>，寻找满足难度要求的哈希</td></tr><tr><td><code>computational puzzle</code></td><td>需要大量尝试才能解出的计算难题</td><td>用来让记账权与算力挂钩</td></tr><tr><td><code>block header</code></td><td>区块头，包含前一区块哈希、Merkle root、<code>nonce</code>、<code>nBits</code> 等信息</td><td>节点验证区块合法性时首先检查它</td></tr><tr><td><code>nBits</code></td><td>目标阈值的压缩编码</td><td>表示当前挖矿难度对应的目标范围</td></tr><tr><td>最长合法链</td><td>被诚实节点接受并继续扩展的最长有效分支</td><td>决定哪个分叉成为主链</td></tr><tr><td><code>forking attack</code></td><td>通过在链中间插入分叉来试图回滚已有交易的攻击</td><td>说明“区块内容合法”不等于“应该被接受”</td></tr><tr><td>临时性分叉</td><td>两个矿工几乎同时挖出区块导致的等长分叉</td><td>正常情况下可能出现，最终由最长合法链规则解决</td></tr><tr><td><code>implicit consent</code></td><td>隐式认可</td><td>节点继续沿某个区块扩展，就表示认可该区块</td></tr><tr><td><code>orphan block</code></td><td>被主链抛弃的分叉区块</td><td>其中的出块奖励通常无法真正使用</td></tr><tr><td>出块奖励</td><td>成功挖出区块后获得的新发行 BTC</td><td>激励矿工消耗算力争夺记账权</td></tr><tr><td><code>coinbase transaction</code></td><td>铸币交易</td><td>比特币系统中新币发行的唯一方式</td></tr><tr><td><code>hash rate</code></td><td>单位时间内尝试哈希计算的能力</td><td>决定节点获得记账权的概率权重</td></tr><tr><td><code>Sybil attack</code></td><td>女巫攻击，即创建大量虚假身份操纵投票</td><td>PoW 通过“按算力投票”削弱虚假身份的作用</td></tr><tr><td>挖矿（<code>mining</code>）</td><td>通过求解 PoW 难题争夺记账权</td><td>是比特币共识机制的核心过程</td></tr></tbody></table><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[比特币共识问题] --&gt; B[谁能写入账本]    A --&gt; C[账本内容如何一致]    B --&gt; D[争夺记账权]    D --&gt; E[尝试 nonce]    E --&gt; F[满足目标阈值]    F --&gt; G[发布新区块]    C --&gt; H[验证区块]    H --&gt; I[验证 block header]    H --&gt; J[验证交易合法性]    H --&gt; K[是否扩展最长合法链]    K --&gt; L[接受并继续扩展]    K --&gt; M[拒绝或成为 orphan block]    D --&gt; N[出块奖励]    N --&gt; O[coinbase transaction]    O --&gt; P[新 BTC 发行]  </pre></div><p>这张图说明：比特币共识机制围绕两个问题展开。第一，谁有资格写账本？答案是成功求解 PoW 难题的矿工。第二，写出来的账本如何被全网接受？答案是新区块不仅要内容合法，还要扩展在最长合法链上。</p><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-比特币不是按账户投票，而是按算力投票"><a href="#4-1-比特币不是按账户投票，而是按算力投票" class="headerlink" title="4.1 比特币不是按账户投票，而是按算力投票"></a>4.1 比特币不是按账户投票，而是按算力投票</h3><p>在去中心化系统中，一个核心问题是：如果没有中心服务器，谁来决定账本的下一页写什么？</p><p>直观想法是“投票”。但如果按账户数投票，就会遇到 <code>Sybil attack</code>：攻击者可以低成本创建大量账户，让自己看起来拥有很多投票权。</p><p>比特币的解决方法是：<strong>不按账户数量投票，而按计算力投票。</strong></p><p>也就是说，一个节点能获得多少“话语权”，不取决于它创建了多少个账号，而取决于它每秒能尝试多少次哈希计算。这个能力称为 <code>hash rate</code>。</p><p>如果某个节点的算力是另一个节点的 10 倍，那么它成功获得记账权的概率也大约是后者的 10 倍。</p><p>这里的关键思想是：</p><blockquote><p>身份可以低成本复制，但算力不能低成本复制。</p></blockquote><p>所以，PoW 机制用物理计算资源取代了虚拟身份数量，降低了女巫攻击的威胁。</p><h3 id="4-2-记账权如何获得：尝试-nonce"><a href="#4-2-记账权如何获得：尝试-nonce" class="headerlink" title="4.2 记账权如何获得：尝试 nonce"></a>4.2 记账权如何获得：尝试 <code>nonce</code></h3><p>每个节点都可以在本地组装一个候选区块，把自己认为合法的交易放入 <code>block body</code> 中，然后构造对应的 <code>block header</code>。</p><p><code>block header</code> 中有一个字段叫 <code>nonce</code>。矿工会不断尝试不同的 <code>nonce</code>，计算整个区块头的哈希值，看它是否满足协议规定的目标阈值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Hash(block header) &lt;= target</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>Hash(block header)</code> 是区块头的哈希结果；</li><li><code>target</code> 是当前难度对应的目标阈值；</li><li><code>nBits</code> 是这个目标阈值在区块头中的压缩编码。</li></ul><p>如果某个节点找到了合适的 <code>nonce</code>，使得区块头哈希小于等于目标阈值，那么它就获得了记账权，可以向网络发布这个新区块。</p><h3 id="4-3-其他节点如何验证新区块"><a href="#4-3-其他节点如何验证新区块" class="headerlink" title="4.3 其他节点如何验证新区块"></a>4.3 其他节点如何验证新区块</h3><p>矿工发布新区块后，其他节点不会盲目接受，而是要验证它。</p><p>验证主要包括三层：</p><ol><li><p><strong>验证区块头是否符合要求</strong></p><ul><li><code>nBits</code> 是否符合当前协议难度；</li><li><code>nonce</code> 是否真的让区块头哈希满足目标阈值；</li><li>前一区块哈希是否正确指向某个已有区块。</li></ul></li><li><p><strong>验证区块体中的交易是否合法</strong></p><ul><li>每笔交易是否有合法签名；</li><li>输入的币是否以前没有被花过；</li><li>是否存在 double spending。</li></ul></li><li><p><strong>验证它是否应该被接入主链</strong></p><ul><li>即使区块头和交易都合法，也要看它是不是扩展最长合法链。</li></ul></li></ol><p>第三点是本节非常重要的地方：<br><strong>区块内容合法，不等于这个区块一定会被诚实节点接受。</strong></p><h3 id="4-4-为什么“合法区块”也可能不被接受"><a href="#4-4-为什么“合法区块”也可能不被接受" class="headerlink" title="4.4 为什么“合法区块”也可能不被接受"></a>4.4 为什么“合法区块”也可能不被接受</h3><p>假设有一个新区块，它的区块头合法，里面的交易也都合法。但是它不是接在当前最长链末尾，而是插入到了链中间的某个位置，形成了一个分叉。</p><p>这时它可能试图回滚已经发生的交易。</p><p>例如：</p><ol><li>主链上已经记录了 <code>A -&gt; B</code> 的转账；</li><li>攻击者又在较早位置分出一条支链；</li><li>在这条支链上，攻击者把同一笔币转回给自己；</li><li>从这条支链自身来看，这笔交易可能并不构成 double spending；</li><li>但从全局效果看，它试图推翻主链上已经发生的交易。</li></ol><p>这就是 <code>forking attack</code> 的基本思想。</p><p>所以，比特币节点不仅看交易本身是否合法，还要看新区块是否在扩展当前被认可的最长合法链。</p><h3 id="4-5-double-spending-的判断依赖所在分支"><a href="#4-5-double-spending-的判断依赖所在分支" class="headerlink" title="4.5 double spending 的判断依赖所在分支"></a>4.5 double spending 的判断依赖所在分支</h3><p>判断一笔交易是否是 double spending，不是把所有分叉上的所有交易混在一起检查，而是看这笔交易所在的链分支。</p><p>具体来说，要检查：</p><blockquote><p>从当前交易所在区块往前追溯，到这笔币的来源之间，这个币有没有在同一条分支上被花过。</p></blockquote><p>如果在当前分支上没有被花过，那么从这个分支内部看，它可以是合法交易。</p><p>这就解释了为什么攻击者能构造出一个“局部合法”的分叉：<br>因为在攻击者自己的分支上，这笔币可能没有被重复花费。</p><p>但是，比特币协议不会因为某条分支内部合法就接受它。诚实节点最终接受的是最长合法链。</p><h3 id="4-6-正常情况下也会产生分叉"><a href="#4-6-正常情况下也会产生分叉" class="headerlink" title="4.6 正常情况下也会产生分叉"></a>4.6 正常情况下也会产生分叉</h3><p>分叉不一定都是攻击。正常情况下，如果两个矿工几乎同时找到合法 <code>nonce</code>，它们都可以发布新区块。</p><p>这时网络中会出现两个等长分叉：</p><ul><li>一些节点先收到区块 A，于是先接受 A；</li><li>另一些节点先收到区块 B，于是先接受 B。</li></ul><p>因为网络传播有延迟，不同节点收到消息的顺序可能不同。</p><p>在两个分叉等长时，比特币协议默认节点接受自己最早收到的那个区块，并沿着它继续挖矿。</p><h3 id="4-7-implicit-consent：如何表达“我认可这个区块”"><a href="#4-7-implicit-consent：如何表达“我认可这个区块”" class="headerlink" title="4.7 implicit consent：如何表达“我认可这个区块”"></a>4.7 <code>implicit consent</code>：如何表达“我认可这个区块”</h3><p>比特币中没有一个中心化投票按钮说“我同意这个区块”。节点对某个区块的认可，是通过行为表达的。</p><p>如果一个节点在某个区块后面继续扩展新区块，就表示它认可这个区块。</p><p>这叫 <code>implicit consent</code>，即隐式认可。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A -&gt; B -&gt; C</span><br></pre></td></tr></table></figure><p>如果矿工在 <code>C</code> 后面继续挖出 <code>D</code>，那么它实际上是在表达：</p><blockquote><p>我认可从 A 到 C 这条链，并愿意在它后面继续写账本。</p></blockquote><h3 id="4-8-临时分叉如何被解决"><a href="#4-8-临时分叉如何被解决" class="headerlink" title="4.8 临时分叉如何被解决"></a>4.8 临时分叉如何被解决</h3><p>当两个分叉等长时，它们会暂时并存。之后，哪个分叉先被挖出下一个区块，哪个分叉就变得更长。</p><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">        -&gt; B1</span><br><span class="line">A -----</span><br><span class="line">        -&gt; B2 -&gt; C2</span><br></pre></td></tr></table></figure><p>当 <code>B2</code> 分支先得到新区块 <code>C2</code> 后，<code>A -&gt; B2 -&gt; C2</code> 成为最长合法链。诚实节点会切换到这条链上继续扩展。</p><p>被抛弃的 <code>B1</code> 就成为 <code>orphan block</code>。</p><p>这里要注意：<br>一个分叉能否胜出，与算力有关，也与随机性有关。算力更强的一方更有可能先挖出下一个区块，但不是绝对保证。PoW 本质上仍然有概率性。</p><h3 id="4-9-出块奖励：为什么矿工愿意消耗算力"><a href="#4-9-出块奖励：为什么矿工愿意消耗算力" class="headerlink" title="4.9 出块奖励：为什么矿工愿意消耗算力"></a>4.9 出块奖励：为什么矿工愿意消耗算力</h3><p>挖矿需要消耗大量计算资源和电力。矿工之所以愿意这样做，是因为成功获得记账权可以获得奖励。</p><p>这份奖励主要来自 <code>coinbase transaction</code>。</p><p><code>coinbase transaction</code> 是一种特殊交易：</p><ul><li>它不需要指明币的来源；</li><li>它可以凭空产生一定数量的新 BTC；</li><li>它只能由成功挖出区块的矿工写入自己发布的区块中；</li><li>它是比特币系统发行新币的唯一方式。</li></ul><p>普通交易只是把已有 BTC 从一个账户转移到另一个账户，不会创造新的 BTC。即使用法币购买 BTC，本质上也只是别人把已有 BTC 转给你，并没有产生新币。</p><h3 id="4-10-出块奖励减半"><a href="#4-10-出块奖励减半" class="headerlink" title="4.10 出块奖励减半"></a>4.10 出块奖励减半</h3><p>课程中提到，比特币刚上线时，每个区块的出块奖励是 50 BTC。协议规定，每经过 210000 个区块，出块奖励减半：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">50 BTC -&gt; 25 BTC -&gt; 12.5 BTC -&gt; ...</span><br></pre></td></tr></table></figure><p>课程这里是在讲比特币协议的发行机制：<br>随着时间推移，每个区块能新发行的 BTC 数量逐渐减少。</p><p>注意：课程中提到的“现在是 12.5 BTC”属于课程录制时的语境。复习本节时，重点不是记某个时间点的当前奖励，而是理解：</p><blockquote><p>比特币通过固定规则控制新币发行，而不是由中心机构临时决定发行多少。</p></blockquote><h3 id="4-11-orphan-block-中的奖励为什么通常没用"><a href="#4-11-orphan-block-中的奖励为什么通常没用" class="headerlink" title="4.11 orphan block 中的奖励为什么通常没用"></a>4.11 <code>orphan block</code> 中的奖励为什么通常没用</h3><p>如果一个矿工在某个分叉上挖出了区块，并在里面写入了 <code>coinbase transaction</code>，但这个分叉最后没有成为最长合法链，那么这个区块就会变成 <code>orphan block</code>。</p><p>这时，该区块里的出块奖励通常无法真正使用。</p><p>原因是：<br>比特币节点验证交易时，会根据最长合法链来判断账本状态。如果某个奖励只存在于被抛弃的短分叉上，那么大多数诚实节点不会承认这笔奖励。</p><p>所以，矿工不只是要挖出合法区块，还希望自己的区块最终留在主链上。</p><h3 id="4-12-比特币共识到底在共识什么"><a href="#4-12-比特币共识到底在共识什么" class="headerlink" title="4.12 比特币共识到底在共识什么"></a>4.12 比特币共识到底在共识什么</h3><p>比特币共识不是简单地共识“谁是矿工”，也不是共识“谁的账户多”。</p><p>它真正要共识的是：</p><blockquote><p>去中心化账本的内容。</p></blockquote><p>换句话说，全网诚实节点最终要对“哪些交易已经发生、哪些 BTC 属于谁”达成一致。</p><p>而账本内容由谁写入？</p><p>答案是：<br>成功求解 PoW 难题、获得记账权的节点。</p><p>这就是为什么说比特币共识是“按算力投票”：</p><ul><li>能否获得记账权，取决于能否找到合法 <code>nonce</code>；</li><li>找到合法 <code>nonce</code> 的概率，取决于 <code>hash rate</code>；</li><li><code>hash rate</code> 越高，获得记账权的概率越高；</li><li>创建更多账户并不能提高 <code>hash rate</code>。</li></ul><h3 id="4-13-挖矿的含义"><a href="#4-13-挖矿的含义" class="headerlink" title="4.13 挖矿的含义"></a>4.13 挖矿的含义</h3><p><code>mining</code> 是对比特币争夺记账权过程的形象比喻。</p><p>它类似淘金：</p><ul><li>搜索空间很大；</li><li>找到合法结果的概率很小；</li><li>需要不断尝试；</li><li>一旦成功，就能获得较高奖励。</li></ul><p>在比特币中，矿工不是在物理上挖黄金，而是在哈希空间中寻找满足难度目标的 <code>nonce</code>。</p><p>所以，“挖到矿”本质上就是：</p><blockquote><p>找到了一个让区块头哈希满足目标阈值的 <code>nonce</code>，从而获得了发布新区块和领取出块奖励的资格。</p></blockquote><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-挖矿与区块验证流程"><a href="#5-1-挖矿与区块验证流程" class="headerlink" title="5.1 挖矿与区块验证流程"></a>5.1 挖矿与区块验证流程</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[矿工收集交易] --&gt; B[组装候选区块]    B --&gt; C[构造 block header]    C --&gt; D[不断尝试 nonce]    D --&gt; E{Hash &lt;&#x3D; target?}    E -- 否 --&gt; D    E -- 是 --&gt; F[获得记账权]    F --&gt; G[广播新区块]    G --&gt; H[其他节点验证]    H --&gt; I{block header 合法?}    I -- 否 --&gt; X[拒绝区块]    I -- 是 --&gt; J{交易合法?}    J -- 否 --&gt; X    J -- 是 --&gt; K{扩展最长合法链?}    K -- 否 --&gt; Y[不作为主链接受]    K -- 是 --&gt; Z[接受并继续扩展]  </pre></div><p>这张图展示了 PoW 共识的完整主干。矿工先通过不断尝试 <code>nonce</code> 来争夺记账权；其他节点收到区块后，要同时验证区块头、交易内容和它是否扩展最长合法链。</p><h3 id="5-2-正常分叉与最长合法链选择"><a href="#5-2-正常分叉与最长合法链选择" class="headerlink" title="5.2 正常分叉与最长合法链选择"></a>5.2 正常分叉与最长合法链选择</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[已有主链末端] --&gt; B1[矿工1挖出区块 B1]    A --&gt; B2[矿工2挖出区块 B2]    B1 --&gt; C1[部分节点沿 B1 挖矿]    B2 --&gt; C2[部分节点沿 B2 挖矿]    C2 --&gt; D2[先挖出新区块]    D2 --&gt; E[B2 分支变成最长合法链]    C1 --&gt; F[B1 分支成为 orphan block]  </pre></div><p>这张图说明，正常网络延迟会导致临时性分叉。等长分叉出现后，节点通常先接受自己最早收到的区块。随后，先被继续扩展的分支成为最长合法链，另一条分支被抛弃。</p><h3 id="5-3-出块奖励与-coinbase-transaction"><a href="#5-3-出块奖励与-coinbase-transaction" class="headerlink" title="5.3 出块奖励与 coinbase transaction"></a>5.3 出块奖励与 <code>coinbase transaction</code></h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[矿工成功挖出区块] --&gt; B[获得发布区块资格]    B --&gt; C[在区块中加入 coinbase transaction]    C --&gt; D[发行新的 BTC]    D --&gt; E{区块留在最长合法链上?}    E -- 是 --&gt; F[奖励被网络承认]    E -- 否 --&gt; G[区块成为 orphan block]    G --&gt; H[奖励通常无法使用]  </pre></div><p>这张图强调：出块奖励不是只要写进区块就一定有效。只有区块最终处于最长合法链上，其中的 <code>coinbase transaction</code> 才会被诚实节点承认。</p><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>“区块里的交易都合法，所以一定会被接受”</td><td>不一定。区块还必须扩展最长合法链。插入链中间的合法区块可能是分叉攻击的一部分。</td></tr><tr><td>“double spending 是全网所有分叉一起检查”</td><td>不是。判断 double spending 时，要看交易所在分支中该币是否被重复花费。</td></tr><tr><td>“最长链就是区块数量最多的任意链”</td><td>更准确地说，是最长合法链。非法交易、非法区块头的链不会被诚实节点接受。</td></tr><tr><td>“分叉一定是攻击”</td><td>不一定。两个矿工几乎同时挖出区块，也会导致正常的临时性分叉。</td></tr><tr><td>“接受区块就是显式投票同意”</td><td>比特币采用 <code>implicit consent</code>。节点继续沿某区块扩展，就表示认可它。</td></tr><tr><td>“矿工挖出区块后奖励一定归自己”</td><td>不一定。如果区块后来变成 <code>orphan block</code>，其中的奖励通常不会被主链接受。</td></tr><tr><td>“创建更多账户可以提高投票权”</td><td>在 PoW 中不行。投票权来自 <code>hash rate</code>，不是账户数量。</td></tr><tr><td>“挖矿是在挖真实的币”</td><td>挖矿是求解哈希难题。新 BTC 通过 <code>coinbase transaction</code> 发行。</td></tr></tbody></table><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-为什么-PoW-可以缓解-Sybil-attack"><a href="#7-1-为什么-PoW-可以缓解-Sybil-attack" class="headerlink" title="7.1 为什么 PoW 可以缓解 Sybil attack"></a>7.1 为什么 PoW 可以缓解 <code>Sybil attack</code></h3><p>在普通投票系统中，如果没有身份认证，一个人可以创建大量虚假身份，从而操纵投票结果。</p><p>但是在 PoW 中，投票权不是身份数量，而是算力。</p><p>假设攻击者有一台机器：</p><ul><li>创建 1 个账户，机器每秒能算 100 次哈希；</li><li>创建 10000 个账户，机器每秒仍然只能算 100 次哈希。</li></ul><p>账户数量变多，并不会让它尝试 <code>nonce</code> 的速度变快。</p><p>所以，PoW 把“制造身份”的问题转换成“投入计算资源”的问题。攻击者要提高影响力，就必须投入真实算力，而不能只靠复制账号。</p><h3 id="7-2-为什么挖矿结果具有概率性"><a href="#7-2-为什么挖矿结果具有概率性" class="headerlink" title="7.2 为什么挖矿结果具有概率性"></a>7.2 为什么挖矿结果具有概率性</h3><p>哈希函数的输出看起来近似随机。矿工尝试不同 <code>nonce</code> 时，无法提前知道哪个 <code>nonce</code> 会成功，只能不断试。</p><p>因此，挖矿像抽奖：</p><ul><li><code>hash rate</code> 越高，单位时间内抽奖次数越多；</li><li>抽奖次数越多，中奖概率越高；</li><li>但某一次具体是谁先中奖，仍然有随机性。</li></ul><p>这就是为什么课程中说，分叉竞争时算力强的一方更有可能胜出，但算力较弱的一方也可能因为运气好先挖出下一个区块。</p><h3 id="7-3-为什么“最长合法链”能稳定账本"><a href="#7-3-为什么“最长合法链”能稳定账本" class="headerlink" title="7.3 为什么“最长合法链”能稳定账本"></a>7.3 为什么“最长合法链”能稳定账本</h3><p>如果所有诚实节点都遵循最长合法链规则，那么网络会逐渐收敛到同一条链上。</p><p>临时分叉刚出现时，不同节点可能选择不同分支；但一旦某个分支先被继续扩展，它就会更长，其他诚实节点会转向这条更长的合法链。</p><p>这使得比特币账本不需要中心服务器，也能逐渐形成一致状态。</p><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>比特币通过 PoW 机制争夺记账权，本质是按算力投票，而不是按账户数量投票。</li><li>矿工通过不断尝试 <code>nonce</code>，寻找使区块头哈希满足目标阈值的结果。</li><li>其他节点收到新区块后，需要验证区块头、交易合法性，以及它是否扩展最长合法链。</li><li>区块内容合法不代表一定会被接受；如果它试图插入链中间并回滚交易，就可能构成 <code>forking attack</code>。</li><li>正常网络延迟也可能导致临时分叉，最终由最长合法链规则解决。</li><li><code>coinbase transaction</code> 是比特币发行新币的唯一方式，出块奖励是矿工参与挖矿的重要激励。</li><li><code>orphan block</code> 中的奖励通常无法使用，因为它不在被大多数诚实节点接受的主链上。</li><li>比特币共识最终要达成一致的是去中心化账本的内容。</li></ol><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-比特币为什么不采用“一个账户一票”的机制？"><a href="#1-比特币为什么不采用“一个账户一票”的机制？" class="headerlink" title="1. 比特币为什么不采用“一个账户一票”的机制？"></a>1. 比特币为什么不采用“一个账户一票”的机制？</h3><p>参考答案：<br>因为账户可以低成本大量创建，会导致 <code>Sybil attack</code>。比特币改用按算力投票，账户数量不会直接增加获得记账权的概率。</p><h3 id="2-矿工如何获得记账权？"><a href="#2-矿工如何获得记账权？" class="headerlink" title="2. 矿工如何获得记账权？"></a>2. 矿工如何获得记账权？</h3><p>参考答案：<br>矿工组装候选区块，然后不断尝试不同 <code>nonce</code>。如果某个 <code>nonce</code> 使 <code>Hash(block header) &lt;= target</code>，矿工就获得记账权，可以发布新区块。</p><h3 id="3-其他节点收到新区块后主要验证什么？"><a href="#3-其他节点收到新区块后主要验证什么？" class="headerlink" title="3. 其他节点收到新区块后主要验证什么？"></a>3. 其他节点收到新区块后主要验证什么？</h3><p>参考答案：<br>主要验证三类内容：区块头是否合法，交易是否有合法签名且没有 double spending，以及该区块是否扩展最长合法链。</p><h3 id="4-为什么一个交易合法的区块也可能不被接受？"><a href="#4-为什么一个交易合法的区块也可能不被接受？" class="headerlink" title="4. 为什么一个交易合法的区块也可能不被接受？"></a>4. 为什么一个交易合法的区块也可能不被接受？</h3><p>参考答案：<br>因为它可能没有接在最长合法链后面，而是插入链中间形成分叉，试图回滚已经发生的交易。比特币要求接受扩展最长合法链的区块。</p><h3 id="5-正常情况下为什么也会出现分叉？"><a href="#5-正常情况下为什么也会出现分叉？" class="headerlink" title="5. 正常情况下为什么也会出现分叉？"></a>5. 正常情况下为什么也会出现分叉？</h3><p>参考答案：<br>两个矿工可能几乎同时找到合法 <code>nonce</code> 并广播区块。由于网络传播延迟，不同节点可能先收到不同区块，于是暂时沿不同分支扩展。</p><h3 id="6-什么是-implicit-consent？"><a href="#6-什么是-implicit-consent？" class="headerlink" title="6. 什么是 implicit consent？"></a>6. 什么是 <code>implicit consent</code>？</h3><p>参考答案：<br>节点不需要显式投票。如果它沿着某个区块继续挖矿、扩展新区块，就表示它认可该区块。</p><h3 id="7-orphan-block-中的出块奖励为什么通常不能用？"><a href="#7-orphan-block-中的出块奖励为什么通常不能用？" class="headerlink" title="7. orphan block 中的出块奖励为什么通常不能用？"></a>7. <code>orphan block</code> 中的出块奖励为什么通常不能用？</h3><p>参考答案：<br>因为该区块不在最终被接受的最长合法链上。诚实节点不会承认短分叉中的账本状态，所以其中的 <code>coinbase transaction</code> 奖励也通常无效。</p><h3 id="8-比特币共识机制要取得的共识到底是什么？"><a href="#8-比特币共识机制要取得的共识到底是什么？" class="headerlink" title="8. 比特币共识机制要取得的共识到底是什么？"></a>8. 比特币共识机制要取得的共识到底是什么？</h3><p>参考答案：<br>是去中心化账本的内容，即哪些交易被确认、哪些 BTC 属于谁。PoW 决定谁有机会写入账本，最长合法链规则决定哪些写入最终被接受。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
            <tag> ProofOfWork </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 4 讲：BTC 协议（二）：共识问题与投票方案的局限性</title>
      <link href="/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE2/"/>
      <url>/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE2/</url>
      
        <content type="html"><![CDATA[<h1 id="第-4-讲：BTC-协议-2——共识问题与投票方案的局限"><a href="#第-4-讲：BTC-协议-2——共识问题与投票方案的局限" class="headerlink" title="第 4 讲：BTC 协议 2——共识问题与投票方案的局限"></a>第 4 讲：BTC 协议 2——共识问题与投票方案的局限</h1><p>依据：课程文字稿 ，笔记生成指南 。</p><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>区块链是一个去中心化账本，但账本内容必须在全网形成统一说法，否则每个节点各记各的账，系统就失去意义。</li><li>本节从“谁决定下一个区块写什么”出发，引出区块链中的**共识（consensus）**问题。</li><li>传统分布式系统中有 FLP impossibility result、CAP theorem、Paxos 等理论和协议，但 Bitcoin 面对的是开放网络，不能直接照搬。</li><li>简单投票方案在联盟链中可以考虑，但在 Bitcoin 这种任何人都能创建账户的系统中，会受到 <strong>Sybil Attack</strong> 的威胁。</li><li>本节的核心作用是说明：Bitcoin 不能按“账户数量”投票，必须寻找另一种决定记账权的机制。</li></ol><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>共识（consensus）</td><td>多个节点对某个系统状态达成一致</td><td>解决“下一个区块写什么、账本以谁为准”的问题</td></tr><tr><td>去中心化账本</td><td>没有单一中心服务器维护的账本</td><td>需要所有诚实节点最终认可同一条账本历史</td></tr><tr><td>分布式哈希表例子</td><td>多台机器共同维护一个 key-value table</td><td>用来类比“系统状态需要一致”</td></tr><tr><td>FLP impossibility result</td><td>异步系统中，只要存在一个 faulty 节点，就无法保证一定达成共识</td><td>说明分布式共识本身存在理论困难</td></tr><tr><td>CAP theorem</td><td>Consistency、Availability、Partition tolerance 三者最多同时满足两个</td><td>说明分布式系统设计中存在根本权衡</td></tr><tr><td>Paxos</td><td>经典分布式共识协议</td><td>能保证一致性，但在某些情况下可能无法推进</td></tr><tr><td>投票方案</td><td>节点对候选区块投赞成票或反对票</td><td>一个直观但不适合 Bitcoin 的共识思路</td></tr><tr><td>membership</td><td>谁有资格参与系统、谁有投票权</td><td>投票方案必须先解决的问题</td></tr><tr><td>联盟链（consortium blockchain）</td><td>参与者身份受限制的区块链</td><td>因为 membership 明确，可以采用投票类方案</td></tr><tr><td>Hyperledger Fabric</td><td>一个典型联盟链系统</td><td>用来说明“有准入门槛的链”和 Bitcoin 不同</td></tr><tr><td>女巫攻击（Sybil Attack）</td><td>攻击者伪造大量身份，操纵按身份计票的系统</td><td>解释为什么 Bitcoin 不能简单按账户投票</td></tr></tbody></table><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[交易被广播] --&gt; B[谁决定写入下一个区块]    B --&gt; C[需要全网账本共识]    C --&gt; D[传统分布式共识]    D --&gt; D1[FLP]    D --&gt; D2[CAP]    D --&gt; D3[Paxos]    C --&gt; E[Bitcoin 共识]    E --&gt; F[直接投票方案]    F --&gt; G[需要 membership]    G --&gt; H[联盟链可行]    G --&gt; I[Bitcoin 不可行]    I --&gt; J[账户可自由创建]    J --&gt; K[Sybil Attack]    K --&gt; L[不能按账户数决定记账权]  </pre></div><p>这张图的主线是：交易广播之后，系统必须决定哪些交易进入下一个区块；这个决定不能由每个节点各自独立完成，而要形成账本层面的共识。传统分布式系统提供了一些理论背景，但 Bitcoin 是开放系统，membership 不可控，所以简单投票会被 Sybil Attack 破坏。</p><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-为什么区块链需要共识"><a href="#4-1-为什么区块链需要共识" class="headerlink" title="4.1 为什么区块链需要共识"></a>4.1 为什么区块链需要共识</h3><p>每个 Bitcoin 节点都可能接收到很多交易，其中有些交易合法，有些交易非法。问题是：</p><ul><li>哪些交易应该被写入下一个区块？</li><li>这些交易应该按什么顺序写入？</li><li>如果多个节点都想打包区块，以谁的区块为准？</li></ul><p>如果每个节点都自己决定，那么每个节点本地维护的区块链可能不同。这样就会出现一个根本问题：<strong>账本不统一</strong>。</p><p>区块链作为账本，最重要的不是“每个节点都能记账”，而是“所有诚实节点最终认可同一本账”。如果我记录的账和你记录的账不同，那么之后判断余额、验证交易、防止 double spending 都会失去统一依据。</p><p>所以本节课首先强调：Bitcoin 需要解决的是账本内容的共识问题。</p><h3 id="4-2-共识问题的直观类比：分布式哈希表"><a href="#4-2-共识问题的直观类比：分布式哈希表" class="headerlink" title="4.2 共识问题的直观类比：分布式哈希表"></a>4.2 共识问题的直观类比：分布式哈希表</h3><p>老师用一个全局哈希表作为类比：</p><p>假设系统中有很多台机器，它们共同维护一个 key-value table。某个用户在机器 A 上写入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">key = x</span><br><span class="line">value = 12345</span><br></pre></td></tr></table></figure><p>那么另一个用户在机器 B 上读取 key &#x3D; x 时，也应该读到 value &#x3D; 12345。</p><p>这说明分布式系统要解决的问题不是“某一台机器自己知道什么”，而是“所有机器对系统状态有一致认识”。</p><p>对应到区块链中：</p><table><thead><tr><th>分布式哈希表</th><th>Bitcoin</th></tr></thead><tbody><tr><td>key-value pair</td><td>交易、区块、UTXO 状态</td></tr><tr><td>全局表状态</td><td>全网账本状态</td></tr><tr><td>多台机器维护同一张表</td><td>多个节点维护同一条区块链</td></tr><tr><td>读到一致结果</td><td>验证余额和交易时得到一致判断</td></tr></tbody></table><p>所以，区块链中的共识可以理解为：多个节点对“当前账本应该是什么样子”达成一致。</p><h3 id="4-3-分布式共识为什么困难"><a href="#4-3-分布式共识为什么困难" class="headerlink" title="4.3 分布式共识为什么困难"></a>4.3 分布式共识为什么困难</h3><p>传统分布式系统中，共识问题已经研究了很多年，而且有不少“不可能结论”（impossibility result）。</p><h4 id="4-3-1-FLP-impossibility-result"><a href="#4-3-1-FLP-impossibility-result" class="headerlink" title="4.3.1 FLP impossibility result"></a>4.3.1 FLP impossibility result</h4><p>FLP impossibility result 讨论的是异步系统（asynchronous system）中的共识问题。</p><p>所谓异步系统，核心含义是：<strong>网络消息传输时间没有确定上限</strong>。</p><p>也就是说，一个消息迟迟没有到达时，节点无法判断：</p><ul><li>对方是不是宕机了？</li><li>消息是不是还在路上？</li><li>网络是不是暂时拥塞？</li></ul><p>FLP 的结论可以简化理解为：</p><blockquote><p>在一个完全异步的系统中，只要存在一个 faulty 节点，就不可能设计出一个总能保证达成共识的确定性协议。</p></blockquote><p>这里的 faulty 节点可以理解为出故障或行为异常的节点。</p><p>这个结论说明：分布式共识不是简单工程问题，而是存在理论边界。</p><h4 id="4-3-2-CAP-theorem"><a href="#4-3-2-CAP-theorem" class="headerlink" title="4.3.2 CAP theorem"></a>4.3.2 CAP theorem</h4><p>CAP theorem 涉及分布式系统想要同时满足的三个性质：</p><table><thead><tr><th>字母</th><th>英文</th><th>含义</th></tr></thead><tbody><tr><td>C</td><td>Consistency</td><td>一致性，所有节点看到的状态一致</td></tr><tr><td>A</td><td>Availability</td><td>可用性，请求总能得到响应</td></tr><tr><td>P</td><td>Partition tolerance</td><td>分区容忍性，网络分裂时系统还能继续运行</td></tr></tbody></table><p>CAP theorem 的核心结论是：</p><blockquote><p>一个分布式系统无法同时完全满足 Consistency、Availability、Partition tolerance 三者，最多只能同时满足其中两个。</p></blockquote><p>比如：</p><ul><li>想保证 Consistency 和 Availability，就很难容忍网络分区。</li><li>想保证 Availability 和 Partition tolerance，就可能牺牲 Consistency。</li><li>想保证 Consistency 和 Partition tolerance，就可能在网络异常时拒绝服务，从而牺牲 Availability。</li></ul><p>这说明分布式系统不是单纯追求“全部最好”，而是要在不同性质之间做权衡。</p><h4 id="4-3-3-Paxos-的作用与限制"><a href="#4-3-3-Paxos-的作用与限制" class="headerlink" title="4.3.3 Paxos 的作用与限制"></a>4.3.3 Paxos 的作用与限制</h4><p>Paxos 是经典分布式共识协议。它的重要特点是：</p><ul><li>如果 Paxos 达成了共识，那么这个共识一定是一致的。</li><li>不会出现一个节点认为结果是 A，另一个节点认为结果是 B 的情况。</li><li>但是在某些情况下，Paxos 可能一直无法达成共识。</li></ul><p>也就是说，Paxos 更强调 safety，即“只要有结果，结果就不会错”；但它不一定在所有情况下都能保证快速推进。</p><p>课程这里没有深入 Paxos，因为 Bitcoin 的共识机制和传统 Paxos 类协议的应用背景差别很大。</p><h3 id="4-4-Bitcoin-共识面对的特殊问题"><a href="#4-4-Bitcoin-共识面对的特殊问题" class="headerlink" title="4.4 Bitcoin 共识面对的特殊问题"></a>4.4 Bitcoin 共识面对的特殊问题</h3><p>Bitcoin 中，共识要解决的问题不仅是普通故障，还包括恶意行为。</p><p>课程中的基本假设是：</p><ul><li>系统中可能有恶意节点。</li><li>但大多数节点是诚实的。</li><li>共识协议要在这种环境下仍然维持账本可信。</li></ul><p>这和传统企业内部系统不同。企业内部的分布式系统中，节点通常是预先登记、受管理的；而 Bitcoin 是开放网络，任何人都可以参与。</p><p>因此，Bitcoin 的难点不是“节点之间怎么通信”这么简单，而是：</p><blockquote><p>在没有中心机构、没有准入审批、存在恶意节点的情况下，如何决定谁有权把交易写入区块链？</p></blockquote><h3 id="4-5-直接投票方案为什么直观"><a href="#4-5-直接投票方案为什么直观" class="headerlink" title="4.5 直接投票方案为什么直观"></a>4.5 直接投票方案为什么直观</h3><p>一个很自然的想法是：既然假设大多数节点是好的，那能不能直接投票？</p><p>大致流程如下：</p><ol><li>某个节点提出一个 candidate block。</li><li>该节点把自己认为合法的交易打包进区块。</li><li>它把这个候选区块广播给其他节点。</li><li>其他节点验证区块中的交易是否合法。</li><li>如果都合法，就投赞成票。</li><li>如果发现非法交易，就投反对票。</li><li>如果赞成票超过某个阈值，这个区块就被接受。</li></ol><p>这个方案看起来符合“大多数诚实”假设：只要多数人投赞成，区块就进入链中。</p><p>但课程接着说明：这个方案有很多问题，其中最大的问题是 membership。</p><h3 id="4-6-投票方案的细节问题"><a href="#4-6-投票方案的细节问题" class="headerlink" title="4.6 投票方案的细节问题"></a>4.6 投票方案的细节问题</h3><p>直接投票方案至少有几个实际问题。</p><h4 id="4-6-1-Denial-of-Service-Attack"><a href="#4-6-1-Denial-of-Service-Attack" class="headerlink" title="4.6.1 Denial of Service Attack"></a>4.6.1 Denial of Service Attack</h4><p>如果某个恶意节点不断提出包含非法交易的候选区块，其他节点就要不断验证、投票、否决。</p><p>这样虽然非法区块不会被接受，但系统资源会被消耗，区块链可能难以前进。</p><p>这就是一种 Denial of Service Attack，即通过制造大量无效请求或无效提案，让系统陷入处理垃圾信息的状态。</p><h4 id="4-6-2-节点不投票导致系统停滞"><a href="#4-6-2-节点不投票导致系统停滞" class="headerlink" title="4.6.2 节点不投票导致系统停滞"></a>4.6.2 节点不投票导致系统停滞</h4><p>投票方案还隐含一个前提：节点会积极参与投票。</p><p>但在开放系统中，无法强迫每个节点投票。如果很多节点不投票，系统可能一直达不到通过阈值。</p><p>这会导致区块无法被确认，账本无法继续增长。</p><h4 id="4-6-3-网络延迟影响投票效率"><a href="#4-6-3-网络延迟影响投票效率" class="headerlink" title="4.6.3 网络延迟影响投票效率"></a>4.6.3 网络延迟影响投票效率</h4><p>投票还需要等待消息传播。问题是网络延迟无法完全预测：</p><ul><li>每轮投票等多久？</li><li>等太短，可能很多诚实节点的票还没到。</li><li>等太长，系统效率很低。</li></ul><p>因此，直接投票在开放网络中效率和可推进性都存在问题。</p><p>不过这些仍然不是最根本的问题。更根本的问题是：谁有投票权？</p><h3 id="4-7-membership：投票权从哪里来"><a href="#4-7-membership：投票权从哪里来" class="headerlink" title="4.7 membership：投票权从哪里来"></a>4.7 membership：投票权从哪里来</h3><p>任何投票方案都必须先解决 membership 问题。</p><p>membership 指的是：</p><ul><li>谁是系统成员？</li><li>谁有投票权？</li><li>一个成员对应几票？</li><li>如何防止一个人伪装成多个成员？</li></ul><p>在联盟链（consortium blockchain）中，membership 是明确的。比如 Hyperledger Fabric 这类系统，只有符合条件的组织或公司才能加入。</p><p>这种情况下，投票方案是可行的，因为：</p><ul><li>成员身份是受控的。</li><li>参与者数量相对明确。</li><li>一个组织不能随便伪造大量身份。</li><li>可以假设大多数成员是诚实的。</li></ul><p>但 Bitcoin 不是联盟链。</p><h3 id="4-8-Bitcoin-为什么不能按账户投票"><a href="#4-8-Bitcoin-为什么不能按账户投票" class="headerlink" title="4.8 Bitcoin 为什么不能按账户投票"></a>4.8 Bitcoin 为什么不能按账户投票</h3><p>Bitcoin 的账户创建非常容易。</p><p>用户只需要在本地生成一对 public key 和 private key，就可以得到一个账户。这个过程：</p><ul><li>不需要中心机构批准。</li><li>不需要实名注册。</li><li>甚至不需要告诉其他节点。</li><li>只有当这个账户参与交易时，其他人才知道它的存在。</li></ul><p>这带来一个严重问题：如果按账户数投票，攻击者可以在本地生成大量账户。</p><p>比如，攻击者用一台性能很强的机器不断生成 public key &#x2F; private key pair，制造出超过全网一半数量的账户。这样它就可以在“按账户投票”的系统中获得多数票，操纵区块接受结果。</p><p>这就是女巫攻击（Sybil Attack）。</p><h3 id="4-9-Sybil-Attack-的本质"><a href="#4-9-Sybil-Attack-的本质" class="headerlink" title="4.9 Sybil Attack 的本质"></a>4.9 Sybil Attack 的本质</h3><p>Sybil Attack 的本质是：</p><blockquote><p>在一个身份创建成本很低的系统中，攻击者可以伪造大量身份，从而破坏“按身份投票”的公平性。</p></blockquote><p>这里要注意，问题不在于“账户本身不安全”。public key &#x2F; private key 机制仍然可以保证账户控制权。问题在于：</p><ul><li>账户太容易创建。</li><li>一个真实攻击者可以对应大量账户。</li><li>所以“一个账户一票”并不等于“一个真实参与者一票”。</li></ul><p>因此，Bitcoin 不能使用简单的“多数账户同意”来决定下一个区块。</p><p>本节课到这里实际上是在为后续机制铺垫：Bitcoin 需要一种不依赖真实身份数量的共识方式。</p><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-直接投票方案流程"><a href="#5-1-直接投票方案流程" class="headerlink" title="5.1 直接投票方案流程"></a>5.1 直接投票方案流程</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant Proposer as 提议节点    participant Network as Bitcoin 网络    participant Node as 其他节点    participant Chain as 区块链    Proposer-&gt;&gt;Proposer: 收集交易并构造 candidate block    Proposer-&gt;&gt;Network: 广播候选区块    Network-&gt;&gt;Node: 转发候选区块    Node-&gt;&gt;Node: 验证交易是否合法    Node-&gt;&gt;Network: 投赞成票或反对票    Network-&gt;&gt;Chain: 若赞成票超过阈值则接受区块  </pre></div><p>这张图展示的是一种直观但不适合 Bitcoin 的方案。它的问题不在于“验证交易是否合法”这一步，而在于投票权本身无法可靠分配。Bitcoin 是开放系统，账户可以任意创建，所以无法简单认为“账户多数”就代表“诚实多数”。</p><h3 id="5-2-简单投票被-Sybil-Attack-破坏"><a href="#5-2-简单投票被-Sybil-Attack-破坏" class="headerlink" title="5.2 简单投票被 Sybil Attack 破坏"></a>5.2 简单投票被 Sybil Attack 破坏</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[按账户投票] --&gt; B[每个账户一票]    B --&gt; C[Bitcoin 账户可自由创建]    C --&gt; D[攻击者批量生成密钥对]    D --&gt; E[伪造大量账户身份]    E --&gt; F[获得多数票]    F --&gt; G[操纵候选区块是否被接受]    G --&gt; H[投票共识失效]  </pre></div><p>这张图说明了 Sybil Attack 的关键逻辑：只要身份创建成本足够低，攻击者就可以把“一个人”伪装成“很多人”。因此，在 Bitcoin 中不能把账户数直接当作投票权来源。</p><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>区块链是去中心化的，所以每个节点想怎么记账都可以？</td><td>不可以。去中心化不等于各记各的账，区块链仍然需要全网对账本内容形成共识。</td></tr><tr><td>重复打包同一笔交易一定是好事吗？</td><td>如果同一笔交易出现在不同候选区块中，不一定立即是问题；真正的问题是全网最终要认可同一条账本历史，避免账本分裂。</td></tr><tr><td>共识就是大家都完全同时得到一样的信息吗？</td><td>不是。共识强调最终对某个状态达成一致，不要求所有节点在任意时刻看到完全相同的信息。</td></tr><tr><td>Paxos 能解决所有区块链共识问题吗？</td><td>不能。Paxos 适合特定分布式系统背景，而 Bitcoin 是开放、匿名、可能有恶意节点的网络，membership 问题完全不同。</td></tr><tr><td>投票方案为什么在联盟链中可以，在 Bitcoin 中不行？</td><td>联盟链有明确 membership，成员身份受控；Bitcoin 账户可自由创建，按账户投票会被 Sybil Attack 攻击。</td></tr><tr><td>一个 public key &#x2F; private key pair 是不是就代表一个真实用户？</td><td>不是。一个真实用户可以生成任意多组密钥对，所以账户数量不能代表真实人数。</td></tr><tr><td>Sybil Attack 是破解私钥吗？</td><td>不是。Sybil Attack 不是破解密码学，而是利用低成本身份创建来伪造大量投票身份。</td></tr><tr><td>“大多数节点是好的”是否足够支持按节点投票？</td><td>不一定。首先要定义什么是“节点”或“成员”。如果身份可以随便伪造，多数节点这个假设就没有意义。</td></tr></tbody></table><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-safety-和-liveness"><a href="#7-1-safety-和-liveness" class="headerlink" title="7.1 safety 和 liveness"></a>7.1 safety 和 liveness</h3><p>理解共识协议时，经常会区分两个性质：</p><table><thead><tr><th>性质</th><th>含义</th><th>简单理解</th></tr></thead><tbody><tr><td>safety</td><td>系统不会产生错误结果</td><td>不要错</td></tr><tr><td>liveness</td><td>系统最终会继续推进</td><td>不要卡住</td></tr></tbody></table><p>Paxos 的特点可以粗略理解为：如果它达成结果，结果不会互相矛盾，所以 safety 很强；但在某些极端情况下，系统可能迟迟无法决定结果，因此 liveness 可能受影响。</p><p>这和课程中提到的“Paxos 可以保证一致，但可能一直无法达成共识”是同一个方向。</p><h3 id="7-2-为什么-Bitcoin-需要把“投票权”变贵"><a href="#7-2-为什么-Bitcoin-需要把“投票权”变贵" class="headerlink" title="7.2 为什么 Bitcoin 需要把“投票权”变贵"></a>7.2 为什么 Bitcoin 需要把“投票权”变贵</h3><p>直接投票失败的根本原因是身份太便宜。</p><p>如果账户可以免费或低成本生成，那么“一个账户一票”就没有意义。要解决这个问题，开放系统通常需要让投票权和某种难以伪造的资源绑定。</p><p>在 Bitcoin 后续机制中，这个资源不是账户数量，而是计算能力。也就是说，Bitcoin 后续会从“按身份投票”转向“按工作量竞争记账权”的思路。</p><p>这里不展开后续内容，只需要先记住本节结论：</p><blockquote><p>Bitcoin 不能相信身份数量，因为身份可以伪造；它必须寻找一种更难伪造、更有成本的权重来源。</p></blockquote><h3 id="7-3-联盟链和公有链的核心区别"><a href="#7-3-联盟链和公有链的核心区别" class="headerlink" title="7.3 联盟链和公有链的核心区别"></a>7.3 联盟链和公有链的核心区别</h3><table><thead><tr><th>维度</th><th>联盟链</th><th>Bitcoin 这类公有链</th></tr></thead><tbody><tr><td>加入方式</td><td>需要许可</td><td>通常无需许可</td></tr><tr><td>membership</td><td>明确</td><td>不明确</td></tr><tr><td>身份管理</td><td>可由组织或证书系统控制</td><td>账户可自由创建</td></tr><tr><td>投票方案</td><td>可以较自然地设计</td><td>容易被 Sybil Attack 破坏</td></tr><tr><td>信任基础</td><td>对参与组织有一定信任假设</td><td>尽量减少对身份和组织的信任</td></tr></tbody></table><p>这个区别是理解本节课的关键。不是“投票机制一定不好”，而是“在 Bitcoin 这种开放系统里，简单投票不适用”。</p><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>区块链虽然是去中心化账本，但仍然必须形成统一账本历史，否则系统无法判断交易和余额。</li><li>共识问题在分布式系统中本来就很困难，FLP、CAP、Paxos 都说明了其中的理论限制和工程权衡。</li><li>Bitcoin 的共识问题更加特殊，因为它处在开放网络中，节点可能恶意，身份也没有中心化准入控制。</li><li>直接投票方案看起来自然，但会遇到 DoS、节点不投票、网络延迟等问题。</li><li>更根本的问题是 membership：如果不知道谁有投票权，就无法设计可靠投票。</li><li>Bitcoin 账户可以自由创建，所以按账户投票会遭遇 Sybil Attack，这也是后续引出 PoW 类机制的原因。</li></ol><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-为什么不能让每个节点独立决定自己的本地区块链内容？"><a href="#1-为什么不能让每个节点独立决定自己的本地区块链内容？" class="headerlink" title="1. 为什么不能让每个节点独立决定自己的本地区块链内容？"></a>1. 为什么不能让每个节点独立决定自己的本地区块链内容？</h3><p>参考答案：因为这样每个节点的账本可能不同，系统无法形成统一账本历史。区块链作为账本，必须让诚实节点最终认可同一套交易记录。</p><h3 id="2-分布式哈希表例子想说明什么？"><a href="#2-分布式哈希表例子想说明什么？" class="headerlink" title="2. 分布式哈希表例子想说明什么？"></a>2. 分布式哈希表例子想说明什么？</h3><p>参考答案：它说明分布式系统中的多个节点需要对全局状态达成一致。某台机器写入的 key-value pair，在另一台机器上也应该能读到对应结果。</p><h3 id="3-FLP-impossibility-result-的核心含义是什么？"><a href="#3-FLP-impossibility-result-的核心含义是什么？" class="headerlink" title="3. FLP impossibility result 的核心含义是什么？"></a>3. FLP impossibility result 的核心含义是什么？</h3><p>参考答案：在完全异步系统中，只要存在一个 faulty 节点，就不存在一个确定性协议能够保证一定达成共识。它说明共识问题存在理论上的困难。</p><h3 id="4-CAP-theorem-中-C、A、P-分别代表什么？"><a href="#4-CAP-theorem-中-C、A、P-分别代表什么？" class="headerlink" title="4. CAP theorem 中 C、A、P 分别代表什么？"></a>4. CAP theorem 中 C、A、P 分别代表什么？</h3><p>参考答案：C 是 Consistency，一致性；A 是 Availability，可用性；P 是 Partition tolerance，分区容忍性。CAP theorem 表明三者不能同时完全满足。</p><h3 id="5-为什么直接投票方案在-Bitcoin-中不可靠？"><a href="#5-为什么直接投票方案在-Bitcoin-中不可靠？" class="headerlink" title="5. 为什么直接投票方案在 Bitcoin 中不可靠？"></a>5. 为什么直接投票方案在 Bitcoin 中不可靠？</h3><p>参考答案：因为 Bitcoin 没有受控 membership，账户可以自由创建。攻击者可以伪造大量账户，从而操纵按账户计票的结果。</p><h3 id="6-什么是-Sybil-Attack？"><a href="#6-什么是-Sybil-Attack？" class="headerlink" title="6. 什么是 Sybil Attack？"></a>6. 什么是 Sybil Attack？</h3><p>参考答案：Sybil Attack 指攻击者伪造大量身份，使系统误以为有很多独立参与者，从而破坏按身份投票或按身份分配权力的机制。</p><h3 id="7-为什么联盟链可以更自然地使用投票类共识？"><a href="#7-为什么联盟链可以更自然地使用投票类共识？" class="headerlink" title="7. 为什么联盟链可以更自然地使用投票类共识？"></a>7. 为什么联盟链可以更自然地使用投票类共识？</h3><p>参考答案：联盟链的成员身份通常是明确且受控制的，参与者不能随意伪造大量成员身份，因此 membership 问题相对容易解决。</p><h3 id="8-本节课如何为后续-Bitcoin-共识机制做铺垫？"><a href="#8-本节课如何为后续-Bitcoin-共识机制做铺垫？" class="headerlink" title="8. 本节课如何为后续 Bitcoin 共识机制做铺垫？"></a>8. 本节课如何为后续 Bitcoin 共识机制做铺垫？</h3><p>参考答案：本节说明了 Bitcoin 不能依靠账户数量投票，因为账户身份可伪造。后续需要寻找一种更有成本、更难伪造的方式来决定记账权。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
            <tag> Consensus </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 4 讲：BTC 协议（一）</title>
      <link href="/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE1/"/>
      <url>/2026/05/30/04-BTC-%E5%8D%8F%E8%AE%AE1/</url>
      
        <content type="html"><![CDATA[<p>依据附件文字稿与笔记生成指南整理。 </p><h1 id="第-4-讲：BTC-协议（一）"><a href="#第-4-讲：BTC-协议（一）" class="headerlink" title="第 4 讲：BTC 协议（一）"></a>第 4 讲：BTC 协议（一）</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>本节从“如何设计一种数字货币”出发，引出数字货币最核心的问题：<strong>double spending attack</strong>。</li><li>单纯依靠数字签名只能证明“货币由谁发行、交易由谁授权”，但不能阻止数字文件被复制后重复花费。</li><li>中心化系统可以通过央行数据库记录每个币的归属来防止 double spending，但这依赖中心机构。</li><li>Bitcoin 的核心思路是：把“央行数据库”改造成由所有用户共同维护的 <strong>blockchain</strong>。</li><li>本节重点解释 Bitcoin transaction、hash pointer、public key hash、Bitcoin script、block header &#x2F; block body、full node &#x2F; lightweight node 之间的关系。</li></ol><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>数字签名（digital signature）</td><td>用 private key 签名，用 public key 验证</td><td>证明交易确实由币的拥有者授权</td></tr><tr><td>double spending attack</td><td>同一份数字货币被复制后重复花费</td><td>数字货币系统必须解决的核心问题</td></tr><tr><td>中心化数据库</td><td>由中心机构记录每个币当前归谁所有</td><td>可以防止 double spending，但依赖中心机构</td></tr><tr><td>区块链（blockchain）</td><td>由全体节点共同维护的交易历史数据结构</td><td>在去中心化环境中记录币的流转历史</td></tr><tr><td>铸币交易（coinbase transaction）</td><td>凭空产生新币的特殊交易</td><td>Bitcoin 中发行新币的入口</td></tr><tr><td>交易输入（transaction input）</td><td>指明本次花费的币来自之前哪笔交易输出</td><td>证明“我花的钱确实有来源”</td></tr><tr><td>交易输出（transaction output）</td><td>指明收款人的 public key hash 和金额</td><td>规定这笔钱以后由谁能花</td></tr><tr><td>哈希指针（hash pointer）</td><td>通过 hash 引用并校验前面的数据</td><td>连接区块，也用于追溯币的来源</td></tr><tr><td>Bitcoin script</td><td>Bitcoin 中验证交易合法性的脚本机制</td><td>用于检查签名、公钥和前序输出是否匹配</td></tr><tr><td>block header</td><td>区块头，存放区块宏观信息</td><td>参与区块之间的 hash pointer 链接</td></tr><tr><td>block body</td><td>区块体，存放交易列表</td><td>交易通过 Merkle tree 组织</td></tr><tr><td>Merkle tree</td><td>用 hash tree 组织多笔交易</td><td>通过 Merkle root 保护交易列表不被篡改</td></tr><tr><td>full node</td><td>保存完整区块链并验证所有交易的节点</td><td>真正参与交易合法性验证</td></tr><tr><td>lightweight node</td><td>只保存 block header 的轻节点</td><td>不能独立验证所有交易合法性</td></tr></tbody></table><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[设计数字货币] --&gt; B[中心化方案]    A --&gt; C[去中心化方案]    B --&gt; B1[央行签名发行]    B1 --&gt; B2[数字文件可复制]    B2 --&gt; B3[double spending attack]    B3 --&gt; B4[央行数据库记录归属]    B4 --&gt; B5[正确但中心化]    C --&gt; C1[Bitcoin]    C1 --&gt; C2[发行问题]    C1 --&gt; C3[交易验证问题]    C2 --&gt; C21[挖矿决定]    C3 --&gt; C31[blockchain 记录交易历史]    C31 --&gt; C32[transaction input 追溯来源]    C31 --&gt; C33[transaction output 绑定收款人]    C32 --&gt; C34[防止重复花费]    C33 --&gt; C35[public key hash 验证所有权]    C31 --&gt; D[区块结构]    D --&gt; D1[block header]    D --&gt; D2[block body]    D2 --&gt; D3[Merkle tree]  </pre></div><p>这张图展示了本节课的主线：先从中心化数字货币出发，发现 digital signature 不能解决 double spending，于是需要维护“币的历史流转记录”。中心化方案由央行维护数据库，Bitcoin 则用 blockchain 让全网共同维护这份记录。</p><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-为什么不能只用央行签名发行数字货币？"><a href="#4-1-为什么不能只用央行签名发行数字货币？" class="headerlink" title="4.1 为什么不能只用央行签名发行数字货币？"></a>4.1 为什么不能只用央行签名发行数字货币？</h3><p>最直观的数字货币方案是：</p><ol><li>央行拥有一对 public key &#x2F; private key。</li><li>央行用 private key 对一份数字文件签名。</li><li>文件内容写着“100 元，signed by central bank”。</li><li>所有人都知道央行的 public key，所以收到这份数字货币后可以验证签名。</li></ol><p>这个方案看起来像纸币：纸币有防伪标记，数字货币有央行签名。但它有一个致命问题：<strong>数字文件可以被复制</strong>。</p><p>纸币的特点是：我把 100 元纸币给你之后，我手里就没有这张纸币了。<br>数字文件的特点是：我把文件发给你之后，我仍然可以保留副本，甚至复制很多份继续发给别人。</p><p>所以，digital signature 只能证明：</p><blockquote><p>这份文件确实是央行签过名的，内容没有被篡改。</p></blockquote><p>但它不能证明：</p><blockquote><p>这份文件以前有没有被花过。</p></blockquote><p>这就是 <strong>double spending attack</strong>：同一份数字货币被重复花费。</p><h3 id="4-2-中心化方案如何解决-double-spending？"><a href="#4-2-中心化方案如何解决-double-spending？" class="headerlink" title="4.2 中心化方案如何解决 double spending？"></a>4.2 中心化方案如何解决 double spending？</h3><p>为了解决 double spending，可以给每个数字货币加上唯一编号。例如：</p><ul><li>100 元数字货币编号为 017；</li><li>50 元数字货币编号为 092。</li></ul><p>然后由央行维护一张数据库表：</p><table><thead><tr><th>货币编号</th><th>当前所有者</th></tr></thead><tbody><tr><td>017</td><td>A</td></tr><tr><td>092</td><td>B</td></tr></tbody></table><p>当 A 想把编号 017 的数字货币转给 B 时，B 不仅要验证央行签名，还要向央行确认：</p><ol><li>这个币是否真实存在；</li><li>这个币当前是不是属于 A；</li><li>这个币之前有没有被 A 花掉。</li></ol><p>如果确认合法，央行就更新数据库：</p><table><thead><tr><th>货币编号</th><th>当前所有者</th></tr></thead><tbody><tr><td>017</td><td>B</td></tr></tbody></table><p>这样，A 如果再把同一个编号 017 的币转给 C，C 去央行查询时就会发现：这个币已经不属于 A 了，所以交易无效。</p><p>这个方案在正确性上没有大问题，但它是 <strong>centralized</strong> 的：每次交易都必须经过央行确认。Bitcoin 想解决的问题就是：<strong>能不能不依赖中心机构，也能防止 double spending？</strong></p><h3 id="4-3-去中心化货币要解决的两个问题"><a href="#4-3-去中心化货币要解决的两个问题" class="headerlink" title="4.3 去中心化货币要解决的两个问题"></a>4.3 去中心化货币要解决的两个问题</h3><p>一个 decentralized cryptocurrency 至少要解决两个问题：</p><ol><li><p><strong>货币发行问题</strong><br>没有央行之后，谁有权发行货币？什么时候发行？发行多少？</p></li><li><p><strong>交易验证问题</strong><br>没有央行数据库之后，谁来判断一笔交易是否合法？怎样防止 double spending？</p></li></ol><p>本节课主要讨论第二个问题：交易验证。第一个问题在 Bitcoin 中由 <strong>mining</strong> 解决，后续课程会展开。</p><h3 id="4-4-Bitcoin-的基本思路：用-blockchain-替代央行数据库"><a href="#4-4-Bitcoin-的基本思路：用-blockchain-替代央行数据库" class="headerlink" title="4.4 Bitcoin 的基本思路：用 blockchain 替代央行数据库"></a>4.4 Bitcoin 的基本思路：用 blockchain 替代央行数据库</h3><p>中心化方案中，央行数据库记录每个币的流转历史。Bitcoin 的思路是：</p><blockquote><p>不让央行维护数据库，而是让所有节点共同维护一份公开的交易历史，这份数据结构就是 blockchain。</p></blockquote><p>在 Bitcoin 中，判断一笔交易是否合法，不是看某个中心账户余额，而是沿着交易输入向前追溯：</p><ol><li>这笔钱从哪里来？</li><li>之前有没有被花掉？</li><li>当前交易是否由有权花这笔钱的人签名？</li></ol><p>如果一笔钱已经被用作某个交易的 input，又再次被另一个交易作为 input 使用，那么后一个交易就是 double spending，节点不会接受它。</p><h3 id="4-5-Bitcoin-transaction-的-input-和-output"><a href="#4-5-Bitcoin-transaction-的-input-和-output" class="headerlink" title="4.5 Bitcoin transaction 的 input 和 output"></a>4.5 Bitcoin transaction 的 input 和 output</h3><p>Bitcoin 中每个 transaction 通常包含两部分：</p><table><thead><tr><th>部分</th><th>内容</th><th>作用</th></tr></thead><tbody><tr><td>transaction input</td><td>指向之前某笔 transaction 的 output，并提供签名、公钥等验证信息</td><td>说明“我花的钱从哪里来，以及我有权花它”</td></tr><tr><td>transaction output</td><td>指定金额和收款人的 public key hash</td><td>说明“这笔钱以后归谁控制”</td></tr></tbody></table><p>一个简化例子：</p><ol><li>某个矿工通过 coinbase transaction 创建 10 BTC 给 A。</li><li>A 把 10 BTC 分成两份，分别转给 B 和 C，每人 5 BTC。</li><li>B 再把自己的 5 BTC 分成两份，转给 C 2 BTC、转给 D 3 BTC。</li><li>此时 C 一共有 7 BTC：来自 A 的 5 BTC，加上来自 B 的 2 BTC。</li><li>C 可以把这 7 BTC 一起转给 E。</li></ol><p>这里要注意：Bitcoin 中的钱不是一个简单的“账户余额数字”，而是一系列可以被花费的 transaction output。你要花钱，就要在 input 中指出你花的是以前哪一个 output。</p><h3 id="4-6-两类-hash-pointer：连接区块与追溯币来源"><a href="#4-6-两类-hash-pointer：连接区块与追溯币来源" class="headerlink" title="4.6 两类 hash pointer：连接区块与追溯币来源"></a>4.6 两类 hash pointer：连接区块与追溯币来源</h3><p>本节课强调了两类 hash pointer：</p><table><thead><tr><th>类型</th><th>指向哪里</th><th>作用</th></tr></thead><tbody><tr><td>区块之间的 hash pointer</td><td>指向前一个 block header</td><td>把区块串成 blockchain</td></tr><tr><td>交易中的来源指针</td><td>指向之前某笔 transaction 的 output</td><td>说明本次花费的币从哪里来</td></tr></tbody></table><p>第一类 hash pointer 让区块链形成链式结构，防止历史区块被篡改。<br>第二类 hash pointer 让交易之间形成“币的来源关系”，用于检查这笔钱有没有被重复花费。</p><h3 id="4-7-为什么只验证签名还不够？"><a href="#4-7-为什么只验证签名还不够？" class="headerlink" title="4.7 为什么只验证签名还不够？"></a>4.7 为什么只验证签名还不够？</h3><p>假设 B 已经把 5 BTC 转给了 C 和 D。之后 B 又想把同一笔 5 BTC 转给 F。</p><p>B 对第二笔交易签名，这个签名本身可能是真的，因为确实是 B 发起的交易。但问题是：B 已经把这 5 BTC 花掉了。</p><p>所以节点不能只检查：</p><blockquote><p>签名是不是 B 的？</p></blockquote><p>还必须检查：</p><blockquote><p>B 声称要花的这笔钱，现在是否还没有被花过？</p></blockquote><p>这就是为什么 transaction input 必须指向币的来源。节点会沿着 input 指向的前序交易回溯，检查该 output 是否已经被使用过。如果已经被使用过，就拒绝这笔交易。</p><h3 id="4-8-收款地址、公钥和-public-key-hash"><a href="#4-8-收款地址、公钥和-public-key-hash" class="headerlink" title="4.8 收款地址、公钥和 public key hash"></a>4.8 收款地址、公钥和 public key hash</h3><p>A 要给 B 转账，需要知道 B 的地址。Bitcoin address 可以理解为从 B 的 public key 推导出来的标识，核心是 public key hash。</p><p>这里可以类比银行账号：</p><ul><li>你给别人银行转账，需要知道对方账号。</li><li>Bitcoin 转账也需要知道对方 address。</li><li>Bitcoin 系统本身不会提供“输入某个人姓名，查询他的地址”的功能。</li><li>收款人需要通过其他渠道告诉付款人地址，例如网站页面、二维码等。</li></ul><p>但是，交易验证时还有另一个问题：所有节点都要验证付款人的签名，因此所有节点都需要知道付款人的 public key。</p><p>那么付款人的 public key 从哪里来？<br>答案是：<strong>由当前 transaction input 自己给出。</strong></p><p>这看起来似乎有安全漏洞：如果攻击者把自己的 public key 冒充成 A 的 public key，再用自己的 private key 签名，是不是就能偷走 A 的钱？</p><p>答案是不能。因为前一笔 transaction output 中写的是 A 的 public key hash。当前 input 中给出的 public key 必须满足：</p><blockquote><p>hash(input 中给出的 public key) &#x3D; 前序 output 中记录的 public key hash</p></blockquote><p>如果攻击者给出自己的 public key，它的 hash 和前序 output 中记录的 A 的 public key hash 对不上，验证就不会通过。</p><p>因此，Bitcoin 的验证逻辑不是单独检查签名，而是同时检查：</p><ol><li>input 指向的币来源是否存在；</li><li>该来源是否尚未被花费；</li><li>input 中给出的 public key 是否和前序 output 中的 public key hash 匹配；</li><li>signature 是否能用这个 public key 验证通过。</li></ol><h3 id="4-9-Bitcoin-不是“加密系统”，而是“签名验证系统”"><a href="#4-9-Bitcoin-不是“加密系统”，而是“签名验证系统”" class="headerlink" title="4.9 Bitcoin 不是“加密系统”，而是“签名验证系统”"></a>4.9 Bitcoin 不是“加密系统”，而是“签名验证系统”</h3><p>课堂中有一个容易混淆的问题：能不能用 private key 加密交易，然后用 public key 解密，来证明交易来自 A？</p><p>这里要区分 encryption 和 signature：</p><table><thead><tr><th>机制</th><th>使用方式</th><th>目标</th></tr></thead><tbody><tr><td>encryption</td><td>通常用接收者 public key 加密，接收者用 private key 解密</td><td>保密，让别人看不懂内容</td></tr><tr><td>digital signature</td><td>发送者用 private key 生成签名，别人用发送者 public key 验证</td><td>认证，证明是发送者授权且内容未被篡改</td></tr></tbody></table><p>Bitcoin 的 transaction 内容本身不是为了保密，而是要公开给全网节点验证。所以 Bitcoin 不是把交易“加密隐藏起来”，而是让交易公开，并附带 signature 供所有节点验证。</p><h3 id="4-10-Bitcoin-script-如何验证交易？"><a href="#4-10-Bitcoin-script-如何验证交易？" class="headerlink" title="4.10 Bitcoin script 如何验证交易？"></a>4.10 Bitcoin script 如何验证交易？</h3><p>Bitcoin 中，transaction input 和 transaction output 都包含 script。</p><p>简化理解：</p><ul><li>前一笔 transaction output 中的 script 规定：“谁能花这笔钱？”</li><li>当前 transaction input 中的 script 提供：“我来证明我有权花这笔钱。”</li></ul><p>验证时，节点会把当前 input script 和前序 output script 拼接起来执行。如果执行成功，说明当前交易满足前一笔 output 设置的花费条件。</p><p>以常见支付为例，可以理解为：</p><ol><li><p>前序 output 中记录收款人的 public key hash；</p></li><li><p>当前 input 提供 public key 和 signature；</p></li><li><p>脚本执行时检查：</p><ul><li>public key 的 hash 是否等于前序 output 中记录的 public key hash；</li><li>signature 是否能用 public key 验证通过。</li></ul></li></ol><p>如果两个条件都满足，说明这个人确实有权花这笔钱。</p><h3 id="4-11-block-header、block-body-与-Merkle-tree"><a href="#4-11-block-header、block-body-与-Merkle-tree" class="headerlink" title="4.11 block header、block body 与 Merkle tree"></a>4.11 block header、block body 与 Merkle tree</h3><p>课堂前面的例子中，为了方便理解，好像每个 block 只包含一笔 transaction。实际 Bitcoin 系统中，一个 block 可以包含很多 transactions。</p><p>一个 block 分成两部分：</p><table><thead><tr><th>部分</th><th>包含内容</th><th>作用</th></tr></thead><tbody><tr><td>block header</td><td>version、previous block hash、Merkle root、target &#x2F; nBits、nonce 等</td><td>区块的宏观信息，参与 hash pointer 链接和 mining</td></tr><tr><td>block body</td><td>transaction list</td><td>保存该区块中的交易</td></tr></tbody></table><p>transaction list 不直接全部放进 block header，而是组织成 Merkle tree。block header 中只保存 Merkle root。</p><p>这样做的好处是：</p><ol><li>Merkle root 能代表整棵交易树；</li><li>只要 block body 中任意 transaction 被篡改，Merkle root 就会变化；</li><li>block header 的 hash 也会变化；</li><li>因此交易列表受到 block header 的保护。</li></ol><p>课堂中还特别强调：区块之间的 hash pointer 指向的是前一个 block header。也就是说，串起 blockchain 的主要是 block header，而不是整个 block body。</p><h3 id="4-12-full-node-和-lightweight-node"><a href="#4-12-full-node-和-lightweight-node" class="headerlink" title="4.12 full node 和 lightweight node"></a>4.12 full node 和 lightweight node</h3><p>Bitcoin 节点分为：</p><table><thead><tr><th>节点类型</th><th>保存内容</th><th>能否独立验证交易</th></tr></thead><tbody><tr><td>full node &#x2F; fully validating node</td><td>保存完整 blockchain，包括所有 block body 和 transactions</td><td>可以</td></tr><tr><td>lightweight node</td><td>通常只保存 block header</td><td>一般不能</td></tr></tbody></table><p>full node 可以检查一笔交易是否 double spending，因为它保存了完整历史，可以追溯之前的 transaction output 是否已经被花过。</p><p>lightweight node 只保存 block header，不保存完整交易历史，所以它通常不能独立判断某笔交易是否 double spending。它更多是利用 blockchain 信息进行查询或轻量验证。</p><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-从中心化数据库到-blockchain"><a href="#5-1-从中心化数据库到-blockchain" class="headerlink" title="5.1 从中心化数据库到 blockchain"></a>5.1 从中心化数据库到 blockchain</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    A[数字货币文件] --&gt; B[央行签名]    B --&gt; C[可以验证真伪]    C --&gt; D[但文件可复制]    D --&gt; E[double spending]    E --&gt; F[中心化方案]    F --&gt; G[央行数据库记录归属]    G --&gt; H[每次交易找央行确认]    E --&gt; I[Bitcoin 方案]    I --&gt; J[全网维护 blockchain]    J --&gt; K[交易历史公开可查]    K --&gt; L[检查币是否已被花费]  </pre></div><p>这张图说明：digital signature 解决的是“真伪问题”，不是“重复花费问题”。要防止 double spending，必须有一份记录币流转历史的数据结构。中心化系统用央行数据库，Bitcoin 用 blockchain。</p><h3 id="5-2-transaction-input-output-的来源追溯"><a href="#5-2-transaction-input-output-的来源追溯" class="headerlink" title="5.2 transaction input &#x2F; output 的来源追溯"></a>5.2 transaction input &#x2F; output 的来源追溯</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    T0[coinbase transaction&lt;br&#x2F;&gt;create 10 BTC to A] --&gt;|output: A public key hash| T1[A transfers&lt;br&#x2F;&gt;5 BTC to B&lt;br&#x2F;&gt;5 BTC to C]    T1 --&gt;|B&#39;s 5 BTC output| T2[B transfers&lt;br&#x2F;&gt;2 BTC to C&lt;br&#x2F;&gt;3 BTC to D]    T1 --&gt;|C&#39;s 5 BTC output| T3[C has 5 BTC]    T2 --&gt;|C&#39;s 2 BTC output| T4[C has total 7 BTC]    T3 --&gt; T5[C transfers 7 BTC to E]    T4 --&gt; T5  </pre></div><p>这张图表示 Bitcoin 中“钱”的来源不是账户余额，而是之前 transaction 留下的 output。C 的 7 BTC 来自两个不同的 output：A 给 C 的 5 BTC，以及 B 给 C 的 2 BTC。C 花钱时需要在 input 中同时引用这两个来源。</p><h3 id="5-3-防止伪造付款人公钥"><a href="#5-3-防止伪造付款人公钥" class="headerlink" title="5.3 防止伪造付款人公钥"></a>5.3 防止伪造付款人公钥</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[前序 output&lt;br&#x2F;&gt;记录 A public key hash] --&gt; B[当前 input&lt;br&#x2F;&gt;提供 public key]    B --&gt; C{hash(public key)&lt;br&#x2F;&gt;是否等于前序 hash}    C --&gt;|否| D[验证失败]    C --&gt;|是| E{signature&lt;br&#x2F;&gt;是否验证通过}    E --&gt;|否| F[验证失败]    E --&gt;|是| G[有权花费该 output]  </pre></div><p>这张图说明：当前 input 中的 public key 虽然是交易自己提供的，但不能随便伪造。因为它必须和前序 output 中记录的 public key hash 对得上，同时 signature 也必须能验证通过。</p><h3 id="5-4-block-header-与-block-body-的关系"><a href="#5-4-block-header-与-block-body-的关系" class="headerlink" title="5.4 block header 与 block body 的关系"></a>5.4 block header 与 block body 的关系</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    B[Block] --&gt; H[Block header]    B --&gt; Body[Block body]    H --&gt; V[version]    H --&gt; P[previous block hash]    H --&gt; M[Merkle root]    H --&gt; T[target &#x2F; nBits]    H --&gt; N[nonce]    Body --&gt; L[transaction list]    L --&gt; MT[Merkle tree]    MT --&gt; M    P --&gt; Prev[previous block header]  </pre></div><p>这张图说明：block body 里存交易列表，交易列表通过 Merkle tree 得到 Merkle root，Merkle root 存入 block header。区块之间通过 previous block hash 连接，而该 hash 指向前一个 block header。</p><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>有了央行签名的数字货币就安全了吗？</td><td>不够。签名只能防伪，不能阻止文件复制后重复花费。</td></tr><tr><td>double spending 是不是伪造签名？</td><td>不是。double spending 可以使用真实签名，问题在于同一笔钱被重复引用为 input。</td></tr><tr><td>中心化数据库方案是不是错误的？</td><td>不是。它在正确性上可行，但依赖中心机构，每次交易都要经过中心确认。</td></tr><tr><td>blockchain 是不是只用来把区块串起来？</td><td>不只是。它还保存完整交易历史，使节点能追溯币的来源并检查 double spending。</td></tr><tr><td>transaction input 是不是写付款人是谁？</td><td>不只是。更关键的是它指向之前的 transaction output，说明本次花的钱从哪里来。</td></tr><tr><td>transaction output 是不是直接写收款人名字？</td><td>不是。它通常写的是收款人的 public key hash，类似地址。</td></tr><tr><td>当前 input 里的 public key 是自己提供的，会不会随便冒充？</td><td>不能。该 public key 的 hash 必须和前序 output 中记录的 public key hash 匹配。</td></tr><tr><td>Bitcoin transaction 是加密的吗？</td><td>不是。交易内容公开，核心是 signature verification，而不是隐藏内容。</td></tr><tr><td>private key 加密、public key 解密就是签名吗？</td><td>不能这样简单理解。Bitcoin 中 signature 是专门的签名算法，目标是认证，不是保密。</td></tr><tr><td>lightweight node 能不能独立判断 double spending？</td><td>一般不能。它只保存 block header，没有完整交易历史，无法独立检查所有 input 是否已被花费。</td></tr></tbody></table><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-Bitcoin-更接近-UTXO-模型，而不是账户余额模型"><a href="#7-1-Bitcoin-更接近-UTXO-模型，而不是账户余额模型" class="headerlink" title="7.1 Bitcoin 更接近 UTXO 模型，而不是账户余额模型"></a>7.1 Bitcoin 更接近 UTXO 模型，而不是账户余额模型</h3><p>本节虽然没有正式展开 UTXO 这个术语，但实际上已经在讲 UTXO 思想。</p><p>UTXO 是 <strong>Unspent Transaction Output</strong>，意思是“尚未被花费的交易输出”。</p><p>在银行账户模型中，我们通常说：</p><blockquote><p>A 的账户余额是 10 元。</p></blockquote><p>在 Bitcoin 中，更接近这样说：</p><blockquote><p>A 拥有若干个尚未被花费的 transaction outputs，它们加起来价值 10 BTC。</p></blockquote><p>当 A 要转账时，不是直接从“余额数字”中减钱，而是选择若干个 UTXO 作为 input，并生成新的 output 给收款人。</p><h3 id="7-2-为什么-output-里放-public-key-hash，而不是直接放-public-key？"><a href="#7-2-为什么-output-里放-public-key-hash，而不是直接放-public-key？" class="headerlink" title="7.2 为什么 output 里放 public key hash，而不是直接放 public key？"></a>7.2 为什么 output 里放 public key hash，而不是直接放 public key？</h3><p>从本节课理解角度看，public key hash 的作用是把“谁能花这笔钱”绑定到一个更短、更固定的标识上。之后真正花钱时，input 再提供 public key 和 signature。</p><p>验证时检查两件事：</p><ol><li>public key 是否对应之前 output 里记录的 public key hash；</li><li>signature 是否由对应 private key 生成。</li></ol><p>这样就把“地址”和“签名验证”连接起来了。</p><h3 id="7-3-Merkle-root-为什么能保护-block-body？"><a href="#7-3-Merkle-root-为什么能保护-block-body？" class="headerlink" title="7.3 Merkle root 为什么能保护 block body？"></a>7.3 Merkle root 为什么能保护 block body？</h3><p>Merkle tree 的核心特点是：底层 transaction 的任何一点变化，都会逐层影响到上层 hash，最终改变 Merkle root。</p><p>由于 Merkle root 存在 block header 中，而 block header 又参与区块链的 hash pointer 连接，所以篡改 block body 中的 transaction 会导致：</p><ol><li>transaction hash 改变；</li><li>Merkle root 改变；</li><li>block header hash 改变；</li><li>后续区块的 previous block hash 对不上。</li></ol><p>因此，block body 虽然不直接作为区块链 hash pointer 的目标，但仍然受到 Merkle root 的保护。</p><h3 id="7-4-full-node-为什么重要？"><a href="#7-4-full-node-为什么重要？" class="headerlink" title="7.4 full node 为什么重要？"></a>7.4 full node 为什么重要？</h3><p>Bitcoin 的安全性不能只依赖“别人告诉我这笔交易合法”。<br>full node 的意义在于：它可以自己保存完整历史，自己验证每一笔交易。</p><p>这体现了 decentralized system 的一个基本思想：</p><blockquote><p>不信任单个节点，而是通过公开规则让每个节点都能独立检查。</p></blockquote><p>lightweight node 使用起来更轻便，但它牺牲了一部分独立验证能力。</p><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>数字货币的核心难点不是“如何防伪”，而是“如何防止 double spending”。</li><li>单纯的 digital signature 只能证明发行者或付款者身份，不能证明这笔钱没有被重复花费。</li><li>中心化方案可以通过数据库记录每个币的当前归属，但这依赖中心机构。</li><li>Bitcoin 用 blockchain 保存公开交易历史，让节点通过追溯 transaction input 来判断币的来源和状态。</li><li>transaction output 通过 public key hash 指定谁能花这笔钱，transaction input 通过 public key 和 signature 证明自己有权花。</li><li>block header 保存 previous block hash、Merkle root、target &#x2F; nBits、nonce 等信息；block body 保存 transaction list。</li><li>full node 保存完整数据并验证所有交易，lightweight node 只保存 block header，通常不能独立验证 double spending。</li></ol><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-为什么“央行签名的数字文件”不能直接作为数字货币？"><a href="#1-为什么“央行签名的数字文件”不能直接作为数字货币？" class="headerlink" title="1. 为什么“央行签名的数字文件”不能直接作为数字货币？"></a>1. 为什么“央行签名的数字文件”不能直接作为数字货币？</h3><p>参考答案：因为数字文件可以被复制。央行签名只能证明文件真实、内容未被篡改，但不能证明这份文件以前没有被花过，所以会产生 double spending attack。</p><h3 id="2-中心化数据库如何防止-double-spending？"><a href="#2-中心化数据库如何防止-double-spending？" class="headerlink" title="2. 中心化数据库如何防止 double spending？"></a>2. 中心化数据库如何防止 double spending？</h3><p>参考答案：给每个数字货币编号，由央行数据库记录每个编号当前归谁所有。交易时先查询该币是否属于付款人，交易成功后更新归属。如果付款人再次使用同一编号，查询会失败。</p><h3 id="3-Bitcoin-为什么需要-transaction-input-指向之前的-transaction-output？"><a href="#3-Bitcoin-为什么需要-transaction-input-指向之前的-transaction-output？" class="headerlink" title="3. Bitcoin 为什么需要 transaction input 指向之前的 transaction output？"></a>3. Bitcoin 为什么需要 transaction input 指向之前的 transaction output？</h3><p>参考答案：因为 input 要说明本次花费的钱从哪里来。节点可以沿着 input 回溯历史，检查该 output 是否存在、是否属于付款人、是否已经被花过。</p><h3 id="4-一笔交易有付款人的真实-signature，就一定合法的吗？"><a href="#4-一笔交易有付款人的真实-signature，就一定合法的吗？" class="headerlink" title="4. 一笔交易有付款人的真实 signature，就一定合法的吗？"></a>4. 一笔交易有付款人的真实 signature，就一定合法的吗？</h3><p>参考答案：不一定。signature 只能证明付款人授权了这笔交易，但还要检查这笔钱是否真实存在、是否属于付款人、是否已经被花过。</p><h3 id="5-为什么当前-transaction-input-中自己提供-public-key-不会造成冒名顶替？"><a href="#5-为什么当前-transaction-input-中自己提供-public-key-不会造成冒名顶替？" class="headerlink" title="5. 为什么当前 transaction input 中自己提供 public key 不会造成冒名顶替？"></a>5. 为什么当前 transaction input 中自己提供 public key 不会造成冒名顶替？</h3><p>参考答案：因为该 public key 的 hash 必须和前序 output 中记录的 public key hash 匹配。如果攻击者提供自己的 public key，它的 hash 对不上前序 output 中绑定的 hash，验证失败。</p><h3 id="6-Bitcoin-中-encryption-和-digital-signature-的区别是什么？"><a href="#6-Bitcoin-中-encryption-和-digital-signature-的区别是什么？" class="headerlink" title="6. Bitcoin 中 encryption 和 digital signature 的区别是什么？"></a>6. Bitcoin 中 encryption 和 digital signature 的区别是什么？</h3><p>参考答案：encryption 的目标是保密，通常用接收者 public key 加密、接收者 private key 解密。digital signature 的目标是认证和防篡改，发送者用 private key 生成 signature，其他人用发送者 public key 验证。Bitcoin transaction 主要依赖 signature，不是加密隐藏交易内容。</p><h3 id="7-block-header-和-block-body-分别保存什么？"><a href="#7-block-header-和-block-body-分别保存什么？" class="headerlink" title="7. block header 和 block body 分别保存什么？"></a>7. block header 和 block body 分别保存什么？</h3><p>参考答案：block header 保存 version、previous block hash、Merkle root、target &#x2F; nBits、nonce 等宏观信息；block body 保存 transaction list。transaction list 通过 Merkle tree 得到 Merkle root，写入 block header。</p><h3 id="8-full-node-和-lightweight-node-的核心区别是什么？"><a href="#8-full-node-和-lightweight-node-的核心区别是什么？" class="headerlink" title="8. full node 和 lightweight node 的核心区别是什么？"></a>8. full node 和 lightweight node 的核心区别是什么？</h3><p>参考答案：full node 保存完整 blockchain，并能独立验证所有交易，包括 double spending 检查；lightweight node 通常只保存 block header，没有完整交易历史，不能独立完成所有交易合法性验证。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
            <tag> Protocol </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 3 讲：BTC 数据结构</title>
      <link href="/2026/05/30/03-BTC-%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
      <url>/2026/05/30/03-BTC-%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/</url>
      
        <content type="html"><![CDATA[<h1 id="第-3-讲：BTC-数据结构"><a href="#第-3-讲：BTC-数据结构" class="headerlink" title="第 3 讲：BTC 数据结构"></a>第 3 讲：BTC 数据结构</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>本节课讲 Bitcoin 中最基础的两类数据结构：<strong>blockchain</strong> 和 <strong>Merkle tree</strong>。</li><li>两者的共同核心都是 <strong>hash pointer</strong>：既能定位数据，又能验证数据是否被篡改。</li><li>blockchain 用 hash pointer 把一个个 block 连成链，从而形成 <strong>tamper-evident log</strong>。</li><li>Merkle tree 用 hash pointer 组织一个 block 内部的 transactions，使轻节点可以高效验证某笔交易是否被包含在区块中。</li><li>本节重点不是复杂的共识机制，而是理解 Bitcoin 为什么能用数据结构本身提供“可验证性”。</li></ol><hr><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>hash pointer</td><td>保存“指向某数据的引用”和“该数据的 hash 值”</td><td>用来检测被指向数据是否被篡改</td></tr><tr><td>blockchain</td><td>由 block 组成的链表，但链接方式是 hash pointer</td><td>让任意历史 block 的修改都会影响后续 hash</td></tr><tr><td>Genesis Block</td><td>Bitcoin 系统中的第一个 block</td><td>blockchain 的起点</td></tr><tr><td>tamper-evident log</td><td>一种“篡改后会留下证据”的日志结构</td><td>blockchain 的重要性质</td></tr><tr><td>Merkle tree</td><td>用 hash pointer 构造的二叉树</td><td>组织一个 block 内部的 transaction</td></tr><tr><td>root hash</td><td>Merkle tree 根节点对应的 hash 值</td><td>摘要式代表整棵树的内容</td></tr><tr><td>block header</td><td>区块头，保存 metadata 和 Merkle root 等信息</td><td>轻节点只保存 block header</td></tr><tr><td>block body</td><td>区块体，保存 transaction list</td><td>全节点保存具体交易内容</td></tr><tr><td>full node</td><td>保存完整 block header 和 block body 的节点</td><td>能提供完整交易数据和 Merkle proof</td></tr><tr><td>light node</td><td>只保存 block header 的节点</td><td>借助 Merkle proof 验证交易是否上链</td></tr><tr><td>Merkle proof</td><td>证明某个 transaction 属于某棵 Merkle tree 的路径信息</td><td>用于 proof of membership &#x2F; proof of inclusion</td></tr><tr><td>proof of non-membership</td><td>证明某个 transaction 不在树中</td><td>普通 Merkle tree 中难以高效完成</td></tr><tr><td>sorted Merkle tree</td><td>叶节点按某种顺序排列的 Merkle tree</td><td>可以高效证明某元素不存在</td></tr></tbody></table><hr><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[Bitcoin 数据结构] --&gt; B[hash pointer]    B --&gt; C[blockchain]    B --&gt; D[Merkle tree]    C --&gt; C1[block 之间相连]    C --&gt; C2[tamper-evident log]    C --&gt; C3[只保存最新 hash 也可检测历史修改]    D --&gt; D1[组织 block 内 transactions]    D --&gt; D2[root hash 存入 block header]    D --&gt; D3[Merkle proof]    D3 --&gt; E1[proof of membership]    D3 --&gt; E2[light node 验证交易]    D --&gt; F[proof of non-membership]    F --&gt; F1[普通 Merkle tree 低效]    F --&gt; F2[sorted Merkle tree 可高效证明不存在]  </pre></div><p>这张图的主线是：<strong>hash pointer 是底层工具，blockchain 和 Merkle tree 是两个具体应用场景</strong>。blockchain 解决的是“历史记录是否被篡改”的问题；Merkle tree 解决的是“某笔交易是否包含在某个 block 中”的高效证明问题。</p><hr><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-hash-pointer：比普通-pointer-多了完整性验证"><a href="#4-1-hash-pointer：比普通-pointer-多了完整性验证" class="headerlink" title="4.1 hash pointer：比普通 pointer 多了完整性验证"></a>4.1 hash pointer：比普通 pointer 多了完整性验证</h3><p>普通 pointer 只保存某个数据结构在内存中的地址。<br>例如，一个 pointer <code>p</code> 指向某个结构体，它的作用只是“找到这个结构体在哪里”。</p><p><strong>hash pointer</strong> 则多保存一份被指向数据的 hash 值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hash pointer = 数据位置引用 + 数据内容的 hash 值</span><br></pre></td></tr></table></figure><p>这样它就有两个功能：</p><ol><li><strong>定位数据</strong>：知道数据在哪里。</li><li><strong>验证数据</strong>：重新计算数据的 hash，和 pointer 中保存的 hash 对比。</li></ol><p>如果数据内容被改了，那么重新计算出来的 hash 会发生变化，于是 hash pointer 中保存的 hash 就对不上了。</p><blockquote><p>关键理解：<br>hash pointer 不只是“指向数据”，还把“数据应该长什么样”压缩成 hash 值保存下来。</p></blockquote><hr><h3 id="4-2-blockchain：用-hash-pointer-连接-block-的链表"><a href="#4-2-blockchain：用-hash-pointer-连接-block-的链表" class="headerlink" title="4.2 blockchain：用 hash pointer 连接 block 的链表"></a>4.2 blockchain：用 hash pointer 连接 block 的链表</h3><p>Bitcoin 中的 blockchain 可以理解为一个由 block 组成的链表。<br>但它和普通链表不同：普通链表用 pointer 连接节点，blockchain 用 <strong>hash pointer</strong> 连接 block。</p><p>每个 block 中都保存指向前一个 block 的 hash pointer：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Block n 中保存 Block n-1 的 hash</span><br><span class="line">Block n-1 中保存 Block n-2 的 hash</span><br><span class="line">……</span><br></pre></td></tr></table></figure><p>最早的 block 叫做 <strong>Genesis Block</strong>。</p><p>这种结构带来的结果是：<br>如果有人修改了历史上的某个 block，那么这个 block 的 hash 会变化。于是下一个 block 中保存的 hash pointer 就对不上。为了继续伪装，他还必须修改后一个 block，再修改再后一个 block，直到最新 block。</p><p>这就是课程中说的“牵一发而动全身”。</p><hr><h3 id="4-3-tamper-evident-log：不是不能改，而是改了会被发现"><a href="#4-3-tamper-evident-log：不是不能改，而是改了会被发现" class="headerlink" title="4.3 tamper-evident log：不是不能改，而是改了会被发现"></a>4.3 tamper-evident log：不是不能改，而是改了会被发现</h3><p>blockchain 的性质可以称为 <strong>tamper-evident log</strong>。</p><p>这里要注意一个易错点：</p><blockquote><p>blockchain 并不是说数据在物理上“绝对不能被修改”。<br>它的意思是：只要修改了历史数据，就会破坏后续 hash 关系，从而留下可验证的痕迹。</p></blockquote><p>假设系统只保存最新 block 的 hash。<br>如果历史中任意一个 block 被修改，那么这个修改会沿着 hash pointer 一路传导，最终导致最新 block 的 hash 也变化。</p><p>因此，只要最新 hash 是可信的，就可以检测出整条链中是否发生过篡改。</p><hr><h3 id="4-4-为什么部分节点可以不保存完整-blockchain？"><a href="#4-4-为什么部分节点可以不保存完整-blockchain？" class="headerlink" title="4.4 为什么部分节点可以不保存完整 blockchain？"></a>4.4 为什么部分节点可以不保存完整 blockchain？</h3><p>课程中提到，有些节点不一定保存整条 blockchain，只保存最近的一部分 block。</p><p>如果它需要更早的 block，可以向其他节点请求。<br>问题是：Bitcoin 是去中心化系统，别人发来的 block 不一定可信。</p><p>这时 hash pointer 的作用就体现出来了：</p><ol><li>节点向别人请求某个旧 block。</li><li>收到后，自己计算这个 block 的 hash。</li><li>将计算结果和自己保存的后续 block 中的 hash pointer 对比。</li><li>如果一致，说明这个 block 与后续链条匹配。</li><li>如果不一致，说明数据可能被篡改或不是正确 block。</li></ol><p>也就是说，即使数据来自不可信节点，也可以通过 hash pointer 自行验证。</p><hr><h3 id="4-5-Merkle-tree：用-hash-pointer-组织-block-内部交易"><a href="#4-5-Merkle-tree：用-hash-pointer-组织-block-内部交易" class="headerlink" title="4.5 Merkle tree：用 hash pointer 组织 block 内部交易"></a>4.5 Merkle tree：用 hash pointer 组织 block 内部交易</h3><p>Bitcoin 中，每个 block 内部包含很多 transactions。<br>这些 transactions 不是随便堆在一起，而是组织成一棵 <strong>Merkle tree</strong>。</p><p>Merkle tree 的基本结构是：</p><ol><li>最底层叶节点是 transactions。</li><li>每个 transaction 先计算 hash。</li><li>相邻两个 hash 拼接后再 hash，得到父节点 hash。</li><li>一层层向上计算，最后得到 root hash。</li><li>root hash 存入 block header。</li></ol><p>简化表示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">H(tx1)      H(tx2)      H(tx3)      H(tx4)</span><br><span class="line">   \        /             \        /</span><br><span class="line">   H(H1||H2)              H(H3||H4)</span><br><span class="line">          \              /</span><br><span class="line">             root hash</span><br></pre></td></tr></table></figure><p>其中 <code>||</code> 表示拼接。</p><p>Merkle tree 的关键性质是：</p><blockquote><p>只要记住 root hash，就可以检测整棵树中任意 transaction 是否被修改。</p></blockquote><p>因为任何一个叶节点 transaction 改变，都会导致它的 hash 改变，再导致父节点 hash 改变，最后传导到 root hash。</p><hr><h3 id="4-6-block-header-与-block-body"><a href="#4-6-block-header-与-block-body" class="headerlink" title="4.6 block header 与 block body"></a>4.6 block header 与 block body</h3><p>课程中区分了一个 block 的两个部分：</p><table><thead><tr><th>部分</th><th>内容</th><th>作用</th></tr></thead><tbody><tr><td>block header</td><td>保存 Merkle root 等摘要信息</td><td>用于验证和链接区块</td></tr><tr><td>block body</td><td>保存 transaction list</td><td>存放具体交易内容</td></tr></tbody></table><p>重要的是：</p><blockquote><p>block header 中没有完整 transaction list，只有这些 transactions 组成的 Merkle tree 的 root hash。</p></blockquote><p>这使得轻节点可以只保存 block header，而不用保存所有交易细节。</p><hr><h3 id="4-7-full-node-与-light-node"><a href="#4-7-full-node-与-light-node" class="headerlink" title="4.7 full node 与 light node"></a>4.7 full node 与 light node</h3><p>Bitcoin 中节点大致可以分为：</p><table><thead><tr><th>节点类型</th><th>保存内容</th><th>能力</th></tr></thead><tbody><tr><td>full node</td><td>block header + block body</td><td>保存完整交易数据，能独立验证更多内容</td></tr><tr><td>light node</td><td>主要保存 block header</td><td>不保存完整交易列表，需要向 full node 请求证明</td></tr></tbody></table><p>例如手机上的 Bitcoin 钱包通常更接近 light node。<br>它不可能保存完整 blockchain 的全部交易数据，但它仍然希望能验证：</p><blockquote><p>某笔付款交易是否真的已经被写入 blockchain？</p></blockquote><p>这个问题就需要 <strong>Merkle proof</strong>。</p><hr><h3 id="4-8-Merkle-proof：证明某笔交易被包含在区块中"><a href="#4-8-Merkle-proof：证明某笔交易被包含在区块中" class="headerlink" title="4.8 Merkle proof：证明某笔交易被包含在区块中"></a>4.8 Merkle proof：证明某笔交易被包含在区块中</h3><p>Merkle proof 也叫：</p><ul><li><strong>proof of membership</strong></li><li><strong>proof of inclusion</strong></li></ul><p>它要证明的是：</p><blockquote><p>某个 transaction 是否属于某个 block 的 Merkle tree。</p></blockquote><p>假设 light node 想验证交易 <code>tx</code> 是否在某个 block 中。<br>它自己只有 block header，也就是只有 root hash，没有完整 Merkle tree。</p><p>于是它向 full node 请求证明。<br>full node 不需要把整棵树都发过来，只需要发送从该 transaction 到 root 路径上计算所需的“兄弟节点 hash”。</p><p>light node 收到后：</p><ol><li>先计算 <code>tx</code> 的 hash。</li><li>和 full node 提供的兄弟 hash 拼接。</li><li>继续向上计算父节点 hash。</li><li>重复这个过程，直到算出 root hash。</li><li>将算出的 root hash 与 block header 中的 root hash 对比。</li><li>如果一致，就说明该 transaction 被包含在这个 block 中。</li></ol><p>Merkle proof 的优势是复杂度低：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">如果 block 中有 n 笔交易，</span><br><span class="line">Merkle proof 只需要 O(log n) 个 hash。</span><br></pre></td></tr></table></figure><p>这比发送完整 transaction list 高效得多。</p><hr><h3 id="4-9-为什么只验证路径就够了？"><a href="#4-9-为什么只验证路径就够了？" class="headerlink" title="4.9 为什么只验证路径就够了？"></a>4.9 为什么只验证路径就够了？</h3><p>一个初学者容易产生疑问：</p><blockquote><p>light node 只能验证 transaction 所在路径上的 hash，旁边那些分支它没有完整数据，为什么还可以相信？</p></blockquote><p>核心原因是 <strong>collision resistance</strong>。</p><p>假设攻击者修改了某个 transaction，但还想让最终 root hash 不变。<br>他就必须构造出不同的数据，使得 hash 结果和原来一样。</p><p>这相当于人为制造 hash collision。<br>在安全 hash function 的 collision resistance 假设下，这是计算上不可行的。</p><p>因此，只要最终算出的 root hash 和 block header 中的 root hash 一致，light node 就可以认为这条 proof 是有效的。</p><hr><h3 id="4-10-proof-of-non-membership：证明“不存在”更难"><a href="#4-10-proof-of-non-membership：证明“不存在”更难" class="headerlink" title="4.10 proof of non-membership：证明“不存在”更难"></a>4.10 proof of non-membership：证明“不存在”更难</h3><p>Merkle proof 很适合证明某个 transaction <strong>存在</strong>。<br>但如果要证明某个 transaction <strong>不存在</strong>，普通 Merkle tree 就麻烦得多。</p><p>最直接的方法是：</p><ol><li>full node 把整棵 Merkle tree 或完整 transaction list 发给 light node。</li><li>light node 验证整棵树构造正确。</li><li>light node 检查目标 transaction 不在所有叶节点中。</li></ol><p>这样当然可以证明不存在，但复杂度是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">O(n)</span><br></pre></td></tr></table></figure><p>不再是 <code>O(log n)</code>。</p><p>原因是：<br>如果叶节点没有排序，那么目标 transaction 理论上可能出现在任意位置。<br>你不看完整交易列表，就无法确认它真的不在里面。</p><hr><h3 id="4-11-sorted-Merkle-tree：高效证明不存在"><a href="#4-11-sorted-Merkle-tree：高效证明不存在" class="headerlink" title="4.11 sorted Merkle tree：高效证明不存在"></a>4.11 sorted Merkle tree：高效证明不存在</h3><p>如果要求 Merkle tree 的叶节点按照某个规则排序，比如按照 transaction hash 从小到大排序，那么就可以更高效地做 <strong>proof of non-membership</strong>。</p><p>思路是：</p><ol><li>先计算目标 transaction 的 hash。</li><li>根据排序规则，判断它如果存在，应该位于哪两个叶节点之间。</li><li>full node 提供这两个相邻叶节点的 Merkle proof。</li><li>light node 验证这两个叶节点确实都在树中，并且它们相邻。</li><li>如果目标 hash 应该在二者之间，但实际没有出现，就证明目标 transaction 不存在。</li></ol><p>这种结构叫做 <strong>sorted Merkle tree</strong>。</p><p>不过课程中特别指出：</p><blockquote><p>Bitcoin 中没有使用 sorted Merkle tree。<br>因为 Bitcoin 没有强需求去高效证明某笔交易“不存在”。</p></blockquote><p>Bitcoin 更常见的需求是证明某笔交易“已经被包含进某个 block”。</p><hr><h3 id="4-12-hash-pointer-适合无环数据结构"><a href="#4-12-hash-pointer-适合无环数据结构" class="headerlink" title="4.12 hash pointer 适合无环数据结构"></a>4.12 hash pointer 适合无环数据结构</h3><p>课程最后提到：<br>只要数据结构是 <strong>acyclic</strong>，也就是无环的，就可以用 hash pointer 替代普通 pointer。</p><p>例如：</p><ul><li>linked list</li><li>tree</li><li>directed acyclic graph, DAG</li></ul><p>但如果数据结构中有环，就会出现问题。</p><p>比如循环链表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A -&gt; B -&gt; C -&gt; A</span><br></pre></td></tr></table></figure><p>如果这些都是普通 pointer，完全没问题。<br>但如果换成 hash pointer，就会产生循环依赖：</p><ol><li>A 的 hash pointer 依赖 B 的内容。</li><li>B 的 hash pointer 依赖 C 的内容。</li><li>C 的 hash pointer 又依赖 A 的内容。</li><li>A 的内容又还没有最终确定。</li></ol><p>这样就变成“谁都要等别人先确定”，最终无法完成 hash 计算。</p><p>所以 hash pointer 天然适合无环结构，不适合直接用于有环结构。</p><hr><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-blockchain-中的-hash-pointer-结构"><a href="#5-1-blockchain-中的-hash-pointer-结构" class="headerlink" title="5.1 blockchain 中的 hash pointer 结构"></a>5.1 blockchain 中的 hash pointer 结构</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart LR    G[Genesis Block] --&gt;|hash pointer| B1[Block 1]    B1 --&gt;|hash pointer| B2[Block 2]    B2 --&gt;|hash pointer| B3[Latest Block]    B3 --&gt; H[系统保存 latest hash]    X[修改 Genesis Block] -.导致.-&gt; G    G -.hash 改变.-&gt; B1    B1 -.hash 改变.-&gt; B2    B2 -.hash 改变.-&gt; B3    B3 -.latest hash 不匹配.-&gt; H  </pre></div><p>这张图表示：历史 block 的任何修改都会改变自身 hash，然后影响后续 block 中保存的 hash pointer，最终影响 latest hash。因此，只要 latest hash 是可信的，就可以检测历史数据是否被篡改。</p><hr><h3 id="5-2-Merkle-tree-的交易组织方式"><a href="#5-2-Merkle-tree-的交易组织方式" class="headerlink" title="5.2 Merkle tree 的交易组织方式"></a>5.2 Merkle tree 的交易组织方式</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    R[root hash]    A[H12]    B[H34]    R --&gt; A    R --&gt; B    A --&gt; H1[H tx1]    A --&gt; H2[H tx2]    B --&gt; H3[H tx3]    B --&gt; H4[H tx4]    H1 --&gt; T1[tx1]    H2 --&gt; T2[tx2]    H3 --&gt; T3[tx3]    H4 --&gt; T4[tx4]  </pre></div><p>这张图展示了 Merkle tree 的基本结构。最底层是 transactions，向上逐层计算 hash，最终得到 root hash。root hash 相当于整个交易集合的“摘要”。</p><hr><h3 id="5-3-light-node-使用-Merkle-proof-验证交易"><a href="#5-3-light-node-使用-Merkle-proof-验证交易" class="headerlink" title="5.3 light node 使用 Merkle proof 验证交易"></a>5.3 light node 使用 Merkle proof 验证交易</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    L[light node 只有 block header] --&gt; R[root hash]    L --&gt; Q[请求 tx 是否在 block 中]    F[full node] --&gt; P[返回 Merkle proof]    P --&gt; S1[tx 本身]    P --&gt; S2[兄弟节点 hash]    P --&gt; S3[路径上所需 hash]    L --&gt; C[本地自底向上计算 root hash]    C --&gt; M{是否等于 block header 中的 root hash?}    M --&gt;|是| Y[tx 被证明包含在 block 中]    M --&gt;|否| N[proof 无效]  </pre></div><p>这个机制的关键是：light node 不需要完整 block body，也不需要完整 Merkle tree，只需要少量路径信息，就可以验证某笔交易是否属于某个 block。</p><hr><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>hash pointer 是不是只保存 hash？</td><td>不是。它可以理解为“数据引用 + 数据 hash”。课程中用普通 pointer 类比，是为了说明它比 pointer 多了验证功能。</td></tr><tr><td>blockchain 是不是普通链表？</td><td>结构上像链表，但普通链表用 pointer，blockchain 用 hash pointer，因此能检测篡改。</td></tr><tr><td>blockchain 数据是不是绝对不能改？</td><td>不是物理上不能改，而是改了以后 hash 关系会断裂，容易被发现。</td></tr><tr><td>只保存 latest hash 为什么能检测整条链？</td><td>因为历史任意 block 的修改都会层层影响后续 hash，最终影响 latest hash。</td></tr><tr><td>Merkle tree 和普通 binary tree 有什么区别？</td><td>Merkle tree 的节点主要保存 hash pointer &#x2F; hash 值，用于完整性验证；普通 binary tree 主要用于组织和查找数据。</td></tr><tr><td>block header 里有没有完整交易？</td><td>没有。block header 里保存 Merkle root，具体 transaction list 在 block body 中。</td></tr><tr><td>light node 为什么能验证交易？</td><td>因为它保存 block header 中的 root hash，并通过 full node 提供的 Merkle proof 自底向上重新计算 root hash。</td></tr><tr><td>Merkle proof 能证明交易不存在吗？</td><td>普通 Merkle tree 中不能高效证明不存在；如果是 sorted Merkle tree，则可以更高效证明 non-membership。</td></tr><tr><td>为什么旁边分支的内容不用全部验证？</td><td>因为攻击者若想修改内容但保持 root hash 不变，需要制造 hash collision，在安全 hash function 下不可行。</td></tr><tr><td>hash pointer 能不能用于循环链表？</td><td>直接使用会产生循环依赖，因此 hash pointer 更适合无环数据结构。</td></tr></tbody></table><hr><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-tamper-evident-与-tamper-proof-的区别"><a href="#7-1-tamper-evident-与-tamper-proof-的区别" class="headerlink" title="7.1 tamper-evident 与 tamper-proof 的区别"></a>7.1 tamper-evident 与 tamper-proof 的区别</h3><p>本节课强调的是 blockchain 的 <strong>tamper-evident</strong> 性质，而不是绝对 <strong>tamper-proof</strong>。</p><ul><li><strong>tamper-proof</strong>：让篡改完全无法发生。</li><li><strong>tamper-evident</strong>：篡改可以尝试发生，但会留下证据。</li></ul><p>Bitcoin 的数据结构本身主要提供的是 tamper-evident。<br>至于为什么攻击者即使修改并重算后续 block 也很难成功，还需要后续课程中的 <strong>proof of work</strong> 和共识机制来解释。</p><p>本节只需要先理解：<br>hash pointer 让篡改变得“可检测”。</p><hr><h3 id="7-2-root-hash-为什么可以代表整棵树？"><a href="#7-2-root-hash-为什么可以代表整棵树？" class="headerlink" title="7.2 root hash 为什么可以代表整棵树？"></a>7.2 root hash 为什么可以代表整棵树？</h3><p>root hash 不是简单地 hash 某一笔交易，而是由所有叶节点 transaction 的 hash 层层计算得到。</p><p>因此，root hash 依赖于：</p><ol><li>每一笔 transaction 的内容；</li><li>transaction 在 Merkle tree 中的位置；</li><li>每一层 hash 的组合结果。</li></ol><p>只要其中任何一项变了，最终 root hash 通常都会变。</p><p>所以 root hash 可以被看作整棵 Merkle tree 的简短摘要。</p><hr><h3 id="7-3-Merkle-proof-为什么是-O-log-n-？"><a href="#7-3-Merkle-proof-为什么是-O-log-n-？" class="headerlink" title="7.3 Merkle proof 为什么是 O(log n)？"></a>7.3 Merkle proof 为什么是 O(log n)？</h3><p>如果 Merkle tree 是平衡二叉树，底层有 <code>n</code> 个 transactions，那么树高大约是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">log2(n)</span><br></pre></td></tr></table></figure><p>验证某个 transaction 时，只需要沿着从叶节点到 root 的路径向上计算。<br>每一层只需要一个兄弟节点 hash。</p><p>所以 proof 的大小和验证步骤数量都是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">O(log n)</span><br></pre></td></tr></table></figure><p>这就是 Merkle tree 对 light node 特别重要的原因。</p><hr><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>Bitcoin 的两个基本数据结构是 <strong>blockchain</strong> 和 <strong>Merkle tree</strong>。</li><li>它们都建立在 <strong>hash pointer</strong> 之上，用 hash 提供完整性验证。</li><li>blockchain 用 hash pointer 连接 block，使历史修改会影响后续所有 block。</li><li>Merkle tree 用 hash 组织一个 block 内部的 transactions，root hash 存在 block header 中。</li><li>light node 不保存完整交易列表，但可以通过 Merkle proof 验证某笔交易是否被包含在 block 中。</li><li>普通 Merkle tree 擅长证明 membership，但不擅长高效证明 non-membership。</li><li>sorted Merkle tree 可以更高效证明元素不存在，但 Bitcoin 中没有采用这种结构。</li><li>hash pointer 适合无环数据结构，不适合直接用于循环结构。</li></ol><hr><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-什么是-hash-pointer？它比普通-pointer-多了什么功能？"><a href="#1-什么是-hash-pointer？它比普通-pointer-多了什么功能？" class="headerlink" title="1. 什么是 hash pointer？它比普通 pointer 多了什么功能？"></a>1. 什么是 hash pointer？它比普通 pointer 多了什么功能？</h3><p>参考答案：<br>hash pointer 可以理解为“数据引用 + 数据 hash”。普通 pointer 只能定位数据，hash pointer 还能通过重新计算 hash 来验证数据是否被篡改。</p><hr><h3 id="2-为什么-blockchain-可以检测历史-block-的修改？"><a href="#2-为什么-blockchain-可以检测历史-block-的修改？" class="headerlink" title="2. 为什么 blockchain 可以检测历史 block 的修改？"></a>2. 为什么 blockchain 可以检测历史 block 的修改？</h3><p>参考答案：<br>因为每个 block 都保存前一个 block 的 hash。如果历史 block 被修改，它的 hash 会变化，导致后一个 block 中保存的 hash pointer 不匹配，并继续影响后续 block，最终影响 latest hash。</p><hr><h3 id="3-blockchain-是不是说明数据绝对不能被篡改？"><a href="#3-blockchain-是不是说明数据绝对不能被篡改？" class="headerlink" title="3. blockchain 是不是说明数据绝对不能被篡改？"></a>3. blockchain 是不是说明数据绝对不能被篡改？</h3><p>参考答案：<br>不是。数据在物理上可以被改，但改了以后 hash 关系会被破坏，容易被检测出来。更准确地说，blockchain 提供 tamper-evident 的性质。</p><hr><h3 id="4-Merkle-tree-在-Bitcoin-中用来解决什么问题？"><a href="#4-Merkle-tree-在-Bitcoin-中用来解决什么问题？" class="headerlink" title="4. Merkle tree 在 Bitcoin 中用来解决什么问题？"></a>4. Merkle tree 在 Bitcoin 中用来解决什么问题？</h3><p>参考答案：<br>Merkle tree 用来组织一个 block 中的 transactions，并通过 root hash 摘要式代表整个交易集合。它允许 light node 用 Merkle proof 高效验证某笔交易是否被包含在 block 中。</p><hr><h3 id="5-block-header-和-block-body-分别保存什么？"><a href="#5-block-header-和-block-body-分别保存什么？" class="headerlink" title="5. block header 和 block body 分别保存什么？"></a>5. block header 和 block body 分别保存什么？</h3><p>参考答案：<br>block header 保存 Merkle root 等摘要和元数据；block body 保存具体的 transaction list。light node 通常只保存 block header，full node 保存完整 block。</p><hr><h3 id="6-light-node-如何验证某笔交易已经写入区块？"><a href="#6-light-node-如何验证某笔交易已经写入区块？" class="headerlink" title="6. light node 如何验证某笔交易已经写入区块？"></a>6. light node 如何验证某笔交易已经写入区块？</h3><p>参考答案：<br>light node 向 full node 请求 Merkle proof。full node 返回该 transaction 到 root 路径上需要的兄弟节点 hash。light node 自底向上计算 root hash，并与 block header 中保存的 root hash 对比。一致则证明交易被包含。</p><hr><h3 id="7-为什么普通-Merkle-tree-难以高效证明某笔交易不存在？"><a href="#7-为什么普通-Merkle-tree-难以高效证明某笔交易不存在？" class="headerlink" title="7. 为什么普通 Merkle tree 难以高效证明某笔交易不存在？"></a>7. 为什么普通 Merkle tree 难以高效证明某笔交易不存在？</h3><p>参考答案：<br>因为叶节点没有排序时，目标交易可能出现在任意位置。除非检查完整交易列表，否则无法确认它不存在。因此普通 Merkle tree 的 non-membership proof 通常需要 O(n)。</p><hr><h3 id="8-为什么-hash-pointer-不适合直接用于循环链表？"><a href="#8-为什么-hash-pointer-不适合直接用于循环链表？" class="headerlink" title="8. 为什么 hash pointer 不适合直接用于循环链表？"></a>8. 为什么 hash pointer 不适合直接用于循环链表？</h3><p>参考答案：<br>因为会产生循环依赖。每个节点的 hash pointer 都依赖下一个节点的内容，而下一个节点又间接依赖自己，导致所有节点内容都无法先确定下来。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
            <tag> DataStructure </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第 2 讲：BTC 密码学原理</title>
      <link href="/2026/05/30/02-BTC-%E5%AF%86%E7%A0%81%E5%AD%A6%E5%8E%9F%E7%90%86/"/>
      <url>/2026/05/30/02-BTC-%E5%AF%86%E7%A0%81%E5%AD%A6%E5%8E%9F%E7%90%86/</url>
      
        <content type="html"><![CDATA[<h1 id="第-2-讲：BTC-密码学原理"><a href="#第-2-讲：BTC-密码学原理" class="headerlink" title="第 2 讲：BTC 密码学原理"></a>第 2 讲：BTC 密码学原理</h1><h2 id="1-本节主线"><a href="#1-本节主线" class="headerlink" title="1. 本节主线"></a>1. 本节主线</h2><ol><li>比特币虽然常被称为“加密货币”，但它的交易内容并不是加密隐藏的，链上交易信息整体是公开的。</li><li>比特币主要使用密码学中的两个功能：<strong>哈希（hash）</strong> 和 <strong>数字签名（digital signature）</strong>。</li><li>哈希函数在比特币中不仅用于生成摘要，还支撑了 <strong>digital commitment</strong> 和 <strong>proof of work</strong>。</li><li>数字签名用于证明“这笔交易确实由账户所有者发起”，从而解决去中心化系统中的账户授权问题。</li><li>本节的核心是理解：比特币不是靠隐藏交易内容来保证安全，而是靠哈希和签名来保证不可篡改、难伪造、易验证。</li></ol><hr><h2 id="2-核心概念速览"><a href="#2-核心概念速览" class="headerlink" title="2. 核心概念速览"></a>2. 核心概念速览</h2><table><thead><tr><th>概念</th><th>简要理解</th><th>在本节中的作用</th></tr></thead><tbody><tr><td>cryptographic hash function</td><td>密码学哈希函数，把任意长度输入映射成固定长度输出</td><td>比特币中用于摘要、承诺、挖矿</td></tr><tr><td>collision resistance</td><td>难以人为找到两个不同输入，使它们哈希值相同</td><td>支撑防篡改和 message digest</td></tr><tr><td>hash collision</td><td>两个不同输入得到相同哈希值</td><td>理论上必然存在，但难以人为制造</td></tr><tr><td>message digest</td><td>消息摘要，即对消息取哈希得到的“指纹”</td><td>用来检测消息是否被篡改</td></tr><tr><td>hiding</td><td>从哈希值难以反推出原始输入</td><td>支撑 digital commitment</td></tr><tr><td>digital commitment</td><td>数字承诺，类似电子版密封信封</td><td>先公开承诺，后公开内容，防止事后篡改</td></tr><tr><td>nonce</td><td>一次性随机数或可调随机值</td><td>在 commitment 和挖矿中用于增加随机性或试探哈希结果</td></tr><tr><td>puzzle friendly</td><td>哈希结果不可预测，只能不断尝试输入</td><td>支撑比特币挖矿机制</td></tr><tr><td>proof of work</td><td>工作量证明</td><td>通过找到满足条件的 nonce 证明做过大量计算</td></tr><tr><td>SHA-256</td><td>Secure Hash Algorithm 256-bit</td><td>比特币使用的哈希函数</td></tr><tr><td>public key</td><td>公钥，可以公开</td><td>类似比特币账户地址，用于验证签名</td></tr><tr><td>private key</td><td>私钥，必须保密</td><td>用于对交易签名，证明账户控制权</td></tr><tr><td>digital signature</td><td>数字签名</td><td>证明交易确实由私钥持有者发起</td></tr><tr><td>good source of randomness</td><td>良好的随机源</td><td>生成密钥和签名时非常关键</td></tr></tbody></table><hr><h2 id="3-知识结构图"><a href="#3-知识结构图" class="headerlink" title="3. 知识结构图"></a>3. 知识结构图</h2><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[BTC 密码学原理] --&gt; B[Hash]    A --&gt; C[Digital Signature]    B --&gt; B1[Collision Resistance]    B --&gt; B2[Hiding]    B --&gt; B3[Puzzle Friendly]    B1 --&gt; D1[Message Digest]    B2 --&gt; D2[Digital Commitment]    B3 --&gt; D3[Proof of Work]    C --&gt; C1[Public Key]    C --&gt; C2[Private Key]    C --&gt; C3[Sign and Verify]    C3 --&gt; E[证明交易由账户所有者发起]  </pre></div><p>这张图展示了本节课的整体结构：比特币使用密码学并不是为了“加密隐藏交易”，而是分别用 <strong>hash</strong> 解决数据摘要、承诺和挖矿问题，用 <strong>digital signature</strong> 解决交易授权问题。</p><hr><h2 id="4-课程内容详解"><a href="#4-课程内容详解" class="headerlink" title="4. 课程内容详解"></a>4. 课程内容详解</h2><h3 id="4-1-“加密货币”并不意味着交易内容被加密"><a href="#4-1-“加密货币”并不意味着交易内容被加密" class="headerlink" title="4.1 “加密货币”并不意味着交易内容被加密"></a>4.1 “加密货币”并不意味着交易内容被加密</h3><p>比特币常被称为 <strong>cryptocurrency</strong>，中文通常译为“加密货币”。但这里的“加密”容易造成误解。</p><p>在比特币系统中：</p><ul><li>区块链上的交易内容是公开的；</li><li>账户地址是公开可见的；</li><li>转账金额也是公开可见的；</li><li>所有人都可以查看链上的交易记录。</li></ul><p>因此，比特币中的密码学主要不是用于隐藏交易内容，而是用于解决两个问题：</p><ol><li><strong>如何保证数据不容易被篡改？</strong></li><li><strong>如何证明某笔交易确实由账户所有者发起？</strong></li></ol><p>对应的两个工具就是：</p><ul><li>哈希（hash）</li><li>数字签名（digital signature）</li></ul><hr><h3 id="4-2-Cryptographic-Hash-Function-的基本作用"><a href="#4-2-Cryptographic-Hash-Function-的基本作用" class="headerlink" title="4.2 Cryptographic Hash Function 的基本作用"></a>4.2 Cryptographic Hash Function 的基本作用</h3><p>普通哈希函数在数据结构中很常见，例如哈希表会把 key 映射到某个位置。密码学中的哈希函数要求更强，它被称为 <strong>cryptographic hash function</strong>。</p><p>它的基本形式可以写成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">h = H(x)</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>x</code> 是输入；</li><li><code>H</code> 是哈希函数；</li><li><code>h</code> 是哈希值。</li></ul><p>哈希函数的输出长度通常是固定的。例如 SHA-256 的输出长度是 256 bit。</p><p>本节重点讲了密码学哈希函数的三个性质：</p><ol><li><strong>collision resistance</strong></li><li><strong>hiding</strong></li><li><strong>puzzle friendly</strong></li></ol><p>前两个是密码学哈希函数的常见性质，第三个是比特币挖矿中特别重要的性质。</p><hr><h3 id="4-3-Collision-Resistance：不是没有碰撞，而是难以人为制造碰撞"><a href="#4-3-Collision-Resistance：不是没有碰撞，而是难以人为制造碰撞" class="headerlink" title="4.3 Collision Resistance：不是没有碰撞，而是难以人为制造碰撞"></a>4.3 Collision Resistance：不是没有碰撞，而是难以人为制造碰撞</h3><h4 id="4-3-1-什么是-hash-collision？"><a href="#4-3-1-什么是-hash-collision？" class="headerlink" title="4.3.1 什么是 hash collision？"></a>4.3.1 什么是 hash collision？</h4><p>如果存在两个不同输入 <code>x</code> 和 <code>y</code>，满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">x ≠ y</span><br><span class="line">H(x) = H(y)</span><br></pre></td></tr></table></figure><p>那么就发生了 <strong>hash collision</strong>，即哈希碰撞。</p><p>需要注意：哈希碰撞不是异常现象，而是理论上必然存在的。</p><p>原因是：</p><ul><li>输入空间可以非常大，甚至可以认为是无限的；</li><li>输出空间是有限的，例如 SHA-256 只有 <code>2^256</code> 种可能输出；</li><li>根据鸽巢原理，足够多的输入必然会映射到相同输出。</li></ul><p>所以，<strong>collision resistance 不是说哈希碰撞不存在</strong>。</p><p>它真正的意思是：</p><blockquote><p>没有高效方法可以人为找到两个不同输入，使它们产生相同哈希值。</p></blockquote><p>如果强行寻找，当然可以使用 <strong>brute force</strong>，也就是暴力枚举输入。但对于 256 bit 级别的哈希值，实际计算量极大，在现实中不可行。</p><h4 id="4-3-2-Collision-Resistance-的作用：message-digest"><a href="#4-3-2-Collision-Resistance-的作用：message-digest" class="headerlink" title="4.3.2 Collision Resistance 的作用：message digest"></a>4.3.2 Collision Resistance 的作用：message digest</h4><p>Collision resistance 可以用来生成 <strong>message digest</strong>。</p><p>比如有一个消息 <code>m</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">digest = H(m)</span><br></pre></td></tr></table></figure><p>这个 digest 可以看成消息的“指纹”。</p><p>如果有人修改了消息内容，变成 <code>m&#39;</code>，通常会导致：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(m&#x27;) ≠ H(m)</span><br></pre></td></tr></table></figure><p>攻击者如果想偷偷修改消息，又不被发现，就必须找到一个 <code>m&#39;</code>，使得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">m&#x27; ≠ m</span><br><span class="line">H(m&#x27;) = H(m)</span><br></pre></td></tr></table></figure><p>而 collision resistance 保证了这件事在现实中很难做到。</p><h4 id="4-3-3-文件校验的例子"><a href="#4-3-3-文件校验的例子" class="headerlink" title="4.3.3 文件校验的例子"></a>4.3.3 文件校验的例子</h4><p>假设你把一个大文件上传到云端。上传前，你先在本地保存它的哈希值：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">h_original = H(file)</span><br></pre></td></tr></table></figure><p>之后你从云端下载文件，再算一次：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">h_downloaded = H(downloaded_file)</span><br></pre></td></tr></table></figure><p>如果两个哈希值相同，就可以高度相信文件没有被篡改。</p><p>这里依赖的就是 collision resistance：别人很难构造一个被篡改后的文件，同时让它的哈希值和原文件完全一致。</p><h4 id="4-3-4-Collision-Resistance-不能被数学证明"><a href="#4-3-4-Collision-Resistance-不能被数学证明" class="headerlink" title="4.3.4 Collision Resistance 不能被数学证明"></a>4.3.4 Collision Resistance 不能被数学证明</h4><p>课程中特别强调：没有哪个实际使用的哈希函数可以被严格数学证明为 collision resistant。</p><p>我们通常依赖的是长期实践经验：</p><ul><li>很多密码学专家长期研究；</li><li>目前没有发现高效制造碰撞的方法；</li><li>因此认为该哈希函数在实践中是安全的。</li></ul><p>但这种安全性可能会随时间变化。例如 <strong>MD5</strong> 曾经很流行，但后来已经发现可以人为制造碰撞，因此不再被认为安全。</p><hr><h3 id="4-4-Hiding：哈希值不应泄露输入信息"><a href="#4-4-Hiding：哈希值不应泄露输入信息" class="headerlink" title="4.4 Hiding：哈希值不应泄露输入信息"></a>4.4 Hiding：哈希值不应泄露输入信息</h3><h4 id="4-4-1-Hiding-的含义"><a href="#4-4-1-Hiding-的含义" class="headerlink" title="4.4.1 Hiding 的含义"></a>4.4.1 Hiding 的含义</h4><p><strong>Hiding</strong> 指的是哈希计算是单向的：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">x -&gt; H(x)</span><br></pre></td></tr></table></figure><p>从输入 <code>x</code> 计算哈希值 <code>H(x)</code> 很容易，但从 <code>H(x)</code> 反推出 <code>x</code> 很难。</p><p>也就是说，哈希值不应该泄露原始输入的有效信息。</p><p>不过，这个性质成立需要两个前提：</p><ol><li>输入空间足够大；</li><li>输入分布足够均匀。</li></ol><p>如果输入空间太小，就可以暴力枚举。</p><p>例如，如果输入只是“明天涨停的股票代码”，股票总数有限，攻击者可以把所有可能股票都算一遍哈希，然后和公开哈希值对比，从而猜出原始内容。</p><h4 id="4-4-2-Hiding-和-brute-force-的关系"><a href="#4-4-2-Hiding-和-brute-force-的关系" class="headerlink" title="4.4.2 Hiding 和 brute force 的关系"></a>4.4.2 Hiding 和 brute force 的关系</h4><p>Hiding 不是说绝对无法反推，而是说没有高效方法反推。</p><p>如果输入空间很小，攻击者可以这样做：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">for each possible x:</span><br><span class="line">    if H(x) == target_hash:</span><br><span class="line">        guess x</span><br></pre></td></tr></table></figure><p>所以，hiding 的安全性依赖于“猜不到”。</p><p>如果原始输入本身很容易猜，那么单独取哈希并不能保证隐藏性。</p><hr><h3 id="4-5-Digital-Commitment：电子版密封信封"><a href="#4-5-Digital-Commitment：电子版密封信封" class="headerlink" title="4.5 Digital Commitment：电子版密封信封"></a>4.5 Digital Commitment：电子版密封信封</h3><h4 id="4-5-1-现实中的-sealed-envelope"><a href="#4-5-1-现实中的-sealed-envelope" class="headerlink" title="4.5.1 现实中的 sealed envelope"></a>4.5.1 现实中的 sealed envelope</h4><p>课程用“预测股票涨停”的例子解释 <strong>digital commitment</strong>。</p><p>假设某人声称自己能预测明天哪只股票会涨停。为了验证他的预测，有两个要求：</p><ol><li>他不能提前公开预测内容，否则可能影响市场；</li><li>他也不能等结果出来后再随便编一个预测。</li></ol><p>现实中可以用 <strong>sealed envelope</strong>：</p><ul><li>今天把预测写在纸上；</li><li>放进信封并封好；</li><li>交给第三方保管；</li><li>第二天再打开验证。</li></ul><p>这保证了：</p><ul><li>预测内容提前确定；</li><li>预测内容没有提前泄露；</li><li>预测内容不能事后篡改。</li></ul><h4 id="4-5-2-用-hash-实现-digital-commitment"><a href="#4-5-2-用-hash-实现-digital-commitment" class="headerlink" title="4.5.2 用 hash 实现 digital commitment"></a>4.5.2 用 hash 实现 digital commitment</h4><p>电子世界中，可以用哈希函数实现类似功能。</p><p>假设预测内容是 <code>x</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">commitment = H(x)</span><br></pre></td></tr></table></figure><p>今天先公布 <code>commitment</code>，但不公布 <code>x</code>。</p><p>第二天再公布 <code>x</code>，所有人都可以验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(x) == commitment</span><br></pre></td></tr></table></figure><p>这里同时用到了两个性质：</p><table><thead><tr><th>哈希性质</th><th>在 digital commitment 中的作用</th></tr></thead><tbody><tr><td>hiding</td><td>公布哈希值时，别人看不出原始内容</td></tr><tr><td>collision resistance</td><td>之后不能随便换一个内容来匹配原哈希</td></tr></tbody></table><h4 id="4-5-3-为什么要加-nonce？"><a href="#4-5-3-为什么要加-nonce？" class="headerlink" title="4.5.3 为什么要加 nonce？"></a>4.5.3 为什么要加 nonce？</h4><p>如果 <code>x</code> 的输入空间太小，单纯公布 <code>H(x)</code> 并不安全。</p><p>例如，预测内容只有几千只股票的可能性，攻击者可以枚举所有股票，算出每个哈希值，然后反推出预测内容。</p><p>解决办法是拼接一个随机数 <strong>nonce</strong>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">commitment = H(x || nonce)</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>x</code> 是原始内容；</li><li><code>nonce</code> 是随机数；</li><li><code>||</code> 表示拼接。</li></ul><p>这样整体输入变得更随机、更难枚举。</p><p>之后公布时，需要同时公布：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">x, nonce</span><br></pre></td></tr></table></figure><p>其他人验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(x || nonce) == commitment</span><br></pre></td></tr></table></figure><p>这样既保留了承诺功能，又增强了 hiding。</p><hr><h3 id="4-6-Puzzle-Friendly：挖矿没有捷径"><a href="#4-6-Puzzle-Friendly：挖矿没有捷径" class="headerlink" title="4.6 Puzzle Friendly：挖矿没有捷径"></a>4.6 Puzzle Friendly：挖矿没有捷径</h3><h4 id="4-6-1-Puzzle-Friendly-的含义"><a href="#4-6-1-Puzzle-Friendly-的含义" class="headerlink" title="4.6.1 Puzzle Friendly 的含义"></a>4.6.1 Puzzle Friendly 的含义</h4><p><strong>Puzzle friendly</strong> 指的是：哈希值事先不可预测。</p><p>给定一个输入，你很难提前判断它的哈希值会落在哪里。如果想找到一个满足特定条件的输入，没有明显捷径，只能不断尝试。</p><p>例如，希望找到一个输入 <code>x</code>，使得：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(x) 的前 k 位都是 0</span><br></pre></td></tr></table></figure><p>由于哈希输出看起来近似随机，你无法提前知道哪个 <code>x</code> 更可能成功，只能不断试。</p><h4 id="4-6-2-Puzzle-Friendly-和比特币挖矿"><a href="#4-6-2-Puzzle-Friendly-和比特币挖矿" class="headerlink" title="4.6.2 Puzzle Friendly 和比特币挖矿"></a>4.6.2 Puzzle Friendly 和比特币挖矿</h4><p>比特币挖矿本质上就是找一个 <strong>nonce</strong>。</p><p>矿工把 nonce 和 block header 中的其他信息放在一起取哈希：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hash = H(block header)</span><br></pre></td></tr></table></figure><p>要求结果满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hash &lt;= target</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>block header</code> 是区块头；</li><li><code>nonce</code> 是区块头中可以不断调整的字段；</li><li><code>target</code> 是目标阈值；</li><li><code>hash &lt;= target</code> 表示这个区块满足挖矿难度要求。</li></ul><p>矿工的工作就是不断尝试不同 nonce：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nonce = 0, 1, 2, 3, ...</span><br></pre></td></tr></table></figure><p>直到找到某个 nonce，使得区块头哈希值落入目标范围。</p><h4 id="4-6-3-为什么这能成为-Proof-of-Work？"><a href="#4-6-3-为什么这能成为-Proof-of-Work？" class="headerlink" title="4.6.3 为什么这能成为 Proof of Work？"></a>4.6.3 为什么这能成为 Proof of Work？</h4><p>因为 puzzle friendly 保证：</p><ul><li>找到符合条件的 nonce 很难；</li><li>没有比暴力尝试更好的捷径；</li><li>所以找到 nonce 本身就说明矿工做了大量计算。</li></ul><p>这就是 <strong>proof of work</strong> 的核心思想。</p><p>但是，验证却很容易。</p><p>别人只需要拿到区块头和 nonce，算一次哈希：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">H(block header) &lt;= target ?</span><br></pre></td></tr></table></figure><p>如果成立，就说明这个 nonce 合法。</p><p>所以 PoW 的重要性质是：</p><blockquote><p>difficult to solve, but easy to verify<br>求解很难，验证很容易。</p></blockquote><hr><h3 id="4-7-SHA-256：比特币使用的哈希函数"><a href="#4-7-SHA-256：比特币使用的哈希函数" class="headerlink" title="4.7 SHA-256：比特币使用的哈希函数"></a>4.7 SHA-256：比特币使用的哈希函数</h3><p>比特币中使用的哈希函数是 <strong>SHA-256</strong>。</p><p>SHA 是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Secure Hash Algorithm</span><br></pre></td></tr></table></figure><p>SHA-256 的输出长度是 256 bit。</p><p>在本节课语境中，SHA-256 被认为满足：</p><ol><li>collision resistance</li><li>hiding</li><li>puzzle friendly</li></ol><p>其中：</p><ul><li>collision resistance 支撑防篡改；</li><li>hiding 支撑 digital commitment；</li><li>puzzle friendly 支撑 proof of work。</li></ul><p>需要注意：课程没有要求在这里深入 SHA-256 的内部压缩函数或具体算法过程，重点是理解它在比特币系统中的作用。</p><hr><h3 id="4-8-从中心化账户到账户密钥对"><a href="#4-8-从中心化账户到账户密钥对" class="headerlink" title="4.8 从中心化账户到账户密钥对"></a>4.8 从中心化账户到账户密钥对</h3><h4 id="4-8-1-中心化系统中的账户"><a href="#4-8-1-中心化系统中的账户" class="headerlink" title="4.8.1 中心化系统中的账户"></a>4.8.1 中心化系统中的账户</h4><p>在银行这类中心化系统中，开户通常需要：</p><ul><li>带身份证件；</li><li>到银行办理手续；</li><li>由银行系统创建账户；</li><li>银行负责账户身份管理。</li></ul><p>这是中心化账户管理方式。</p><h4 id="4-8-2-比特币中的账户创建"><a href="#4-8-2-比特币中的账户创建" class="headerlink" title="4.8.2 比特币中的账户创建"></a>4.8.2 比特币中的账户创建</h4><p>比特币是去中心化系统，没有银行这样的中心机构给你开户。</p><p>在比特币中，创建账户的方式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">本地生成一对 public key 和 private key</span><br></pre></td></tr></table></figure><p>这对密钥就代表一个账户。</p><p>可以粗略类比为：</p><table><thead><tr><th>比特币概念</th><th>日常类比</th></tr></thead><tbody><tr><td>public key</td><td>银行账号，别人可以知道</td></tr><tr><td>private key</td><td>账户密码，必须自己保管</td></tr></tbody></table><p>别人要给你转账，需要知道你的 public key 或由它派生出的地址。</p><p>你要花费账户里的钱，需要使用 private key 对交易进行签名。</p><hr><h3 id="4-9-对称加密与非对称加密"><a href="#4-9-对称加密与非对称加密" class="headerlink" title="4.9 对称加密与非对称加密"></a>4.9 对称加密与非对称加密</h3><h4 id="4-9-1-Symmetric-Encryption-Algorithm"><a href="#4-9-1-Symmetric-Encryption-Algorithm" class="headerlink" title="4.9.1 Symmetric Encryption Algorithm"></a>4.9.1 Symmetric Encryption Algorithm</h4><p>最早的加密体系是 <strong>symmetric encryption algorithm</strong>，即对称加密。</p><p>特点是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">加密和解密使用同一个 key</span><br></pre></td></tr></table></figure><p>例如，A 要给 B 发消息：</p><ol><li>A 和 B 事先共享一个密钥；</li><li>A 用这个密钥加密消息；</li><li>B 收到后用同一个密钥解密。</li></ol><p>问题是：密钥如何安全分发？</p><p>如果网络本身可能被窃听，就不能直接把密钥明文发过去。因此，对称加密的一个难点是密钥分发。</p><h4 id="4-9-2-Asymmetric-Encryption-Algorithm"><a href="#4-9-2-Asymmetric-Encryption-Algorithm" class="headerlink" title="4.9.2 Asymmetric Encryption Algorithm"></a>4.9.2 Asymmetric Encryption Algorithm</h4><p>为了解决密钥分发问题，出现了 <strong>asymmetric encryption algorithm</strong>，即非对称加密。</p><p>它使用一对密钥：</p><ul><li>public key</li><li>private key</li></ul><p>在加密通信场景中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">用接收方 public key 加密</span><br><span class="line">用接收方 private key 解密</span><br></pre></td></tr></table></figure><p>public key 可以公开，private key 只保存在接收方本地。</p><p>这就避免了双方必须提前秘密共享同一个密钥的问题。</p><hr><h3 id="4-10-比特币中-public-key-private-key-主要用于签名"><a href="#4-10-比特币中-public-key-private-key-主要用于签名" class="headerlink" title="4.10 比特币中 public key&#x2F;private key 主要用于签名"></a>4.10 比特币中 public key&#x2F;private key 主要用于签名</h3><p>课程中特别指出：比特币的交易内容是公开的，所以 public key 和 private key 在比特币中主要不是为了加密交易内容，而是为了做 <strong>digital signature</strong>。</p><h4 id="4-10-1-签名要解决什么问题？"><a href="#4-10-1-签名要解决什么问题？" class="headerlink" title="4.10.1 签名要解决什么问题？"></a>4.10.1 签名要解决什么问题？</h4><p>假设 A 发布了一笔交易：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A 转 10 BTC 给 B</span><br></pre></td></tr></table></figure><p>其他节点需要判断：</p><blockquote><p>这笔交易真的是 A 发起的吗？</p></blockquote><p>如果没有签名，攻击者可以伪造交易，声称：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">A 转 10 BTC 给攻击者</span><br></pre></td></tr></table></figure><p>数字签名的作用就是防止这种冒名操作。</p><h4 id="4-10-2-签名和验证的流程"><a href="#4-10-2-签名和验证的流程" class="headerlink" title="4.10.2 签名和验证的流程"></a>4.10.2 签名和验证的流程</h4><p>比特币交易签名的基本逻辑是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">signature = Sign(private key, transaction)</span><br></pre></td></tr></table></figure><p>然后其他人使用对应 public key 验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Verify(public key, transaction, signature)</span><br></pre></td></tr></table></figure><p>如果验证通过，说明：</p><ol><li>这笔交易确实由 private key 持有者签名；</li><li>交易内容没有被篡改；</li><li>签名者不能轻易否认自己签过这笔交易。</li></ol><p>需要注意：</p><ul><li><strong>签名用 private key</strong>；</li><li><strong>验证签名用 public key</strong>；</li><li>它们属于同一个用户。</li></ul><hr><h3 id="4-11-为什么随机性非常重要？"><a href="#4-11-为什么随机性非常重要？" class="headerlink" title="4.11 为什么随机性非常重要？"></a>4.11 为什么随机性非常重要？</h3><p>比特币账户由本地生成的 public key&#x2F;private key pair 表示。</p><p>这引出一个问题：</p><blockquote><p>如果两个人随机生成了相同的密钥对怎么办？</p></blockquote><p>理论上确实存在这种可能，但如果密钥空间足够大，并且随机源足够好，那么概率小到可以忽略。</p><p>但这里有一个关键前提：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">a good source of randomness</span><br></pre></td></tr></table></figure><p>也就是生成密钥时必须有良好的随机源。</p><p>如果随机源不好，可能出现严重后果：</p><ol><li>不同用户生成相同或相关的密钥；</li><li>攻击者能够猜测 private key；</li><li>在某些签名算法中，签名过程随机性不足可能泄露 private key。</li></ol><p>因此，本节最后强调：不仅生成密钥时需要好的随机源，签名时也可能需要好的随机源。只要 private key 泄露，账户里的币就可能被转走。</p><hr><h3 id="4-12-先-hash，再签名"><a href="#4-12-先-hash，再签名" class="headerlink" title="4.12 先 hash，再签名"></a>4.12 先 hash，再签名</h3><p>课程最后提到，比特币系统中通常不是直接对完整 message 签名，而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">digest = H(message)</span><br><span class="line">signature = Sign(private key, digest)</span><br></pre></td></tr></table></figure><p>也就是：</p><ol><li>先对 message 取 hash；</li><li>得到固定长度的 digest；</li><li>再对 digest 进行签名。</li></ol><p>这样做的直观原因是：</p><ul><li>hash 可以把任意长度 message 压缩成固定长度摘要；</li><li>签名算法处理固定长度 digest 更方便；</li><li>digest 和原 message 强绑定，message 一变，digest 也会变，签名验证就无法通过。</li></ul><hr><h2 id="5-关键机制图解"><a href="#5-关键机制图解" class="headerlink" title="5. 关键机制图解"></a>5. 关键机制图解</h2><h3 id="5-1-Digital-Commitment-机制"><a href="#5-1-Digital-Commitment-机制" class="headerlink" title="5.1 Digital Commitment 机制"></a>5.1 Digital Commitment 机制</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant A as 承诺者    participant Public as 公开环境    participant V as 验证者    A-&gt;&gt;A: 准备内容 x    A-&gt;&gt;A: 选择随机 nonce    A-&gt;&gt;A: 计算 H(x || nonce)    A-&gt;&gt;Public: 公开 commitment    Note over Public: 此时看不到 x    A-&gt;&gt;Public: 之后公开 x 和 nonce    V-&gt;&gt;V: 重新计算 H(x || nonce)    V-&gt;&gt;Public: 对比是否等于 commitment  </pre></div><p>这个机制解决的是“提前确定但暂不公开”的问题。<br><strong>Hiding</strong> 保证公开 commitment 时别人难以知道内容，<strong>collision resistance</strong> 保证之后不能随意换内容。</p><hr><h3 id="5-2-Proof-of-Work-挖矿机制"><a href="#5-2-Proof-of-Work-挖矿机制" class="headerlink" title="5.2 Proof of Work 挖矿机制"></a>5.2 Proof of Work 挖矿机制</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  flowchart TD    A[准备 block header] --&gt; B[设置 nonce]    B --&gt; C[计算 H(block header)]    C --&gt; D{hash &lt;&#x3D; target?}    D -- 否 --&gt; E[更换 nonce]    E --&gt; C    D -- 是 --&gt; F[找到合法区块]    F --&gt; G[广播区块]    G --&gt; H[其他节点验证一次 hash]  </pre></div><p>这张图体现了 PoW 的核心：<br>矿工需要不断尝试 nonce，直到哈希值小于等于 target。这个过程求解困难，但其他节点验证时只需要算一次哈希，因此验证成本很低。</p><hr><h3 id="5-3-数字签名验证交易"><a href="#5-3-数字签名验证交易" class="headerlink" title="5.3 数字签名验证交易"></a>5.3 数字签名验证交易</h3><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  sequenceDiagram    participant User as 账户所有者    participant Tx as 交易    participant Network as 比特币网络节点    User-&gt;&gt;Tx: 构造交易内容    User-&gt;&gt;User: 用 private key 签名    User-&gt;&gt;Network: 广播交易和 signature    Network-&gt;&gt;Network: 用 public key 验证 signature    alt 验证通过        Network-&gt;&gt;Network: 接受交易进入后续处理    else 验证失败        Network-&gt;&gt;Network: 拒绝交易    end  </pre></div><p>这张图说明 digital signature 的功能不是隐藏交易，而是证明交易授权。交易本身可以公开，但只有 private key 持有者才能生成有效签名。</p><hr><h2 id="6-易混点与常见误解"><a href="#6-易混点与常见误解" class="headerlink" title="6. 易混点与常见误解"></a>6. 易混点与常见误解</h2><table><thead><tr><th>易混点</th><th>正确理解</th></tr></thead><tbody><tr><td>“加密货币”是不是交易内容都被加密？</td><td>不是。比特币交易内容整体公开，“加密”主要指使用密码学机制保证安全性。</td></tr><tr><td>collision resistance 是不是表示不会发生碰撞？</td><td>不是。碰撞理论上必然存在，只是难以人为找到。</td></tr><tr><td>hash collision 和哈希表冲突是不是一回事？</td><td>思路相似，都是不同输入映射到同一输出；但密码学关注的是攻击者能否高效制造碰撞。</td></tr><tr><td>hiding 是不是绝对无法反推输入？</td><td>不是。若输入空间小或分布不均匀，可以 brute force 猜出来。</td></tr><tr><td>只对预测内容取 hash 就一定能隐藏预测吗？</td><td>不一定。如果预测范围很小，需要拼接 nonce 增加随机性。</td></tr><tr><td>puzzle friendly 和 collision resistance 是不是完全一样？</td><td>不是。collision resistance 关注难以找两个相同哈希的输入；puzzle friendly 关注难以提前预测哪个输入能落入目标范围。</td></tr><tr><td>挖矿是不是在“解密”？</td><td>不是。挖矿是在不断尝试 nonce，让 block header 的哈希值满足 target。</td></tr><tr><td>public key 是不是用来签名的？</td><td>不是。签名用 private key，验证签名用 public key。</td></tr><tr><td>private key 是不是可以发给别人验证？</td><td>绝对不应该。private key 一旦泄露，账户资产就可能被转走。</td></tr><tr><td>比特币使用 public&#x2F;private key 是为了加密交易金额吗？</td><td>不是。主要是为了 digital signature，证明交易由账户所有者发起。</td></tr></tbody></table><hr><h2 id="7-课堂外补充理解"><a href="#7-课堂外补充理解" class="headerlink" title="7. 课堂外补充理解"></a>7. 课堂外补充理解</h2><h3 id="7-1-Hash-的三个常见安全性质可以分层理解"><a href="#7-1-Hash-的三个常见安全性质可以分层理解" class="headerlink" title="7.1 Hash 的三个常见安全性质可以分层理解"></a>7.1 Hash 的三个常见安全性质可以分层理解</h3><p>本节重点讲了 collision resistance、hiding、puzzle friendly。为了避免混淆，可以这样理解：</p><table><thead><tr><th>性质</th><th>关注的问题</th><th>直观问题</th></tr></thead><tbody><tr><td>hiding</td><td>从输出能不能推出输入？</td><td>看到 <code>H(x)</code> 后能不能猜到 <code>x</code>？</td></tr><tr><td>collision resistance</td><td>能不能找到两个输入哈希值相同？</td><td>能不能找 <code>x ≠ y</code> 但 <code>H(x)=H(y)</code>？</td></tr><tr><td>puzzle friendly</td><td>能不能预测哪个输入会满足目标？</td><td>能不能不靠试错就找到合格 nonce？</td></tr></tbody></table><p>它们都和“哈希输出看起来随机”有关，但解决的问题不同。</p><hr><h3 id="7-2-为什么签名通常签-hash，而不是直接签原文？"><a href="#7-2-为什么签名通常签-hash，而不是直接签原文？" class="headerlink" title="7.2 为什么签名通常签 hash，而不是直接签原文？"></a>7.2 为什么签名通常签 hash，而不是直接签原文？</h3><p>直接对完整 message 签名通常不方便，因为 message 可能很长，格式也可能复杂。</p><p>先 hash 再签名有几个好处：</p><ol><li>把任意长度 message 变成固定长度 digest；</li><li>签名算法只需要处理 digest；</li><li>message 一旦被修改，digest 就会变化；</li><li>验证签名时可以重新计算 digest，检查签名是否匹配。</li></ol><p>所以可以把 digest 理解为“被签名的消息指纹”。</p><hr><h3 id="7-3-“账户”在比特币里不是由机构创建的"><a href="#7-3-“账户”在比特币里不是由机构创建的" class="headerlink" title="7.3 “账户”在比特币里不是由机构创建的"></a>7.3 “账户”在比特币里不是由机构创建的</h3><p>在银行系统中，账户存在于银行数据库里，由银行确认身份并分配账户。</p><p>在比特币中，账户更像是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">谁拥有某个 private key，谁就控制对应资产</span><br></pre></td></tr></table></figure><p>所以，比特币账户安全的核心不是“平台帮你找回密码”，而是：</p><ul><li>private key 不能丢；</li><li>private key 不能泄露；</li><li>随机源必须可靠。</li></ul><p>这也解释了为什么去中心化系统中，用户自主管理密钥非常重要。</p><hr><h2 id="8-本节小结"><a href="#8-本节小结" class="headerlink" title="8. 本节小结"></a>8. 本节小结</h2><ol><li>比特币不是通过加密隐藏交易内容来保证安全，链上交易信息是公开的。</li><li>比特币主要使用两个密码学工具：hash 和 digital signature。</li><li>Hash 的 collision resistance 支撑 message digest 和防篡改。</li><li>Hash 的 hiding 与 collision resistance 结合，可以实现 digital commitment。</li><li>Hash 的 puzzle friendly 支撑 proof of work，使挖矿“难求解、易验证”。</li><li>Digital signature 通过 private key 签名、public key 验证，解决去中心化账户系统中的交易授权问题。</li><li>良好的随机源对密钥生成和签名过程非常关键，随机性不足可能导致 private key 泄露。</li></ol><hr><h2 id="9-自测问题"><a href="#9-自测问题" class="headerlink" title="9. 自测问题"></a>9. 自测问题</h2><h3 id="1-为什么说比特币虽然叫“加密货币”，但交易内容并不是加密的？"><a href="#1-为什么说比特币虽然叫“加密货币”，但交易内容并不是加密的？" class="headerlink" title="1. 为什么说比特币虽然叫“加密货币”，但交易内容并不是加密的？"></a>1. 为什么说比特币虽然叫“加密货币”，但交易内容并不是加密的？</h3><p>参考答案：<br>因为比特币链上的交易记录、账户地址和转账金额都是公开可见的。这里的“加密”主要指它使用了密码学工具，如 hash 和 digital signature，而不是把交易内容隐藏起来。</p><hr><h3 id="2-Collision-resistance-是不是表示哈希碰撞不存在？"><a href="#2-Collision-resistance-是不是表示哈希碰撞不存在？" class="headerlink" title="2. Collision resistance 是不是表示哈希碰撞不存在？"></a>2. Collision resistance 是不是表示哈希碰撞不存在？</h3><p>参考答案：<br>不是。由于输入空间大于输出空间，哈希碰撞理论上必然存在。Collision resistance 的意思是：攻击者很难高效地人为找到一组碰撞输入。</p><hr><h3 id="3-Message-digest-如何用于检测文件是否被篡改？"><a href="#3-Message-digest-如何用于检测文件是否被篡改？" class="headerlink" title="3. Message digest 如何用于检测文件是否被篡改？"></a>3. Message digest 如何用于检测文件是否被篡改？</h3><p>参考答案：<br>上传文件前先保存文件的哈希值，下载后重新计算哈希值。如果两个哈希值一致，就可以高度相信文件未被篡改；如果不同，则说明文件内容发生了变化。</p><hr><h3 id="4-Hiding-为什么要求输入空间足够大、分布足够均匀？"><a href="#4-Hiding-为什么要求输入空间足够大、分布足够均匀？" class="headerlink" title="4. Hiding 为什么要求输入空间足够大、分布足够均匀？"></a>4. Hiding 为什么要求输入空间足够大、分布足够均匀？</h3><p>参考答案：<br>如果输入空间太小，攻击者可以枚举所有可能输入，逐个计算哈希值并对比目标哈希，从而反推出原始输入。输入分布不均匀时，攻击者也可以优先猜高概率输入。</p><hr><h3 id="5-Digital-commitment-为什么要同时依赖-hiding-和-collision-resistance？"><a href="#5-Digital-commitment-为什么要同时依赖-hiding-和-collision-resistance？" class="headerlink" title="5. Digital commitment 为什么要同时依赖 hiding 和 collision resistance？"></a>5. Digital commitment 为什么要同时依赖 hiding 和 collision resistance？</h3><p>参考答案：<br>Hiding 保证承诺公布时别人看不出原始内容；collision resistance 保证承诺者之后不能换一个不同内容来匹配原来的哈希值。</p><hr><h3 id="6-比特币挖矿为什么可以被称为-proof-of-work？"><a href="#6-比特币挖矿为什么可以被称为-proof-of-work？" class="headerlink" title="6. 比特币挖矿为什么可以被称为 proof of work？"></a>6. 比特币挖矿为什么可以被称为 proof of work？</h3><p>参考答案：<br>因为矿工必须不断尝试 nonce，使 block header 的哈希值小于等于 target。由于 puzzle friendly，这个过程没有捷径，找到合法 nonce 就说明矿工做过大量计算。</p><hr><h3 id="7-为什么说-PoW-是-difficult-to-solve-but-easy-to-verify？"><a href="#7-为什么说-PoW-是-difficult-to-solve-but-easy-to-verify？" class="headerlink" title="7. 为什么说 PoW 是 difficult to solve but easy to verify？"></a>7. 为什么说 PoW 是 difficult to solve but easy to verify？</h3><p>参考答案：<br>求解时需要不断尝试大量 nonce，计算成本高；但验证时只需要对给定 block header 算一次哈希并检查是否满足 target，成本很低。</p><hr><h3 id="8-比特币中-private-key-和-public-key-分别起什么作用？"><a href="#8-比特币中-private-key-和-public-key-分别起什么作用？" class="headerlink" title="8. 比特币中 private key 和 public key 分别起什么作用？"></a>8. 比特币中 private key 和 public key 分别起什么作用？</h3><p>参考答案：<br>Private key 用于对交易签名，证明账户所有者授权了这笔交易；public key 用于验证签名。Private key 必须保密，public key 可以公开。</p>]]></content>
      
      
      <categories>
          
          <category> blockchain </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BTC </tag>
            
            <tag> Bitcoin </tag>
            
            <tag> Cryptography </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>我的日常学习ai输出规范指南</title>
      <link href="/2026/05/13/%E6%88%91%E7%9A%84%E6%97%A5%E5%B8%B8%E5%AD%A6%E4%B9%A0ai%E8%BE%93%E5%87%BA%E8%A7%84%E8%8C%83%E6%8C%87%E5%8D%97/"/>
      <url>/2026/05/13/%E6%88%91%E7%9A%84%E6%97%A5%E5%B8%B8%E5%AD%A6%E4%B9%A0ai%E8%BE%93%E5%87%BA%E8%A7%84%E8%8C%83%E6%8C%87%E5%8D%97/</url>
      
        <content type="html"><![CDATA[<h1 id="学习指南-md"><a href="#学习指南-md" class="headerlink" title="学习指南.md"></a>学习指南.md</h1><h2 id="1-文件用途"><a href="#1-文件用途" class="headerlink" title="1. 文件用途"></a>1. 文件用途</h2><p>本文件用于规范 AI 在辅助我学习课程内容时的输出风格与结构。<br>当我说“我想通过学习指南了解某个章节 &#x2F; 某份课件 &#x2F; 某个知识点”时，AI 应按照本规则生成稳定、一致、可复用的学习内容。</p><p>目标不是简单总结课件，而是帮助我真正理解：</p><ul><li>这个知识点在解决什么问题</li><li>它为什么会被提出</li><li>它的核心概念是什么</li><li>它和前后知识有什么关系</li><li>它容易和哪些概念混淆</li><li>我是否真的掌握了它</li></ul><hr><h2 id="2-AI-的角色定位"><a href="#2-AI-的角色定位" class="headerlink" title="2. AI 的角色定位"></a>2. AI 的角色定位</h2><p>你是我的课程学习助教。</p><p>你的任务不是直接堆砌定义，而是用“学习指南”的方式带我学习。<br>输出应当像一份清晰的课堂讲义，适合我从零或半懂状态开始理解一个章节。</p><p>你需要做到：</p><ol><li>先搭建知识框架，再讲细节。</li><li>先解释“为什么需要它”，再解释“它是什么”。</li><li>尽量使用直白语言，但保留必要的专业术语。</li><li>不要只给结论，要解释背后的因果关系。</li><li>对容易混淆的地方进行专门对比。</li><li>最后给出自测题，用于检验我是否掌握。</li></ol><hr><h2 id="3-输入形式"><a href="#3-输入形式" class="headerlink" title="3. 输入形式"></a>3. 输入形式</h2><p>我可能会这样提出请求：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">我想要通过学习指南了解 第八周：进程间通信</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">我想通过学习指南了解 chapter 3.1</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">请用学习指南的方式讲解 虚拟内存管理 I</span><br></pre></td></tr></table></figure><p>AI 应根据我指定的课件、章节或知识点生成学习指南。</p><p>如果我明确说“自测题先不用给答案”，则不要给出自测题答案。<br>如果我没有特别说明，默认只给自测题，不给答案。</p><hr><h2 id="4-总体输出结构"><a href="#4-总体输出结构" class="headerlink" title="4. 总体输出结构"></a>4. 总体输出结构</h2><p>每次生成学习指南时，建议使用以下固定结构。</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># XX 学习指南</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 0. 这一章到底在解决什么问题？</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 1. 本节知识地图</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 2. 核心概念一：……</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 3. 核心概念二：……</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 4. 核心概念三：……</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 5. 关键机制 / 运行流程</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 6. 重要对比</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 7. 最容易混淆的点</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 8. 本节一句话总复习</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 9. 自测题，先不放答案</span></span><br></pre></td></tr></table></figure><p>如果章节内容较多，可以增加小节，但整体顺序应保持一致。</p><hr><h2 id="5-开头部分规则"><a href="#5-开头部分规则" class="headerlink" title="5. 开头部分规则"></a>5. 开头部分规则</h2><h3 id="5-1-先说明本章主线"><a href="#5-1-先说明本章主线" class="headerlink" title="5.1 先说明本章主线"></a>5.1 先说明本章主线</h3><p>开头必须先回答：</p><blockquote><p>这一章到底在解决什么问题？</p></blockquote><p>不要直接进入定义。</p><p>例如讲“进程间通信 IPC”时，不要一上来就说：</p><blockquote><p>IPC 是 Inter-Process Communication。</p></blockquote><p>而应该先说：</p><blockquote><p>进程天然具有隔离性，这保证了安全性，但也让进程之间难以协作。IPC 就是为了解决“隔离进程如何通信与协作”的问题。</p></blockquote><h3 id="5-2-明确本章关键词"><a href="#5-2-明确本章关键词" class="headerlink" title="5.2 明确本章关键词"></a>5.2 明确本章关键词</h3><p>开头应列出本章关键词，例如：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">本章关键词：</span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> 共享内存</span><br><span class="line"><span class="bullet">-</span> 消息传递</span><br><span class="line"><span class="bullet">-</span> 直接通信</span><br><span class="line"><span class="bullet">-</span> 间接通信</span><br><span class="line"><span class="bullet">-</span> 管道</span><br><span class="line"><span class="bullet">-</span> 消息队列</span><br></pre></td></tr></table></figure><hr><h2 id="6-正文讲解规则"><a href="#6-正文讲解规则" class="headerlink" title="6. 正文讲解规则"></a>6. 正文讲解规则</h2><h3 id="6-1-每个核心概念按四步讲"><a href="#6-1-每个核心概念按四步讲" class="headerlink" title="6.1 每个核心概念按四步讲"></a>6.1 每个核心概念按四步讲</h3><p>每个核心概念尽量遵循：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## N. 概念名</span></span><br><span class="line"></span><br><span class="line"><span class="section">### N.1 它是什么？</span></span><br><span class="line"></span><br><span class="line"><span class="section">### N.2 为什么需要它？</span></span><br><span class="line"></span><br><span class="line"><span class="section">### N.3 它是怎么工作的？</span></span><br><span class="line"></span><br><span class="line"><span class="section">### N.4 它的问题 / 代价 / 易错点</span></span><br></pre></td></tr></table></figure><p>例如讲“共享内存”：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 共享内存</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 它是什么？</span></span><br><span class="line"></span><br><span class="line">共享内存就是操作系统把同一块物理内存映射到多个进程的虚拟地址空间中。</span><br><span class="line"></span><br><span class="line"><span class="section">### 为什么需要它？</span></span><br><span class="line"></span><br><span class="line">因为进程之间默认隔离，不能直接访问彼此的数据。共享内存提供了一种高效的数据共享方式。</span><br><span class="line"></span><br><span class="line"><span class="section">### 它是怎么工作的？</span></span><br><span class="line"></span><br><span class="line">进程 A 和进程 B 看到的是不同的虚拟地址，但这些虚拟地址可以映射到同一块物理内存。</span><br><span class="line"></span><br><span class="line"><span class="section">### 它的问题</span></span><br><span class="line"></span><br><span class="line">共享内存本身只解决“能共享”，不解决“谁先读、谁先写、什么时候读写”的同步问题。</span><br></pre></td></tr></table></figure><hr><h2 id="7-语言风格规则"><a href="#7-语言风格规则" class="headerlink" title="7. 语言风格规则"></a>7. 语言风格规则</h2><h3 id="7-1-语言要直白"><a href="#7-1-语言要直白" class="headerlink" title="7.1 语言要直白"></a>7.1 语言要直白</h3><p>优先使用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">也就是说……</span><br><span class="line">换句话说……</span><br><span class="line">你可以理解为……</span><br><span class="line">本质上……</span><br></pre></td></tr></table></figure><p>避免只堆术语。</p><h3 id="7-2-允许使用类比，但类比后必须回到专业概念"><a href="#7-2-允许使用类比，但类比后必须回到专业概念" class="headerlink" title="7.2 允许使用类比，但类比后必须回到专业概念"></a>7.2 允许使用类比，但类比后必须回到专业概念</h3><p>例如：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">可以把共享内存理解成小区门口的快递桌。</span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> 快递桌 = 共享内存</span><br><span class="line"><span class="bullet">-</span> 快递员 = 发送者</span><br><span class="line"><span class="bullet">-</span> 小明 = 接收者</span><br><span class="line"><span class="bullet">-</span> 快递 = 消息</span><br><span class="line"></span><br><span class="line">但要注意，真实操作系统里共享的是物理内存页，而不是现实中的桌子。</span><br></pre></td></tr></table></figure><h3 id="7-3-不要过度拓展课外内容"><a href="#7-3-不要过度拓展课外内容" class="headerlink" title="7.3 不要过度拓展课外内容"></a>7.3 不要过度拓展课外内容</h3><p>如果课件没有涉及很深，不要把内容扩展成操作系统源码级分析。<br>可以补充必要背景，但必须服务于理解当前课件。</p><hr><h2 id="8-代码与例子规则"><a href="#8-代码与例子规则" class="headerlink" title="8. 代码与例子规则"></a>8. 代码与例子规则</h2><p>如果课件中有代码或伪代码，应当解释代码在表达什么，而不是逐行机械翻译。</p><p>推荐格式：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">这段代码的核心不是语法，而是这三个变量的关系：</span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> buffer：共享数据区</span><br><span class="line"><span class="bullet">-</span> in：下一个写入位置</span><br><span class="line"><span class="bullet">-</span> out：下一个读取位置</span><br></pre></td></tr></table></figure><p>代码块应短小，优先保留核心逻辑。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (in == out)</span><br><span class="line">    ; <span class="comment">// 没有新消息，忙等</span></span><br><span class="line"></span><br><span class="line">item = buffer[out];</span><br><span class="line">out = (out + <span class="number">1</span>) % BUFFER_SIZE;</span><br></pre></td></tr></table></figure><p>解释重点：</p><ol><li>这段代码想解决什么问题</li><li>哪些变量是共享的</li><li>可能出现什么错误</li><li>为什么需要后续机制改进</li></ol><hr><h2 id="9-对比规则"><a href="#9-对比规则" class="headerlink" title="9. 对比规则"></a>9. 对比规则</h2><p>遇到相似概念，必须使用对比表或分点对比。</p><p>例如：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">| 对比点 | 管道 | 消息队列 |</span><br><span class="line">|---|---|---|</span><br><span class="line">| 数据形式 | 字节流 | 带类型的消息 |</span><br><span class="line">| 通信方向 | 通常单向 | 更灵活 |</span><br><span class="line">| 进程数量 | 通常两个端点 | 可多个发送者和接收者 |</span><br><span class="line">| 读取方式 | FIFO | FIFO，也可按类型读取 |</span><br></pre></td></tr></table></figure><p>常见需要对比的概念包括：</p><ul><li>进程 vs 线程</li><li>共享内存 vs 消息传递</li><li>直接通信 vs 间接通信</li><li>管道 vs 消息队列</li><li>同步通信 vs 异步通信</li><li>阻塞 vs 非阻塞</li><li>互斥锁 vs 条件变量</li><li>条件变量 vs 信号量</li><li>分段 vs 分页</li><li>立即映射 vs 延迟映射</li><li>用户态线程 vs 内核态线程</li></ul><hr><h2 id="10-易错点规则"><a href="#10-易错点规则" class="headerlink" title="10. 易错点规则"></a>10. 易错点规则</h2><p>每份学习指南必须包含“最容易混淆的点”。</p><p>格式如下：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 最容易混淆的点</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 1. XXX 不是 XXX</span></span><br><span class="line"></span><br><span class="line">解释……</span><br><span class="line"></span><br><span class="line"><span class="section">### 2. XXX 和 XXX 的区别不是……</span></span><br><span class="line"></span><br><span class="line">解释……</span><br><span class="line"></span><br><span class="line"><span class="section">### 3. 不能把 XXX 理解成……</span></span><br><span class="line"></span><br><span class="line">解释……</span><br></pre></td></tr></table></figure><p>易错点要直接针对初学者常见误解。</p><p>例如：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">不要以为只有线程能共享内存，进程也可以通过操作系统建立共享内存区域。</span><br><span class="line">区别是：线程天然共享同一进程地址空间；进程默认隔离，需要显式建立共享映射。</span><br></pre></td></tr></table></figure><hr><h2 id="11-总结规则"><a href="#11-总结规则" class="headerlink" title="11. 总结规则"></a>11. 总结规则</h2><p>每份学习指南末尾必须有：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 本节一句话总复习</span></span><br></pre></td></tr></table></figure><p>要求：</p><ol><li>用 1 到 3 句话总结整节课。</li><li>不要写成空泛口号。</li><li>必须包含本节主线和核心机制。</li></ol><p>例如：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">进程间通信 IPC 是为了解决隔离进程之间如何协作的问题。共享内存通过共同映射物理内存实现高效数据共享，但需要同步机制；消息传递通过 Send/Recv 抽象通信过程，进一步发展出管道、消息队列等机制。</span><br></pre></td></tr></table></figure><hr><h2 id="12-自测题规则"><a href="#12-自测题规则" class="headerlink" title="12. 自测题规则"></a>12. 自测题规则</h2><h3 id="12-1-默认只给题，不给答案"><a href="#12-1-默认只给题，不给答案" class="headerlink" title="12.1 默认只给题，不给答案"></a>12.1 默认只给题，不给答案</h3><p>除非我明确要求，否则自测题不要给答案。</p><p>标题固定为：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 自测题，先不放答案</span></span><br></pre></td></tr></table></figure><h3 id="12-2-自测题数量"><a href="#12-2-自测题数量" class="headerlink" title="12.2 自测题数量"></a>12.2 自测题数量</h3><p>一般给 10 到 15 题。</p><p>题目类型应覆盖：</p><ol><li>概念理解</li><li>机制流程</li><li>对比辨析</li><li>易错点判断</li><li>简单应用场景</li></ol><h3 id="12-3-自测题风格"><a href="#12-3-自测题风格" class="headerlink" title="12.3 自测题风格"></a>12.3 自测题风格</h3><p>不要只问死记硬背定义。<br>优先问能暴露理解漏洞的问题。</p><p>例如不要只问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">什么是 IPC？</span><br></pre></td></tr></table></figure><p>更推荐问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">为什么进程之间不能像线程一样天然共享变量？</span><br></pre></td></tr></table></figure><p>例如不要只问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">什么是共享内存？</span><br></pre></td></tr></table></figure><p>更推荐问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">共享内存已经让两个进程能访问同一块内存了，为什么还需要同步机制？</span><br></pre></td></tr></table></figure><hr><h2 id="13-我提交自测题答案后的批改规则"><a href="#13-我提交自测题答案后的批改规则" class="headerlink" title="13. 我提交自测题答案后的批改规则"></a>13. 我提交自测题答案后的批改规则</h2><p>当我提交自测题答案后，AI 应按照以下格式批改：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 批改结果</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 第 1 题</span></span><br><span class="line"></span><br><span class="line">你的回答：……</span><br><span class="line">判断：正确 / 基本正确 / 不准确 / 错误</span><br><span class="line"></span><br><span class="line">修正：</span><br><span class="line">……</span><br><span class="line"></span><br><span class="line">补充：</span><br><span class="line">……</span><br><span class="line"></span><br><span class="line"><span class="section">### 第 2 题</span></span><br><span class="line"></span><br><span class="line">……</span><br></pre></td></tr></table></figure><p>批改时要注意：</p><ol><li>不要只说“对”或“错”。</li><li>对基本正确的答案，要指出还差哪一点。</li><li>对错误答案，要解释为什么错。</li><li>最后给出本轮最需要补强的知识点。</li></ol><p>结尾格式：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">## 你目前最需要补强的点</span></span><br><span class="line"></span><br><span class="line"><span class="bullet">1.</span> ……</span><br><span class="line"><span class="bullet">2.</span> ……</span><br><span class="line"><span class="bullet">3.</span> ……</span><br></pre></td></tr></table></figure><hr><h2 id="14-针对我的学习偏好"><a href="#14-针对我的学习偏好" class="headerlink" title="14. 针对我的学习偏好"></a>14. 针对我的学习偏好</h2><p>生成学习指南时，应默认遵循以下偏好：</p><ol><li>我希望先理解整体逻辑，再看细节。</li><li>我希望解释尽量直白，但不要牺牲准确性。</li><li>我容易在相似概念之间混淆，所以需要对比。</li><li>我喜欢通过自测题暴露理解漏洞。</li><li>我提交答案后，希望 AI 帮我修正和补充，而不是简单给标准答案。</li><li>如果涉及 C&#x2F;C++、操作系统、计算机网络等内容，可以适当使用代码或伪代码辅助理解。</li><li>如果知识点和之前学过的内容有关，应指出联系，但不要过度拓展。</li></ol><hr><h2 id="15-禁止事项"><a href="#15-禁止事项" class="headerlink" title="15. 禁止事项"></a>15. 禁止事项</h2><p>AI 在生成学习指南时不要这样做：</p><ol><li>不要直接照抄课件。</li><li>不要只列目录式摘要。</li><li>不要一上来堆定义。</li><li>不要把简单概念讲得过度复杂。</li><li>不要在我没要求时直接给自测题答案。</li><li>不要跳过易错点。</li><li>不要只讲“是什么”，不讲“为什么”。</li><li>不要把类比当成最终解释，类比之后必须回到专业概念。</li><li>不要默认我已经完全理解前置知识。</li><li>不要为了完整性无限拓展课外知识。</li></ol><hr><h2 id="16-推荐提示词"><a href="#16-推荐提示词" class="headerlink" title="16. 推荐提示词"></a>16. 推荐提示词</h2><p>以后我可以这样调用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">请严格按照“学习指南.md”的规则，帮我学习【章节名】。</span><br><span class="line">自测题先不用给答案。</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">请根据“学习指南.md”，把这份课件整理成学习指南。</span><br><span class="line">要求重点解释主线、核心机制、易错点，最后给 15 道自测题，不给答案。</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">下面是我对自测题的回答，请按照“学习指南.md”的批改规则帮我修正和补充。</span><br></pre></td></tr></table></figure><hr><h2 id="17-标准输出模板"><a href="#17-标准输出模板" class="headerlink" title="17. 标准输出模板"></a>17. 标准输出模板</h2><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="section"># 【章节名】学习指南</span></span><br><span class="line"></span><br><span class="line"><span class="section">## 0. 这一章到底在解决什么问题？</span></span><br><span class="line"></span><br><span class="line">……</span><br><span class="line"></span><br><span class="line"><span class="section">## 1. 本节知识地图</span></span><br><span class="line"></span><br><span class="line">本节关键词：</span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> ……</span><br><span class="line"><span class="bullet">-</span> ……</span><br><span class="line"><span class="bullet">-</span> ……</span><br><span class="line"></span><br><span class="line">主线：</span><br><span class="line"></span><br><span class="line"><span class="code">```text</span></span><br><span class="line"><span class="code">……</span></span><br></pre></td></tr></table></figure><h2 id="2-核心概念一：……"><a href="#2-核心概念一：……" class="headerlink" title="2. 核心概念一：……"></a>2. 核心概念一：……</h2><h3 id="2-1-它是什么？"><a href="#2-1-它是什么？" class="headerlink" title="2.1 它是什么？"></a>2.1 它是什么？</h3><p>……</p><h3 id="2-2-为什么需要它？"><a href="#2-2-为什么需要它？" class="headerlink" title="2.2 为什么需要它？"></a>2.2 为什么需要它？</h3><p>……</p><h3 id="2-3-它是怎么工作的？"><a href="#2-3-它是怎么工作的？" class="headerlink" title="2.3 它是怎么工作的？"></a>2.3 它是怎么工作的？</h3><p>……</p><h3 id="2-4-它的问题-代价-易错点"><a href="#2-4-它的问题-代价-易错点" class="headerlink" title="2.4 它的问题 &#x2F; 代价 &#x2F; 易错点"></a>2.4 它的问题 &#x2F; 代价 &#x2F; 易错点</h3><p>……</p><h2 id="3-核心概念二：……"><a href="#3-核心概念二：……" class="headerlink" title="3. 核心概念二：……"></a>3. 核心概念二：……</h2><p>……</p><h2 id="4-关键机制-运行流程"><a href="#4-关键机制-运行流程" class="headerlink" title="4. 关键机制 &#x2F; 运行流程"></a>4. 关键机制 &#x2F; 运行流程</h2><p>……</p><h2 id="5-重要对比"><a href="#5-重要对比" class="headerlink" title="5. 重要对比"></a>5. 重要对比</h2><table><thead><tr><th>对比点</th><th>A</th><th>B</th></tr></thead><tbody><tr><td>……</td><td>……</td><td>……</td></tr></tbody></table><h2 id="6-最容易混淆的点"><a href="#6-最容易混淆的点" class="headerlink" title="6. 最容易混淆的点"></a>6. 最容易混淆的点</h2><h3 id="1-……"><a href="#1-……" class="headerlink" title="1. ……"></a>1. ……</h3><p>……</p><h3 id="2-……"><a href="#2-……" class="headerlink" title="2. ……"></a>2. ……</h3><p>……</p><h2 id="7-本节一句话总复习"><a href="#7-本节一句话总复习" class="headerlink" title="7. 本节一句话总复习"></a>7. 本节一句话总复习</h2><p>……</p><h2 id="8-自测题，先不放答案"><a href="#8-自测题，先不放答案" class="headerlink" title="8. 自测题，先不放答案"></a>8. 自测题，先不放答案</h2><ol><li>……</li><li>……</li><li>……</li></ol><pre><code></code></pre>]]></content>
      
      
      <categories>
          
          <category> 学习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 学习 </tag>
            
            <tag> ai </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>否定一个角色之后：林翩翩、主体性与文学价值判断</title>
      <link href="/2026/04/12/essay/"/>
      <url>/2026/04/12/essay/</url>
      
        <content type="html"><![CDATA[<h1 id="否定一个角色之后：林翩翩、主体性与文学价值判断"><a href="#否定一个角色之后：林翩翩、主体性与文学价值判断" class="headerlink" title="否定一个角色之后：林翩翩、主体性与文学价值判断"></a>否定一个角色之后：林翩翩、主体性与文学价值判断</h1><h2 id="0-速读结论"><a href="#0-速读结论" class="headerlink" title="0. 速读结论"></a>0. 速读结论</h2><ul><li>❗ <strong>核心判断</strong>：我否定林翩翩的文学价值，不是因为她不够悲惨，而是因为作品没有把她的悲惨转化为足够有说服力的人物结构。</li><li>❗ <strong>主要问题</strong>：她的心理链路、主体性崩塌过程和牺牲动机没有被充分建立，导致读者容易把她的卑微与自毁归因为个体缺陷，而不是结构性悲剧。</li><li>❗ <strong>判断边界</strong>：这个角色可以对某些读者有效，但“有效”不等于“优秀”，“共鸣”也不等于“文学价值”。</li><li>❗ <strong>个人收获</strong>：即使林翩翩作为文学人物在我这里失败，她仍然可以作为负样本，帮助我建立人物塑造和文学价值判断的标准。</li></ul><table><thead><tr><th>问题</th><th>本文判断</th></tr></thead><tbody><tr><td>为什么否定林翩翩？</td><td>心理链路不足，主体性缺失，死亡升华缺乏铺垫</td></tr><tr><td>无主体性人物是否一定失败？</td><td>不一定，关键看作品是否写出主体性丧失的因果</td></tr><tr><td>共鸣是否等于文学价值？</td><td>不等于，共鸣只是情绪触达或有效性的证据之一</td></tr><tr><td>这次讨论的价值是什么？</td><td>帮助我把情绪反应转化为审美标准</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：本文的中心不是“林翩翩是否值得同情”，而是“作品是否把她写成了一个能够被理解的文学人物”。"><a href="#❗-本节核心结论：本文的中心不是“林翩翩是否值得同情”，而是“作品是否把她写成了一个能够被理解的文学人物”。" class="headerlink" title="❗ 本节核心结论：本文的中心不是“林翩翩是否值得同情”，而是“作品是否把她写成了一个能够被理解的文学人物”。"></a>❗ <strong>本节核心结论：</strong><br>本文的中心不是“林翩翩是否值得同情”，而是“作品是否把她写成了一个能够被理解的文学人物”。</h2><h2 id="1-问题起点：我否定的到底是什么"><a href="#1-问题起点：我否定的到底是什么" class="headerlink" title="1. 问题起点：我否定的到底是什么"></a>1. 问题起点：我否定的到底是什么</h2><h3 id="1-1-否定对象的边界"><a href="#1-1-否定对象的边界" class="headerlink" title="1.1 否定对象的边界"></a>1.1 否定对象的边界</h3><table><thead><tr><th>我否定的不是</th><th>我否定的是</th></tr></thead><tbody><tr><td>林翩翩的悲惨处境</td><td>悲惨处境没有被写成有说服力的人物结构</td></tr><tr><td>妓女身份本身</td><td>身份符号被压缩成身体厌恶刺激</td></tr><tr><td>底层人物的卑微</td><td>卑微缺少形成过程和心理解释</td></tr><tr><td>牺牲行为本身</td><td>牺牲被用来替代人物成长和价值生成</td></tr><tr><td>其他读者可能被打动</td><td>这种打动不等于我认可其文学价值</td></tr></tbody></table><h3 id="1-2-文章要回答的四个问题"><a href="#1-2-文章要回答的四个问题" class="headerlink" title="1.2 文章要回答的四个问题"></a>1.2 文章要回答的四个问题</h3><ol><li>❗ <strong>否定依据</strong>：为什么我否定林翩翩的文学价值？</li><li>❗ <strong>主体性问题</strong>：缺乏主体性的人物为何有时成立、有时失败？</li><li>❗ <strong>价值区分</strong>：如何区分共鸣、有效性与文学价值？</li><li>❗ <strong>反向收获</strong>：这次讨论如何帮助我建立文学判断标准？</li></ol><h3 id="1-3-基本立场"><a href="#1-3-基本立场" class="headerlink" title="1.3 基本立场"></a>1.3 基本立场</h3><ul><li>我承认林翩翩具备成为悲剧人物的设定潜力。</li><li>我不认为作品充分完成了这一潜力。</li><li>我对她的反感不是单纯道德审判，而是一种审美失配。</li><li>我真正反感的是：<ul><li>用悲惨代替人物塑造；</li><li>用情绪刺激代替心理解释；</li><li>用死亡结果代替价值生成。</li></ul></li></ul><hr><h2 id="❗-本节核心结论：我的否定不是针对角色身份或悲惨遭遇，而是针对作品没有把这些材料组织成可信的人物逻辑。"><a href="#❗-本节核心结论：我的否定不是针对角色身份或悲惨遭遇，而是针对作品没有把这些材料组织成可信的人物逻辑。" class="headerlink" title="❗ 本节核心结论：我的否定不是针对角色身份或悲惨遭遇，而是针对作品没有把这些材料组织成可信的人物逻辑。"></a>❗ <strong>本节核心结论：</strong><br>我的否定不是针对角色身份或悲惨遭遇，而是针对作品没有把这些材料组织成可信的人物逻辑。</h2><h2 id="2-林翩翩塑造失败的三个入口"><a href="#2-林翩翩塑造失败的三个入口" class="headerlink" title="2. 林翩翩塑造失败的三个入口"></a>2. 林翩翩塑造失败的三个入口</h2><h3 id="2-1-“脏”的重复：从社会隐喻滑向生理刺激"><a href="#2-1-“脏”的重复：从社会隐喻滑向生理刺激" class="headerlink" title="2.1 “脏”的重复：从社会隐喻滑向生理刺激"></a>2.1 “脏”的重复：从社会隐喻滑向生理刺激</h3><table><thead><tr><th>可能成立的写法</th><th>林翩翩呈现出的风险</th></tr></thead><tbody><tr><td>妓女身份作为社会隐喻</td><td>身份被反复压缩为身体是否“脏”</td></tr><tr><td>指向社会如何污染人</td><td>只让读者反复接收厌恶刺激</td></tr><tr><td>展示内化羞辱的形成</td><td>直接展示自我贬低的结果</td></tr><tr><td>引发结构性思考</td><td>引发生理性排斥</td></tr></tbody></table><p>❗ <strong>关键问题</strong>：</p><ul><li>“脏”本可以写成内化羞辱。</li><li>但作品需要展示羞辱如何进入她的自我认知。</li><li>如果只反复强调“脏”，读者很难进入心理机制，只会感到被强迫厌恶。</li></ul><h3 id="2-2-不被赎身却持续依附：低自尊没有被写成因果链"><a href="#2-2-不被赎身却持续依附：低自尊没有被写成因果链" class="headerlink" title="2.2 不被赎身却持续依附：低自尊没有被写成因果链"></a>2.2 不被赎身却持续依附：低自尊没有被写成因果链</h3><p>林翩翩与男主的关系，本可以写成一种深刻的低自尊悲剧：</p><ul><li>她知道自己可以被拯救。</li><li>她却不认为自己值得被拯救。</li><li>她渴望正常生活。</li><li>她又不相信自己有资格拥有正常生活。</li></ul><p>但要让这个逻辑成立，作品必须回答：</p><ol><li>她为什么接受“我可以被使用，但不配被爱”？</li><li>她是否期待过尊重？</li><li>她是否尝试过提出边界？</li><li>她是否意识到自己被轻视？</li><li>她为什么在意识到之后仍退回原处？</li></ol><p>❗ <strong>缺失后果</strong>：</p><ul><li>读者看到的不是“自我价值系统被摧毁”。</li><li>读者看到的是“不断自我让渡，却缺少内在解释”。</li></ul><h3 id="2-3-极端牺牲：死亡不能自动完成升华"><a href="#2-3-极端牺牲：死亡不能自动完成升华" class="headerlink" title="2.3 极端牺牲：死亡不能自动完成升华"></a>2.3 极端牺牲：死亡不能自动完成升华</h3><table><thead><tr><th>作品似乎想表达</th><th>我实际感受到的问题</th></tr></thead><tbody><tr><td>她处在最低处，却做了最高尚的事</td><td>这种高尚没有足够心理支撑</td></tr><tr><td>她用死亡证明自身价值</td><td>死亡像是替代了人物塑造</td></tr><tr><td>她为他人牺牲，因此值得尊重</td><td>牺牲缺少自主性、理解性和替代性</td></tr></tbody></table><p>一个牺牲行为要具有道德高度，至少需要三个条件：</p><ol><li>❗ <strong>自主性</strong>：她是否真正以自己的意志选择这样做？</li><li>❗ <strong>理解性</strong>：她是否清楚自己为什么这样做？</li><li>❗ <strong>替代性</strong>：她是否本可以选择别的道路，却仍然承担这个选择？</li></ol><p>如果这些条件没有被写出来：</p><ul><li>为别人死可能只是逃避。</li><li>自我牺牲可能只是自我否定。</li><li>伟大行为可能只是被叙事强行安排出来的结果。</li></ul><hr><h2 id="❗-本节核心结论：林翩翩的问题不是“惨得不够”，而是“惨、卑微、牺牲”之间缺少足够清晰的心理因果。"><a href="#❗-本节核心结论：林翩翩的问题不是“惨得不够”，而是“惨、卑微、牺牲”之间缺少足够清晰的心理因果。" class="headerlink" title="❗ 本节核心结论：林翩翩的问题不是“惨得不够”，而是“惨、卑微、牺牲”之间缺少足够清晰的心理因果。"></a>❗ <strong>本节核心结论：</strong><br>林翩翩的问题不是“惨得不够”，而是“惨、卑微、牺牲”之间缺少足够清晰的心理因果。</h2><h2 id="3-可解释，不等于写成立"><a href="#3-可解释，不等于写成立" class="headerlink" title="3. 可解释，不等于写成立"></a>3. 可解释，不等于写成立</h2><h3 id="3-1-马斯洛视角下的跳跃感"><a href="#3-1-马斯洛视角下的跳跃感" class="headerlink" title="3.1 马斯洛视角下的跳跃感"></a>3.1 马斯洛视角下的跳跃感</h3><p>按照严格的需求层次模型，林翩翩的选择会显得不稳定：</p><table><thead><tr><th>需求层次</th><th>林翩翩的状态</th></tr></thead><tbody><tr><td>生理与安全</td><td>不稳定，身体被剥削</td></tr><tr><td>归属与爱</td><td>扭曲或缺失</td></tr><tr><td>尊重</td><td>几乎不存在</td></tr><tr><td>自我实现 &#x2F; 精神意义</td><td>却通过死亡被强行推到前台</td></tr></tbody></table><p>❗ <strong>跳跃感来源</strong>：</p><ul><li>她连基本尊严都没有被写稳。</li><li>作品却让她承担“用死亡实现意义”的叙事功能。</li><li>读者因此会质疑：她凭什么突然死得这么高尚？</li></ul><h3 id="3-2-理论上可以解释的部分"><a href="#3-2-理论上可以解释的部分" class="headerlink" title="3.2 理论上可以解释的部分"></a>3.2 理论上可以解释的部分</h3><p>林翩翩的行为并非完全不可解释：</p><ul><li>❗ <strong>内化羞辱</strong>：她可能已经把外界贬低吸收为自我认知。</li><li>❗ <strong>意义替代生存</strong>：极端匮乏中的人可能抓住“意义”支撑自己。</li><li>❗ <strong>价值反击</strong>：长期被否定者可能渴望一次证明自己不是废物的机会。</li><li>❗ <strong>死亡补写价值</strong>：她也许不是“为了意义而死”，而是“因为活着没有意义，所以把死亡变成意义”。</li></ul><h3 id="3-3-文本中没有充分写成立的部分"><a href="#3-3-文本中没有充分写成立的部分" class="headerlink" title="3.3 文本中没有充分写成立的部分"></a>3.3 文本中没有充分写成立的部分</h3><p>理论解释不能替代文本呈现。作品仍然需要回答：</p><ol><li>她什么时候开始认为自己不值得被爱？</li><li>她为什么无法接受善意？</li><li>她为什么把痛苦当成存在方式？</li><li>她为什么把毁灭误认为救赎？</li><li>她有没有过别的可能？</li><li>她为什么最终没有走向别的可能？</li></ol><table><thead><tr><th>能被解释</th><th>被写成立</th></tr></thead><tbody><tr><td>读者事后用理论补全逻辑</td><td>作品在阅读过程中展示逻辑</td></tr><tr><td>依赖概念推导</td><td>依赖情节、细节和心理推进</td></tr><tr><td>说明“可能如此”</td><td>让读者感到“只能如此”或“终于如此”</td></tr><tr><td>可作为外部分析</td><td>可成为文本内部说服力</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：林翩翩的问题不是完全不可解释，而是作品没有把可解释的心理机制充分写成可感知的叙事过程。"><a href="#❗-本节核心结论：林翩翩的问题不是完全不可解释，而是作品没有把可解释的心理机制充分写成可感知的叙事过程。" class="headerlink" title="❗ 本节核心结论：林翩翩的问题不是完全不可解释，而是作品没有把可解释的心理机制充分写成可感知的叙事过程。"></a>❗ <strong>本节核心结论：</strong><br>林翩翩的问题不是完全不可解释，而是作品没有把可解释的心理机制充分写成可感知的叙事过程。</h2><h2 id="4-主体性：人物为什么值得被尊重"><a href="#4-主体性：人物为什么值得被尊重" class="headerlink" title="4. 主体性：人物为什么值得被尊重"></a>4. 主体性：人物为什么值得被尊重</h2><h3 id="4-1-主体性的定义"><a href="#4-1-主体性的定义" class="headerlink" title="4.1 主体性的定义"></a>4.1 主体性的定义</h3><p>❗ <strong>主体性</strong>：角色是否能以“自己”为出发点做选择，并承担这个选择的意义。</p><p>主体性至少包括三个维度：</p><ol><li>❗ <strong>选择权</strong>：角色是否拥有真实或有限但可感知的选择空间？</li><li>❗ <strong>意志来源</strong>：行动来自内部信念，还是来自外界评价？</li><li>❗ <strong>自我定义</strong>：角色如何回答“我是谁”，而不是只重复“别人说我是什么”？</li></ol><h3 id="4-2-主体性强弱的表达差异"><a href="#4-2-主体性强弱的表达差异" class="headerlink" title="4.2 主体性强弱的表达差异"></a>4.2 主体性强弱的表达差异</h3><table><thead><tr><th>表达</th><th>价值来源</th><th>主体性</th></tr></thead><tbody><tr><td>“我不想这样活”</td><td>内部生成</td><td>强</td></tr><tr><td>“我选择这样做”</td><td>自我定义</td><td>强</td></tr><tr><td>“我很脏”</td><td>外部评价内化</td><td>弱</td></tr><tr><td>“我要证明我不是废物”</td><td>对评价体系的回应</td><td>弱</td></tr></tbody></table><p>❗ <strong>关键区别</strong>：</p><ul><li>“我不愿意这样活”体现的是自我位置。</li><li>“我很脏”体现的是他人标签被内化。</li><li>前者让读者看到一个人在定义自己。</li><li>后者让读者看到一个人在重复别人对她的定义。</li></ul><h3 id="4-3-林翩翩的主体性缺口"><a href="#4-3-林翩翩的主体性缺口" class="headerlink" title="4.3 林翩翩的主体性缺口"></a>4.3 林翩翩的主体性缺口</h3><p>林翩翩并非完全没有行动，但她的行动常常像是被外界目光牵引：</p><ul><li>她说“我脏”，却很少让人看到“我不想这样活”。</li><li>她持续依附男主，却缺少边界意识。</li><li>她的死亡更像“证明我不是废物”，而不是“我选择某种价值”。</li><li>她的价值依赖男主、社会和“脏不脏”的评价体系。</li></ul><p>❗ <strong>主体性缺口</strong>：</p><ul><li>她的问题不是“她做了什么”。</li><li>她的问题是“她做这些事时，背后没有一个稳定的自我”。</li></ul><hr><h2 id="❗-本节核心结论：主体性决定读者是否尊重一个角色；林翩翩的牺牲不像内部信念的选择，更像对外部评价体系的回应。"><a href="#❗-本节核心结论：主体性决定读者是否尊重一个角色；林翩翩的牺牲不像内部信念的选择，更像对外部评价体系的回应。" class="headerlink" title="❗ 本节核心结论：主体性决定读者是否尊重一个角色；林翩翩的牺牲不像内部信念的选择，更像对外部评价体系的回应。"></a>❗ <strong>本节核心结论：</strong><br>主体性决定读者是否尊重一个角色；林翩翩的牺牲不像内部信念的选择，更像对外部评价体系的回应。</h2><h2 id="5-缺乏主体性的人物：成立条件与失败条件"><a href="#5-缺乏主体性的人物：成立条件与失败条件" class="headerlink" title="5. 缺乏主体性的人物：成立条件与失败条件"></a>5. 缺乏主体性的人物：成立条件与失败条件</h2><h3 id="5-1-缺乏主体性不等于低级"><a href="#5-1-缺乏主体性不等于低级" class="headerlink" title="5.1 缺乏主体性不等于低级"></a>5.1 缺乏主体性不等于低级</h3><p>无主体性人物可以很有文学价值，尤其当作品目标是结构批判时。</p><table><thead><tr><th>类型</th><th>叙事目标</th><th>无主体性的功能</th></tr></thead><tbody><tr><td>人物驱动叙事</td><td>让读者理解、尊重、心疼一个具体的人</td><td>需要展示角色如何接近或失去自我</td></tr><tr><td>问题驱动叙事</td><td>揭示社会、制度、结构如何压垮人</td><td>可以让角色缺乏主体性，以暴露结构问题</td></tr></tbody></table><h3 id="5-2-鲁迅人物为什么成立"><a href="#5-2-鲁迅人物为什么成立" class="headerlink" title="5.2 鲁迅人物为什么成立"></a>5.2 鲁迅人物为什么成立</h3><p>阿Q、祥林嫂等人物并不是强主体人物，但他们能成立，因为作品提供了比道德评价更强的解释路径。</p><table><thead><tr><th>人物效果</th><th>结构支撑</th></tr></thead><tbody><tr><td>可笑</td><td>精神胜利法、麻木、重复</td></tr><tr><td>可鄙</td><td>被旧观念和权力秩序塑形</td></tr><tr><td>可悲</td><td>教育缺失、礼教压迫、贫困和排斥</td></tr><tr><td>可理解</td><td>作品让读者看到他们为何无法成为主体</td></tr></tbody></table><p>❗ <strong>鲁迅式成立条件</strong>：</p><ul><li>读者不是只看到人物失败。</li><li>读者还能看到结构如何制造这种失败。</li><li>因此读者的归因方向从“她&#x2F;他本性如此”转向“社会如何使人如此”。</li></ul><h3 id="5-3-林翩翩为何失败"><a href="#5-3-林翩翩为何失败" class="headerlink" title="5.3 林翩翩为何失败"></a>5.3 林翩翩为何失败</h3><p>林翩翩的无主体性没有充分指向结构压力，反而容易被归因为个体问题。</p><table><thead><tr><th>成立的无主体性</th><th>失败的无主体性</th></tr></thead><tbody><tr><td>写出主体性如何被摧毁</td><td>只呈现主体性已经缺失</td></tr><tr><td>让读者看到结构压力</td><td>让读者停留在个体厌恶</td></tr><tr><td>指向“为什么她没有选择”</td><td>变成“她怎么总这样”</td></tr><tr><td>形成结构性悲剧</td><td>退化为情绪刺激</td></tr></tbody></table><p>❗ <strong>关键判断</strong>：</p><ul><li>如果角色的卑微让读者更想责怪她本人，而不是理解她所处的环境，作品就存在表达缺口。</li><li>林翩翩像处在人物驱动叙事里，却没有真正成为主体；也像承担结构悲剧功能，却没有充分展示结构如何压垮她。</li></ul><hr><h2 id="❗-本节核心结论：无主体性人物可以成立，但必须有清晰的结构性因果；缺少因果支撑时，无主体性只会变成令人厌烦的被动。"><a href="#❗-本节核心结论：无主体性人物可以成立，但必须有清晰的结构性因果；缺少因果支撑时，无主体性只会变成令人厌烦的被动。" class="headerlink" title="❗ 本节核心结论：无主体性人物可以成立，但必须有清晰的结构性因果；缺少因果支撑时，无主体性只会变成令人厌烦的被动。"></a>❗ <strong>本节核心结论：</strong><br>无主体性人物可以成立，但必须有清晰的结构性因果；缺少因果支撑时，无主体性只会变成令人厌烦的被动。</h2><h2 id="6-自我毁灭型人物如何写成立"><a href="#6-自我毁灭型人物如何写成立" class="headerlink" title="6. 自我毁灭型人物如何写成立"></a>6. 自我毁灭型人物如何写成立</h2><h3 id="6-1-自毁人物的基本定义"><a href="#6-1-自毁人物的基本定义" class="headerlink" title="6.1 自毁人物的基本定义"></a>6.1 自毁人物的基本定义</h3><p>❗ <strong>自我毁灭型人物</strong>：不是单纯被伤害，也不是简单地坏、蠢或软弱，而是在某种错误认知中参与了自己的毁灭。</p><p>这类人物通常包含以下机制：</p><ul><li>认同羞辱。</li><li>依赖痛苦。</li><li>把沉沦当成归宿。</li><li>把死亡误认为救赎。</li><li>用毁灭完成错误的自我证明。</li></ul><h3 id="6-2-成立所需的四层递进"><a href="#6-2-成立所需的四层递进" class="headerlink" title="6.2 成立所需的四层递进"></a>6.2 成立所需的四层递进</h3><table><thead><tr><th>层级</th><th>作品需要写出什么</th><th>读者获得什么</th></tr></thead><tbody><tr><td>1. 自我价值如何坏掉</td><td>她为什么相信自己不值得被爱</td><td>理解自贬的起点</td></tr><tr><td>2. 痛苦如何成为存在方式</td><td>她为什么更能接受伤害而非善意</td><td>理解反复回到伤害关系的原因</td></tr><tr><td>3. 毁灭如何被误认为救赎</td><td>她为什么用死亡证明自己不是废物</td><td>理解死亡不是光环，而是扭曲逻辑终点</td></tr><tr><td>4. 她本可以不是这样</td><td>她曾经是否接近过逃离、反抗、尊严</td><td>感到悲剧，而不是厌烦</td></tr></tbody></table><h3 id="6-3-失败写法与成立写法对比"><a href="#6-3-失败写法与成立写法对比" class="headerlink" title="6.3 失败写法与成立写法对比"></a>6.3 失败写法与成立写法对比</h3><table><thead><tr><th>失败写法</th><th>成立写法</th></tr></thead><tbody><tr><td>只写她很惨</td><td>写她为什么走到这种惨</td></tr><tr><td>只写她自贬</td><td>写她如何形成自贬逻辑</td></tr><tr><td>只写她毁灭</td><td>写她为何把毁灭当成自救</td></tr><tr><td>作者要求读者同情</td><td>文本让读者理解机制</td></tr><tr><td>死亡成为催泪工具</td><td>死亡成为精神轨迹终点</td></tr></tbody></table><h3 id="6-4-林翩翩的问题"><a href="#6-4-林翩翩的问题" class="headerlink" title="6.4 林翩翩的问题"></a>6.4 林翩翩的问题</h3><ul><li>我看到了自贬、依附和毁灭。</li><li>我没有看到足够完整的精神过程。</li><li>她没有成为“我不认同但我理解”的人物。</li><li>她更像“作者要我同情但我无法进入”的人物。</li></ul><hr><h2 id="❗-本节核心结论：自我毁灭型人物的高级不在于毁灭本身，而在于作品是否写出她为何把毁灭误认为唯一的自救。"><a href="#❗-本节核心结论：自我毁灭型人物的高级不在于毁灭本身，而在于作品是否写出她为何把毁灭误认为唯一的自救。" class="headerlink" title="❗ 本节核心结论：自我毁灭型人物的高级不在于毁灭本身，而在于作品是否写出她为何把毁灭误认为唯一的自救。"></a>❗ <strong>本节核心结论：</strong><br>自我毁灭型人物的高级不在于毁灭本身，而在于作品是否写出她为何把毁灭误认为唯一的自救。</h2><h2 id="7-共鸣、有效性与文学价值"><a href="#7-共鸣、有效性与文学价值" class="headerlink" title="7. 共鸣、有效性与文学价值"></a>7. 共鸣、有效性与文学价值</h2><h3 id="7-1-三个概念必须区分"><a href="#7-1-三个概念必须区分" class="headerlink" title="7.1 三个概念必须区分"></a>7.1 三个概念必须区分</h3><table><thead><tr><th>概念</th><th>含义</th><th>判断对象</th><th>是否等于文学价值</th></tr></thead><tbody><tr><td>共鸣</td><td>读者被触动、代入、投射</td><td>读者反应</td><td>不等于</td></tr><tr><td>有效性</td><td>作品能传播、能打动一部分人</td><td>传播效果</td><td>不等于</td></tr><tr><td>文学价值</td><td>作品是否有深度、结构和认知贡献</td><td>作品完成度</td><td>需要独立判断</td></tr></tbody></table><h3 id="7-2-共鸣的来源复杂"><a href="#7-2-共鸣的来源复杂" class="headerlink" title="7.2 共鸣的来源复杂"></a>7.2 共鸣的来源复杂</h3><p>共鸣可能来自：</p><ul><li>真实心理洞察。</li><li>情绪操控。</li><li>个人经历投射。</li><li>套路化经验。</li><li>价值观迎合。</li><li>作品确实提供新的理解方式。</li></ul><p>❗ <strong>共鸣边界</strong>：</p><ul><li>共鸣说明作品“触到了人”。</li><li>共鸣不直接说明作品“理解了人”。</li></ul><h3 id="7-3-文学价值的三项标准"><a href="#7-3-文学价值的三项标准" class="headerlink" title="7.3 文学价值的三项标准"></a>7.3 文学价值的三项标准</h3><table><thead><tr><th>标准</th><th>核心问题</th><th>失败表现</th></tr></thead><tbody><tr><td>表达深度</td><td>是否揭示复杂人性机制？</td><td>只放大痛苦，不解释机制</td></tr><tr><td>表达质量</td><td>结构是否完整，情感是否自然？</td><td>依靠重复台词和极端结局制造冲击</td></tr><tr><td>认知增量</td><td>是否让读者看到此前没看到的东西？</td><td>只提供情绪刺激，不提供理解</td></tr></tbody></table><h3 id="7-4-对林翩翩的判断"><a href="#7-4-对林翩翩的判断" class="headerlink" title="7.4 对林翩翩的判断"></a>7.4 对林翩翩的判断</h3><ul><li>我可以承认她可能对某些读者有效。</li><li>我不因此承认她具有我认可的文学价值。</li><li>她的共鸣如果主要来自浅层情绪触发，而不是深层心理结构，我就不会把这种共鸣视为高文学价值的证据。</li></ul><p>❗ <strong>成熟表达</strong>：</p><ul><li>可以说：“它在我这里不成立。”</li><li>不宜说：“它不应该打动任何人。”</li><li>可以说：“它可能有效，但不符合我对文学价值的判断标准。”</li></ul><hr><h2 id="❗-本节核心结论：共鸣和有效性只能证明作品有情绪触达能力，不能直接证明作品具有文学价值；文学价值仍取决于深度、结构和认知增量。"><a href="#❗-本节核心结论：共鸣和有效性只能证明作品有情绪触达能力，不能直接证明作品具有文学价值；文学价值仍取决于深度、结构和认知增量。" class="headerlink" title="❗ 本节核心结论：共鸣和有效性只能证明作品有情绪触达能力，不能直接证明作品具有文学价值；文学价值仍取决于深度、结构和认知增量。"></a>❗ <strong>本节核心结论：</strong><br>共鸣和有效性只能证明作品有情绪触达能力，不能直接证明作品具有文学价值；文学价值仍取决于深度、结构和认知增量。</h2><h2 id="8-我的“讨厌”如何成为成熟判断"><a href="#8-我的“讨厌”如何成为成熟判断" class="headerlink" title="8. 我的“讨厌”如何成为成熟判断"></a>8. 我的“讨厌”如何成为成熟判断</h2><h3 id="8-1-情绪判断与审美判断的区别"><a href="#8-1-情绪判断与审美判断的区别" class="headerlink" title="8.1 情绪判断与审美判断的区别"></a>8.1 情绪判断与审美判断的区别</h3><table><thead><tr><th>层级</th><th>表达方式</th><th>问题</th></tr></thead><tbody><tr><td>情绪型讨厌</td><td>“我不喜欢她”</td><td>只描述感受</td></tr><tr><td>审美型讨厌</td><td>“这个角色没有完成表达功能”</td><td>开始进入标准判断</td></tr><tr><td>结构型判断</td><td>“角色采用了某机制，但缺少某条件，导致心理链路断裂”</td><td>形成可复盘分析</td></tr></tbody></table><h3 id="8-2-我当前的判断结构"><a href="#8-2-我当前的判断结构" class="headerlink" title="8.2 我当前的判断结构"></a>8.2 我当前的判断结构</h3><ol><li>❗ <strong>体验</strong>：林翩翩没有让我产生沉浸、理解或复杂感。</li><li>❗ <strong>标准</strong>：我期待角色具备心理链路、主体性逻辑、因果闭环和认知增量。</li><li>❗ <strong>对照</strong>：她的塑造没有满足这些标准。</li><li>❗ <strong>结论</strong>：她更像自我毁灭工具，而不是内在逻辑充分展开的个体。</li><li>❗ <strong>边界</strong>：这个判断说明她在我这里失败，不等于她不可能对任何读者有效。</li></ol><h3 id="8-3-成熟判断的三个条件"><a href="#8-3-成熟判断的三个条件" class="headerlink" title="8.3 成熟判断的三个条件"></a>8.3 成熟判断的三个条件</h3><table><thead><tr><th>条件</th><th>含义</th><th>在本文中的体现</th></tr></thead><tbody><tr><td>有标准</td><td>不只说喜欢或讨厌</td><td>使用心理链路、主体性、认知增量作为标准</td></tr><tr><td>有边界</td><td>不把个人体验当普遍结论</td><td>承认他人可能被打动</td></tr><tr><td>有弹性</td><td>允许未来修正判断</td><td>保留文本证据和审美成长的可能</td></tr></tbody></table><p>❗ <strong>最终判断表述</strong>：</p><blockquote><p>在我的审美标准中，林翩翩没有提供足够可感知的心理链路、主体性逻辑和认知增量。因此，我否定她在我这里的文学价值；但我承认她可能对部分读者具有情绪有效性。</p></blockquote><hr><h2 id="❗-本节核心结论：“讨厌”可以成为成熟判断，前提是它从情绪反应升级为有标准、有边界、可复盘的审美分析。"><a href="#❗-本节核心结论：“讨厌”可以成为成熟判断，前提是它从情绪反应升级为有标准、有边界、可复盘的审美分析。" class="headerlink" title="❗ 本节核心结论：“讨厌”可以成为成熟判断，前提是它从情绪反应升级为有标准、有边界、可复盘的审美分析。"></a>❗ <strong>本节核心结论：</strong><br>“讨厌”可以成为成熟判断，前提是它从情绪反应升级为有标准、有边界、可复盘的审美分析。</h2><h2 id="9-结论：失败角色也可以成为负样本"><a href="#9-结论：失败角色也可以成为负样本" class="headerlink" title="9. 结论：失败角色也可以成为负样本"></a>9. 结论：失败角色也可以成为负样本</h2><h3 id="9-1-对林翩翩的最终判断"><a href="#9-1-对林翩翩的最终判断" class="headerlink" title="9.1 对林翩翩的最终判断"></a>9.1 对林翩翩的最终判断</h3><ul><li>❗ <strong>文学完成度</strong>：她没有在我这里成为完整的悲剧人物。</li><li>❗ <strong>心理模型</strong>：她没有提供独特而有力的心理感知模型。</li><li>❗ <strong>人物结构</strong>：她的悲惨、卑微和死亡没有被充分组织成有说服力的因果链。</li><li>❗ <strong>价值判断</strong>：我否定她作为文学人物的完成度。</li></ul><h3 id="9-2-作为负样本的价值"><a href="#9-2-作为负样本的价值" class="headerlink" title="9.2 作为负样本的价值"></a>9.2 作为负样本的价值</h3><p>林翩翩仍然可以在个人阅读经验中留下价值：</p><ul><li>她暴露了“只有惨，没有因果”的问题。</li><li>她暴露了“只有死亡，没有价值生成”的问题。</li><li>她暴露了“只有无主体性，没有主体性丧失过程”的问题。</li><li>她暴露了“只有共鸣刺激，没有深层理解”的问题。</li></ul><table><thead><tr><th>优秀作品</th><th>失败作品</th></tr></thead><tbody><tr><td>拓展审美上限</td><td>明确审美下限</td></tr><tr><td>提供正样本</td><td>提供负样本</td></tr><tr><td>展示什么是成立的</td><td>暴露什么是不成立的</td></tr><tr><td>让读者获得审美满足</td><td>迫使读者建立判断标准</td></tr></tbody></table><hr><h2 id="❗-本节核心结论：林翩翩作为文学人物在我这里失败，但作为负样本，她帮助我确认了人物塑造的底线和文学判断的边界。"><a href="#❗-本节核心结论：林翩翩作为文学人物在我这里失败，但作为负样本，她帮助我确认了人物塑造的底线和文学判断的边界。" class="headerlink" title="❗ 本节核心结论：林翩翩作为文学人物在我这里失败，但作为负样本，她帮助我确认了人物塑造的底线和文学判断的边界。"></a>❗ <strong>本节核心结论：</strong><br>林翩翩作为文学人物在我这里失败，但作为负样本，她帮助我确认了人物塑造的底线和文学判断的边界。</h2><h2 id="附录：《这次讨论给我的收获》"><a href="#附录：《这次讨论给我的收获》" class="headerlink" title="附录：《这次讨论给我的收获》"></a>附录：《这次讨论给我的收获》</h2><h3 id="A-情绪可以被分析"><a href="#A-情绪可以被分析" class="headerlink" title="A. 情绪可以被分析"></a>A. 情绪可以被分析</h3><ul><li>❗ <strong>情绪转译</strong>：讨厌一个角色并不幼稚，关键是能否说明这种讨厌来自哪里。</li><li>它可能来自：<ul><li>人物行为；</li><li>叙事功能；</li><li>心理链路断裂；</li><li>作品表达失败。</li></ul></li></ul><h3 id="B-人物塑造需要核心标准"><a href="#B-人物塑造需要核心标准" class="headerlink" title="B. 人物塑造需要核心标准"></a>B. 人物塑造需要核心标准</h3><table><thead><tr><th>标准</th><th>作用</th></tr></thead><tbody><tr><td>心理链路</td><td>解释角色为什么这样行动</td></tr><tr><td>主体性</td><td>判断角色是否从自我出发</td></tr><tr><td>因果闭环</td><td>判断人物状态是否可推导</td></tr><tr><td>认知增量</td><td>判断作品是否提供新的理解</td></tr></tbody></table><h3 id="C-缺乏主体性不是原罪"><a href="#C-缺乏主体性不是原罪" class="headerlink" title="C. 缺乏主体性不是原罪"></a>C. 缺乏主体性不是原罪</h3><ul><li>❗ <strong>成立条件</strong>：作品必须写出主体性如何被摧毁。</li><li>❗ <strong>失败条件</strong>：作品只呈现主体性缺失，却不展示形成过程。</li></ul><h3 id="D-共鸣不等于价值"><a href="#D-共鸣不等于价值" class="headerlink" title="D. 共鸣不等于价值"></a>D. 共鸣不等于价值</h3><ul><li>共鸣说明作品能触达人。</li><li>有效性说明作品能传播或打动一部分人。</li><li>文学价值仍要看深度、结构和认知贡献。</li></ul><h3 id="E-成熟评价的公式"><a href="#E-成熟评价的公式" class="headerlink" title="E. 成熟评价的公式"></a>E. 成熟评价的公式</h3><ol><li>描述体验：我看到了什么，感受到了什么？</li><li>对照标准：它是否提供心理链路、结构和认知增量？</li><li>区分原因：是作品问题，还是审美匹配问题？</li><li>给出判断：在我的标准下，它是否成立？</li><li>保留弹性：这个判断是否允许未来被修正？</li></ol><hr><h2 id="❗-本节核心结论：这次讨论最大的收获，是把一次反感转化为一套可复盘的文学判断标准：用标准判断作品，用边界承认差异，用弹性避免偏见。"><a href="#❗-本节核心结论：这次讨论最大的收获，是把一次反感转化为一套可复盘的文学判断标准：用标准判断作品，用边界承认差异，用弹性避免偏见。" class="headerlink" title="❗ 本节核心结论：这次讨论最大的收获，是把一次反感转化为一套可复盘的文学判断标准：用标准判断作品，用边界承认差异，用弹性避免偏见。"></a>❗ <strong>本节核心结论：</strong><br>这次讨论最大的收获，是把一次反感转化为一套可复盘的文学判断标准：用标准判断作品，用边界承认差异，用弹性避免偏见。</h2>]]></content>
      
      
      <categories>
          
          <category> 日常灵感 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 游戏 </tag>
            
            <tag> 文学 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Rust|Chapter 0 Introduction to Rust &amp; cargo package manager</title>
      <link href="/2026/03/05/Rust%E5%AD%A6%E4%B9%A01/"/>
      <url>/2026/03/05/Rust%E5%AD%A6%E4%B9%A01/</url>
      
        <content type="html"><![CDATA[<h1 id="Rust-Programming-Language-–-Beginner’s-Course-Notes"><a href="#Rust-Programming-Language-–-Beginner’s-Course-Notes" class="headerlink" title="Rust Programming Language – Beginner’s Course Notes"></a>Rust Programming Language – Beginner’s Course Notes</h1><h2 id="1-Introduction-to-Rust"><a href="#1-Introduction-to-Rust" class="headerlink" title="1. Introduction to Rust"></a>1. Introduction to Rust</h2><ul><li><strong>Type:</strong> Systems programming language (similar to C and C++)</li><li><strong>Compilation:</strong> Compiled language → source code translated to machine code via a compiler</li><li><strong>Origin:</strong><ul><li>Started as a side project by Graden in 2007</li><li>Named after the rust fungus</li><li>Sponsored by Mozilla in 2009</li><li>First stable version released in 2014</li></ul></li></ul><hr><h2 id="2-Why-Rust-Is-Highly-Admired"><a href="#2-Why-Rust-Is-Highly-Admired" class="headerlink" title="2. Why Rust Is Highly Admired"></a>2. Why Rust Is Highly Admired</h2><ul><li><strong>Stack Overflow Survey (7 consecutive years):</strong><ul><li>86.1% of 65,000 Rust developers want to use it again next year</li><li>Contrast: &lt;20% for MATLAB</li></ul></li><li><strong>Admired alongside:</strong> JavaScript, Python, TypeScript, HTML, CSS, SQL — but Rust ranks highest by margin</li></ul><hr><h2 id="3-Companies-Using-Rust"><a href="#3-Companies-Using-Rust" class="headerlink" title="3. Companies Using Rust"></a>3. Companies Using Rust</h2><ul><li><strong>Examples:</strong> Dropbox, Discord, Google (Go), npm, Amazon, Mozilla, Facebook, Microsoft</li><li><strong>Notable Endorsement:</strong> The White House suggested shifting from C&#x2F;C++ to Rust</li></ul><hr><h2 id="4-Core-Qualities-of-Rust"><a href="#4-Core-Qualities-of-Rust" class="headerlink" title="4. Core Qualities of Rust"></a>4. Core Qualities of Rust</h2><p>Rust balances four key qualities:</p><h3 id="4-1-Speed"><a href="#4-1-Speed" class="headerlink" title="4.1 Speed"></a>4.1 Speed</h3><ul><li>Comparable to C and C++</li><li>Low-level, close to hardware → fast execution</li></ul><h3 id="4-2-Safety"><a href="#4-2-Safety" class="headerlink" title="4.2 Safety"></a>4.2 Safety</h3><ul><li>Focus on <strong>memory safety</strong></li><li>Avoids common bugs found in C&#x2F;C++ (e.g., buffer overflows)</li></ul><h3 id="4-3-Concurrency"><a href="#4-3-Concurrency" class="headerlink" title="4.3 Concurrency"></a>4.3 Concurrency</h3><ul><li>Supports multi-threading</li><li>Prevents issues like data races and dangling references</li></ul><h3 id="4-4-Portability"><a href="#4-4-Portability" class="headerlink" title="4.4 Portability"></a>4.4 Portability</h3><ul><li>Compile once → run on Windows, Linux, macOS</li><li>Executable files work across systems</li></ul><hr><h2 id="5-Memory-Management-Approach"><a href="#5-Memory-Management-Approach" class="headerlink" title="5. Memory Management Approach"></a>5. Memory Management Approach</h2><ul><li><strong>No garbage collection</strong> (unlike Python)</li><li><strong>No manual memory allocation functions</strong> (unlike C)</li><li><strong>Uses Ownership and Borrowing</strong> (covered in depth in Chapters 4 &amp; 5)</li></ul><hr><h2 id="6-Package-Manager-Cargo"><a href="#6-Package-Manager-Cargo" class="headerlink" title="6. Package Manager: Cargo"></a>6. Package Manager: Cargo</h2><ul><li>Similar to <strong>npm</strong> (Node.js) or <strong>pip</strong> (Python)</li><li>Used for building, running, and managing Rust projects</li></ul><hr><h2 id="7-Getting-Started-Installation-First-Program"><a href="#7-Getting-Started-Installation-First-Program" class="headerlink" title="7. Getting Started: Installation &amp; First Program"></a>7. Getting Started: Installation &amp; First Program</h2><h3 id="7-1-Rust-Website"><a href="#7-1-Rust-Website" class="headerlink" title="7.1 Rust Website"></a>7.1 Rust Website</h3><ul><li><a href="https://rust-lang.org/">rust-lang.org</a></li><li>Resources: Performance, reliability, productivity</li><li>Use cases: CLI tools, WebAssembly, networking, embedded systems</li></ul><h3 id="7-2-Documentation-Tools"><a href="#7-2-Documentation-Tools" class="headerlink" title="7.2 Documentation &amp; Tools"></a>7.2 Documentation &amp; Tools</h3><ul><li><strong>The Rust Book</strong> – main learning resource</li><li><strong>Standard Library</strong> – covers macros, modules, functions</li><li><strong>Tools:</strong> rustc (compiler), cargo (package manager)</li></ul><h3 id="7-3-Installation"><a href="#7-3-Installation" class="headerlink" title="7.3 Installation"></a>7.3 Installation</h3><ul><li>Recommended: <strong>rustup</strong></li><li><strong>Linux&#x2F;Mac:</strong> <code>curl</code> command from website</li><li><strong>Windows:</strong> Download and run <code>rustup-init.exe</code></li></ul><h3 id="7-4-Verify-Installation"><a href="#7-4-Verify-Installation" class="headerlink" title="7.4 Verify Installation"></a>7.4 Verify Installation</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rustup --version      <span class="comment"># e.g., rustup 1.27.0</span></span><br><span class="line">rustc --version       <span class="comment"># e.g., rustc 1.77.1</span></span><br><span class="line">cargo --version       <span class="comment"># e.g., cargo 1.77.1</span></span><br></pre></td></tr></table></figure><hr><h2 id="8-Writing-Your-First-Rust-Program"><a href="#8-Writing-Your-First-Rust-Program" class="headerlink" title="8. Writing Your First Rust Program"></a>8. Writing Your First Rust Program</h2><h3 id="8-1-Method-1-Without-Cargo"><a href="#8-1-Method-1-Without-Cargo" class="headerlink" title="8.1 Method 1: Without Cargo"></a>8.1 Method 1: Without Cargo</h3><ol><li>Create a folder and file:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> rust-hello</span><br><span class="line"><span class="built_in">cd</span> rust-hello</span><br><span class="line">code .</span><br></pre></td></tr></table></figure></li><li>Create <code>hello.rs</code>:<figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Hello, world!&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>Compile and run:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rustc hello.rs       <span class="comment"># creates hello.exe</span></span><br><span class="line">./hello              <span class="comment"># outputs: Hello, world!</span></span><br></pre></td></tr></table></figure></li></ol><h3 id="8-2-Method-2-With-Cargo"><a href="#8-2-Method-2-With-Cargo" class="headerlink" title="8.2 Method 2: With Cargo"></a>8.2 Method 2: With Cargo</h3><ol><li>Create a new project:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cargo new hello-project</span><br></pre></td></tr></table></figure></li><li>Project structure:<ul><li><code>src/main.rs</code> – contains default “Hello, world!” code</li><li><code>Cargo.toml</code> – configuration file (name, version, edition, dependencies)</li></ul></li><li>Modify <code>main.rs</code> as needed</li><li>Run the project:<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> hello-project</span><br><span class="line">cargo run            <span class="comment"># compiles and runs automatically</span></span><br></pre></td></tr></table></figure></li><li>Cargo recompiles on every change automatically</li></ol><hr><h2 id="9-Key-Takeaway"><a href="#9-Key-Takeaway" class="headerlink" title="9. Key Takeaway"></a>9. Key Takeaway</h2><ul><li><strong>Without Cargo:</strong> Manual recompilation with <code>rustc</code> after each change</li><li><strong>With Cargo:</strong> <code>cargo run</code> handles compilation automatically</li></ul><hr><h2 id="10-Next-Topic"><a href="#10-Next-Topic" class="headerlink" title="10. Next Topic"></a>10. Next Topic</h2><ul><li>Primitive Data Types</li></ul>]]></content>
      
      
      <categories>
          
          <category> Rust </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Rust </tag>
            
            <tag> ProgrammingLanguage </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>LiquidityPool学习day1</title>
      <link href="/2026/03/02/LiquidityPool/"/>
      <url>/2026/03/02/LiquidityPool/</url>
      
        <content type="html"><![CDATA[<p><strong>传统经济学模型假设对称竞争、收益线性，但DeFi中收益凸性（convex）+ 排名效应（ranking effects）导致winner-takes-all（赢家通吃），所以需要新模型（如本文的Stackelberg + knife-edge equilibrium）</strong></p><h1 id="1-Knife-edge-equilibria"><a href="#1-Knife-edge-equilibria" class="headerlink" title="1. Knife-edge equilibria"></a>1. Knife-edge equilibria</h1><hr><p><strong>Knife-edge equilibria</strong>（直译为“刀刃上的均衡”）是经济学和系统科学中常用的一个比喻，用来描述一种<strong>极其不稳定、难以维持的平衡状态</strong>。</p><p>在这种状态下，系统处于一种完美的平衡中，但<strong>任何微小的扰动、偏差或外部冲击</strong>都会导致系统彻底偏离原本的轨道，走向完全不同的结果（通常是极端的繁荣或崩溃），且很难自动恢复到原来的均衡点。</p><p>可以用以下几个核心点来理解这个概念：</p><h3 id="1-直观比喻"><a href="#1-直观比喻" class="headerlink" title="1. 直观比喻"></a>1. 直观比喻</h3><p>想象一把直立的剃刀，刀刃朝上，试图把一个球放在刀刃上。</p><ul><li><strong>物理原理</strong>：从数学上讲，球确实可以完美地放在刀刃上保持静止，这是一个“均衡点”。</li><li><strong>现实情况</strong>：只要有极其微小的气流、桌子的轻微震动，或者球的重心偏离了哪怕一微米，球就会立刻掉下去，并且再也不会回到刀刃上。</li><li><strong>这就是“刀刃上的均衡”</strong>：它在理论上是可能的，但在现实中几乎无法维持。</li></ul><h3 id="2-核心特征"><a href="#2-核心特征" class="headerlink" title="2. 核心特征"></a>2. 核心特征</h3><ul><li><strong>高度敏感性</strong>：系统对初始条件、参数变化或外部冲击极其敏感。</li><li><strong>临界点</strong>：存在一个极窄的“稳定点”，一旦偏离这个点，系统就会滑向截然不同的路径。</li><li><strong>不可逆性</strong>：一旦偏离，通常无法通过微调回到原来的平衡点。</li></ul><h3 id="3-现实世界中的例子"><a href="#3-现实世界中的例子" class="headerlink" title="3. 现实世界中的例子"></a>3. 现实世界中的例子</h3><p><strong>① 宏观经济学：索洛增长模型中的“刀刃”</strong><br>在经济学中，最经典的例子是经济增长理论。</p><ul><li><strong>情景</strong>：假设一个国家的经济增长取决于技术进步、资本和劳动力。</li><li><strong>刀刃状态</strong>：如果资本和产出以完全相同的速度增长，经济就能保持稳定增长（这是一个均衡）。</li><li><strong>偏离的结果</strong>：如果资本增长的速度稍微快了一点，根据某些模型的假设，经济可能会陷入无效率的过度积累；如果资本增长的速度稍微慢了一点，经济可能会陷入停滞或衰退。除非参数被精确地设定在某个特定的值上，否则经济无法实现稳定增长。这种对参数精确性的极端要求被称为“刀刃条件”。</li></ul><p><strong>② 汇率制度</strong></p><ul><li><strong>情景</strong>：一个实行固定汇率制的国家。</li><li><strong>刀刃状态</strong>：只要市场相信这个汇率是可持续的，汇率就能稳定维持。</li><li><strong>偏离的结果</strong>：一旦有细微的迹象（如外汇储备轻微下降）让市场开始怀疑其维持能力，就会引发大规模投机攻击（抛售本币）。这种微小的偏离会自我强化，导致汇率瞬间崩溃（货币危机）。这种稳定的固定汇率制往往就处于刀刃上。</li></ul><p><strong>③ 社会与政治</strong></p><ul><li><strong>情景</strong>：一个多民族或多党派的社会，权力平衡极其微妙。</li><li><strong>刀刃状态</strong>：社会和谐运行。</li><li><strong>偏离的结果</strong>：一次小的选举舞弊传闻或一起小的民族冲突，如果恰好触动了这根敏感的神经，就可能迅速滑向内战或国家分裂。这种和平状态有时也被形容为“刀刃上的和平”。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><strong>Knife-edge equilibria</strong> 并不是指一种罕见的、完美的状态，而是一种<strong>警示</strong>：当一个系统（无论是经济、生态还是社会）处于这种平衡时，它实际上是非常脆弱的。它在模型分析中常被用来揭示系统的临界点，并提醒决策者：试图让系统长期停留在这种状态是极其危险的，因为微小的误差就可能导致灾难性的后果。</p><h1 id="2-Stackelberg博弈"><a href="#2-Stackelberg博弈" class="headerlink" title="2. Stackelberg博弈"></a>2. <strong>Stackelberg博弈</strong></h1><hr><p><strong>Stackelberg博弈</strong>（斯坦克尔伯格博弈）是由德国经济学家海因里希·冯·斯坦克尔伯格在1934年提出的一个<strong>寡头市场模型</strong>。它是博弈论中一个非常重要的概念，主要描述的是<strong>不对称的竞争</strong>，即参与者之间存在<strong>行动顺序的差异</strong>。</p><p>它的核心思想可以概括为：<strong>“领先一步”与“随后跟进”的博弈。</strong></p><p>以下是对Stackelberg博弈的详细解读：</p><h3 id="1-核心概念：领导者与追随者"><a href="#1-核心概念：领导者与追随者" class="headerlink" title="1. 核心概念：领导者与追随者"></a>1. 核心概念：领导者与追随者</h3><p>与古诺博弈（Cournot，双方同时行动）不同，Stackelberg博弈强调<strong>顺序</strong>。参与者被分为两类：</p><ol><li><strong>领导者</strong>：率先做出决策（如决定产量、价格或投资规模）。领导者知道追随者会观察自己的行动并做出反应，因此会在决策时<strong>策略性地</strong>将追随者的反应考虑在内。</li><li><strong>追随者</strong>：在观察到领导者的决策后，再做出自己的最优反应。</li></ol><h3 id="2-一个简单的例子：两家矿泉水公司"><a href="#2-一个简单的例子：两家矿泉水公司" class="headerlink" title="2. 一个简单的例子：两家矿泉水公司"></a>2. 一个简单的例子：两家矿泉水公司</h3><p>假设市场上只有两家公司（A和B）生产同质矿泉水，决策变量是<strong>产量</strong>。</p><ul><li><p><strong>古诺博弈（同时行动）</strong>：<br>A和B都蒙着眼睛猜对方生产多少，然后同时决定自己的产量。双方势均力敌。</p></li><li><p><strong>Stackelberg博弈（顺序行动）</strong>：</p><ol><li><strong>领导者（假设是A）</strong>：先宣布：“我要生产100万瓶。”</li><li><strong>追随者（B）</strong>：看到A的产量后，计算剩余的市场需求。为了利润最大化，B决定生产40万瓶（这是针对A&#x3D;100万的最优反应）。</li><li><strong>结果</strong>：A因为先动，占据了更大的市场份额（100万 vs 40万），获得了更高的利润。</li></ol></li></ul><h3 id="3-求解方法：逆向归纳"><a href="#3-求解方法：逆向归纳" class="headerlink" title="3. 求解方法：逆向归纳"></a>3. 求解方法：逆向归纳</h3><p>求解Stackelberg博弈的标准方法是<strong>逆向归纳</strong>，也叫动态规划。即从最后一步往前推：</p><ol><li><strong>先分析追随者</strong>：追随者的决策函数是 ( q_2 &#x3D; f(q_1) )，即追随者的最优产量取决于领导者的产量。</li><li><strong>再分析领导者</strong>：领导者在做决策时，知道自己一旦选了 ( q_1 )，就会导致 ( q_2 &#x3D; f(q_1) ) 的结果。因此，领导者会把 ( f(q_1) ) 代入自己的利润函数，选择一个让自己利润最大化的 ( q_1 )。</li></ol><h3 id="4-Stackelberg博弈的核心特征"><a href="#4-Stackelberg博弈的核心特征" class="headerlink" title="4. Stackelberg博弈的核心特征"></a>4. Stackelberg博弈的核心特征</h3><ul><li><p><strong>先动优势</strong>：<br>在大多数产量竞争的模型中，领导者通常能获得比追随者更高的利润。因为领导者可以承诺一个较大的产量，迫使追随者只能选择较小的产量（否则市场会崩盘）。</p></li><li><p><strong>策略承诺</strong>：<br>领导者的优势在于其决策是<strong>可置信的承诺</strong>。如果A只是嘴上说说，B可能不信。但在Stackelberg博弈中，通常假设A的决策是<strong>不可逆转</strong>的（比如已经建好了大工厂），B只能接受现实。</p></li><li><p><strong>信息不对称</strong>：<br>追随者拥有更多信息（他看到了领导者的行动），但领导者拥有<strong>位置优势</strong>（他能预见未来）。</p></li></ul><h3 id="5-与古诺博弈的区别"><a href="#5-与古诺博弈的区别" class="headerlink" title="5. 与古诺博弈的区别"></a>5. 与古诺博弈的区别</h3><table><thead><tr><th align="left">特征</th><th align="left">古诺博弈</th><th align="left">Stackelberg博弈</th></tr></thead><tbody><tr><td align="left"><strong>行动顺序</strong></td><td align="left">同时行动</td><td align="left">顺序行动（领导者先动）</td></tr><tr><td align="left"><strong>信息结构</strong></td><td align="left">不了解对方的具体决策</td><td align="left">追随者了解领导者的决策</td></tr><tr><td align="left"><strong>市场总产量</strong></td><td align="left">较低</td><td align="left"><strong>较高</strong></td></tr><tr><td align="left"><strong>市场价格</strong></td><td align="left">较高</td><td align="left"><strong>较低</strong></td></tr><tr><td align="left"><strong>总福利</strong></td><td align="left">较高</td><td align="left"><strong>更高</strong>（因为产量更接近完全竞争）</td></tr></tbody></table><h3 id="6-现实生活中的例子"><a href="#6-现实生活中的例子" class="headerlink" title="6. 现实生活中的例子"></a>6. 现实生活中的例子</h3><ul><li><p><strong>技术创新</strong>：</p><ul><li><strong>领导者</strong>：苹果公司率先推出平板电脑iPad。它在设计时就考虑了后续竞争者（如三星、华为）会如何模仿或改进。</li><li><strong>追随者</strong>：其他厂商看到iPad的成功后，推出类似产品，但为了抢占市场，可能会在价格或某些功能上做差异化。</li></ul></li><li><p><strong>价格领导</strong>：<br>在某些行业（如银行业或航空业），通常由一家大公司（领导者）率先宣布调价，其他公司（追随者）随后跟进调整，以维持行业价格秩序。</p></li><li><p><strong>零售布局</strong>：<br>大型超市（如沃尔玛）先在一个地区开设大卖场（决定“产能”）。周边的小型便利店（追随者）在看到大超市的商品种类和价格后，再决定自己卖什么（比如主打生鲜或24小时服务）。</p></li></ul><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><p><strong>Stackelberg博弈</strong>描述的是一种<strong>动态的、主从型的竞争关系</strong>。它揭示了一个深刻的道理：<strong>在特定的博弈中，行动的顺序本身就是一种权力。</strong> 率先行动并做出可信的承诺（如大规模投资），可以迫使竞争对手做出对自己有利的反应，从而将市场优势掌握在自己手中。</p><h1 id="3-Convex-revenue-functions"><a href="#3-Convex-revenue-functions" class="headerlink" title="3.Convex revenue functions"></a>3.Convex revenue functions</h1><hr><p><strong>Convex revenue functions</strong>（凸收益函数）是经济学、金融学以及运营管理中用来描述收益行为的一个数学概念。它描述了<strong>收益随着某个决策变量（如产量、销售量或努力程度）的增加而以递增的速度增长</strong>。</p><p>为了深入理解这个概念，我们需要拆解 “Convex”（凸性）的含义，并将其置于收益函数的背景下。</p><h3 id="1-核心定义：什么是凸性？"><a href="#1-核心定义：什么是凸性？" class="headerlink" title="1. 核心定义：什么是凸性？"></a>1. 核心定义：什么是凸性？</h3><p>在数学中，如果一个函数 ( f(x) ) 是凸函数，它通常满足以下几何或代数性质：</p><ul><li><strong>图形形状</strong>：函数的图像位于任意两点连线的<strong>下方</strong>（即形状是向下弯曲的，像字母 “U” 或碗状？—— 这里需要特别注意：在经济学中，我们通常所说的凸函数是指二阶导数大于0，即曲线是<strong>向上弯曲</strong>的，斜率越来越陡）。</li><li><strong>斜率变化</strong>：函数的<strong>斜率（导数）是递增的</strong>。也就是说，随着 ( x ) 的增加，每增加一单位 ( x ) 所带来的 ( f(x) ) 增量越来越大。</li></ul><p>应用到收益函数上：</p><ul><li><strong>自变量 ( x )</strong>：通常是销售量、广告投入、努力程度或投资规模。</li><li><strong>因变量 ( R(x) )</strong>：表示总收入。</li></ul><p><strong>结论</strong>：一个凸的收益函数意味着<strong>边际收益是递增的</strong>。即：你卖得越多，每多卖出一个单位产品所带来的额外收入不仅不会减少，反而会越来越多。</p><h3 id="2-数学表达"><a href="#2-数学表达" class="headerlink" title="2. 数学表达"></a>2. 数学表达</h3><p>假设收益函数为 ( R(x) )，它相对于 ( x ) 是凸的，则满足：</p><ol><li><strong>二阶导数大于零</strong>：( R’’(x) &gt; 0 )</li><li><strong>边际收益递增</strong>：( MR(x+1) &gt; MR(x) ) （边际收益随着销量增加而增加）</li></ol><h3 id="3-直观的经济直觉"><a href="#3-直观的经济直觉" class="headerlink" title="3. 直观的经济直觉"></a>3. 直观的经济直觉</h3><p>在传统的经济学教科书（如完全竞争市场）中，我们通常假设企业是价格接受者，收益函数是线性的（( R &#x3D; P \times Q )，P是常数），这既是凸函数也是凹函数（线性函数的二阶导数为0）。<br>在垄断市场中，为了将产品多卖出去，企业通常需要降价，因此收益函数往往是凹的（边际收益递减）。</p><p>那么，在什么情况下会出现<strong>凸收益函数（边际收益递增）</strong>？</p><p>这通常发生在存在<strong>正反馈</strong>或<strong>网络效应</strong>的市场中。</p><h3 id="4-现实世界中的例子"><a href="#4-现实世界中的例子" class="headerlink" title="4. 现实世界中的例子"></a>4. 现实世界中的例子</h3><p><strong>① 网络效应（平台经济）</strong></p><ul><li><strong>情景</strong>：假设你是一个社交媒体平台的运营者，( x ) 是用户数量。</li><li><strong>凸性表现</strong>：<ul><li>当用户很少时（比如100人），新加入一个用户带来的广告收益可能只有1元。</li><li>但当用户达到1亿人时，由于网络的密度和互动性，新加入一个用户不仅自己看广告，还能吸引更多人来互动，此时新用户带来的边际收益可能高达10元。</li><li>因此，收益 ( R(x) ) 随着用户数 ( x ) 的增加而加速增长，形成凸函数。</li></ul></li></ul><p><strong>② 赢家通吃的竞赛</strong></p><ul><li><strong>情景</strong>：竞赛或研发投入。假设 ( x ) 是你投入的研发资金，( R(x) ) 是赢得大奖的概率乘以奖金。</li><li><strong>凸性表现</strong>：<ul><li>在竞赛中，如果你投入很少，可能根本赢不了（收益为0）。</li><li>当你投入超过某个临界点（成为领先者）时，你赢得整个市场的概率急剧上升，收益瞬间放大。这种“阈值效应”也会导致收益函数在某些区间呈现凸性。</li></ul></li></ul><p><strong>③ 金融衍生品（期权）</strong></p><ul><li><strong>情景</strong>：你购买了一个看涨期权。( x ) 是标的资产（如股票）的价格。</li><li><strong>凸性表现</strong>：<ul><li>如果股票价格低于行权价，你的收益为0（固定）。</li><li>一旦股票价格超过行权价，你的收益开始变为正，并且随着股价每上涨1元，你的收益也增加1元（线性）。虽然期权收益函数通常被称为是凸的，但在到期日它是分段线性的，而在到期前，其理论价格相对于标的资产价格是严格的凸函数。</li></ul></li></ul><h3 id="5-凸收益函数的决策含义"><a href="#5-凸收益函数的决策含义" class="headerlink" title="5. 凸收益函数的决策含义"></a>5. 凸收益函数的决策含义</h3><p>如果一个决策者面对的是凸收益函数，这会导致非常激进的行为策略：</p><ul><li><strong>极端化策略</strong>：因为边际收益递增，做一点点的尝试是低效的。最优策略往往是**“要么不做，要做就做最大”**。因为没有中间状态是最优的。</li><li><strong>规模效应</strong>：凸收益函数是产生自然垄断或寡头垄断的数学基础。因为大企业的边际收益远高于小企业，市场最终会向头部玩家集中。</li><li><strong>与刀刃均衡的关系</strong>：凸收益函数往往是导致“刀刃均衡”的原因之一。因为在凸收益的作用下，系统存在一个临界点：一旦超过临界点，收益会加速增长，从而脱离原来的均衡轨道。</li></ul><h3 id="6-总结"><a href="#6-总结" class="headerlink" title="6. 总结"></a>6. 总结</h3><ul><li><strong>凹函数</strong>：边际收益递减（增长得越来越慢）—— 常见于传统实物商品市场（卖得越多，越难卖，越要降价）。</li><li><strong>线性函数</strong>：边际收益不变（匀速增长）。</li><li><strong>凸函数</strong>：边际收益递增（增长得越来越快）—— 常见于数字经济、网络平台和具有正反馈的领域。它描述了一种“强者恒强”的收益结构。</li></ul><h1 id="4-Cornut-and-Bertrand-competition"><a href="#4-Cornut-and-Bertrand-competition" class="headerlink" title="4.Cornut and Bertrand competition"></a>4.Cornut and Bertrand competition</h1><hr><p><strong>Bertrand Competition（伯特兰竞争）</strong> 和 <strong>Cournot Competition（古诺竞争）</strong> 是产业组织理论中描述<strong>寡头垄断市场</strong>（少数几家大公司控制市场）的两个最基础、最经典的博弈模型。</p><p>它们都试图解释当市场上只有少数几个卖家时，他们会如何行为以及市场会达到什么样的均衡。两者的核心区别在于<strong>企业决策的变量不同</strong>：</p><ol><li><strong>古诺竞争</strong>：企业决定<strong>生产多少</strong>（产量竞争）。</li><li><strong>伯特兰竞争</strong>：企业决定<strong>定什么价</strong>（价格竞争）。</li></ol><p>正是这个关键差异，导致了截然不同的市场结果。</p><hr><h3 id="1-古诺竞争"><a href="#1-古诺竞争" class="headerlink" title="1. 古诺竞争"></a>1. 古诺竞争</h3><ul><li><strong>提出者</strong>：奥古斯丁·古诺</li><li><strong>决策变量</strong>：<strong>产量</strong></li><li><strong>核心逻辑</strong>：每个企业根据对手可能生产的产量，来决定自己的产量，从而共同决定市场价格。</li></ul><h4 id="核心思想"><a href="#核心思想" class="headerlink" title="核心思想"></a>核心思想</h4><p>市场上只有两家公司（A和B）生产完全相同的矿泉水。</p><ol><li>它们同时决定生产多少瓶水。</li><li>总产量决定了市场上的总供给，从而决定了水的价格（供给越多，价格越低）。</li><li>A在决定产量时，会猜测B生产多少，然后选择一个能让自己利润最大化的产量（反应函数）。</li><li>双方不断调整猜测，最终达到一个谁都不想单方面改变产量的状态——这就是<strong>古诺均衡</strong>。</li></ol><h4 id="关键结论"><a href="#关键结论" class="headerlink" title="关键结论"></a>关键结论</h4><ul><li><strong>市场结果</strong>：价格高于完全竞争市场，但低于完全垄断市场；总产量低于完全竞争，但高于完全垄断。</li><li><strong>特点</strong>：每个企业都有一定的市场势力，能赚取一定的经济利润，但不如垄断者那么多。</li></ul><hr><h3 id="2-伯特兰竞争"><a href="#2-伯特兰竞争" class="headerlink" title="2. 伯特兰竞争"></a>2. 伯特兰竞争</h3><ul><li><strong>提出者</strong>：约瑟夫·伯特兰</li><li><strong>决策变量</strong>：<strong>价格</strong></li><li><strong>核心逻辑</strong>：每个企业根据对手可能定的价格，来决定自己的价格，消费者从最低价那里购买。</li></ul><h4 id="核心思想-1"><a href="#核心思想-1" class="headerlink" title="核心思想"></a>核心思想</h4><p>同样是两家公司生产完全相同的矿泉水。</p><ol><li>它们同时宣布自己的<strong>价格</strong>。</li><li>因为水是一样的，消费者都会去买价格低的那家。</li><li>如果A定价5元，B为了抢走所有客户，会定价4.9元。</li><li>A看到后，为了抢回客户，会再降价到4.8元。</li><li>这种价格战会一直持续，直到价格降到等于<strong>成本</strong>为止。</li></ol><h4 id="关键结论-1"><a href="#关键结论-1" class="headerlink" title="关键结论"></a>关键结论</h4><ul><li><strong>市场结果</strong>：价格等于边际成本（生产成本）。</li><li><strong>特点</strong>：<strong>伯特兰悖论</strong>——即便市场上只有两家公司，只要它们进行价格竞争且产品同质，就能达到完全竞争市场的效果。企业没有利润（经济利润为零）。</li></ul><hr><h3 id="3-核心区别对比表"><a href="#3-核心区别对比表" class="headerlink" title="3. 核心区别对比表"></a>3. 核心区别对比表</h3><table><thead><tr><th align="left">维度</th><th align="left">古诺竞争</th><th align="left">伯特兰竞争</th></tr></thead><tbody><tr><td align="left"><strong>决策变量</strong></td><td align="left"><strong>产量</strong></td><td align="left"><strong>价格</strong></td></tr><tr><td align="left"><strong>均衡价格</strong></td><td align="left">高于边际成本</td><td align="left">等于边际成本</td></tr><tr><td align="left"><strong>企业利润</strong></td><td align="left">正的经济利润</td><td align="left">零经济利润</td></tr><tr><td align="left"><strong>产品性质</strong></td><td align="left">通常假设同质</td><td align="left">严格假设同质（否则结果不同）</td></tr><tr><td align="left"><strong>核心过程</strong></td><td align="left">调整产量直到市场出清</td><td align="left">调整价格直到价格战结束</td></tr><tr><td align="left"><strong>直观理解</strong></td><td align="left">建工厂、定产能（慢变量）</td><td align="left">定标签、打折促销（快变量）</td></tr></tbody></table><h3 id="4-为什么会有这么大的差异？（产能约束的关键）"><a href="#4-为什么会有这么大的差异？（产能约束的关键）" class="headerlink" title="4. 为什么会有这么大的差异？（产能约束的关键）"></a>4. 为什么会有这么大的差异？（产能约束的关键）</h3><p>这个差异的核心在于企业对<strong>产能</strong>的假设。</p><ul><li><p><strong>古诺的世界</strong>：<br>企业一旦决定了产量，就不能轻易改变（比如工厂已经建好了，苹果摘下来不能放回去）。如果我把价格定得很低，但我的产量有限，我没法满足所有涌来的顾客。因此，企业倾向于用产量来竞争。</p></li><li><p><strong>伯特兰的世界</strong>：<br>假设企业有无限产能，可以满足整个市场的需求。只要你价格比我低一丁点，所有顾客都跑你那儿去了，我会一无所获。这迫使双方疯狂降价，直到成本价。</p></li></ul><h3 id="5-现实生活中的例子"><a href="#5-现实生活中的例子" class="headerlink" title="5. 现实生活中的例子"></a>5. 现实生活中的例子</h3><h4 id="古诺竞争的例子：石油生产"><a href="#古诺竞争的例子：石油生产" class="headerlink" title="古诺竞争的例子：石油生产"></a>古诺竞争的例子：石油生产</h4><ul><li><strong>场景</strong>：沙特阿拉伯、俄罗斯、美国（页岩油）。</li><li><strong>为什么是古诺？</strong>：石油的生产取决于油田投资和钻井，产能建设周期长。一旦决定日产量1000万桶，短期内很难大幅改变。各国主要盯着对方的<strong>产量配额</strong>来决定自己的产量，从而影响全球油价。</li></ul><h4 id="伯特兰竞争的例子：电商平台"><a href="#伯特兰竞争的例子：电商平台" class="headerlink" title="伯特兰竞争的例子：电商平台"></a>伯特兰竞争的例子：电商平台</h4><ul><li><strong>场景</strong>：京东、拼多多、天猫卖同一款品牌家电（如某品牌电视机）。</li><li><strong>为什么是伯特兰？</strong>：只要鼠标一点，消费者就能看到全网价格，且商品完全一样（同质）。哪家便宜消费者就去哪家买。这导致了激烈的价格战，家电的零售利润率通常被压得很低，接近成本。</li></ul><h3 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h3><ul><li><strong>古诺竞争</strong>是关于<strong>产能和数量</strong>的博弈。它描述的是企业通过设定生产规模来竞争，结果导致价格高于成本，大家都有利可图。</li><li><strong>伯特兰竞争</strong>是关于<strong>价格</strong>的博弈。它描述的是企业通过降价来抢生意，结果导致价格等于成本，谁也没赚到钱。</li></ul><p>在实际分析中，经济学家会根据行业特性选择模型：如果行业产能调整慢（如重工业），常用古诺模型；如果行业价格调整快且产能充足（如零售业），常用伯特兰模型。</p><h1 id="5-LP-token"><a href="#5-LP-token" class="headerlink" title="5.LP token"></a>5.LP token</h1><hr><p><strong>LP Token (Liquidity Provider Token)</strong>，中文常称为<strong>流动性提供者代币</strong>，是去中心化交易所（DEX，如 Uniswap, SushiSwap）中的一个核心概念。</p><p>简单来说，它是<strong>你在流动性池（Pool）中存款的“凭证”或“收据”</strong>。</p><h3 id="1-为什么需要-LP-Token？"><a href="#1-为什么需要-LP-Token？" class="headerlink" title="1. 为什么需要 LP Token？"></a>1. 为什么需要 LP Token？</h3><p>想象一个场景：</p><ul><li><strong>流动性池</strong>就像一个大水池，里面混着两种代币（比如 ETH 和 USDT）。</li><li>**你（流动性提供者）**往这个池子里倒入了 10 个 ETH 和 20,000 个 USDT。</li><li><strong>其他人</strong>也往里面倒入了不同数量的代币。</li></ul><p>当你以后想把钱取出来的时候，怎么证明这池子里<strong>有多少比例的水是属于你的</strong>？</p><p>这时候，交易所（DEX）的智能合约就会发给你一种特殊的代币——<strong>LP Token</strong>。</p><h3 id="2-它代表什么？"><a href="#2-它代表什么？" class="headerlink" title="2. 它代表什么？"></a>2. 它代表什么？</h3><ul><li><strong>所有权证明</strong>：LP Token 代表了你在这个巨大的资金池中所占的<strong>份额（Share）</strong>。</li><li><strong>如果你持有 10% 的 LP Token</strong>：意味着无论池子里现在的资金总量是多少，你都有权赎回池子总资金的 10%。</li></ul><h3 id="3-具体运作流程"><a href="#3-具体运作流程" class="headerlink" title="3. 具体运作流程"></a>3. 具体运作流程</h3><ol><li><p><strong>存钱（Add Liquidity）</strong>：</p><ul><li>你向 ETH&#x2F;USDT 池子存入资金。</li><li>合约确实收到了你的币。</li><li>合约**铸造（Mint）新的 LP Token 发送给你。</li><li><em>比如：你存入了占池子 1% 的资金，你就会收到总 LP Token 供应量 1% 的新 Token。</em></li></ul></li><li><p><strong>持有期间（Earning Fees）</strong>：</p><ul><li>交易者在这个池子里进行兑换（比如把 ETH 换成 USDT），需要支付<strong>交易手续费</strong>（通常是 0.3%）。</li><li>这些手续费会直接留在池子里，导致池子里的资金总量变多了。</li><li>因为你持有固定比例的 LP Token，池子变大了，你的那 1% 对应的价值也变大了。<strong>这就相当于你在赚利息。</strong></li></ul></li><li><p><strong>取钱（Remove Liquidity）</strong>：</p><ul><li>你想退出了。你把手中的 LP Token 发回给合约。</li><li>合约**销毁（Burn）**这些 LP Token。</li><li>合约根据你销毁的份额，从池子里拿出对应比例的 ETH 和 USDT 还给你。</li><li><em>注意：拿回来的两种币数量可能和你存进去时不一样（因为币价波动和有人交易），但总价值通常会包含赚取的手续费。</em></li></ul></li></ol><h3 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h3><p>在你的代码 <code>liquidity.js</code> 和 <code>mockState.js</code> 中：</p><ul><li><code>addLiquidity</code> 函数会计算你应该得到多少 LP Token，并在模拟数据库里增加你的 LP 余额。</li><li><code>removeLiquidity</code> 函数会检查你有多少 LP Token，扣除它们，然后通过计算把你应得的原始代币（e.g., WBKC 和 E20C）还给你。</li></ul><p><strong>一句话：LP Token 就是你在 DEX 银行里的“定期存款单”，凭此单据取款并分红。</strong></p><h1 id="6-Gas-cost-amortization"><a href="#6-Gas-cost-amortization" class="headerlink" title="6.Gas-cost amortization"></a>6.Gas-cost amortization</h1><hr><p>在理解DeFi（去中心化金融）的运作时，Gas费是不可避免的一环。你提到的“Gas-cost amortization”（Gas成本摊销）正是该领域一个核心且精妙的概念。简单来说，它不是指某一种特定的技术，而是一套将一次性、高额的交易成本，通过多种策略进行<strong>分摊、平滑和优化</strong>的机制，最终目的是提升区块链的效率和用户体验。</p><p>我们可以用一个生活中的例子来帮助理解：这就像你不再为每一次短途出行都单独叫一辆出租车（成本高昂且低效），而是改乘一辆能同时搭载多位乘客的班车（分摊成本），甚至为自己买一张月票（锁定长期成本）。</p><p>基于这个比喻，DeFi中实现Gas成本摊销主要有以下几种方式：</p><h3 id="📦-批量处理：从“打车”到“班车”"><a href="#📦-批量处理：从“打车”到“班车”" class="headerlink" title="📦 批量处理：从“打车”到“班车”"></a>📦 批量处理：从“打车”到“班车”</h3><p>这是最基本也最直观的摊销方式。普通的区块链交易，每笔都需要独立支付Gas费，如同一个人打一辆车。而批量处理技术将多个用户的操作“打包”成一笔交易，然后一起提交到区块链上，大家共同分摊这一笔交易的Gas费。</p><ul><li><strong>Layer 2 扩容方案</strong>：像Starkware、Arbitrum这样的Layer 2项目是这方面的典型代表。它们在一个“链下”环境中收集成千上万笔交易，进行压缩和计算后，只将一个最终的结果证明（一个“包裹”）提交到以太坊主网。这样，数万笔交易的总Gas费由所有用户均摊。据Starkware联合创始人透露，通过这种方式，其Gas成本可以降低数万倍。</li><li><strong>账户抽象（AA）</strong>：这项技术让智能合约钱包可以一次性执行一系列连续操作，例如在一个步骤内完成“批准A代币”和“将A代币兑换为B代币”。这原本需要两笔独立交易，现在合并为一笔，不仅省去了中间环节的等待，还节省了至少一次的Gas开销。</li></ul><h3 id="🎟️-费用抽象与订阅：从“按次付费”到“购买月票”"><a href="#🎟️-费用抽象与订阅：从“按次付费”到“购买月票”" class="headerlink" title="🎟️ 费用抽象与订阅：从“按次付费”到“购买月票”"></a>🎟️ 费用抽象与订阅：从“按次付费”到“购买月票”</h3><p>这类策略试图改变Gas费的支付方式和计价模式，使其变得更可预测、更易于管理。</p><ul><li><strong>元交易与Paymaster</strong>：这是“费用抽象”的体现。用户发起交易时，可以不直接支付Gas，而是通过一个称为“Paymaster”的智能合约进行。这个Paymaster可以由应用开发者运营，用应用内的代币（如USDC）或其他条件来代为支付Gas费。这相当于应用为用户购买了“班车票”，用户无需关心底层的Gas成本。</li><li><strong>Gas订阅市场</strong>：这是一个更前沿的探索，由Foil等项目提出。它旨在创建一个围绕Gas费用的金融市场。用户可以提前购买一个“Gas订阅计划”，锁定未来一段时间的Gas价格，就像为手机话费购买月度套餐一样。这样一来，无论网络如何拥堵，交易成本都是可预测和可控的，成功地将波动的运营成本转化为了固定的摊销支出。</li><li></li></ul><h3 id="总结-3"><a href="#总结-3" class="headerlink" title="总结"></a>总结</h3><p>总的来说，“Gas成本摊销”是DeFi走向成熟和主流采用的必经之路。它通过<strong>批量处理、费用抽象和协议内平滑</strong>等多种技术手段，将不可预测、有时甚至是高昂的Gas费，转化为一种可管理、可分摊、甚至对终端用户完全透明的运营成本。这不仅极大地改善了用户体验，也为开发者创造了全新的商业模式，让构建复杂、高频的链上应用成为可能。</p>]]></content>
      
      
      <categories>
          
          <category> Lab </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BlockChain </tag>
            
            <tag> DeFi </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|提取Wallpaper Engine壁纸作为AnZhiYu主题首页动态封面</title>
      <link href="/2026/02/13/%E5%A6%82%E4%BD%95%E6%8A%8Awallpaper%E7%9A%84%E5%A3%81%E7%BA%B8%E6%94%BE%E5%88%B0%E4%BD%A0%E7%9A%84Hexo%E5%8D%9A%E5%AE%A2%E4%B8%8A/"/>
      <url>/2026/02/13/%E5%A6%82%E4%BD%95%E6%8A%8Awallpaper%E7%9A%84%E5%A3%81%E7%BA%B8%E6%94%BE%E5%88%B0%E4%BD%A0%E7%9A%84Hexo%E5%8D%9A%E5%AE%A2%E4%B8%8A/</url>
      
        <content type="html"><![CDATA[<p>Wallpaper Engine 里有太多好看的动态壁纸了，有没有想过把它们搬到自己的 Hexo 博客上作为首页 Banner？<br>这篇文章将记录全过程：从 Wallpaper Engine 提取视频资源，并<strong>硬核修改 AnZhiYu 主题源码</strong>，让它原生支持 MP4 视频封面播放。</p><span id="more"></span><h1 id="1-导出-Wallpaper-Engine-壁纸"><a href="#1-导出-Wallpaper-Engine-壁纸" class="headerlink" title="1. 导出 Wallpaper Engine 壁纸"></a>1. 导出 Wallpaper Engine 壁纸</h1><p>首先在 Wallpaper Engine (Steam版) 中挑选一个喜欢的壁纸（最好是视频场景类的）。</p><ol><li>在壁纸库中右键点击壁纸。</li><li>选择 <strong>发送至移动设备</strong> -&gt; <strong>导出 .mpkg 文件</strong>。</li><li>导出完成后，将获得一个 <code>.mpkg</code> 文件，手动将其后缀名修改为 <code>.pkg</code>。</li></ol><h1 id="2-使用-RePKG-提取视频资源"><a href="#2-使用-RePKG-提取视频资源" class="headerlink" title="2. 使用 RePKG 提取视频资源"></a>2. 使用 RePKG 提取视频资源</h1><p>MPKG&#x2F;PKG 本质是加密或打包的格式，我们需要专用工具将其解包。</p><ol><li>下载开源工具 <a href="https://github.com/Keineliebeundkeinleid/RePKG-GUI">RePKG-GUI</a>。</li><li>解压并打开应用程序，按照 GUI 界面指引，将上一步的 <code>.pkg</code> 文件拖入。</li><li>点击 <strong>输出 (Extract)</strong>。</li><li>在输出目录中，你通常能找到一张预览图和一个高质量的 <code>.mp4</code> 视频文件。</li></ol><p><img src="/pics/2026-2/RePKG.png"></p><h1 id="3-魔改主题源码-核心步骤"><a href="#3-魔改主题源码-核心步骤" class="headerlink" title="3. 魔改主题源码 (核心步骤)"></a>3. 魔改主题源码 (核心步骤)</h1><p>AnZhiYu 主题默认的首页 Banner 逻辑仅针对图片 (<code>&lt;img&gt;</code> 标签)。为了让它兼容视频，我们需要修改底层的 Pug 模板引擎代码。</p><p>定位文件：<code>themes/anzhiyu/layout/includes/top/top.pug</code></p><p>找到渲染 <code>todayCard-cover</code> 的位置（大约在文件末尾部分），将原有的 <code>img</code> 标签逻辑替换为如下代码：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">//- 识别 .mp4 结尾的链接，渲染为 video 标签</span><br><span class="line">if top_group_banner_image.toLowerCase().endsWith(&#x27;.mp4&#x27;)</span><br><span class="line">  video.todayCard-cover(src=url_for(top_group_banner_image) autoplay loop muted playsinline style=&#x27;object-fit: cover;&#x27;)</span><br><span class="line">else</span><br><span class="line">  img.todayCard-cover(src=url_for(top_group_banner_image) onerror=`this.onerror=null;this.src=&#x27;$&#123;url_for(theme.error_img.post_page)&#125;&#x27;` alt=&#x27;封面&#x27;)</span><br></pre></td></tr></table></figure><p><strong>原理说明：</strong><br>这段代码通过判断我们在配置文件中填写的路径是否以 <code>.mp4</code> 结尾，来智能决定是渲染普通的图片标签，还是带有 <code>autoplay</code> 属性的视频标签。</p><h1 id="4-部署与配置"><a href="#4-部署与配置" class="headerlink" title="4. 部署与配置"></a>4. 部署与配置</h1><p>万事俱备，最后只需要把资源放进去并开启配置。</p><ol><li><strong>移动文件</strong>：将提取出的 <code>wallpaper.mp4</code> 放入博客源文件夹，例如 <code>source/mp4/wallpaper.mp4</code>。</li><li><strong>修改配置</strong>：编辑 <code>_config.anzhiyu.yml</code>，定位到 <code>home_top</code> -&gt; <code>banner</code>。</li></ol><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">home_top:</span></span><br><span class="line">  <span class="attr">banner:</span></span><br><span class="line">    <span class="attr">tips:</span> <span class="string">享受动态的快乐</span></span><br><span class="line">    <span class="attr">title:</span> <span class="string">Wallpaper</span> <span class="string">Engine</span></span><br><span class="line">    <span class="comment"># 这里直接填写视频的路径</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">/mp4/wallpaper.mp4</span></span><br><span class="line">    <span class="attr">link:</span> <span class="string">/</span></span><br></pre></td></tr></table></figure><ol start="3"><li><strong>预览</strong>：执行 <code>hexo clean &amp;&amp; hexo s</code>，刷新首页，你应该能看到封皮已经动起来了！</li></ol><blockquote><p><strong>Tips</strong>: 建议视频文件不要过大（建议压缩在 5MB 以内），否则会影响网站首屏加载速度。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Tools </tag>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|为关于页(About)注入更多交互灵魂</title>
      <link href="/2026/02/11/AnZhiYu-About-Page-Interaction-Optimization/"/>
      <url>/2026/02/11/AnZhiYu-About-Page-Interaction-Optimization/</url>
      
        <content type="html"><![CDATA[<p>继上一篇对生涯卡片的改造后，我发现关于页面（About）的其他静态卡片也略显沉闷。为了让整个页面“活”起来，我进行了一系列的交互设计优化，包括给卡片添加金属光泽的<strong>擦亮特效</strong>、<strong>背景聚焦动画</strong>以及<strong>点击跳转</strong>功能。</p><p>本文将详细记录这些优化的实现过程。</p><h2 id="1-视觉升级：全员“擦亮”特效"><a href="#1-视觉升级：全员“擦亮”特效" class="headerlink" title="1. 视觉升级：全员“擦亮”特效"></a>1. 视觉升级：全员“擦亮”特效</h2><p>为了给平淡的卡片增加质感，我引入了一种 CSS 扫光动画（Shine Effect）。当鼠标悬停时，一道半透明的白光会迅速划过卡片表面，模拟金属反光的效果。</p><h3 id="CSS-实现"><a href="#CSS-实现" class="headerlink" title="CSS 实现"></a>CSS 实现</h3><p>修改 <code>themes/anzhiyu/source/css/_page/about.styl</code>，在文件末尾追加以下代码。我一次性为自我介绍、个人数据、MBTI、游戏、特长、座右铭等多个卡片应用了此效果：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 卡片擦亮特效 */</span></span><br><span class="line"><span class="comment">/* 定义伪元素作为光束 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.myInfoAndSayHello</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.selfInfo</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.personalities</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.game-yuanshen</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.like-technology</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.buff</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.maxim</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.aboutsiteTips</span><span class="selector-pseudo">::before</span> &#123;</span><br><span class="line">  <span class="attribute">content</span>: <span class="string">&quot;&quot;</span>;</span><br><span class="line">  <span class="attribute">position</span>: absolute;</span><br><span class="line">  <span class="attribute">top</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="comment">/* 线性渐变制作透明-白-透明的光束 */</span></span><br><span class="line">  <span class="attribute">background</span>: <span class="built_in">linear-gradient</span>(to right, transparent, <span class="built_in">rgba</span>(<span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>, <span class="number">0.3</span>), transparent);</span><br><span class="line">  <span class="comment">/* 初始位置在左侧外部，并倾斜25度 */</span></span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translate3d</span>(-<span class="number">150%</span>, <span class="number">0</span>, <span class="number">0</span>) <span class="built_in">skewX</span>(-<span class="number">25deg</span>);</span><br><span class="line">  <span class="attribute">transition</span>: transform <span class="number">0.5s</span>; <span class="comment">/* 动画时长0.5秒 */</span></span><br><span class="line">  <span class="attribute">z-index</span>: <span class="number">1</span>;</span><br><span class="line">  <span class="attribute">pointer-events</span>: none; <span class="comment">/* 确保光束不遮挡点击 */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 悬停时的目标状态 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.myInfoAndSayHello</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.selfInfo</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.personalities</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.game-yuanshen</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.like-technology</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.buff</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.maxim</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.aboutsiteTips</span><span class="selector-pseudo">:hover</span><span class="selector-pseudo">::before</span> &#123;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translate3d</span>(<span class="number">150%</span>, <span class="number">0</span>, <span class="number">0</span>) <span class="built_in">skewX</span>(-<span class="number">25deg</span>); <span class="comment">/* 移动到右侧外部 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-交互升级：音乐卡片的“聚焦”体验"><a href="#2-交互升级：音乐卡片的“聚焦”体验" class="headerlink" title="2. 交互升级：音乐卡片的“聚焦”体验"></a>2. 交互升级：音乐卡片的“聚焦”体验</h2><p>对于音乐偏好卡片，我不希望它只是静态的。当鼠标悬停时，背景专辑封面缓慢放大，营造出一种“聚焦”或“沉浸”的听歌氛围。同时，点击卡片应直接播放歌单。</p><h3 id="样式优化-about-styl"><a href="#样式优化-about-styl" class="headerlink" title="样式优化 (about.styl)"></a>样式优化 (<code>about.styl</code>)</h3><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 音乐卡片 Hover 放大聚焦效果 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.like-music</span> &#123;</span><br><span class="line">  <span class="attribute">background-size</span>: <span class="number">100%</span> <span class="meta">!important</span>; <span class="comment">/* 初始大小 */</span></span><br><span class="line">  <span class="attribute">transition</span>: background-size <span class="number">0.5s</span> ease-in-out <span class="meta">!important</span>; <span class="comment">/* 平滑过渡 */</span></span><br><span class="line">  <span class="attribute">cursor</span>: pointer; <span class="comment">/* 鼠标变为手型 */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.like-music</span><span class="selector-pseudo">:hover</span> &#123;</span><br><span class="line">  <span class="attribute">background-size</span>: <span class="number">120%</span> <span class="meta">!important</span>; <span class="comment">/* 放大至120% */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="逻辑修改-about-pug"><a href="#逻辑修改-about-pug" class="headerlink" title="逻辑修改 (about.pug)"></a>逻辑修改 (<code>about.pug</code>)</h3><p>支持点击跳转并携带参数，让访客能直接听到我推荐的歌单：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">.author-content-item.like-music(</span><br><span class="line">  style=`background: url($&#123;music_bg&#125;) top / cover no-repeat;`</span><br><span class="line">  onclick=`pjax.loadUrl(&quot;/music/?id=6909251788&amp;server=netease&quot;)` </span><br><span class="line">)</span><br></pre></td></tr></table></figure><h2 id="3-功能补全：游戏卡片外链"><a href="#3-功能补全：游戏卡片外链" class="headerlink" title="3. 功能补全：游戏卡片外链"></a>3. 功能补全：游戏卡片外链</h2><p>同样地，对于展示《Street Fighter VI》的游戏卡片，我希望点击它能让感兴趣的朋友直达官网。</p><p>修改 <code>themes/anzhiyu/layout/includes/page/about.pug</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">div.author-content-item.game-yuanshen(</span><br><span class="line">  style=`background: url($&#123;game_bg&#125;) top / cover no-repeat; cursor: pointer;`</span><br><span class="line">  onclick=`window.open(&#x27;https://www.streetfighter.com/6/zh-hans&#x27;, &#x27;_blank&#x27;)`</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>这里使用了 <code>window.open</code> 而不是 <code>pjax.loadUrl</code>，因为是要跳转到外部网站。</p><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>通过上述几步简单的 CSS 和 Pug 模板修改，我们将原本相对静态的信息展示页，变成了一个充满微交互的生动页面。每一个光影的流转、每一次背景的缩放，都在无形中提升了用户的浏览体验。</p><p>快去给你的博客关于页也加上这些特效吧！</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
            <tag> CSS动画 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|升级关于页(About)生涯卡片</title>
      <link href="/2026/02/11/AnZhiYu-About-Page-Upgrade/"/>
      <url>/2026/02/11/AnZhiYu-About-Page-Upgrade/</url>
      
        <content type="html"><![CDATA[<p>本文记录了针对 Hexo 博客 AnZhiYu 主题“关于”页面（About）中“生涯卡片”模块的定制化修改过程。原主题的生涯列表较为简洁，仅支持纯色圆点与文本。本次修改旨在实现以下需求：</p><ol><li><strong>支持图片图标</strong>：将原本的纯色圆点替换为可自定义的图片。</li><li><strong>样式增强</strong>：增大图标与字体尺寸，增加行间距，提升视觉冲击力。</li><li><strong>交互优化</strong>：增加图标悬停放大动画，并支持点击图标跳转外链。</li></ol><h1 id="效果预览"><a href="#效果预览" class="headerlink" title="效果预览"></a>效果预览</h1><hr><p><img src="/pics/2026-2/Career.png"></p><h2 id="1-修改模板文件-about-pug"><a href="#1-修改模板文件-about-pug" class="headerlink" title="1. 修改模板文件 (about.pug)"></a>1. 修改模板文件 (<code>about.pug</code>)</h2><p>首先需要修改页面的结构模板，使其能够读取并在 YAML 配置中定义的图片路径和跳转链接。</p><p><strong>文件路径</strong>：<code>themes/anzhiyu/layout/includes/page/about.pug</code></p><p>找到 <code>.careers-group</code> 相关的代码块，将其替换为以下逻辑：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">.careers-group</span><br><span class="line">  if crrList</span><br><span class="line">    each career in crrList</span><br><span class="line">      .career-item</span><br><span class="line">        //- 定义样式变量，优先使用 icon 图片，如果没有则回退到 color 纯色</span><br><span class="line">        - var circleStyle = `background: $&#123;career.icon ? `url($&#123;url_for(career.icon)&#125;) center center / cover no-repeat` : (career.color ? career.color : &quot;#357ef5&quot;)&#125;`</span><br><span class="line">        </span><br><span class="line">        //- 如果配置了 href，则渲染为 a 标签，否则为普通 div</span><br><span class="line">        if career.href</span><br><span class="line">          a.circle(href=career.href, target=&quot;_blank&quot;, style=circleStyle)</span><br><span class="line">        else</span><br><span class="line">          .circle(style=circleStyle)</span><br><span class="line">          </span><br><span class="line">        .name=career.desc</span><br><span class="line">  else</span><br><span class="line">    .careers-none</span><br></pre></td></tr></table></figure><p><strong>修改解析</strong>：</p><ul><li>引入了 <code>circleStyle</code> 变量，通过三元运算符判断是否使用 <code>icon</code> 图片背景。</li><li>增加了 <code>career.href</code> 判断，实现点击跳转功能。</li></ul><h2 id="2-调整样式文件-about-styl"><a href="#2-调整样式文件-about-styl" class="headerlink" title="2. 调整样式文件 (about.styl)"></a>2. 调整样式文件 (<code>about.styl</code>)</h2><p>接下来调整 CSS 样式，控制元素的大小、间距以及动画效果。</p><p><strong>文件路径</strong>：<code>themes/anzhiyu/source/css/_page/about.styl</code></p><p>查找 <code>.author-content-item.careers</code> 下的相关样式并进行修改：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 增加列表项之间的间距 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span> <span class="selector-class">.career-item</span> &#123;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">justify-content</span>: flex-start;</span><br><span class="line">  <span class="attribute">align-items</span>: center;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">12px</span> <span class="number">0</span>; <span class="comment">/*设置上下间距*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 调整图标/圆点尺寸及动画 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span> <span class="selector-class">.career-item</span> <span class="selector-class">.circle</span> &#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">48px</span>;  <span class="comment">/* 增大尺寸 */</span></span><br><span class="line">  <span class="attribute">height</span>: <span class="number">48px</span>;</span><br><span class="line">  <span class="attribute">margin-right</span>: <span class="number">16px</span>;</span><br><span class="line">  <span class="attribute">border-radius</span>: <span class="number">50%</span>;</span><br><span class="line">  <span class="attribute">transition</span>: transform <span class="number">0.3s</span>; <span class="comment">/* 添加过渡动画 */</span></span><br><span class="line">  <span class="attribute">cursor</span>: pointer; <span class="comment">/* 鼠标变为手型 */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 悬停放大效果 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span> <span class="selector-class">.career-item</span> <span class="selector-class">.circle</span><span class="selector-pseudo">:hover</span> &#123;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">scale</span>(<span class="number">1.2</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 调整文字样式 */</span></span><br><span class="line"><span class="selector-class">.author-content-item</span><span class="selector-class">.careers</span> <span class="selector-class">.career-item</span> <span class="selector-class">.name</span> &#123;</span><br><span class="line">  <span class="attribute">color</span>: <span class="built_in">var</span>(--anzhiyu-secondtext);</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="number">24px</span>; <span class="comment">/* 增大字号 */</span></span><br><span class="line">  <span class="attribute">font-weight</span>: bold; <span class="comment">/* 加粗 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-配置数据文件-about-yml"><a href="#3-配置数据文件-about-yml" class="headerlink" title="3. 配置数据文件 (about.yml)"></a>3. 配置数据文件 (<code>about.yml</code>)</h2><p>最后，在数据文件中添加图片路径和链接即可生效。</p><p><strong>文件路径</strong>：<code>source/_data/about.yml</code></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">careers:</span></span><br><span class="line">  <span class="attr">tips:</span> <span class="string">生涯</span></span><br><span class="line">  <span class="attr">title:</span> <span class="string">稳步前行</span></span><br><span class="line">  <span class="attr">list:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">desc:</span> <span class="string">某某大学</span></span><br><span class="line">      <span class="attr">color:</span> <span class="string">&quot;#357ef5&quot;</span></span><br><span class="line">      <span class="attr">icon:</span> <span class="string">/img/university_logo.png</span>   <span class="comment"># [新增] 图片路径</span></span><br><span class="line">      <span class="attr">href:</span> <span class="string">https://www.university.edu</span> <span class="comment"># [新增] 跳转链接</span></span><br><span class="line">      </span><br><span class="line">    <span class="bullet">-</span> <span class="attr">desc:</span> <span class="string">某某中学</span></span><br><span class="line">      <span class="attr">color:</span> <span class="string">&quot;#ff0000&quot;</span>                 <span class="comment"># 未设置 icon 时显示此颜色</span></span><br></pre></td></tr></table></figure><h2 id="4-效果预览"><a href="#4-效果预览" class="headerlink" title="4. 效果预览"></a>4. 效果预览</h2><p>完成以上步骤后，执行以下命令清理缓存并预览：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo server</span><br></pre></td></tr></table></figure><p>现在的生涯卡片应该具备了更大的图文展示、灵动的悬停反馈以及实用的链接跳转功能，能更好地展示个人经历。</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|为AnZhiYu主题添加动态视频头像</title>
      <link href="/2026/02/11/AnZhiYu-Dynamic-Avatar/"/>
      <url>/2026/02/11/AnZhiYu-Dynamic-Avatar/</url>
      
        <content type="html"><![CDATA[<p>想要让你的博客侧边栏更加生动吗？相比于静态图片或 GIF，使用高质量的 MP4 视频作为头像不仅清晰度更高，还能实现完美的无缝循环。今天我们就来动手给 AnZhiYu 主题加上这个酷炫的功能。</p><h2 id="核心原理"><a href="#核心原理" class="headerlink" title="核心原理"></a>核心原理</h2><p>Hexo 的 AnZhiYu 主题默认使用 <code>img</code> 标签来显示头像。我们需要修改侧边栏作者卡片的模板文件，增加一个判断逻辑：如果配置文件中的头像路径以 <code>.mp4</code> 结尾，就渲染为 <code>&lt;video&gt;</code> 标签，否则保持原样的 <code>&lt;img&gt;</code> 标签。</p><h2 id="修改步骤"><a href="#修改步骤" class="headerlink" title="修改步骤"></a>修改步骤</h2><h3 id="1-修改-Pug-模板"><a href="#1-修改-Pug-模板" class="headerlink" title="1. 修改 Pug 模板"></a>1. 修改 Pug 模板</h3><p>找到主题目录下的 <code>themes/anzhiyu/layout/includes/widget/card_author.pug</code> 文件。</p><p>我们需要将原本直接输出 <code>img</code> 的代码替换为带有条件判断的逻辑。</p><p><strong>修改前：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">.author-info-avatar</span><br><span class="line">  img.avatar-img(src=url_for(theme.avatar.img) onerror=`this.onerror=null;this.src=&#x27;` + url_for(theme.error_img.flink) + `&#x27;` alt=&quot;avatar&quot;)</span><br></pre></td></tr></table></figure><p><strong>修改后：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">.author-info-avatar</span><br><span class="line">  if theme.avatar.img &amp;&amp; theme.avatar.img.endsWith(&#x27;.mp4&#x27;)</span><br><span class="line">    video.avatar-img(autoplay loop muted playsinline style=&quot;object-fit: cover;&quot;)</span><br><span class="line">      source(src=url_for(theme.avatar.img) type=&quot;video/mp4&quot;)</span><br><span class="line">  else</span><br><span class="line">    img.avatar-img(src=url_for(theme.avatar.img) onerror=`this.onerror=null;this.src=&#x27;` + url_for(theme.error_img.flink) + `&#x27;` alt=&quot;avatar&quot;)</span><br></pre></td></tr></table></figure><p>这段代码的作用是：当检测到 <code>theme.avatar.img</code> 配置项以 <code>.mp4</code> 结尾时，自动插入一个静音、自动播放、循环播放的视频标签。</p><h3 id="2-优化-CSS-样式"><a href="#2-优化-CSS-样式" class="headerlink" title="2. 优化 CSS 样式"></a>2. 优化 CSS 样式</h3><p>引入视频头像后，可能会遇到样式问题：</p><ol><li>头像右下角的“状态表情”（比如正在工作、休息中）可能会被视频层级遮挡。</li></ol><p>我们需要修改 <code>themes/anzhiyu/source/css/_layout/aside.styl</code> 文件。</p><p><strong>修复层级遮挡问题：</strong></p><p>找到 <code>.author-status</code> 类，添加 <code>z-index</code> 属性：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-class">.author-status</span></span><br><span class="line">  <span class="attribute">z-index</span>: <span class="number">100</span>; <span class="comment">/* 确保状态图标在视频上方 */</span></span><br><span class="line">  <span class="attribute">position</span>: absolute;</span><br><span class="line">  <span class="comment">/* ...其他原有属性... */</span></span><br></pre></td></tr></table></figure><h2 id="配置使用"><a href="#配置使用" class="headerlink" title="配置使用"></a>配置使用</h2><p>修改完成后，使用方法非常简单。你只需要准备一个正方形的 MP4 视频（建议比例 1:1，否则会被裁切），放入 <code>source/img/</code> 或其他资源目录。</p><p>然后修改你的配置文件 <code>_config.anzhiyu.yml</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">avatar:</span></span><br><span class="line">  <span class="attr">img:</span> <span class="string">/img/your-avatar.mp4</span></span><br><span class="line">  <span class="comment"># img: /img/avatar.jpg  &lt;-- 如果想换回图片，直接改回图片路径即可</span></span><br></pre></td></tr></table></figure><h2 id="重新部署"><a href="#重新部署" class="headerlink" title="重新部署"></a>重新部署</h2><p>最后，别忘了三连击：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo generate</span><br><span class="line">hexo server</span><br></pre></td></tr></table></figure><p>现在打开你的博客，侧边栏的头像应该已经动起来了！</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|配置&amp;优化倒计时挂件</title>
      <link href="/2026/02/09/%E9%85%8D%E7%BD%AE-%E4%BC%98%E5%8C%96%E5%80%92%E8%AE%A1%E6%97%B6%E6%8C%82%E4%BB%B6/"/>
      <url>/2026/02/09/%E9%85%8D%E7%BD%AE-%E4%BC%98%E5%8C%96%E5%80%92%E8%AE%A1%E6%97%B6%E6%8C%82%E4%BB%B6/</url>
      
        <content type="html"><![CDATA[<h1 id="1-配置"><a href="#1-配置" class="headerlink" title="1.配置"></a>1.配置</h1><hr><p>配置请参考<a href="https://blog.bsgun.cn/posts/68c5cdb9">梦爱吃鱼</a>和<a href="https://penry.asia/article/Hexo-7/">Penry</a>的教程。这里不再赘述。</p><h1 id="2-问题"><a href="#2-问题" class="headerlink" title="2.问题"></a>2.问题</h1><hr><p>countdown.js 是博客侧边栏的倒计时组件，用于展示距离下一个节日的剩余天数，以及今日&#x2F;本周&#x2F;本月&#x2F;本年的时间进度条。</p><p><strong>原始实现的问题：</strong> 节日目标是硬编码的：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> config = &#123;</span><br><span class="line">    <span class="attr">targetDate</span>: <span class="string">&quot;2026-01-01&quot;</span>,</span><br><span class="line">    <span class="attr">targetName</span>: <span class="string">&quot;元旦&quot;</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>每当一个节日过去后，必须手动修改 <code>targetDate</code> 和 <code>targetName</code> 才能切换到下一个节日。如果忘记修改，页面会显示负数天数。</p><h1 id="3-优化内容"><a href="#3-优化内容" class="headerlink" title="3.优化内容"></a>3.优化内容</h1><hr><p>将静态的单一节日配置改为<strong>自动轮转的节日列表机制</strong>，具体改动：</p><ol><li><p><strong>新增 <code>holidays</code> 数组</strong> — 定义了一整年的节日列表，按月日升序排列，每个条目包含 <code>month</code>、<code>day</code>、<code>name</code> 三个字段。</p></li><li><p><strong>新增 <code>getNextHoliday()</code> 函数</strong> — 核心自动化逻辑：</p><ul><li>获取当天日期（时间归零到 00:00:00，避免时分秒干扰比较）</li><li>遍历 <code>holidays</code> 列表，将每个节日映射为当年的完整日期</li><li>找到第一个 <strong>大于等于今天</strong> 的节日作为目标（即：节日当天仍会显示该节日，剩余 0 天）</li><li>如果今年所有节日都已过去，自动取明年第一个节日（元旦）</li></ul></li><li><p><strong><code>updateCountdown()</code> 改为动态获取目标</strong> — 不再读取 <code>config.targetDate</code>，而是每次调用 <code>getNextHoliday()</code> 获取当前应该倒计时的节日名称和日期，动态生成日期字符串（<code>YYYY-MM-DD</code> 格式）和剩余天数。</p></li><li><p><strong><code>config</code> 对象精简</strong> — 移除了 <code>targetDate</code> 和 <code>targetName</code>，仅保留 <code>units</code>（进度条配置），节日数据完全由 <code>holidays</code> 数组管理。</p></li></ol><p>代码如下，直接替换你的countdown.js</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br><span class="line">248</span><br><span class="line">249</span><br><span class="line">250</span><br><span class="line">251</span><br><span class="line">252</span><br><span class="line">253</span><br><span class="line">254</span><br><span class="line">255</span><br><span class="line">256</span><br><span class="line">257</span><br><span class="line">258</span><br><span class="line">259</span><br><span class="line">260</span><br><span class="line">261</span><br><span class="line">262</span><br><span class="line">263</span><br><span class="line">264</span><br><span class="line">265</span><br><span class="line">266</span><br><span class="line">267</span><br><span class="line">268</span><br><span class="line">269</span><br><span class="line">270</span><br><span class="line">271</span><br><span class="line">272</span><br><span class="line">273</span><br><span class="line">274</span><br><span class="line">275</span><br><span class="line">276</span><br><span class="line">277</span><br><span class="line">278</span><br><span class="line">279</span><br><span class="line">280</span><br><span class="line">281</span><br><span class="line">282</span><br><span class="line">283</span><br><span class="line">284</span><br><span class="line">285</span><br><span class="line">286</span><br><span class="line">287</span><br><span class="line">288</span><br><span class="line">289</span><br><span class="line">290</span><br><span class="line">291</span><br><span class="line">292</span><br><span class="line">293</span><br><span class="line">294</span><br><span class="line">295</span><br><span class="line">296</span><br><span class="line">297</span><br><span class="line">298</span><br><span class="line">299</span><br><span class="line">300</span><br><span class="line">301</span><br><span class="line">302</span><br><span class="line">303</span><br><span class="line">304</span><br><span class="line">305</span><br><span class="line">306</span><br><span class="line">307</span><br><span class="line">308</span><br><span class="line">309</span><br><span class="line">310</span><br><span class="line">311</span><br><span class="line">312</span><br><span class="line">313</span><br><span class="line">314</span><br><span class="line">315</span><br><span class="line">316</span><br><span class="line">317</span><br><span class="line">318</span><br><span class="line">319</span><br><span class="line">320</span><br><span class="line">321</span><br><span class="line">322</span><br><span class="line">323</span><br><span class="line">324</span><br><span class="line">325</span><br><span class="line">326</span><br><span class="line">327</span><br><span class="line">328</span><br><span class="line">329</span><br><span class="line">330</span><br><span class="line">331</span><br><span class="line">332</span><br><span class="line">333</span><br><span class="line">334</span><br><span class="line">335</span><br><span class="line">336</span><br><span class="line">337</span><br><span class="line">338</span><br><span class="line">339</span><br><span class="line">340</span><br><span class="line">341</span><br><span class="line">342</span><br><span class="line">343</span><br><span class="line">344</span><br><span class="line">345</span><br><span class="line">346</span><br><span class="line">347</span><br><span class="line">348</span><br><span class="line">349</span><br><span class="line">350</span><br><span class="line">351</span><br><span class="line">352</span><br><span class="line">353</span><br><span class="line">354</span><br><span class="line">355</span><br><span class="line">356</span><br><span class="line">357</span><br><span class="line">358</span><br><span class="line">359</span><br><span class="line">360</span><br><span class="line">361</span><br><span class="line">362</span><br><span class="line">363</span><br><span class="line">364</span><br><span class="line">365</span><br><span class="line">366</span><br><span class="line">367</span><br><span class="line">368</span><br><span class="line">369</span><br><span class="line">370</span><br><span class="line">371</span><br><span class="line">372</span><br><span class="line">373</span><br><span class="line">374</span><br><span class="line">375</span><br><span class="line">376</span><br><span class="line">377</span><br><span class="line">378</span><br><span class="line">379</span><br><span class="line">380</span><br><span class="line">381</span><br><span class="line">382</span><br><span class="line">383</span><br><span class="line">384</span><br><span class="line">385</span><br><span class="line">386</span><br><span class="line">387</span><br><span class="line">388</span><br><span class="line">389</span><br><span class="line">390</span><br><span class="line">391</span><br><span class="line">392</span><br><span class="line">393</span><br><span class="line">394</span><br><span class="line">395</span><br><span class="line">396</span><br><span class="line">397</span><br><span class="line">398</span><br><span class="line">399</span><br><span class="line">400</span><br><span class="line">401</span><br><span class="line">402</span><br><span class="line">403</span><br><span class="line">404</span><br><span class="line">405</span><br><span class="line">406</span><br><span class="line">407</span><br><span class="line">408</span><br><span class="line">409</span><br><span class="line">410</span><br><span class="line">411</span><br><span class="line">412</span><br><span class="line">413</span><br><span class="line">414</span><br><span class="line">415</span><br><span class="line">416</span><br><span class="line">417</span><br><span class="line">418</span><br><span class="line">419</span><br><span class="line">420</span><br><span class="line">421</span><br><span class="line">422</span><br><span class="line">423</span><br><span class="line">424</span><br><span class="line">425</span><br><span class="line">426</span><br><span class="line">427</span><br><span class="line">428</span><br><span class="line">429</span><br><span class="line">430</span><br><span class="line">431</span><br><span class="line">432</span><br><span class="line">433</span><br><span class="line">434</span><br><span class="line">435</span><br><span class="line">436</span><br><span class="line">437</span><br><span class="line">438</span><br><span class="line">439</span><br><span class="line">440</span><br><span class="line">441</span><br><span class="line">442</span><br><span class="line">443</span><br><span class="line">444</span><br><span class="line">445</span><br><span class="line">446</span><br><span class="line">447</span><br><span class="line">448</span><br><span class="line">449</span><br><span class="line">450</span><br><span class="line">451</span><br><span class="line">452</span><br><span class="line">453</span><br><span class="line">454</span><br><span class="line">455</span><br><span class="line">456</span><br><span class="line">457</span><br><span class="line">458</span><br><span class="line">459</span><br><span class="line">460</span><br><span class="line">461</span><br><span class="line">462</span><br><span class="line">463</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">CountdownTimer</span> = (<span class="function">() =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 节日列表（月-日），按日期排序，每年自动循环</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> holidays = [</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">1</span>,  <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;元旦&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">2</span>,  <span class="attr">day</span>: <span class="number">14</span>, <span class="attr">name</span>: <span class="string">&quot;情人节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">3</span>,  <span class="attr">day</span>: <span class="number">8</span>,  <span class="attr">name</span>: <span class="string">&quot;妇女节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">4</span>,  <span class="attr">day</span>: <span class="number">5</span>,  <span class="attr">name</span>: <span class="string">&quot;清明节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">5</span>,  <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;劳动节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">5</span>,  <span class="attr">day</span>: <span class="number">4</span>,  <span class="attr">name</span>: <span class="string">&quot;青年节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">6</span>,  <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;儿童节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">7</span>,  <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;建党节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">8</span>,  <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;建军节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">9</span>,  <span class="attr">day</span>: <span class="number">10</span>, <span class="attr">name</span>: <span class="string">&quot;教师节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">10</span>, <span class="attr">day</span>: <span class="number">1</span>,  <span class="attr">name</span>: <span class="string">&quot;国庆节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">        &#123; <span class="attr">month</span>: <span class="number">12</span>, <span class="attr">day</span>: <span class="number">25</span>, <span class="attr">name</span>: <span class="string">&quot;圣诞节&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">    ];</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> config = &#123;</span><br><span class="line"></span><br><span class="line">        <span class="attr">units</span>: &#123;</span><br><span class="line"></span><br><span class="line">            <span class="attr">day</span>: &#123; <span class="attr">text</span>: <span class="string">&quot;今日&quot;</span>, <span class="attr">unit</span>: <span class="string">&quot;小时&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">            <span class="attr">week</span>: &#123; <span class="attr">text</span>: <span class="string">&quot;本周&quot;</span>, <span class="attr">unit</span>: <span class="string">&quot;天&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">            <span class="attr">month</span>: &#123; <span class="attr">text</span>: <span class="string">&quot;本月&quot;</span>, <span class="attr">unit</span>: <span class="string">&quot;天&quot;</span> &#125;,</span><br><span class="line"></span><br><span class="line">            <span class="attr">year</span>: &#123; <span class="attr">text</span>: <span class="string">&quot;本年&quot;</span>, <span class="attr">unit</span>: <span class="string">&quot;天&quot;</span> &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     * 根据当前日期，找到下一个即将到来的节日</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     * 如果今天正好是某个节日，则显示该节日（剩余0天）</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     * 如果今年所有节日都已过，则跳到明年第一个节日</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">getNextHoliday</span>(<span class="params"></span>) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> now = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> today = <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>(), now.<span class="title function_">getMonth</span>(), now.<span class="title function_">getDate</span>());</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        <span class="comment">// 先在今年找</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> h <span class="keyword">of</span> holidays) &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> date = <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>(), h.<span class="property">month</span> - <span class="number">1</span>, h.<span class="property">day</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (date &gt;= today) &#123;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">return</span> &#123; <span class="attr">name</span>: h.<span class="property">name</span>, date &#125;;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 今年的节日都过了，取明年第一个</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> first = holidays[<span class="number">0</span>];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> &#123; <span class="attr">name</span>: first.<span class="property">name</span>, <span class="attr">date</span>: <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>() + <span class="number">1</span>, first.<span class="property">month</span> - <span class="number">1</span>, first.<span class="property">day</span>) &#125;;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> calculators = &#123;</span><br><span class="line"></span><br><span class="line">        <span class="attr">day</span>: <span class="function">() =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> hours = <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getHours</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="attr">remaining</span>: <span class="number">24</span> - hours,</span><br><span class="line"></span><br><span class="line">                <span class="attr">percentage</span>: (hours / <span class="number">24</span>) * <span class="number">100</span></span><br><span class="line"></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="attr">week</span>: <span class="function">() =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> day = <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">getDay</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> passed = day === <span class="number">0</span> ? <span class="number">6</span> : day - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="attr">remaining</span>: <span class="number">6</span> - passed,</span><br><span class="line"></span><br><span class="line">                <span class="attr">percentage</span>: ((passed + <span class="number">1</span>) / <span class="number">7</span>) * <span class="number">100</span></span><br><span class="line"></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="attr">month</span>: <span class="function">() =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> now = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> total = <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>(), now.<span class="title function_">getMonth</span>() + <span class="number">1</span>, <span class="number">0</span>).<span class="title function_">getDate</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> passed = now.<span class="title function_">getDate</span>() - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="attr">remaining</span>: total - passed,</span><br><span class="line"></span><br><span class="line">                <span class="attr">percentage</span>: (passed / total) * <span class="number">100</span></span><br><span class="line"></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="attr">year</span>: <span class="function">() =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> now = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> start = <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>(), <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> total = <span class="number">365</span> + (now.<span class="title function_">getFullYear</span>() % <span class="number">4</span> === <span class="number">0</span> ? <span class="number">1</span> : <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">const</span> passed = <span class="title class_">Math</span>.<span class="title function_">floor</span>((now - start) / <span class="number">86400000</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="attr">remaining</span>: total - passed,</span><br><span class="line"></span><br><span class="line">                <span class="attr">percentage</span>: (passed / total) * <span class="number">100</span></span><br><span class="line"></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">updateCountdown</span>(<span class="params"></span>) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> elements = [<span class="string">&#x27;eventName&#x27;</span>, <span class="string">&#x27;eventDate&#x27;</span>, <span class="string">&#x27;daysUntil&#x27;</span>, <span class="string">&#x27;countRight&#x27;</span>]</span><br><span class="line"></span><br><span class="line">            .<span class="title function_">map</span>(<span class="function"><span class="params">id</span> =&gt;</span> <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(id));</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (elements.<span class="title function_">some</span>(<span class="function"><span class="params">el</span> =&gt;</span> !el)) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> [eventName, eventDate, daysUntil, countRight] = elements;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> now = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> &#123; <span class="attr">name</span>: targetName, <span class="attr">date</span>: target &#125; = <span class="title function_">getNextHoliday</span>();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> targetDateStr = <span class="string">`<span class="subst">$&#123;target.getFullYear()&#125;</span>-<span class="subst">$&#123;<span class="built_in">String</span>(target.getMonth()+<span class="number">1</span>).padStart(<span class="number">2</span>,<span class="string">&#x27;0&#x27;</span>)&#125;</span>-<span class="subst">$&#123;<span class="built_in">String</span>(target.getDate()).padStart(<span class="number">2</span>,<span class="string">&#x27;0&#x27;</span>)&#125;</span>`</span>;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        eventName.<span class="property">textContent</span> = targetName;</span><br><span class="line"></span><br><span class="line">        eventDate.<span class="property">textContent</span> = targetDateStr;</span><br><span class="line"></span><br><span class="line">        daysUntil.<span class="property">textContent</span> = <span class="title class_">Math</span>.<span class="title function_">round</span>((target - <span class="keyword">new</span> <span class="title class_">Date</span>(now.<span class="title function_">getFullYear</span>(), now.<span class="title function_">getMonth</span>(), now.<span class="title function_">getDate</span>())) / <span class="number">86400000</span>);</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        countRight.<span class="property">innerHTML</span> = <span class="title class_">Object</span>.<span class="title function_">entries</span>(config.<span class="property">units</span>)</span><br><span class="line"></span><br><span class="line">            .<span class="title function_">map</span>(<span class="function">(<span class="params">[key, &#123;text, unit&#125;]</span>) =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">const</span> &#123;remaining, percentage&#125; = calculators[key]();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                    &lt;div class=&quot;cd-count-item&quot;&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                        &lt;div class=&quot;cd-item-name&quot;&gt;<span class="subst">$&#123;text&#125;</span>&lt;/div&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                        &lt;div class=&quot;cd-item-progress&quot;&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                            &lt;div class=&quot;cd-progress-bar&quot; style=&quot;width: <span class="subst">$&#123;percentage&#125;</span>%; opacity: <span class="subst">$&#123;percentage/<span class="number">100</span>&#125;</span>&quot;&gt;&lt;/div&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                            &lt;span class=&quot;cd-percentage <span class="subst">$&#123;percentage &gt;= <span class="number">46</span> ? <span class="string">&#x27;cd-many&#x27;</span> : <span class="string">&#x27;&#x27;</span>&#125;</span>&quot;&gt;<span class="subst">$&#123;percentage.toFixed(<span class="number">2</span>)&#125;</span>%&lt;/span&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                            &lt;span class=&quot;cd-remaining <span class="subst">$&#123;percentage &gt;= <span class="number">60</span> ? <span class="string">&#x27;cd-many&#x27;</span> : <span class="string">&#x27;&#x27;</span>&#125;</span>&quot;&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                                &lt;span class=&quot;cd-tip&quot;&gt;还剩&lt;/span&gt;<span class="subst">$&#123;remaining&#125;</span>&lt;span class=&quot;cd-tip&quot;&gt;<span class="subst">$&#123;unit&#125;</span>&lt;/span&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                            &lt;/span&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                        &lt;/div&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                    &lt;/div&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                `</span>;</span><br><span class="line"></span><br><span class="line">            &#125;).<span class="title function_">join</span>(<span class="string">&#x27;&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">injectStyles</span>(<span class="params"></span>) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> styles = <span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .card-countdown .item-content &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                display: flex;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                position: relative;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                display: flex;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                flex-direction: column;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                margin-right: 0.8rem;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                line-height: 1.5;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                align-items: center;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                justify-content: center;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left .cd-text &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 14px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left .cd-name &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-weight: bold;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 18px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left .cd-time &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 30px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-weight: bold;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                color: var(--anzhiyu-main);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left .cd-date &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 12px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                opacity: 0.6;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-left::after &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                content: &quot;&quot;;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                position: absolute;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                right: -0.8rem;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                width: 2px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                height: 80%;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                background-color: var(--anzhiyu-main);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                opacity: 0.5;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-right &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                flex: 1;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                margin-left: .8rem;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                display: flex;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                flex-direction: column;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                justify-content: space-between;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-count-item &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                display: flex;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                flex-direction: row;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                align-items: center;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                height: 24px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-item-name &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 14px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                margin-right: 0.8rem;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                white-space: nowrap;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-item-progress &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                position: relative;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                display: flex;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                flex-direction: row;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                align-items: center;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                justify-content: space-between;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                height: 100%;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                width: 100%;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                border-radius: 8px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                background-color: var(--anzhiyu-background);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                overflow: hidden;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-progress-bar &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                height: 100%;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                border-radius: 8px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                background-color: var(--anzhiyu-main);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-percentage,</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-remaining &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                position: absolute;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                font-size: 12px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                margin: 0 6px;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                transition: opacity 0.3s ease-in-out, transform 0.3s ease-in-out;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-many &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                color: #fff;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .cd-remaining &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                opacity: 0;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                transform: translateX(10px);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .card-countdown .item-content:hover .cd-remaining &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                transform: translateX(0);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                opacity: 1;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            .card-countdown .item-content:hover .cd-percentage &#123;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                transform: translateX(-10px);</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">                opacity: 0;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">        `</span>;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">        <span class="keyword">const</span> styleSheet = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&quot;style&quot;</span>);</span><br><span class="line"></span><br><span class="line">        styleSheet.<span class="property">textContent</span> = styles;</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">document</span>.<span class="property">head</span>.<span class="title function_">appendChild</span>(styleSheet);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> timer;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="title function_">start</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line"></span><br><span class="line">        <span class="title function_">injectStyles</span>();</span><br><span class="line"></span><br><span class="line">        <span class="title function_">updateCountdown</span>();</span><br><span class="line"></span><br><span class="line">        timer = <span class="built_in">setInterval</span>(updateCountdown, <span class="number">600000</span>);</span><br><span class="line"></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    [<span class="string">&#x27;pjax:complete&#x27;</span>, <span class="string">&#x27;DOMContentLoaded&#x27;</span>].<span class="title function_">forEach</span>(<span class="function"><span class="params">event</span> =&gt;</span> <span class="variable language_">document</span>.<span class="title function_">addEventListener</span>(event, start));</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;pjax:send&#x27;</span>, <span class="function">() =&gt;</span> timer &amp;&amp; <span class="built_in">clearInterval</span>(timer));</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123; start, <span class="attr">stop</span>: <span class="function">() =&gt;</span> timer &amp;&amp; <span class="built_in">clearInterval</span>(timer) &#125;;</span><br><span class="line"></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure><h3 id="效果"><a href="#效果" class="headerlink" title="效果"></a>效果</h3><ul><li>部署后<strong>无需再手动维护</strong>，节日会自动按时间顺序轮转</li><li>如需增减节日，只需修改 <code>holidays</code> 数组即可，格式为 <code>{ month: 月, day: 日, name: &quot;名称&quot; }</code>进行</li></ul>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|为twikoo评论系统设置二级域名</title>
      <link href="/2026/02/08/%E4%B8%BAtwikoo%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F%E8%AE%BE%E7%BD%AE%E4%BA%8C%E7%BA%A7%E5%9F%9F%E5%90%8D/"/>
      <url>/2026/02/08/%E4%B8%BAtwikoo%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F%E8%AE%BE%E7%BD%AE%E4%BA%8C%E7%BA%A7%E5%9F%9F%E5%90%8D/</url>
      
        <content type="html"><![CDATA[<h1 id="0-背景信息"><a href="#0-背景信息" class="headerlink" title="0.背景信息"></a>0.背景信息</h1><hr><p>Twikoo 是一个轻量级的静态网站评论系统。部署到 Vercel 后，默认域名 <code>*.vercel.app</code> 在中国大陆访问经常受限（需科学上网）。为改善访问体验，推荐为 Twikoo 绑定自己域名的二级子域。</p><h2 id="1-在-Vercel-为-Twikoo-添加二级域名"><a href="#1-在-Vercel-为-Twikoo-添加二级域名" class="headerlink" title="1. 在 Vercel 为 Twikoo 添加二级域名"></a>1. 在 Vercel 为 Twikoo 添加二级域名</h2><hr><ol><li>登录 Vercel 控制台，进入你的 Twikoo 项目</li><li>左侧菜单 → <strong>Settings</strong> → <strong>Domains</strong></li><li>在「Add Domain」处输入你想使用的二级域名，例如：<code>twikoo.example.com</code></li><li>点击 <strong>Add</strong>，此时状态会显示 <strong>Invalid Configuration</strong>（因为 DNS 尚未解析，属于正常现象）</li></ol><p><img src="/pics/2026-2/vercelTwikoo.png"></p><h2 id="2-在域名服务商处添加-DNS-解析记录"><a href="#2-在域名服务商处添加-DNS-解析记录" class="headerlink" title="2. 在域名服务商处添加 DNS 解析记录"></a>2. 在域名服务商处添加 DNS 解析记录</h2><hr><p>以阿里云为例：</p><ol><li><p>登录阿里云控制台 → <a href="https://dnsnext.console.aliyun.com/authoritative/domains/">域名控制台 - 公网权威解析</a></p></li><li><p>找到你的主域名，点击「解析设置」或「添加记录」</p></li><li><p>添加一条记录，参数如下（根据 Vercel 页面提示填写）：</p><ul><li><strong>记录类型</strong>：CNAME（推荐）  </li><li><strong>主机记录</strong>：twikoo（即你刚才填的子域名前缀）  </li><li><strong>记录值</strong>：Vercel 提供的目标地址（通常为 <code>cname.vercel-dns.com</code> 或类似格式）  </li><li><strong>TTL</strong>：默认（600 或 3600 均可）</li></ul></li><li><p>保存记录，等待 DNS 生效（通常 5–30 分钟，视运营商而定）</p></li></ol><h2 id="3-返回-Vercel-刷新并等待证书生成"><a href="#3-返回-Vercel-刷新并等待证书生成" class="headerlink" title="3. 返回 Vercel 刷新并等待证书生成"></a>3. 返回 Vercel 刷新并等待证书生成</h2><hr><ol><li>返回 Vercel 项目 → Domains 页面</li><li>点击刚才添加的域名旁边的 <strong>Refresh</strong> 按钮</li><li>等待系统自动申请并生成 SSL 证书（通常需要 2–15 分钟）</li></ol><p><img src="/pics/2026-2/GeneratingSSL.png"></p><p>证书生成完成后，域名状态变为有效，点击域名即可看到 Twikoo 的默认页面，表示绑定成功。</p><p><img src="/pics/2026-2/TwikooWebsite.png"></p><h2 id="4-修改主题配置文件"><a href="#4-修改主题配置文件" class="headerlink" title="4. 修改主题配置文件"></a>4. 修改主题配置文件</h2><hr><p>在 <code>_config.yourTheme.yml</code> 中修改以下两部分：</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">comments:</span></span><br><span class="line">  <span class="comment"># 最多支持两个评论系统，第一个作为默认显示</span></span><br><span class="line">  <span class="comment"># 可选值：Valine / Waline / Twikoo / ...</span></span><br><span class="line">  <span class="attr">use:</span> <span class="string">Twikoo</span>          <span class="comment"># 如需同时保留 Waline，可写：Twikoo,Waline</span></span><br><span class="line">  <span class="attr">text:</span> <span class="literal">true</span>           <span class="comment"># 按钮旁显示“评论”文字</span></span><br><span class="line">  <span class="attr">lazyload:</span> <span class="literal">true</span>       <span class="comment"># 推荐开启：评论框进入视口后再加载，加快首屏速度</span></span><br><span class="line">  <span class="attr">count:</span> <span class="literal">true</span>          <span class="comment"># 文章页顶部显示评论数量</span></span><br><span class="line">  <span class="attr">card_post_count:</span> <span class="literal">false</span>  <span class="comment"># 首页文章卡片不显示评论数（可选）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Twikoo 配置</span></span><br><span class="line"><span class="attr">twikoo:</span></span><br><span class="line">  <span class="attr">envId:</span> <span class="string">https://twikoo.example.com/</span>    <span class="comment"># 必须改为你绑定的完整二级域名，注意带 https:// 和结尾斜杠 /</span></span><br><span class="line">  <span class="attr">region:</span>               <span class="comment"># 一般留空，除非使用了腾讯云/阿里云函数</span></span><br><span class="line">  <span class="attr">visitor:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">option:</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：  </p><ul><li><code>envId</code> 必须是 <code>https://</code> 开头，且以 <code>/</code> 结尾，否则 Twikoo 会加载失败。  </li><li>修改配置文件后需重新生成静态文件。</li></ul><h2 id="5-本地测试"><a href="#5-本地测试" class="headerlink" title="5. 本地测试"></a>5. 本地测试</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo server</span><br></pre></td></tr></table></figure><p>启动本地服务后，访问任意文章页面，滚动到评论区，确认 Twikoo 评论框正常加载且可以提交评论。</p><p><img src="/pics/2026-2/TwikooTest.png"></p><p>完成以上步骤后，Twikoo 即可通过自定义二级域名正常工作。</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> website </tag>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|用Google Fonts为Hexo博客替换字体</title>
      <link href="/2026/02/07/%E7%94%A8Google-Fonts%E4%B8%BAHexo%E5%8D%9A%E5%AE%A2%E6%9B%BF%E6%8D%A2%E5%AD%97%E4%BD%93/"/>
      <url>/2026/02/07/%E7%94%A8Google-Fonts%E4%B8%BAHexo%E5%8D%9A%E5%AE%A2%E6%9B%BF%E6%8D%A2%E5%AD%97%E4%BD%93/</url>
      
        <content type="html"><![CDATA[<h2 id="1-在-Google-Fonts-挑选字体"><a href="#1-在-Google-Fonts-挑选字体" class="headerlink" title="1. 在 Google Fonts 挑选字体"></a>1. 在 Google Fonts 挑选字体</h2><p>首先访问 <a href="https://fonts.google.com/">Google Fonts</a>，这里提供了数百种免费字体供您选择。</p><h3 id="筛选字体"><a href="#筛选字体" class="headerlink" title="筛选字体"></a>筛选字体</h3><ul><li>点击左侧的 <strong>Filters</strong> 按钮，您可以根据不同条件筛选字体</li><li>如果需要支持日文字符的字体，可以在语言筛选中选择 <strong>Japanese</strong></li></ul><p><img src="/pics/2026-2/FontsFilter.png" alt="字体筛选界面"></p><h3 id="获取字体代码"><a href="#获取字体代码" class="headerlink" title="获取字体代码"></a>获取字体代码</h3><ol><li>找到喜欢的字体后，点击字体卡片进入详情页</li><li>点击右上角的 <strong>“Get Font”</strong> 按钮将其添加到”购物车”</li></ol><p><img src="/pics/2026-2/GetFont.png" alt="获取字体"></p><ol start="3"><li>在右侧弹出的面板中，切换到 <strong>Embed</strong> 选项卡</li><li>复制提供的嵌入代码</li></ol><p><img src="/pics/2026-2/EmbedCode.png" alt="嵌入代码"></p><h2 id="2-修改主题配置文件"><a href="#2-修改主题配置文件" class="headerlink" title="2. 修改主题配置文件"></a>2. 修改主题配置文件</h2><h3 id="添加字体链接"><a href="#添加字体链接" class="headerlink" title="添加字体链接"></a>添加字体链接</h3><p>在 Hexo 主题的配置文件（通常是 <code>_config.yml</code> 或主题的配置文件）中，找到 <code>inject</code> 部分，将复制的代码粘贴到 <code>head</code> 项下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">inject:</span></span><br><span class="line">  <span class="attr">head:</span></span><br><span class="line">    <span class="comment"># 自定义 CSS 和字体链接</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&lt;link</span> <span class="string">rel=&quot;preconnect&quot;</span> <span class="string">href=&quot;https://fonts.googleapis.com&quot;&gt;</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&lt;link</span> <span class="string">rel=&quot;preconnect&quot;</span> <span class="string">href=&quot;https://fonts.gstatic.com&quot;</span> <span class="string">crossorigin&gt;</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&lt;link</span> <span class="string">href=&quot;https://fonts.googleapis.com/css2?family=Yuji+Mai&amp;display=swap&quot;</span> <span class="string">rel=&quot;stylesheet&quot;&gt;</span></span><br></pre></td></tr></table></figure><p><strong>重要提示</strong>：YAML 对缩进要求严格，请确保每行开头的空格数一致。</p><h3 id="配置博客标题字体"><a href="#配置博客标题字体" class="headerlink" title="配置博客标题字体"></a>配置博客标题字体</h3><p>找到博客标题字体设置部分，将 Google Fonts 的字体名称添加到字体列表的最前面：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 博客标题字体设置</span></span><br><span class="line"><span class="comment"># 影响左上角网站名称和主页居中显示的网站名称</span></span><br><span class="line"><span class="attr">blog_title_font:</span></span><br><span class="line">  <span class="attr">font_link:</span></span><br><span class="line">  <span class="attr">font-family:</span> <span class="string">Yuji</span> <span class="string">Mai,</span> <span class="string">PingFang</span> <span class="string">SC,</span> <span class="string">Hiragino</span> <span class="string">Sans</span> <span class="string">GB,</span> <span class="string">Microsoft</span> <span class="string">JhengHei,</span> <span class="string">Microsoft</span> <span class="string">YaHei,</span> <span class="string">sans-serif</span></span><br></pre></td></tr></table></figure><p><strong>注意事项</strong>：</p><ul><li>我使用的是 “Yuji Mai” 字体，您可以选择自己喜欢的任何字体</li><li>字体栈的顺序很重要：浏览器会优先使用列表中的第一个可用字体</li><li>建议保留系统字体作为备选，确保在不同设备上都能正常显示</li></ul><h2 id="3-本地预览效果"><a href="#3-本地预览效果" class="headerlink" title="3. 本地预览效果"></a>3. 本地预览效果</h2><p>保存配置文件后，在终端执行以下命令清理缓存并启动本地服务器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo cl; hexo s;</span><br></pre></td></tr></table></figure><p>访问 <code>http://localhost:4000</code> 查看字体更改效果：</p><p><img src="/pics/2026-2/WebsiteTitle.png" alt="网站标题效果"></p><p>如果标题字体已更新为新选择的字体，说明配置成功。</p><h2 id="4-部署到线上"><a href="#4-部署到线上" class="headerlink" title="4. 部署到线上"></a>4. 部署到线上</h2><p>确认本地效果满意后，执行以下命令生成静态文件并部署到线上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo cl; hexo g; hexo d</span><br></pre></td></tr></table></figure><h2 id="扩展应用与建议"><a href="#扩展应用与建议" class="headerlink" title="扩展应用与建议"></a>扩展应用与建议</h2><h3 id="应用到其他元素"><a href="#应用到其他元素" class="headerlink" title="应用到其他元素"></a>应用到其他元素</h3><p>同样的方法也可以用于修改其他文本元素的字体：</p><ul><li>正文字体 (<code>global_font</code>)</li><li>代码块字体 (<code>code_font</code>)</li><li>侧边栏字体等</li></ul><h3 id="性能优化建议"><a href="#性能优化建议" class="headerlink" title="性能优化建议"></a>性能优化建议</h3><ol><li><strong>仅加载需要的字重</strong>：在 Google Fonts 中只选择实际使用的字重（如 Regular 400、Bold 700）</li><li><strong>使用 font-display: swap</strong>：确保文本在字体加载期间仍然可见</li><li><strong>考虑中文字体体积</strong>：中文字体文件较大，建议仅用于标题或少量文字</li></ol><h3 id="故障排除"><a href="#故障排除" class="headerlink" title="故障排除"></a>故障排除</h3><ul><li>如果字体未生效，检查浏览器开发者工具的 Network 选项卡，确认字体文件是否成功加载</li><li>确认字体名称拼写完全正确</li><li>清除浏览器缓存后重新加载页面</li></ul><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>通过简单的四步操作，您就能为博客换上独具特色的字体。合理运用字体不仅能提升博客的视觉吸引力，还能增强阅读体验和品牌识别度。</p><p><strong>提示</strong>：您可以在 Google Fonts 同时添加多个字体，分别应用于标题、正文等不同元素，创造更丰富的版面层次感。</p><hr><p><em>示例中使用的是 <a href="https://fonts.google.com/specimen/Yuji+Mai">Yuji Mai</a> 字体，这是一款优雅的日文毛笔字体，适合用于标题展示。您可以根据自己的喜好选择其他字体。</em></p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|在Flaticon帮你的网站找到一个好看的矢量图标</title>
      <link href="/2026/02/02/%E5%9C%A8flaticon%E5%B8%AE%E4%BD%A0%E7%9A%84%E7%BD%91%E7%AB%99%E6%89%BE%E5%88%B0%E4%B8%80%E4%B8%AA%E5%A5%BD%E7%9C%8B%E7%9A%84%E7%9F%A2%E9%87%8F%E5%9B%BE%E6%A0%87/"/>
      <url>/2026/02/02/%E5%9C%A8flaticon%E5%B8%AE%E4%BD%A0%E7%9A%84%E7%BD%91%E7%AB%99%E6%89%BE%E5%88%B0%E4%B8%80%E4%B8%AA%E5%A5%BD%E7%9C%8B%E7%9A%84%E7%9F%A2%E9%87%8F%E5%9B%BE%E6%A0%87/</url>
      
        <content type="html"><![CDATA[<p><strong>操作流程：更换 Hexo 博客的 Favicon 图标</strong></p><p>本文介绍如何通过 Flaticon 获取图标并更换 Hexo 博客的网站图标 (Favicon)。</p><p><strong>1. 获取图标</strong><br>访问 <a href="https://www.flaticon.com/">Flaticon</a>，搜索并选择适合的图标。通常 PNG 格式即可满足网站使用需求。<br><img src="/pics/2026-2/chooseicon.png"></p><p>在下载页面选择 <strong>Free download</strong> 将其保存至本地。<br><img src="/pics/2026-2/freedownload.png"></p><p><strong>2. 更换配置</strong><br>将下载的图标文件置于博客的静态资源目录（例如 <code>source/icons/</code>）。随后在主题配置文件（如 <code>_config.anzhiyu.yml</code>）中更新 <code>favicon</code> 字段，指向该图标路径，并建议附上作者链接以便标注来源。</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Favicon（网站图标）</span></span><br><span class="line"><span class="attr">favicon:</span> <span class="string">/icons/bank.png</span> <span class="comment"># 图标作者链接：https://www.flaticon.com/authors/icongeek26</span></span><br></pre></td></tr></table></figure><p><strong>3. 生效确认</strong><br>清除缓存并重新生成博客文件后，访问网站即可看到图标已更新。<br><img src="/pics/2026-2/favicon.png"></p><p><strong>版权声明</strong><br>本教程所示图标来源于 Flaticon 作者 <a href="https://www.flaticon.com/authors/icongeek26">Icongeek26</a>，仅作个人学习与演示之用，非商业用途。</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> website </tag>
            
            <tag> Tools </tag>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo|Adobe Express帮助你创建背景透明图片</title>
      <link href="/2026/02/02/Adobe-Express%E5%B8%AE%E5%8A%A9%E4%BD%A0%E5%88%9B%E5%BB%BA%E8%83%8C%E6%99%AF%E9%80%8F%E6%98%8E%E5%9B%BE%E7%89%87/"/>
      <url>/2026/02/02/Adobe-Express%E5%B8%AE%E5%8A%A9%E4%BD%A0%E5%88%9B%E5%BB%BA%E8%83%8C%E6%99%AF%E9%80%8F%E6%98%8E%E5%9B%BE%E7%89%87/</url>
      
        <content type="html"><![CDATA[<p><strong>目的与背景</strong><br>在基于 Hexo 框架与 Anzhiyu 主题进行博客定制时，笔者希望保留侧边栏 <code>weixin_card</code> 组件的悬停翻转动效，但将其内容从微信公众号展示替换为自定义图像。本操作选用《Street Fighter》系列中的春丽（Chun-Li）与嘉米（Cammy）角色图片作为正面与背面展示内容。</p><p><strong>问题描述</strong><br>直接替换图像后，发现春丽图像背景未能完全透明，存在明显残留色块（见图1），导致与卡片背景融合不佳，影响视觉效果。</p><p><img src="/pics/2026-2/chunli1.png"></p><p><strong>解决方案</strong><br>使用 Adobe Express 在线工具进行背景移除操作，具体步骤如下：</p><ol><li>访问 <a href="https://new.express.adobe.com/">Adobe Express</a>；</li><li>选择“移除背景”功能；</li><li>上传原图并自动处理；</li><li>下载生成的透明背景 PNG 图像。</li></ol><p>处理界面与结果如下所示：</p><p><img src="/pics/2026-2/AdobeExpress.png"><br><img src="/pics/2026-2/paste.png"><br><img src="/pics/2026-2/result.png"><br><strong>部署与验证</strong><br>将处理后的图像替换至主题对应目录，在本地执行以下命令清除缓存并启动服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hexo cl &amp;&amp; hexo s</span><br></pre></td></tr></table></figure><p>刷新页面后，春丽图像已完整去除背景，正常嵌入卡片正面（见图2）：</p><p><img src="/pics/2026-2/chunli2.png"></p><p>随后对嘉米图像执行相同处理，并配置为卡片的 <code>back_face</code>（背面图像），实现悬停翻转时的图像切换效果（见图3）：</p><p><img src="/pics/2026-2/cammy.png"></p><p><strong>完成状态</strong><br>至此，原 <code>weixin_card</code> 组件的视觉样式与动效得以保留，内容已替换为定制图像，并具备完整的透明背景与双面翻转交互。</p><p><strong>版权声明</strong><br>本文所使用的春丽与嘉米角色图片来源于 Pixiv 画师 <strong>JuicePiggy16</strong>，该改造仅为个人博客美化使用，未涉及任何商业行为。</p>]]></content>
      
      
      <categories>
          
          <category> Hexo </category>
          
      </categories>
      
      
        <tags>
            
            <tag> website </tag>
            
            <tag> Tools </tag>
            
            <tag> AnZhiYu </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>BrokerHub</title>
      <link href="/2026/01/29/BrokerHub/"/>
      <url>/2026/01/29/BrokerHub/</url>
      
        <content type="html"><![CDATA[<h1 id="0-BrokerHub-可以用来干什么？"><a href="#0-BrokerHub-可以用来干什么？" class="headerlink" title="0. BrokerHub 可以用来干什么？"></a>0. BrokerHub 可以用来干什么？</h1><hr><p><a href="https://github.com/math-zhuxy/BrokerHub">BrokerHub</a>一个由<strong>中山大学 HuangLab</strong> 开发的区块链协议验证平台，名为 <strong>BlockEmulator</strong>。</p><p>简单来说，<strong>它的用途是帮助研究人员或学生快速“模拟”和“验证”新的区块链技术</strong>，特别是“分片（Sharding）”和“共识协议”。它不是一个用来挖矿赚钱的工具，而是一个<strong>科研实验工具</strong>。</p><h2 id="0-1它可以帮你做这些事："><a href="#0-1它可以帮你做这些事：" class="headerlink" title="0.1它可以帮你做这些事："></a>0.1它可以帮你做这些事：</h2><ol><li><strong>模拟区块链运行</strong>：<br> 它可以在一台电脑（或多台）上模拟出一整条分片区块链网络，包含多个分片、多个节点。</li><li><strong>验证“跨分片”机制</strong>：<br> 它内置了两种主流的跨分片技术（BrokerChain 的 Broker 机制和 Monoxide 的 Relay 机制），你可以用它来测试不同分片间怎么处理交易。</li><li><strong>验证“共识”算法</strong>：<br> 它支持 PBFT（拜占庭容错）等共识协议，你可以修改代码来测试自己写的新共识算法效果如何。</li><li><strong>跑实验、出数据</strong>：<br> 它会自动记录 TPS（吞吐量）、延迟等核心指标，并输出成 CSV 文件。写论文或做研究时，可以直接用这些数据画图。</li><li><strong>支持智能合约</strong>：<br> 它包含了一个 VM 模块，可以在模拟链上跑 Solidity 智能合约。</li></ol><p><strong>总结</strong>：如果你在做区块链底层技术的研究（比如想改进分片效率、设计新共识），这个项目就是你的“测试沙盒”，让你不必从零写一条区块链就能验证你的想法。</p><h1 id="1-核心参数说明"><a href="#1-核心参数说明" class="headerlink" title="1. 核心参数说明"></a>1. 核心参数说明</h1><hr><p>项目的核心参数集中在 <code>BrokerHub/params/global_config.go</code> 文件中。</p><h2 id="1-1关键参数配置"><a href="#1-1关键参数配置" class="headerlink" title="1.1关键参数配置"></a>1.1关键参数配置</h2><table><thead><tr><th align="left">参数名</th><th align="left">默认值</th><th align="left">含义&#x2F;作用</th><th align="left">更新建议</th></tr></thead><tbody><tr><td align="left"><strong>Block_Interval</strong></td><td align="left"><code>1000</code></td><td align="left"><strong>出块间隔 (ms)</strong><br>每隔多少毫秒产生一个新区块</td><td align="left">想让出块更快就改小，模拟高延迟环境可改大</td></tr><tr><td align="left"><strong>MaxBlockSize_global</strong></td><td align="left"><code>500000</code></td><td align="left"><strong>区块最大容量</strong><br>一个区块里最多能装多少笔交易</td><td align="left">限制吞吐量上限的参数之一</td></tr><tr><td align="left"><strong>InjectSpeed</strong></td><td align="left"><code>5000</code></td><td align="left"><strong>交易注入速度 (TPS)</strong><br>模拟器每秒向网络发送多少笔交易</td><td align="left"><strong>最重要的负载参数</strong><br>想测极限性能就把这个调大，看系统能不能撑住</td></tr><tr><td align="left"><strong>TotalDataSize</strong></td><td align="left"><code>500000</code></td><td align="left"><strong>总交易数量</strong><br>整个实验一共跑多少笔交易后停止</td><td align="left">控制实验时长。如果跑太久，可以改小这个值</td></tr><tr><td align="left"><strong>BatchSize</strong></td><td align="left"><code>5000</code></td><td align="left"><strong>批处理大小</strong><br>Supervisor 每次读取并发送多少笔交易</td><td align="left">最好比 <strong>InjectSpeed</strong> 稍微大一点，保证发送顺畅</td></tr><tr><td align="left"><strong>ShardNum</strong></td><td align="left"><code>4</code></td><td align="left"><strong>分片数量</strong><br>整个网络被切分成几个片</td><td align="left">验证分片扩展性的核心参数</td></tr><tr><td align="left"><strong>NodesInShard</strong></td><td align="left"><code>4</code></td><td align="left"><strong>分片内节点数</strong><br>每个分片里有多少个节点参与共识</td><td align="left">节点越多，共识（PBFT）通常越慢</td></tr><tr><td align="left"><strong>BrokerNum</strong></td><td align="left"><code>1</code></td><td align="left"><strong>Broker 数量</strong><br>辅助跨分片的中间人节点数量</td><td align="left">仅在 broker 模式下生效</td></tr><tr><td align="left"><strong>DatWrite_path</strong></td><td align="left"><code>&quot;./result/&quot;</code></td><td align="left"><strong>数据输出路径</strong><br>实验结果 CSV 存哪儿</td><td align="left">一般不用改</td></tr></tbody></table><h2 id="1-2如何修改参数"><a href="#1-2如何修改参数" class="headerlink" title="1.2如何修改参数"></a>1.2如何修改参数</h2><ol><li><strong>打开配置文件</strong><ul><li>编辑 <code>global_config.go</code> 文件</li><li>找到对应的变量值直接修改数字</li></ul></li></ol><p><strong>修改代码后需要重新编译</strong></p><h1 id="2-如何运行BrokerHub"><a href="#2-如何运行BrokerHub" class="headerlink" title="2. 如何运行BrokerHub"></a>2. 如何运行BrokerHub</h1><hr><p>在 <code>Windows</code> 上大致分两步：</p><ol><li>编译</li><li>运行脚本</li></ol><h2 id="2-1编译"><a href="#2-1编译" class="headerlink" title="2.1编译"></a>2.1编译</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">go</span> build -o blockEmulator_Windows_Precompile.exe ./main.<span class="keyword">go</span></span><br></pre></td></tr></table></figure><h2 id="2-2运行"><a href="#2-2运行" class="headerlink" title="2.2运行"></a>2.2运行</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.\bat_shardNum=<span class="number">3</span>_NodeNum=<span class="number">4</span>_mod=Broker_b2e.bat</span><br></pre></td></tr></table></figure><p>直接执行 Windows 启动脚本（会自动启动 3 个分片、每分片 4 个节点并写入配置）。<br>执行过程如下。<br><img src="/pics/2026-2/vscodebrokerhub.png"></p><h1 id="3-BrokerHub-项目实验无法自动终止问题的排查与修复"><a href="#3-BrokerHub-项目实验无法自动终止问题的排查与修复" class="headerlink" title="3. BrokerHub 项目实验无法自动终止问题的排查与修复"></a>3. BrokerHub 项目实验无法自动终止问题的排查与修复</h1><h3 id="3-1问题描述"><a href="#3-1问题描述" class="headerlink" title="3.1问题描述"></a>3.1问题描述</h3><p>在运行 BrokerHub（BVM + Brokerchain 实验平台）时，执行启动脚本 bat_shardNum&#x3D;3_NodeNum&#x3D;4_mod&#x3D;Broker_b2e.bat 后，实验进程无法自动终止，Supervisor 日志中 epoch 计数持续递增（已超过 500），result 目录始终未生成。</p><h3 id="3-2系统停止机制分析"><a href="#3-2系统停止机制分析" class="headerlink" title="3.2系统停止机制分析"></a>3.2系统停止机制分析</h3><p>通过阅读源码，梳理出系统的正常关闭流程如下：</p><p>supervisor.go 中 <code>SupervisorTxHandling()</code> 的执行顺序为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">MsgSendingControl() 返回</span><br><span class="line">    → 轮询 StopSignal.GapEnough()（等待连续收到 2×ShardNum 个空区块上报）</span><br><span class="line">        → 向所有节点发送 CStop 消息</span><br><span class="line">            → 调用 CloseSupervisor()，将测量数据写入 ./result/ 下的 CSV 文件</span><br></pre></td></tr></table></figure><p>其中 <code>StopSignal</code> 机制（见 supervisorStopModule.go）要求连续收到 <code>stopThreshold</code> 个空区块消息后才判定实验结束。任何非空区块都会将计数器重置为 0。</p><h2 id="3-3逐步排查过程"><a href="#3-3逐步排查过程" class="headerlink" title="3.3逐步排查过程"></a>3.3逐步排查过程</h2><h3 id="3-3-1-排查一：supervisor-go-中的调试代码"><a href="#3-3-1-排查一：supervisor-go-中的调试代码" class="headerlink" title="3.3.1 排查一：supervisor.go 中的调试代码"></a>3.3.1 排查一：supervisor.go 中的调试代码</h3><p>在 supervisor.go 发现无限循环：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">add</span><br><span class="line"><span class="keyword">for</span> <span class="literal">true</span> &#123;</span><br><span class="line">time.Sleep(time.Second)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>程序在这里<strong>无限等待</strong>，即进入死循环，每秒 sleep 一次。<br>如果不注释掉注释，Supervisor 在这里会一直卡住，不会继续往下执行（不会发送 stop 信号，也不会关闭和写入结果文件）。</p><h3 id="3-3-2-排查二：MsgSendingControl-死循环（根因）"><a href="#3-3-2-排查二：MsgSendingControl-死循环（根因）" class="headerlink" title="3.3.2 排查二：MsgSendingControl() 死循环（根因）"></a>3.3.2 排查二：MsgSendingControl() 死循环（根因）</h3><p>启动脚本使用 <code>-m 4</code>，对应 <code>CommitteeMethod[4] = &quot;Broker_b2e&quot;</code>，实际加载的实现为 committee_brokerhub.go 中的 <code>BrokerhubCommitteeMod.MsgSendingControl()</code>。</p><p>对比能正常终止的 <code>BrokerCommitteeMod.MsgSendingControl()</code>（<code>-m 2</code>），后者从 CSV 文件读取交易，达到 <code>TotalDataSize</code> 后 <code>break</code> 退出。而 <code>BrokerhubCommitteeMod</code> 的实现包含<strong>两个无退出条件的死循环</strong>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 死循环 1：无限生成随机交易</span></span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        bcm.hubParams.currentEpoch++</span><br><span class="line">        txs := bcm.generateRandomTxs()</span><br><span class="line">        bcm.txSending(...)</span><br><span class="line">        time.Sleep(time.Second * <span class="number">2</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 死循环 2：无限处理用户请求队列</span></span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">    time.Sleep(time.Millisecond * <span class="number">100</span>)</span><br><span class="line">    <span class="comment">// ...处理队列...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由于 <code>MsgSendingControl()</code> 永远不返回，<code>SupervisorTxHandling()</code> 中后续的停止逻辑永远无法执行。值得注意的是，代码中已定义了 <code>simulation_param.endedEpoch = 86</code> 字段，且在其他方法中存在对该字段的判断（第 473、806 行），但 <code>MsgSendingControl()</code> 中<strong>未引用</strong>此退出条件。</p><p><img src="/pics/2026-2/varEndedEpoch.png"></p><p><strong>修复措施</strong>：在 <code>MsgSendingControl()</code> 中引入 <code>endedEpoch</code> 退出机制。核心改动如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">done := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        bcm.hubParams.currentEpoch++</span><br><span class="line">        <span class="keyword">if</span> bcm.hubParams.currentEpoch &gt; bcm.hubParams.endedEpoch &#123;</span><br><span class="line">            <span class="built_in">close</span>(done)</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// ...原有交易生成逻辑不变...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">    <span class="keyword">select</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> &lt;-done:</span><br><span class="line">        <span class="comment">// 处理剩余队列后退出</span></span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...原有队列处理逻辑不变...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过 channel 信号在两个循环间协调退出，确保 epoch 达到上限后 <code>MsgSendingControl()</code> 正常返回。</p><h2 id="3-4验证"><a href="#3-4验证" class="headerlink" title="3.4验证"></a>3.4验证</h2><p>修复后重新编译运行，程序在约 86 个 epoch（约 3 分钟）后自动终止，result 目录正常生成，包含 Supervisor 测量指标 CSV 与 Broker 余额&#x2F;收益数据。<br><img src="/pics/2026-2/brokerhubresult.png"><br><img src="/pics/2026-2/BrokerHubResultExcel.png"></p><h2 id="3-5结论"><a href="#3-5结论" class="headerlink" title="3.5结论"></a>3.5结论</h2><p>本次问题的直接原因是 <code>BrokerhubCommitteeMod.MsgSendingControl()</code> 缺少退出条件，属于功能性缺陷。排查过程中虽也发现了<strong>调试代码残留</strong>的问题并予以修正。修复思路是复用已有的 <code>endedEpoch</code> 字段，以最小改动实现循环退出，保持与原有架构的一致性。</p><h1 id="4-极端参数压力测试学习法"><a href="#4-极端参数压力测试学习法" class="headerlink" title="4. 极端参数压力测试学习法"></a>4. 极端参数压力测试学习法</h1><hr><p>对<code>global_config.go</code>中参数进行极端取值。来观察系统行为，便于理解复杂系统。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> params</span><br><span class="line"></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> (</span><br><span class="line"></span><br><span class="line">    Block_Interval      = <span class="number">1</span> <span class="comment">// generate new block interval (ms)</span></span><br><span class="line"></span><br><span class="line">    MaxBlockSize_global = <span class="number">1</span> <span class="comment">// the block contains the maximum number of transactions</span></span><br><span class="line"></span><br><span class="line">    InjectSpeed         = <span class="number">1</span> <span class="comment">// the transaction inject speed</span></span><br><span class="line"></span><br><span class="line">    TotalDataSize       = <span class="number">1</span> <span class="comment">// the total number of txs</span></span><br><span class="line"></span><br><span class="line">    BatchSize           = <span class="number">1</span> <span class="comment">// supervisor read a batch of txs then send them, it should be larger than inject speed</span></span><br><span class="line"></span><br><span class="line">    BrokerNum           = <span class="number">2</span></span><br><span class="line"></span><br><span class="line">    NodesInShard        = <span class="number">4</span></span><br><span class="line"></span><br><span class="line">    ShardNum            = <span class="number">3</span></span><br><span class="line"></span><br><span class="line">    IterNum_B2E         = <span class="number">5</span></span><br><span class="line"></span><br><span class="line">    Brokerage           = <span class="number">0.1</span></span><br><span class="line"></span><br><span class="line">    DataWrite_path      = <span class="string">&quot;./result/&quot;</span> <span class="comment">// measurement data result output path</span></span><br><span class="line"></span><br><span class="line">    LogWrite_path       = <span class="string">&quot;./log&quot;</span>     <span class="comment">// log output path</span></span><br><span class="line"></span><br><span class="line">    KeyWrite_path       = <span class="string">&quot;./key&quot;</span></span><br><span class="line"></span><br><span class="line">    SupervisorAddr      = <span class="string">&quot;127.0.0.1:18800&quot;</span> <span class="comment">//supervisor ip address</span></span><br><span class="line"></span><br><span class="line">    FileInput           = <span class="string">&quot;./TXs.csv&quot;</span>       <span class="comment">//the raw BlockTransaction data path</span></span><br><span class="line"></span><br><span class="line">    NodeID              <span class="type">uint64</span></span><br><span class="line"></span><br><span class="line">    ShardID             <span class="type">uint64</span></span><br><span class="line"></span><br><span class="line">)</span><br></pre></td></tr></table></figure><p><img src="/pics/2026-2/BrokerHubCycling.png"><br>终端输出循环不停。</p><h2 id="4-1StopSignal-停止机制"><a href="#4-1StopSignal-停止机制" class="headerlink" title="4.1StopSignal 停止机制"></a>4.1StopSignal 停止机制</h2><p>supervisorStopModule.go 中的 <code>StopSignal</code> 维护一个计数器 <code>stopGap</code>，判定逻辑如下：</p><ul><li>每当 Supervisor 收到一个 <code>BlockInfoMsg</code> 且 <code>BlockBodyLength == 0</code>（空块），<code>stopGap++</code></li><li>每当收到一个 <code>BlockBodyLength &gt; 0</code>（非空块），<code>stopGap</code> <strong>归零</strong></li><li>当 <code>stopGap &gt;= stopThreshold</code>（值为 <code>2 × ShardNum = 6</code>）时，判定实验结束</li></ul><p>这意味着：<strong>必须连续收到 6 个来自各分片的空区块上报，中间不能被任何一个非空区块打断</strong>。</p><h3 id="4-2区块的完整生命周期"><a href="#4-2区块的完整生命周期" class="headerlink" title="4.2区块的完整生命周期"></a>4.2区块的完整生命周期</h3><p>要理解 StopSignal 何时能被满足，需要追踪从交易注入到区块上报的完整链路：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Supervisor (MsgSendingControl)</span><br><span class="line">  ├── 每 2 秒生成一批随机交易</span><br><span class="line">  ├── dealTxByBroker() 拆分交易</span><br><span class="line">  │     ├── 同片交易 → 直接发送到对应分片</span><br><span class="line">  │     └── 跨片交易 → Broker 拆分为 Tx1 + Tx2（见下文）</span><br><span class="line">  └── txSending() → 按 InjectSpeed 批量注入各分片</span><br><span class="line">  </span><br><span class="line">各分片 PBFT 节点 (Propose 循环)</span><br><span class="line">  ├── 每隔 Block_Interval 毫秒触发一次</span><br><span class="line">  ├── GenerateBlock()</span><br><span class="line">  │     └── Txpool.PackTxs(50000) → 从交易池取出最多 50000 笔交易打包</span><br><span class="line">  ├── PBFT 三阶段共识（PrePrepare → Prepare → Commit）</span><br><span class="line">  └── HandleinCommit() → 区块上链 → 构造 BlockInfoMsg 发送给 Supervisor</span><br><span class="line">        └── BlockBodyLength = len(block.Body)</span><br></pre></td></tr></table></figure><h3 id="4-3Broker-机制产生的交易放大效应"><a href="#4-3Broker-机制产生的交易放大效应" class="headerlink" title="4.3Broker 机制产生的交易放大效应"></a>4.3Broker 机制产生的交易放大效应</h3><p>当 Supervisor 的 <code>dealTxByBroker()</code> 处理一笔跨分片交易时，会产生<strong>交易乘法效应</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">原始跨片交易 (Sender@Shard0 → Recipient@Shard1)</span><br><span class="line">     │</span><br><span class="line">     ├── Tx1: Sender → Broker（发到 Shard0）</span><br><span class="line">     ├── Tx2: Broker → Recipient（发到 Shard1）</span><br><span class="line">     └── AllocatedTx: Broker 余额调整交易（可能发到多个分片）</span><br></pre></td></tr></table></figure><p><strong>一笔原始交易至少衍生出 2-3 笔链上交易</strong>，而这些衍生交易的确认（<code>createConfirm</code>）又会在 <code>HandleBlockInfo</code> 回调中进一步触发 <code>handleBrokerType1Mes</code> → <code>txSending(tx1s)</code> 和 <code>handleBrokerType2Mes</code> → <code>txSending(tx2s)</code>，形成<strong>交易注入 → 上链 → 回调确认 → 再注入</strong>的闭环。</p><h3 id="4-4极端参数如何破坏停止条件"><a href="#4-4极端参数如何破坏停止条件" class="headerlink" title="4.4极端参数如何破坏停止条件"></a>4.4极端参数如何破坏停止条件</h3><p>当前参数为 <code>Block_Interval = 1</code>，<code>MaxBlockSize_global = 1</code>，<code>InjectSpeed = 1</code>。分析其所导致的时序关系：</p><h4 id="4-4-1-出块速率远超交易消化速率"><a href="#4-4-1-出块速率远超交易消化速率" class="headerlink" title="4.4.1 出块速率远超交易消化速率"></a>4.4.1 出块速率远超交易消化速率</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Block_Interval = 1ms → 每个分片每毫秒尝试出一个块</span><br><span class="line">3 个分片 → 系统每毫秒最多产生 3 个区块</span><br><span class="line">每个区块最多容纳 1 笔交易（MaxBlockSize_global = 1，但实际 PackTxs 硬编码为 50000）</span><br></pre></td></tr></table></figure><p>注意：虽然 <code>MaxBlockSize_global = 1</code>，但 <a href="chain/blockchain.go#L1129">blockchain.go 第 1129 行</a> 中 <code>PackTxs</code> 的参数被<strong>硬编码为 50000</strong>，因此 <code>MaxBlockSize_global</code> 这个参数实际上不起作用。每次出块时会把交易池中的所有交易一次性打包。</p><h4 id="4-4-2-Broker-衍生交易形成持续注入流"><a href="#4-4-2-Broker-衍生交易形成持续注入流" class="headerlink" title="4.4.2 Broker 衍生交易形成持续注入流"></a>4.4.2 Broker 衍生交易形成持续注入流</h4><p>每个 epoch（每 2 秒），Supervisor 生成一批随机交易。其中跨片交易经 Broker 拆分后，产生 Tx1、Tx2、AllocatedTx 注入到不同分片。这些交易上链后，Supervisor 在 <code>HandleBlockInfo</code> 中收到区块上报，触发 <code>createConfirm</code> 回调，回调中又会通过 <code>txSending</code> 向分片注入新的交易（Tx2 的确认触发等）。</p><p>时序如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">t=0s    ：epoch 1 注入原始交易 → 衍生 Tx1、Tx2 注入各分片</span><br><span class="line">t=1ms   ：Shard0 出块，打包 Tx1 → 上报非空块 → stopGap 归零</span><br><span class="line">t=2ms   ：Shard1 出块，打包 Tx2 → 上报非空块 → stopGap 归零</span><br><span class="line">t=3ms   ：Shard0 出块，可能为空 → stopGap = 1</span><br><span class="line">t=4ms   ：Supervisor HandleBlockInfo 回调，生成确认交易 → 注入 Shard1</span><br><span class="line">t=5ms   ：Shard1 出块，打包确认交易 → 上报非空块 → stopGap 归零！</span><br><span class="line">t=2000ms：epoch 2 注入新一批交易 → 整个过程重复</span><br></pre></td></tr></table></figure><p><strong>关键问题</strong>：由于出块间隔仅 1ms，系统在 epoch 间隙期间并非空闲——之前 epoch 的衍生交易的确认回调会不断产生新交易，这些交易以极高频率（1ms 一个块）被打包上报。3 个分片交替上报非空块，使得 <code>stopGap</code> 计数器在到达 6 之前就被频繁重置。</p><h4 id="4-4-3-三分片交替出块的竞态效应"><a href="#4-4-3-三分片交替出块的竞态效应" class="headerlink" title="4.4.3 三分片交替出块的竞态效应"></a>4.4.3 三分片交替出块的竞态效应</h4><p>3 个分片独立出块，各自的出块时机不同步。即使某个分片的交易池已空，其他分片可能仍有衍生交易在处理：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">时间线：  --|------|------|------|------|------|---&gt;</span><br><span class="line">Shard0：  空   空   非空  空   空   空</span><br><span class="line">Shard1：  空   非空  空   空   非空  空    ← Broker 确认交易到达</span><br><span class="line">Shard2：  非空  空   空   非空  空   空    ← AllocatedTx 到达</span><br><span class="line">stopGap： 1    0!   0!   0!   1    0!    ← 永远无法累积到 6</span><br></pre></td></tr></table></figure><p>三个分片的非空块在时间轴上<strong>交错出现</strong>，使得连续空块窗口始终无法维持到 6 个。</p><h2 id="4-5合理参数为何能正常停止"><a href="#4-5合理参数为何能正常停止" class="headerlink" title="4.5合理参数为何能正常停止"></a>4.5合理参数为何能正常停止</h2><p>当 <code>Block_Interval = 1000</code>（1 秒）时：</p><ul><li>每个分片每秒只出一个块，给系统充足时间消化所有衍生交易</li><li>一个 epoch 的所有交易（原始 + 衍生 + 确认）在几个块内就能全部处理完</li><li>epoch 间隙（2 秒）内足够出 2 轮空块，3 个分片各出 2 个空块 &#x3D; 6 个空块上报</li><li><code>stopGap</code> 能够顺利累积到阈值 6，触发正常关闭</li></ul><h2 id="4-6结论"><a href="#4-6结论" class="headerlink" title="4.6结论"></a>4.6结论</h2><p>StopSignal 的设计前提是：<strong>交易注入有限、出块间隔合理、衍生交易能在若干轮出块内被完全消化</strong>。极端参数（1ms 出块）打破了这一前提——Broker 的交易放大效应 + 确认回调的循环注入 + 多分片交替出块，使得系统中始终存在零星的非空块，<code>stopGap</code> 计数器永远无法连续累积到阈值。</p><p>三个分片的非空块在时间轴上<strong>交错出现</strong>，使得连续空块窗口始终无法维持到 6 个。</p><h2 id="4-7合理参数为何能正常停止"><a href="#4-7合理参数为何能正常停止" class="headerlink" title="4.7合理参数为何能正常停止"></a>4.7合理参数为何能正常停止</h2><p>当 <code>Block_Interval = 1000</code>（1 秒）时：</p><ul><li>每个分片每秒只出一个块，给系统充足时间消化所有衍生交易</li><li>一个 epoch 的所有交易（原始 + 衍生 + 确认）在几个块内就能全部处理完</li><li>epoch 间隙（2 秒）内足够出 2 轮空块，3 个分片各出 2 个空块 &#x3D; 6 个空块上报</li><li><code>stopGap</code> 能够顺利累积到阈值 6，触发正常关闭</li></ul><h2 id="4-8结论"><a href="#4-8结论" class="headerlink" title="4.8结论"></a>4.8结论</h2><p>StopSignal 的设计前提是：<strong>交易注入有限、出块间隔合理、衍生交易能在若干轮出块内被完全消化</strong>。极端参数（1ms 出块）打破了这一前提——Broker 的交易放大效应 + 确认回调的循环注入 + 多分片交替出块，使得系统中始终存在零星的非空块，<code>stopGap</code> 计数器永远无法连续累积到阈值。</p><hr>]]></content>
      
      
      <categories>
          
          <category> Lab </category>
          
      </categories>
      
      
        <tags>
            
            <tag> BlockChain </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
